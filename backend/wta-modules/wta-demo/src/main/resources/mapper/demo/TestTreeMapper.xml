<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.namewta.demo.mapper.TestTreeMapper">

    <select id="selectStructure" resultType="org.namewta.demo.domain.TestTree">
        select id, parent_id from test_tree
        where id = #{id} and del_flag = 0
        <if test="locking">for update</if>
    </select>

    <select id="lockTreeRoots" resultType="org.namewta.demo.domain.TestTree">
        select id, parent_id from test_tree force index (PRIMARY)
        where del_flag = 0 and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
        order by id for update
    </select>

    <select id="selectVisibleForUpdate" resultType="org.namewta.demo.domain.TestTree">
        select id, parent_id, dept_id, user_id from test_tree
        where id = #{id} and del_flag = 0 for update
    </select>

    <select id="selectChildIdsForUpdate" resultType="java.lang.Long">
        select id from test_tree force index (idx_test_tree_parent_id)
        where del_flag = 0 and parent_id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
        order by id for update
    </select>
</mapper>
