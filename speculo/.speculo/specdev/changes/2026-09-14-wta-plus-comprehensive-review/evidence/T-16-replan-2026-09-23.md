# T-16 evidence

HEAD `b5cd3db5` closes AC-016. The workflow task service and the task dialog already enforced the boundaries. The default Playwright config ignores `workflow-task-integrity.spec.ts`, so this candidate adds a Vite fixture server and a dedicated Playwright config. No production workflow code changed. AC-016 stays in the T-30 combined run.

## Real engine

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=WorkflowTaskEngineIntegrationTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Dworkflow.engine.integration=true \
  -Dworkflow.mysql.integration.url='jdbc:mysql://127.0.0.1:33123/namewta_workflow_test_t16?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai' \
  -Dprofile.redis.integration.port=33124
```

Exit 0 on parent `410de23f`. The following commit only adds the browser runner. Isolated MySQL 8.4.9 database `namewta_workflow_test_t16` loaded the workflow table definitions, and Redis 8.6.3 was removed after the run. `WorkflowTaskEngineIntegrationTest`: tests=1, failures=0, errors=0, skipped=0. It drove Warm-Flow, MyBatis, and the shipped task service. A stranger received HTTP 403 for the next node and the instance variables. The assignee and the variable-query permission could read. A second completion of the same task did not add history, and a held lock did not advance the node.

## Browser task switch

```bash
cd frontend && pnpm exec playwright test --config playwright.task-integrity.config.ts
```

Exit 0 on `b5cd3db5`. 10 passed, 0 skipped, 15.7s. Loading B after A fails does not submit A. A late A response cannot replace B. Confirmation for submit, terminate, or return does not write after an A/B switch. Closing or unmounting drops the outstanding confirmation. Two clicks make one request. A late success for A does not close B.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase. The runner files were outside the original ticket write set and are recorded on the ticket.
