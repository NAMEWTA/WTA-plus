# T-01 / AC-001 current contract review (read-only)

- Fixed current source: `9a8140ff5c8dce4fcf05a67972bd8ec0d750fc3f` (clean worktree when inspected).
- Historical implementation: `3aa047b85033afd7588923ffa7b37481dc2d1645`. At that commit `.github/workflows/quality-gates.yml` did not exist; it is a later committed addition. Current behavior is assessed at the fixed HEAD, not projected from the historical evidence.
- Sources: `spec.md:64`, `ticket/01-restore-trustworthy-gates.md:79-109`, `evidence/T-01.md:23-56`, current workflow, named scripts/tests, project profile and migration decision. Scope is AC-001; no gate, service, build, test, remote CI or branch protection was run or queried in this review.
- Verdict: **no static product/contract defect found in AC-001**. The current implementation supplies the required candidate commands and truthful candidate wording. This is *not* a runtime pass or a remote required-check assertion. Lead still needs current-command exit codes and candidate evidence before marking the ticket complete.

## Clause-by-clause assessment

| AC-001 clause | Fixed-source evidence and assessment |
|---|---|
| Clean clone works without `temp/release` | `.agents/skills/engineering-standards/scripts/validate-skill-facts.mjs:60-69` checks ignore protection with `git check-ignore --no-index`, checks tracked files with `git ls-files`, and rejects obsolete `temp/relase`; it does **not** require `temp/release` to exist. `validate-skill-facts.test.mjs:12-31` constructs a fresh Git repository without `temp` and asserts success and continued absence; lines 33-84 cover unprotected, force-tracked, obsolete path, and missing directory creator. The checker does not require the new directory; Lead’s root-facts exit-0 evidence, rather than this private-directory review, establishes the current migration state. Static pass for the contract; current execution belongs in Lead evidence. |
| Javadoc/comment `DSTransactional` does not trigger layer violation, real illegal import fails | `validate-module-mode.mjs` removes comments/string/text-block content before matching source imports/annotations; matching is at `:269-299`. `validate-module-mode.test.mjs:46-64` explicitly exercises Javadoc, line comments, literals/text blocks; `:66-120` exercises forbidden imports, static imports, real annotations and missing actual chain edges. No new exception/allowlist was introduced to hide a real violation. Static pass. |
| CI file and candidate/required truth | Current `.github/workflows/quality-gates.yml:1-105` exists with PR/push-main/manual triggers, read-only contents permission, and serial `release-contracts -> frontend -> backend -> external-services` dependencies. It was absent at historical implementation commit, hence historical evidence cannot prove its runtime. `scripts/README.md:230-239`, project profile `00-project-profile.md:24,40-57,75`, and `02-decisions-and-exceptions.md:104,130` call it a **candidate**, explicitly say no post-push remote success/branch-protection evidence, and reserve required-check selection. No active/required remote claim found in those authoritative documents. Static pass; remote status remains unknown. |
| Declared commands resolve in their own cwd | `release-contracts` defaults to repository root. Workflow `:30-35` contains Node invocations targeting existing entry files: skill facts, handbook checker and its test, three checker/SpecDev test files, fullstack skill validator, and FM validator; each path exists in fixed tree and uses Node built-ins/local files. `:38-40` invokes the same existing layered checker on all five existing directories: profile-person, profile-enterprise, notify, sso, third. The loop is inside an Actions `run` step, so shell nonzero status stops that step under Actions' default Linux `-e` runner behavior; the final iteration is not a substitute for checking earlier failures. `:41-44` invokes existing Bash scripts; `verify-release.sh:11-39` itself calls Node and release tests and conditionally Compose. `frontend` sets `working-directory: frontend` at workflow `:49-51`; `frontend/package.json:13-31` supplies every called script, tooling-openapi has `openapi:check`, package manager `pnpm@10.34.5` is declared at `:66`, and lockfile exists. Node 24 setup and Playwright Chromium install precede E2E. `frontend/playwright.config.ts:23-28` starts its own built Admin preview at `127.0.0.1:4173`; default suite mocks product API (e.g. `system-identity.spec.ts:35-37`) and therefore does not require a running backend. `backend` overrides cwd to `backend` for each `./mvnw` invocation (`quality-gates.yml:86-92`); wrapper and POM exist. Bundle verification and external-service Bash scripts run from repository root (`:90,93,105`) and locate that root with Git. Static path/cwd/declared-dependency pass. |
| Frontend/Maven test versus package evidence | Workflow `:64-74` performs install, architecture check/test, OpenAPI, lint, typecheck, unit tests, `build:dev`, `build:prod`, browser dependency install and Admin Playwright as distinct steps; a build is not counted as a test. Workflow `:86-93` runs `./mvnw test` first, `clean package -DskipTests` then full bundle inspection, a second `clean package -Pbundle-core -Dmaven.test.skip=true` then core bundle inspection. The `clean` separates mutually different bundles. `verify-admin-bundle.sh:25-59` asserts required/optional JAR membership; `backend/pom.xml:78` has normal test default. Workflow `:95-105` separately invokes real Redis/MySQL/MinIO selection; `run-external-services.sh:105-135` names selected integration tests and then checks Surefire reports. This is proper separation in the candidate. Static pass; actual current exit codes and skip counts still must be recorded separately. |

## Important evidence limits and scope

`evidence/check-t01-ci.py:24-43` only feeds workflow shell text to `bash -n` and statically checks some `pnpm`, `mvnw`, and `bash` paths. It does **not** execute Node entries, expand the five module values, install dependencies, start Playwright, or prove any GitHub Actions job can run. The table above uses direct fixed-source inspection for those points; Lead's actual gate logs are still required. The historical `evidence/T-01.md:25-56` records local checks but predates the current workflow and current tree; it should remain historical rather than being copied into current exit-code evidence.

The default Playwright config deliberately excludes dedicated SSO/HTTPS/real-service selections (`frontend/playwright.config.ts:7`) and a live Nacos case skips without its external URL (`frontend/e2e/nacos-console.spec.ts:78-82`). `scripts/README.md:239` already says the default Admin suite cannot establish SSO acceptance. These are explicit scope boundaries, not AC-001 defects; required real scenarios belong to their own tickets and the all-AC candidate. `release-artifacts/scripts/verify-release.sh:30-37` skips Compose parsing if Docker Compose is unavailable, whereas the downstream `external-services` job requires Docker and will fail without it. Thus the four-job chain is not evidenced as green merely because the release-contract step passes.

No remote CI/branch-protection read was authorized. Do not label this review `required checks passed`; retain `MIG-CI` as candidate until an authorized push produces a run and branch-protection evidence. No missing historical clean exact-HEAD artifact is inferred or recreated; its absence is a historical provenance limit, not a reason to cancel the current AC-001 verification.
