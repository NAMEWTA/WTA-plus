# T-38 candidate B — fixed incremental functional/engineering review

**Verdict: pass for the A→B generated-contract increment; no blocking finding.** This is a read-only comparison of fixed commits and prior immutable capture evidence, not a fresh build or service run. Whole-ticket completion still depends on Lead's B-source real retry/Wake replay and remaining frontend/core gates.

- Source A: `7a6f75ac9238399daf7936797d07da141f0f5a03` (backend full-JAR source, clean tree `3c519c0cad48d11ec1331a92884332a0ca230f4b`)
- Candidate B: `a3b289efbaf5862e55a40a6c58a6ad66c79b40b2`
- Fixed command: `git diff 7a6f75ac9238399daf7936797d07da141f0f5a03...a3b289efbaf5862e55a40a6c58a6ad66c79b40b2`. Exactly five files changed, all under `frontend/packages/api-contracts/`: `README.md`, `generated/openapi.ts`, `openapi/current.json`, and the new revision's `provenance.json`/`source.json`. Backend, release SQL and engineering standards have no A→B diff. The source-A functional review is `/tmp/wta-t38/source-a-functional-review.md` and is inherited only for unchanged product paths.

## Immutable live-source chain

`/tmp/wta-t38/openapi-live/26d29e5a680cf899/result.json` records v2 direct HTTP 200 `application/json` from owned `/v3/api-docs`, 508,562 bytes, SHA-256 `8f57fc94ab0936c48d57b5947879ea0e7280a6b559dc4895852a9670ae97b3c7`. I compared the new revision's `source.json` to that private raw capture **byte for byte**; they are identical. Its `provenance.json` records the same raw hash, source-A backend commit, repository `NAMEWTA/WTA-plus`, runtime endpoint `/v3/api-docs`, OpenAPI `3.1.0`, generator `openapi-typescript@7.13.0`, and totals 436 paths/445 schemas. `current.json` points to that immutable revision `5f2afc58f2a31e4c172fbf12076d43f1aba8fbfbba6e71079701bc335c976302`.

The v2 result binds clean source A/tree **before and after** the capture, full-JAR SHA-256 `01678e64ede453a387d352b7961cb97ee382bd945ed2a2935851f46ee52c1ef4` and same JAR after, package-proof digest `d522fee9c6ceccc2c48cd3fa1ef159e6992099a06ed03713ae7b867de4f8101d`, full bundle module check, exact six SQL baselines and successful fresh initialization. It reports `acceptance=true` and no cleanup errors: owned process group empty, dual-labelled full-ID containers gone, two captured anonymous volumes absent and four loopback ports closed. The earlier v1 run `/tmp/wta-t38/openapi-live/b6b7be1d9ef06cea/result.json` failed because its owned Redis lacked authentication; it remains failure history and is not reused as source. **No full JAR was built from B**: B only commits the contract output generated from the verified A JAR.

## Contract delta checked against actual source

The old active full snapshot had 433 paths/444 schemas. The new raw document has 436/445. Structural comparison found **zero removed paths, HTTP operations or schemas**. The only added paths are `POST /resource/oss/{ossId}/publish`, `POST /resource/oss/{ossId}/unpublish`, and `POST /resource/oss/{ossIds}/restore`; the only added schema is `PublishRequest` with required nonblank `targetConfigKey`. These map to preexisting `SysOssController` methods, so their appearance is a correction of the older stale snapshot, not a new B backend behavior. The existing `RetryReceipt` adds `queuedCount` as OpenAPI integer/int32, and `generated/openapi.ts` contains it as a numeric field. `DeliveryReceipt.providerMessageId` and `NotificationRetryCommand.idempotencyKey` change descriptions only; `SysOssVo` gains `accessPolicy`/`restorable` from earlier backend work. The `README.md` totals/source statement matches provenance and the raw capture.

Two generated metadata shifts are visible and do not block T-38: the new OSS publish operation takes `publish_1`, moving notice publish to `publish_2`; repository search found no handwritten consumer of either generated operation ID. The existing OSS delete operation retains its path, POST method, permission and `remove_18` ID, but its summary is blank in the live source because the current `SysOssController` deletion method has adjacent Javadoc blocks. That is a source-code documentation cleanup for its owner, not a reason to edit generated JSON by hand. Neither shift loses a route, schema or T-38 retry/cancel response contract.

## Verification boundary

Source-A owned real tests were 13 manual retry + 1 Wake, zero fail/error/skip, with clean source/resource cleanup. Lead's default full backend suite on source A had 251 fresh classes, 1,017 total cases, 854 executed passes and 163 explicit environment skips; B changes no backend or SQL bytes, so those inputs are identical, but this report does not call them newly run on B. Lead's B-source real 14-case replay and frontend/core gates were pending when this review finished. I did not run Maven, Docker, JVM, HTTP, `openapi:check`, frontend tests or builds. All generated-contract claims above come from fixed files and the prior recorded direct HTTP capture.
