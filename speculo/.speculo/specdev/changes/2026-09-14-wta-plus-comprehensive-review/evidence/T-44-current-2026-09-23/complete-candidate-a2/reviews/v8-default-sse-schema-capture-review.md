# T44 private v8 OpenAPI capture supplement

- Frozen source: `/tmp/wta-t44/run-minio-real-v7.py`, SHA-256 `ed3f0f8600728c469e8a6cca412a9f1fc1d8e0a38b04e7c05b82b06fa507b98c`. Its owned A2 run `570b67d1325ff08b` was accepted with 18 log canaries at zero hits. That run's raw OpenAPI used `message.enabled=false`, so it is not the default SSE HTTP contract capture.
- New private driver: `/tmp/wta-t44/run-minio-real-v8.py`, SHA-256 `d20856e951bf308ea558cee6bc79ff7b5593beff4e89bd2b044be4f4ffcdb65e`. The only overlay change is `message.enabled=true`; repository `application.yml` defaults `message.transport=sse`. At actual `/v3/api-docs` capture, v8 requires `/resource/oss/config/diagnose/{ossConfigId}`, `/resource/message`, and `/resource/message/close`. `schema_capture` records the enabled flag, default transport, expected paths, and observed paths even if this assertion fails. A missing path is a fixed failure, not a passing capture.
- Relative to v7, the remaining edit adds the fixed failure reason to its existing exception allowlist. Business scenarios, auth, resource ownership/cleanup, count gates, and all 18 log canaries are byte-for-byte unchanged. v7 is unchanged.
- Offline checks: `python3 -m py_compile /tmp/wta-t44/run-minio-real-v8.py` exit 0; `python3 /tmp/wta-t44/run-minio-real-v8.py --preflight` exit 0 and reported `static-only`, `services_started=false`, four input checks true. An exact line diff found only the three intended hunks. No Maven, service, or browser was run for v8.
- v8 live acceptance and the three OpenAPI path assertions remain pending Lead's owned run. Do not replace v7's accepted functional/security evidence or call the default SSE schema captured until v8 succeeds.
