# T-23 evidence

Current product source already meets AC-023. No new implementation was required. This cancels only the repeated construction. AC-023 stays owned by T-30. The adjacent OIDC change was not modified.

The callback cases live in `NotifyAtomicResultIntegrationTest`, which ran on `ea124ded`. The following docs commit `449376aa` does not change product code. The same run is tests=66, failures=0, errors=0, skipped=0. MySQL `mysql:8.4.9` and Redis `redis:8.6.3` were isolated on `127.0.0.1` and removed afterwards. The credential was the test's environment variable and is not recorded. Command and schema load are in `evidence/T-22-replan-2026-09-23.md`.

Methods in that run that close AC-023: `callbackRollbackMustAllowSameEventRetry` retries the same event after a rollback; `twoInstancesRaceOnTheSameEventAndOnlyRefreshOnce` lets two instances race one event; `sameEventFromDifferentAccountsMustNotCollide` and `sameAccountKeyAndEventAcrossChannelsRemainIndependent` keep different accounts or channels apart; `earlyCallbackDoesNotConsumeReceiptAndCanRetryAfterDispatchResult` does not consume an early callback; `duplicateEventWithChangedFactsMustBeRejected` rejects a changed duplicate; `realHttpCallbackVerifiesRawBodyAndReturnsRetryableStatus` checks the signed raw body; `callbackDatabaseFailureRemainsRetryableWithTheProductionGlobalExceptionHandler` stays retryable; `receiptInsertFailureAndStatusCasMissBothRollbackAndAllowRetry` rolls back a receipt failure; `restartedJvmsSeeCommittedReceiptAndRejectChangedFacts` keeps the committed receipt across a new JVM; `commitDisconnectOrLostAcknowledgementCanRetryWithoutLosingTheEvent` retries a lost commit acknowledgement without dropping the event.

## Notify module regression

```bash
cd backend && ./mvnw -pl wta-modules/wta-notify -am test
```

Exit 0 on `449376aa`. `wta-notify` tests=117, failures=0, errors=0, skipped=0. Upstream `wta-common-redis` reported tests=2, skipped=2 in `RedisUtilsDeadlineIntegrationTest`, and `RepeatSubmitRedisIntegrationTest` executed 0. Those are property-gated Redis checks, not the callback contract, and they are not counted as passes.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase, operation `verify-affected-contract-and-quality-gates`, status passed. No production source edit.
