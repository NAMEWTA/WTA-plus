# T-11 evidence

Current HEAD `408f12d1` already meets AC-011. No new implementation was required. This cancels only the repeated construction. AC-011 stays owned by T-30. The product tree is the same as `a139dbc5`; later commits are review records only.

## Production bundles

`cd frontend && pnpm build:prod` exited 0. admin-web, home-web, and sso-web `build-mode.json` are `production`, and each `index.html` loads `/assets/`. A scan of `frontend/apps/admin-web/dist` and `frontend/apps/home-web/dist` found none of `AES/ECB`, `JSEncrypt`, `BEGIN PRIVATE KEY`, or `encrypt-key`.

## Browser journey

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=BrowserHttpsTransportIntegrationTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Dbrowser.transport.integration=true \
  -Dnamewta.repo.root=/srv/WTA-plus
```

Exit 0. `BrowserHttpsTransportIntegrationTest`: tests=1, failures=0, errors=0, skipped=0. Playwright: 6 passed, 0 skipped, 8.4s. The test printed that production App HTTPS, MVC JSON binding, registration, password policy, and redacted audit passed. Admin and home registered and logged in over ordinary JSON. Login failure did not create a session. The download kept the binary bytes and showed the JSON download failure. Unknown Client and malformed registration were rejected. Request and response `encrypt-key` / `isEncrypt` headers were absent, and the audit text did not contain the fixture password or tokens. User persistence, password grant, token minting, and the export bytes are fixtures; this does not claim a production database transaction.

`@namewta/adapter-axios-browser` tests: 46 passed, 0 skipped. They reject an `encrypt-key` request header and return JSON, text, and binary bodies without an application encryption layer.

`wta-common-encrypt` has no test sources. OpenAPI HMAC signing and OSS presign remain in the product tree; the default suite on `a139dbc5` executed those areas with the same production sources. AES use that remains is `AES/GCM/NoPadding`, not ECB. There is no second browser transport left to cut over.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase. No production source edit.
