# Local-only overrides for WTA-plus box E2E. Do not commit secrets.
server:
  port: 18080

spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    dynamic:
      primary: master
      strict: true
      datasource:
        master:
          type: ${spring.datasource.type}
          driverClassName: com.mysql.cj.jdbc.Driver
          url: "jdbc:mysql://172.16.105.9:3306/wta-plus?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8&autoReconnect=true&rewriteBatchedStatements=true&allowPublicKeyRetrieval=true&nullCatalogMeansCurrent=true"
          username: "namewta_app"
          password: "7jREnQfF4apSrXrZR3XUzit7uwDFJ5h4"
      hikari:
        maxPoolSize: 20
        minIdle: 10
        connectionTimeout: 30000
        validationTimeout: 5000
        idleTimeout: 600000
        maxLifetime: 1800000
        keepaliveTime: 30000
  data:
    redis:
      host: "172.16.105.9"
      port: 16379
      database: 1
      password: "piV2N8Q33HXh2ANLsgTMGYyK4S7RrDvS"
      timeout: 10s
      ssl:
        enabled: false

spring.boot.admin.client:
  enabled: false

snail-job:
  enabled: false
  group: "wta_group"
  token: "SJ_cKqBTPzCsWA3VyuCfFoccmuIEGXjr5KT"
  server:
    host: 127.0.0.1
    port: 17888
  namespace: local
  port: 2${server.port}

snail-ai:
  enabled: false

oss:
  readiness:
    diagnostic-objects:
      minio: .well-known/oss-readiness/private-canary.txt

captcha:
  enable: false

namewta:
  sso:
    enabled: true
    web-origin: http://127.0.0.1:4176
    cookie-name: Sso-Token

# redisson 配置
redisson:
  # redis key前缀
  keyPrefix: WTA
