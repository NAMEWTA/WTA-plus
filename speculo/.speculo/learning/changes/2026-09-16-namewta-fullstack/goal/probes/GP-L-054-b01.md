---
artifact: learning-goal-probe
probe_id: GP-L-054-b01
lesson_id: L-054
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-054 / b01

审问对象是已写成的 Lesson `lessons/L-054-notify-web-domain.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **一格**：`A:createNotifyWebDomain`。OBJ-54 原文：能说明 `createNotifyWebDomain` **公告/收件箱/配置/监控页**。档是**解释+走查**。矩阵 (a) **没有** Vue 页符号，也没有 `createNotifyWebDomain` 独行；符号落在 **九工厂同行** `createAdminWebDomain, createSystemWebDomain, createProfileWebDomain, createNotifyWebDomain, createThirdWebDomain, createWorkflowWebDomain, createDemoWebDomain, createAiWebDomain, createProfileSelfWebDomain`（模块 `web-domains/*`，仍 uncovered，备注「manifest 工厂」）。矩阵 (a) 厨房独行 `createNotificationService, notificationDirectory.searchUsers, usersByIds` **仍 uncovered**（L-053 已 written，备注空）。矩阵 (b) **没有** runtime 校验 / 页面扣扳机 / 四键注册行；通知 (b) 已 covered 的是公告 publish/retract、应用 submit/retry/cancel、Outbox claim、供应商 callback。矩阵 (c) `admin-web` / `home-web` 容器与 `frontend-direction` / `admin-web-composition` 已 covered（L-004 / L-007 / L-008）。矩阵 (d) **没有** 浏览器通知菜单流；已 covered 的「主路径 通知提交」原文仍是 `NotificationController.submit → Outbox → claim → 渠道`（L-051/L-052）。本批按派单只审问这一格 (a) 切片；口试按磁盘：工厂 id 是 `web-domain-notify`，`domainId` `'notify'`，本课认 **四键一次冻住 + 四页怎么喊 `runtime.service` + 厨房剩枪无 Vue + 顶栏旁路不是第五键**，不把九工厂行整行闭合，不把厨房独行提前盖章，也不把 L-024 系统监控四页或 L-051 应用四枪算进本切片。`createNotificationService` / `notificationDirectory` 留给 L-053；厅堂插头留给 L-007；导航 host / 九工厂行警告留给 L-020；系统监控四页留给 L-024；公告/收件箱/配置/收件人/监控/回调/应用/Outbox 八扇 Java 留给 L-045…L-052。

抽查锚点（2026-09-17）：`frontend/packages/web-domains/notify/src/{index,index.test,runtime}.ts`；`NoticePage.vue` / `notice/{RecipientUserPicker.vue,useRecipientSelection.ts,useRecipientSelection.test.ts}`；`InboxPage.vue`；`ConfigPage.vue`；`NotificationPage.vue`；`{monitor,notice,inbox,config}/index.ts`；包 `package.json` `exports` 与 `AGENTS.md`；`frontend/packages/domains/notify/src/{index,transport,notification/index,callback/index,monitor/index}.ts`；`apps/admin-web/src/application/services.ts` 第 70–88 行；`router/adminManifestRegistry.ts` 第 291–376 行与 `.test.ts` 第 116–118 行；`layout/components/notice/index.vue`；`utils/push.ts`；`apps/home-web/src/router/homeManifestRegistry.ts` 第 39 行；`50-cde-base-dml.sql` 通知中心块 + 补偿块 + 末尾 DELETE。

## Q1 澄清

- objective_id: OBJ-54
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-54；chain L-054 覆盖列只 `A:createNotifyWebDomain`；L-054 `claimed_cells` 一格；矩阵 (a) 九工厂同行仍 uncovered、无 Vue 行、无本工厂独行；厨房独行仍 uncovered（L-053）；(b) 无本课符号；(c) 两厅堂已 covered；(d) 无浏览器通知菜单流，已 covered 的主路径是 submit→Outbox；课开篇「只给 createNotifyWebDomain 这一颗」+ 负空间 L-053 / L-007 / L-020 / L-024 / L-045…L-052
- difficulty: medium

课把浏览器通知这一头收成「管理端墙上的四行点菜单，怎么把同一座厨房喂进四张页」。计划里至少有六套可数对象，派单点名 **一格 (a) 切片**：

1. **OBJ-54 原文。** `course.md`：能说明 `createNotifyWebDomain` 公告/收件箱/配置/监控页。一句里是 **菜单工厂 + 四页消费**，不是 `createNotificationService`、不是八扇 Java 窗、也没有把九个 `create*WebDomain` 写成口试清单。
2. **chain / 课 `claimed_cells`。** 一格。符号就是 `createNotifyWebDomain`。磁盘上 **没有** `createNotifyMonitorWebDomain` 四个包。管理端工厂 id `web-domain-notify`，四键一次冻住。chain **不**写 Vue 文件名，也 **不**写顶栏 `layout/components/notice`。
3. **矩阵 (a) 两处不对齐。** 厨房独行仍 uncovered，本课不得当第二张厨房章。九工厂行把本课切片和 `createSystemWebDomain` 等邻居绑死。Lead 若按**现印同行**标 covered，会把 L-020 / 未写的 third/workflow/demo/ai 菜单假闭合。矩阵 **没有** 四张 Vue 符号。
4. **课自己的操作定义。** 本课认：工厂冻四键 `notify/{monitor,notice,inbox,config}/index`；`h(page, { runtime })` 把厅堂 `notificationService` 塞进页面；公告走 `notices` 六枪 + `directory` 选人；收件箱走 list/read；配置走账号/场景/试发（短信无 textarea）；监控只 `deliveries`。厨房里 `snapshot` / `inbox.seen` / `config.account(id)` **有对象、无 Vue 扳机**。应用 submit 四枪连厨房字段都没有。顶栏 list/read/readAll 是旁路，不是第五键。工厂 **不** `requireNotifyWebRuntime`。
5. **顶栏不是第二张 claimed 格。** OBJ 要「页面如何消费」。课把消息盒子的直接 import 收进对照走查，并写明不是本工厂的键。不要为了盖章把 `push.ts` 再立一行，也不要把顶栏降成「纯邻居、本课不认调用」。
6. **邻格。** L-053 厨房 URL 与 directory 三口。L-045…L-052 八扇 Java。L-007 厅堂插头。L-020 九工厂行警告。L-024 系统监控四页（另一工厂 `createMonitorWebDomain`，九工厂行里甚至没有这个名字）。L-051 应用四枪。

问：本格闭合标准是「四键 + 不校验托盘 + 四页实际扣的 `runtime.service.*` / `directory` + 厨房剩枪无 Vue + 顶栏旁路，且不碰九工厂整行、不升 Vue 矩阵行、不提前盖厨房章」，还是 OBJ 那句必须把四张 Vue 升成矩阵行、或必须先把九工厂行拆开、或必须等 GP-L-053 才给本切片盖章？Lead 若凭「`createNotifyWebDomain` 四字在函数表里」把九工厂行标 covered，是否假闭合了另外八个工厂？

## Q2 证据

- objective_id: OBJ-54
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: web-domain `index.ts` 6–82；`index.test.ts` 8–63；`runtime.ts` 12–28；`NoticePage.vue` 7 / 43–74 / 141 / 168–171 / 192 / 196–225 / 272–273 / 283 / 302 / 330 / 362–365 / 387–388；`RecipientUserPicker.vue` 71；`useRecipientSelection.ts` 35–50 / 58–68 / 88–91；`useRecipientSelection.test.ts` 27–36 / 40–54；`InboxPage.vue` 10 / 22 / 85 / 99–100 / 115–123；`ConfigPage.vue` 8–18 / 32 / 38–46 / 195–223 / 310 / 323 / 337–351 / 366 / 377 / 411–422 / 452–460；`NotificationPage.vue` 42 / 81–88；子路径 re-export 各 1 行；`AGENTS.md` 3；包 `package.json` 6–8；domains `transport.ts` 22–70；`index.ts` 3–8；`notification/index.ts` 3–6；`callback/index.ts` 2–5；`monitor/index.ts` 2–6；admin `services.ts` 70–88；`adminManifestRegistry.ts` 291–376；registry 测试 116–118；`layout/components/notice/index.vue` 54 / 100 / 114；`push.ts` 21–23 / 107；home `homeManifestRegistry.ts` 39；DML 503–515 / 1526–1570 / 1611–1618
- difficulty: high

课的四键菜单 + 页面消费链哪些能在工作树直接复现，哪些只是测试名单比课表窄、或口试把「SQL 里写过」说成「init 后还活着」？

**仍能复现（本切片）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂 `createNotifyWebDomain`，id `web-domain-notify`，`domainId` `'notify'`，空 `messages` | `index.ts` 6–10 |
| 四键顺序：`notify/{monitor,notice,inbox,config}/index` → `NotificationMonitor` / `NotifyNotice` / `NotifyInbox` / `NotifyConfig` | `index.ts` 44–80；`index.test.ts` 21–26 |
| `h(page, { runtime })` 闭包托盘；页面自己不 `addRoute` | `index.ts` 49–51 / 59–61 / 67–69 / 77–79 |
| `service: {} as never` 仍能冻出四键 | `index.test.ts` 9–26 |
| 无 `requireNotifyWebRuntime`；对照档案楼 `requireProfileWebRuntime` | `runtime.ts` 全文 1–29；profile `runtime.ts` 64 |
| 权限四组 id：`notify-monitor/notice/inbox/config`；config 六串测试全量 toEqual | `index.ts` 11–42；`index.test.ts` 27–36 |
| `NotifyWebRuntime.service` 类型来自 `createNotificationService`；directory 三口；`subscribeInbox` / `inboxChanged` 可选 | `runtime.ts` 12–28 |
| 包名 `@namewta/web-domain-notify`；子路径 `./monitor` `./notice` `./inbox` `./config` 只是再 export Vue | `package.json` 6；各 `*/index.ts` 各 1 行 |
| 包 **没有** `@namewta/domain-system` | `package.json` 8 |
| 四张页 **零** `@/application/services` | web-domain 包 grep 空 |
| 公告：`notices.list/get/save/publish/retract/remove`；对话框按钮「保存草稿」；列表再发布 | `NoticePage.vue` 168–171 / 283 / 330 / 362–365 / 387–388 |
| 只有 `DRAFT`/`RETRACTED` 可编辑；已发布 warning「此通知已经发布，不能编辑」 | 272–273 / 332–335 |
| 渠道只收 `IN_APP\|SMS\|MAIL`；默认草稿 `['IN_APP']` | 196–199 / 225 |
| 选人走 `runtime.directory`，不是 `service.notices` | 141；`RecipientUserPicker.vue` 71 |
| 空白不搜索；300ms 防抖；翻页保留已选；`usersByIds` 恢复草稿 | `useRecipientSelection.ts` 35–50 / 58–68 / 88–91；测试 27–36 / 40–54 |
| `userTypes` 再 `status === '0'` 过滤 | `NoticePage.vue` 302–303 |
| 按钮核 `notify:notice:add\|edit\|publish\|retract\|remove`；列表查询不核 `query` | 7 / 43–74 |
| 收件箱：一次 `inbox.list()`，无分页组件；状态看 `readTime` 不看 `seenTime` | `InboxPage.vue` 10 / 22 / 85 |
| 点行/`标记已读` → `inbox.read` → `inboxChanged?.()`；有 `path` 才 `navigate` | 99–100 / 115–118 |
| `onMounted` 订 `subscribeInbox`；`onActivated` keep-alive 再刷 | 121–127 |
| 全仓 Vue **零** `inbox.seen(` / `runtime.service.snapshot` / `config.account(` | frontend `*.vue` grep 空 |
| 配置 TAB 只有 MAIL/SMS；密钥列 `mailPassSet`/`accessKeySecretSet`；编辑清空密文字段，不打 `config.account(id)` | `ConfigPage.vue` 16–18 / 32 / 337–341 |
| 账号九枪：`accounts/addAccount/editAccount/changeStatus/removeAccount/scenes/saveScene/testAccount/testTemplate` | 310 / 323 / 349–351 / 366 / 377 / 411 / 452–460 |
| 启停核 `notify:config:edit`；试发核 `test` | 38–46 / 54 |
| 短信块：`smsTemplateCode` + 参数映射 +「禁止自由正文」；无 `type="textarea"`、无 `mailBody` | 214–223；`index.test.ts` 39–62 |
| 账号试发不传 `sceneCode`；模板试发带 `channel` | 452–460 |
| 监控页文件 `NotificationPage.vue`，`componentName` 却是 `NotificationMonitor`；只 `runtime.service.deliveries(query)` | `index.ts` 48；页 81–88 |
| `load()` 无 `catch`，`finally` 仍关 loading | 81–87 |
| 监控页 **没有** `hasPermission`；收件箱页也没有 | 两页 script 无此调用 |
| 厨房根上 `snapshot`/`deliveries`；`notices` 复数；inbox 四枪；config 含 `account(id)` | `transport.ts` 22–70 |
| `./notification` / `./callback` 只有资源标签，re-export 同一份厨房 | `notification/index.ts` 3–6；`callback/index.ts` 2–5 |
| `notifyDomainModule.capabilities` 两张名片，不是 componentKey | domains `index.ts` 3–6 |
| admin 厅堂：`notificationService = createNotificationService(domainHttp)`；directory 空白短路 + `userTypes` 转 `systemService` | `services.ts` 70–88 |
| registry 第 291 行注入 `service`/`directory`/`notify:inbox-updated`/`inboxChanged→initMessageBox` | `adminManifestRegistry.ts` 291–306 |
| `selectedManifestIds` 含 `'web-domain-notify'` | 376 |
| `monitor/notify/index` + `system` → `undefined`；`notify/monitor/index` + `notify` → `NotificationMonitor` | registry 测试 116–118 |
| home **不**选这份 id，也 **没有** `createNotificationService` | `homeManifestRegistry.ts` 39；home-web grep 空 |
| 顶栏直接 `notificationService.inbox.read` / `readAll`；`push.ts` `inbox.list`；`refreshMessageInbox` 先派 `notify:inbox-updated` | notice 54 / 100 / 114；`push.ts` 21–23 / 107 |
| 通知中心四张 C 型页 + F 型 `notify:notification:submit\|retry\|cancel\|query` **无 component**；inbox 有 `seen`/`read` F 型，**无** `readAll` 菜单行 | DML 1526–1570 |
| `notify:monitor:query` 备注「查询通知快照」 | DML 1528 |

**课写过、测试名单更窄或口试把 SQL 插入说成 init 后仍活着（不是证伪本格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「包内 AGENTS.md 写三页」所以要拿测试当权威 | `AGENTS.md` 第 3 行仍是 `notify/notice/index`、`notify/monitor/index`、`notify/inbox/index`，漏 config。工厂/测试/种子/ConfigPage 都是四页。课已点破 |
| 「两颗种子都能让 `getRouters` 吐出同一 `componentKey`」；正例 9「旧日志菜单仍打开同一张监控页」 | 补偿块 **insert** 在 503–515（父「日志管理」`1761400000000000108`，path `notify-monitor`，子按钮 `system:notify:query/remove`）。同文件 1611–1618 **delete** `menu_id in (1761400000000000125, 1080, 1081)` 以及 `system:notify:*`。全新跑完 init **不会**留下第二张监控页。component 字符串对照仍成立；「两颗同时活着」以脚本末尾为准 |
| 「测试锁死四键顺序与 config 六串权限」 | 四键顺序 + config 六串 + `notify:monitor:list` 包含 + `notify-config` id。**没有** notice 七串 / inbox 三串 / monitor 两串的全量 `toEqual` |
| 口试表把动词写成 GET/POST | 厨房 `method: 'get'`/`'post'` 小写。课图表题已写「动词以 transport 为准」 |
| 课 `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |

问：在四键顺序、空 service 仍能冻、页面不 import 厅堂单例、公告六枪+directory、收件箱只 list/read、配置短信无 textarea、监控只 deliveries、snapshot/seen/account(id) 无 Vue、home 不选、顶栏旁路已经能互证的前提下，(a) 是否还要把「AGENTS 漏 config」「补偿种子随后被 DELETE」「权限数组测试只锁 config 全量」写成开篇半句，才算本切片讲完？还是这些已经落在课的表/反例里，Lead 可以把本切片视为已讲、并保持九工厂行 uncovered、厨房独行仍留给 L-053？

## Q3 假设与反例

- objective_id: OBJ-54
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无四个 notify web-domain 包；无 `requireNotifyWebRuntime`；Vue 不 snapshot / 不 seen / 不 `config.account`；厨房无 submit 方法；`deliveries` 在根上；`notices` 复数；home 无此工厂；顶栏直接 import；`monitor/notify/index` 为 undefined；capabilities 不是菜单；AGENTS 三页不是权威；补偿种子随后 DELETE；九工厂行不因本课整行闭合
- difficulty: high

课的隐含假设若反转，合格读者会把哪座工厂或哪只抽屉认错？

1. **「四张 Vue `import { notificationService }`。」** 包内搜不到 `@/application/services`。直接 import 的是顶栏和 `push.ts`。课反例 1 覆盖。
2. **「`createNotifyWebDomain` 等于九个 web-domain 工厂都 covered。」** 矩阵 (a) 同行绑死。GP-L-020 / GP-L-044 已挖过。课反例 2 / 误区 3 覆盖。
3. **「`NotificationPage.vue` 会 `submit` 统一通知。」** 那张页只 `deliveries`。submit 是 L-051；厨房 facade 没收这四枪。课反例 3 覆盖。
4. **「系统监控里的通知页是 `monitor/notify/index`。」** 测试：该键 + `system` 为 `undefined`。真键是 `notify/monitor/index` + `notify`。课反例 4 覆盖。
5. **「监控页会打 snapshot。」** 厨房有 `snapshot(notificationId)`。Vue 搜不到。课反例 5 覆盖。
6. **「收件箱点开走 `seen`。」** 页走 `read`。`seen` 只有 transport 和菜单 F 型。课反例 6 覆盖。
7. **「`notificationService.recipients.search`。」** 没有这个字段。选人是 `runtime.directory.searchUsers`。课反例 7 覆盖。
8. **「`createNotifyWebDomain(undefined)` 会给出空菜单 / 当场 throw。」** 今天不会在工厂里 throw。对照档案楼不要背错。课反例 8 / 误区 11 覆盖。
9. **「短信场景能像邮件一样写正文。」** 测试禁止 textarea。课反例 9 覆盖。
10. **「home 的厨房没有 inbox / 少两道菜。」** home **没有**这座厨房，也没有这份菜单工厂。不要套 L-039「同一厨房两份菜单」。课反例 10 覆盖。
11. **「包 README/AGENTS 写三页，所以没有配置页。」** 磁盘四页。课反例 11 / 误区 12 覆盖。
12. **「`capabilities` 有 notification-monitor 所以一定有 snapshot 按钮。」** 名片有能力，页面可以不挂。课反例 12 覆盖。
13. **「`service.notices` 写成 `service.notice` / `service.monitor.deliveries`。」** 厨房字段复数；deliveries 在根上。课误区 4 / 5 覆盖。
14. **「页面自己拼 `/notify/notice/list`。」** 厨房才拼。课误区 6 覆盖。
15. **「选人走 `createSystemService.users`。」** 走 notify recipients HTTP，经 directory；用户类型才借 system。课误区 7 覆盖。
16. **「配置页也能管站内信账号。」** TAB 只有邮件和短信。课误区 8 覆盖。
17. **「InboxPage 有全部已读。」** 在顶栏。工厂权限目录也没有 `notify:inbox:readAll`。课误区 9 覆盖。
18. **「`subscribeInbox` 是厨房方法。」** 是厅堂把 window 事件塞进 runtime 的可选口。课误区 10 覆盖。
19. **「日志管理下的通知监控是 `createMonitorWebDomain`。」** component 已经是 `notify/monitor/index`。全新 init 连这颗菜单都删了。课误区 13 覆盖；活菜单以 1618 行为准。
20. **「前端授权。」** 藏按钮 ≠ 授权。收件箱/监控页连 UI 闸都没有。课误区 14 / 边界 3 覆盖。
21. **「OBJ-54 包含 `createNotificationService`。」** OBJ-53。课误区 1 覆盖。
22. **「OBJ-54 包含 `NotifyNoticeController`。」** OBJ-45。课误区 2 覆盖。
23. **「种子有 `notify:notification:submit` 就一定有提交页。」** F 型无 component。课类比失效边界覆盖。
24. **「`./notification` 子路径等于有 submit 方法。」** 只有 `controller` + `basePath` 标签。课机制 7 覆盖。
25. **「两颗监控种子会一直并存。」** insert 之后同文件 DELETE。口试不要把补偿块说成当前基座仍挂着的第二张页。

问：读完后仍说不出的，是不是「工厂的名字」，而是 **厨房方法全集 ≠ 墙上的菜 ≠ 页面已扣的扳机、九工厂行不能因本课整行闭合、工厂不 fail-closed、顶栏不是第五键、home 根本不选、snapshot/seen/account(id) 目前只有厨房、应用 API 连封面都没印、补偿种子被脚本末尾删掉**？

## Q4 视角

- objective_id: OBJ-54
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-54 vs L-007/L-020/L-024/L-044/L-045…L-053；九工厂行；claimed_cells 工厂名 vs 四张 Vue / 顶栏；厨房独行仍 uncovered
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这一格 (a) 切片**？

| 视角 | 多出来的边 | 是否本课这一格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 九工厂同行 vs chain 工厂名；厨房独行仍 uncovered；无 Vue 行；(b) 无本课符号；(d) 无浏览器通知菜单流 | **是** 闭合定义。本切片可视为已讲。九工厂行保持 uncovered。不要发明 (b)/(d)，不要把四张 Vue 升成新行才能给切片盖章，不要用本课盖厨房章 |
| 管理端菜单工厂 | 四键顺序、四组 `notify:*`、不校验托盘、`h(page, { runtime })`、未选无键 | **是** 切片里的 `createNotifyWebDomain` |
| 四页 owner | notices 六枪；inbox list/read；config 账号/场景/试发；deliveries；directory 选人；短信无 textarea | **是** OBJ 对照走查，不是矩阵第二格 |
| 顶栏消息盒子 | 直接 import 厨房 list/read/readAll；`notify:inbox-updated` | **是** 旁路对照。不是第五键，不要升格 |
| 厨房作者（L-053） | URL 全表；directory 空白短路；`snapshot`/`seen`/`account(id)`/`readAll` | 本课借用剩枪负空间 **是**。工厂盖章 **否**（L-053） |
| 厅堂电工（L-007） | `createNotificationService(domainHttp)`；directory 转 system `userTypes` | 插头一句 **是**。12 个 `create*` 全表 **否** |
| 导航 host（L-020） | admin 选 `web-domain-notify`；home 不选；错误键 `monitor/notify/index` 失败关闭 | 选择集一句 **是**。15 键 / 恢复链 **否**。九工厂行 **否** 整行 |
| 系统监控（L-024） | `createMonitorWebDomain` / `monitor/online/index` / `web-domain-system-monitor` | 走错楼一句 **是**。保安亭四页 **否** |
| 档案楼（L-044） | `requireProfileWebRuntime` 十一口当场 throw | fail-closed 对照 **是**。企业三页 **否** |
| 公告窗（L-045） | `/notify/notice` 六扇 | URL 对齐 **是**。Java 五层 **否** |
| 收件箱窗（L-046） | seen/read/readAll | 页只 read、顶栏加 readAll、seen 无 Vue **是** 边界。Controller **否** |
| 配置窗（L-047） | 十扇；密钥布尔；SYNC 试发 | UI 篱笆 **是**。后端模板闸 **否** |
| 收件人窗（L-048） | search/by-ids | directory 口 **是** 边界。UseCase clamp **否** |
| 监控窗（L-049） | snapshot + deliveries 两扇 GET | 页只 deliveries **是** 边界。Query(false)/500 **否** |
| 回调窗（L-050） | 匿名 POST | 无 Vue **是** 边界。HMAC **否** |
| 应用提交（L-051） | submit/query/retry/cancel | 厨房无方法、种子 F 型无 component **是** 负空间。写篮 **否** |
| Outbox（L-052） | 领取/失败保持待领取 | 无管理页 **是** 负空间。CAS **否** |
| MySQL 8.4 | 菜单种子 / 补偿块 DELETE | 种子键对照 **是** (a)。容器 **否** |

问：Lead 若凭「工厂名在九工厂行里」把该行标 covered，是否还要求读者能指着 **四键、页面不喊 snapshot/seen/account(id)、工厂不校验托盘、顶栏旁路、home 不选、短信无 textarea**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要等 L-053 才给**本切片**当已讲？九工厂行是否例外——必须保持 uncovered，直到其余工厂也有课？厨房独行是否必须保持 uncovered，本课只往备注写「notify 四页已讲」？

## Q5 元问题

- objective_id: OBJ-54
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 九工厂同行仍 uncovered、厨房独行仍 uncovered、无 Vue 行、无本课 (b)/(d)
- difficulty: low

建议：`defer`

原因：

- **`A:createNotifyWebDomain` 是 chain 切片，不是矩阵独行。** L 合同章节齐全（宏观地图、定义表、机制链 1–8、带 caption / 文字等价物的两张图、正反例、变式、误区、暂停走查、来源表 S-005/S-006/S-010/S-L054-01…08）。能复现：四键顺序与 `h(page, { runtime })`、空 service 仍能冻、无 fail-closed、公告六枪+directory、收件箱 list/read、配置九枪且短信无自由正文、监控只 deliveries、厨房 snapshot/seen/account(id) 无 Vue、应用 API 无厨房字段、顶栏旁路、home 不选。不必为「工厂的名字」再挖五问。
- **Lead 不可以凭本课把九工厂同行标 `covered`。** GP-L-020 / GP-L-044 已立过同一条。本切片证据可写进该行备注（「notify 管理端四页 + 顶栏旁路已讲」）。**厨房独行保持 uncovered**，等 L-053，不要用本课开第二张厨房章。
- 本批可粘贴的精度项不挡住切片视为已讲：（1）`AGENTS.md` 第 3 行仍写三页，权威是工厂/测试；（2）补偿块 insert 之后同文件 1611–1618 DELETE，全新 init **没有**日志管理下的第二张监控页；（3）权限数组测试只全量锁 config 六串；（4）动词小写 `'get'`/`'post'`。不要把八扇 Java、厨房 URL 全表、系统监控四页、九工厂其余八个名字塞进本格。
- 不建议 `mine-more`：b01 未证明本切片还有真实矩阵空洞；再五问只会把 L-053 厨房 URL、L-049 snapshot、L-046 seen、L-051 submit 或 L-024 保安亭再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；「两颗种子随后删一颗」是半句精度，不是第二容器。不建议 `split-lesson`：未满 10 问；四页消费是 chain 原设计的同一 OBJ-54，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：不校验 runtime、顶栏旁路、短信无 textarea 已是 (a) 的解释到副作用。不建议把四张 Vue 升成矩阵行才给切片盖章：课已写明格子按 chain 认工厂。不建议等 GP-L-053 才给本切片当已讲。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:createNotifyWebDomain | 课按磁盘讲了四键、不校验托盘、四页实际扣的 `runtime.service.*`/`directory`、厨房剩枪无 Vue、顶栏旁路、home 不选。**矩阵无此独行**；符号落在九工厂同行。残留精度：AGENTS 漏 config；补偿种子随后 DELETE；权限测试只锁 config 全量。 | 可选半句：口试「两颗监控种子」以脚本末尾 DELETE 为准（全新基座只剩通知中心那颗）；AGENTS 三页不是权威名单。不要求重写机制链。Lead **不要**改九工厂行为 covered。可将本切片证据写入该行备注。 | defer（切片已讲；九工厂行不可因本课整行闭合） |
| 九工厂行 `create*WebDomain`（含 `createNotifyWebDomain`） | 本课只用选择集 + 四键当切片证据。其余八个工厂未因本课讲完。 | 保持 uncovered。L-020 已覆盖 `createSystemWebDomain` 切片；L-039/L-044 已覆盖 profile 两份菜单；其余后续课。 | defer（九工厂行保持 uncovered） |
| A:createNotificationService / notificationDirectory.searchUsers, usersByIds | 厨房独行仍 uncovered。本课只借用封面形状与剩枪负空间。 | 不要用本课盖厨房章。备注可写「notify 四页消费已讲」。 | defer（L-053） |
| A:admin-web.services | 插头 `createNotificationService(domainHttp)` 与 directory 接线已借用。 | 保持。L-007。 | defer（L-007） |
| A:createSystemWebDomain / B:navigation host | 选择集 + 错误键 `monitor/notify/index` 已点。15 键全表未讲。 | 保持。L-020。 | defer（L-020） |
| A:createMonitorService / system monitor pages | 走错楼一句。 | 保持。L-024。 | defer（L-024） |
| A:createProfileWebDomain enterprise | fail-closed 对照已借用。 | 保持。L-044。 | defer（L-044） |
| NotifyNoticeController.* | 六扇 URL 对齐旁证已借用。 | 保持。L-045。 | defer（L-045） |
| NotifyInboxController.list, seen, read, readAll | 页只 read、顶栏 readAll、seen 无 Vue。 | 保持。L-046。 | defer（L-046） |
| NotifyConfigController.* | 短信无 textarea、`mailPassSet` 占位。 | 保持。L-047。 | defer（L-047） |
| NotifyRecipientController.search, byIds | directory 点选。 | 保持。L-048。 | defer（L-048） |
| NotificationMonitorController.snapshot, deliveries | 页只 deliveries、无 snapshot 框。 | 保持。L-049。 | defer（L-049） |
| ProviderCallbackController.callback | 无 Vue 页。 | 保持。L-050。 | defer（L-050） |
| NotificationController.submit,query,retry,cancel | 厨房无方法；种子 F 型无 component。 | 保持。L-051。 | defer（L-051） |
| B:NotifyOutboxClaimUseCase / D:fail-outbox | 无 outbox 管理页。 | 保持。L-052。 | defer（L-052） |
| NotifyClient.send / NotifyDispatcher.send | 矩阵 (b) 仍 uncovered。 | 不要并进菜单工厂。 | defer（L-082） |
| (b) runtime 校验 / 页面扣扳机 / 四键注册 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 浏览器通知菜单流 | 矩阵无此行。ASCII 图不是新的 (d)。已 covered 的主路径仍是 submit→Outbox。 | 不要发明前端通知菜单数据流行。 | defer（未入库） |
| MySQL 8.4 / Redis 8 | 菜单种子是 (a) 对照，不是容器闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本切片无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（九工厂同行是矩阵绑行问题，课内已把切片讲完并显式禁止整行闭合；厨房独行有自己的课）；合格读者说得出工厂目的（管理端四键点菜单把厨房喂进页面）、主路径（公告 save 再 publish / 收件箱 list+read / 配置账号场景试发 / 监控 deliveries）与失败路径（工厂不校验、缺方法在 `onMounted` 才炸、已发布不能编辑、短信无 textarea、监控 `load` 无 catch、home 不选这份 id）；把可选半句写进原课不会超过 40 分钟；后端八扇/厨房/厅堂插头/系统监控/九工厂其余名字已有邻课。满 10 问前不拆。四页虽跨公告/收件箱/配置/监控四张门脸，仍是 chain 原设计的同一 OBJ-54，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对九工厂整行、L-053 厨房、L-007 厅堂、L-020 导航、L-024 系统监控、L-044 档案楼对照、L-045…L-052 八扇 Java、L-082 send、未入库 (b)/(d) 以及 MySQL/Redis 容器显式建议 `defer`；对 `A:createNotifyWebDomain` 建议视为已讲切片、**不要**把九工厂同行标 `covered`、**不要**用本课盖仍 uncovered 的厨房独行。本课不主张改 Java 窗、发明 Vue 矩阵行、或把顶栏升成第五键。
