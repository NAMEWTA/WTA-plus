---
artifact: learning-goal-probe
probe_id: GP-L-029-b01
lesson_id: L-029
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-029 / b01

审问对象是已写成的 Lesson `lessons/L-029-oss-frontend.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（Lesson `claimed_cells` / 矩阵两行）：`A:createOssUploadClient.upload`、`B:createOssUploadClient.upload`（原文：浏览器直传；progress；abort）。OBJ-29：能口述 `createOssUploadClient` + `useDirectOssUpload` + FileUpload 如何拿 ticket 直传 MinIO（`course.md` 档是**解释+走查**，不是 OBJ-27 那种方法性状）。chain 覆盖列写成 `A:createOssUploadClient.upload A:useDirectOssUpload`：**没有 (b)**，多写了一格矩阵里不存在的 `A:useDirectOssUpload`。矩阵 (a)(b) 均仍 `uncovered`。矩阵 (d) 主路径已 `covered`（L-027 + GP-L-027-b01），备注「浏览器 PUT 细节 L-029」；(d) 失败路径只挂 L-027。矩阵 (c) MinIO/OSS 仍 uncovered。本批按派单审问 **`upload()` 主路径 + 浏览器 progress/abort**；两扇门面是口述走查，不是另两行符号。`SysOssUploadController` 五扇留给 L-027；对象卡片柜留给 L-026；配置切换留给 L-025；迁移窗留给 L-028；厅堂 `services.ts` 十二工厂留给 L-007；adapter 包所有权留给 L-010；头像 `updateProfile` 留给 L-022；demo 富文本资产留给 L-076。

抽查锚点（2026-09-16）：`frontend/packages/adapters/oss-upload-browser/src/{client,transport,fingerprint,resume-store,types,client.test,index}.ts`；`package.json` 只依赖 `platform-contracts`；`frontend/packages/platform/contracts/src/index.ts` `UploadClient`；`frontend/packages/domains/system/src/resource-service.ts` `createOssService` / `validatePresigned`；`resource-service.test.ts` 五枪 URL 与 unsafe init；`frontend/apps/admin-web/src/application/services.ts`；`hooks/oss/useDirectOssUpload.ts` 及其测试；`components/FileUpload/index.vue`；`components/ImageUpload/index.vue`；`components/Editor/index.vue`；`views/system/user/profile/userAvatar.vue`；`packages/web-kit/file-upload/src/{FileUpload.vue,ImageUpload.vue,upload-request.ts,types.ts}`；`web-domains/workflow/src/components/ProcessActionDialog.vue`；`backend/.../OssUploadProperties.java` `maxSignParts=20`；home-web 零 `createOssUploadClient`。

## Q1 澄清

- objective_id: OBJ-29
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-29；chain L-029 覆盖列 `A:createOssUploadClient.upload A:useDirectOssUpload`（无 B）；L-029 `claimed_cells` 两格都是 `createOssUploadClient.upload`；矩阵 (a) `createOssUploadClient.upload`、(b) 同符号「浏览器直传；progress；abort」仍 uncovered；(d) 主路径已 covered、备注挂 L-029；(c) MinIO/OSS uncovered；课开篇「口试名单就是矩阵两格，外加 OBJ-29 点名的两扇门面」+ 误区 1
- difficulty: medium

课把旅客这一头收成「`upload()` 拿票送货 + progress/abort」。计划里至少有四套可数对象，派单点名的是 (a) 与 (b)：

1. **OBJ-29 原文。** `course.md`：能口述 `createOssUploadClient` + `useDirectOssUpload` + FileUpload 如何拿 ticket 直传 MinIO。点名的是**工厂 + 两扇门面**，没有把 `resolve`/`remove`、IndexedDB 库名、MinIO CORS 配桶写成口试清单。
2. **chain 覆盖列。** `A:createOssUploadClient.upload` 和 `A:useDirectOssUpload`。不写 (b)。矩阵 **没有** `useDirectOssUpload` 行。
3. **Lesson `claimed_cells` / 矩阵两格。** (a) 与磁盘 `UploadClient.upload` 1:1；(b) 原文「浏览器直传；progress；abort」。课把 `useDirectOssUpload` / `FileUpload` 收成「顺着两扇门面把票送到 `upload`」，并写明「不是另两行符号」。
4. **课自己的操作定义。** (a) 指纹 → 续传或 init → SINGLE 一次 PUT 或 MULTIPART 窗口 8 → complete → ossId → downloadUrl；字节不进 Spring。(b) 进度只出 `xhr.upload.onprogress`；取消只认 `AbortSignal`，有 `uploadToken` 才 `abortUpload`；complete 校验失败不由 adapter DELETE。(d) 主路径的浏览器 PUT 半边是本课函数级，不是再开一条数据流。
5. **邻格。** GP-L-027 已把 Relational 走廊（init JSON → PUT 不进 Controller → complete 临时行）标成 (d) 后端半边；本课补 xhr / IndexedDB / 假 XHR。L-007 已认厅堂插头。L-010 已认 adapter 住哪。

问：Lead 盖章时，闭合标准是矩阵这两行（`upload` 目的 + progress/abort 性状），还是 OBJ-29 三个名字必须各自成行？chain 多写的 `A:useDirectOssUpload` 是走查门面还是漏入库的 (a)？chain 不写 (b) 时，标 (b) covered 的证据锚是 `claimed_cells` 还是要先改 chain？(d) 已 covered 的前提下，本课是否还要再碰那一行，还是只把「浏览器 PUT 细节」写进备注证据？

## Q2 证据

- objective_id: OBJ-29
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: `client.ts` 15–26 / 56–58 / 70–74 / 93–129 / 131–160 / 162–256 / 258–303；`transport.ts` 3–36；`fingerprint.ts` 1 / 11–17；`resume-store.ts` 3–16 / 36–41；`types.ts` 70–79 / 102–104；`client.test.ts` 52–142（四例，无 abort/progress）；`platform/contracts` `UploadClient` 88–99；`resource-service.ts` 63–66 / 157–207；`resource-service.test.ts` 129–137 / 223–243；`services.ts` 22–42；`useDirectOssUpload.ts` 10–44 及其测试 16–28；`FileUpload.vue` 15 / 47–56 / 70–74 / 92–137；`upload-request.ts` 10–25；`Editor/index.vue` 86–99 / 318–320；`userAvatar.vue` 108 / 136–154；`OssUploadProperties.maxSignParts=20`；home-web 零工厂
- difficulty: high

课的 `upload` 主路径与 (b) progress/abort 哪些能在工作树直接复现，哪些只是测试名单比课表窄、或邻格事实？

**仍能复现（本课两格）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂第一行 `requireClientId`；空 `VITE_APP_CLIENT_ID` 在 `services.ts` 37 行求值即抛，早于 44 行 `createIdentityAccessService` | `client.ts` 56–58 / 259；`services.ts` 37–44 |
| 缺省 `fingerprint` / IndexedDB resume / `transferToOss`；厅堂可覆盖 `transfer` | `client.ts` 261–263；`services.ts` 22–41 |
| `UploadClient.upload` 的 `signal` **必选**；还实现 `resolve` / `remove`，口试格子仍是 `upload` | `platform/contracts` 88–99；`client.ts` 266–301 |
| 网关八口；五枪路径与 L-027 对齐；token `encodeURIComponent` | `types.ts` 70–79；`resource-service.ts` 179–204；测试 `upload%2Ftoken` |
| `initUpload`/`signParts`/`resumeUpload` 先 `validatePresigned`（http(s)、不许 userinfo）；adapter 不再验 URL | `resource-service.ts` 53–66 / 181 / 190 / 199；unsafe init 测试 223–243 |
| 指纹 `v1:` + 头尾各最多 1MiB SHA-256，再和 name/size/`lastModified` 哈希；不是整文件哈希 | `fingerprint.ts` 1 / 11–17 |
| 续传键 `SHA-256(clientId:getToken()\|\|'anonymous':policy:fingerprint)`；IDB 字段名 `fingerprint` 存的是 storageKey | `client.ts` 70–74 / 148–149；`resume-store.ts` 3–4 / 15 |
| IDB 库 `wta-oss-upload` / store `sessions`；`get`/`put`/`remove` 抛错尽量吞，当前页继续 | `resume-store.ts` 3–41；`findResume` 101–105；`initialize` 147–158；`safeRemoveResume` 85–91 |
| 陈旧码/中文五句 → 撕条再 `initialize`；其它错误原样抛，不偷偷开票 | `client.ts` 19–26 / 118–128；测试「上传会话不存在」→ `fresh-upload` |
| COMPLETED resume：零 `transfer`、零 `completeUpload`，还 `completedOssId` | `client.ts` 274–277；测试 id=`9002` |
| SINGLE：必须 `presignedRequest`；一次 PUT 整文件；complete 仍 POST `{ parts: [] }` | `uploadSingle` 162–172；`completeUpload` 280 |
| MULTIPART：`SIGN_WINDOW=8`（后端 `maxSignParts=20`）；同窗 `allSettled`；单片最多 3 次、失败只重签该号；要 ETag；手工按 partNumber 插排（避 `toSorted`） | `client.ts` 16–18 / 189–255；测试第 2 枪 `signParts('upload-1', [1])`；`OssUploadProperties` 35 |
| complete `data` 必须非空 string/number；`null` →「完成 OSS 上传失败」，不打 downloadUrl | `requireOssId` 49–54；测试 112–120 |
| downloadUrl 失败 → `URL.createObjectURL(file)`；blob 不写进 `sys_oss.url` | `resolveUploadResult` 76–83；L-026 目录列空串是邻格 |
| 真 XHR：`open(method,url)`、只设 `requiredHeaders`、`send(body)`、`lengthComputable` 才 `onProgress`、成功读 `ETag`、`onabort` → `DOMException('上传已取消','AbortError')`；无 `Authorization` 字面 | `transport.ts` 9–35 |
| 进度百分数：SINGLE `loaded/file.size*100`；MULTIPART 累加 part + `Math.max` 防回退 | `uploadSingle` 170；`uploadMultipart` 226–231 / `uploadSignedPart` 191–194 |
| 取消：`signal.aborted && uploadToken` 才 `abortUpload` + 撕 IDB；abort HTTP `.catch(() => undefined)`；`AbortError` 改抛 `OssUploadError('上传已取消','aborted')` | `client.ts` 284–290 |
| 假 XHR：`new XMLHttpRequest()` 后改写 `abort`，**从不** `send`；`onSuccess` 形状分叉（web-kit=`UploadResult`，门面=`{ossId,fileName,url}`） | `upload-request.ts` 12–24；`useDirectOssUpload.ts` 31–40 |
| 门面不是 composable：四个导出，没有 `useDirectOssUpload()`；默认 policy `general`；`isHandled` 抑制文案 | `useDirectOssUpload.ts` 全文；测试两例 |
| FileUpload 默认 `policy:'document'`、`show-file-list=false`、逗号/扩展名/MB 尺子（等于上限也拒）；App 包装写死 `client: ossUploadClient` | `FileUpload.vue` 15 / 47–56 / 92–107；`components/FileUpload/index.vue` 17 |
| workflow `<FileUpload :file-size="20" />` 不传 client，用 runtime 厅堂包装 | `ProcessActionDialog.vue` 23–27 / 151；`adminManifestRegistry` `fileUpload` |
| Editor `editor-image`/`editor-video`；卸载 `activeUploads.forEach(abort)` | `Editor/index.vue` 53 / 86–99 / 318–320 |
| 头像 `policy:'avatar'`，`requestUpload` 空函数，每次 `new AbortController()` 不交给取消钮；complete 后再 `updateProfile` | `userAvatar.vue` 108 / 136–154 |
| 开发代理仅 `import.meta.env.DEV` + `VITE_APP_OSS_PROXY_PREFIX` 时改写 PUT 到当前 Origin | `services.ts` 22–31 |
| home-web 无 `createOssUploadClient`；adapter `package.json` 无 Vue | 厅堂 grep 空；adapter 依赖只有 `platform-contracts` |
| (b) **没有** abort/progress 单测；`transport`/`fingerprint`/`resume-store` 无自己的测试文件 | `client.test.ts` 四个 `it`；目录清单 |

**课写过、磁盘已削薄或说满（不是证伪两格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「开票 HTTP 还在飞时用户取消：本地抛错，**不一定**打 DELETE」 | `findResume`/`initialize`/`completeUpload`/`signParts` **都不接收** `AbortSignal`。取消停的是 `transferToOss` 那只 PUT。开票若已返回，下一句就赋值 `uploadToken`，随后 PUT 看见 `signal.aborted` → **会** `abortUpload`。真正「有取消、无 DELETE」是 token 赋值前（指纹/`resumeKey`）或 `initialize` 自己抛错。口试不要说成「init 飞行中 abort 一定留 Redis 孤儿票」 |
| 「回显 `resolve` ← `listByIds`，每个 id 再 downloadUrl」 | adapter `resolve` 只 `map item.url`（`client.ts` 293–297）。逐 id `downloadUrl` 在 **`createOssService.listByIds`**（169–177），不是 `upload()` 格子 |
| 「`OssUploadError` code 是 aborted/contract/network/server/unknown」 | 类型联合是真的。`upload()` 自己构造的是 `aborted`/`contract`，其余把 gateway/XHR `Error` 原样抛出。`network`/`server`/`unknown` 本文件没有 `new` |
| 「FileUpload 没有进度条」所以 (b) 不可见 | FileUpload `show-file-list=false` 是真。`ImageUpload` 是 `picture-card` 且 `show-file-list=true`。口试 (b) 仍是回调与假 `ProgressEvent`，不是某张皮有没有条 |
| 「`javascript:` 直接 `ResourceSecurityError`」写在 init 句 | `requireSafeUrl` 对非 http(s) 一律扔。公开单测钉的是 init 的 `https://token:secret@…` userinfo，以及 **downloadUrl** 的 `javascript:`。机制同函数，例题不是同一枪 |

问：在指纹/续传/SINGLE+窗口 8、真 XHR PUT、假 XHR 只当取消手柄、有 token 才 DELETE、complete 失败不由 adapter abort、门面只改字段名、FileUpload 默认 document 已经能互证的前提下，(a)(b) 是否还要把 **「init 飞行中一定留票」「resolve 自己打 downloadUrl」「有 abort 单测」** 写成开篇半句，才算把 `upload` 讲完？还是这些已经（或应当）落在反例/边界里，Lead 可以直接盖章？

## Q3 假设与反例

- objective_id: OBJ-29
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无 MultipartFile；SIGN_WINDOW=8；signal 必选；假/真两辆 XHR；abort 条件有 token；complete 失败不 DELETE；FileUpload 默认 document；门面默认 general；无 `useDirectOssUpload()`；home-web 无工厂；无 abort 单测；IDB 失败不阻断；PUT 不带 Admin-Token
- difficulty: high

课的隐含假设若反转，合格读者会把哪一跳或哪只 XHR 认错？

1. **「字节经过 `SysOssUploadController`。」** Controller 无文件参数。PUT 目标是预签名 URL。课反例 1。
2. **「`FileUpload` 自己 init。」** 壳只调 `props.client.upload`。课反例 2。
3. **「`const { upload } = useDirectOssUpload()`。」** 磁盘没有这个函数。课反例 3。
4. **「FileUpload 默认 `general`。」** 默认 `document`。`general` 是 adapter 与 `directOssUploadRequest` 的缺省。课反例 4。
5. **「前端一次 `signParts` 20 个号。」** 常量 8。20 是后端上限。课反例 5。
6. **「进度用 axios `onUploadProgress`。」** 控制面 axios；数据面 XHR。课反例 6。
7. **「complete 失败前端必须 abort。」** L-027 `cleanupAfterFailure`；本课 catch 不因校验失败打 DELETE。课反例 7；与 GP-L-027 对齐。
8. **「取消一定 `abortUpload`。」** 没有 token 不会。abort HTTP 还被吞。课反例 8。再加：控制面 JSON 枪没接 `signal`。
9. **「IndexedDB 坏了就不能传。」** `get`/`put`/`remove` 吞错。课反例 9。
10. **「续传键是文件指纹原串。」** 身份哈希；字段名叫 `fingerprint`。课反例 10。
11. **「`parts` 网关方法用来 PUT 分片。」** 前端 `resumeUpload`；PUT 走 `signParts` 给的 URL。课反例 11。
12. **「预签名 PUT 带着 `Admin-Token`。」** 只带 `requiredHeaders`。课反例 12。
13. **「home-web 也能直传。」** 无工厂。课反例 13。
14. **「adapter 依赖 Vue。」** 只有 `platform-contracts`。课反例 15。
15. **「有 abort 单测所以 (b) 已由测试闭合。」** 没有。课反例 16。
16. **「OBJ-29 的格子是 `FileUpload.vue`。」** 矩阵两格都是 `upload`。课误区 1。
17. **「厅堂只注入了四口。」** 整份 `resources.oss`。课误区 4。
18. **「取消 = `client.remove(ossId)`。」** remove 删已编目对象。课误区 9。
19. **「`directOssUploadRequest` 的 onSuccess 拿到 `{id,name,url}`。」** 拿到 `{ossId,fileName,url}`。课误区 13。
20. **「头像裁剪提交途中关对话框会 abort。」** `uploadImg` 的 signal 没有交给可点按钮；`closeDialog` 不 `abort()`。课 (b) 边界。
21. **「COMPLETED resume 还要再 PUT/complete。」** 测试钉死零次。
22. **「L-027 的 (b) 和本课 (b) 是同一格。」** 那边是服务端 complete/abort；这边是浏览器 progress/abort。课误区 16。

问：读完后仍说不出的，是不是「函数名叫 `upload`」，而是 **两辆 XHR 谁在 PUT、进度只出真 XHR、取消有 token 才 DELETE、complete 失败不靠前端 abort、FileUpload 默认 document、门面不是 hook、窗口是 8 不是 20**？

## Q4 视角

- objective_id: OBJ-29
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-29 vs L-007/L-010/L-022/L-025/L-026/L-027/L-028/L-076；chain 多写的 `A:useDirectOssUpload`；GP-L-027 对 (d) 的操作定义
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A/B:createOssUploadClient.upload`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a)(b) 同符号两行 = 磁盘一个函数的目的+性状；OBJ 写三名字、chain 写成两个 A 且漏 B；课已拆「格子两行 + 门面走查」；(c) MinIO 仍 uncovered；(d) 已 covered、备注等本课 PUT | **是** 闭合定义。(a)(b) 可单独 covered。不要为门面发明第三行。不要把 MinIO 容器标 covered。不要把已 covered 的 (d) 重开 |
| 旅客 `upload()`（本函数） | 指纹、续传/init、SINGLE/窗口 8、complete、ossId、downloadUrl | **是** (a) |
| 数据面 XHR | `transferToOss` PUT、`requiredHeaders`、ETag、CORS 文案、`onprogress` | **是** (a) 的送货 + (b) 进度来源 |
| 取消门 | 假 XHR → `AbortSignal` → 真 XHR `abort` → 有 token 才 DELETE | **是** (b)。控制面 JSON 没接 signal 是同一性状的边界，不是新格 |
| 两扇门面 | `useDirectOssUpload` 改字段名 + 假 XHR；FileUpload 注入 client、默认 document | OBJ 走查 **是**。矩阵符号 **否** |
| 厅堂插头（L-007） | `gateway: resources.oss` 整对象；开发代理 `transfer` | 注入点一句 **是**。十二工厂全表 **否** |
| 票房（L-027） | 五条 URL 对得上；complete 失败服务端清；HTTP abort 三分支 | 对齐一句 **是**。五扇/临时行/魔数 **否** |
| 卡片柜（L-026） | `listByIds` 填 url；`list` 写空串；`remove` ≠ abort | resolve/remove 邻居 **是**。四扇 CRUD **否** |
| 配置柜（L-025） | 策略 `requiredPermission` 在后端 | 负空间 **是**。切桶 **否** |
| 搬家柜台（L-028） | 无 | **否** |
| 头像（L-022） | complete 后再 `updateProfile({ avatar: result.id })` | 变式 B 一句 **是**。资料窗 **否** |
| 富文本（L-076） | Editor 卸载 abort；`richtext-*` 资产口在 `services.ts` | 变式 C / 卸载 abort **是**。demo assets API **否** |
| 包所有权（L-010） | adapter 不住 Vue；web-kit 不拥有网关 | 负空间 **是**。三间房间全表 **否** |
| MinIO/OSS 容器 | CORS / 暴露 ETag / 开发代理绕同源 | 失败文案 **是**。配桶手册 **否**（(c)） |

问：Lead 若凭「函数表里有 `upload`」把 (a) 标 covered，是否还要求读者能指着 **真 XHR PUT、窗口 8、假 XHR 从不 send、有 token 才 abortUpload、complete 失败不 DELETE**？这些是不是已经在 (b) 和图/反例里，而不需要把 FileUpload 升成第三行、也不需要把 MinIO 容器标 covered？

## Q5 元问题

- objective_id: OBJ-29
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；单一函数 `createOssUploadClient.upload` 跨 adapter 控制面/数据面，门面不是第二 C4；矩阵 (a)(b) 仍 uncovered；(d) 已 covered
- difficulty: low

建议：`defer`

原因：

- **`A:createOssUploadClient.upload` 与矩阵 (a) 该行 1:1；`B:createOssUploadClient.upload` 与矩阵 (b)「浏览器直传；progress；abort」1:1。** L 合同章节齐全（直觉、定义、机制链 0–4、带 caption / 文字等价物的三层图 + 主路径图 + 双 XHR 图、正反例、变式、误区、来源表）。**主叙事站得住且与源码对齐**：厅堂单例 → `upload`；控制面 JSON / 数据面 PUT；指纹与身份哈希续传；SINGLE 一次 PUT、MULTIPART 窗口 8 且单片重签 3 次；进度只出真 XHR；取消只认 `AbortSignal` 且有 token 才 DELETE；complete 校验失败不由 adapter 再打 DELETE；假 XHR 从不 `send`；门面不是 composable；FileUpload 默认 `document`、adapter 默认 `general`；home-web 无工厂；(b) 无 abort 单测、证据是实现。不必为「函数名叫 upload」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例/边界，或只需半句、不挡住把**两行**标 covered：（1）`AbortSignal` 只接到 `transferToOss`，init/resume/sign/complete 的 JSON 枪不会被取消打断；token 一旦赋值，随后 PUT 看到 aborted **会** `abortUpload`；（2）`resolve` 本身不打 downloadUrl，是网关 `listByIds` 逐 id 要；（3）`OssUploadError` 的 `network`/`server`/`unknown` 本文件未构造；（4）ImageUpload 皮上可能看见 el-upload 列表，口试 (b) 仍是回调。不要把票房五扇、卡片柜、切桶、迁移、十二工厂、MinIO 配桶塞进 38 分钟课。
- **(d) 主路径** 矩阵已 covered。本课把浏览器 PUT / 假 XHR / 取消 DELETE 补成函数级证据。Lead 可将本 probe 追加进该行备注，**不要**把已 covered 的 (d) 打回 uncovered，也不要把第四跳业务表（头像/流程）写进本课才能盖章。
- 不建议 `mine-more`：b01 未证明本课两格还有真实矩阵空洞；再五问只会把 L-027 票房、L-026 卡片柜、MinIO CORS 手册或 FileUpload Vue 皮再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；半句「JSON 枪不接 signal」是可选补丁，不是第二容器。不建议 `split-lesson`：未满 10 问；两格 + 门面走查是同一 OBJ-29、同一个 `upload()`，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 `A:useDirectOssUpload` 或 `A:FileUpload` 行：chain 多写的那个 A 是走查，矩阵没有。不建议把 MinIO/OSS 标 covered。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:createOssUploadClient.upload | 指纹→续传或 init→SINGLE/窗口 8→complete→ossId→downloadUrl；字节不进 Spring；五枪 URL 对齐；IDB 可缺席；COMPLETED resume 零 PUT——均与源码/四例单测对齐。残留精度：gateway JSON 不接 `signal`；`resolve` 不自己 downloadUrl。 | 可选半句：取消只停 PUT；token 赋值后会 abortUpload。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-029 来源表 S-L029-01…08）。 | defer（本行可 covered；精度项不挡盖章） |
| B:createOssUploadClient.upload | 真 XHR `onprogress`（`lengthComputable`）→百分数；MULTIPART `Math.max`；假 XHR 只当 abort 手柄；`signal.aborted && uploadToken` 才 DELETE；complete 失败不 DELETE；头像 signal 无取消钮；**无** abort/progress 单测——实现可复现。 | 口试按源码，不要假装有 abort 单测。Lead 可将本行标 `covered`。 | defer（本行可 covered；测试缺口不挡 explained 档） |
| A:useDirectOssUpload | chain 有、矩阵无、`claimed_cells` 无。课已走查四导出 + 非 hook。 | 不要入库新行。保持走查。 | defer（未入库 / 走查门面） |
| FileUpload / ImageUpload | OBJ 点名的壳；默认 document/image；尺子；包装注入 client。 | 保持走查，不升格。 | defer（未入库） |
| D:主路径 OSS 直传 | 已 covered（L-027）。本课补 PUT xhr / 假 XHR。 | 备注可追加本 probe。不要打回 uncovered。 | defer（已 covered；本课只补数据面函数级） |
| D:失败路径 OSS abort | 只挂 L-027。本课对齐「校验失败不由 adapter DELETE」；用户取消才 HTTP abort。 | 保持。不要把取消 DELETE 写成第二条失败路径。 | defer（L-027） |
| A:SysOssUploadController.init,signParts,parts,complete,abort / B:complete/abort | 五条 URL 对表已借用。 | 保持。 | defer（L-027） |
| A:SysOssController.list,listByIds,downloadUrl,remove | `listByIds` 填 url、`remove`≠abort 已划。 | 保持。 | defer（L-026） |
| A:SysOssConfigController.* / B:updateOssConfigStatus | 策略权限在后端。 | 保持。 | defer（L-025） |
| A:SysOssMigrationController.* | 负空间。 | 保持。 | defer（L-028） |
| A:admin-web.services / C:admin-web-composition | 整份 `resources.oss` + 开发代理已借用。 | 全表 L-007。 | defer（L-007） |
| A:adapters / A:web-kit | 包所有权已划。 | 保持。 | defer（L-010） |
| 头像 updateProfile | 变式 B 一句。 | 保持。 | defer（L-022） |
| TestRichText assets | Editor 卸载 abort 已借用。 | 全表 L-076。 | defer（L-076） |
| MinIO/OSS 容器 | CORS 文案与开发代理只作为 PUT 失败句。 | 不要因本课标 covered。 | defer（(c) 容器 / 运维手册） |
| Redis 8 | 票 TTL 在 L-027。便利贴只有 token+过期。 | 保持。 | defer（L-084 / L-027） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；两行无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出 `upload` 目的、主路径（票→真 PUT→complete ossId）与失败/取消路径（无 token 不 DELETE；complete 失败不靠前端 abort）；把可选半句写进原课不会超过 40 分钟；票房/卡片柜/切桶/迁移/厅堂工厂/MinIO 容器已有后续课或范围外 defer。满 10 问前不拆。门面与 adapter 虽跨包，仍是 chain 原设计的同一 OBJ-29，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-027 票房、L-026 卡片柜、L-025 配置、L-028 迁移、L-007 厅堂、L-010 包权、L-022 头像、L-076 富文本、MinIO 容器、Redis、未入库门面行以及已 covered 的 (d) 显式建议 `defer`；对 `A:createOssUploadClient.upload` 与 `B:createOssUploadClient.upload` 建议 Lead 标 `covered`（证据锚本 probe 与 L-029 来源表）。本课不主张改 MinIO 容器、票房五扇、卡片柜、迁移窗或任何 (c) 格子；不主张把 `useDirectOssUpload` / FileUpload 升成新矩阵行。
