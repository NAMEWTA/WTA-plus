# Evidence T-06

- Goal: same-session three hard gates on 4174/18080 with screenshots `temp/team/lead/e2e/sso-*.png`.
- Runtime: MariaDB 11.8 `127.0.0.1:3306` db `ry-namewta`; Redis 8 `127.0.0.1:6379`; `wta-admin.jar --spring.profiles.active=dev,local --server.port=18080`; Vite admin 4174 / home 4175 / sso-web 4176; `captcha.enable=false`; user `WTA` / `admin123`.
- Unit suite (twice, both exit 0):
  - `backend/ ./mvnw -pl wta-admin,wta-modules/wta-sso,wta-modules/wta-system -am test -Dtest=SsoAuthorizationServiceTest,PkceS256Test,SsoTokenExtrasTest,SsoClientFieldsSupportTest,SsoRedirectUrisTest,SysClientServiceSsoUnitTest,AuthClientContextSsoUnitTest,PasswordPolicyBoundaryUnitTest -Dsurefire.failIfNoSpecifiedTests=false`
  - logs: `/tmp/grok-goal-64584c24c117/implementer/sso-unit-1.log`, `sso-unit-2.log`
- Hard E2E (twice + launcher, all exit 0):
  - `pnpm exec playwright test --config playwright.sso.config.ts` (from `frontend/`)
  - `scripts/sso-hard-e2e.sh`
  - logs: `/tmp/grok-goal-64584c24c117/implementer/sso-e2e-1.log`, `sso-e2e-2.log`
- Screenshots (OBJECTIVE path; copies also in this evidence dir):
  - `temp/team/lead/e2e/sso-ac001-default-provider-path.png` — AC-001 first third-party slot is **WTA SSO**
  - `temp/team/lead/e2e/sso-ac001-admin-logged-in.png` — AC-001 admin dashboard as **WTA** after Code+PKCE
  - `temp/team/lead/e2e/sso-ac002-reuse-no-password.png` — AC-002 home reuses SSO session (no password field)
  - `temp/team/lead/e2e/sso-ac003-client-isolation.png` — AC-003 home after second ticket; cross-client `GET /system/user/getInfo` with admin Token + home `clientid` returns business `code=401` 「客户端ID与Token不匹配」
- Extra: `Set-Cookie: Sso-Token=...; HttpOnly; SameSite=Lax` on `POST /sso/login`; first-party SPA sources have no `sso_secret` (only a negative test assertion).
- Residual: home Client `access_path=/home/**` still 403s `/system/user/getInfo` after SSO (does not block Token issue or isolation). Archived notify change untouched. Not pushed.
