# T45 固定候选 09be6db 的真实运行核验

**结论：本票现有真实运行轴通过。** 固定源 `09be6db6c3bace8594f7db0fe49a221e3cc5f140` / tree `2d5459e7dc77ec4120516d97a63de17eee390be4`。这是只读核验意见；AC-045 的最终治理状态仍由 Lead 按实现审查、写集和归集证据决定。A1/A2/C3、恢复 R1/R2 及旧两 JUnit 失败均保留原时点，未追认为成功。

- **受限 MinIO/full-app/Chrome：** `/tmp/wta-t45/ui-v5-runs/7d8fa892845f33d4/result.json` 为 `accepted=true`、`phase=postcheck`、`cleanup_errors=[]`。运行源 clean 09be6db；full JAR SHA `a0daf03adaf28c3eea2c136eef2963746b68f57fc8a4db64eca733c82f401776` 与 `/tmp/wta-t45/r3-full-package-proof.json` 的同源 clean/full 产物相同。Admin dist manifest 原字节 SHA、311 文件数、构建日志 SHA 与结果匹配；逐文件重算当前 311 项库存与 manifest 完全相同、无 symlink。core 后续 clean 构建可能覆盖 target JAR，不能将**当前** target 冒称此前 full JAR；本次运行自身已在前后强验其 full JAR/proof。
- **原 A2 行为由同次 v5 保留：** 启动自动 MinIO 网络请求数 0；三次 private A/public/private B 诊断各有精确 5 个请求：签名对象 HEAD200、Policy GET403、ACL GET403，匿名对象 HEAD/public200或private403、Range GET/public206或private403。三个诊断均 6 个事实、4 个桶级 UNKNOWN；public 对象 ALLOWED、private 对象 DENIED，未宣称全桶安全。实际最小身份的 policy/object ACL 写被拒，正常公私访问与默认路由切换仍通过；`INVALID_ACCESS_POLICY` 负例、普通用户403和匿名401仍通过。此同次 v5 已含原 A2 full HTTP 检查，**只计一次**，不把早期 A2结果叠加成新的独立真实运行。
- **浏览器夹具与身份：** 在 INVALID_ACCESS_POLICY HTTP 负例通过后，同页管理员列表实测 before status/code200、total=rowcount7，bad/public/private 均在页、未知策略1；仅本次 owned ID+key+policy9 精确删除 1 行，after status/code200、total=rowcount6、bad无、公私仍在、未知策略0。Chrome 两个固定用例各一次通过，`expected=2/unexpected=0/skipped=0/flaky=0`、exit0；管理员公私六事实页面与普通/匿名权限均覆盖。R1 行数与 R2 SQL 排序规则失败不是此次通过证据。
- **安全、来源与清理：** 后端已知凭据 canary 命中0、审计 canary0；原日志仅留脱敏摘要和源/脱敏 SHA。六 SQL 103 表、三 owned 容器完整 ID、两匿名卷、六个随机 loopback 端口、两个进程组均由 runner finally 检查，browser profile 进程残留0、临时私有文件已删、清理错误0。原字节 OpenAPI 为私有0600文件，SHA重算与结果相同；438 paths/450 schemas 与正式生成源的 paths/components 全等，差异仅本次 owned loopback server 端口（`r3-openapi-contract-equivalence.json`）。
- **两类真实 JUnit：** `/tmp/wta-t45/two-junit-runtime-review.md` 独立核实同一 09be6db 的 `45cde3cd5fe92d7c`：ReadinessMinio 与 AccessUrlMinio 两份 fresh XML 各1/0failure/0error/0skip、Maven0、acceptance=true、cleanup0、源码前后 clean 等值。其 owned 匿名 PUT403 是隔离兼容负控；产品诊断只读由上述每次精确五请求证明，两者不互相替代。
- **构建与门禁：** `green04` 34类/170项/0skip；当前 default fresh 262类/1139项，0failure/error、216环境skip（实际执行923项，skip不计通过）；full package/bundle exit0。`r3core-core-package.json` 与 `r3core-core-bundle.json` 均 exit0，core artifact记录同源 SHA `e61a1acaa1f2c9343d32f12e53802548f02f206353121ab5372d26cd08425192`，source前后 clean 09be6db，core/full模块清单由各自 bundle verifier 验证。前端架构、OpenAPI、lint、typecheck、tests及五个静态事实门禁依据逐输入树相等复用 R1 记录，**不是**冒称09be重跑；09be 的生产 build 和 Admin dist 已实际重做，详 `r3-frontend-evidence-reuse.json`、`r3-static-evidence-reuse.json` 与 r3 构建记录。

材料范围：本审查核了运行 result、两份脱敏 fresh XML、full/core proof、dist manifest与当前库存、私有 OpenAPI SHA、等价记录。未接触生产环境或供应商账号；T45 的受限身份/真实浏览器均只在 owned 隔离资源验证。
