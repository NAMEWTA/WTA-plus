# T-50 fixed C recovery engineering review

Verdict: **PASS for the fixed recovery diff, bounded evidence reuse, fresh C owned real gate, and C static checks.** Fixed B `50757f9b872b0b9b0131e4b2a4538bff7473f74d` to C `84ce0a9162dfc507fb4e8339575247477e57684f` via three-dot Git diff. The old B real run remains `acceptance=false`; the new C run supplies its own complete acceptance proof. This verdict concerns the local candidate, not a production deployment or change archive.

## Exact C delta

Twelve paths changed: one product-document path, `frontend/packages/api-contracts/README.md`, and eleven files in this change's SpecDev governance. The README replaces a stale fixed T-38 backend commit/count narrative with the active immutable `current.json` → revision `source.json`/`provenance.json` source chain. It accurately says new notification submissions support ALL, ASYNC and priority 0, and HTTP JSON must explicitly include priority; the generated transport type is required. No product Java, TypeScript, generated OpenAPI, tool, test, build configuration, or SQL file changed.

The governance delta preserves the prior blocked snapshot with attempts=3, records A1 test-expectation failure, A2 success but incomplete live OpenAPI required contract, and B's passing test assertions with failed exact-clean check. Its new Dispatch 02 requires all builds to finish before a serial owned eight-class run, retains original failed evidence, keeps the frozen driver and cleanup/clean-source rules, and resets only a new recovery batch to zero attempts. It does not mark T-50 done or erase the old failed B result. The README is inside the ticket's existing `api-contracts` writable scope.

## Evidence reuse boundary

I independently compared Git subtrees at B and C. `backend` is identical at `507250fd94c75aa15a89506e2a4771aa3497469b`; generated contracts, immutable OpenAPI revision/pointer, OpenAPI tooling, the three App sources, domains, and web-domains are each byte-identical subtrees. `/tmp/wta-t50/c1-source-equivalence.json` reports clean C tree `c118348385489be74b55233508ae0b5f40a93ee1` and the same twelve-path diff. Thus A3's backend default/full JAR/live capture proof and B's frontend executable gates/core-only package proof can be cited for the **same inputs**; they must retain their original A3 or B execution SHA rather than be relabeled as C runs. The A3 full JAR used for live OpenAPI and B's smaller core package are distinct artifacts and are not interchangeable.

B's recorded frontend architecture, lint, typecheck, workspace test, and production build all exited 0. Parsing `/tmp/wta-t50/b1-frontend-test.log` gives **29 Vitest packages, 99 test files, 628 executed passing tests**, plus **two Node test packages, 108 passing tests** (tooling OpenAPI 7; architecture 101), totaling **736 actual tests, zero failures/skips**. `@namewta/api-contracts` has a `test` script that runs `tsc --noEmit`; it is a passing compile check, not an additional test case. Exact per-package counts and source-log hash are retained at `/tmp/wta-t50/b1-frontend-counts-review.json` (0600).

The B production build exited 0. I read each existing `dist/build-mode.json`: admin-web, home-web and sso-web each identify their own app and `production` mode. `/tmp/wta-t50/b1-frontend-artifact-manifest.json` (0600) records per-file SHA-256/size and deterministic aggregate tree hashes of the current build outputs: admin-web 312 files / 8,928,213 bytes / `3011e35288ae5b99d97100c292fa70de1ce79430239dd7c8dee0370123691042`; home-web 14 / 1,524,397 / `fdf27cf4a7d7dbe79a8650add0586905188aee7be1a7514e669e710e57fed778`; sso-web 4 / 91,461 / `fd102f1dba7cb183e7ffbcf2b8363f5859b94f42325fbb73639b3e217287a204`. The aggregate is SHA-256 over sorted `relative-path\0size\0file-sha\n` records, so it is a local evidence fingerprint rather than a Git tree hash. B core package/bundle and static facts records also exit 0; B core artifact manifest is `/tmp/wta-t50/b1-core-artifact.json` and correctly remains labeled source B.

## Fresh C recovery gate and remaining checks

Recovery-batch attempt 1, `/tmp/wta-t50/runs/e0ba930d8ef45c5d/result.json`, completed with `acceptance=true`, exit 0 and Maven exit 0 against clean C. Source before **and** after are the same C HEAD `84ce0a9162dfc507fb4e8339575247477e57684f` and tree `c118348385489be74b55233508ae0b5f40a93ee1`. The eight exact classes have 15 + 8 + 1 + 19 + 66 + 13 + 10 + 3 = **135 tests**, zero failures, errors or skips. I independently parsed all eight retained XML files and verified their per-file SHA-256 and counts against `xml-manifest.json`; `/tmp/wta-t50/c1-retained-verification.json` agrees. Cleanup records an empty Maven process group, no owned container full IDs, the captured anonymous volume absent, both allocated ports closed, and no errors.

Attempt accounting retains **three** old-batch attempts plus **one** successful recovery-batch attempt, four historical runs in all; the old B failed result is not overwritten. I checked all seven C fresh records: `c1-{openapi-check,skill-facts,fullstack-facts,notify-layered,fm,handbooks,diff-check}.json` each has exit 0. `c1-static-source-before.json` and `c1-static-source-after.json` both record the same clean C HEAD/tree. Lead's independent `/tmp/wta-t50/lead-frontend-evidence-check.json` confirms the B-source 628 Vitest plus 108 Node tests (736 actual executions) and separately rehashes all 330 files in the three production-mode App artifact manifests against C's equivalent executable inputs. These are reused B results, accurately labeled B; only the real and static gates above were freshly run against C. No Maven, Docker, frontend build, or service was run by this reviewer.
