# T-17 evidence

HEAD `90c80ca6` closes AC-017. Designer message checks were already in the shipped component and unit tests. The default Playwright browser binary is not installed on this machine, so this candidate adds a runner that uses the system Chrome and the existing admin preview. No designer production code changed. AC-017 stays in the T-30 combined run.

## Commands

```bash
cd frontend && pnpm --filter @namewta/web-domain-workflow exec vitest run src/designer.test.ts
```

Exit 0. tests=23, skipped=0. Wrong origins, a detached frame, and a mismatched port are rejected. The configured absolute origin, including its port, is accepted.

```bash
cd frontend && pnpm exec playwright test --config playwright.workflow-definition.config.ts
```

Exit 0. 5 passed, 0 skipped. The admin production preview served the app. The spec covers category, definition, designer, and SpEL mutations; a visible business failure without a false success; permissions that hide mutations; a real iframe whose foreign origin, other window, and unknown payload do not close the tab while a legitimate close does; and a refresh that replaces the window and releases listeners.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase. The runner file was outside the original ticket write set and is recorded on the ticket.
