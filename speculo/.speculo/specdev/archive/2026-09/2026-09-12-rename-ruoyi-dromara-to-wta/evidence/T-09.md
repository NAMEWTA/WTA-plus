# Evidence: T-09 — Residual Classification Report

- **Change:** `2026-09-12-rename-ruoyi-dromara-to-wta`
- **Ticket:** `ticket/09-residual.md`
- **Workspace:** `/workspace/vp-dev/WTA-plus` (prep tree; legacy remotes not rewritten)
- **状态:** done

## 1. 实现摘要

对 WTA-plus 准备树扫描 `ruoyi` / `org.dromara` **以及路径形** `org/dromara`（排除 `node_modules`/`target`/`.git`）。**未**以 zero-match 为通过条件。

第二轮（skeptic）补扫：把测试/JSON/AGENTS/`start-dev.sh` 中的 `src/main/java/org/dromara/...` 全部改为 `org/namewta`；活页面去掉 `plus-doc.dromara.org` 与 `github.com/dromara/*`。契约测试现断言这些路径与 URL 不得回归。

## 4. 验收

| Contract | 结果 |
|---|---|
| AC-007 residual classified | pass |
| NAC-05 zero-match not used as pass | pass |

## Residual classes

| Class | Count (approx) | Disposition |
|---|---|---|
| KEEP third-party `org.dromara.{sms4j,warm,easyes,easy-es,mica}` (dot imports/coords) | many | **allowed KEEP** |
| `gitee.com/dromara/warm-flow` in `30-ry-workflow.sql` | 1 | **allowed KEEP** third-party SQL provenance |
| OpenAPI `openapi/revisions/**` hash snapshots (`gitee.com/dromara/RuoYi-Vue-Plus` in frozen contact.url) | 16 | **allowed REVIEW** — content-addressed history, not rewritten |
| Speculo process/history / change id | few | **allowed** |
| Hard-cut / migrate / attribution / negative tests (`ruoyi-namewta.yml` forbidden, migrate SQL, README “based on RuoYi-Vue-Plus”, contract test KEEP path literals) | few | **allowed** |
| Live FE `plus-doc.dromara.org` / `github.com/dromara/WTA-*` | 0 after fix | **was defect, now gone** (T-06) |
| `src/main/java/org/dromara/...` path strings in tests/JSON/AGENTS/scripts | 0 after fix | **was defect, now `org/namewta`** |

No remaining owned `org/dromara/` path in product source (contract test `owned filesystem paths use org/namewta not org/dromara`).

## 5. Commands

- `rg -n -i 'ruoyi|org\.dromara' /workspace/vp-dev/WTA-plus` → `{SCRATCH}/rg-residual.txt`
