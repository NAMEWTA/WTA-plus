---
artifact: learning-goal-probe
probe_id: GP-L-036-b01
lesson_id: L-036
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-036 / b01

审问对象是已写成的 Lesson `lessons/L-036-person-rebind.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、verdict、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（`claimed_cells` / 矩阵）：`A:PersonRebindController.probe,match,confirm,submit,unbind`、`B:PersonRebindController.confirm`、`D:fail-profile-rebind`（矩阵行名 **`D:失败路径 profile 换绑`**，备注 `rebind match/confirm 失败不改绑定`）。OBJ-36 原文：能口述 `PersonRebindController` probe/match/confirm/submit/unbind；档是**方法性状**。chain 覆盖列**只写 (a)**。矩阵 (a)(b) 与该条 (d) 均仍 `uncovered`。矩阵 **没有** `publishApprovedRebind` / listener 独行，也 **没有** 「主路径 profile 换绑」。本批按派单审问 **五扇窗 + (b) confirm + D 失败不改绑定**。`PersonAdminController` 十二扇留给 L-034；`PersonApplicationController.current/submit` 留给 L-035；材料 attach/detach 留给 L-037；核身匿名回调留给 L-038；厨房 `createPersonRebindService` / `createProfileService` 留给 L-039；企业 `EnterpriseTransferController.confirm` 留给 L-042。(c) MySQL 8.4 / Redis 8 仍 uncovered；(d) 「主路径 profile 申请」挂 L-035/L-034，不是本课。

抽查锚点（2026-09-16）：`controller/self/PersonRebindController.java`；`PersonRebindExceptionHandler.java`；`usecase/PersonRebindUseCase.java`；`usecase/impl/PersonRebindUseCaseImpl.java`；`service/PersonRebindService.java`；`service/IPersonRebindService.java`（生产无 `implements`）；`dao/PersonRebindDao.java`；`mapper/person/PersonRebindMapper.xml`；`listener/PersonRebindProcessListener.java`；`PersonRebindNotificationListener.java`；`controller/self/PersonRebindHttpContractTest.java`；`service/impl/PersonRebindServiceTest.java`；`PersonRebindServicePersistenceTest.java`；`PersonRebindMySqlE2ETest.java`；测试夹具 `service/impl/PersonRebindServiceImpl.java`；`frontend/packages/domains/profile/src/person/rebind/service.ts`；`permissions.ts`；`03-backend-module-modes.md` layered 行；`50-cde-base-dml.sql` `profile:person:apply`。

## Q1 澄清

- objective_id: OBJ-36
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-36；chain L-036 覆盖列只 (a)；L-036 `claimed_cells` 四项实为三格（D 中英同行）；矩阵 (a) 五方法一行、(b) confirm「改绑定；失败保持原绑定」、(d)「失败路径 profile 换绑」备注 match/confirm；矩阵无 publish/listener/主路径换绑行；(c) MySQL/Redis uncovered
- difficulty: medium

课把自助换绑收成「五扇 JSON 窗 + confirm 冻栅栏 + 失败不切口袋」。计划里至少有四套可数对象，派单点名的是 **Controller 五法 + (b) confirm + D 失败路径**：

1. **OBJ-36 原文。** `course.md`：能口述 `PersonRebindController` probe/match/confirm/submit/unbind。档是方法性状。点名的是五方法，没有把 `publishApprovedRebind`、listener、厨房工厂、企业 transfer 写成口试清单，也没有写出「失败不改绑定」这半句。
2. **chain 覆盖列。** 只有 `A:PersonRebindController.probe,match,confirm,submit,unbind`。不写 (b)，不写 (d)。
3. **Lesson `claimed_cells` / 矩阵三格。** (a) 与磁盘五个 `@PostMapping` 1:1；(b) 原文「改绑定；失败保持原绑定」；(d) 行名「失败路径 profile 换绑」，备注只写 match/confirm。课把 D 的中英名写成同一行别名，不是两行。
4. **课自己的操作定义。** (a) 门牌 `/profile/person/rebind`，全 POST、全 `profile:person:apply`、全 `@Log` 且不存报文，参数表无 `long`/`Long`，注入类型是 `PersonRebindUseCase`；(b) confirm **改的是申请单** `rebind_intent='Y'` + 三件套栅栏 + version+1，**不是**立刻 `unbind` 旧 `profile_person_binding`；失败抛类别码，UseCase `@DSTransactional` 回滚，旧行仍 `ACTIVE`；(d) match 软拒绝零写；confirm 硬拒绝回滚；submit/publish 栅栏失败同构不切口袋。真正换口袋是工作流 `FINISH` → `publishApprovedRebind`，无 HTTP 映射。
5. **邻格。** L-035 普通申请 INSERT 写死 `rebind_intent='N'`，被占用时喊 `PERSON_REBIND_CONFIRMATION_REQUIRED` 把人推向本门。L-034 `manageBinding` / `decide` 看见 `rebindIntent=Y` 会拒。L-039 才认厨房。L-042 企业 confirm **当场换口袋**。L-038 核身回调。矩阵 (d) 主路径申请挂 L-035/L-034。

问：Lead 盖章时，(b)「改绑定」是必须等于 `update profile_person_binding`，还是接受课的操作定义（confirmIntent 只冻申请栅栏，旧 `ACTIVE` 直到 publish）就能标 covered？(d) 是否必须另建「submit/publish 栅栏失败」行，还是矩阵那句 match/confirm + 同河同构已经够？chain 只写 (a) 时，标 (b)/(d) 的证据锚是 `claimed_cells` 还是要先改 chain 覆盖列？listener / `publishApprovedRebind` 是不是必须升成 (a) 第六法才算五扇讲完？

## Q2 证据

- objective_id: OBJ-36
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 33–91；Handler 9–16；UseCase 17–27 / 32–55；UseCaseImpl 22–70；Service 65 / 152–247 / 252–338 / 364–445 / 479–489 / 646–649 / 741–749 / 809–846；Dao 28 / 75–76；XML 7–40 / 42–48 / 68–87 / 110–117 / 160–167；ContractTest 24–64；ServiceTest 71–153；PersistenceTest 49–129；E2E 70–186 / 188–211；测试夹具 ServiceImpl 23–73；Listener 28–37；NotificationListener 17–19；frontend `service.ts` 16–28；`permissions.ts` 7；登记表 layered 行；DML 534 / 1508
- difficulty: high

课的五扇、(b) confirm 与 D 哪些能在工作树直接复现，哪些只是测试名单比课表窄、或邻格事实？

**仍能复现（本课三格）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 门牌 `/profile/person/rebind`；正好五扇 `@PostMapping`：`/probe` `/match` `/confirm` `/submit` `/unbind`；无 `BaseController` | Controller 33–91；合同测试 26–30 |
| 五扇共用 `profile:person:apply`；参数类型不含 `long`/`Long`；`@Log` 均 `isSaveRequestData/isSaveResponseData=false` | Controller 每方法；合同测试 31–39；DML 菜单种子同串 |
| `BusinessType`：confirm / unbind = `UPDATE`，probe / match / submit = `OTHER` | Controller 43–44 / 54–55 / 65–66 / 76–77 / 87–88。合同测试**不**断言 BusinessType |
| 字段名 `service`，类型 `PersonRebindUseCase` | Controller 36 |
| probe 不调 `LoginHelper`；其余四扇 `LoginHelper.getUserId()` | Controller 46 vs 57 / 68 / 79 / 90 |
| handler `@RestControllerAdvice(basePackageClasses = PersonRebindController.class)`，`R.fail(exception.getMessage())` | Handler 9–16 |
| VO：probe 只有 `status`；match `status, maskedPhone`；confirm `status, maskedPhone, version` 且不含 `profileId/bindingId/userId/phone` | 三个 record；合同测试 45–54 |
| wta-profile = layered；事务在 UseCaseImpl 五个业务方法 + `publishApproved` + `handleProcess` 的 `@DSTransactional`；listener `handle` **无**该注解 | 登记表；UseCaseImpl 27–70；合同测试 61–63 |
| 生产注入具体 `PersonRebindService`；`IPersonRebindService` **没有**生产 `implements` | UseCaseImpl 24；全仓 grep 空 |
| 接口无 userId 的 match/confirm/submit/unbind `@Deprecated` 默认抛 `UnsupportedOperationException("请传入 userId")` | UseCase 17–27 |
| 测试夹具 `PersonRebindServiceImpl` 在 `src/test/...`：`extends PersonRebindService implements PersonRebindUseCase`，无 userId 重载改走 `LoginHelper`；生产入口不是它 | 夹具 23–63 |
| DAO 持 Mapper；生产 Service **不** import `PersonRebindMapper`；Mapper 虽 `extends BaseMapperPlus`，换绑 SQL 是手写 XML | Dao 26–28；Service import 表；XML 全文 |
| probe：`identityKey = upper(type)+":"+upper(number)`；SQL 四态 BOUND（含 SUSPENDED）→ IN_PROGRESS → UNBOUND → AVAILABLE；VO 无档案号 | Service 152–162；XML 7–26；E2E 80–86 `personProfileId` 不存在 |
| probe body 只 `documentTypeCode`≤64 + `documentNumber`≤128 | ProbeBo 9–10 |
| match 任一条不可用即 `unavailable()` = `NOT_AVAILABLE` + `maskedPhone=null`，**不抛**；申请人已有有效档案 / 无开放可编辑申请 / 身份不全等 / 无精确候选 / `oldUserId==userId` | Service 166–181 / 844–846 |
| 精确候选：档案 ACTIVE + 绑定 **只** ACTIVE + 姓名/证件/`identity_key`/性别/生日/`valid_from <=> ` / `valid_until <=> ` | XML 28–40 |
| 精确匹配才查 `UserService.selectPhonenumberById`；成功 `138****8000`；短号 `<8` → `****` | Service 180 / 809–816；ServiceTest 71–98 |
| match **无** INSERT/UPDATE | Service.match 全文只读 |
| confirm 硬前置失败 `PersonRebindException`；`requireIdentity` 把内部 IDENTITY_* 收成 `PERSON_REBIND_NOT_AVAILABLE` | Service 186–205 / 741–749 |
| `lockOpenApplication` SQL 含 WAITING；下一步 `EDITABLE_STATUSES = DRAFT,BACK,CANCEL` 挡掉 | XML 42–48；Service 65 / 193–196 |
| 申请人已绑定 → `PERSON_REBIND_APPLICANT_ALREADY_BOUND`；候选缺失或自己 → `NOT_AVAILABLE`；`lockFrozenCandidate` 对不上 → `NOT_AVAILABLE`；`confirmIntent` 非 1 行 → `VERSION_CONFLICT` | Service 198–201 / 407–414 / 396–402 |
| `confirmIntent` XML **只** UPDATE `profile_person_application`（`rebind_intent='Y'` + 三件套 + version+1），WHERE 仅 `DRAFT\|BACK\|CANCEL`；没有 binding 表 | XML 80–87；Dao 75–76 |
| 单元：`confirmationFreezesTheCurrentBindingWithoutSwitchingIt` → version=4，`never().unbindBinding(202)` | ServiceTest 102–118 |
| E2E confirm 200 + `CONFIRMED` + version=1 后立刻读旧 binding 仍 `ACTIVE` | E2E 98–106 |
| submit body 只有 `expectedVersion`；冻材料快照、WAITING、`workflow.start`；`never().unbindBinding` | Service 210–238；ServiceTest 122–138；E2E 107–117 |
| 栅栏空 → `PERSON_REBIND_CONFIRMATION_REQUIRED`；锁空/身份变 → `PERSON_REBIND_BINDING_CHANGED`，不插 submission、不开工单 | Service 419–428；ServiceTest 142–152 |
| submission 拷贝 `rebindIntent/target/expectedBinding*` | Service 646–649 |
| 核身指纹 `identityKey + snapshotVersion + REBIND` 的 SHA-256 | Service 820–821 |
| unbind 无 body；解**当前登录人**有效绑定；事件 `SELF_SERVICE` / `PERSON_SELF_UNBOUND`；`never().updateProfile`；档案仍 ACTIVE | Controller 89–90；Service 243–247 / 434–445；XML 160–167；PersistenceTest 115–128；E2E 143–150 |
| listener `@EventListener` + `@Order(HIGHEST_PRECEDENCE)`，无 HTTP | Listener 28–37；合同测试 57–63 |
| `publishApprovedRebind`：`rebind_intent != Y` → `Optional.empty()`；锁不到期望 binding → `PERSON_REBIND_BINDING_CHANGED`，在 supersede/unbind/insertBinding/finish **之前**停 | Service 255–272；PersistenceTest 84–96 / 99–111 |
| E2E 竞态：人工 `binding_version=2` 再 publish，抛 `BINDING_CHANGED`，rollback 后申请 WAITING、旧 ACTIVE、申请人 0 绑定、版本仍 1、通知审计 0 | E2E 156–185 |
| 成功 FINISH 后旧 UNBOUND、申请人 1 条 ACTIVE、申请 FINISH | E2E 119–134 |
| 通知 `@DsTxEventListener` AFTER_COMMIT；E2E 渠道 offline：审计 FAILED 两行，绑定已切完 | NotificationListener 17–19；NotificationTest 57–59；E2E 136–141 |
| 前端五枪 URL 全 `post`；confirm `{ identity, expectedVersion }`；unbind 无 data；权限常量 `profilePermissions.person.apply` | `service.ts` 20–27；`permissions.ts` 7。格子仍归 L-039 |

**邻格仍能复现（不计入本行闭合）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 普通申请被占用喊换绑确认 | L-035 / ApplicationController 路径，本课只作入口对照 |
| 管理端 decide 看见 `rebindIntent=Y` 拒 | L-034 发布端口 |
| 企业 confirm 当场换口袋 | L-042 `EnterpriseTransferController` |
| 厨房工厂 | L-039 `createPersonRebindService` |

**课写过、测试名单更窄或夹具与生产分叉（不是证伪）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「错误姓名不会去查手机」 | 单元：成功+错误姓名之后 `verify(users).selectPhonenumberById(202L)` 默认 times=1。E2E 错名只断言 JSON 无 `maskedPhone`，**没有** `never()` 查号 |
| 「confirm 失败事务回滚」 | 生产：UseCaseImpl `@DSTransactional`。E2E HTTP 夹具是 `new PersonRebindController(PersonRebindServiceImpl)`，**绕过** UseCaseImpl 事务，靠 XML 根本不写 binding + 成功后再 `session.commit()` 读仍 ACTIVE |
| match/probe 也挂 `@DSTransactional` | UseCaseImpl 27–37 有。课已写「match 无写，无事务可回」 |
| submit 身份不全 | confirm 收成 `NOT_AVAILABLE`；submit 直接 `validateComplete`，可冒 `PERSON_REBIND_IDENTITY_INVALID` |
| Jackson 省略 null | E2E 错名 `maskedPhone` `doesNotExist()`；单元是 `isNull()` |
| E2E 通知 | 主路径测试直接 `notifyOldAccount`；注解 AFTER_COMMIT 在 `PersonRebindNotificationTest` |

问：在五 POST、一把权限、VO 不露内部号、confirmIntent 只改申请单、`never unbindBinding`、E2E confirm/submit 后旧 ACTIVE、publish 竞态写库前停、通知失败不撤回已经能互证的前提下，(a)(b)(d) 是否还要把「E2E 夹具无 UseCase 事务」「错名查号无 E2E never」「submit 可冒 IDENTITY_INVALID」写成开篇半句才算讲完？还是这些已经落在课的分层/反例里，Lead 可以直接盖三格？

## Q3 假设与反例

- objective_id: OBJ-36
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: confirmIntent 无 binding 表；match `unavailable()`；probe 无 userId；五扇全 POST；注入 UseCase 非 IPersonRebindService；测试夹具不在 main；unbind 不删档案；submit never unbind；listener HIGHEST_PRECEDENCE 且 rebind_intent!=Y 则 empty；handler 类别码；UPDATE 只表示申请单 version；申请人已绑定不可用；oldUserId==userId 不可用；VO 禁内部号；handleProcess 无 RequestMapping；企业 confirm 另一扇门；通知失败≠D；SUSPENDED 只进 probe BOUND
- difficulty: high

课的隐含假设若反转，合格读者会把哪把枪或哪张表认错？

1. **「confirm 成功 = 旧账号已经解绑。」** 测试名与 E2E 都说还没有。课反例 1 / 误区 2–3。
2. **「矩阵 (b) 写『改绑定』所以成功时 binding 已换人。」** 磁盘把「改」做成意图栅栏。口试要拆两层。课误区 3。把假设反转后，Lead 会用本行假闭合 `publishApprovedRebind` 或拒绝盖章。
3. **「match 失败会抛 `PERSON_REBIND_NOT_AVAILABLE`。」** match 走软拒绝 200 + `NOT_AVAILABLE`。那条码是 confirm 的硬拒绝。课反例 2 / 误区 4。
4. **「probe 用当前登录人的档案。」** probe 不读 userId。课反例 3。
5. **「五扇窗有 GET / unbind 是 DELETE。」** 合同测试每方法 `@PostMapping`。课反例 4。
6. **「Controller 注入 `IPersonRebindService`。」** 注入 UseCase。接口生产未接线。课反例 5。
7. **「这是 classic，Service 直接持 Mapper。」** layered；生产 Service 持 DAO。`PersonRebindServiceImpl` 在 test。课反例 6 / 18。
8. **「unbind 删除 `profile_person`。」** 只改 binding。课反例 7。
9. **「submit 会把旧绑定换过来，因为已经 CONFIRMED。」** `never unbindBinding`。课反例 8。
10. **「换绑发布走普通 `PersonApplicationProcessListener`。」** rebind 监听器最高优先级；`rebind_intent != Y` 时 empty。课反例 9。
11. **「失败码是中文 msg。」** 类别码原样进 `R.fail`。课反例 10。
12. **「confirm 的 `BusinessType.UPDATE` 表示更新了 binding。」** 更新申请单 version 与意图。课反例 11。
13. **「申请人已经绑定别的档案也能换。」** `requireApplicantUnbound`。课反例 12。
14. **「候选人是自己也能 match。」** `unavailable()`。课反例 13。
15. **「VO 会回 `profileId` 给前端接着调管理端 binding。」** 合同禁止。课反例 14。
16. **「`handleProcess` 是第六个 HTTP 方法。」** listener → UseCase，无映射。课反例 15 / 误区 13。
17. **「企业 `EnterpriseTransferController.confirm` 和本课 confirm 同一套。」** L-042，当场换口袋。课反例 16。
18. **「D 格子包含通知失败回滚绑定。」** 通知在事务后；失败只记审计。课反例 17 / 误区 15。
19. **「D 覆盖管理端 `manageBinding` 失败。」** L-034。课误区 14。
20. **「OBJ-36 包含 `createPersonRebindService`。」** OBJ-39。课误区 1。
21. **「SUSPENDED 也能被 match 选中。」** probe BOUND 含 SUSPENDED；候选只要 ACTIVE。课误区 11。
22. **「deprecated 无 userId 方法会自己从 LoginHelper 取。」** 生产默认抛；自己取的是测试夹具。课误区 16。
23. **「必须另建 (d) 主路径换绑才能讲完真正换口袋。」** 矩阵没有该行。publish 成功是 (a)/(b)/(d) 的对照收尾，不是第四格。

问：读完后仍说不出的，是不是「五扇的名字」，而是 **confirm 不切 binding、match 软拒绝、D 不含通知失败、unbind 不是换绑收尾枪、listener 不是第六扇 HTTP、矩阵口号『改绑定』≠ UPDATE binding 表**？

## Q4 视角

- objective_id: OBJ-36
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(d)；OBJ-36 vs L-034/L-035/L-037/L-038/L-039/L-042；chain 只 (a)；无主路径换绑行；(c) MySQL 8.4
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于本课三格？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 五法 vs OBJ 一句；(b) 口号「改绑定」vs XML 只改申请单；(d) 备注只写 match/confirm，课加 submit/publish 同构；chain 不写 (b)/(d) | **是** 闭合定义。(a)(b)(d) 可按课的操作定义盖章。不要发明 listener 行或「主路径 profile 换绑」 |
| 控制面作者 | 五 POST、一把权限、VO 组件名、probe 无 userId、handler 类别码 | **是** (a) |
| (b) confirm 作者 | confirmIntent 三件套、never unbind、成功仍 ACTIVE、失败回滚 | **是** (b)。publish 换口袋 **否** 本行副作用的成功半边，只作对照 |
| D 失败路径 | match 零写；confirm 硬拒绝；submit `BINDING_CHANGED` 不开工单；publish 竞态写库前停 | **是** (d)。通知失败 **否** |
| UseCase 电工 | `@DSTransactional` 在 Impl 不在 listener；测试夹具绕过事务 | 生产边界 **是**。(E2E 夹具分叉是精度，不是第二格) |
| 申请窗（L-035） | current/submit；INSERT `rebind_intent='N'`；占用时确认码 | 入口一句 **是**。current/save **否** |
| 管理端（L-034） | `manageBinding` / decide 拒 Y | 负空间一句 **是**。十二扇 **否** |
| 核身邮筒（L-038） | submit 打 attempt；回调盖章 | 指纹含 REBIND **是** 对照。匿名回调 **否** |
| 材料（L-037） | ALWAYS 闸；WORKING→SUBMISSION | submit 前置 **是** 一句。attach/detach **否** |
| 厨房（L-039） | 五枪 URL 全 post | 对照 **是**。工厂格子 **否** |
| 企业转移（L-042） | confirm 当场换口袋 | 对照篱笆 **是**。三扇窗 **否** |
| 工作流 | FINISH + 最高优先级 listener | 换口袋触发器 **是** 对照。请假流 **否** |
| 通知渠道 | AFTER_COMMIT；失败不撤回 | 用来把 D 收窄 **是**。Outbox **否** |
| MySQL 8.4 / Redis 8 | 表在 10/50 基座；无本课 Redis | **否**。不要因 XML 标 (c) covered |
| API-005 | 五扇已是 POST+`@Log`；probe/match 偏查询仍 POST | 盘点 **是**。不发动传输 Ratchet **否** |

问：Lead 若凭「五扇都在函数表里」把 (a) 标 covered，是否还要求读者能指着 **confirmIntent 不改 binding、match 200 软拒绝、publish 竞态旧行仍 ACTIVE**？这些是不是已经在 (b)/(d) 的课内表/反例里，而不需要新行、也不需要把 listener 算进 (a)？

## Q5 元问题

- objective_id: OBJ-36
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 五法、(b) confirm、(d) 失败路径 profile 换绑 均 uncovered；chain 只 (a)
- difficulty: low

建议：`defer`

原因：

- **三格与磁盘 1:1（在课的操作定义下）。** (a) 五个 `@PostMapping`；(b) confirm 冻申请栅栏、失败保持原 `ACTIVE`；(d) match 软拒绝零写 + confirm 硬拒绝回滚，submit/publish 栅栏失败同构。L 合同章节齐全（宏观地图、直觉、定义表、机制链 1–6、带 caption / 文字等价物的控制面图与 D 时间线、正反例、变式、误区、暂停走查、来源表 S-004/S-008/S-010/S-L036-01…08）。**主叙事站得住且与源码对齐**：全 POST 一把权限；confirmIntent 无 binding 表；单元 `never unbindBinding`；E2E confirm/submit 后旧 ACTIVE、FINISH 后才切换；竞态 `PERSON_REBIND_BINDING_CHANGED` 写库前停；通知失败不撤回。不必为「五扇的名字」再挖五问。
- 本批可粘贴的精度项已经落在课的分层/反例或测试边界，不挡住把**三格**标 covered：（1）E2E HTTP 夹具走测试 `PersonRebindServiceImpl`，生产事务在 UseCaseImpl；（2）错名不查手机：单元 times=1，E2E 只锁 JSON；（3）submit 身份闸可冒 `IDENTITY_INVALID`，confirm 收成 `NOT_AVAILABLE`；（4）probe/match 也挂事务注解但 match 无写。不要把厨房工厂、企业当场换口袋、管理端 `manageBinding`、核身回调、listener 第六法塞进本三格。
- 不建议 `mine-more`：b01 未证明本行还有真实矩阵空洞；再五问只会把 L-039 厨房、L-042 企业 confirm、L-034 管理端或 L-038 回调再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；矩阵口号「改绑定」已被课拆成两层，不是缺章。不建议 `split-lesson`：未满 10 问；三格是同一 OBJ-36 的窗/性状/失败路径，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (d) 主路径换绑行，也不建议发明 listener (a) 行。
- chain 覆盖列只写 (a)。标 (b)/(d) covered 的证据锚是本课 `claimed_cells` + 本 probe，不是先改 chain。miner 不改 chain。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:PersonRebindController.probe, match, confirm, submit, unbind | 门牌、五 POST、一把 `profile:person:apply`、`@Log` 不存报文、参数无 long、probe 无 userId、VO 组件名、layered UseCase 事务、非 `IPersonRebindService`——均与源码/合同测试对齐。unbind 是解自己，不是换绑 HTTP 收尾。 | 可选半句：E2E HTTP 夹具绕过 UseCaseImpl 事务，口试仍以生产 `@DSTransactional` 为准。Lead 可将本行标 `covered`（证据锚本 probe 与 L-036 来源表 S-L036-01…05）。 | defer（本行可 covered） |
| B:PersonRebindController.confirm | 矩阵口号「改绑定」；磁盘 confirmIntent 只 UPDATE 申请单；成功 never unbind、E2E 仍 ACTIVE；失败类别码 + 生产事务回滚。残留精度：E2E 夹具无 UseCase 事务；合同测试不锁 BusinessType。 | 不要把口号改写成必须 `update profile_person_binding` 才盖章。课已拆「冻栅栏 / 换口袋」。Lead 可将本行标 `covered`。 | defer（本行可 covered；口号≠XML 表名不挡盖章） |
| D:fail-profile-rebind / D:失败路径 profile 换绑 | 矩阵备注 match/confirm。match 软拒绝零写；confirm 硬拒绝不改 binding；submit/publish 栅栏失败同构（单元拒 submit、持久化/E2E 竞态写库前停）。通知失败已从 D 剔除。 | 不要另建 submit/publish 失败行，也不要发明主路径换绑行。Lead 可将本行标 `covered`。 | defer（本行可 covered） |
| publishApprovedRebind / PersonRebindProcessListener | 课已讲 FINISH 换口袋与 Order。矩阵 (a) **无此符号**。 | 保持对照，不升第六法。 | defer（未入库对照；不走 b02） |
| A:PersonApplicationController.current, submit | 占用时确认码把人推向本门。 | 保持。 | defer（L-035） |
| A:PersonAdminController.* | decide/manageBinding 负空间。 | 保持。 | defer（L-034） |
| A:PersonMaterialSelfController.* / MaterialTag | ALWAYS 闸一句。 | 保持。 | defer（L-037） |
| A:PersonVerificationAnonymousController.callback | attempt 指纹含 REBIND。 | 保持。 | defer（L-038） |
| A:createProfileService / createPersonRebindService | 五枪 URL 对照。 | 保持。 | defer（L-039） |
| A:EnterpriseTransferController.send, confirm, unbind / B:confirm | 当场换口袋对照。 | 保持。 | defer（L-042） |
| D:主路径 profile 申请 | 矩阵挂 L-035/L-034。 | 不要因本课标该行 covered。 | defer（L-035/L-034） |
| C:MySQL 8.4 / Redis 8 | XML 表不是容器课。 | 不要假闭合 (c)。 | defer（(c) 仍 uncovered） |
| IPersonRebindService | 生产未接线。 | 不是矩阵行。 | defer（未入库） |
| API-005 变更 POST+`@Log` | 五扇已对齐；probe/match 偏查询仍 POST。 | 本课盘点，不发动 Ratchet。 | defer（传输 Ratchet） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本三格无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出五扇目的、confirm 主路径（冻栅栏不换口袋）与失败路径（match 软拒绝 / confirm 回滚 / publish 竞态旧 ACTIVE）；矩阵口号与 XML 表名之差已在课内拆开，不是第二容器；把可选半句写进原课不会超过 40 分钟；申请窗、管理端、材料、核身、厨房、企业转移已有邻课。满 10 问前不拆。五窗与 (b)/(d) 同属 chain 原设计的 OBJ-36，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-034 管理端、L-035 申请、L-037 材料、L-038 核身、L-039 厨房、L-042 企业转移、(d) 主路径申请、(c) MySQL/Redis、未入库 listener/`IPersonRebindService`、API-005 显式建议 `defer`；对 `A:PersonRebindController.probe,match,confirm,submit,unbind`、`B:PersonRebindController.confirm`、`D:fail-profile-rebind`（矩阵行「失败路径 profile 换绑」）建议 Lead 标 `covered`（证据锚本 probe 与 L-036 来源表）。本课不主张改邻课符号、发明主路径换绑行，或把 listener 算进 (a)。
