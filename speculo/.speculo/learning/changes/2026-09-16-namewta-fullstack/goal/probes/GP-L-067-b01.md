---
artifact: learning-goal-probe
probe_id: GP-L-067-b01
lesson_id: L-067
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-067 / b01

审问对象是已写成的 Lesson `lessons/L-067-flw-category.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **一格**：`A:FlwCategoryController.*`。OBJ-67 原文：能口述 `FlwCategoryController`。档是**解释+走查**。矩阵 (a) **有独行**，七名与磁盘 1:1：`list, export, getInfo, add, edit, remove, categoryTree`，模块 `wta-workflow`，仍 uncovered，备注空。矩阵 (a) **没有** `IFlwCategoryService` / `queryCategory` / `CategoryPage.vue` 行。厨房 `createWorkflowDefinitionService` 与九工厂行里的 `createWorkflowWebDomain` 仍 uncovered（L-073）。矩阵 (b) **没有** 分类 601 / `100L` / 无页导出独行；workflow 已印的 (b) 是 `IFlwTaskService` 五支副作用与 `TestLeaveController.submitAndFlowStart`，均 uncovered，挂 L-070 / L-072。矩阵 (c) `wta-modules` / `layered-classic` 已 covered（L-002 / L-003）；登记表 classic 行就是 `wta-modules/wta-workflow`。MySQL 8.4 / Redis 8 / `dao-mapper-ownership` 仍 uncovered。矩阵 (d) **没有** 分类 CRUD 走廊；已 uncovered 的「主路径 工作流请假」原文仍是 `TestLeave.submitAndFlowStart → 任务完成`（挂 L-072, L-070）。本批按派单只审问这一格 (a) 切片；口试按磁盘：同一门牌 `/workflow/category` **正好 7** 个公开映射，classic 三跳，门卫只抱 `IFlwCategoryService`，`export` 在 Java+菜单、厨房与分类页没收，`categoryTree` 无权限字，`queryCategory` 不是第八扇 HTTP，`100L` ≠ 种子根。不要把 Service/Mapper 升成第二张 (a)，不要发明 (b)，也不要把 L-068…L-073 六扇邻课算进本切片。

抽查锚点（2026-09-17）：`FlwCategoryController.java`；`IFlwCategoryService.java`；`FlwCategoryServiceImpl.java`；`FlwCategoryMapper.java` 与空 XML；`FlowCategory` / `FlowCategoryBo` / `FlowCategoryVo`；`FlowConstant`；`ConditionalOnEnable`；`CategoryNameTranslationImpl`；`application.yml` `warm-flow`；对照 `FlwDefinitionController` / `FlwInstanceController` / `FlwSpelController` / `FlwTaskController` / `TestLeaveController`；`TreeBuildUtils`；`R.warn` / `HttpStatus.WARN` / `BaseController.toAjax` / `RepeatSubmit`；登记表 `03-backend-module-modes.md`；`30-cde-workflow.sql`；厨房 `domains/workflow/src/index.ts` 与 `index.test.ts`；`CategoryPage.vue`；`DefinitionPage.vue` / `InstancePage.vue` / `MyDocumentPage.vue`；web-domain `permissions` `workflow-category`；e2e `workflow-definition.spec.ts`；axios-browser `code === 601`；OpenAPI `/workflow/category**`。工作树 **零** `FlwCategory*Test`。

## Q1 澄清

- objective_id: OBJ-67
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-67；chain L-067 覆盖列只 `A:FlwCategoryController.*`；L-067 `claimed_cells` 一格；矩阵 (a) 七名独行仍 uncovered、无 Service/Vue 行；厨房与九工厂行仍 uncovered（L-073）；(b) 无本课符号、任务/请假挂 L-070/L-072；(c) 房间与登记表已 covered；(d) 无分类 CRUD 流，已 uncovered 的主路径是请假；课开篇七扇 + 负空间 L-068…L-073 / L-003 / vendor-engine
- difficulty: medium

课把分类抽屉收成「一块牌子、七扇窗、classic 三跳、500 与 601 两套信封」。计划里至少有四套可数对象，派单点名 **一格 (a) 切片**：

1. **OBJ-67 原文。** `course.md`：能口述 `FlwCategoryController`。一句里是 **走查**，不是 `FlwDefinitionController.publish`、不是 `WorkflowService.startWorkFlow`、也没有把厨房六枪写成第二张矩阵行。课把 OBJ 扩成 classic 抽屉 + 失败信封，那是走查，不是第二格。
2. **chain / 课 `claimed_cells`。** 一格。符号就是 `FlwCategoryController.*`。矩阵把 `.*` 展开成七个方法名，与磁盘公开映射 1:1。chain **不**写 Service、不写 Vue、不写 (b)。
3. **矩阵 (a) 对齐。** 本课有独行，Lead 按现印七名标 covered **不会**把定义十三扇或厨房假闭合。不要把 `IFlwCategoryService` 或 `queryCategory` 另立一行才给本切片盖章。厨房独行与九工厂行仍 uncovered，本课不得当第二张前端章。
4. **课自己的操作定义。** 门牌 `/workflow/category`；GET 三 / POST 二 / PUT 一 / DELETE 一；无类上 `@SaIgnore`；六扇贴权限字，`categoryTree` 故意不贴。门卫只注入 `IFlwCategoryService`，不抱 `DefService`。`list` 扁 `List` 不分页；`export` 同一 `queryList` 改 Excel 流、返回 `void`；`getInfo` 无行仍 `R.ok(null)`；`add`/`edit` 重名 `R.fail` 500 + `@RepeatSubmit` 默认 5000ms；`edit` 另闸「父不能是自己」；`remove` 三道 `R.warn` 601 后才逻辑删；设计器 `queryCategory` 不是 HTTP。
5. **导出与树不是第二张 claimed 格。** OBJ 要「能口述七扇」。课把「Java+菜单有 export、分类页没扳机」和「管理页 `list`+`handleTree`、图纸/实例页才打 `categoryTree`」收进对照走查。不要为了盖章把 `CategoryPage.vue` 再立一行，也不要把无页导出降成「本课不认这扇窗」。
6. **邻格。** L-068 发布/导入。L-069 实例与 `WorkflowService`。L-070 任务枢纽。L-071 SpEL `/{ids}`。L-072 请假 `submitAndFlowStart`。L-073 厨房 URL 与十二键菜单。L-003 已认 classic，本课不得再盖 `C:layered-classic`。Warm-Flow 引擎内部保持 `deferred(vendor-engine)`。

问：本格闭合标准是「七扇路径/动词/权限/信封 + classic 三跳门卫不抱引擎 + `100L` ≠ 种子根 + list 扁 / tree 后端 `label` / 设计器侧门 + 厨房六枪没收 export、逗号删除不是后端批量，且不升 Service/Vue 矩阵行、不发明 (b)、不提前盖 L-073」？还是 OBJ 那句必须把七个方法拆成七行、或必须等 GP-L-073 厨房缺枪才给本切片盖章？Lead 若凭「workflow 房间还没挖完」把这一行留空洞，是否假判成未讲完？Lead 若用本课把厨房或请假走廊标 covered，是否假闭合了 L-073 / L-072？

## Q2 证据

- objective_id: OBJ-67
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 31–147；ServiceImpl 34–40 / 59–69 / 97–123 / 132–138 / 148–178 / 188–193 / 203–210 / 219–244 / 276–279；Mapper 17–44；XML 5–7；Entity 16–59；Bo 15 / 30–48；Vo 16 / 32–67；FlowConstant 34 / 39；ConditionalOnEnable 27；yml 408–410 / 423；Translation 22–40；TreeBuildUtils 33 / 99–109；R 183–184；HttpStatus 92；BaseController 19–20；RepeatSubmit 20；modes.md 22；pom bundle-core 195；SQL 168–185 / 267–280；DefinitionController 39–52；InstanceController 40–41；SpelController 38 / 47–50 / 101–103；厨房 index.ts 37–43 / 338–344；index.test.ts 15–20 / 45–46 / 86–91；web-domain 66–71；CategoryPage 34–38 / 195–208 / 245–253 / 274–277；DefinitionPage 12 / 256 / 447–457；InstancePage 177–179；MyDocumentPage 98–100；axios 225–228；e2e 100–111 / 128–133 / 223–235 / 306–307 / 328–357；OpenAPI 1076–1079 / 3680–3691；零 FlwCategory*Test
- difficulty: high

课的七扇 + classic 抽屉哪些能在工作树直接复现，哪些只是测试名单比课表窄、或口试把邻课门卫说成「只有分类才单字段」？

**仍能复现（本切片）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 类上 `@ConditionalOnEnable` `@Validated` `@RequiredArgsConstructor` `@RestController` `@RequestMapping("/workflow/category")`，继承 `BaseController` | Controller 31–36 |
| 构造只注入 `IFlwCategoryService`；无 `DefService` / `LoginHelper` / `PageQuery` / `@DataPermission` / `@SaIgnore` | Controller 38；模块内 `DataPermission` grep 空 |
| 七扇：`GET /list`、`POST /export`、`GET /{categoryId}`、`POST /`、`PUT /`、`DELETE /{categoryId}`、`GET /categoryTree` | Controller 46–147 |
| `list` 权限 `workflow:category:list`，回 `R.ok(List)`，不是 `PageResult` | 46–50；对照 SpEL `PageResult`+`PageQuery` 47–50；定义 list 49–52 |
| `export` 权限 export、`@Log EXPORT`、`void`、同一 `queryList` → `ExcelBuilder` 表名「流程分类」 | 59–65 |
| Excel 只出 id / 名称 / 顺序 / 创建时间；`parentName` / `ancestors` 无 `@ExcelProperty` | Vo 32–67 |
| `getInfo` 权限 query、路径 `@NotNull`、`R.ok(queryById)`，无行不 fail | 73–77；ServiceImpl 49–50 |
| `add` 权限 add、`@Log INSERT`、`@RepeatSubmit()`、AddGroup；重名 `R.fail` 不 insert；过闸 `toAjax(insertByBo)` | 85–94 |
| `edit` 权限 edit、`@Log UPDATE`、`@RepeatSubmit()`、EditGroup；重名或 `parentId.equals(categoryId)` → `R.fail` | 102–114 |
| `remove` 权限 remove、`@Log DELETE`、**无** `@RepeatSubmit`；路径单个 `Long` 不是 `Long[]` | 122–136；对照 SpEL `/{ids}` `Long[]` 101–103 |
| 删除三道 `R.warn`：`FLOW_CATEGORY_ID.equals` / `hasChildByCategoryId` / `checkCategoryExistDefinition`，短路不调 `deleteWithValidById` | 126–136 |
| `categoryTree` **无** `@SaCheckPermission`、无 `@Log`、无 `@RepeatSubmit` | 144–147；OpenAPI 3688–3690 描述无权限字块 |
| `@RepeatSubmit` 默认间隔 5000ms | RepeatSubmit 20 |
| `toAjax(rows>0)` 才 ok，否则 fail | BaseController 19–20 |
| `R.warn` → `HttpStatus.WARN=601`；axios `code===601` → `kind:'warning'` 并 reject | R 183–184；HttpStatus 92；axios 225–228 |
| 电闸 `warm-flow.enabled=true`；LiteFlow `enable` 跟着同一把 | ConditionalOnEnable 27；yml 408–410 / 423 |
| classic：Impl 抱 `FlwCategoryMapper` + 删前 `DefService`；另实现 `CategoryService` | ServiceImpl 37–40 |
| `queryList`：`eqIfPresent` id/parent、`likeIfText` 名称、按 ancestors/parentId/orderNum/categoryId 升序 | 188–193 |
| 树：空列表 `newArrayList`；非空 `buildMultiRoot`，`setName(categoryName)`；全局 `nameKey` 被改成 `"label"` | ServiceImpl 109–123；TreeBuildUtils 33 / 99 |
| `queryCategory` 返回 Warm-Flow `core.dto.Tree`（id/name/parentId），**无** RequestMapping | 132–138 |
| 同父同名 `exists` 则不唯一；编辑 `neIfPresent(categoryId)` | 148–154 |
| 删前定义闸：`setCategory(id.toString())` + `defService.exists` | 164–168 |
| `hasChild`：`parentId=该 id` 的行 `exists` | 177–178 |
| 新增：父行空 → `ServiceException("父级流程分类不存在!")`；`ancestors=父.ancestors + "," + parentId`；无 `@CacheEvict`、无 `@Transactional` | 203–210；`SEPARATOR=","` |
| 改：`@CacheEvict` + `@Transactional`；无行 / 顶级父改非 0 / 新父不存在 抛异常；父变则 `findInSet` 改子孙 ancestors | 219–244 |
| `deleteWithValidById` 只 `@CacheEvict` + `deleteById`，不再验孩子 | 276–279 |
| 译名缓存 `flow_category_name#30d` 在 `selectCategoryNameById`，无 `unless` | 59–69；FlowConstant 34 |
| `FLOW_CATEGORY_ID=100L`；种子根 `1762300000000000100` / `parent_id=0` / `ancestors='0'` / 名「OA审批」 | FlowConstant 39；SQL 184–185 |
| `@TableName("flow_category")` + `@TableLogic delFlag`；JavaDoc 仍写 `wf_category` | Entity 16–23 / 58；Bo/Vo 15–16 |
| Mapper XML 空壳；`findInSet(ancestors)` default；`selectCategoryIdsByParentId` 含自己 | Mapper 25–44；XML 5–7 |
| 定义/实例/任务 ServiceImpl 都调这支展开器（课正文只点名实例） | Definition 107；Instance 162；Task 286 |
| 登记表 classic：`wta-modules/wta-workflow`，不改内部层次 | modes.md 22 |
| `bundle-core` 注释「不装配工作流引擎」；full 才含 `wta-workflow` | pom 195；ProfileModuleGraphContractTest 35–37 |
| 定义门卫另抱 `DefService`；实例另抱 `InsService`；SpEL/任务/请假也是单模块服务字段 | Definition 39–40；Instance 40–41；Spel 38 |
| 厨房六枪：list/get/add/put/delete/tree；**没有** export 方法 | index.ts 338–344；interface 259–281 无 exportCategory |
| `deleteCategory(['1','2'])` 测成 `/workflow/category/1,2`；斜杠/空格/逗号会 `encodeURIComponent` 但仍用逗号拼接 | index.test.ts 19 / 45 / 86–91 |
| 资源标签 `controller:'FlwCategoryController'` `basePath:'/workflow/category'` | category/index.ts 3–6 |
| `CategoryTreeVO` 显示名键是 `label` | index.ts 37–42 |
| web-domain `workflow-category` 五串 **不含** export；定义组 **含** `workflow:definition:export` | web-domain 66–71 vs 82 |
| 分类页工具栏只有新增+展开，无导出钮；`listCategories` + `handleTree(...,'categoryId','parentId')`；下拉树再打一次 `listCategories`；编辑先 `getCategory`；删除传 `row.categoryId` 单个 | CategoryPage 34–38 / 195–208 / 251 / 274–277 |
| 定义页 `categoryTree()`；`filter-field="label"`；`id==='0'` 清空筛选 | DefinitionPage 12 / 447–457 / 256 |
| 实例页 / 我的单据也喊 `categoryTree()` | InstancePage 177–179；MyDocumentPage 98–100 |
| 种子 C 型 `workflow/category/index` + F 型五颗，第 5 颗仍是 `workflow:category:export` | SQL 267–280 |
| e2e：分类页对「审批」行新增，POST body `{categoryName:'财务审批', parentId:'c1', orderNum:0}`；失败 500 见「分类保存失败」、零「操作成功」、对话框不关；list+query 藏新增钮 | e2e 230–235 / 306–307 / 328–357 |
| e2e 把 `categoryTree` 回成 `{id,parentId,label,weight,children}` | e2e 101–110 |
| OpenAPI `POST /export` 写了 export 权限；`GET /categoryTree` 描述无权限字 | OpenAPI 1076–1079 / 3688–3690 |
| 无 Java `FlwCategory*Test` | 工作树搜空 |

**课写过、测试名单更窄或口试把邻课门卫说偏（不是证伪本格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 暂停走查「只有分类这份门卫字段是单纯的 `IFlwCategoryService`」 | 字面真（只有这份注入该类型）。若读成「只有分类才单字段、不抱引擎」，则 SpEL / 任务 / 请假同样只抱自己的 `IFlw*Service`。准确句已在开篇：分类/SpEL/请假像普通柜台；**定义/实例**才直接抱 `DefService`/`InsService` |
| 「展开器在实例课认」 | Mapper 这支也被定义列表、任务查询调用。本课借用即可，不要把三处消费升成本格第二行 |
| 「测试锁死厨房六枪」 | 锁 URL + 逗号删除 + segment 编码。**没有** `exportCategory` 不存在的 `not.toContain`，也 **没有** 锁 `id==='web-domain-workflow'` |
| web-domain「权限组五串不含 export」 | 测试只 `toContain('workflow:category:query')`。五串全量与 export 缺席靠字面量，测试没点名 |
| 课 `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |
| `buildMultiRoot` 走 `label` | `DEFAULT_CONFIG` 把 hutool 全局 `nameKey` 改成 `label`；`buildMultiRoot` 自己没再传 config。前端 `CategoryTreeVO.label` 与 e2e mock 已互证 |

问：在七扇映射、classic 三跳、500/601、`100L`≠种子根、list 扁、export 无页扳机、tree 无权限字且 JSON `label`、`queryCategory` 非 HTTP、厨房六枪没收 export、删除单个 Long 已经能互证的前提下，(a) 是否还要把「暂停第 4 步措辞」「展开器也被定义/任务调用」「权限数组测试只锁 query」写成开篇半句，才算本切片讲完？还是这些已经落在课的表/反例里（暂停措辞除外），Lead 可以把本切片视为已讲、并保持厨房/九工厂行 uncovered？

## Q3 假设与反例

- objective_id: OBJ-67
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无第八扇 HTTP queryCategory；无批量 `/{ids}`；无 PageResult list；分类页不打 categoryTree；厨房无 exportCategory；表名 flow_category；100L≠种子根；无 FlwCategoryUseCase；门卫不注入 DefService；601 非成功；不能 POST parentId=0 新根；名称只同父唯一；deleteWithValidById 不再验；OBJ-67 不含厨房
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪道闸认错？

1. **「七扇都要 `workflow:category:*`。」** `categoryTree` 没有权限注解。课反例 1 / 误区 3 覆盖。
2. **「`list` 是分页树。」** 扁 `List`，不分页；管理页树是 `handleTree`。课反例 2 / 误区 4 覆盖。
3. **「删除是 `/{ids}` 批量，和 SpEL 一样。」** 单个 `Long`。厨房测试的 `1,2` 绑不上。课反例 3 覆盖。
4. **「默认分类就是种子 OA 审批。」** 常量 `100L`；种子雪花 id。课反例 4 / 误区 8 覆盖。
5. **「表名 `wf_category`。」** 注解和 DDL 都是 `flow_category`。课反例 5 / 误区 7 覆盖。
6. **「这是 layered，Service 不许碰 Mapper。」** 登记 classic。课反例 6 / 误区 14 覆盖。
7. **「门卫注入了 `DefService`。」** 定义课才直接抱引擎。课反例 7。
8. **「`queryCategory` 是第八扇 REST。」** Warm-Flow UI 插件口。课反例 8。
9. **「`R.warn` 仍是成功信封。」** code 601；axios reject。课反例 9 / 误区 12 覆盖。
10. **「分类页喊 `categoryTree`。」** 它喊两次 `listCategories`。课反例 10。
11. **「可以 POST 一个 `parentId=0` 的新根。」** `selectById(0)` 为空。课反例 11 / 误区 10 覆盖。
12. **「名称全局唯一。」** 只在同一 `parentId` 下唯一。课反例 12 / 误区 9 覆盖。
13. **「`deleteWithValidById` 会再查孩子。」** Valid 在门卫。课反例 13 / 误区 11 覆盖。
14. **「OBJ-67 包含 `createWorkflowDefinitionService`。」** OBJ-73。课反例 14 / 误区 1 覆盖。
15. **「导出在分类页工具栏。」** 2026-09-17 没有。后端窗仍在。课误区 6 覆盖。
16. **「JSON 树字段是 `name`。」** HTTP 树显示名键是 `label`。课误区 5 覆盖。
17. **「classic 等于每扇窗都一样。」** 定义/实例门卫会直接抱引擎。课误区 2 覆盖。
18. **「跨模块删分类走本课 REST。」** 跨模块合同是 `WorkflowService`。课误区 15 覆盖。
19. **「关电闸还能 GET /list 拿空数组。」** Bean 不注册。课正例 8 覆盖。
20. **「前端授权。」** 藏按钮 ≠ `@SaCheckPermission`；树这一扇连权限字都没有。课边界 7 / 误区 13 覆盖。
21. **「厨房能拼逗号 = 后端批量删。」** 管理页传单个 id；`1,2` 进不了三道黄灯。课图边界覆盖。

问：读完后仍说不出的，是不是「Controller 的名字」，而是 **七扇合同 ≈ 磁盘映射、缺的是厨房没收的 export 与逗号删除、`100L` 护不住种子根、`categoryTree` 无权限字、设计器侧门不是 HTTP、九工厂/厨房行不能因本课闭合、601 不是成功**？

## Q4 视角

- objective_id: OBJ-67
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-67 vs L-003/L-017/L-068…L-073；claimed_cells 七扇 vs 厨房六枪 / 一张分类页；厨房独行仍 uncovered
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这一格 (a) 切片**？

| 视角 | 多出来的边 | 是否本课这一格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 七名独行 vs chain `.*`；无 Service/Vue 行；(b) 无分类独行；(d) 无分类 CRUD 流，请假走廊挂 L-072 | **是** 闭合定义。本切片可视为已讲。独行可标 covered。不要发明 (b)/(d)，不要把一张 Vue 升成新行才能给切片盖章，不要用本课盖厨房章 |
| 分类门卫 | 七扇路径/动词/六串权限/树无 perm、500 vs 601、单个 Long 删除、`RepeatSubmit` 只罩 add/edit | **是** 切片里的 `FlwCategoryController.*` |
| classic 厨师 | 父存在/顶级父/ancestors/`DefService.exists`/`queryCategory` 侧门/译名缓存/空 XML | **是** 走查后果，不是矩阵第二格 |
| 导出窗 | Java+菜单+OpenAPI 有；厨房无枪；分类页无钮；请假页才有导出钮 | **是** 对照。不要把「没按钮」说成「没这扇窗」 |
| 两张树 | 管理页 list+handleTree；图纸/实例/我的单据 `categoryTree`+`label` | **是** 对照。不要升格成前端格子 |
| 厨房作者（L-073） | 六枪 URL；`segment()`；逗号删除；权限组漏 export | 本课借用封面形状与没收窗负空间 **是**。工厂盖章 **否**（L-073） |
| 定义窗（L-068） | 门卫抱 `DefService`；list 分页；筛分类时 `selectCategoryIdsByParentId` IN | 对照一句 **是**。发布/导入 **否** |
| 实例/任务（L-069/L-070） | 同样展开子孙；实例抱 `InsService`；任务十六扇 | 展开器旁证 **是**。大厅/枢纽 **否** |
| SpEL（L-071） | `/{ids}` 批量、list 分页、五扇全有权限字 | 批量对照 **是**。表达式目录 **否** |
| 请假（L-072） | `submitAndFlowStart`；(d) 走廊 | 负空间 **是**。示例流 **否** |
| 登记表（L-003） | classic 允许 Impl 抱 Mapper | 不改登记 **是**。房间画像 **否**（已 covered） |
| 部门树（L-017） | 整表 List + 有孩子不删；部门有 `@DataPermission`，分类没有 | 对照 **是**。部门房间 **否** |
| MySQL 8.4 | 表 `flow_category` + 种子根 + 菜单 F 型 export | 种子对照 **是** (a)。容器 **否** |
| Redis 8 | 译名缓存名带 `#30d` | 副作用一句 **是**。容器 **否** |

问：Lead 若凭「七个方法名在独行里」把该行标 covered，是否还要求读者能指着 **七扇合同、classic 三跳、500/601、`100L`≠种子、list 扁、export 无页扳机、tree 无 perm、侧门非 HTTP、厨房逗号不是后端批量**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要等 L-073 才给**本切片**当已讲？厨房独行是否必须保持 uncovered，本课只往备注写「分类七扇消费已讲」？

## Q5 元问题

- objective_id: OBJ-67
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 七名独行仍 uncovered、厨房独行仍 uncovered、无本课 (b)/(d)
- difficulty: low

建议：`defer`

原因：

- **`A:FlwCategoryController.*` 是 chain 切片，也是矩阵独行。** L 合同章节齐全（宏观地图、定义表、机制链 1–9、带 caption / 文字等价物的三张图、正反例、变式、误区、暂停走查、来源表 S-004/S-008/S-010/S-L067-01…09）。能复现：七扇映射与权限字、`categoryTree` 无 perm、`export` 是流、list 扁列表、add/edit 500 闸 + 5 秒防重、remove 601 三道黄灯后逻辑删、classic 门卫不抱引擎、`queryCategory` 非 HTTP、`100L` ≠ 种子根、表名 `flow_category`、厨房六枪没收 export、分类页不打 tree。不必为「Controller 的名字」再挖五问。
- **Lead 可以把本课这一行标 `covered`。** 七名与磁盘 1:1，不会绑死定义/任务/厨房。**厨房独行与九工厂行保持 uncovered**，等 L-073，不要用本课开第二张前端章。不要发明分类 (b) 行：601 / `100L` / 无页导出已是 (a) 的解释到副作用。
- 本批可粘贴的精度项不挡住切片视为已讲：（1）暂停第 4 步「只有分类这份单字段」以开篇「定义/实例才抱引擎」为准；（2）`selectCategoryIdsByParentId` 也被定义列表、任务查询调用；（3）web-domain 测试只锁 `category:query`；（4）厨房测试锁 `/1,2` 不是后端合同；（5）课估时 38、chain 35。不要把发布闸、`WorkflowService`、任务十六扇、厨房 URL 全表塞进本格。
- 不建议 `mine-more`：b01 未证明本切片还有真实矩阵空洞；再五问只会把 L-073 厨房、L-068 发布或 L-072 请假再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；暂停措辞是半句精度，不是第二容器。不建议 `split-lesson`：未满 10 问；七扇是 chain 原设计的同一 OBJ-67，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行。不建议等 GP-L-073 才给本切片当已讲。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:FlwCategoryController.list, export, getInfo, add, edit, remove, categoryTree | 课按磁盘讲了七扇路径/动词/权限/信封、classic 三跳、`100L`≠种子根、list 扁、export 无页扳机、tree 无 perm 且 JSON `label`、`queryCategory` 非 HTTP、厨房没收 export 与逗号删除。残留精度：暂停第 4 步措辞；展开器也被定义/任务调用；权限测试只锁 query；课估时 38 vs chain 35。 | 可选半句：口试「单字段门卫」以开篇为准——不抱引擎的是分类/SpEL/请假，直接抱引擎的是定义/实例。不要求重写机制链。Lead **可以**改本行为 covered。 | defer（切片已讲；独行可闭合） |
| A:createWorkflowDefinitionService | 厨房独行仍 uncovered。本课只借用六枪 URL 与没收窗负空间。 | 不要用本课盖厨房章。备注可写「分类七扇消费已讲」。 | defer（L-073） |
| A:createWorkflowWebDomain（九工厂行） | 本课只用分类五串权限 + 无 export 当切片证据。其余工厂未因本课讲完。 | 保持 uncovered。 | defer（L-073 / 九工厂行保持 uncovered） |
| A:FlwDefinitionController.* / B:IFlwDefinitionService.publish,importJson,removeDef | 门卫抱 `DefService`、list 分页、分类 IN 展开已借用。 | 保持。L-068。 | defer（L-068） |
| A:FlwInstanceController.* / A:WorkflowService.* | 实例也喊 `categoryTree`；跨模块不打分类 REST。 | 保持。L-069。 | defer（L-069） |
| A:FlwTaskController.* / B:IFlwTaskService.* | 任务查询同样展开子孙。 | 保持。L-070。 | defer（L-070） |
| A:FlwSpelController.* | `/{ids}` 与分页对照已借用。 | 保持。L-071。 | defer（L-071） |
| A:TestLeaveController.list, export, get, add, submitAndFlowStart, edit, remove / D:主路径 工作流请假 | 请假页才有导出钮；(d) 走廊不是分类 CRUD。 | 不要并进分类窗。 | defer（L-072） |
| C:layered-classic / wta-modules | 登记表 classic 行已 covered。 | 不要用本课再盖房间章，也不要发动迁 layered。 | defer（L-003 已 covered） |
| (b) 分类 601 / 100L / 无页导出 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 分类 CRUD 流 | 矩阵无此行。已有的工作流走廊仍是请假。 | 不要发明前端分类数据流行。 | defer（未入库） |
| Mapper XML / BO/VO getter | 空 XML 与校验组已走查。 | 保持 covered-by-parent。 | defer（covered-by-parent） |
| 测试夹具 FlwCategory*Test | 工作树没有这类测试。 | 保持 deferred(test-fixture)。证据走 Controller/SQL/e2e。 | defer（deferred(test-fixture)） |
| WarmFlow 厂商引擎内部 | `DefService.exists` / `queryCategory` 只点到包边界。 | 保持 deferred(vendor-engine)。 | defer（deferred(vendor-engine)） |
| MySQL 8.4 / Redis 8 | 种子表与译名缓存是 (a) 对照，不是容器闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本切片无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（厨房/定义/请假是邻课格子，课内已把七扇讲完并显式禁止整行假闭合）；合格读者说得出函数目的（档案室抽屉标签、七扇 HTTP）、主路径（扁列表 / Excel 流 / 单条详情 / 过唯一闸新增改名 / 过三道黄灯逻辑删 / 无 perm 的后端树）与失败路径（重名 500、父是自己 500、父不存在抛异常、不能造根、601 黄灯不删、`100L` 护不住种子、厨房逗号绑不上 Long、关电闸 404）；把可选半句写进原课不会超过 40 分钟；邻课已规划。满 10 问前不拆。七扇虽跨读写/导出/树，仍是 chain 原设计的同一 OBJ-67，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对厨房/九工厂、L-068…L-072 邻窗、L-003 已 covered 的登记表、未入库 (b)/(d)、covered-by-parent 的 XML、deferred 的测试夹具与厂商引擎、以及 MySQL/Redis 容器显式建议 `defer`；对 `A:FlwCategoryController.*` 建议视为已讲切片、**可以**把矩阵七名独行标 `covered`、**不要**用本课盖仍 uncovered 的厨房独行与九工厂行。本课不主张改 Java 窗、发明 Vue 矩阵行、或把 `queryCategory` 升成第八扇 HTTP。
