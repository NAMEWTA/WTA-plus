# T-26 本地实现审查

Revision103；可逆实现与本地验证完成，状态review。未暂存、提交、推送或部署；implementation_commit/result_sha/candidate为空，未完成direct-parent交付，不标Done。

## 合同与实现

重新枚举的70项旧PUT/DELETE/PATCH候选（27文件）均为第一方业务变更，全部切换POST。原路径与参数位置保留；仅根路径更新与新增POST冲突时添加/update，Profile自身更新无冲突所以保持根POST。旧方法不保留双挂或兼容adapter。System/Workflow/Demo沿用classic结构；未修改数据库schema或搬迁业务事务。

另将20个产生消息/队列/缓存写入等副作用的GET切为POST，保留原输入与授权。下一节点查询改GET，taskId和可选JSON对象variables放query，支持嵌套数组/数字/布尔值；null/数组/非法JSON拒绝且不调用Service。真实Warm-Flow/MySQL验证办理人可查询、无关用户拒绝、旧POST拒绝。候选逐项映射见T-26-migration-map.json、T-26-additional-migration-map.json、T-26-http-contracts.md。

所有检查范围内POST有非空业务Log；认证、上传签名/令牌、账户凭据等接口关闭请求/响应正文审计。发布、停用、完成和变更类操作的BusinessType纠正为UPDATE。LogAspect的operUrl使用服务端路由模板，无匹配时固定[unmapped]，避免新增审计复制path令牌；映射/未映射两种真实切面canary检查通过。此保证仅针对操作审计事件，不宣称既有access/error日志所有sink已同时改造。

仓内四domain及对应测试、六个浏览器fixture同步方法和路径，生成声明经正式fetch/generate/check重建；模板现状已符合GET/POST合同，验证后无需改写。父Workflow Skill两份引用同步事实。存储直传签名PUT保持，未误改供应商协议；未触及的认证挑战、供应商回调与其他模块协议仍由原owner负责，不宣称全后端每个GET绝无基础设施副作用。

## OpenAPI真实性

current revision为`7b969de6b8ecd2efe207faeeb8639edca40af9fdbed4d34968fd2e9e42a72f32`；rawSha256为`e63f642a4d781973e691a3daab6bd4be32300fd804f99df329e68f849549fc96`（两者用途不同）；generated/openapi.ts SHA256为`2c2a00a739834a1d651b77d17fca8c8b3ea69a5a85f44893ef8fd1dbd444db25`。415路径、442schemas。

输入由上一份正式schema和当前编译的314条Spring MVC映射派生，89项已收录合同变化逐项核对；provenance明确working-tree-uncommitted和baseline-plus-compiled-Spring-MVC-mappings，不冒充完整live /v3/api-docs捕获。Easy-ES两项因原快照未启用条件而未收录schema，但实际编译路由及HTTP新旧方法已验证。剩余3项PUT/DELETE属于锁定SnailAI 1.1.1 jar内的SnailAiChatGatewayController，经jar SHA与javap核实，证据见T-26-third-party-protocols.json；不虚构自有合同。

## 实际验证

完整命令/cwd/退出码见对应JSON，日志保留早期失败。所有最终列出命令exit0。

| 证据 | 结果与范围 |
|---|---|
| T-26-mapping-red | 真实编译映射旧实现1项失败、零skip，列出70旧方法和缺失审计 |
| T-26-mapping-export | 6/6、零skip；314实际Spring MVC映射、无碰撞、POST审计及LogAspect canary |
| T-26-mysql-final | 6/6、零skip；真实Jetty/Spring MVC/security、91旧方法拒绝和67权限拒绝；另含五资源实际业务持久化及4项上传HTTP合同 |
| T-26-engine-first | 1/1、零skip；真实Warm-Flow/MyBatis/MySQL、任务关系权限与新GET/旧POST合同，已有完成防重链回归 |
| T-26-upload-browser-first | Maven38/38、零skip，内含Chrome10/10与真实MinIO字节核对；上传取消/错误/重试及存储PUT保持 |
| T-26-default-browser-second | 53通过、1skip、零失败；skip为无真实Nacos代理参数的独立场景，不涉及本票CRUD |
| T-26-backend-final | backend: ./mvnw -B -ntp test；777项=695通过+82环境门控skip，零失败；required服务测试另行启用通过 |
| T-26-frontend-first | frontend: lint/typecheck/test/build:prod四命令通过；612 Vitest+108 Node=720测试；三App生产构建通过 |
| T-26-bundles-final | clean package -DskipTests及clean package -Pbundle-core -Dmaven.test.skip=true均通过；打包不计作测试 |
| T-26-static-final | System/Workflow/Demo classic、fullstack Skill、工程事实、docs/fm及diff共7条通过 |
| T-26-openapi-final / T-26-final-readonly-checks | 正式fetch/generate/check及pnpm openapi:check通过；architecture:check 35workspace/0 baseline违例 |

91方法穷举使用真实HTTP/Spring MVC/安全拦截器，多数Controller行为是受控替身；授权请求包括有意缺字段的拒绝结果，所以该矩阵证明路由选择/旧方法拒绝/方法权限，不能冒充91项业务均成功。Auth和下一节点使用实际Controller，Service边界受控。实际GlobalExceptionHandler对不支持方法保留HTTP200并返回R.code405；不用框架默认HTTP405替代产品合同。

持久化代表场景另外使用实际Controller→Service→Mapper/MySQL、真实Log和Redis RepeatSubmit，没有mock业务Service/Mapper：岗位、流程SpEL、Demo单表/树表各新增2条→GET读取→POST更新→逗号ID批删；旧方法、缺权限及无效正文拒绝后数据库不变。部门使用实际DSTransactional/DataPermission代理，新建三层树、GET读取、移动子树、拒绝成环/越权并断言结构不变，再按原单项删除合同清理。观察结果见T-26-mysql-final-observations.json。其他资源由逐项HTTP传输矩阵、domain测试及浏览器合同回归覆盖，不宣称它们都完成了逐项真实数据库生命周期；外部SMS/邮件等使用受控边界，没有发送真实消息。

所有本轮MySQL/Redis/MinIO均为隔离loopback资源，六文件基座初始化通过，容器/网络/卷清单恢复；预览端口及临时配置已清理。默认82个环境skip不算真实集成通过。未执行远程CI或生产部署，T-30仍负责change整体组合验收。

## 失败与修正

首次默认Maven仅上传abort测试还期待DeleteMapping，已同步POST并反断言旧DeleteMapping不存在，最终全量通过。HTTP fixture先后修复DAO销毁API、登录User-Agent及Sa-Token routeMatcher装配；加入GlobalExceptionHandler后补齐SpringUtils启动顺序，并给拒绝请求体探针独占连接以消除Jetty keepalive早期关闭造成的Broken pipe。未更改产品权限/错误码以迎合fixture。

默认浏览器首轮复用未带消息/控制台测试开关的dist，48通过/5失败/1skip；按既有测试环境重新构建三App后53通过/1skip，断言未放宽。OpenAPI准备阶段因条件Easy-ES未收录、供应商协议识别而中止的证据保留，最终基于实际编译/jar证据分类，不捏造schema或替换供应商协议。

## Skill Execution Records

| Skill / phase / operation | 实际操作与结果 |
|---|---|
| engineering-standards / implement / apply-scope-contract | 70旧方法逐项迁移、GET读/POST变更与安全Log、classic边界、真实授权/参数/错误合同；passed |
| namewta-fullstack-development / implement / apply-scope-contract | 四domain、浏览器消费者及正式OpenAPI同批同步，五资源真实HTTP/MySQL与真实上传验收；passed |
| wta-common-modules-guide / implement / reuse-logging-and-module-http-contracts | 复用JsonUtils、LogAspect、SecurityConfig、Sa-Token、Redis与既有MyBatis入口，审计路由模板防token泄漏；passed |
| wta-module-guide / implement / reuse-logging-and-module-http-contracts | 保持System/Workflow/Demo结构，核对Warm-Flow读取语义和锁定SnailAI供应商合同，修正父Workflow两份事实引用；passed |
| engineering-standards / verify / verify-affected-contract-and-quality-gates | 运行上列定向/真实服务/浏览器/全量/双bundle/静态门禁，明确skip与替身边界；passed |

T-26-checkpoint.json登记70产品路径，其中21条上游重叠记录前后hash；445条非重叠上游路径不变，累计515条最新路径。HEAD保持76dbbe84a34624234e379661a57b232529e34ed3，index为空。治理结果见T-26-governance.json。

## 实施历史

# T-26 实施记录

Revision100：T-26开始，466条最新上游哈希全部一致。先重新枚举所有旧CRUD方法、只读POST和GET副作用，按资源逐项固定无冲突路由/参数/权限/Log/调用者；Auth解绑已在原70候选内但漏于写集，登记AuthController精确写集及wta-admin集成测试目录。25review/1in_progress/3ready/2blocked/0Done，全部提交暂缓。

完成标准：每个旧mapping有明确迁移/保留证据，安全Log、权限、参数和仓内调用者/正式生成物一致；新旧method真实HTTP正负向与各资源代表读写/批删验收通过，完整后端/前端/架构/OpenAPI/构建门禁完成后本地review。未提交、不标Done。

Revision101：T-26确认70旧方法/27文件全部为第一方变更入口。无子路径PUT仅在与新增POST冲突时迁移为/update，其余保持原路径只换POST；2个账户解锁GET改POST，下一节点查询改GET并以JSON query保持嵌套变量类型。额外GET副作用与只读POST单列owner和理由，不宣称仅70替换即全仓合规。补LogAspect精确写集：新增上传审计会经operUrl泄漏path中的令牌，使用服务端路由模板而非原始URI，未匹配时固定占位；添加common/module Skill绑定。

Revision102：T-26已迁移70旧方法和20 GET副作用，下一节点改GET JSON query。6项编译后真实Spring MVC/操作日志测试零skip；4个domain定向测试通过。314实际MVC映射用于核对89项已收录OpenAPI变更，2项Easy-ES因原快照关闭条件未收录、单独编译映射验证；SnailAI 1.1.1三项供应商PUT/DELETE经锁定jar javap核实保留，不伪造仓内协议。正式fetch/generate/check通过，415路径442schemas；provenance明确baseline+未提交工作树编译映射，不冒充完整live捕获。登记Workflow两份父Skill引用精确写集修正方法事实；required HTTP/全量验证仍待执行。
