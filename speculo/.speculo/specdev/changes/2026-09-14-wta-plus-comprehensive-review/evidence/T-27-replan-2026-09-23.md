# T-27 evidence

Current HEAD `9b1201a7` already meets AC-027. No new implementation was required. This cancels only the repeated construction. AC-027 stays owned by T-30. The adjacent OIDC change was not modified.

`TestTreeServiceImpl` has no TODO or FIXME on the save or delete path. The demo tree rules stay in the demo module and are not copied into System department moves.

## MySQL tree

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=DemoTreeMySqlIntegrationTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Ddemo.tree.mysql.integration=true \
  -Ddemo.tree.mysql.integration.url='jdbc:mysql://127.0.0.1:33138/namewta_tree_test_t27?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai'
```

Exit 0. Isolated MySQL `mysql:8.4.9` (`sha256:c36050afdca850f23cef85703f84c7531a5ae155a11b5ee1c60acb09937c4084`) on `127.0.0.1:33138`, database `namewta_tree_test_t27`, loaded `10-cde-base-ddl.sql` through `50-cde-base-dml.sql`. The password is the existing test default and is not copied here. The container was removed after the run.

`DemoTreeMySqlIntegrationTest`: tests=15, failures=0, errors=0, skipped=0, 3.681s. A missing parent cannot create an orphan. A descendant cannot become the parent. Children block deletion even when the legacy validation flag is false. Root, child, cross-tree, and duplicate-name moves are valid. Null, negative, self, and deleted parents fail without a partial write. An existing cycle or orphan cannot be extended. Bulk delete rejects any parent or missing row before writing. A database failure rolls back every leaf delete. Hidden children still block deleting a visible parent. Data scope denies a hidden current node or new parent. Mixed visible and hidden leaves are not partially deleted. Simultaneous opposite moves cannot create a cycle. Concurrent leaf deletion and child creation leave no orphan.

## Regression

```bash
cd backend && ./mvnw -pl wta-modules/wta-demo -am test
cd frontend && pnpm --filter @namewta/domain-demo test
node docs/fm/scripts/validate.mjs
```

All exit 0. `wta-demo`: tests=2, failures=0, errors=0, skipped=0. Upstream `wta-common-redis` reported tests=2, skipped=2 in `RedisUtilsDeadlineIntegrationTest`; those gated checks are not the tree contract and are not counted as passes. `@namewta/domain-demo`: 2 files, 4 tests, 0 skipped. The FreeMarker validator checked 32 templates.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase, operation `verify-affected-contract-and-quality-gates`, status passed. No production source edit.
