# T-49 evidence (not closed)

SINGLE upload policy `avatar` no longer carries multipart threshold or part size. `OssUploadProperties` does not require those fields when mode is SINGLE. AUTO and MULTIPART still reject an invalid part size or a part count above 10000.

```bash
cd backend && ./mvnw -pl wta-admin -am -Dtest=org.namewta.test.oss.upload.OssUploadPropertiesUnitTest -Dsurefire.failIfNoSpecifiedTests=false test
```

Exit 0. `Tests run: 4, Failures: 0, Errors: 0, Skipped: 0`.

Not run, so AC-049 is not closed: default-storage switch on real MinIO, historical object routing, attachment-free notification OSS client count, and a fresh canary startup. OIDC issuer and client settings were not deleted by this edit. No SLA or skip was counted as a pass.
