# T-19 evidence

Current HEAD `4c988e15` already meets AC-019. No new implementation was required. This cancels only the repeated construction. AC-019 stays owned by T-30. The adjacent OIDC change was not modified. E2E is not-required; the App regression stays with T-30.

## Page query ownership

```bash
cd frontend && pnpm --filter @namewta/web-domain-system exec vitest run src/user/page-query.test.ts --reporter=verbose
```

Exit 0. The shell `pnpm` resolved to the repository `packageManager` `pnpm@10.34.5`. Tests=26, failures=0, skipped=0, 2.39s. The file mounts the real `UserPage.vue`, `RolePage.vue`, and `MenuPage.vue` setup with `createSystemService` and a controlled HTTP promise. It is not a copied query implementation.

Covered for all three pages: a late older filter cannot replace the newer list; an older completion does not clear the current loading flag; an older failure does not show its error or clear current loading; the current failure settles, loading ends, and a retry can return an empty page; the request snapshots nested filters, Client, sort, and paging before later edits; a response after unmount is not applied. A completed query does not reset the edit draft, and a Role query does not reset `selectedButtonPermissionIds`. Role and Menu clear the previous Client rows before the next query, including when that query fails, and clearing the Client invalidates the previous request immediately. User edit `withLoading` finishing does not settle the list query loading flag.

`useLatestQuery` only receives the query snapshot, the request, and the apply callback. It does not take the form or the permission selection.

## Frontend gates

Commands from `frontend`, same pnpm 10.34.5. Each exit 0.

```bash
pnpm architecture:check
pnpm lint
pnpm typecheck
pnpm test
pnpm build:prod
```

`architecture:check`: 33 workspace packages, 0 reviewed baseline violations. `lint`: oxlint reported 0 warnings and 0 errors, including the root e2e/config pass and the workspace packages. `typecheck`: `vue-tsc --noEmit` completed for the workspace, including `admin-web`, `home-web`, and `sso-web`. `pnpm test`: 103 files, 663 tests passed, 0 skipped. The only skip text is `skipped 0` from the openapi and architecture package reporters. `@namewta/web-domain-system` is 11 files, 81 tests, included in that total. `build:prod` built admin, home, and sso (`built in` 5.84s, 832ms, 114ms). Chunk-size warnings are existing reporter output, not failures.

## Files over 1,000 lines

Counted on this HEAD. Only two files in the ticket scope exceed 1,000 lines.

| File | Lines | Retention |
|---|---|---|
| `frontend/packages/web-domains/system/src/user/UserPage.vue` | 1210 | List, department filter, user form, and per-Client role candidates stay in this page. Credential, import, and view dialogs already have their own modules. Query lifetime is `useLatestQuery`; edit loading is separate. Not split only to reduce the line count. |
| `frontend/packages/web-domains/system/src/role/RolePage.vue` | 1281 | Role list, form, menu/button selection, inheritance, and the data-permission tree stay in one owner. The query owner does not receive that permission state. The permission algorithm was not rewritten. |

`MenuPage.vue` is 741 lines. System, workflow, and demo `composables.ts` are 173, 108, and 115 lines. They are not one shared wrapper: workflow search can reset page size and returns selected rows; demo search resets page number only and its dialog title is required; system search also accepts an initial page number and `useLatestQuery` exists only there. Admin `useDialogState` remains because `RoleSelect` calls it and needs `toggleDialog` / `setTitle`. Admin no longer has `useLoading`, `useFormDialog`, or `useSearchReset`.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase, operation `verify-affected-contract-and-quality-gates`, status passed. Inputs were this ticket and the commands above. Output is this file. No production source edit.
