# T42 private full-JAR HTTP/OpenAPI v4 — independent delta review

Fixed input: v4 `/tmp/wta-t42/capture-live-openapi-v4.py` SHA-256 `a467d3d9cb124308914d4e67a28819be407eaed4b6a4ef81d55bb6b5c80fbbee`, compared with preserved v3 SHA-256 `1a77d7034f4326b325be70bca69f8d5acbda74eccd76ff96c778ede0b25be728`. The v4 freeze SHA-256 is `8b1b8995299389ccf633b3f34f193f35c68890ff97ae6a54f83ad2dbe61c14fd`; its runner, runbook and offline-test digests match disk. Freeze records 9 offline tests, py_compile/preflight exit 0, **no service execution or actual acceptance**. Preflight observed a dirty source and correctly does not claim a clean execution proof. This review did not run a build, service or test, and did not change the repository.

**Static verdict: v3's numeric-token blocker is closed; no new blocking finding in the v4 delta.** This is readiness for a Lead-owned exact-clean run, not product acceptance.

- `:536-568` extracts the former positive SQL join into `exact_source_relation_count`. It validates decimal ID shapes and still joins the receipt's Intent to the relation and exact `sys_oss_ref`, checking the original uploaded `source_oss_id`, position 0, `QUEUED`, actor user ID and Client PK. The original quoted-large-ID positive path at `:644-650` calls the same query; no positive requirement was removed.
- `:687-722` sends the numeric JSON token separately. A success response must contain a fresh decimal-string receipt ID distinct from all three earlier receipts. The accepted branch at `:551-565` requires exactly +1 Intent, Relation, Recipient, Delivery, Outbox and uploaded-source ref, exactly one matching original source/actor/Client relation, and no additional S3 request. Python's integer JSON emission and SQL use the original integer/string bytes without floating-point conversion; a rounded source cannot satisfy the equality join. The rejected branch at `:566-569` requires all six counts and S3 traffic unchanged. A contradictory HTTP status/business-code pair fails. Both branches remain an observation outside the published string-array schema; acceptance of a numeric token no longer independently fails the gate.
- The v3 quoted-ID, omitted/empty, malformed decimal/missing ID, real no-token and logged-in no-permission hard checks remain unchanged, as do exact clean HEAD/JAR/package proof, six SQL/104 tables, default SSE/full OpenAPI no-loss/schema, owned MinIO upload, future schedule/no-provider check, credential confinement and full owner cleanup. The v4 offline test exercises both numeric branches, count/reference mismatches, HTTP/business mismatch, exact SQL source/actor/Client predicates, and retained proof/cleanup markers. The offline counts are manifest evidence only, not a live verdict.

Limit: the numeric accepted SQL receipt join is not additionally scoped by `app_id`; the separate app-scoped six-table deltas each require one new row. Together with a fresh receipt ID and exact source/actor/Client, this is a sufficient narrow test for the current owned fixture, though it is not a general proof of every possible unrelated concurrent app. The full-JAR run must still provide fresh `result.json`, exact source/JAR before-after and zero cleanup errors. No HTTP/OpenAPI or OSS outcome has yet been observed from v4.
