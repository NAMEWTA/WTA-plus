---
artifact: learning-goal-probe
probe_id: GP-L-037-b01
lesson_id: L-037
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-037 / b01

审问对象是已写成的 Lesson `lessons/L-037-person-materials.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列三格星号：`A:PersonMaterialAdminController.*`、`A:PersonMaterialSelfController.*`、`A:MaterialTagController.*`。OBJ-37 原文：能口述个人材料 attach/detach 与 `MaterialTagController`。矩阵 (a) **三行**仍 uncovered，符号比星号窄：

| 矩阵 (a) 符号 | 磁盘公开映射 |
| --- | --- |
| `PersonMaterialAdminController.list, accessUrl` | 与磁盘两扇 GET 1:1 |
| `PersonMaterialSelfController.attach, detach` | 与磁盘两扇 POST 1:1 |
| `MaterialTagController.tree, update, status, archive` | 磁盘还有第五扇 **`create`** |

矩阵 (b) **没有** attach / detach / `reconcileReferences` / `accessUrlView` 行。矩阵 (c) MySQL 8.4 仍 uncovered。矩阵 (d) 无材料挂接流；profile 申请主路径留给 L-035 / L-034。`ProfileMaterialPort` 落在聚合行 `CompositeProfileService, ProfileService, PersonIdentityLookupService, ProfileMaterialPort`，不是本三行。厨房 `createProfileService` / web-domain 留给 L-039。档案审核窗 archive 出门条留给 L-034。企业材料 Admin/Self 留给 L-043。OSS 直传/卡片柜留给 L-027 / L-026。核身回调留给 L-038。本批按派单审问这三格 (a)；口试按磁盘，不要把矩阵漏写的 `create` 背成「没有新增」。

抽查锚点（2026-09-16）：`backend/wta-modules/wta-profile/wta-profile-person/src/main/java/org/namewta/profile/person/controller/{admin/{PersonMaterialAdminController,MaterialTagController,ProfileMaterialExceptionHandler,PersonAdminController},self/PersonMaterialSelfController}.java`；`usecase/ProfileMaterialUseCase.java` 与 `usecase/impl/ProfileMaterialUseCaseImpl.java`；`service/{ProfileMaterialService,IProfileMaterialService,PersonProfileApiService}.java`；`adapter/security/SaTokenProfileMaterialAccessPolicy.java`；`adapter/api/PersonProfileMaterialOwnerContributor.java`；`mapper/person/{ProfileMaterialMapper,PersonApplicationMapper}.xml`；`src/test/.../controller/admin/ProfileMaterialHttpContractTest.java`；`src/test/.../service/impl/{ProfileMaterialServiceTest,ProfileMaterialMySqlE2ETest,ProfileMaterialServiceImpl,PersonProfileMaterialOwnerContributorTest}.java`；`wta-api/.../ProfileMaterialPort.java`；`10-cde-base-ddl.sql` `profile_material_*`；`50-cde-base-dml.sql` 种子树；`frontend/packages/domains/profile/src/{person/materials/{service,index}.ts,material-tags/{service,index}.ts}`；`frontend/packages/api-contracts/generated/openapi.ts` vs `openapi/current.json`；企业对照 `EnterpriseMaterialSelfController.java`；登记表 `03-backend-module-modes.md` layered 行。

## Q1 澄清

- objective_id: OBJ-37
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-37；chain L-037 覆盖列三格 `.*`；L-037 `claimed_cells` 三格；矩阵 (a) 三行（标签行漏 `create`）仍 uncovered；(b) 无本课符号；(c) MySQL 8.4 uncovered；(d) 无材料挂接流；课开篇「两块门牌、三份 Controller」+ 负空间 L-034/L-035/L-039/L-043
- difficulty: medium

课把挂钩房收成「两块门牌、三份类」。计划里至少有五套可数对象，派单点名三格 (a)：

1. **OBJ-37 原文。** `course.md`：能口述个人材料 attach/detach 与 `MaterialTagController`。点名的是挂摘 + 分类牌类，**没有**把 Admin `list` / `accessUrl` 写成口试名词，也没有点名 `create`。
2. **chain / 课 `claimed_cells`。** 三格星号。`*` 在磁盘上 = Admin 两扇 GET + Self 两扇 POST + 标签 **五**扇（含 `create`）。chain 不写 Vue / `createProfileService` / `ProfileMaterialPort` 聚合行。
3. **矩阵 (a) 三行。** Admin / Self 与磁盘 1:1。标签行写成四扇，漏 `create`。Lead 若按**现印**标签行标 covered，会把磁盘第五扇假闭合进「未点名」。
4. **课自己的操作定义。** 口试按磁盘：共用 `/profile/person/materials`，GET 在 Admin、POST 在 Self；分类牌 `/profile/material-tags` 含 `create`；`owner(...)` 写死 `ProfileType.PERSON`；无 `@DeleteMapping`；挂摘不碰字节、不砸 OSS 箱子。格子不认厨房工厂。
5. **邻格。** archive 出门条是 L-034 `PersonAdminController`。`validateRequired` / `snapshotImmutable` 无本课 HTTP，交卷在 L-035。厨房 URL 对照在 L-039。企业对称切片在 L-043。OSS 直传/卡片柜在 L-027 / L-026。

问：本三格闭合标准是「磁盘公开映射（含标签 `create`）+ GET/POST 切开 + 挂摘只动夹子 + 分类牌受限树」，还是 OBJ-37 那句必须把 Admin 两扇从矩阵删掉、或必须先改 `course.md` 才盖章？若是前者，Lead 标三行 covered 是否必须先把标签行符号改成 `tree, create, update, status, archive`，而不是把 `create` 留给 b02、也不是把 `ProfileMaterialPort` 聚合行或厨房工厂算进本三行？

## Q2 证据

- objective_id: OBJ-37
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Admin 29–62；Self 29–72；Tag 32–100；HttpContractTest 19–62；Policy 15–68；Service 46–53 / 81–86 / 92–96 / 142–227 / 287–378 / 382–394 / 493–500 / 596–601 / 648–652；UseCase 48–50；UseCaseImpl 20–90；Handler 9–21；Port 56–83；IProfileMaterialService 1–5；测试夹具 ServiceImpl 15–16；Contributor 13–49；ApiService 50–70 / 97–99；Application XML 37–81；Material XML 7–15 / 57–59 / 67–72 / 74–78 / 103–118；DDL 646–673 / 1155–1187；DML 1377–1422；E2E 66–138；ServiceTest 68–135 / 138–173 / 176–201 / 218–229；PersonAdmin 26 / 70–83；企业 Self 41–78；厨房 `person/materials/{service,index}.ts`；`material-tags/{service,index}.ts`；generated openapi 2092–2134；`openapi/current.json` 无 `/profile/`；登记表 layered 行；R.fail 130–131 + `HttpStatus.ERROR=500`
- difficulty: high

课的机制链哪些能在工作树直接复现，哪些只是图/校验句比磁盘窄？

**仍能复现（本三格）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 没有 `PersonMaterialController` Java 类；前端资源标签才叫这个名字 | 仓库无该类；`person/materials/index.ts` 4–6 `controller: 'PersonMaterialController'` |
| Admin + Self 共用 `@RequestMapping("/profile/person/materials")`；Admin 只有 GET，Self 只有 POST | Admin 29 / 37 / 48；Self 29 / 37 / 51 |
| `owner(...)` 写死 `ProfileType.PERSON`；路径 `ownerType` 只是四块板 | Admin 60–62；Self 64–66 |
| Admin `list`：`GET /{ownerType}/{ownerId}`；权限 OR 五颗含 `:material`；无 `@Log`；返回 `R<List<MaterialReferenceView>>` | Admin 37–43 |
| Admin `accessUrl`：`GET .../{materialRefId}/access-url`；同 OR；返回 `R<PersonProfileAccessUrl>`，调 `accessUrlView` | Admin 48–55；VO 15–16 不暴露 `OssAccessUrl` 类型 |
| Self `attach`：`POST /{ownerType}/{ownerId}`；`:material`；`@Log` INSERT 且不记体；body `{ossId, materialNodeId}` | Self 37–46 / 71–72 |
| Self `detach`：`POST .../{materialRefId}/detach`；`:material`；`@Log` UPDATE 不记体；`R<Void>` | Self 51–59 |
| 标签门牌 `/profile/material-tags`；`tree/create/update/status/archive` 五扇；`create` 是 `POST` 类根 | Tag 32 / 40–93 |
| `tree` 权限 OR 含 `material-tag:query`、两边 `:material`、两边 `:override`；`includeDisabled` 默认 `false` | Tag 40–46；HttpContractTest 43–50 |
| 三份类都没有 `@DeleteMapping`；每个 `@PostMapping` 都有 `@Log` 且 `isSaveRequestData=false`、`isSaveResponseData=false` | HttpContractTest 19–39 / 53–61 |
| `wta-profile` layered；生产 Controller 只持 `ProfileMaterialUseCase`；UseCaseImpl `@DSTransactional` 转 `ProfileMaterialService` | 登记表 layered 行；三份 Controller 字段；UseCaseImpl 18–90 |
| `IProfileMaterialService` 空接口 extends `ProfileMaterialPort`，生产目录无 `@Deprecated`；测试夹具才 `extends ProfileMaterialService implements ProfileMaterialUseCase, ProfileMaterialPort` | IProfileMaterialService 1–5；测试夹具 15–16 |
| UseCase `accessUrlView` 是 default，旧适配器扔 `UnsupportedOperationException("旧适配器不支持文件访问地址入口")`；生产 Impl 覆盖 | UseCase 48–50；UseCaseImpl 70–76 |
| list/accessUrl/validateRequired 的 UseCase 方法都有 `@DSTransactional`（读路径也是事务，因为 `FOR UPDATE`） | ServiceTest 68–78；lock XML 见下 |
| `lockOwner`：WORKING 锁申请行带回申请人；SUBMISSION join 申请；SOURCE/VERSION 锁行且申请人为 null；找不到 → `MATERIAL_OWNER_NOT_FOUND` | ApiService 50–63；Application XML 37–73；Service 287–291 |
| 企业 owner 打进 PERSON 贡献者 → `IllegalArgumentException("material owner must use PERSON profileType")` | ApiService 97–99；ContributorTest 58–62 |
| 注解过了还有策略：WORKING attach = 申请人 + `:material`；SOURCE attach = `:override`；`requireWrite` **没有** override；`requireRead` = 管理四权 **或** 申请人 + `:material` | Policy 26–58 |
| WORKING 可编辑集合 `DRAFT/BACK/CANCEL`；个人实现是 count **== 1**；已提交 → `MATERIAL_OWNER_READ_ONLY`；SOURCE 挂接不查这扇 | Application XML 75–81；ApiService 65–70；Service 149–151 / 218–229 单测 |
| attach 只接受 WORKING/SOURCE，否则 `IMMUTABLE_MATERIAL_OWNER`；校验失败零 `insertReference`、零 `reconcileReferences` | Service 145–147；ServiceTest 102–135 |
| 节点必须是启用 TAG，scope 为 COMMON 或与档案类型同名 | Service 371–378 |
| 当前 ATTACHED `< 10`；OSS 上传者必须是策略返回的当前用户；大小 `(0, 10MiB]`；扩展名与 suffix 一致；MIME 只认 jpeg/png/pdf | Service 46–53 / 154–161 / 382–394 |
| WORKING `immutable_flag=N`，SOURCE `Y`；唯一键冲突 → `MATERIAL_ALREADY_ATTACHED`；随后 `reconcileReferences("profile_material_ref", refId, empty, Set.of(ossId))` | Service 166–173 / 493–500 |
| detach 仅 WORKING；塑封夹子 `IMMUTABLE_MATERIAL`；已 DETACHED 直接 return 不打 OSS；SQL 只更新 `ATTACHED + immutable_flag='N'`；reconcile 从借出簿去掉，不是删对象 | Service 179–197；XML 103–108；ServiceTest 159–173 |
| 夹子必须属于这把 owner 钥匙，否则对外仍 `MATERIAL_NOT_FOUND` | Service 648–652 |
| `list` 按 owner 拉未删引用，**含 DETACHED**，`order by attached_time, material_ref_id` | XML 110–118；Service 202–206 |
| `accessUrl`：WORKING 已摘 → `MATERIAL_NOT_ATTACHED`；非 WORKING 无这道短路；过策略才 `resolveAccessUrl`，再投影四字段 | Service 211–227 |
| E2E：WORKING attach → snapshot SUBMISSION → access-url PRIVATE → detach WORKING，复印件仍 ATTACHED、同一 oss 两行；拒绝后 HTTP 200 + `msg=MATERIAL_ACCESS_DENIED`，`never() presignDownload`，`resolveAccessUrl` 停在成功那一次 | E2E 66–138 |
| 业务失败走 `ProfileMaterialExceptionHandler` → `R.fail(message)`；`basePackages` 同时罩 person **和** enterprise 的 admin/self | Handler 9–21 |
| HTTP create 不能再种根（`parentId==0` 且 current==null → `MATERIAL_ROOT_SCOPE_FIXED`）；新建 `systemRequired` → `SYSTEM_MATERIAL_TAG_PROTECTED` | Service 315–323 / 176–183 单测 |
| 停用：系统牌或 depth=1 → `SYSTEM_MATERIAL_TAG_PROTECTED`；分类还有孩子 → `MATERIAL_CATEGORY_NOT_EMPTY` | Service 115–120 |
| 归档：同样保护根/系统牌；有孩子 **或任何引用（XML 不滤 status）** → `MATERIAL_NODE_IN_USE`；成功 `del_flag=1` 且 `status=1` | Service 130–136；XML 57–59 / 67–72 |
| `tree` SQL 把 `COMMON` 一并带上；默认只 `status='0'`；`includeDisabled=true` 时 Service 另要 `requireCatalogRead`（`:query` 或 `:manage`） | XML 7–15；Service 81–86；Policy 20–23 |
| 码/scope/nodeType 不可变；TAG 改码 → 系统牌 `SYSTEM_MATERIAL_TAG_PROTECTED`，普通牌 `MATERIAL_TAG_CODE_IMMUTABLE` | Service 347–366 / 196–200 单测 |
| 生成列 `active_owner_oss_key`：同一 owner 上同一 oss 同时只能一条 ATTACHED；唯一键全表 | DDL 1169–1183 |
| 树形 CHECK：根 CATEGORY depth=1 parent=0 无码非系统；二级 CATEGORY depth=2；TAG depth 2 或 3 且有码 | DDL 667–672 |
| `material_tag_code` 全表唯一 | DDL 665 |
| 种子三棵根 + 五枚个人 TAG；大陆身份证必传是人像面 **和** 国徽面 | DML 1377–1401 |
| 厨房四枪 GET list / POST attach / POST detach / GET access-url；`encodeURIComponent` 路径段 | `person/materials/service.ts` 26–39 |
| 标签厨房五枪含 create；`includeDisabled` 默认 false | `material-tags/service.ts` 15–35 |
| 生成 OpenAPI 已有 `/profile/person/materials` 四条路径（list+attach 同 URL 的 GET/POST、detach、access-url）；`openapi/current.json` 搜不到 `/profile/` | generated 2092–2134；current.json 零命中 |
| archive 出门条是隔壁门牌 `/profile/person/archive`：`reviewMaterial` 权限 `:review`；`material` 权限单独 `:material` | PersonAdmin 26 / 70–83 |
| 企业 Self 才有 `@Positive`（ownerId / materialRefId / AttachRequest 两字段）；个人 Self/Admin 都没有 | 企业 Self 41–78 vs 个人 Self 41–56 / 71–72、Admin 40–53 |
| `validateRequired` / `snapshotImmutable` 在 UseCase/Service 上，**没有**本课 HTTP | UseCase 54–59；三份 Controller 无这两扇 |

**课写过、图或校验句比磁盘窄（不是证伪）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 图 2 只给人像面/国徽面标 `systemRequired=Y` | DML 1387–1391：**五枚**个人 TAG 全是 `'Y'`（含 `PERSON_IDENTITY_FRONT/BACK`、`PERSON_PASSPORT_DATA_PAGE`）。文字等价物「这些系统牌停用/归档都会被拒」对五枚都成立，ASCII 少标了三枚 |
| 「Self 的 `ownerId` 没有 `@Positive`」 | 成立。未写：Admin 的 `ownerId`/`materialRefId`、Self 的 `materialRefId`、`AttachRequest.ossId/materialNodeId` 同样没有。企业 Admin/Self/记录体才有 |
| 「非法 id 会在 `MaterialOwnerKey` compact 或后续 `requirePositive` 爆」 | compact 抛的是 `IllegalArgumentException("ownerId must be positive")` / `"ossId and materialNodeId must be positive"`（Port 56–83），**不是** `ProfileMaterialException`，进不了本课 Handler 的 `R.fail`。`requirePositive` 失败码是 `materialNodeId_INVALID` 这种 `{field}_INVALID`，没有 `MATERIAL_` 前缀 |
| 「HTTP 仍可能是 200，`msg` 才是业务码」 | E2E `status().isOk()` + `msg` 成立。未写：`R.fail` 把 JSON `code` 写成 `HttpStatus.ERROR`（**500**）。口试若说「信封 code 也是 200」会错 |
| 「个人 countEditable」只写了状态集合 | 个人是 `== 1`（ApiService 70）；企业对照是 `> 0`。本课不必展开企业，但「有一行可编辑申请」比「count>0」更紧 |
| 「拒绝时不再调 `resolveAccessUrl` / `presignDownload`」 | `never() presignDownload` 成立；`resolveAccessUrl` 是成功那次 `times(1)`，拒绝请求没有第二次。主语是「拒绝那一枪」，不是「整个测试零 resolve」 |

问：在两块门牌三份类、GET/POST 切开、写死 PERSON、无 DELETE、策略两道门、attach 拒绝链零写库、detach 不砸箱、E2E 鉴权先于出门条、标签五扇含 create 已经能互证的前提下，(a) 是否还要把「图 2 少标三枚系统牌」「个人 Admin/AttachRequest 也无 `@Positive`」「非法 id 是 IAE 不是材料业务码」「JSON code=500」写成开篇半句，才算三行讲完？还是这些已经落在课的表/反例或邻格里，Lead 只要补矩阵标签行的 `create` 就可以盖章？

## Q3 假设与反例

- objective_id: OBJ-37
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无 `PersonMaterialController` 类；Admin 无 POST；detach 非 DELETE；override 不能 requireWrite；archive 出门条是 L-034；企业 URL 另一前缀；同一 oss 不能双 ATTACHED；Handler 200+R.fail；矩阵漏 create；current.json 无 `/profile/`；夹具 ServiceImpl 不得回流生产
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪只夹子认错？

1. **「一份 `PersonMaterialController`。」** 没有这个 Java 类。课开篇表 / 误区 9 覆盖。
2. **「Admin 类也能 attach。」** Admin 只有 GET。课反例 1 覆盖。
3. **「detach = DELETE / 删 MinIO。」** POST `.../detach` + reconcile 减引用。课反例 2 覆盖。合同测试禁 DeleteMapping。
4. **「列表 VO 的 url 就能预览。」** list 是夹子清单；出门条只在 access-url。课反例 3 覆盖。
5. **「`/profile/person/archive/.../access-url` 是本课 Admin 材料窗。」** L-034。课反例 4 覆盖。
6. **「URL 上的 ownerType 能把档案改成企业。」** 写死 PERSON。课反例 5 覆盖。
7. **「override 可以帮申请人摘 WORKING 夹子。」** `requireWrite` 只要申请人。课反例 6 覆盖。
8. **「同一张照片夹到人像面和国徽面。」** 生成唯一列按 owner+oss，不是按 tag。课反例 7 覆盖。
9. **「挂别人的 OSS。」** `MATERIAL_OSS_OWNER_MISMATCH`。课反例 8 覆盖。
10. **「提交后还能在 WORKING 上补图。」** `MATERIAL_OWNER_READ_ONLY`。课反例 9 覆盖。
11. **「归档标签 = DELETE 节点。」** POST + 软删；DETACHED 历史也占着牌。课反例 10 覆盖。
12. **「create 可以再种根 / 新建系统牌。」** `MATERIAL_ROOT_SCOPE_FIXED` / `SYSTEM_MATERIAL_TAG_PROTECTED`。课反例 11 覆盖。
13. **「改标签编码、改 PERSON 为 COMMON。」** 码/scope 不可变。课反例 12 覆盖。
14. **「业务失败一定 403/404。」** 材料业务码走 Handler → HTTP 200 + `R.fail`。课反例 13 覆盖。
15. **「矩阵没写 create，所以没有新增。」** 磁盘有。课反例 14 覆盖。这是**矩阵符号**缺口，不是课缺口。
16. **「这是 classic，Controller 直接碰 Mapper。」** 登记表 layered。课误区 15 覆盖。
17. **「材料上传走这扇 POST。」** body 只有 ossId + nodeId。课误区 3 覆盖。
18. **「`includeDisabled` 只看 HTTP 注解。」** 开停用节点时 Service 另要标签 query/manage。课误区 7 覆盖。
19. **「企业也有一份 `MaterialTagController`。」** 全仓一份，住在 person 包。课误区 8 覆盖。
20. **「快照窗在本课。」** `snapshotImmutable` 无 HTTP。课误区 11 覆盖。
21. **「OpenAPI current.json 就是权威。」** 快照缺 `/profile/`；生成文件与厨房硬编码才看得到。课误区 13 覆盖。
22. **「OBJ-37 还包含 `createProfileService`。」** OBJ-39。课误区 1 覆盖。
23. **「Admin 就是管理员专用，申请人 list 不了。」** GET 的 OR 含 `:material`；`requireRead` 也给申请人。课误区 5 覆盖。
24. **「个人 HTTP 非法 id 会变成 `MATERIAL_*` 信封。」** compact 是 IAE；企业才在控制器用 `@Positive` 先挡。课写了「不要把两边校验说成同一份」，但主语只钉了 Self `ownerId`。
25. **「图 2 没标 Y 的三枚个人 TAG 可以停用/归档。」** 种子全是系统牌。ASCII 少标不是规则例外。
26. **「chain 的 `MaterialTagController.*` 已随矩阵四扇闭合。」** 星号含 `create`；现印矩阵不含。Lead 若不改符号就盖章，第五扇会假闭合。

问：读完后仍说不出的，是不是「三份类的名字」，而是 **标签行必须把 `create` 写进矩阵符号、Admin GET 申请人也能打、override 不能 detach、摘夹子不砸箱、业务码经常是 200 信封、个人路径校验比企业松、五枚个人 TAG 都是系统牌**？

## Q4 视角

- objective_id: OBJ-37
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-37 vs L-034/L-035/L-036/L-038/L-039/L-043/L-026/L-027；`ProfileMaterialPort` 聚合行；MySQL 8.4；API-005 存量 POST
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课三格 (a)**？

| 视角 | 多出来的边 | 是否本课三格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 三行；标签行漏 `create`；chain 三格 `*`；(b) 无挂摘行；(c) MySQL 8.4 uncovered；(d) 无材料流 | **是** 闭合定义。Admin/Self 可按现印盖章。标签行先补 `create` 再 covered。不要发明 (b)，不要把 MySQL 容器或 Port 聚合行假闭合 |
| 挂钩房（本三份类） | 两块门牌、GET/POST 切开、写死 PERSON、无 DELETE、安全 `@Log`、create 第五扇 | **是** (a) |
| 策略第二道门 | WORKING 申请人 vs SOURCE override；`requireWrite` 无 override；`requireRead` 给申请人 | **是** 三行的 explained 失败句。不要另开 Policy 行 |
| 夹子/借出簿 | attach 拒绝链零写库；detach 只改状态；reconcile ±ossId；生成唯一列 | **是** Self 两扇。`objectStore.delete` **否**（L-026） |
| 出门条 | 先 `requireRead` 再 `resolveAccessUrl`；投影 `PersonProfileAccessUrl` | **是** Admin `accessUrl`。PUBLIC/PRIVATE TTL **否**（L-026） |
| 分类牌 | 三层受限树、乐观锁、COMMON 并入、软删、系统牌保护 | **是** Tag 五扇（含 create） |
| 无 HTTP 的复印/必传 | `snapshotImmutable` 三对迁移；`validateRequired` 缺码 | 负空间一句 **是**。调用点 **否**（L-035） |
| 档案审核窗（L-034） | `/profile/person/archive/.../access-url` 两扇 | 对照门牌 **是**。十二扇档案窗 **否** |
| 自助申请（L-035） | current/submit 何时 validate/snapshot | **否** |
| 换绑 / 核身（L-036 / L-038） |  | **否** |
| 厨房（L-039） | `createPersonMaterialService` 四枪、`createMaterialTagService` 五枪、`encodeURIComponent` | URL 对照 **是**。工厂格子 **否** |
| 企业材料（L-043） | `/profile/enterprise/materials`；企业 Self 有 `@Positive`；分类牌仍打本课 Tag 窗 | 对称一句 **是**。企业 Admin/Self 两行 **否** |
| OSS 卡片柜 / 直传（L-026 / L-027） | 涂黑 url、PENDING、init/complete | 夹子≠箱子 **是** 边界。四扇/五扇 **否** |
| `ProfileMaterialPort` 聚合行 | wta-api 端口；person Service 经 `IProfileMaterialService` 实现 Port | 本课当电线 **是**。聚合行四名字 **否** |
| API-005 | 变更目标 POST；本课挂摘/改牌已是 POST，无 DELETE | 盘点现状 **是**。不发动传输 Ratchet **否** |
| MySQL 8.4 / Redis 8 | 三张材料表、CHECK、生成列 | 表证据 **是** (a)。容器 **否**（L-085） |
| 测试夹具 ServiceImpl | 同时实现 UseCase+Port、把 Mapper 塞进夹具 | 误区「不得回流生产」**是**。夹具行已 deferred(test-fixture) **否** 另开 |

问：Lead 若凭「三份类名在函数表里」把三行标 covered，是否还要求读者能指着 **标签 `create`、Admin OR 含申请人、override 不能摘、摘夹子不砸箱、200+业务码、个人无 `@Positive`**？这些是不是已经在本三格函数表/反例里，而不需要 (b) 新行、也不需要把厨房或企业材料算进来？标签行是否例外——必须先改符号？

## Q5 元问题

- objective_id: OBJ-37
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 三行、标签漏 create、无本课 (b)、无材料 (d)
- difficulty: low

建议：`defer`

原因：

- **三格 (a) 与磁盘公开映射对齐，主叙事站得住。** L 合同章节齐全（宏观地图、定义表、机制链 1–7、带 caption / 文字等价物 / 图边界的两张图、正反例、变式、误区、暂停走查、来源表 S-004/S-008/S-010/S-015/S-L037-01…08）。能复现：两块门牌三份类、GET 在 Admin、POST 在 Self、写死 PERSON、无 DELETE、安全 `@Log`、策略两道门、attach 拒绝链零写库、detach 不砸箱、list 含 DETACHED、出门条先鉴权、标签五扇含 create、受限树+乐观锁+软删、业务码走 `R.fail`、layered 五层 + Policy/Contributor/OssService 三根电线。不必为「类名」再挖五问。
- 本批可粘贴的精度项不挡住把 **Admin / Self 两行**标 covered，也不挡住把 **标签行在补上 `create` 之后**标 covered：（1）图 2 少标三枚系统牌；（2）个人 Admin/AttachRequest 同样无 `@Positive`，非法 id 是 Port compact 的 IAE；（3）JSON `code=500` 而 HTTP 行 200；（4）个人可编辑计数是 `==1`。不要把厨房工厂、archive 审核窗、企业材料两行、`validateRequired` HTTP、MySQL 容器塞进这三格。
- 不建议 `mine-more`：b01 证明的「真实矩阵缺口」是标签行**符号漏写** `create`，课已经把第五扇讲完。再五问只会把 L-035 快照、L-039 厨房或 L-043 企业 Self 再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；半句精度（图 2 三枚 Y、IAE vs `R.fail`）不是第二容器。不建议 `split-lesson`：未满 10 问；三份类是 chain 原设计的同一 OBJ-37、同一人材料 HTTP，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：挂摘副作用、reconcile、出门条关闭是 (a) 的 explained。不建议改 `course.md` OBJ 才盖章：Admin 两扇已在矩阵/chain/claimed_cells，OBJ 短句只是没点名。
- 标签行 `create`：**Lead 改矩阵符号**（与磁盘/chain `*` / 课函数表对齐），不是 miner 再问，也不是把 create 并进企业材料课。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:PersonMaterialAdminController.list, accessUrl | 共用门牌、仅 GET、OR 含 `:material`、无 `@Log`、`accessUrlView` 投影、list 含 DETACHED、拒绝后不二次 `resolveAccessUrl`——均与源码对齐。残留：Admin 路径也无 `@Positive`；JSON `code=500`。 | 可选半句：个人 Admin 路径校验与 Self 一样松；信封 HTTP 200、JSON code 500、`msg` 才是材料码。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-037 来源表 S-L037-01…06）。 | defer（本行可 covered；精度项不挡盖章） |
| A:PersonMaterialSelfController.attach, detach | POST 挂摘、body 无字节、WORKING/SOURCE 才能挂、仅 WORKING 可变摘、override 不能 `requireWrite`、reconcile ±ossId、不 DELETE——均与源码对齐。残留：AttachRequest 字段无 `@Positive`；非法 id 是 IAE。 | 可选半句：企业 Self 才有 `@Positive`；个人非法 id 先撞 Port compact，不是 Handler 业务码。Lead 可将本行标 `covered`。 | defer（本行可 covered） |
| A:MaterialTagController.tree, update, status, archive | 课按磁盘讲了 **五**扇（含 `create`）；tree OR + COMMON 并入；乐观锁；系统牌保护；归档软删且引用（含 DETACHED）占用即拒。**矩阵符号漏 `create`。** 图 2 少标三枚个人系统牌。 | Lead 把本行符号改成 `tree, create, update, status, archive`（与 chain `*` / 课函数表 / 磁盘一致）再标 `covered`。可选：图 2 五枚 TAG 都标 `systemRequired=Y`。不要为此开 b02。 | defer（符号补丁后可 covered；不是课空洞） |
| A:createProfileService / A:createProfileWebDomain person/self | 厨房四枪/五枪只作对照。 | 保持。L-039。 | defer（L-039） |
| A:PersonAdminController.* | archive 两扇出门条已作负空间。 | 保持。L-034。 | defer（L-034） |
| A:PersonApplicationController.current, submit | validate/snapshot 调用点未讲。 | 保持。L-035。 | defer（L-035） |
| A:PersonRebindController.* / A:PersonVerificationAnonymousController.callback | 本课未认。 | 保持。L-036 / L-038。 | defer（邻课） |
| A:EnterpriseMaterialAdminController.* / A:EnterpriseMaterialSelfController.* | 对称切片；分类牌仍打本课 Tag 窗；企业 Self 有 `@Positive`。 | 保持。L-043。 | defer（L-043） |
| A:SysOssController.* / A:SysOssUploadController.* | 夹子≠箱子；直传不在本课 POST。 | 保持。L-026 / L-027。 | defer（OSS 课） |
| CompositeProfileService, ProfileService, PersonIdentityLookupService, ProfileMaterialPort | 本课把 Port 当电线；聚合行四名字未认。 | 不要并进三份 Controller 才能盖章。 | defer（未认的聚合行） |
| (b) attach/detach/reconcile/accessUrlView | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 材料挂接流 | 矩阵无此行。ASCII 图不是新的 (d)。 | 不要发明个人材料数据流行。 | defer（未入库） |
| MySQL 8.4 / Redis 8 | 三张材料表是 (a) 证据，不是容器闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |
| API-005 变更 POST | 本课挂摘/改牌已是 POST，无 DELETE。 | 本课盘点，不发动 Ratchet。 | defer（传输 Ratchet） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本三行无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（标签 `create` 是矩阵符号漏写，课内已讲）；合格读者说得出三份类目的（只读两扇 / 挂摘两扇 / 分类牌五扇）、主路径（WORKING attach → list/access-url → detach，箱子仍在）与失败路径（策略拒绝不签发、不可变板不能摘、业务码 200 信封）；把可选半句写进原课不会超过 40 分钟；档案窗/申请闸/厨房/企业材料/OSS 已有邻课。满 10 问前不拆。三份类虽跨 admin/self 包，仍是 chain 原设计的同一 OBJ-37，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-034 档案窗、L-035 申请闸、L-036/L-038 邻课、L-039 厨房、L-043 企业材料、L-026/L-027 OSS、Port 聚合行、未入库 (b)/(d)、MySQL/Redis 容器以及 API-005 显式建议 `defer`；对 `A:PersonMaterialAdminController.list, accessUrl` 与 `A:PersonMaterialSelfController.attach, detach` 建议 Lead 标 `covered`；对 `A:MaterialTagController.tree, update, status, archive` 建议 Lead **先把符号补上 `create`** 再标 `covered`（证据锚本 probe 与 L-037 来源表）。本课不主张改 Java 窗、厨房工厂、企业材料两行或任何 (b)(c)(d) 格子。
