# T-42 private closure draft — evidence map, not a Done decision

Final generated-contract source is `73c28e751f6276e45b0488ab5d8eacbf9f0c77fb`, tree `748f1b22d4d8617582cbca01fc9eb70e289b35eb`; backend/test input source is `c980698b794fba9b02d0041207f57c7b5aa77bb7`, tree `b518062e41cdd42b9be37b53cb6ea110d61d1a40`, baseline `5417c257130216e2b283ca933d9496d76c10f46a`. Ticket authority: `ticket/42-mail-attachment-contract.md` §10 and revisions 220–236. Independent code review: `/tmp/wta-t42/final-independent-contract-review.md`. **State: draft; do not mark Ticket done or check unchecked boxes from this file.** `/tmp/wta-t42/final-backend-input-equivalence.json` proves c980→73c changes only four formally generated frontend contract files; backend/default/full/core/HTTP input is unchanged. Selected Mail27/Notify135 suites retained their older byte-identical inputs as delimited by `/tmp/wta-t42/dispatch04-real-input-equivalence.json`; neither was rerun at c980 or 73c. Frontend build and final static/governance gates were unfinished at this review time.

| Ticket §10 checkbox | Current code and actual evidence | Status / remaining proof |
|---|---|---|
| AC-042 no-link body mail, notice/workflow path retained | `NotifySceneCatalog` defines `demo-mail` title/content exception; Demo caller uses typed request. Red tests recorded original missing path/attachment loss; current `DemoNotifyCallerUnitTest`, `NotifyAttachmentDispatcherUnitTest` and full-context Mail27 source cover the positive and retained path validation. | Product behavior and selected tests substantiated. Final source/test retention still to bind to closing commit. |
| AC-042 one/multiple authorized attachments reach adapter; empty costs zero OSS | Real Mail27 `/tmp/wta-t42/runs/a36d6d66aa5e06bd/result.json` has 27/0/0/0, fresh 104-table MySQL/Redis/MinIO, exact original clean source, private byte/order/owner cases, and separate submit/worker zero-S3 case; only physical `MailNotificationSender` is a controlled byte-capture substitute. `NotifyAttachmentSnapshotTransactions` owns relation reservation/READY and bounded System copy. | Required owned integration behavior evidenced via source-equivalent test. No claim of real SMTP supplier delivery or production throughput at 10/25 MiB limits. |
| AC-042 unauthorized/missing/pending source denied; partial failure no send/unowned resource | Submit captures trusted User/Client, System checks ACTIVE/owner/Client/service and binds actual relation reference. Mail27 includes invalid owner/client, pending/late revocation, missing private target, second attachment confirmation/reservation failures, copy ACK loss, late PUT, `COPY_UNKNOWN`, actual source/target reference checks and sender zero. `send_reserved` plus terminal-unsent-only release prevents old in-flight send from losing refs. | Product/static and live DB/OSS cases evidenced. Unknown PUT/commit remains deliberately owned/manual, not auto-cleaned; no claim that an uncertain remote object was deleted. |
| AC-042 async worker cannot adopt own identity; retry/dedupe avoid growing snapshots | Intent persists original actor; background `NotifyContextConfiguration` returns null only for absent Sa-Token context; valid request resolver is checked after real login. Worker uses Intent actor. Mail27 includes changed User/Client, duplicate attachment set/order, forced unique-key collision with current locked read, two-recipient shared snapshot, old lease/cancel/send reservation and BEFORE/AFTER commit ACK. Notify eight-class regression `/tmp/wta-t42/regression-runs/0c1f52d8c3de3c81/result.json` has 135/0/0/0 at byte-equivalent selected inputs. | Evidenced for the controlled integration matrix; external Provider exactly-once is not asserted. |
| AC-042 production Snapshot SPI full-app wiring, not mock-only | Mail27 loads `NamewtaApplication` with real Notify/System/OSS/Redis/transaction beans and owned MinIO; production `NotifyAttachmentSnapshotAdapter` delegates System `OssService`; only bottom physical Mail sender is replaced. Core package/bundle and full-JAR HTTP also pass at c980. | Evidenced for wiring and actual S3 bytes; no real SMTP call by design. |
| Actual bound Skill use with hashes/inputs/steps/outputs | Ticket frontmatter binds engineering-standards, fullstack, module, common and Java API skills (implement), plus engineering verify; source/report choices match their scope. `T-42-replan-2026-09-23.md` still states Skill Execution Records are to be appended. | **Pending:** Lead must produce/check actual implement+verify execution records and current Skill hashes; frontmatter binding alone is not execution proof. |
| Normal, failure, regression, required E2E current-candidate evidence | Mail27 27/0/0/0 and Notify135 135/0/0/0 reusable only by explicit selected-input equality; c980 targeted three suites 8/0/0/0; default 265 fresh suites 1,184 = 940 executed + 244 environment skips, 0 fail/error; full package/bundle and core package/bundle exit 0. Full-JAR HTTP run `/tmp/wta-t42/openapi-http-live/2a1672a910adffea/result.json` accepted true, exact clean c980/full JAR, fresh 104-table owned DB/Redis/MinIO, default-SSE schema no loss, exact quoted >2^53 source relation, seven invalid/missing string-ID cases plus no token/no permission zero-write negatives, and cleanup empty. Earlier failed runs remain failed. At 73c, frontend architecture/architecture-test101/openapi-check/lint/typecheck and `pnpm test` commands have exit 0; build was still running. | **Pending:** final frontend build/artifact counts and source-after clean, applicable static/Skill gates and final result linkage. The 244 backend environment skips are not passes; owned service gates cover T42-required real classes. |
| Write set, shared owner, generated contract aligned | `5417..c980` product/test/SQL/skill/release tree contains 80 changed paths; c980→73c adds only four generated contract paths. New table and six-SQL 104 count, source/snapshot owner manifest, no allowlist bypass, direct API consumers and documentation were reviewed. Live raw OpenAPI is 438 paths/450 schemas with zero loss, optional `string[]` attachments and SSE routes. The committed revision provenance `rawSha256=3b7f8a6a...a1dda0` matches the live raw and committed source bytes, and `current.json` points to that revision; `openapi:check` exits 0 at 73c. | **Pending:** final path-scope/ancestry audit, generated consumer/build artifact and current source-after clean evidence. |
| Commit/direct-parent/result outlet | Local implementation c980 and generated-contract 73c commits exist; `/tmp/wta-t42/final-backend-input-equivalence.json` binds their input reuse. Live owned results and package proofs are retained. | **Pending:** final direct-parent and immutable result/evidence linkage under the active Goal. Do not infer Done from commit existence. |

## Eight accepted design choices and T-42's actual reach

The current ADR's historical `ADR-CR-002`–`009` are change-wide decisions, not eight separate T-42 features. This mapping checks that the T-42 implementation does not violate them; it does not pronounce adjacent tickets complete.

| Accepted choice | T-42 relation and evidence boundary |
|---|---|
| CR-002 log-copy and body limits | Internal pre-send callback is JSON-ignored and removed from FULL/REDACT event copies; attachment IDs/credentials do not become audit payload. The HTTP runner logs only safe codes/counts; T-02/T-03 body-budget completion is separate. |
| CR-003 trusted address/repeat-submit owner | T-42 does not change trusted proxy parsing or Redis repeat-submit ownership. Actual duplicate Intent/unique-key current-read path is tested; no global exactly-once promise is added. |
| CR-004 no browser shared encryption secret | T-42 adds typed public ID strings and standard full-JAR HTTP; it does not reintroduce browser private keys or alter HTTPS transport. T-11 remains separate. |
| CR-005 complete single-source release artifact | Full and core JARs have separate clean-source hashes/proofs; the HTTP run pins the full JAR specifically. Formal OpenAPI source is now committed with matching raw hash; frontend build and final release provenance remain outstanding. A core JAR is not substituted for full HTTP evidence. |
| CR-006 facts and gates accurately reflect source | Six-SQL fresh DB is 104 tables; related static facts, tests and OSS owner manifest were synchronized. Historical failing default attempts remain retained. Formal static/Skill proof is still required. |
| CR-007 bounded async/UI state | Mail copy and external send remain outside short DB result transactions, with lease/deadline and durable unknown fences. T-42 does not claim UI generation/session work for adjacent tickets; frontend checks for its generated consumer are pending. |
| CR-008 direct cutover of in-repo public API | `NotificationCommand` gains optional `List<String>` and in-repo Java constructors/consumers are migrated. No unauthorized actor field or legacy `templateParams` attachment backdoor; out-of-tree independent SPI implementations are outside the accepted direct-cutover guarantee. Formal generated TS consumer is committed and current OpenAPI check/typecheck exit 0; build artifact still pending. |
| CR-009 notification short transactions and recoverable queue | Intent/relation/ref submit, snapshot reservation/confirm and Mail send reservation use bounded short transactions and required locks; real MinIO/SMTP side effects are not claimed rollbackable. Commit ACK unknown and provider unknown conservatively retain ownership; Mail27/Notify135 exercise owner, token, rollback and conflict boundaries. No new queue/state platform was introduced. |

Current verdict: **no remaining reviewed product-source blocker**, but required verification and governance outlets above prevent AC-042 closure now. Subsequent evidence should be appended with exact source, command, fresh counts, skip count and cleanup, leaving each prior failed attempt unchanged.

## Required static gate result after this draft's first checkpoint

The fixed `73c28e` notify layered validator later exited **1** with eight architecture errors in `/tmp/wta-t42/static04-notify-layered.{json,log}`. **This supersedes the earlier “no product-source blocker” sentence above: there is now a confirmed blocking architecture defect, and no Ticket checkbox can be closed yet.** The defects concern adapter→DAO/Service imports, actor Port location, snapshot Service naming and four transaction annotations outside UseCase. `/tmp/wta-t42/layered-recovery-review.md` specifies a Port→UseCase→Service→DAO repair without weakening the validator. All earlier successful dynamic checks remain historical evidence for their immutable inputs; a new production revision must rerun its affected live transaction/worker/HTTP gates. FM/handbooks/release-contract success cannot cancel the failed layered gate.
