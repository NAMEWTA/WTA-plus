---
artifact: learning-goal-probe
probe_id: GP-L-043-b01
lesson_id: L-043
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-043 / b01

审问对象是已写成的 Lesson `lessons/L-043-enterprise-materials.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、verdict、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain / `claimed_cells` / 矩阵 (a) **两行**）：`A:EnterpriseMaterialAdminController.*`、`A:EnterpriseMaterialSelfController.*`。矩阵现印符号比星号窄、与磁盘公开映射 1:1：`EnterpriseMaterialAdminController.list, accessUrl` 与 `EnterpriseMaterialSelfController.attach, detach`。OBJ-43 原文：能口述企业材料 admin/self 切片。档是**解释+走查**。chain 覆盖列只写这两格 (a)。矩阵 (a) 两行模块都是 `wta-profile-enterprise`，均仍 `uncovered`。矩阵 (b) **没有** attach/detach/`reconcileReferences`/`accessUrlView` 独行；唯一企业 (b) 是 `EnterpriseTransferController.confirm`（L-042）。矩阵 (c) `wta-modules` 业务房间已 covered（L-002/L-003）；MySQL 8.4 / `dao-mapper-ownership` 仍 uncovered。矩阵 (d) **没有**企业材料挂接流；已 covered 的「主路径 profile 申请」是个人 submit → admin decide。`ProfileMaterialPort` 落在聚合行 `CompositeProfileService, ProfileService, PersonIdentityLookupService, ProfileMaterialPort`，不是本两行。分类牌 `MaterialTagController` 已随 L-037 covered。档案审核窗 archive 出门条留给 L-040。自助 current/submit 的 `validateRequired` / `snapshotImmutable` 调用点留给 L-041。厨房 `createEnterpriseMaterialService` / web-domain 留给 L-044。OSS 直传/卡片柜留给 L-027 / L-026。本批按派单审问这两格 (a)；口试按磁盘四扇 HTTP，不要把 UseCase 上无映射的 tree/create/snapshot 背成本课第五扇窗。

抽查锚点（2026-09-16）：`backend/wta-modules/wta-profile/wta-profile-enterprise/src/main/java/org/namewta/profile/enterprise/controller/{admin/EnterpriseMaterialAdminController,self/EnterpriseMaterialSelfController,admin/EnterpriseAdminController}.java`；`usecase/ProfileMaterialUseCase.java` 与 `usecase/impl/{EnterpriseProfileMaterialUseCaseImpl,EnterpriseProfileApiUseCaseImpl}.java`；`service/{EnterpriseMaterialService,EnterpriseProfileApiService,EnterpriseAdminService,EnterpriseApplicationService}.java`；`adapter/api/EnterpriseMaterialOwnerContributor.java`；`resources/mapper/enterprise/EnterpriseApplicationMapper.xml` 四条 lock；`pom.xml` 只声明 `wta-api`；测试 `EnterpriseMaterialAdminControllerContractTest`、`EnterpriseMaterialSelfControllerContractTest`、`EnterpriseBeanValidationContractTest`、`EnterpriseMaterialOwnerContributorTest`、`EnterpriseModuleArchitectureTest`；person 包 `controller/admin/{ProfileMaterialExceptionHandler,MaterialTagController,PersonMaterialAdminController}`、`controller/self/PersonMaterialSelfController`、`adapter/security/SaTokenProfileMaterialAccessPolicy`、`service/{ProfileMaterialService,IProfileMaterialService}`、`mapper/person/ProfileMaterialMapper.xml`、`ProfileMaterialServiceTest`、`ProfileMaterialMySqlE2ETest`；`wta-api/.../ProfileMaterialPort.java`；`10-cde-base-ddl.sql` `profile_material_ref`；`50-cde-base-dml.sql` 企业 TAG / 必传 / 菜单；登记表 `03-backend-module-modes.md` layered 行；`frontend/packages/domains/profile/src/{enterprise/materials/{service,index}.ts,service.test.ts,permissions.ts}`；`frontend/packages/api-contracts/generated/openapi.ts` vs `openapi/current.json`。

## Q1 澄清

- objective_id: OBJ-43
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-43；chain L-043 覆盖列两格 `.*`；L-043 `claimed_cells` 两格；矩阵 (a) 两行仍 uncovered，符号与磁盘四扇 1:1；(b) 无本课符号；(c) profile 房间已 covered、MySQL 8.4 未 covered；(d) 无企业材料流；课开篇「一块门牌、两份类、四扇窗」+ 负空间 L-037/L-040/L-041/L-042/L-044
- difficulty: medium

课把企业挂钩房收成「一块门牌、两份类、四扇 HTTP」。计划里至少有五套可数对象，派单点名的是 **两格 (a)**：

1. **OBJ-43 原文。** `course.md`：能口述企业材料 admin/self 切片。点名的是两份类，**没有**把 `list` / `accessUrl` / `attach` / `detach` 写成口试名词，也没有点名分类牌或厨房工厂。
2. **chain 覆盖列。** 两格星号。`*` 在磁盘上 = Admin 两扇 GET + Self 两扇 POST。chain 不写 Vue / `createEnterpriseMaterialService` / `ProfileMaterialPort` 聚合行 / `MaterialTagController`。
3. **课 `claimed_cells` / 矩阵 (a) 两行。** 符号就是 `EnterpriseMaterialAdminController.list, accessUrl` 与 `EnterpriseMaterialSelfController.attach, detach`，模块 `wta-profile-enterprise`。与磁盘四映射 1:1。Lead 若按行标 covered，**不会**把 L-037 标签五扇、L-040 十二扇或 L-044 工厂假闭合。
4. **课自己的操作定义。** 共用 `/profile/enterprise/materials`；GET 只读在 Admin、POST 挂摘在 Self；`owner(...)` 写死 `ProfileType.ENTERPRISE`；无 `@DeleteMapping`；挂摘不碰字节、不砸 OSS 箱子；分类牌仍打 `/profile/material-tags`（L-037）；archive 出门条是另一块门牌（L-040）。格子不认厨房工厂。
5. **个人对照不是同一套格子。** L-037 三格含 `MaterialTagController`（矩阵漏写的 `create` 已由 GP-L-037 记下）。本课**不**把分类牌再入格。企业 Self 路径与 `AttachRequest` 有 `@Positive`；个人没有。
6. **邻格。** L-040 `reviewMaterial` / `material` 写死 SUBMISSION / VERSION。L-041 submit 喊 `validateRequired(..., "*", qualifiers)` 与 `snapshotImmutable`，无材料 HTTP。L-042 转移。L-044 厨房四枪 URL。L-026 / L-027 仓库与直传。L-003 已认 `wta-profile` = layered。

问：本两格闭合标准是「磁盘四扇（GET 切开 Admin、POST 切开 Self）+ 写死 ENTERPRISE + 挂摘只动夹子 + 分类牌/archive/submit 闸都在邻格」，还是 OBJ-43 那句必须把 Admin 两扇从矩阵删掉、或必须先把 `MaterialTagController` / 厨房工厂并进本课才盖章？Lead 若只凭本课标 L-037 或 L-040 的 archive 出门条 covered，是否假闭合了标签五扇与档案十二扇？

## Q2 证据

- objective_id: OBJ-43
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Admin 28–67；Self 29–78；AdminContract 26–45；SelfContract 26–49；BeanValidation 56–74 / 112–113；UseCase 48–50；UseCaseImpl 20–90；EnterpriseMaterialService 11–88；Contributor 13–48；ApiService 43–95；ApiUseCaseImpl 32–42；Application XML 323–364；ArchitectureTest 75–113；pom 19；Policy 26–62；Handler 9–21；Port 56–83；Person Service 45–53 / 142–227 / 241 / 287–298 / 371–394 / 493–500 / 514–516 / 596–599 / 648–651；Material XML 74–78 / 103–118；DDL 1169–1183；DML 1378–1394 / 1420–1422 / 1515；Admin archive 70–84；AdminService 160–170 / 224–234；ApplicationService 164–176；Person Admin/Self 无 `@Positive`；EnterpriseProfileAccessUrl 13；R.fail 130 + HttpStatus.ERROR=500；E2E 82–138（个人 URL）；openapi.ts 2460–2497 / 5457–5468；current.json 无 `/profile/`；厨房 service.ts 28–39、index.ts 4–6、父级 service.test.ts 124 / 143；permissions.ts 14–20；登记表 layered 行 18
- difficulty: high

课的机制链哪些能在工作树直接复现，哪些只是图/校验句比磁盘窄、或把邻格 HTTP 说成本课第五扇？

**仍能复现（本两格）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 没有 `EnterpriseMaterialController` Java 类；前端资源标签才叫这个名字 | 仓库无该类；`enterprise/materials/index.ts` 4–6 `controller: 'EnterpriseMaterialController'` |
| Admin + Self 共用 `@RequestMapping("/profile/enterprise/materials")`；Admin 只有 GET，Self 只有 POST | Admin 28 / 36 / 47；Self 29 / 37 / 51 |
| `owner(...)` 写死 `ProfileType.ENTERPRISE`；路径 `ownerType` 只是四块板 | Admin 59–67；Self 64–72；两份 Contract `profileType()==ENTERPRISE` |
| Admin `list`：`GET /{ownerType}/{ownerId}`；权限 OR 五枚含 `:material`；无 `@Log`；返回 `R<List<MaterialReferenceView>>` | Admin 36–41；AdminContract 29–38 |
| Admin `accessUrl`：`GET .../{materialRefId}/access-url`；同 OR；返回 `R<EnterpriseProfileAccessUrl>`，调 `accessUrlView` | Admin 47–53；VO 13 四字段，不暴露 `OssAccessUrl` 类型 |
| Self `attach`：`POST /{ownerType}/{ownerId}`；`:material`；`@Log` INSERT 标题「挂接企业认证材料」且不记体；body `{ossId, materialNodeId}` | Self 37–45 |
| Self `detach`：`POST .../{materialRefId}/detach`；`:material`；`@Log` UPDATE 标题「解除企业认证材料」不记体；`R<Void>` | Self 51–58 |
| Admin **正好两扇 GET**、**零** POST/DELETE；Self **正好两扇 POST**、**零** GET/DELETE；Self 每扇 `isSaveRequestData=false`、`isSaveResponseData=false` | AdminContract 29–31；SelfContract 29–41 |
| Admin / Self 路径 `ownerId` / `materialRefId` 带 `@Positive`；`AttachRequest` 两字段也带；类上 `@Validated`；个人 Admin/Self/`AttachRequest` **没有** | BeanValidation 56–74 / 112–113；企业 Self 42 / 55–56 / 78；个人 Self 41–56 / 71–72、Admin 40–53 |
| `wta-profile` layered；生产 Controller 只持企业包 `ProfileMaterialUseCase`（字段名 `materialPort`）；禁止 import service | 登记表 18；ArchitectureTest 109–112；两份 Controller 第 9 / 32 行 |
| UseCase `accessUrlView` 是 default，旧适配器扔 `UnsupportedOperationException("旧适配器不支持文件访问地址入口")`；生产 Impl 覆盖 | UseCase 48–50；UseCaseImpl 70–76 |
| UseCase 每个方法（含 list/accessUrlView）都有 `@DSTransactional`；薄 `EnterpriseMaterialService` 把调用转给 `ProfileMaterialPort`，并投影四字段 | UseCaseImpl 20–90；EnterpriseMaterialService 11–88 / 68–73 |
| `EnterpriseMaterialService` **不**实现 Port，也**没有** `IEnterpriseMaterialService`；生产 service 包无 `*ServiceImpl`、无材料 Mapper | 类声明 11；ArchitectureTest 81–84；pom 只 `wta-api`（19） |
| `IProfileMaterialService` 空接口 extends Port，实现类在 person `ProfileMaterialService` | IProfileMaterialService 1–5；Person Service 45 |
| Handler 住在 person 包；`basePackages` 同时罩 person **和** enterprise 的 admin/self；企业模块没有自己的材料 Advice | Handler 9–21 |
| `lockOwner`：WORKING 锁申请行带回申请人；SUBMISSION join 申请；SOURCE/VERSION SQL 选出主键但 Service 把申请人丢成 null；找不到 → `MATERIAL_OWNER_NOT_FOUND` | ApiService 43–58；XML 323–356；Person Service 287–289 |
| PERSON 钥匙进企业贡献者 → `IllegalArgumentException("material owner must use ENTERPRISE profileType")`；对 VERSION 喊 `isWorkingEditable` → `"editable owner must use WORKING"` | ApiService 61–65 / 92–95；ContributorTest 67–74（只断言类型，不钉句子） |
| 企业四条独立 lock，不像个人 SOURCE/VERSION 合成 `lockMaterialImmutableOwner` | XML 323–356 vs person 那条合成锁 |
| 注解过了还有策略：前缀 `profile:` + 小写 profileType；WORKING attach = 申请人 + `:material`；SOURCE attach = `:override`；`requireWrite` **没有** override；`requireRead` = 管理四权 **或** 申请人 + `:material` | Policy 26–62 |
| WORKING 可编辑集合 `DRAFT/BACK/CANCEL`；企业实现是 count **`> 0`**；已提交 → `MATERIAL_OWNER_READ_ONLY`；SOURCE 挂接不查这扇 | XML 358–364；ApiService 66；Person Service 149–151 / 295–297 |
| attach 只接受 WORKING/SOURCE，否则 `IMMUTABLE_MATERIAL_OWNER`；校验失败零 `insertReference`、零 `reconcileReferences` | Person Service 145–147；ServiceTest 102–135 |
| 节点必须是启用 TAG，scope 为 COMMON 或 ENTERPRISE，否则 `MATERIAL_TAG_UNAVAILABLE` / `MATERIAL_TAG_NOT_APPLICABLE` | Person Service 371–376 |
| 当前 ATTACHED `< 10`；OSS 上传者必须是策略返回的当前用户；大小 `(0, 10MiB]`；扩展名与 suffix 一致；MIME 只认 jpeg/png/pdf | Person Service 46–53 / 154–161 / 382–394 |
| WORKING `immutable_flag=N`，SOURCE `Y`；唯一键冲突 → `MATERIAL_ALREADY_ATTACHED`；随后 `reconcileReferences("profile_material_ref", refId, empty, Set.of(ossId))` | Person Service 166–172 / 493–500 |
| detach 仅 WORKING；塑封夹子 `IMMUTABLE_MATERIAL`；已 DETACHED 直接 return 不打 OSS；SQL 只更新 `ATTACHED + immutable_flag='N'`；reconcile 从借出簿去掉，不是删对象 | Person Service 179–196；XML 103–108 |
| 夹子必须属于这把 owner 钥匙，否则对外仍 `MATERIAL_NOT_FOUND` | Person Service 648–651 |
| `list` 按 owner 拉未删引用，**含 DETACHED**，`order by attached_time, material_ref_id` | XML 110–118；Person Service 202–206 |
| `accessUrl`：WORKING 已摘 → `MATERIAL_NOT_ATTACHED`；非 WORKING 无这道短路；过策略才 `resolveAccessUrl`，再投影四字段 | Person Service 211–219；EnterpriseMaterialService 68–73 |
| 生成列 `active_owner_oss_key`：同一 owner 上同一 oss 同时只能一条 ATTACHED | DDL 1169–1183 |
| 种子企业根下两棵二级分类、三枚 TAG；三枚 TAG `systemRequired=Y`；必传三条 `document_type_code='*'`：ALWAYS 执照+法人证件，办理人非法人再要授权书 | DML 1382–1394 / 1420–1422 |
| 菜单权限串 `profile:enterprise:material` | DML 1515 |
| 厨房四枪 GET list / POST attach / POST detach / GET access-url；`encodeURIComponent` 路径段；`'VERSION', 9, 10` → `GET .../VERSION/9/10/access-url` | `enterprise/materials/service.ts` 28–39；父级 `src/service.test.ts` 124 / 143 |
| 生成 OpenAPI 已有企业材料四条路径（`list_1`/`attach_1` 同 URL 的 GET/POST、`detach_1`、`accessUrl_1`）；`put?: never` / `delete?: never`；`openapi/current.json` 搜不到 `/profile/` | generated 2460–2497 / 5457–5468；current.json 零命中 |
| archive 出门条是隔壁门牌 `/profile/enterprise/archive`：`reviewMaterial` 权限 `:review` 写死 SUBMISSION；`material` 权限单独 `:material` 写死当前 VERSION | AdminController 70–84；AdminService 160–170 |
| 直建 SOURCE attach 循环 `EnterpriseAdminMaterialBo` 调端口，**不经** Self Controller；`validateRequired(..., legalDocumentTypeCode, {ALWAYS})` **不加**授权书条件 | AdminService 224–234 |
| submit 调 `validateRequired(working, "*", ALWAYS±HANDLER_NOT_LEGAL_REPRESENTATIVE)` 再 `snapshotImmutable(WORKING→SUBMISSION)`；无材料 HTTP | ApplicationService 164–176 |
| `validateRequired` / `snapshotImmutable` / tree/createNode 在 UseCase 上，**没有**本课 HTTP | UseCase 16–31 / 54–59；两份 Controller 无这些映射 |
| 非法 id：企业先撞 `@Positive`；若落到 Port compact，抛的是 `IllegalArgumentException("ownerId must be positive")` / `"ossId and materialNodeId must be positive"`，不是 `MATERIAL_*` | Port 56–83；Person Service `requirePositive` 失败码是 `{field}_INVALID`（596–599） |

**课写过、图或校验句比磁盘窄（不是证伪）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「本课 Self 类**可以**打 SOURCE attach」 | 映射存在。Self 注解**只**要 `:material`（Self 38）；策略 SOURCE **只要** `:override`（Policy 33–34）。两道门叠起来：只有 override、没有 `:material` 的管理员进不了这扇 POST，要走 L-040 `admin-create` 端口循环。课没把「SOURCE 走 Self HTTP = 两枚通行证」写成半句 |
| 「HTTP 仍可能是 200，`msg` 才是业务码」 | Handler `R.fail` + 个人 E2E `status().isOk()` 成立。未写：`R.fail(String)` 把 JSON `code` 写成 `HttpStatus.ERROR`（**500**）。口试若说「信封 code 也是 200」会错。企业材料 HTTP **没有**自己的 MockMvc E2E；200 信封证人是个人夹具 + Handler `basePackages` 含 enterprise |
| 「企业根下两棵二级分类、三枚 TAG，全部 `systemRequired=Y`」 | 三枚 TAG 是 `'Y'`（DML 1392–1394）。两棵二级 CATEGORY 与企业根是 `'N'`（1382–1386）。图 2 只给 TAG 标了 Y，文字「全部」写宽了。停用/归档保护是 L-037 标签窗，不是本课四扇 |
| 「`service.test.ts` 里 `'VERSION', 9, 10`」 | 枪在父级 `frontend/packages/domains/profile/src/service.test.ts` 124 / 143，**不是** `enterprise/materials/service.test.ts`（该文件不存在） |
| 「个人 countEditable」对照 | 企业是 `> 0`（ApiService 66）；个人是 `== 1`。主键行上等价，口试不必展开 |
| ContributorTest「PERSON 钥匙会抛错」 | 断言 `IllegalArgumentException`，**不**钉 `"material owner must use ENTERPRISE profileType"`。句子在 ApiService 94 |
| 已摘再 detach 幂等 | Person Service 191–192 有 early return；`ProfileMaterialServiceTest` 没有单独用例钉「第二次不打 OSS」 |
| Admin 合同「无 `@Log`」 | 源码 Admin 两扇确实无 `@Log`；合同测试只数 GET、禁 POST/DELETE、钉 OR 五枚，**没有**断言无日志注解 |
| 厨房 `accessUrl` 返回类型 | TS 写成 `OssAccessUrl`（service.ts 16），HTTP 投影是 `EnterpriseProfileAccessUrl`。四字段同形。工厂格子是 L-044 |

问：在一块门牌两份类、GET/POST 切开、写死 ENTERPRISE、无 DELETE、策略两道门、attach 拒绝链零写库、detach 不砸箱、list 含 DETACHED、出门条先鉴权已经能互证的前提下，两格 (a) 是否还要把「SOURCE 走 Self HTTP 需要 `:material` 加 `:override`」「JSON `code=500`」「E2E 证人是个人 URL」「图 2 文字把 CATEGORY 也写成 Y」写成开篇半句才算讲完？还是这些已经落在课的表/反例或邻格里，Lead 可以按现印两行盖章？

## Q3 假设与反例

- objective_id: OBJ-43
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无 EnterpriseMaterialController 类；Admin 无 POST；detach 非 DELETE；override 不能 requireWrite；archive 出门条是 L-040；person URL 另一前缀；同一 oss 不能双 ATTACHED；Handler 200+R.fail；current.json 无 `/profile/`；SOURCE Self 需两枚通行证；分类牌是 L-037；submit 闸是 L-041
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪只夹子认错？

1. **「一份 `EnterpriseMaterialController`。」** 没有这个 Java 类。课开篇表 / 误区 8 覆盖。
2. **「Admin 类也能 attach。」** Admin 只有 GET。课反例 1 覆盖。合同测试 `noneMatch PostMapping \|\| DeleteMapping`。
3. **「detach = DELETE / 删 MinIO。」** POST `.../detach` + reconcile 减引用。课反例 2 覆盖。
4. **「列表 VO 的 url 就能预览。」** list 是夹子清单；出门条只在 access-url。课反例 3 覆盖。
5. **「`/profile/enterprise/archive/{profileId}/material/{ref}/access-url` 是本课 Admin 材料窗。」** L-040。课反例 4 覆盖。
6. **「URL 上的 ownerType 能把档案改成个人。」** 写死 ENTERPRISE。课反例 5 覆盖。
7. **「override 可以帮申请人摘 WORKING 夹子。」** `requireWrite` 只要申请人。课反例 6 覆盖。
8. **「同一张执照照片夹到营业执照和法人证件。」** 生成唯一列按 owner+oss，不是按 tag。课反例 7 覆盖。
9. **「挂别人的 OSS。」** `MATERIAL_OSS_OWNER_MISMATCH`。课反例 8 覆盖。
10. **「提交后还能在 WORKING 上补图。」** `MATERIAL_OWNER_READ_ONLY`。课反例 9 覆盖。
11. **「企业也有 MaterialTagController / 材料 Mapper。」** 分类牌在 person 包；企业 POM 看不见 person Mapper。课反例 10 / 误区 7 / 11。
12. **「业务失败一定 403/404。」** 材料业务码走 Handler → HTTP 200 + `R.fail`。课反例 12。信封 JSON `code=500` 课未点名。
13. **「`EnterpriseMaterialService` 就是 Port 实现。」** 它是转发器。课反例 13。
14. **「字段名 `materialPort` = Controller 直接拿跨模块端口。」** 类型是企业包 UseCase。课反例 14。
15. **「snapshot / validateRequired 是本课第四、第五扇窗。」** UseCase 有方法，Controller 无映射。课反例 15。
16. **「材料上传走这扇 POST。」** body 只有 ossId + nodeId。课误区 3。
17. **「Admin 就是管理员专用，申请人 list 不了。」** GET 的 OR 含 `:material`；`requireRead` 也给申请人。课误区 5。
18. **「apply 权限也能挂材料。」** 申请桌是 `:apply`（L-041）。材料自助是 `:material`。课误区 16。
19. **「OpenAPI current.json 就是权威。」** 快照缺 `/profile/`。课误区 12。
20. **「OBJ-43 还包含 `createEnterpriseMaterialService`。」** OBJ-44。课误区 1。
21. **「管理员有 override 就能 POST Self SOURCE attach。」** 策略过 override，注解不过 `:material`。课正例 5 只说「Self 类可以打」，没写两枚通行证。这是本批最值得贴的半句，不是第二容器。
22. **「个人 HTTP 非法 id 和企业一样先被 `@Positive` 挡住。」** 企业才有。课误区 15 覆盖主语，Q2 补了 Port compact 是 IAE。
23. **「图 2 没标 Y 的二级分类也是系统牌。」** 磁盘 `systemRequired=N`。不要把标签窗保护规则误套到 CATEGORY。
24. **「chain 的 `.*` 已随矩阵两行闭合到 tree/createNode。」** 星号只覆盖两份 Controller 的公开映射。UseCase 目录方法无本课 HTTP。
25. **「企业 POM 依赖 person，所以企业自己写夹子 SQL。」** 只依赖 `wta-api`。夹子 DAO/XML 在 person。

问：读完后仍说不出的，是不是「两份类的名字」，而是 **GET 在 Admin 且申请人也能打、POST 挂摘在 Self、override 不能 detach、SOURCE 走 Self HTTP 需要两枚通行证、摘夹子不砸箱、业务码经常是 200 信封、分类牌/archive/submit 闸都不在本课四扇**？

## Q4 视角

- objective_id: OBJ-43
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-43 vs L-003/L-026/L-027/L-037/L-040/L-041/L-042/L-044；`ProfileMaterialPort` 聚合行；MySQL 8.4；API-005 存量 POST
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课两格 (a)**？

| 视角 | 多出来的边 | 是否本课两格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 两行 vs 个人 L-037 三行（含标签）；(b) 无挂摘行；(c) MySQL 8.4 uncovered；(d) 无材料流 | **是** 闭合定义。两行可按现印盖章。不要发明 (b)/(d)，不要把 Port 聚合行或厨房工厂算进来 |
| 挂钩房（本两份类） | 一块门牌、GET/POST 切开、写死 ENTERPRISE、无 DELETE、安全 `@Log`、企业 `@Positive` | **是** (a) |
| 策略第二道门 | WORKING 申请人 vs SOURCE override；`requireWrite` 无 override；`requireRead` 给申请人；Self SOURCE 还需注解 `:material` | **是** 两行的 explained 失败句。不要另开 Policy 行 |
| 夹子/借出簿 | attach 拒绝链零写库；detach 只改状态；reconcile ±ossId；生成唯一列 | **是** Self 两扇。`objectStore.delete` **否**（L-026） |
| 出门条 | 先 `requireRead` 再 `resolveAccessUrl`；投影 `EnterpriseProfileAccessUrl` | **是** Admin `accessUrl`。PUBLIC/PRIVATE TTL **否**（L-026） |
| 所有者锁 | 四条独立 FOR UPDATE；SOURCE/VERSION 申请人为 null；可编辑 `DRAFT/BACK/CANCEL` | **是** 四扇的前置。person 合成锁 **否**（对照一句即可） |
| 无 HTTP 的复印/必传 | `snapshotImmutable` 三对迁移；`validateRequired` 缺码 `MISSING_REQUIRED_MATERIAL:TAG` | 负空间一句 **是**。调用点 **否**（L-041 submit / L-040 直建） |
| 档案审核窗（L-040） | `/profile/enterprise/archive/.../access-url` 两扇 + 直建 SOURCE 循环 | 对照门牌 **是**。十二扇档案窗 **否** |
| 自助申请（L-041） | current/save/submit/probe 何时 validate/snapshot | **否** |
| 转移（L-042） | send/confirm/unbind；(b) confirm | **否** |
| 分类牌（L-037） | `/profile/material-tags` 五扇；企业 scope 也打这里 | 对照一句 **是**。标签行 **否**（已 covered） |
| 厨房（L-044） | `createEnterpriseMaterialService` 四枪、`encodeURIComponent`、资源字符串 | URL 对照 **是**。工厂格子 **否** |
| OSS 卡片柜 / 直传（L-026 / L-027） | 涂黑 url、PENDING、init/complete | 夹子≠箱子 **是** 边界。仓库四扇 **否** |
| `ProfileMaterialPort` 聚合行 | wta-api 端口；person Service 经 `IProfileMaterialService` 实现 Port | 本课当电线 **是**。聚合行四名字 **否** |
| API-005 | 变更目标 POST；本课挂摘已是 POST，无 DELETE | 盘点现状 **是**。不发动传输 Ratchet **否** |
| MySQL 8.4 / Redis 8 | 三张材料表、生成列、企业四条 lock | 表证据 **是** (a)。容器 **否**（L-085） |
| 个人对称（L-037） | 三份类、无 `@Positive`、合成锁、count `== 1` | 对照一句 **是**。个人三行 **否**（已 covered） |

问：Lead 若凭「两份类名在函数表里」把两行标 covered，是否还要求读者能指着 **Admin OR 含申请人、override 不能摘、SOURCE 走 Self 要两枚通行证、摘夹子不砸箱、200+业务码、企业有 `@Positive`、分类牌/archive 不在本课**？这些是不是已经在本两格函数表/反例里，而不需要 (b) 新行、也不需要把厨房或标签窗算进来？

## Q5 元问题

- objective_id: OBJ-43
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 两行、无 (b)、无材料 (d)
- difficulty: low

建议：`defer`

原因：

- **两格 (a) 与矩阵 / `claimed_cells` / OBJ-43 1:1。** L 合同章节齐全（宏观地图、定义表、机制链 1–6、带 caption / 文字等价物 / 图边界的两张图、正反例、变式、误区、暂停走查、来源表 S-004/S-008/S-010/S-015/S-L037-01/S-L040-01/S-L041-01/S-L043-01…08）。**主叙事站得住且与源码对齐**：一块门牌两份类四扇 HTTP、GET 在 Admin、POST 在 Self、写死 ENTERPRISE、无 DELETE、安全 `@Log`、策略两道门、attach 拒绝链零写库、detach 不砸箱、list 含 DETACHED、出门条先鉴权、企业 `@Positive`、分层五层 + Policy/Contributor/OssService 三根电线、夹子表在 person、企业只锁四块板。不必为「两份类名」再挖五问。
- 本批可粘贴的精度项不挡住把 **两行 (a)** 标 covered：（1）SOURCE 走 Self HTTP = 注解 `:material` **加** 策略 `:override`，override-only 管理员走 L-040 直建端口；（2）JSON `code=500` 而 HTTP 行 200，企业材料无自己的 MockMvc E2E；（3）图 2 文字把二级 CATEGORY 也写成 `systemRequired=Y`；（4）厨房对照枪在父级 `src/service.test.ts`。不要把分类牌、archive 十二扇、submit 闸、厨房工厂、MySQL 容器塞进这两格。
- 不建议 `mine-more`：b01 未证明两行还有真实矩阵空洞；再五问只会把 L-037 标签、L-040 直建、L-041 submit、L-044 厨房或 L-026 仓库再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；半句精度（两枚通行证、信封 code=500、图 2 文字）不是第二容器。不建议 `split-lesson`：未满 10 问；两份类是 chain 原设计的同一 OBJ-43、同一企业材料 HTTP，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：挂摘副作用、reconcile、出门条关闭是 (a) 的 explained。不建议改 `course.md` OBJ 才盖章：四扇已在矩阵/chain/`claimed_cells`，OBJ 短句只是没点名方法。
- **不要把分类牌并进本课，也不要等 GP-L-042 / L-044 才给本两行盖章。** `MaterialTagController` 已随 L-037 covered。L-042 转移仍 uncovered，不是本课符号。L-044 工厂是厨房格子。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:EnterpriseMaterialAdminController.list, accessUrl | 共用门牌、仅 GET、OR 含 `:material`、无 `@Log`、`accessUrlView` 投影、list 含 DETACHED、拒绝后不二次 `resolveAccessUrl`、路径 `@Positive`——均与源码对齐。残留：JSON `code=500`；企业材料无自己的 MockMvc E2E（200 信封证人是个人夹具 + Handler `basePackages`）。 | 可选半句：信封 HTTP 200、JSON code 500、`msg` 才是材料码。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-043 来源表 S-L043-01…06）。 | defer（本行可 covered；精度项不挡盖章） |
| A:EnterpriseMaterialSelfController.attach, detach | POST 挂摘、body 无字节、WORKING/SOURCE 才能挂、仅 WORKING 可变摘、override 不能 `requireWrite`、reconcile ±ossId、不 DELETE、`AttachRequest`/`path` `@Positive`——均与源码对齐。残留：SOURCE 走本窗 HTTP 需要 `:material` **加** `:override`；已摘再 detach 的幂等无单独单测。 | 可选半句：只有 override 的管理员打不进 Self POST，直建走 L-040 端口循环；企业非法 id 先撞 `@Positive`，个人没有。Lead 可将本行标 `covered`。 | defer（本行可 covered） |
| A:MaterialTagController.tree, update, status, archive | 企业 scope 分类牌仍打这扇；本课只对照。图 2 文字把二级 CATEGORY 写成 `systemRequired=Y`（磁盘为 N）。 | 保持。L-037 已 covered。不要把标签行并进本课。图 2 半句可留给原课补丁，不是盖章前提。 | defer（L-037 已 covered） |
| A:EnterpriseAdminController.* | 本课只有两张 archive 出门条对照 + 直建 SOURCE 循环负空间。 | 保持。L-040 已 covered。 | defer（L-040） |
| A:EnterpriseApplicationController.current, save, submit, probe | submit 的 ALWAYS±授权书闸已借用。挂材料 HTTP 未算进该行。 | 保持。L-041 已 covered。 | defer（L-041） |
| A:EnterpriseTransferController.send, confirm, unbind / B:confirm | 本课未认。 | 保持。L-042。 | defer（L-042） |
| A:createProfileService / A:createProfileWebDomain enterprise | 厨房四枪 URL 只作对照。资源标签名 `EnterpriseMaterialController` 不是 Java 类。 | 保持。L-044（person 厨房已由 L-039 covered）。 | defer（L-044） |
| A:PersonMaterialAdminController.list, accessUrl / A:PersonMaterialSelfController.attach, detach | 对称切片；个人无 `@Positive`。 | 保持。L-037 已 covered。 | defer（L-037） |
| A:SysOssController.* / A:SysOssUploadController.* | 夹子≠箱子；直传不在本课 POST。 | 保持。L-026 / L-027。 | defer（OSS 课） |
| CompositeProfileService, ProfileService, PersonIdentityLookupService, ProfileMaterialPort | 本课把 Port 当电线；聚合行四名字未认。 | 不要并进两份 Controller 才能盖章。 | defer（未认的聚合行） |
| (b) attach/detach/reconcile/accessUrlView | 矩阵无此行。课已写进 (a)。唯一企业 (b) 是转移 confirm。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 企业材料挂接流 | 矩阵无此行。ASCII 图不是新的 (d)。 | 不要发明企业材料数据流行。 | defer（未入库） |
| MySQL 8.4 / Redis 8 / dao-mapper-ownership | 三张材料表与四条 lock 是 (a) 证据，不是容器/组件闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085 / L-083） |
| API-005 变更 POST | 本课挂摘已是 POST，无 DELETE。 | 本课盘点，不发动 Ratchet。 | defer（传输 Ratchet） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本两行无「课没讲到」的 uncovered 空洞需要靠 b02 继续挖问（SOURCE 两枚通行证、信封 code=500、图 2 文字是半句精度）；合格读者说得出两份类目的（只读两扇 / 挂摘两扇）、主路径（WORKING attach → list/access-url → detach，箱子仍在；SOURCE 一挂即塑封）与失败路径（策略拒绝不签发、不可变板不能摘、业务码 200 信封）；把可选半句写进原课不会超过 40 分钟；档案窗/申请闸/分类牌/厨房/OSS 已有邻课。满 10 问前不拆。两份类虽跨 admin/self 包，仍是 chain 原设计的同一 OBJ-43，同一 `wta-profile-enterprise` 容器，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-037 分类牌、L-040 档案窗、L-041 申请闸、L-042 转移、L-044 厨房、L-026/L-027 OSS、Port 聚合行、未入库 (b)/(d)、MySQL/Redis 容器、dao-mapper 组件以及 API-005 显式建议 `defer`；对 `A:EnterpriseMaterialAdminController.list, accessUrl` 与 `A:EnterpriseMaterialSelfController.attach, detach` 建议 Lead 标 `covered`（证据锚本 probe 与 L-043 来源表）。本课不主张改 Java 窗、把分类牌并进来、发明 (b)(c)(d) 格子、或等转移/厨房课才盖章。
