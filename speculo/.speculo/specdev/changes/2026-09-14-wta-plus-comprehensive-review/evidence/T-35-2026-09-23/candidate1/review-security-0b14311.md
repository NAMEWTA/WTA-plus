# T-35 fixed candidate 2 — security/specification delta review

**Source verdict: PASS for the five-path delta; no remaining blocker found in this review.** Base candidate `41882b54a2b644f7ae661b84205cfcc9d1b72886`, candidate 2 `0b14311c9652b4d765c4db8c8898d28662eb5adb`, tree `61db0cf144ec2aa1d42cdf01c9cd81aaebb4eaf8`; candidate 2 is the direct child of candidate 1. Lead reported a clean pre-acceptance snapshot at 09:24:54Z. Read-only `git diff 41882b5..0b14311 -- backend` shows exactly five product paths: Dispatch service, planner, their unit tests, and the isolated SMS integration test. No repo files, services, Maven, or E2E were changed/run by this reviewer. This source verdict does not mark AC-035's required six-case real integration as passed; Lead had not supplied its result at this checkpoint.

## Previously blocking failure phase is closed

`DispatchNotificationService.java:89,111-131,170-181` now sets `smsClientEntered` immediately before `notifyClient.send`. A generic SMS failure while planning, acquiring quota, or assembling the request is classified `FAILED/PREPARATION_RETRYABLE` with fixed text, without entering the common client. `NotifyDispatchResultService.java:86-95,152-156` does not treat this category as terminal `configFailure`: it records an attempt, returns Delivery to `PENDING`, and Outbox to `READY` with bounded backoff/max attempts. This is neither `UNKNOWN` nor `WAITING_RECEIPT`, closing the candidate-1 AC-035 blocker. The new unit test `DispatchNotificationServiceTest.java:232-250` injects a throwing quota and asserts zero `NotifyClient.send`, `PENDING/READY`, and the fixed code. The new integration scenario (`NotifySmsDispatchIntegrationTest.java:343-365`) corrupts only its owned Redis template counter, expects zero supplier calls and release of the previously held account slot, repairs that owned counter, then expects one accepted call and two attempts. Its execution remains pending.

The existing `NotifyValidationException` catch at `DispatchNotificationService.java:160-169` remains earlier than the generic catch: SMS validation codes are allowlisted into `SMS_LOCAL_VALIDATION_*`, `FAILED`, and terminal `DONE` via `configFailure`'s `SMS_` prefix. The existing unit tests inject both known and arbitrary validation codes; the latter becomes a fixed `VALIDATION_ERROR` rather than persisting a canary. Once the code has entered `NotifyClient.send`, a generic runtime failure stays `UNKNOWN/DISPATCH_ERROR` and therefore `WAITING_RECEIPT`; the existing physical supplier exception path maps `PROVIDER_ERROR` to `PROVIDER_OUTCOME_UNKNOWN`. No new automatic retry of an ambiguous provider call was introduced. IN_APP and MAIL generic behavior remains the candidate-1 behavior. A common-client exception that actually occurs before supplier I/O remains conservatively UNKNOWN; T-37 owns deeper common idempotency phase classification, so this review does not claim every internal common failure obtains a receipt.

## Quota release check

`NotifySendPlanner.finish` (`NotifySendPlanner.java:125-159`) encloses acquisition and later local calculations in a catch that releases previously **confirmed** holds before rethrowing the original failure. Denial paths continue to release earlier holds. `releaseHeld` (`:193-208`) attempts all held keys in reverse order, collects release exceptions, clears the list, and propagates cleanup failure without allowing a second decrement. The catch adds a cleanup failure as suppressed on the original exception and preserves that original classification. Planner tests at `NotifySendPlannerTest.java:68-126` cover later quota failure releasing the account hold and a throwing release preserving the original exception. This is sound best-effort compensation for confirmed holds. It cannot guarantee release if the quota port increments but throws before reporting success, or if Redis itself refuses the release; the code does not falsely retry a possibly applied decrement. The real integration scenario tests the normal recoverable second-key failure, not every partial Redis I/O state.

## Isolated fixture change and prior security scope

The six-SQL base already seeds `auth-captcha/SMS`. `NotifySmsDispatchIntegrationTest.java:129,189-213,221-243,274-282,316-323` now snapshots that known unbound seed row, replaces its account/template fields only inside the uniquely named owned database, and restores the seed in `@AfterEach`; it removes only its generated account/intent and owned Redis keys. `@Execution(SAME_THREAD)` prevents this class from overlapping its own fixed scene-binding updates. The integration test still asserts original provider ID retained in the internal DB, hidden REDACT query/duplicate receipt/monitor ID, FULL projection preservation, safe snapshot, and callback-compatible internal ID as reviewed in candidate 1. I did not re-review unchanged product code: the prior fixed-source findings and closed-event/log/management-projection checks are in `/tmp/wta-t35/review-security-41882b5.md`; its only blocking finding is superseded by the phase fix above.

## Evidence boundary

The candidate-2 six-case MySQL/Redis result, exact selected XML counts/zero skips, cleanup and final clean source identity must be supplied by Lead before ticket acceptance. Candidate-1 passing units or its earlier five-case run cannot substitute for candidate-2 integration. This report evaluates source and test design only; no test execution is attributed to this reviewer.

## Addendum — missed typed pre-provider failure (C2 remains blocked)

The PASS above was limited to the five changed paths and missed a relevant unchanged common-client phase. Independent functional review at `/tmp/wta-t35/review-functional-0b14311.md` identified the concrete path, which I verified against the fixed `0b14311` source: `NotifyDispatcher.send` calls `beginIdempotency` at `NotifyDispatcher.java:73-78` **before** `adapter.send` at `:98-100`; `NotifyIdempotencyCoordinator.begin` wraps Redis acquire failure in `NotifyIdempotencyUnavailableException(phase="ACQUIRE")` (`NotifyIdempotencyCoordinator.java:28-41`). C2 sets `smsClientEntered=true` just before the entire `notifyClient.send` call (`DispatchNotificationService.java:130-131`), so that typed `ACQUIRE` failure reaches its generic catch (`:170-180`) and still becomes `UNKNOWN/WAITING_RECEIPT` with zero supplier I/O. This is the same AC-035 local-error contract violation as the earlier blocker, now inside the common-client call. In contrast, `NotifyIdempotencyUnavailableException(phase="COMPLETE")` occurs after provider work may have happened (`NotifyIdempotencyCoordinator.java:44-49`), so it must remain conservatively UNKNOWN. A typed ACQUIRE-only correction and zero-supplier assertion are needed; generic errors after client entry must not all become retryable.

**Corrected overall recommendation for C2: REQUEST CHANGES.** The five-path pre-client quota fix and quota-release behavior passed this incremental source check, but C2 is not a two-axis acceptance pass. Lead subsequently reported the six real integration cases passed and owned-resource cleanup completed; I have not independently inspected that run, and those six cases do not erase this uncovered ACQUIRE path. The original incremental PASS text is intentionally preserved above as a historical checkpoint, not the final C2 verdict. C3 is being edited separately and was not reviewed here.
