---
artifact: learning-goal-probe
probe_id: GP-L-041-b01
lesson_id: L-041
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-041 / b01

审问对象是已写成的 Lesson `lessons/L-041-enterprise-application.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、verdict、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain / `claimed_cells` / 矩阵 (a) **两行**）：`A:EnterpriseApplicationController.current,save,submit,probe`、`A:EnterpriseVerificationAnonymousController.callback`。OBJ-41 原文：能口述 `EnterpriseApplicationController.current/save/submit/probe` 与 `EnterpriseVerificationAnonymousController.callback`。档是**解释+走查**。chain 覆盖列只写这两格 (a)。矩阵 (a) 两行模块都是 `wta-profile-enterprise`，均仍 `uncovered`。矩阵 (b) **没有** submit 回滚 / probe 占用灯 / callback 幂等独行；唯一企业 (b) 是 `EnterpriseTransferController.confirm`（L-042）。矩阵 (c) `wta-modules` 业务房间已 covered（L-002/L-003）；MySQL 8.4 / `dao-mapper-ownership` 仍 uncovered。矩阵 (d) **没有**「企业申请」走廊；已 covered 的「主路径 profile 申请」原文是 `PersonApplication.submit → admin decide`，备注 L-035+L-034。课 `claimed_cells` **未**列 (d)。个人对照：L-035 格子只有 `current, submit`（save 是邻居、probe 不存在）；个人回调单独是 L-038。本课按 OBJ-41 **把 save/probe 入格，并把企业回调钉在同一课**。`EnterpriseAdminController.*` / `decide` 留给 L-040；转移三枪留给 L-042；材料 attach/detach 留给 L-043；`createProfileService` / enterprise 页面留给 L-044；个人核身回调留给 L-038。

抽查锚点（2026-09-16）：`backend/wta-modules/wta-profile/wta-profile-enterprise/src/main/java/org/namewta/profile/enterprise/controller/{self/EnterpriseApplicationController,anonymous/EnterpriseVerificationAnonymousController,advice/{EnterpriseApplicationExceptionHandler,EnterpriseVerificationCallbackExceptionHandler}}.java`；`usecase/{EnterpriseApplicationUseCase,EnterpriseVerificationUseCase,impl/{EnterpriseApplicationUseCaseImpl,EnterpriseVerificationUseCaseImpl}}.java`；`service/{EnterpriseApplicationService,IEnterpriseApplicationService,EnterpriseVerificationAttemptService}.java`；`dao/{EnterpriseApplicationDao,EnterpriseVerificationAttemptDao}.java`；`src/main/resources/mapper/enterprise/{EnterpriseApplicationMapper,EnterpriseVerificationAttemptMapper}.xml`；`listener/EnterpriseApplicationProcessListener.java`；`adapter/{gateway/SpringEnterpriseWorkflowGateway,provider/EnterpriseManualVerificationProvider,codec/EnterpriseVerificationEvidenceCodec}.java`；`domain/{application/{EnterpriseApplication,EnterpriseIdentityFields},bo/{EnterpriseApplicationSaveBo,EnterpriseApplicationSubmitBo,EnterpriseApplicationProbeBo},vo/{EnterpriseApplicationVo,EnterpriseApplicationProbeVo},verification/{EnterpriseVerificationCallbackOutcome,EnterpriseVerificationFailureCategory},ProfileEnterpriseApplication}.java`；`src/test/.../controller/{self/EnterpriseApplicationHttpContractTest,anonymous/EnterpriseVerificationAnonymousControllerTest}.java`；`architecture/EnterpriseModuleArchitectureTest.java`；`service/impl/{EnterpriseApplicationServiceTest,EnterpriseApplicationPersistenceTest,EnterpriseApplicationMySqlE2ETest,EnterpriseApplicationServiceImpl,EnterpriseVerificationCallbackContractTest,EnterpriseVerificationMySqlE2ETest}.java`；`release-artifacts/docker/infrastructure/mysql/init/{10-cde-base-ddl.sql,50-cde-base-dml.sql}`；`.agents/skills/engineering-standards/references/project/03-backend-module-modes.md` layered 行；`frontend/packages/domains/profile/src/enterprise/application/{service,index}.ts`（旁证，格子归 L-044）；`frontend/packages/api-contracts/generated/openapi.ts` 四枪自助 + 门铃。

## Q1 澄清

- objective_id: OBJ-41
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-41；chain L-041 覆盖列两格 (a)；L-041 `claimed_cells` 两格；矩阵 (a) 两行仍 uncovered；(b) 无本课符号；(c) profile 房间已 covered、MySQL 8.4 未 covered；(d) 无企业申请行，已 covered 的主路径是个人 submit→decide；课开篇「认矩阵 (a) 两格」+ 负空间 L-040/L-042/L-043/L-044/L-038；对照 L-035 三枪/无 probe、L-038 单独回调
- difficulty: medium

课把企业自助窗收成「四枪名字 + 一扇门铃 + 分层五层」。计划里至少有六套可数对象，派单点名的是 **两格 (a)**：

1. **OBJ-41 原文。** `course.md`：能口述 `EnterpriseApplicationController.current/save/submit/probe` 与 `EnterpriseVerificationAnonymousController.callback`。档是解释+走查。一句里五枪、两份 Controller。没有把 `handleProcess`、`EnterpriseAdminController.decide`、材料 attach、转移 send/confirm/unbind、厨房工厂写成口试清单。
2. **chain 覆盖列。** 两格 (a)。不写 (b)，不写 (d)，不写 save 之外的邻居。
3. **课 `claimed_cells` / 矩阵 (a) 两行。** 符号就是 `EnterpriseApplicationController.current, save, submit, probe` 与 `EnterpriseVerificationAnonymousController.callback`，模块 `wta-profile-enterprise`。与磁盘四映射 + 一扇匿名 POST 1:1。Lead 若按行标 covered，**不会**把管理端十二扇、转移三枪或个人 L-038 假闭合。
4. **课自己的操作定义。** GET 根 = `current`（无 body，空申请 `R.ok(null)`）；POST 根 = `save`（入格，不是邻居）；POST `/submit` = `submit`（body 只有 `expectedVersion`）；POST `/probe` = `probe`（body 只有 `unifiedCreditCode`，**不**读 `userId`）。门铃 `POST /profile/enterprise/verification/providers/{providerCode}/callback`，`@SaIgnore`。自助权限一把 `profile:enterprise:apply`。分层 `Controller/Listener → UseCase(@DSTransactional) → Service → DAO → Mapper XML`。submit 出口是 `WAITING` + 核身尝试 + 工作流门铃，**不是** `profile_enterprise`。callback 出口是 `ACCEPTED` / `IDEMPOTENT` / `LATE_IGNORED`，**不**发布档案。
5. **个人对照不是同一套格子。** L-035 矩阵只认 `current, submit`；save 是课内邻居。个人没有 probe。个人回调是 OBJ-38 / L-038 独行。本课把 save/probe **升格入 (a)**，并把企业门铃钉在同一 OBJ-41。不要用 L-035 的「两枪」口试清单复读本课。
6. **矩阵 (d) 不是本课符号。** 「主路径 profile 申请」已按个人 submit + L-034 decide 盖章。课变式写「把企业 submit → admin decide 说完整需要 L-040」，那是邻格对照，不是第二张 (d) 行。不要为本课发明企业申请走廊才能盖 (a)。
7. **邻格。** L-040 管理端十二扇与 decide。L-042 转移。L-043 ALWAYS / 授权书材料窗。L-044 厨房四枪 URL。L-038 个人门铃。L-003 已认 `wta-profile` = layered。

问：本两格闭合标准是「四枪 URL/动词/权限 + 空申请 200 空 data + probe 不读口袋只回 status + submit 先冻快照再按门铃且失败回滚 + 门铃三态不发布 + 字段名 `service`/`coordinator` 实际是 UseCase/端口」，还是 OBJ 那句必须把 callback 拆成第二课才盖章、或必须先发明企业 (d) 走廊 / 等 GP-L-040 decide 才能给自助四枪盖章？Lead 若只凭本课标 L-040 或 L-038 covered，是否假闭合了管理端十二扇与个人门铃？

## Q2 证据

- objective_id: OBJ-41
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 29–74；Anonymous 27–72；Advice 10–16 与 12–25；UseCase 12–42；UseCaseImpl 26–56；VerificationUseCaseImpl 17–26；Service 58–183 / 188–223 / 262–303 / 307–384 / 848–870；AttemptService 28–124；Mapper XML 7–15 / 57–76 / 79–96 / 99–117 / 155–162；HttpContractTest 24–64；ArchitectureTest 74–114 / 162–172；AnonymousControllerTest 42–86；CallbackContractTest 42–88；ServiceTest 72–157；PersistenceTest 36–51；E2E 71–174 / 209–241 / 343–353；Verification E2E 77–113；Manual 13–30；Listener 19–33；Gateway 15–49；VO/BO；DDL 981–1026；DML 535 / 1428 / 1430 / 1514；frontend service.ts 13–25；openapi.ts 2380–2393 / 2624–2687；R.fail 130 + HttpStatus.ERROR=500
- difficulty: high

课的四枪自助 + 一扇门铃哪些能在工作树直接复现，哪些只是测试名单比课表窄、或把档案诞生讲成了另一扇门？

**仍能复现（两格 (a)）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 自助门牌 `/profile/enterprise/application`；四映射无 PUT/DELETE/`{id}` | Controller 29；openapi.ts 2624–2687 `put?: never` / `delete?: never`；合同测试 26–27 / 47–54 |
| 门铃门牌 `/profile/enterprise/verification/providers/{providerCode}/callback`；无 GET | Anonymous 27 / 48；openapi.ts 2380–2393 `get?: never` |
| 字段名叫 `service`，类型是 `EnterpriseApplicationUseCase` | Controller 32 |
| 门铃字段名叫 `useCase` + `timeSource`，**不是** `@RequiredArgsConstructor` | Anonymous 30–40 |
| `current` GET 根、无 `@Log`；`save` POST 根 `@Log` UPDATE 双 false；`submit` POST `/submit` `@Log` OTHER 双 false；`probe` POST `/probe` `@Log` OTHER 双 false | Controller 37–74；合同测试 36–41 / 47–54 |
| 四方法权限都是 `profile:enterprise:apply`；`self` 无 `@SaIgnore` | Controller 每方法；合同测试 32–35；ArchitectureTest 166–171 |
| 门铃方法 `@SaIgnore`；`@Log` 标题「企业认证供应商回调」双 false | Anonymous 45–47；AnonymousControllerTest 61–63 |
| 主人只来自 `LoginHelper.getUserId()`（probe 除外）；SaveBo 组件名不含 `userId`/`applicantUserId`；SubmitBo 只有 `@PositiveOrZero int expectedVersion`；ProbeBo 只有 `unifiedCreditCode` | Controller 40 / 51 / 62 / 73；SaveBo 10–29；SubmitBo 6；ProbeBo 7；合同测试 28–30 |
| Controller 不把 SubmitBo 整份下传，只拆 int；probe **不**传 userId | Controller 62 / 73 |
| 生产 Controller 只 import usecase，不 import service | ArchitectureTest 107–112；Controller import 第 14 行 |
| UseCase 无参 default `@Deprecated`，调用抛「请传入 userId」；带 userId 的 default 回落到无参；`probe` 不是 default | UseCase 14–37 |
| 生产实现覆盖 `current/save/submit/probe/handleProcess`，**每一枪** `@DSTransactional`；核身 `callback` 同样盖章；Service 方法本身无此注解 | UseCaseImpl 26–56；VerificationUseCaseImpl 20–26 |
| 生产 Service 类名就是 `EnterpriseApplicationService`，实现 `EnterpriseApplicationPublicationPort`，**不** `implements IEnterpriseApplicationService` | Service 58；接口 `IEnterpriseApplicationService.java` 存在但生产类声明不含它 |
| 核身协调器类名 `EnterpriseVerificationAttemptService`，实现端口 `EnterpriseVerificationService`；UseCase 字段名叫 `coordinator` | AttemptService 29；VerificationUseCaseImpl 18 |
| 生产 service 包 **没有** `*ServiceImpl`；测试适配器在 `src/test/.../EnterpriseApplicationServiceImpl`，同时 `implements UseCase` + `IEnterpriseApplicationService` | ArchitectureTest 81–84；测试类 20–21 |
| `current`：`requireUserId` → `selectOpenByUserId`（**无** `for update`）→ `Vo.from` 或 `null` | Service 106–108 / 262–263；XML 7–15 |
| 开着的 SQL 状态：`DRAFT,BACK,CANCEL,WAITING` 且 `del_flag='0'`；无 `LIMIT 1` | XML 14 |
| 空申请是成功：Controller `R.ok(voOrNull)`，不是 404 | Controller 40；Service `orElse(null)` |
| VO 有企业展示字段 + `snapshotVersion`/`version`；**没有** `identityKey`/`applicantUserId`/`targetProfileId` | Vo 11–37 vs IdentityFields 14 |
| ProbeVo 组件名恰好只有 `status` | ProbeVo 4；合同测试 55–56 |
| 可编辑 = `DRAFT/BACK/CANCEL`；`EnterpriseApplication.editable()` 与 `EDITABLE_STATUSES` 同组；`WAITING` 不可 save/submit | domain 21–23；Service 61 / 132–133 / 154–155 |
| `save`：WAITING → `ENTERPRISE_APPLICATION_READ_ONLY`；normalize 信用代码 strip+`Locale.ROOT` 大写当 `identityKey`，邮箱小写；INSERT 写死 `DRAFT`、`submission_seq=0`、`version=0`；`updateDraft` 把 BACK/CANCEL 拉回 DRAFT 且 `version+1`，WHERE 带 `applicant_user_id` | Service 127–145 / 307–328；XML 79–117；IdentityFields 34–45 |
| 账号未绑、信用代码已有档案时 `targetProfileId` 盯那份；单测钉 9201 | Service 137–142；ServiceTest 72–88 |
| `probe`：null command → `ENTERPRISE_PROBE_REQUIRED`；格式错 → `ENTERPRISE_CREDIT_CODE_INVALID`；`selectProbeStatus` CASE 顺序 `IN_PROGRESS` → `BOUND` → `UNBOUND` → `AVAILABLE`；不写库、不锁行 | Service 113–121 / 301–302；XML 57–76；ServiceTest 92–97 空白/大小写 |
| probe 的 `IN_PROGRESS` 用申请表 `identity_key` 等值；`BOUND`/`UNBOUND` 用档案表 `upper(trim(unified_credit_code))` | XML 59–74 |
| `submit` 顺序：锁开着的行 → 本人且可编辑 → 版本 → `validateComplete` → 供应商 → `requireSubmissionAllowed` → ALWAYS（±授权书）材料 → `insertSubmission` → `snapshotImmutable(WORKING→SUBMISSION)` → `markWaiting` → `startAttempt` → `workflow.start` | Service 151–182；ServiceTest 101–118 |
| 无开着的行 → `ENTERPRISE_APPLICATION_NOT_FOUND`（`lockOpenByUserId` → `requireApplication`） | Service 268–269 |
| `WAITING` 再 submit → `ENTERPRISE_APPLICATION_NOT_EDITABLE` | Service 154–155 |
| 版本不对 / `markWaiting` 条件不中 → `ENTERPRISE_APPLICATION_VERSION_CONFLICT` | Service 157–158；XML 160–161 `submission_seq = #{submissionSeq} - 1` 且可编辑且旧 version |
| 账号已有有效绑定 → `ENTERPRISE_ACCOUNT_ALREADY_RESPONSIBLE`；企业已有有效绑定（含 `SUSPENDED`）→ `ENTERPRISE_RESPONSIBLE_ALREADY_BOUND` | Service 333–346；PersistenceTest 36–51 |
| 办理人非法人才加 `HANDLER_NOT_LEGAL_REPRESENTATIVE`；法人只有 `ALWAYS`；文档类型参数 `"*"` | Service 165–170；ServiceTest 101–136 |
| 指纹 `SHA-256(identityKey + "\n" + snapshotVersion)`；供应商异常收成 `ENTERPRISE_PROVIDER_UNAVAILABLE` | Service 848–870 |
| 网关：无 `WorkflowService` 或无 `profile.enterprise.flowCode` → `ENTERPRISE_WORKFLOW_UNAVAILABLE`；`startCompleteTask` 失败 → `ENTERPRISE_WORKFLOW_START_FAILED`；变量 `profileType=ENTERPRISE`、`snapshotVersion`、`submissionId`；`businessId` 是申请主键字符串 | Gateway 27–49 |
| 自助 Advice `R.fail(exception.getMessage())`；`R.fail(String)` 的 JSON `code=500`（`HttpStatus.ERROR`）；HTTP 状态由 MockMvc `isOk()` | Advice 14–16；R.java 130；HttpStatus 82；E2E 149–150 |
| 门铃 Advice：`INVALID_SIGNATURE`/`EXPIRED_CALLBACK` → HTTP 401；其余 → HTTP 400；`msg` 英文句子，类别在 `data.category` | CallbackAdvice 17–24；AnonymousControllerTest 83–86；Verification E2E 94–97 |
| 生成列 `open_user_id` / `open_identity_key` 只在进行中四态取值；两 unique key；实体 `FieldStrategy.NEVER` | DDL 986–1026；实体 34–40 |
| 配置种子 `profile.enterprise.provider.default=manual`、`profile.enterprise.flowCode=profile_enterprise_verification`；权限串 `profile:enterprise:apply` | DML 535 / 1428 / 1430 / 1514 |
| 登记表 `wta-modules/wta-profile` = layered | `03-backend-module-modes.md` 18 |
| 前端厨房四枪 URL/动词与本窗对齐；`controller: 'EnterpriseApplicationController'`；probe 走 `raw` 再 `projectStatusProbeResponse` | `service.ts` 17–24；`index.ts` 4–7 |
| submit 成功当时 VO `WAITING`；档案行要另调 `finish()` | E2E `apply` 176–183 / `finish` 209–216 |
| 门铃成功三态；伪造签名 401；冲突 400；申请仍 `WAITING`；证据 JSON 不含 `"signature"` | AnonymousControllerTest 70–77；CallbackContractTest 42–66；Verification E2E 82–113 |
| 默认 `manual.authenticate` 直接 `UNSUPPORTED_CALLBACK`；`start` 返回 `pending()` | Manual 20–29 |
| 测试供应商时钟偏差 300 秒 → `EXPIRED_CALLBACK` | DeterministicTestProvider 21；CallbackContractTest 79–83 |

**邻格仍能复现（不计入本两行 (a) 闭合；用来判断走廊能不能单独盖章）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| E2E 在 submit 之后另调 `handleProcessEvent("finish")` 才出现 `profile_enterprise` + ACTIVE 绑定 | E2E `finish()` 209–216 / 人生 71–86 |
| Listener 把 `ProcessEvent` 转成 `EnterpriseApplicationProcessCommand`，`@Order(HIGHEST_PRECEDENCE)`，不是这五扇 HTTP 的同步返回 | Listener 19–33 |
| `handleProcess`：flowCode 不对或 businessId 非正数 → return；必须仍是 `WAITING` 且 seq 对得上；`FINISH`+`REJECT` → `INVALID` 不发布；`FINISH` 非拒绝 → `publishApproved`；迟到快照不发布 | Service 188–222；ServiceTest 139–157 |
| **管理端汇**是 L-040 `decide`：门牌 `/profile/enterprise/archive/application/{applicationId}/decision`，权限 `profile:enterprise:override` | AdminController 90–96 |
| 迟到 finish(2) 在当前快照 3 时不 `publishApproved` | ServiceTest 149–152 |

**课写过、测试名单更窄或把汇讲偏（不是证伪 (a)）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「连只读的 `current` 和只读的 `probe` 也盖了 `@DSTransactional`」 | UseCaseImpl 26–48 **有**；合同测试只断言 `save`/`submit`/`handleProcess`（57–63），**没有** `current(long)` / `probe` |
| 「E2E 工作流失败回滚是 UseCase 事务章的铁证」 | E2E fixture 237–241 把 **测试适配器** `EnterpriseApplicationServiceImpl` 直接塞进 Controller，**不经过** `UseCaseImpl`。失败后显式 `session.rollback()`（151 行）。HTTP `isOk` + `$.msg=ENTERPRISE_WORKFLOW_UNAVAILABLE` + 回滚后 DRAFT/0 submission/0 attempt 仍成立，但证人是 JDBC 会话回滚，不是 Spring `@DSTransactional` |
| 门铃 E2E「走 UseCaseImpl」 | Verification E2E 77–78 `new EnterpriseVerificationUseCaseImpl(coordinator)`，同样是手工 new，**不是** Spring 代理；`session.commit()` 自己管事务。`@DSTransactional` 在这份夹具里同样不生效 |
| UseCaseImpl.probe 的 javadoc 写成「预检查企业申请是否满足提交条件」 | UseCaseImpl 44。磁盘实现是占用灯 `Service.probe`，不是提交预检。课机制链 5 对齐 Service，**没有**把这行 javadoc 当口试定义 |
| 「档案/绑定是 `handleProcess` 收到 `FINISH` 之后」 | 这是引擎回写门 + E2E 短路。生产管理端汇是 L-040 `decide` HTTP。矩阵 (d) **没有**企业申请行 |
| 口试动词写成 GET/POST | 厨房 `method: 'get'`/`'post'` 小写（axios 合同）。语义对 |
| `selectOpenByUserId` 无 `LIMIT 1` | XML 事实；课边界 4 已写。无单测打脏数据多行 |
| Service 构造有未使用的 `Object jsonMapper` | Service 80；真正序列化 `JsonUtils.toJsonString`。课边界 3 已写 |
| 300 秒过期窗口 | 测试供应商数字，不是 `manual` 行为。课图 4 边界已写 |

问：在四枪 URL/权限/空申请 200、probe 四态灯、submit 先冻材料再开流程、工作流失败不留快照、门铃三态不发布、生成列占坑、分层不越层已经能互证的前提下，两格 (a) 是否还要把「合同测试没钉 current/probe 的事务注解」「自助 E2E 绕开 UseCaseImpl」「UseCaseImpl.probe javadoc 写偏」写成开篇半句才算讲完？企业 (d) 走廊是否根本不存在于矩阵，因而不必等 GP-L-040 才能给本两行盖章？

## Q3 假设与反例

- objective_id: OBJ-41
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 空申请非 404；WAITING 不可再 submit；body 无 userId；probe 非匿名/非 current 过滤；工作流失败不留 submission；非转移窗；字段名 service≠classic；自助 Advice HTTP 仍 200、门铃 Advice 改状态；Listener/callback 非 submit 同步返回；VO 无 identityKey；材料窗是 L-043；E2E finish≠admin decide；manual 拒收回调；个人 L-035 无 probe、个人回调是 L-038
- difficulty: high

课的隐含假设若反转，合格读者会把哪把枪或哪条走廊认错？

1. **「`current` 没有申请就 404。」** 磁盘 `orElse(null)` + `R.ok`。课反例 1 覆盖。
2. **「`WAITING` 再 POST `/submit` 幂等返回同一 VO。」** `ENTERPRISE_APPLICATION_NOT_EDITABLE`。课反例 2 覆盖。
3. **「body 里塞 `userId` 能替别人交。」** SaveBo 无此字段；`updateDraft` WHERE 还带 `applicant_user_id`。课反例 3 覆盖。
4. **「工作流 Bean 缺失时 submission 留下当审计。」** 与事务边界相反。课反例 4 覆盖。证人精度见 Q2：E2E 靠 `session.rollback()`。
5. **「这扇窗的 submit 就是换负责人 / 企业转移。」** 账号已绑停在 `ENTERPRISE_ACCOUNT_ALREADY_RESPONSIBLE`；企业已绑停在 `ENTERPRISE_RESPONSIBLE_ALREADY_BOUND`。课反例 5 / L-042。
6. **「`probe` 是 GET `/application/{creditCode}` 或匿名查询。」** POST、要 `profile:enterprise:apply`、只回 status。课反例 6。
7. **「`probe` 看的是我的申请。」** Controller 不调 `LoginHelper`。别人占用同一信用代码也会 `IN_PROGRESS`。课开篇对照表。
8. **「字段名 `service` / `coordinator` = classic `Controller → Service → Mapper`。」** 架构测试会拆掉 Controller import service。课反例 7。
9. **「自助 `R.fail` 是 HTTP 500 页面；门铃失败也是 HTTP 200 + `msg=类别码`。」** 自助 E2E `status().isOk()`；门铃伪造签名 `isUnauthorized()`，冲突 `isBadRequest()`。课反例 8 / 误区 7。
10. **「`current` 是 `GET /application/{id}`。」** 没有 id。办结后 SQL 看不见，变 null。课误区 1。
11. **「`WAITING` 排除出 current。」** SQL 包含它。课误区 2 / 图 3。
12. **「submit body 还要再交企业字段。」** SubmitBo 只有版本号。课误区 3。
13. **「UseCase 是空转发，可以删。」** 事务注解除了它没处盖。课误区 5。测试适配器不是生产盖章位置（见 Q2）。
14. **「`EnterpriseApplicationServiceImpl` 是生产分层。」** 只在 test。课误区 6。
15. **「材料校验是 L-041 的 HTTP。」** submit 喊端口，挂材料是 L-043。课误区 8。
16. **「VO 带 `identityKey`。」** 没有。课误区 9。
17. **「Listener 或 callback 画进 submit 时序的同步返回。」** 浏览器拿到的是 WAITING。课误区 10。
18. **「供应商回调会发布档案 / 改申请状态。」** E2E 在 `ACCEPTED` 之后申请仍 `WAITING`。课反例 8 / 合同测试名 `…WithoutPublishing`。
19. **「默认供应商 `manual` 会接回调。」** `authenticate` 抛 `UNSUPPORTED_CALLBACK`。课反例 9。
20. **「本课覆盖管理员 `decide`，所以企业申请走廊可 covered。」** 矩阵 (d) **没有**这行。课自己写「decide 是另一张桌子」。把假设反转后，Lead 会用本行假闭合 L-040。
21. **「E2E `finish` 就是管理端 decide。」** `finish()` 造 `ProcessEvent` 直接打 `handleProcessEvent`。生产 decide 是另一扇 HTTP。课把两条门收成一句「流程回写 / 管理员决定」，没有把「E2E 短路 ≠ decide HTTP」写成反例（与 GP-L-035 同构精度）。
22. **「企业格子和个人一样只有 current/submit；callback 该等 L-038。」** OBJ-41 把 save/probe 入格，门铃钉在本课。L-038 符号是 `PersonVerificationAnonymousController.callback`。假等 L-038 会把企业门铃留成空洞。
23. **「save 不该在矩阵 (a)，因为 L-035 没写。」** 本课格子名就是四枪。降格会发明「邻居」口径，和磁盘/OBJ 冲突。
24. **「进行中四态之外，decide 过程中 current 仍看得到本子。」** 若企业 decide 也走 `OVERRIDE_PENDING` 一类中间态，那是 L-040 状态机，不是本两格的证伪。本课 XML 的 GET 四态不含终态。
25. **「企业 probe 和个人 rebind probe 是同一扇门。」** 门牌 `/profile/enterprise/application/probe` vs `/profile/person/rebind/probe`。四态单词碰巧同形。课误区 12。

问：读完后仍说不出的，是不是「五枪的名字」，而是 **本两行含 save/probe 且含门铃、空申请是 200 空 data、probe 不认口袋、submit ≠ 档案、callback ≠ 档案、E2E finish 不是 admin decide、manual 拒收、字段名 service 不是 classic**？

## Q4 视角

- objective_id: OBJ-41
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-41 vs L-003/L-035/L-038/L-040/L-042/L-043/L-044；claimed_cells 无 (d)；Admin decide 链；生成列；UseCase 事务；门铃三态
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:EnterpriseApplicationController.current, save, submit, probe`** 或 **`A:EnterpriseVerificationAnonymousController.callback`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 两行 vs 个人 L-035 一格+L-038 一格；(d) 无企业申请行；`claimed_cells` 无 (d)；(b) 无 submit 回滚/callback 幂等行 | **是** 闭合定义。两行可单独 covered。不要发明 (d)。save/probe 不要降格 |
| 自助窗作者 | 四映射、权限、Log 不存包体、LoginHelper（probe 除外）、Advice 类别码 | **是** 第一格。decide HTTP **否** |
| 门铃柜员 | `@SaIgnore`、信封四字段、`timeSource.now()`、HTTP 401/400、三态不发布、`manual` 拒收 | **是** 第二格。个人门铃 **否**（L-038） |
| 分层电工 | Controller↛Service；Service↛Mapper；DAO `@Repository`；XML 语句 id 对齐；禁止注解 SQL；SaIgnore 只在 anonymous | **是** 本窗五层。整个 profile 房间画像 **否**（L-003 已 covered） |
| 提交规则书 | 锁→完整校验→负责人闸→ALWAYS±授权书→冻快照→WAITING→核身→门铃；失败类别码 | **是** 第一格主链。材料挂载 HTTP **否**（L-043） |
| 占用灯作者 | CASE 四态；不读 userId；格式闸 | **是** 第一格的 probe。个人 rebind probe **否**（L-036） |
| 档案走廊作者 | 源：POST `/submit`；中：WAITING + 流程实例；汇：档案/绑定 | submit 半段 **是** 第一格副作用。汇若按管理端 = L-040 decide，**否**。矩阵无企业 (d) 行，**不要**发明 |
| 管理端柜员（L-040） | `decide` 十二扇之一 | 对照一句「submit ≠ 档案」**是**。十二扇全表 **否**。GP-L-040 尚未落盘 |
| 转移窗（L-042） | send/confirm/unbind；(b) confirm | 失败闸 **是** 边界。当场换口袋 **否** |
| 材料窗（L-043） | attach/detach WORKING | submit 的 `validateRequired` **是** 闸。HTTP **否** |
| 前端厨房（L-044） | `createEnterpriseApplicationService` 四枪 | URL 对齐旁证 **是**。工厂格子 **否** |
| 个人对照（L-035/L-038） | 无 probe；callback 拆课 | 「企业多 probe、门铃钉在本课」**是** 对照。个人两格 **否** |
| MySQL 8.4 容器 | 生成列当裁判 | 本窗 unique 意图 **是** 机制。容器课 **否**（L-085） |
| dao-mapper-ownership | DAO 唯一持 Mapper | 本窗举例 **是**。组件行 **否**（L-083） |
| (b) submit 回滚 / callback 幂等 | 矩阵无此行 | 课已写进 (a)。不要发明 (b) |

问：Lead 若凭「五枪都在函数表里」把两行标 covered，是否还要求读者能指着 **空申请 200、probe 四态灯、submit 先冻后按铃、失败不 publish、门铃三态不改申请、生成列占坑、分层不越层、submit ≠ 档案、callback ≠ 档案**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要把 admin decide 算进 (a)、更不需要等 L-038？企业 (d) 是否明确写成「矩阵无此行，对照一句即可」？

## Q5 元问题

- objective_id: OBJ-41
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 两行、无 save 降格、无 (b)；矩阵 (d) 无企业申请行；个人回调是 L-038 另一符号
- difficulty: low

建议：`defer`

原因：

- **两格 (a) 与矩阵 / `claimed_cells` / OBJ-41 1:1。** L 合同章节齐全（宏观地图、直觉十四句、定义表、机制链 1–10、带 caption / 文字等价物 / 图边界的四张图、正反例、变式、误区、暂停走查、来源表 S-004/S-008/S-010/S-L041-01…10）。**主叙事站得住且与源码对齐**：四扇自助 HTTP、一扇门铃、权限一把、LoginHelper（probe 除外）、空申请 `R.ok(null)`、UseCase 盖事务章、probe 四态灯、submit 先冻材料再 `workflow.start`、失败类别码、生成列占坑、submit 当时没有 `profile_enterprise`、callback 三态不发布、`manual` 拒收。不必为「五枪名字」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例或邻格，不挡住把 **两行 (a)** 标 covered：（1）合同测试未钉 `current(long)` / `probe` 的 `@DSTransactional`（源码有）；（2）自助 E2E 工作流失败走测试适配器 + `session.rollback()`，门铃 E2E 手工 `new UseCaseImpl` 同样不是 Spring 代理；（3）UseCaseImpl.probe javadoc 写成「预检查」，实现是占用灯；（4）厨房动词小写；（5）`jsonMapper` 未使用。不要把 decide、材料窗、转移、厨房工厂、个人门铃塞进本两行。
- **不要发明企业 (d) 行，也不要等 GP-L-040 才给本两行盖章。** 矩阵「主路径 profile 申请」已按个人 submit + L-034 decide covered。本课只把企业源讲完：POST `/submit` → `WAITING` + 门铃；callback 只给尝试盖章。汇在 L-040 的 decide。课/E2E 常用 Listener `FINISH` 当短路；这是邻格对照，不是 (a) 的空洞。GP-L-040 尚未落盘，不阻塞本两行。
- **不要把企业门铃留给 L-038。** L-038 符号是 `PersonVerificationAnonymousController.callback`。OBJ-41 已经把企业门铃钉在本课；拆走会制造矩阵空洞，且未满 10 问、未超 40 分钟。
- 不建议 `mine-more`：b01 未证明两行还有真实矩阵空洞；再五问只会把 L-040 decide、L-042 转移、L-043 材料、L-044 厨房或 L-038 个人门铃再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；可选半句（E2E 短路 ≠ decide HTTP；E2E 回滚证人是 JDBC 会话；probe javadoc 写偏）不是第二容器。不建议 `split-lesson`：未满 10 问；两格是 chain 原设计的同一 OBJ-41（自助四枪 + 企业门铃），同一模块同一容器，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：乐观锁、回滚、生成列、门铃幂等是 (a) 的解释到副作用。不建议把 save/probe 降成邻居才盖章：格子名就是四枪。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:EnterpriseApplicationController.current, save, submit, probe | 门牌四映射、权限/Log、LoginHelper（probe 除外）、空申请 200 空 data、probe 四态灯不认口袋、分层五层、submit 先冻快照再按门铃、失败类别码、生成列占坑、submit ≠ 档案——均与源码对齐。残留精度：（1）合同测试未钉 current/probe 的 `@DSTransactional`；（2）E2E 失败回滚绕开 UseCaseImpl；（3）UseCaseImpl.probe javadoc 写偏；（4）厨房动词小写。 | 可选半句：口试事务章以 `EnterpriseApplicationUseCaseImpl` 为准；E2E 工作流失败的回滚证人是测试会话；probe 以 Service/XML 占用灯为准，不以 UseCaseImpl javadoc 为准。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-041 来源表 S-L041-01…10）。 | defer（本行可 covered；精度项不挡盖章） |
| A:EnterpriseVerificationAnonymousController.callback | 门牌 `@SaIgnore`、信封四字段、`timeSource.now()`、三态 `ACCEPTED`/`IDEMPOTENT`/`LATE_IGNORED`、401/400 Advice、不改申请状态、不发布档案、`manual` 拒收、证据 JSON 无 signature——均与源码/合同/E2E 对齐。残留精度：门铃 E2E 手工 new UseCaseImpl，不是 Spring 事务代理。 | 可选半句：口试事务章以生产 `EnterpriseVerificationUseCaseImpl` 为准。不要把本格留给 L-038。Lead 可将本行标 `covered`。 | defer（本行可 covered；不要拆给 L-038） |
| D:主路径 profile 申请（PersonApplication.submit → admin decide） | 矩阵已 covered（L-035+L-034）。本课企业 submit 半段是对照，不是该行符号。 | 不要用本课改该行，也不要发明「企业申请」第二 (d)。对照一句「企业同样 submit ≠ 档案」即可。 | defer（未入库；矩阵无企业 (d)） |
| A:EnterpriseAdminController.* / decide | 本课只保证 submit/callback 不发布档案。decide HTTP 未认格。GP-L-040 尚未落盘。 | 保持。L-040。 | defer（L-040） |
| A:EnterpriseTransferController.send, confirm, unbind / B:confirm | 本课只保证占用停在 `ENTERPRISE_ACCOUNT_ALREADY_RESPONSIBLE` / `ENTERPRISE_RESPONSIBLE_ALREADY_BOUND`。 | 保持。L-042。 | defer（L-042） |
| A:EnterpriseMaterialAdminController.list, accessUrl / A:EnterpriseMaterialSelfController.attach, detach | submit 的 ALWAYS±授权书闸已借用。挂材料 HTTP 未讲。 | 保持。L-043。 | defer（L-043） |
| A:createProfileService / createProfileWebDomain enterprise | 四枪 URL 旁证已借用。 | 保持。L-044。 | defer（L-044） |
| A:PersonApplicationController.current, submit | 个人无 probe、save 不入格的对照。 | 保持。L-035 已 covered。 | defer（L-035） |
| A:PersonVerificationAnonymousController.callback | 同形门铃、不同门牌。 | 保持。L-038。不要把企业门铃算进该行。 | defer（L-038） |
| handleProcess / EnterpriseApplicationProcessListener | 课已讲 FINISH 发布与 Order。矩阵 (a) **无此符号**。 | 保持对照，不升第六法。 | defer（未入库对照） |
| IEnterpriseApplicationService | 生产未接线。 | 不是矩阵行。 | defer（未入库） |
| (b) submit 回滚 / callback 幂等 / 生成列 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| C:MySQL 8.4 / mysql-base | 生成列是本窗机制，不是容器课。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |
| C:dao-mapper-ownership | 本窗 DAO 持 Mapper 是举例。 | 保持。L-083。 | defer（L-083） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本两行 (a) 无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出五枪目的（看我的进行中本子 / 铅笔改字 / 交不可变复印件并按门铃 / 问占用灯 / 供应商门铃盖章）、主路径（save → submit → WAITING；callback → ACCEPTED 且申请仍 WAITING）与失败路径（版本冲突、不可编辑、负责人闸、工作流失败不留快照、伪造签名 401、`manual` 拒收）；企业 (d) 缺的是矩阵根本没有这行，不是讲义容器；把可选半句写进原课不会超过 40 分钟；管理端、转移、材料、厨房、个人申请/门铃已有邻课。满 10 问前不拆。四枪与门铃虽跨 self 与 anonymous 两份 Controller，仍是 chain 原设计的同一 OBJ-41，同一 `wta-profile-enterprise` 容器，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-040 decide、L-042 转移、L-043 材料、L-044 厨房、L-038 个人门铃、L-035 个人申请、未入库 handleProcess/`IEnterpriseApplicationService`/(b)/企业 (d)、MySQL 容器与 dao-mapper 组件显式建议 `defer`；对 `A:EnterpriseApplicationController.current, save, submit, probe` 与 `A:EnterpriseVerificationAnonymousController.callback` 建议 Lead 标 `covered`（证据锚本 probe 与 L-041 来源表）；对矩阵「主路径 profile 申请」建议保持已 covered 的个人定义，不要发明企业 (d)。本课不主张降格 save/probe、把门铃拆给 L-038、发明 (b)、或把 Listener FINISH 写成矩阵 (d) 的汇。
