# T-18 evidence

Current HEAD `a43d133d` already meets AC-018. No new implementation was required. This cancels only the repeated construction. AC-018 stays owned by T-30. The adjacent OIDC change was not modified.

## Browser and MinIO

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=BrowserUploadLifecycleIntegrationTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Dbrowser.upload.integration=true \
  -Dnamewta.repo.root=/srv/WTA-plus \
  -Doss.minio.integration.endpoint='http://127.0.0.1:33125'
```

Exit 0. Isolated MinIO `quay.io/minio/minio:RELEASE.2025-04-22T22-12-26Z` on `127.0.0.1:33125` was removed after the run. Credentials were the existing test defaults and are not copied here. `BrowserUploadLifecycleIntegrationTest`: tests=1, failures=0, errors=0, skipped=0. Playwright: 10 passed, 0 skipped, 33.8s. The test printed that Chrome file and image bytes were verified in the owned MinIO after preview failures.

The cases cover user-import network, business, 401, and HTTP 401 resets with a retry on the current token; cancelling an import and retrying from a fresh dialog; a real file and image upload whose preview URL fails without creating an unreleased blob URL or reporting the completed upload as failed, while confirm stays disabled until the ownership id returns; workflow import error then success with the multipart bytes; closing an image dialog releasing its local preview and ignoring a late completion; and aborting a workflow import so another attempt can succeed. Reference-protected delete responses were not treated as successful removals.

## Upload service

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=OssUploadServiceUnitTest \
  -Dsurefire.failIfNoSpecifiedTests=false
```

Exit 0. tests=14, failures=0, errors=0, skipped=0. Completed objects are not deleted by leftover cleanup records, and another user or client is rejected.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase. No production source edit.
