# T42 working-tree fixture follow-up (read-only snapshot)

Snapshot HEAD `a20c977f285d449732301eb42cc8bd1ed67a75c7`; working tree has uncommitted T42 changes and is **not** a final candidate. `NotifyMailAttachmentIntegrationTest.java` SHA-256 `719942a94ec1998d68d3250541755e6068fc835302337f89e74dbefc35be3910`. Private frozen v5 runner SHA-256 `25ee81558bffd6e0a28b4130b06641bce0c450b9bb2cf7717e1d733179bc7faa`; proxy SHA-256 `0b465c2a3a518ad503daeeba787df14a9cc4be06f48d42de517ff9fc14020fca`. No build, test, service or repository edit was performed in this follow-up.

## HeldWorker and proxy compatibility

The three held-PUT tests at lines 396-443 and 525-546 now use try-with-resources `HeldWorker`. Its sole worker is an owned single-thread executor; `start()` submits `worker.poll()`, `awaitDone()` waits at most 40 seconds, and `close()` attempts to release a still-held PUT before cancel/shutdown, waits at most 5 seconds for the executor, then checks `/disarm` (lines 742-815). If the thread does not stop, or the proxy cannot reset, `workerFixturePoisoned` is set and a cleanup exception is raised; `@BeforeEach` blocks later tests and `@AfterAll` avoids deleting owned DB facts while a worker may still be active (lines 122-126, 163-170). This closes the earlier unbounded common-pool worker leak at the Java test level. Java try-with-resources keeps a primary assertion failure and suppresses a secondary `close()` failure rather than silently replacing it.

Frozen proxy v5 permits `/disarm` in NORMAL only when `active_holds == 0` (proxy lines 247-258), so the normal `release()`→`awaitDone()`→`close()` sequence is compatible. A still-active held PUT returns 409. `HOLD_CANCELLED` remains fail-closed to later PUT retries until explicit disarm after all waiters drain. The helper's failure path may release a held PUT before unwinding; this is deliberate to let `worker.poll()` end, while any already-forwarded provider write cannot be undone. A late provider proxy thread may outlive the Java worker after a failure; the private runner's final proxy drain/cleanup must reject such a run, and the next test is blocked only if the worker/reset checks fail. This is a remaining failure-path evidence limit, not a claim of actual leak.

## Method selection

The working-tree class currently has 16 `@Test` methods, including `undeliverableMailWithoutOutboxReleasesItsSourceReference` at lines 600-635. The runner has no hard-coded 15 count: `--expected-methods` accepts a comma-separated unique identifier list (runner lines 117-122); the source guard checks every supplied name exists (lines 373-390); the final fresh XML gate requires its test count and method set exactly match the supplied list with zero failure/error/skip (lines 125-134). If the class grows again, Lead should freeze the final source hash and pass the full exact list. Supplying 15 when the class actually executes 16 fails the XML gate. Do not treat the current 16 as a final selector count while writer work continues.

The new 16th test checks a real USER target without email becomes UNDELIVERABLE, creates zero outbox rows, then releases only its owned source reference with no captured SMTP (lines 600-635). It does not by itself prove the broader cancel/in-flight/expiry/reclaim race being designed separately.
