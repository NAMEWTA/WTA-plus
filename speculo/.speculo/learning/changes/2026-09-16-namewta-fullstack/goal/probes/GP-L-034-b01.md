---
artifact: learning-goal-probe
probe_id: GP-L-034-b01
lesson_id: L-034
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-034 / b01

审问对象是已写成的 Lesson `lessons/L-034-person-admin.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止 `Q-question 问答目录`、learner 协议字段、`verdict`。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain / `claimed_cells` / 矩阵 (a) **一行**）：`A:PersonAdminController.*`。派单与矩阵符号把十二个 Java 名写全：`page, eligibleUsers, detail, reviewContext, reviewMaterial, material, decide, create, revise, assign, manageBinding, revoke`。OBJ-34 原文：能口述 `PersonAdminController` **档案/审核/绑定/吊销**。档是**解释+走查**，不是方法性状。chain 覆盖列只写 `A:PersonAdminController.*`。矩阵 (a) 该行模块 `wta-profile-person`，仍 `uncovered`。矩阵 (b) **没有** decide/revoke/binding 独行；唯一相关 (b) 是 `PersonRebindController.confirm`（L-036）。矩阵 (c) `wta-modules` 业务房间已 covered（L-002/L-003）；MySQL 8.4 / `dao-mapper-ownership` 仍 uncovered。矩阵 (d) **有**「主路径 profile 申请」=`PersonApplication.submit → admin decide`，备注 L-035, L-034，仍 uncovered。本批按派单审问 **这一行十二扇窗**；自助 `PersonApplicationController`、换绑、材料总库、核身回调、前端工厂、企业柜台不是本行符号。

抽查锚点（2026-09-16）：`backend/wta-modules/wta-profile/wta-profile-person/src/main/java/org/namewta/profile/person/controller/admin/{PersonAdminController,PersonAdminExceptionHandler}.java`；`usecase/PersonAdminUseCase.java` + `usecase/impl/PersonAdminUseCaseImpl.java`；`service/{PersonAdminService,IPersonAdminService}.java`；`dao/PersonAdminDao.java`；`mapper/PersonAdminMapper.java` + `resources/mapper/person/PersonAdminMapper.xml`；`domain/{ProfilePerson,ProfilePersonBinding,ProfilePersonVersion,ProfilePersonApplication}.java`；`domain/vo/PersonAdminResultVo.java`；`domain/bo/PersonAdmin{Query,Create,Decision,Binding,Assign,Revoke}Bo.java`；`port/PersonApplicationPublicationPort.java`；`adapter/gateway/SpringPersonWorkflowGateway.java`；测试 `PersonAdminHttpContractTest`、`PersonModuleArchitectureTest`、`PersonAdminServiceTest`、`PersonAdminMySqlE2ETest`、`src/test/.../service/impl/PersonAdminServiceImpl.java`；DDL `10-cde-base-ddl.sql` `profile_person*` 生成列；DML `50-cde-base-dml.sql` 210050…0010–0015；`controller/self/{PersonApplicationController,PersonRebindController}` 与 `controller/admin/{PersonMaterialAdminController,MaterialTagController}` 仅作负空间。

## Q1 澄清

- objective_id: OBJ-34
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-34；chain L-034 覆盖列只 `A:PersonAdminController.*`；L-034 `claimed_cells` 一格；矩阵 (a) 十二名与磁盘公开方法 1:1 仍 uncovered；矩阵 (b) 无本课符号；(c) wta-modules 已 covered、MySQL uncovered；(d) 申请主路径仍 uncovered；课开篇十二扇 + 负空间 L-035…L-040
- difficulty: medium

课把管理端档案柜台收成「一块门牌、十二扇窗、五层楼梯」。计划里至少有五套可数对象，闭合标准要以哪一套盖章？

1. **OBJ-34 原文。** `course.md`：能口述 `PersonAdminController` 档案/审核/绑定/吊销。四个词是口试主题，没有把 `eligibleUsers` / `reviewMaterial` / `PersonAdminResultVo.version` 三把尺写成清单。
2. **chain / 课 `claimed_cells`。** 一格：`A:PersonAdminController.*`。chain 不写 UseCase/Service/XML 符号。
3. **矩阵 (a) 一行。** 十二个方法名与磁盘公开映射同名，模块 `wta-profile-person`。与 Controller 1:1。Lead 若按行标 covered，**不会**把 `PersonApplicationController.current/submit` 假闭合——那是另一行。
4. **课自己的操作定义。** 门牌 `/profile/person/archive`；GET 六 / POST 六；五颗权限不并成「档案管理员」；写窗 `LoginHelper.getUserId()`；Controller 注入 `PersonAdminUseCase`（字段名叫 `service`）；生产主链不用 `IPersonAdminService`；layered 不是 classic `I*Service`。
5. **邻格 / 未入库。** L-035 自助门；L-036 换绑（(b) `confirm`）；L-037 材料总库；L-038 核身；L-039 前端工厂；L-040 企业柜台；(d) submit→decide 主路径；(c) MySQL 8.4。

问：本格 (a) 闭合标准是「十二扇路径/动词/权限/写窗操作者 + layered 不走 `IPersonAdminService` + 档案/审核/绑定/吊销四件不是同一支笔」，还是 OBJ-34 四个词就够盖章？若是前者，Lead 标本行 covered 是否必须先给 UseCase/Service/XML 或 (d) 申请流单独立行，而不是把十二扇并进自助门、换绑 (b)、企业柜台或 MySQL 容器？

## Q2 证据

- objective_id: OBJ-34
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 23–150；Handler 8–16；UseCase 14–80；UseCaseImpl 18–100；Service 38–275 / 279–293 / 315–515；CreateBo 14–18；QueryBo 7；HttpContractTest 19–45；ArchitectureTest 29–48 / 92–136；Mapper.xml 7–30 / 73–83 / 98–164 / 209–252；DDL 694–750 / 858–876；DML 1507–1512；ServiceTest 41–102；E2E 73–176；夹具 ServiceImpl 18–47；PublicationPort 370–378；SpringPersonWorkflowGateway 51–64；IPersonAdminService 35–42
- difficulty: high

课的柜台机制链哪些能在工作树直接复现，哪些只是测试名单比课表窄、或邻格事实？

**仍能复现（本格）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 类注解 `@Validated @RestController @RequiredArgsConstructor @RequestMapping("/profile/person/archive")`；无 `BaseController`、无 `@ConditionalOnProperty`、无 PUT/DELETE | Controller 23–27；映射全是 Get/Post；合同测试 44–45 方法名不含 `export`/`delete` |
| 公开 HTTP **正好十二个**，与矩阵十二名 1:1 | Controller 35–150；HttpContractTest 23–34 |
| GET 六扇权限：page/detail=`query`；reviewContext/reviewMaterial=`review`；material=`material`；**eligibleUsers=`override` 不是 query** | 35–84；合同测试 23–28 |
| POST 六扇：decide/create/revise/assign/revoke=`override`；**只有** manageBinding=`manage`；六扇 `@Log` 双 false | 89–150；合同测试 29–43 |
| 写窗一律 `LoginHelper.getUserId()`；GET 不传操作者 | 94 / 105 / 116 / 127 / 138 / 149 vs 37–84 |
| Controller 字段名 `service`，类型 `PersonAdminUseCase`；源码不 import `service` 包 | 29；ArchitectureTest 126–130 |
| HTTP 方法 `reviewContext` 调 UseCase/Service **`review`** | Controller 61–65；UseCase 47–48；Service 134–140 |
| UseCaseImpl **十二个**方法都有 `@DSTransactional`（含 page/detail） | UseCaseImpl 25–99 |
| 无操作者 default 抛 `请传入 operatorId`；生产 Controller 走带 operatorId 的重载 | UseCase 14–31 vs Controller 94 等 |
| `PersonAdminService` 是 `@Service` 具体类，**不** `implements IPersonAdminService`；全仓只有接口自己一处引用 | Service 38–39；grep 仅 `IPersonAdminService.java` |
| 生产主构造接 `PersonWorkflowGateway`；兼容构造吃 `WorkflowService` + 无用 `jsonMapper` | Service 49–82 |
| `page`：pageNum&lt;1→1；pageSize≤0→20、上限 200；空 status → SQL `p.status != 'REVOKED'`；乱写 → `PERSON_QUERY_STATUS_INVALID`；姓名 like、证件号精确大写 | Service 279–291；XML 7–29 |
| 名册左连「还钉着」`status in ('ACTIVE','SUSPENDED')` | XML 22–23 / 38–39 |
| `eligibleUsers`：blank → `[]`；`searchActiveUsers(strip, 50)`，留下 `status=="0"` 且无有效钉子，最多 20 | Service 109–118；ServiceTest 82–89 |
| `detail` 缺行 → `PERSON_PROFILE_NOT_FOUND`；注销本仍能打开（`requireProfile` 不查 REVOKED） | Service 123–129 / 619–624；E2E 145–152 |
| 材料名牌 `materials.list(VERSION, currentVersionId)`；无 CURRENT 则 `versions.getFirst()`（XML `order by version_no desc`） | Service 125–128；XML 50–55 |
| `selectReview` 要求 `submission_seq > 0`；找不到 → `PERSON_REVIEW_CONTEXT_NOT_FOUND` | XML 98–106；Service 638–642 |
| `reviewMaterial` owner=`SUBMISSION`；`material` owner=`VERSION` 且必须有 CURRENT，否则 `PERSON_PROFILE_VERSION_NOT_FOUND` | Service 144–155 |
| 出门条投影 `PersonProfileAccessUrl(accessType, url, expiresAt, fileName)`，不漏 system 类型 | Service 157–159；record 15 |
| `decide` 只认大写 `APPROVED`/`REJECTED`；reason 必填 ≤500；Service 字段 `workflow==null` → `PERSON_ADMIN_WORKFLOW_UNAVAILABLE` 且 `never beginDecision` | Service 40 / 164–172 / 718–722；ServiceTest 93–101 |
| 先 `beginDecision`（WAITING→OVERRIDE_PENDING + 决定记录 PENDING）再 `workflow.terminate`；抛错 → `PERSON_ADMIN_WORKFLOW_TERMINATION_FAILED` | Service 174–180 / 315–325；ServiceTest 41–78 |
| REJECTED：`markRejected` 申请 `INVALID`，盒 `("REJECTED", 0, null, null, decisionVersion)`，不发布 | Service 181–184；XML 149–155；E2E 156–164 |
| APPROVED：`resumeWaiting` 拨回 WAITING → `publishApproved`（只认 WAITING）→ `snapshotImmutable` → `markApproved`（SQL 要求已是 FINISH） | Service 185–193；XML 136–147 |
| 发布看见 `rebindIntent='Y'` → `PERSON_REBIND_NOT_SUPPORTED_BY_THIS_COMMAND` | PersonApplicationService 370–378 |
| 直建材料 `@Size(max = 10)`；先 SOURCE attach，`validateRequired(..., ALWAYS)`，再冻 VERSION | CreateBo 18；Service 207–218 |
| 直建撞活身份键 → `PERSON_ADMIN_IDENTITY_CONFLICT`；insert 档案不写生成列 | Service 363–370；XML 165–172；DDL 707–719 |
| `revise` 锁 **ACTIVE**（`requireWritable` / XML `status='ACTIVE'`），不是「已退回申请」；版本冲突 `PERSON_PROFILE_VERSION_CONFLICT` | Service 225–239 / 399–404 / 628–633；XML 200–207；接口注释仍写「已退回」 |
| 绑定三动作 SUSPEND/RESUME/UNBIND；错动作 `PERSON_BINDING_ACTION_INVALID`；错状态 `PERSON_BINDING_STATE_CONFLICT`；尺是 `binding_version` | Service 40–41 / 244–251 / 444–472 |
| `assign` 两端都没有有效钉子，否则 `PERSON_BINDING_TARGET_INELIGIBLE`；新行 `binding_version=1`，不是把 UNBOUND 改回去 | Service 257–264 / 477–489；XML 209–214 |
| `revoke` 改 `status='REVOKED'` 且 `del_flag` 仍 `'0'`；有钉子则 UNBOUND；之后 `PERSON_PROFILE_REVOKED_READ_ONLY` | XML 247–252；Service 494–514 / 628–633；E2E 141–152 |
| 有效钉子 SQL / 生成列都是 ACTIVE **或** SUSPENDED，没有枚举值 `EFFECTIVE` | XML 73–83；DDL 858–876；ArchitectureTest 172 锁的是申请 XML |
| `PersonAdminResultVo.version`：decide=decisionVersion；create=1；revise/revoke=档案 version±1；manageBinding/assign=binding_version | Service 183–193 / 391 / 439 / 472 / 486 / 513 |
| Handler 只捕 `PersonAdminException` → `R.fail(getMessage())`；不改 HTTP 状态码 | Handler 8–16 |
| 菜单五串 query/review/material/manage/override 与 DML 210050…0010–0015 文案一致 | DML 1507–1512 |
| admin/self 源码不得 `@SaIgnore`；anonymous 文件名必须带 Anonymous | ArchitectureTest 29–48 |
| 五层 import 方向：Controller→usecase 禁 service；UseCase 禁 dao/mapper；Service 禁 mapper/`IService`；DAO `@Repository` 只包 Mapper | ArchitectureTest 92–136 |
| E2E 生命线：create→revise 两页 OVERRIDE→SUSPEND/RESUME/UNBIND/assign 事件 5→GET 修订名+出门条→revoke 默认 page 0 / REVOKED page 1→驳回 INVALID:1:ADMIN_OVERRIDE→terminate 失败仍 WAITING:0 | E2E 73–176 |
| 夹具 `PersonAdminServiceImpl` 在 `src/test`：`extends PersonAdminService implements PersonAdminUseCase`，`new PersonAdminDao(mapper)`，无操作者重载改走 `LoginHelper` | 夹具 18–47；E2E 90–132 `new PersonAdminController(service)` |

**邻格仍能复现（不计入本行闭合）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 自助门 `@RequestMapping("/profile/person/application")` | `PersonApplicationController`；L-035 |
| 换绑门与 `rebindIntent` | `PersonRebindController`；发布端口 376–378；L-036 |
| 材料总库 `/profile/person/materials/{ownerType}/{ownerId}` | `PersonMaterialAdminController`；L-037 |
| 企业柜台同构但表/权限前缀不同 | L-040；DDL 企业 binding 1110+ |
| 前端 `createPersonArchiveService` / `createProfileService` | L-039 |
| system 资料页 `SysProfileController` | L-022；classic `wta-system` |

**课写过、测试名单更窄或生产缝与测试缝不完全同一句话（不是证伪）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「网关是 null → `PERSON_ADMIN_WORKFLOW_UNAVAILABLE`，一行都不写」 | **Service 字段** null（兼容构造 `legacyWorkflow(null)`）确实如此，单测 93–101。生产 `@Autowired` 主构造注入的是 `SpringPersonWorkflowGateway` Bean，字段几乎不会 null。内层 `WorkflowService` 缺失时网关抛 `PersonApplicationException("PERSON_WORKFLOW_UNAVAILABLE")`，被 `decide` 的 catch 包成 **`PERSON_ADMIN_WORKFLOW_TERMINATION_FAILED`**（此时 `beginDecision` 已写，靠 `@DSTransactional` 回滚） |
| `workflow.terminate(String.valueOf(applicationId), reason)` | 生产 Service 用 `Long.toString`；网关再调 `terminateInstance`。语义同，字符串 API 不是 `String.valueOf` |
| 「关键字空 → 空列表」 | Service `isBlank` → `[]`。HTTP `@RequestParam String keyword` 默认必填，**缺参数是框架 400**，不是空列表 |
| 「没有 CURRENT 时退回版本列表第一页」 | `getFirst()` 成立；XML `order by version_no desc`，第一页是**号最大**的那页，不是最早插入 |
| 「事件来源 XML 写死 `ADMIN_OVERRIDE`」 | `insertBindingEvent` 229–231 对 **create 钉子** 也写这串，即使 binding 行 `source_type='ADMIN_CREATE'` |
| `IPersonAdminService.revise`「修改已退回」/ `revoke`「撤销转移挑战」 | 接口注释；磁盘锁 ACTIVE / 改 REVOKED。课迁移表已点 |
| E2E「绕过 UseCase」 | MockMvc 只打 GET detail/material；create/revise/binding/revoke/decide 直接打夹具 ServiceImpl |

问：在十二扇路径/动词/五颗权限、写窗 LoginHelper、UseCase 事务、有效钉子 ACTIVE|SUSPENDED、decide 先 terminate 再发布、revoke≠UNBIND、生产不注入 `IPersonAdminService` 已经能互证的前提下，(a) 是否还要把「生产缺 WorkflowService 时失败码是 TERMINATION_FAILED 不是 UNAVAILABLE」「缺 keyword 是 400」写成开篇半句，才算柜台讲完？还是这些已经落在课的失败关闭/测试夹具边界里，Lead 可以直接盖章？

## Q3 假设与反例

- objective_id: OBJ-34
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无 IPersonAdminService 生产实现；Controller 字段是 UseCase；GET 也有事务；无 EFFECTIVE；POST revoke 非 DELETE；eligibleUsers 非 query；manage 只一扇；detail 名牌≠出门条；decide 只 WAITING；rebind 拒发布；夹具 ServiceImpl 非主链；SysProfile 是另一栋楼
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪层楼梯认错？

1. **「这是 `SysProfileController` / 账号资料页。」** 另一栋 classic 楼。课误区 1 / 反例 11。
2. **「layered 所以 Controller 调 Service / `IPersonAdminService`。」** 注入 UseCase；接口零实现。课误区 2 / 反例 12 / 迁移表。
3. **「字段名叫 service 就是 `PersonAdminService`。」** 类型是 UseCase。课开篇包边界。
4. **「UseCase 只包写操作。」** page 也 `@DSTransactional`。课误区 3。
5. **「生效绑定=状态字 EFFECTIVE。」** SQL/生成列是 ACTIVE|SUSPENDED。课误区 4。
6. **「注销=逻辑删除 / HTTP DELETE / 只解绑。」** `POST .../revoke` 改 REVOKED，`del_flag` 仍 `'0'`。课误区 5 / 反例 1。
7. **「detail 能看材料就能下文件。」** list 名牌 ≠ access-url。课误区 6 / 正例 7。
8. **「审核材料=材料总库窗。」** 本课 URL 写死 SUBMISSION/VERSION。课误区 7。
9. **「通过=把申请 update 成 FINISH。」** 先停工作流，再发布端口造页。课误区 8 / 机制链 5。
10. **「候选人挂 query，因为只是查询。」** 合同测试钉 override。课误区 9 / 反例 8。
11. **「`PersonAdminResultVo.version` 都是档案乐观锁。」** 三把尺。课误区 10。
12. **「给 manageBinding 传 ASSIGN / decide 传 RETURN。」** 集合只有三动作 / 两决定。课反例 2 / 4。
13. **「工作流挂了驳回已经落库。」** terminate 抛错整单回滚，E2E WAITING:0。课反例 5。
14. **「对换绑 WAITING 件用本课 decide。」** 锁得住，发布端口拒 `rebindIntent`。课反例 6。
15. **「测试夹具 `PersonAdminServiceImpl` 就是生产分层。」** 夹具持 Mapper 且无操作者重载走 LoginHelper。课反例 10 / 误区 15。
16. **「没有工作流 Bean 时 decide 降级直接改申请。」** Service 字段 null 直接 UNAVAILABLE。课误区 14。**半句精度：**生产网关 Bean 在、内层 WorkflowService 不在，走的是 terminate 失败码 + 回滚，不是降级。
17. **「十二扇也包含自助 current/submit / 前端工厂，因为 OBJ 写了档案。」** 课负空间明确不认。把假设反转后，Lead 会用本行假闭合 L-035/L-039 或 (d) 申请主路径。
18. **「reviewContext 和 Service.review 必须同名才算覆盖。」** HTTP/矩阵名是 `reviewContext`；里屋方法是 `review`。课已写。不要另开 Service 行才盖章。

问：读完后仍说不出的，是不是「类名」，而是 **本行不含自助/换绑/材料总库/前端、eligibleUsers 不是 query、manage 只一扇、吊销≠解绑、decide 先掐机器、盒子里的 version 要问哪把尺、生产主链不叫 `IPersonAdminService`**？

## Q4 视角

- objective_id: OBJ-34
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-34 vs L-003/L-022/L-035/L-036/L-037/L-038/L-039/L-040；IPersonAdminService；MySQL 生成列；API-005
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:PersonAdminController.*`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 十二名 1:1 vs OBJ 四个词；(b) 无 decide 性状行；(d) submit→decide 跨课 | **是** 闭合定义。十二扇行可单独 covered。(d) **不要**因本课假闭合 |
| 柜台作者 | 十二映射、五权限、写窗 LoginHelper、Handler 类别码 | **是** (a) 逐方法 |
| UseCase / 事务 | 十二 `@DSTransactional`；无操作者 default 抛错 | **是** 解释到副作用。不要发明 `PersonAdminUseCaseImpl` 行 |
| Service 状态机 | decide 栅栏、直建/覆盖、绑定三动作、吊销 | **是** (a) explained。密文/工作流引擎内部 **否** |
| DAO/XML | FOR UPDATE、有效钉子集合、生成列不手写 | **是** 走查。格子仍是 Controller。(c) dao-mapper-ownership **否**（L-083） |
| 发布端口 / 网关 | publishApproved 只认 WAITING 且拒换绑；terminate 适配 | URL/失败关闭 **是** 边界。申请 Mapper / 流程引擎 **否**（L-035 / L-070） |
| 自助申请（L-035） | current/save/submit 如何把申请推进 WAITING | **否** |
| 换绑（L-036） | probe/match/confirm；(b) confirm 改绑定 | **否**。本课只保证 decide 发布拒 Y |
| 材料总库（L-037） | attach/detach / 标签树 | **否**。本课只有两张出门条 |
| 核身（L-038） | 匿名回调 `@SaIgnore` | **否**。本课 admin 禁止 SaIgnore |
| 前端（L-039） | `createProfileService` / archive 资源标签 | **否**。门牌对照一句即可 |
| 企业柜台（L-040） | 同构十二扇、转移挑战 | **否**。篱笆一句 |
| 资料页（L-022） | 昵称/改密 | **否** |
| API-005 | 查询 GET、变更 POST；本切片已无 PUT/DELETE | 盘点现状 **是**。不发动传输 Ratchet **否** |
| MySQL 8.4 / Redis 8 | 生成列唯一键 / 会话 | 生成列语义 **是** 解释。容器 covered **否** |
| 夹具 ServiceImpl | E2E 抄近路 | **否** 主箭头。课已标抽屉家具 |

问：Lead 若凭「十二个方法名都在函数表里」把 `PersonAdminController.*` 标 covered，是否还要求读者能指着 **eligibleUsers=override、manage 只一扇、吊销≠解绑、decide 先 terminate、盒子 version 三把尺、生产不走 IPersonAdminService**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要把 (d) 申请主路径算进来？

## Q5 元问题

- objective_id: OBJ-34
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 十二扇独行、无 (b)、 (d) 申请流跨 L-035
- difficulty: low

建议：`defer`

原因：

- **`A:PersonAdminController.*` 与矩阵 (a) 该行 1:1。** L 合同章节齐全（宏观地图、十二窗表、精确定义/English、机制链 1–6、带 caption / 文字等价物 / 图边界的两张图、正反例、变式、迁移、误区、暂停走查、来源表 S-004/S-008/S-010/S-L003-01/S-L034-01…08）。**主叙事站得住且与源码对齐**：一块门牌十二扇；GET/POST；五颗权限；写窗 LoginHelper；Controller→UseCase→Service→DAO→XML；有效钉子 ACTIVE|SUSPENDED；decide 先掐工作流再发布或 INVALID；revoke 改 REVOKED 不 del；生产不注入 `IPersonAdminService`。不必为「类名」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例/迁移或测试夹具边界，不挡住把**本行**标 covered：（1）生产缺内层 `WorkflowService` 时失败码是 TERMINATION_FAILED + 回滚，UNAVAILABLE 是 Service 字段 null；（2）缺 `keyword` 查询参数是框架 400；（3）`insertBindingEvent` 来源写死 ADMIN_OVERRIDE 含直建钉子；（4）无 CURRENT 时 `getFirst()` 是 version_no 最大页；（5）E2E 写路径走夹具不是 UseCaseImpl。不要把自助门、换绑 (b)、材料总库、前端工厂、企业柜台、MySQL 容器塞进十二扇格。
- 不建议 `mine-more`：b01 未证明本行还有真实矩阵空洞；再五问只会把 L-035 如何推进 WAITING、L-036 confirm、或工作流引擎 terminateInstance 再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；半句生产/测试缝不是第二容器。不建议 `split-lesson`：未满 10 问；十二扇是 chain 原设计的同一 OBJ-34、同一管理端柜台，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：decide 失败关闭、绑定状态机、吊销只读是 (a) 的解释到副作用（OBJ 档是解释+走查）。
- (d)「PersonApplication.submit → admin decide」课内只讲 decide 半段，submit 在 L-035。这是 Lead 等两课都 mined 再标该数据流，不是 miner 再问，也不是把 (d) 并进本行才能盖章。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:PersonAdminController.page, eligibleUsers, detail, reviewContext, reviewMaterial, material, decide, create, revise, assign, manageBinding, revoke | 十二扇路径/动词/五权限、写窗 LoginHelper、layered UseCase、有效钉子 ACTIVE\|SUSPENDED、decide 先 terminate 再发布、revoke≠UNBIND、ResultVo 三把尺、生产不走 IPersonAdminService——均与源码对齐。残留精度：（1）生产内层 WorkflowService 缺失 → TERMINATION_FAILED + 回滚，不是字段 null 的 UNAVAILABLE；（2）缺 keyword → 框架 400；（3）binding_event 来源写死 ADMIN_OVERRIDE；（4）无 CURRENT 时第一页=version_no desc；（5）E2E 写路径走夹具。 | 可选半句：口试「没有工作流」要问是 Service 字段 null 还是网关里 `getIfAvailable()` 空；缺候选人关键字先问 HTTP 必填。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-034 来源表 S-L034-01…08）。 | defer（本行可 covered；精度项不挡盖章） |
| D:主路径 profile 申请（submit → admin decide） | 本课只讲 decide 半段；WAITING 从哪来未认。 | 不要并进十二扇才能盖章。等 L-035 mined 后再标该 (d)。 | defer（L-035 + 本行对照；Lead 改 (d)） |
| B:PersonRebindController.confirm | 本课只保证发布拒 `rebindIntent=Y`。 | 保持。L-036。 | defer（L-036） |
| A:PersonApplicationController.current, submit | 自助门未讲。 | 保持。L-035。 | defer（L-035） |
| A:PersonMaterialAdminController.* / Self / MaterialTag | 本课只有两张 archive 出门条。 | 保持。L-037。 | defer（L-037） |
| A:PersonVerificationAnonymousController.callback | admin 无 SaIgnore；回调是另一扇。 | 保持。L-038。 | defer（L-038） |
| A:createProfileService / createProfileWebDomain person | 门牌对照一句。 | 保持。L-039。 | defer（L-039） |
| A:EnterpriseAdminController.* | 同构篱笆，表名不同。 | 保持。L-040。 | defer（L-040） |
| A:SysProfileController.profile, updatePwd | 资料页已 covered。 | 对照一句即可。 | defer（L-022 已 covered） |
| IPersonAdminService / 测试夹具 PersonAdminServiceImpl | 矩阵无此符号。课已标抽屉家具。 | 不要为本课发明 (a)/(b) 行。 | defer（未入库） |
| (b) decide 失败关闭 / 绑定状态机 / 吊销只读 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| C:MySQL 8.4 / dao-mapper-ownership | 生成列语义已借用。容器/所有权未认。 | 不要因本课标容器 covered。 | defer（(c) / L-085 / L-083） |
| API-005 变更 POST | 本切片已全 GET/POST。 | 本课盘点，不发动 Ratchet。 | defer（传输 Ratchet） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本行无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出柜台目的（管理端十二扇管一本个人档案）、主路径（名册/详情 GET、直建 POST、decide 先停工作流再发布或驳回）与失败路径（无网关不写、terminate 回滚、REVOKED 只读、绑定状态冲突）；邻课缺的是别的矩阵行不是讲义；把可选半句写进原课不会超过 40 分钟。满 10 问前不拆。十二扇虽穿过 UseCase/Service/DAO/XML 与三个端口，仍是 chain 原设计的同一 OBJ-34，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-035 自助、L-036 换绑、(d) 申请主路径、L-037 材料、L-038 核身、L-039 前端、L-040 企业、L-022 资料页、未入库 IPersonAdminService/(b)、MySQL/DAO 容器以及 API-005 显式建议 `defer`；对 `A:PersonAdminController.*` 建议 Lead 标 `covered`（证据锚本 probe 与 L-034 来源表）。本课不主张改 Java 窗、企业柜台、申请 Controller 或任何 (b)(c)(d) 格子。
