# T-38 Lead Dispatch — 精确且安全的通知重试/取消

- 当前工作区 main/direct-parent；base `b47ff8b91cfef02a9f28de0e201edcd1575a950f`，无新worktree。用户明确Goal、native gpt-6-sol/xhigh、本地commit/direct-parent已授权。
- 范围/写集：Ticket38 frontmatter 18条为权威；目录仅本票需要。产品writer cors_audit，Lead不并行改产品。Read-only ops_audit负责私有owned驱动/审查，legacy_audit负责安全/规格。所有change治理文档由Lead写。
- 输入：Map→绑定Skills/scope references→Ticket38/AC038、spec/ADR、T36/T37当前Evidence；/tmp/wta-t38-current-audit.md为固定617a369只读事实，b47ff8b仅治理增量。历史旁路日志/源码点不得冒充当前执行。
- 先写测试获得真实行为红灯（现有API的path/body conflict且零Service调用），Lead协调Maven。编译缺API不算红灯。其后非空实现，返回实际paths/调用方/验证矩阵，Lead提交clean candidate再做两轴固定review与requiredE2E。
- HTTP path权威、delivery归属、非空不支持key拒绝、actual queuedCount与零任务状态保持；完整并发/rollback/wake通过真实MySQL。FAILED必须有明确未发送/本地安全与预算事实，UNKNOWN不可盲发；活READY/PROCESSING与外部WAITING不被复活、复制或清cache绕过。保留T36锁序/原子站内事实与T37owner/digest/TTL。具体当前合同详Ticket revision164。
- 权限沿既有admin全局控制面及公共Client会话验证；appId并非ClientID/owner。前端只做权限投影、正确ID与计数反馈、loading/error/重复点击/过期结果保护，后端为最终授权者。
- 生成OpenAPI遵循现有工具，不手改生成声明；公共RetryReceipt直接切换同步消费者，按用户已接受仓内基座合同，不发明兼容桥。
- 测试：current已有notify单元与相关common消费者；新真实`org.namewta.test.notify.NotifyManualRetryIntegrationTest`和wake精确启用零skip；并发两事务同delivery、与claim交错、故障rollback、未知拒绝且无Provider调用；前端domain/web-domain测试、全typecheck/lint/build、OpenAPI/facts/layered及适用full/core。
- Maven/前端构建/真实服务由Lead协调独占；无Docker启动权限交给产品writer。私有驱动使用T38_MYSQL_PASSWORD环境、owned新库与随机loopback、按标签/fullID/卷/pgid清理；不读取真实密钥、不供应商I/O/生产SQL。
- 遇到缺路径先通知Lead修订，不能先改后报；三正式候选失败先复盘。无远程push/部署/归档。

IN_APP UNKNOWN沿Ticket既有条款可在同Intent/Delivery、唯一无活租约任务、剩余预算与T36原子幂等事实证明下安全重做，测试已存在消息/关系不重复及不重复push。不得把外部UNKNOWN拒绝泛化为全部IN_APP；无法证明安全的历史形状精确拒绝。批量含外部UNKNOWN在写入前拒绝；定向A只评估A，不能因未选中的B未知误伤。
