# T44 v7 dynamic secret log gate

Candidate `/tmp/wta-t44/run-minio-real-v7.py`, SHA-256 `ed3f0f8600728c469e8a6cca412a9f1fc1d8e0a38b04e7c05b82b06fa507b98c`, derives from the frozen v6 without changing network requests, database fixtures, container ownership, cleanup or success thresholds. No service run was made by this reviewer. V5 remains `accepted=false` despite its successful business phases. Lead subsequently ran v6 as `4ca003409fc458ce`: its business phases passed, but the private safe summary counted seven MinIO app access-key hits and one secret-key hit, so v6 also remains `accepted=false`.

The v6 scanner covered ten generated credential canaries, but did not retain the two real upload-control `uploadToken` values or any SigV4 `X-Amz-Signature` values. V7 captures those two tokens and the two PUT signatures from the actual init responses, plus four private GET signatures from actual `download-url` responses, only in a local in-memory map. It requires the exact dynamic slots and a single 64-hex signature in the response query before completing each original request. The postcheck scanner now requires all ten fixed plus eight dynamic categories. Any exact hit still fails the gate; only fixed category names, counts, logger/exception and line metadata are written to the 0600 safe summary in the owned 0700 directory. Raw values, raw messages, full URL and session tokens are not reported or persisted; raw backend log remains deleted by `finally`.

Static/isolated checks completed: `py_compile` exit 0; synthetic redaction and private file-mode test `/tmp/wta-t44/test-v7-log-diagnostic.py` exit 0; synthetic capture of two upload tokens, two PUT and four GET signatures `/tmp/wta-t44/test-v7-dynamic-capture.py` exit 0. These do not substitute for Lead's exact-clean full-app MinIO rerun and private diagnostic inspection. The active product writer may change sanitizer behavior; keep this driver at its fixed SHA when correlating results.
