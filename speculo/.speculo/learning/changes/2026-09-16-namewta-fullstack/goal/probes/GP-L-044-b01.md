---
artifact: learning-goal-probe
probe_id: GP-L-044-b01
lesson_id: L-044
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-044 / b01

审问对象是已写成的 Lesson `lessons/L-044-profile-enterprise-frontend.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **一格**：`A:createProfileWebDomain enterprise`。OBJ-44 原文：能说明 web-domain **enterprise 页面如何消费** `profileService`。档是**解释+对照**。矩阵 (a) **没有** `enterprise` 切片行，也没有 Vue 页符号；符号落在 **九工厂同行** `createAdminWebDomain, createSystemWebDomain, createProfileWebDomain, createNotifyWebDomain, createThirdWebDomain, createWorkflowWebDomain, createDemoWebDomain, createAiWebDomain, createProfileSelfWebDomain`（模块 `web-domains/*`，仍 uncovered，备注「manifest 工厂」）。矩阵 (a) 厨房独行 `createProfileService` **已经** covered（L-039 + GP-L-039-b01；备注「person/self 厨房；enterprise 页面 L-044」）。矩阵 (b) **没有** runtime 校验 / 页面扣扳机 / APPROVE 映射行。矩阵 (c) `admin-web` / `home-web` 容器与 `frontend-direction` 已 covered（L-004 / L-007 / L-008）。矩阵 (d) **没有** 浏览器企业档案流；已 covered 的「主路径 profile 申请」原文仍是 `PersonApplication.submit → admin decide`。本批按派单只审问这一格 (a) 切片；口试按磁盘：管理端工厂 id 仍是 `web-domain-profile`，本课认 **后三键贡献 + 三张企业页怎么喊厨房 + self 认证表单实际扣的三枪**，不把九工厂行整行闭合，不把厨房独行再盖第二次章，也不把 L-039 的 person/material-tag 四页再讲一遍。`EnterpriseAdminController` 十二扇留给 L-040；自助 current/save/submit/probe 与匿名门铃留给 L-041；转移三枪留给 L-042；材料 Admin/Self 留给 L-043；厨房工厂与 person/self/material-tag 门脸留给 L-039；厅堂插头留给 L-007 / L-008；九工厂行的 `createSystemWebDomain` 留给 L-020；system 资料页留给 L-022；直传控件留给 L-029。

抽查锚点（2026-09-17）：`frontend/packages/web-domains/profile/src/{index,index.test,runtime}.ts`；`src/enterprise/{registration,logic,logic.test,components.test,index}.ts`；`EnterpriseProfilePage.vue` / `EnterpriseProfileDetailPage.vue` / `EnterpriseProfileDetailPanel.vue` / `EnterpriseProfileReviewPage.vue` / `EnterpriseIdentityForm.vue`；`src/self/{registration,registration.test,runtime}.ts`；`EnterpriseVerificationPage.vue` / `ProfileCenterPage.vue`；`frontend/packages/domains/profile/src/{service,service.test,permissions,index,transport}.ts`；`enterprise/{archive,application,transfer,materials}/{service,index}.ts`；两包 `package.json` `exports`；两包 `README.md`；`apps/admin-web/src/application/services.ts` 第 53 行；`apps/home-web/src/application/services.ts` 第 16 行；`adminManifestRegistry.ts` `adminProfileWebRuntime` 与 `.test.ts`；`homeManifestRegistry.ts` `selectedManifestIds`；`api-contracts/generated/openapi.ts` `/profile/enterprise/**`；`50-cde-base-dml.sql` 企业菜单/流程键。

## Q1 澄清

- objective_id: OBJ-44
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-44；chain L-044 覆盖列只 `A:createProfileWebDomain enterprise`；L-044 `claimed_cells` 一格；矩阵 (a) 九工厂同行仍 uncovered、无 enterprise 切片行、无 Vue 行；厨房独行已 covered（L-039）；(b) 无本课符号；(c) 两厅堂已 covered；(d) 无浏览器企业流，已 covered 的主路径是个人 submit→decide；课开篇「口试名单就是矩阵 (a) 这一格」+ 负空间 L-039 / L-040…L-043 / L-007 / L-008 / L-020 / L-022 / L-029
- difficulty: medium

课把浏览器企业这一头收成「同一本管理端总菜单里的后三张企业点菜单，加一张用户端认证表怎么喊同一座厨房」。计划里至少有六套可数对象，派单点名 **一格 (a) 切片**：

1. **OBJ-44 原文。** `course.md`：能说明 web-domain enterprise 页面如何消费 `profileService`。一句里是 **页面消费**，不是 `EnterpriseAdminController`、不是厨房再开一座工厂、也没有把九个 `create*WebDomain` 写成口试清单。
2. **chain / 课 `claimed_cells`。** 一格。符号写成 `createProfileWebDomain enterprise`。磁盘上 **没有** `createEnterpriseWebDomain`。管理端工厂仍是 `createProfileWebDomain`（id `web-domain-profile`，七键一次冻住）。用户端工厂是旁边的 `createProfileSelfWebDomain`（id `web-domain-profile-self`，第三键 `profile/enterprise/application`）。chain **不**写 Vue 文件名，也 **不**写 `createEnterpriseWebContribution` / `createProfileSelfWebDomain` 这两个函数名。
3. **矩阵 (a) 两处不对齐。** 厨房独行已 covered，本课不得再当第二张厨房章。九工厂行把本课切片和 `createSystemWebDomain` 等邻居绑死。Lead 若按**现印同行**标 covered，会把 L-020 / 未写的 notify/third/workflow/demo/ai 菜单假闭合。矩阵 **没有** `enterprise` 切片行，也没有三张 Vue 符号。
4. **课自己的操作定义。** 本课认：`createEnterpriseWebContribution` 冻住三键 `profile/enterprise/{index,detail,review}`，权限摊平 `profilePermissions.enterprise` 六串；名册/详情/审核喊 `runtime.service.enterprise.archive.*`，新建顺手 `materialTags.tree('ENTERPRISE'|'COMMON')`，找人走厅堂 `findUsers('ENTERPRISE')`；self 认证页喊 `enterprise.application.current/save/submit`（键在 self 工厂，**不是**本格 admin 工厂）。厨房里 `probe` / `transfer` / `materials` 四枪 **有对象、无 Vue 扳机**。前四键工厂会一并注册，页面细节归 L-039。
5. **self 表单不是第二张 claimed 格。** OBJ 要「页面如何消费」。课把 self 第三键的**字段与三枪**收进对照走查，并写明 self 工厂格子仍归 L-039。不要为了盖章把 `createProfileSelfWebDomain` 再立一行，也不要把认证页降成「纯邻居、本课不认调用」。
6. **邻格。** L-039 厨房 + person/self/material-tag。L-040 十二扇 archive。L-041 自助四枪 + 门铃。L-042 转移。L-043 材料总库。L-007 / L-008 两厅堂插头。L-020 九工厂行警告。L-022 system 资料页。L-029 `fileUpload`。

问：本格闭合标准是「enterprise 贡献三键 + 管理端三页实际扣的 archive/标签树/`findUsers` + self 认证三枪 + 厨房剩枪无 Vue，且不碰九工厂整行、不升 Vue 矩阵行、不重盖厨房章」，还是 OBJ 那句必须把三张 Vue 升成矩阵行、或必须先把九工厂行拆开、或必须等 GP-L-042/L-043 才给本切片盖章？Lead 若凭「`createProfileWebDomain` 四字在函数表里」把九工厂行标 covered，是否假闭合了另外七个工厂？

## Q2 证据

- objective_id: OBJ-44
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: web-domain `index.ts` 16–32；`index.test.ts` 26–73；`runtime.ts` 29–81；`enterprise/registration.ts` 15–52；`EnterpriseProfilePage.vue` 7–8 / 14–16 / 101–105 / 153 / 179 / 184–187 / 211 / 229–230 / 247–260；`EnterpriseProfileDetailPage.vue` 3 / 16；`EnterpriseProfileDetailPanel.vue` 23–27 / 184–185 / 211 / 234 / 243 / 253–277；`EnterpriseProfileReviewPage.vue` 39–41 / 62 / 68 / 81 / 90 / 97 / 102–105 / 121–123；`logic.ts` 16–34 / 36–48 / 55–67 / 69–71；`logic.test.ts` 22–41 / 68–89；`components.test.ts` 6–41；`EnterpriseIdentityForm.vue` 80–93；`self/registration.ts` 8–21；`self/runtime.ts` 12–15；`self/registration.test.ts` 15–23；`EnterpriseVerificationPage.vue` 53–60；`ProfileCenterPage.vue` 15 / 23；domains `service.ts` 55–89；`service.test.ts` 38–46 / 48–100 / 102–145 / 147–185；`enterprise/archive/service.ts` 40–63；`enterprise/application/service.ts` 13–25；`enterprise/transfer/service.ts` 15–21；`enterprise/materials/service.ts` 28–39；`enterprise/materials/index.ts` 4–6；`permissions.ts` 14–21；根 `index.ts` 113–126；两包 `package.json` exports；domain `README.md` 19；web-domain `README.md` 7；admin `services.ts` 53；home `services.ts` 16；`adminManifestRegistry.ts` 141–185 / 373；registry 测试 141–179；`homeManifestRegistry.ts` 26–39；DML 535 / 1443 / 1513；generated `openapi.ts` 2380+ / 2624 / 2668 / 5477+
- difficulty: high

课的企业贡献 + 页面消费链哪些能在工作树直接复现，哪些只是测试名单比课表窄、或口试数字写偏？

**仍能复现（本切片）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 管理端工厂 `createProfileWebDomain`，id `web-domain-profile`；一次拼 material-tag + person + enterprise 三份贡献 | `index.ts` 16–32 |
| 七键顺序，后三行才是企业：`profile/enterprise/{index,detail,review}` → `EnterpriseProfile*` | `index.test.ts` 39–47 |
| `createEnterpriseWebContribution` 三键 + 权限目录 id `profile-enterprise`，六串取 `Object.values(profilePermissions.enterprise)` | `registration.ts` 20–46 |
| `createProfileWebDomain(undefined)` / `createEnterpriseWebContribution(undefined)` / 缺 `confirm` / `service: {}` 立刻 throw | `index.test.ts` 26–35 |
| `requireProfileWebRuntime`：host 九口 + `fileUpload` + 标签五口 + 两边 archive **十一**口；**没有** `eligibleUsers` / `application` / `materials` / `transfer` | `runtime.ts` 29–81 |
| 空 `selectedManifestIds` 的 fixture `componentKeys()` 是 `[]` | `index.test.ts` 65–72 |
| admin 选 `web-domain-profile`；home 选 `web-domain-profile-self`，**不**选管理端这份 id | registry 366–376；home 26–39 |
| 名册：`archive.page`，空状态收成 `undefined`；下拉只有 `ACTIVE` / `REVOKED`；持 `override` 才见新建 | `EnterpriseProfilePage.vue` 7–16 / 179 / 184–187 |
| 新建：`findUsers('ENTERPRISE')` + `tree('ENTERPRISE'|'COMMON')` + `runtime.fileUpload`（10MB、最多 10、`png/jpg/jpeg/pdf/doc/docx`）+ `archive.create({ identity, bindUserId, reason, materials })`；源码禁止 `materials: []`；`bindUserId` 可 null | 页 101–105 / 153 / 211 / 229–230 / 247–260；`components.test.ts` 19–24 |
| `flattenEnterpriseMaterialOptions` 只收启用中的 `TAG`，scope 是 ENTERPRISE 或 COMMON | `logic.ts` 55–67 |
| `isEnterpriseIdentityComplete` 九个法定字段；联系人/电话/邮箱/注册资本不是这道闸 | `logic.ts` 36–48；`logic.test.ts` 44–65 |
| 详情薄壳 `route.query.id` 交给同一份 Panel；列表抽屉也复用 Panel | `EnterpriseProfileDetailPage.vue` 3 / 16；名册抽屉 52–54 |
| 详情：`archive.detail` / `archive.material` + `downloadMaterial`；高风险 `confirm` 后 `revise` / `assign` / `manageBinding` / `revoke`。当前版本 `status === 'CURRENT'`，否则 `versions[0]` | Panel 184–185 / 211 / 243 / 255–277 |
| 告警条写死：法定代表人是企业法定字段；认证负责人是当前绑定的系统账户，两者不会自动等同 | Panel 23–27；`components.test.ts` 7–11 |
| `enterpriseActionMatrix`：`REVOKED` 四钮全关；`assign/revise/revoke` 要 override；`manageBinding` 要 manage 且存在 ACTIVE/SUSPENDED。`enterpriseBindingAction`：ACTIVE→SUSPEND，SUSPENDED→RESUME，其余→UNBIND | `logic.ts` 16–34；`logic.test.ts` 22–41 / 68–70 |
| `assign` 时 `userId === ''` 只 warning，不发 HTTP | Panel 253 |
| 审核：`archive.review` / `reviewMaterial`；缺 `taskId` 只 warning「缺少流程任务编号」；流程钮 `completeWorkflowTask({ variables: { profileDecision } })`；覆盖才 `decide` 且 `APPROVE`→`APPROVED` | Review 62 / 68 / 81 / 90 / 97 / 102–105 / 121–123；`components.test.ts` 35–39 |
| 厨房 `review` URL 是 `.../review-context`，不是 `/review`；create 是 POST `.../admin-create`；注销 POST `.../revoke` | `archive/service.ts` 44–63 |
| `'app/2'` → `/profile/enterprise/archive/application/app%2F2/material/ref%2F2/access-url` | `service.test.ts` 167 / 181 |
| 厅堂 `findUsers('ENTERPRISE')` → `enterprise.archive.eligibleUsers`；nickName 空时标签 `owner (owner)`；`completeWorkflowTask` → `workflowService.completeTask`；`downloadMaterial` 造 `<a download>` | `adminManifestRegistry.ts` 145–182；registry 测试 157–179 |
| self 工厂第三键 `profile/enterprise/application`；id `web-domain-profile-self`；`permissions: Object.freeze([])`；校验只拒缺 runtime，测试可塞 `service: {}` | `self/registration.ts` 8–21；`self/runtime.ts` 12–15；`registration.test.ts` 9 / 18–22 |
| 认证页：`current` → 保存 `save` → 提交先 save 再 `submit(version)`。五证件。`handlerIsLegalRepresentative`。`valid()` 另要 `contactName` / `contactPhone`。失败走 `error instanceof Error ? error.message` | `EnterpriseVerificationPage.vue` 53–60 |
| 中心页企业卡写死 `'profile:enterprise:apply'`，`router.push('/profile/enterprise')`，不 import `profilePermissions` | `ProfileCenterPage.vue` 23 |
| 旧名 `enterprise.application.sendTransfer === enterprise.transfer.send`（以及 `confirmTransfer`） | `service.ts` 66–73；测试 44–45 |
| application 四枪源码：GET 根、POST 根、POST `/submit` `{ expectedVersion }`、POST `/probe` 再 `projectStatusProbeResponse` | `application/service.ts` 16–24 |
| transfer 三枪全 POST `/profile/enterprise/transfer/{send,confirm,unbind}` | `transfer/service.ts` 17–20 |
| materials 四枪：GET list / POST attach / POST `.../detach` / GET access-url | `materials/service.ts` 32–38 |
| 全仓 Vue **零** `enterprise.materials.*` / `enterprise.transfer.*` / `enterprise.application.probe` | Vue grep 空；厨房测试 60–67 / 124 |
| 资源标签 `controller: 'EnterpriseMaterialController'` 只是字符串 | `materials/index.ts` 4–6 |
| 仓库无 `createEnterpriseWebDomain`、无 `@namewta/domain-profile-enterprise`、无 `src/enterprise/anonymous/` | 全仓 grep 空；web-domain README 第 7 行 |
| 菜单种子：管理端 `profile/enterprise/index`（query）；home `profile/enterprise/application`（apply）；工作流表单 `profile/enterprise/review`；detail **无**独立菜单行 | DML 1513 / 535 / 1443 |
| 生成 OpenAPI 已有 `/profile/enterprise/archive`、`application`、`transfer`、`materials`、匿名 callback | `openapi.ts` 2380+ |
| `safeEnterpriseErrorMessage` 吞掉信用代码，只回「操作失败，请刷新后重试」；详情源码禁 `delete` / `export` / `console.` | `logic.ts` 69–71；`logic.test.ts` 89；`components.test.ts` 32 |
| 读 GET 写 POST；测试锁死方法不是 get/post 的枪不存在；URL 不含 `export` | `service.test.ts` 183–184 |
| 403 原样抛出（厨房测的是 person.archive.detail，工厂同一 `http`） | `service.test.ts` 207–209 |
| `profileDomainModule.capabilities` 含 `enterprise-application` / `enterprise-transfer` / `enterprise-material` / `enterprise-archive`；名片不是 componentKey | 根 `index.ts` 113–126 |
| 两厅堂各调一次同一厨房：`profileService = createProfileService(domainHttp)` | admin `services.ts` 53；home `services.ts` 16 |

**课写过、测试名单更窄或口试数字写偏（不是证伪本格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「`EnterpriseIdentityForm` 证件类型写了十三项」 | 数组是 **十二** 项（与 `PersonIdentityForm` 同一份名单）。居住证/通行证/多地护照在，没有第十三项 |
| 「self 证件五选项：居民身份证 / 港澳台身份证 / 中国护照」 | **五项独立 label**：居民身份证 + 香港/澳门/台湾居民身份证 + 中国护照。数量对，口试不要背成三个大类名 |
| 口试表把动词写成 GET/POST | 厨房 `method: 'get'`/`'post'` 小写。语义对，字符串不是大写 |
| 「旧名与新名是同一只手」举三只 | 源码三只都挂上了（含 `unbind`）。facade 测试只锁 `sendTransfer` / `confirmTransfer` 的 `toBe`，**没有** `unbind` 的 `===` |
| application 三枪 URL | 源码与 Vue 有；`service.test.ts` **没有** `current` / `save` / `submit` 请求表（只打了 probe + 转移旧名） |
| archive 十二枪 | 源码十二口全在。facade 企业段只打 `create` / `eligibleUsers` / `reviewMaterial`，**没有** page/detail/review/decide/revise/assign/binding/revoke 的企业 URL 行 |
| 覆盖决定要 `APPROVED` | **页面**映射：`decision.value === 'APPROVE' ? 'APPROVED' : 'REJECTED'`。厨房测试 `person.archive.decide(..., { decision: 'APPROVE' })` **原样** POST，工厂不改码；企业 `decide` 不在 facade 测试表里 |
| 「测试禁止源码出现 `materials: []`」 | Vue `components.test.ts` 成立。厨房测试自己就 `enterprise.archive.create({ ..., materials: [] })` |
| 「OpenAPI 快照尚无 `/profile/**`」 | domain `README.md` 第 19 行仍写这句。`generated/openapi.ts` **已经**有企业路径。课已要求以生成文件为准 |
| PERSON 牌进不了新建下拉 | `logic.ts` 过滤成立。`logic.test.ts` 只喂了一张 ENTERPRISE TAG，不断言 PERSON 被丢 |

问：在贡献三键、fail-closed、十一口不含 eligibleUsers、页面不喊 materials/transfer/probe、APPROVE→APPROVED 只发生在覆盖决定、self 先 save 再 submit、法人≠负责人已经能互证的前提下，(a) 是否还要把「证件 12 不是 13」「facade 测试没打 current/save/submit」「厨房 create 夹具用空 materials」写成开篇半句，才算本切片讲完？还是这些已经落在课的表/反例里，Lead 可以把本切片视为已讲、并保持九工厂行 uncovered？

## Q3 假设与反例

- objective_id: OBJ-44
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无 `createEnterpriseWebDomain`；home 厨房仍有 archive；Vue 不 attach / 不 probe / 不 transfer；流程钮不打 `/decision`；`review` URL 是 `/review-context`；self 校验只拒缺 runtime；中心页字面量 apply；capabilities 有 enterprise-transfer 无转移页；九工厂行不因本课整行闭合；管理端 12 证件 ≠ self 5；错误文案两套策略
- difficulty: high

课的隐含假设若反转，合格读者会把哪座工厂或哪只抽屉认错？

1. **「home 的 `profileService` 没有 `enterprise.archive`。」** 对象上有。差在 self 三键。课反例 1 / L-008 / L-039 覆盖。
2. **「`/user/profile` 或 home `/profile/enterprise` 就是管理端企业名册。」** admin 个人中心是 system 资料页（L-022）。home `/profile/enterprise` 是 self 企业认证。管理端名册键是 `profile/enterprise/index`。课反例 2 覆盖。
3. **「有一份 `createEnterpriseWebDomain`，是第九个工厂。」** 没有这份函数。企业三键是 `createProfileWebDomain` 里的 `createEnterpriseWebContribution`。课反例 5 / 开篇表覆盖。
4. **「一份 `createProfileWebDomain` 同时给 home 用。」** home 调 `createProfileSelfWebDomain`。课开篇表覆盖。
5. **「页面自己拼 `/profile/enterprise/archive`。」** Vue 只喊 `runtime.service.enterprise.archive.*`。课开篇表覆盖。
6. **「新建档案会 `enterprise.materials.attach`。」** 全仓 Vue 搜不到。直建走 `archive.create.materials` + `fileUpload`。课反例 3 覆盖。
7. **「详情下载走 `enterprise.materials.accessUrl`。」** 走 `enterprise.archive.material`。课误区 5 覆盖。
8. **「审核通过 = `decide({ decision: 'APPROVE' })`。」** 覆盖决定要 `APPROVED`。流程变量才 `APPROVE`。课反例 4 覆盖。厨房测试夹具用 `APPROVE`，不要把测试 data 背成页面合同。
9. **「流程审核按钮打 `/decision`。」** 默认 `completeWorkflowTask`。课误区 6 覆盖。
10. **「`review` 的 URL 是 `/review`。」** 是 `/review-context`。课误区 7 覆盖。
11. **「页面 `findUsers` 就是厨房方法。」** 是 runtime 口。厅堂按 profileType 转 `eligibleUsers`。课误区 8 / 机制 2 覆盖。
12. **「`createProfileWebDomain(undefined)` 会给出空企业菜单。」** 立刻 throw。贡献自己也会 throw。课反例 6 覆盖。
13. **「前端资源名 `EnterpriseMaterialController` 对应一个 Java 类。」** 只是标签。后端两份类（L-043）。课反例 7 覆盖。
14. **「厨房会读 `Admin-Token` / 会 HMAC / 会按 App 裁掉 archive。」** 只收 `HttpClient`。课反例 8 / 误区 4 覆盖。
15. **「注销是 DELETE，导出在 archive.export。」** POST `.../revoke`；测试禁 export。课反例 9 覆盖。
16. **「转移挑战页喊 `archive.manageBinding('UNBIND')`。」** 没有转移页。管理端解绑是 archive binding 窗；自助解绑是 L-042 的 transfer。课反例 10 覆盖。
17. **「self 企业页会先 probe 信用代码。」** 厨房有 probe。认证页 `valid()` 只看必填字符串。课反例 11 覆盖。
18. **「`capabilities` 有 enterprise-transfer 所以一定有转移页。」** 名片有能力，manifest 可以不挂页。课误区 10 覆盖。
19. **「管理端证件十三项 = 用户端证件五项。」** 两张表各自硬编码。磁盘管理端是 **十二** 项，self 是五项独立 label。课误区 11 覆盖；数字以源码为准。
20. **「中心页 import 了 `profilePermissions`。」** 没有。课误区 12 覆盖。
21. **「本课覆盖 person 三页 / 标签管理页。」** L-039。工厂会**一并注册**那四键，本课不认页面调用。课误区 13 覆盖。
22. **「匿名核身也有一张 Vue。」** 没有。L-041。课误区 14 / 边界 7 覆盖。
23. **「法定代表人账户就是认证负责人。」** 页面专门写了相反的那句话。课误区 15 覆盖。
24. **「self 校验和 admin 一样严。」** self 工厂只拒缺 runtime；认证页甚至不校验厨房有没有 `application.current`。课误区 16 覆盖。
25. **「把九个 web-domain 工厂一行标 covered。」** GP-L-020 / GP-L-039 已警告。课反例 5 / 误区 17 覆盖。
26. **「OBJ-44 包含 `EnterpriseAdminController`。」** OBJ-40。课误区 1 覆盖。
27. **「`createProfileWebDomain` 等于 `createProfileSelfWebDomain`。」** id、键、runtime、权限目录、校验强度全不同。课误区 2 覆盖。
28. **「企业贡献是第四个包 `@namewta/web-domain-profile-enterprise`。」** 子路径 `./enterprise`，还在同一个 web-domain 包里。课误区 3 覆盖。
29. **「本课格子包含厨房 `createProfileService`。」** 厨房独行已由 L-039 盖章。本课只认页面怎么喊它。
30. **「runtime 校验了 `eligibleUsers`。」** 找人走 `runtime.findUsers`。课边界 1 覆盖。

问：读完后仍说不出的，是不是「工厂的名字」，而是 **厨房方法全集 ≠ 墙上的菜 ≠ 页面已扣的扳机、九工厂行不能因本课整行闭合、覆盖决定才改 APPROVED、self 空 permissions 不是用户没有 apply 权、probe/transfer/materials 目前只有测试、法人 ≠ 负责人、管理端 12 证件 ≠ self 5**？

## Q4 视角

- objective_id: OBJ-44
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-44 vs L-007/L-008/L-020/L-022/L-029/L-039/L-040…L-043；九工厂行；claimed_cells 切片名 vs `createEnterpriseWebContribution` / self 第三键；厨房独行已 covered
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这一格 (a) 切片**？

| 视角 | 多出来的边 | 是否本课这一格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 九工厂同行 vs chain 切片名 `enterprise`；厨房独行已 covered；无 Vue 行；(b) 无投影/runtime 行；(d) 无浏览器企业流 | **是** 闭合定义。本切片可视为已讲。九工厂行保持 uncovered。不要发明 (b)/(d)，不要把三张 Vue 升成新行才能给切片盖章，不要重盖厨房章 |
| 管理端菜单工厂 | 七键里后三键、fail-closed 十一口、权限摊平含 enterprise 六串、未选无键 | **是** 切片里的 `createProfileWebDomain` 贡献。前四键 **调用** **否**（L-039） |
| 企业页面 owner | page/create/detail/material/revise/assign/manageBinding/revoke/review/decide；`tree('ENTERPRISE')`；`findUsers('ENTERPRISE')`；fileUpload；APPROVE→APPROVED；法人≠负责人 | **是** OBJ 对照走查，不是矩阵第二格 |
| 用户端认证表单 | current/save/submit；五证件；`handlerIsLegalRepresentative`；中心页字面量 apply | **是** 消费对照。self 工厂格子 **否**（L-039）。后端材料闸 / probe **否**（L-041 / L-043） |
| 厨房作者（L-039） | 四抽屉冻住；旧名 `===`；probe 投影；九子路径 URL | 本课借用 URL/剩枪 **是** 负空间。工厂盖章 **否**（已 covered） |
| 厅堂电工（L-007 / L-008） | 两厅堂各 `createProfileService(domainHttp)`；Token 在 HTTP 适配器；`findUsers` / `completeWorkflowTask` / `downloadMaterial` 接线 | 插头与找人接线一句 **是**。12 个 `create*` 全表 **否** |
| 导航 host（L-020） | admin 选 `web-domain-profile`；home 选 `web-domain-profile-self` | 选择集一句 **是**。15 键 / 恢复链 **否**。九工厂行 **否** 整行 |
| 档案窗（L-040） | `/profile/enterprise/archive` 十二扇 | URL 对齐 **是**。Java 窗 **否** |
| 自助申请 / 门铃（L-041） | current/save/submit/probe 五层；`@SaIgnore` 回调 | 三枪 URL / 无 Vue 门铃 **是** 边界。submit 事务 / 门铃三态 **否** |
| 转移（L-042） | send/confirm/unbind | 厨房有枪、页面未扣 **是** 边界。Controller / (b) confirm **否** |
| 材料总库（L-043） | attach/detach 四枪；ALWAYS 闸 | 厨房有枪、直建不喊 attach **是** 边界。Java 窗 **否** |
| person 菜单（L-039） | 前四键 + self 个人认证 | 工厂一并注册 **是** 负空间。页面消费 **否** |
| system 资料页（L-022） | `/user/profile`、`SysProfileController` | 走错楼一句 **是**。社交/改密 **否** |
| OSS 直传（L-029） | `runtime.fileUpload` → `ossUploadClient.upload` | 夹 `ossId` **是** 边界。adapter 窗口 **否** |
| Java 聚合行 `CompositeProfileService…` | wta-api 端口 | **否**。不要把后端聚合行并进前端工厂 |
| MySQL 8.4 / Redis 8 | 菜单种子 / 会话 | 种子键对照 **是** (a)。容器 **否** |

问：Lead 若凭「工厂名在九工厂行里」把该行标 covered，是否还要求读者能指着 **后三键、页面不喊 attach/probe/transfer、覆盖决定才改 APPROVED、self 三枪、法人≠负责人、runtime 不含 eligibleUsers**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要等 L-042/L-043 才给**本切片**当已讲？九工厂行是否例外——必须保持 uncovered，直到其余七个工厂也有课？厨房独行是否已经闭合，本课只往备注写「enterprise 页面已讲」？

## Q5 元问题

- objective_id: OBJ-44
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 九工厂同行仍 uncovered、厨房独行已 covered、无 Vue 行、无本课 (b)/(d)
- difficulty: low

建议：`defer`

原因：

- **`A:createProfileWebDomain enterprise` 是 chain 切片，不是矩阵独行。** L 合同章节齐全（宏观地图、定义表、机制链 1–8、带 caption / 文字等价物的两张图、正反例、变式、误区、暂停走查、来源表 S-005/S-006/S-010/S-L039-01/04 + S-L044-01…07）。能复现：贡献三键与六串权限、七键后三行、缺 runtime 关门、名册 page/create、详情四条高风险命令、审核分流工作流/`decide`（APPROVE→APPROVED）、`findUsers('ENTERPRISE')`、`tree('ENTERPRISE'|'COMMON')`、self 认证三枪、厨房 probe/transfer/materials 无 Vue、法人≠负责人。不必为「工厂的名字」再挖五问。
- **Lead 不可以凭本课把九工厂同行标 `covered`。** GP-L-020 / GP-L-039 已立过同一条。本切片证据可写进该行备注（「profile 管理端 enterprise 三页 + self 企业认证表单已讲」）。**厨房独行保持已 covered**，不要重开第二张厨房章。
- 本批可粘贴的精度项不挡住切片视为已讲：（1）管理端证件是 **12** 项不是课文「十三项」；（2）self 五选项是五项独立 label，不是三个大类名；（3）动词小写 `'get'`/`'post'`；（4）facade 测试未打 `current`/`save`/`submit`、未锁 `unbind ===`、企业 archive 测试表窄于十二枪；（5）厨房 create 夹具用 `materials: []`，Vue 禁止；（6）domain README 第 19 行与 generated `/profile/enterprise/**` 并存，课已点破。不要把 Java 十二扇、转移页、materials 四枪 Vue、person 三页、九工厂其余七个名字塞进本格。
- 不建议 `mine-more`：b01 未证明本切片还有真实矩阵空洞；再五问只会把 L-040 十二扇、L-041 probe/门铃、L-042 转移、L-043 attach 或 L-039 前四键再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；「十三→十二」是半句精度，不是第二容器。不建议 `split-lesson`：未满 10 问；admin 贡献与 self 表单消费是 chain 原设计的同一 OBJ-44，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：fail-closed runtime、空 permissions、错误文案分裂是 (a) 的解释到副作用。不建议把三张 Vue 升成矩阵行才给切片盖章：课已写明格子按 chain 认工厂切片。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:createProfileWebDomain enterprise | 课按磁盘讲了贡献三键、七键后三行、admin fail-closed 十一口、页面实际扣的 archive/标签树/`findUsers`、审核分流、self 认证三枪、厨房剩枪无 Vue、法人≠负责人。**矩阵无此切片行**；符号落在九工厂同行。残留精度：证件 12 不是 13；self 五项独立 label；facade 测试表窄于源码/Vue。 | 可选半句：口试证件数以 Vue 数组为准（管理端 12、self 5 个独立项）；`current/save/submit` 以资源 `service.ts` 与认证页为准，不要只背 facade 测试表；覆盖码映射在 Vue 不在工厂。不要求重写机制链。Lead **不要**改九工厂行为 covered。可将本切片证据写入该行备注。 | defer（切片已讲；九工厂行不可因本课整行闭合） |
| 九工厂行 `create*WebDomain`（含 `createProfileWebDomain` / `createProfileSelfWebDomain`） | 本课只用选择集 + 后三键当切片证据。其余七个工厂未因本课讲完。 | 保持 uncovered。L-020 已覆盖 `createSystemWebDomain` 切片；L-039 已覆盖 profile 两份菜单的 person/self；其余后续课。 | defer（九工厂行保持 uncovered） |
| A:createProfileService | 厨房独行已 covered（L-039）。本课只借用企业四抽屉 URL 与剩枪负空间。 | 不要重盖厨房章。备注已指向「enterprise 页面 L-044」，Lead 可在备注补「页面消费已讲」。 | defer（已 covered） |
| A:createProfileWebDomain person/self | 前四键工厂一并注册；页面调用不认。self 工厂三键已在 L-039；本课只认第三键的企业表单字段。 | 保持划在 L-039。不要并进本格才能盖章。 | defer（L-039） |
| A:createProfileSelfWebDomain（九工厂行内符号） | 课已锁第三键调用 + 空 permissions + 瘦校验。函数名不在 chain 格子字面。 | 保持划在 L-039 切片。表单消费证据可写进本课备注。 | defer（工厂格子 L-039；表单对照已在本课） |
| EnterpriseAdminController.* | 十二扇 URL 对齐旁证已借用。 | 保持。L-040。 | defer（L-040） |
| EnterpriseApplicationController.current,save,submit,probe / EnterpriseVerificationAnonymousController.callback | 三枪 URL / 无 Vue 门铃已点。probe 无页面。 | 保持。L-041。 | defer（L-041） |
| EnterpriseTransferController.send,confirm,unbind | 厨房有枪、页面未扣。 | 保持。L-042。不要把 archive.manageBinding 说成转移页。 | defer（L-042） |
| EnterpriseMaterialAdminController.* / EnterpriseMaterialSelfController.* | 厨房四枪无 Vue；直建走 archive.create.materials。 | 保持。L-043。 | defer（L-043） |
| A:admin-web.services / A:home-web.services | 插头 `createProfileService(domainHttp)` 与 `findUsers` 接线已借用。 | 保持。L-007 / L-008。 | defer（L-007 / L-008） |
| A:createSystemWebDomain / B:navigation host | 选择集 + 两份 manifest id 已点。15 键全表未讲。 | 保持。L-020。 | defer（L-020） |
| A:SysProfile / 资料页 | `/user/profile` 走错楼一句。 | 保持。L-022。 | defer（L-022） |
| A:createOssUploadClient.upload | `runtime.fileUpload` 只当口。 | 保持。L-029。 | defer（L-029） |
| CompositeProfileService, ProfileService, PersonIdentityLookupService, ProfileMaterialPort | 后端聚合行四名字未认。 | 不要并进前端工厂才能盖章。 | defer（未认的 Java 聚合行） |
| (b) runtime 校验 / 页面扣扳机 / APPROVE 映射 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 浏览器企业档案流 | 矩阵无此行。ASCII 图不是新的 (d)。已 covered 的主路径仍是个人 submit→decide。 | 不要发明前端企业申请/审核数据流行。 | defer（未入库） |
| MySQL 8.4 / Redis 8 | 菜单种子是 (a) 对照，不是容器闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本切片无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（九工厂同行是矩阵绑行问题，课内已把切片讲完并显式禁止整行闭合）；合格读者说得出工厂目的（管理端总菜单的企业贡献 + 页面消费同一座厨房）、主路径（admin 走 archive+标签树+`findUsers` / home 走 application 三枪）与失败路径（缺 runtime 关门、缺 taskId 不发 HTTP、`assign` 空账户不发、REVOKED 四钮全关、管理端吞错误 / self 抛原文）；把可选半句写进原课不会超过 40 分钟；后端五层/个人前端/厅堂插头/九工厂其余名字已有邻课。满 10 问前不拆。admin 贡献与 self 表单虽跨两个 manifest id，仍是 chain 原设计的同一 OBJ-44，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对九工厂整行、L-039 person/self/厨房、L-040…L-043 后端窗、L-007/L-008 厅堂、L-020 导航、L-022 资料页、L-029 直传、Java 聚合行、未入库 (b)/(d) 以及 MySQL/Redis 容器显式建议 `defer`；对 `A:createProfileWebDomain enterprise` 建议视为已讲切片、**不要**把九工厂同行标 `covered`、**不要**重盖已 covered 的厨房独行。本课不主张改 Java 窗、发明 Vue 矩阵行、或把 person 三页算进本切片。
