# T-50 recovery Source C1 security/documentation review

Fixed B `50757f9b872b0b9b0131e4b2a4538bff7473f74d` → clean C1 `84ce0a9162dfc507fb4e8339575247477e57684f`, tree `c118348385489be74b55233508ae0b5f40a93ee1`, direct parent B. Read-only fixed-SHA `git diff`/`git show` review; no build, service, Maven, frontend test, or repository write was performed by this reviewer. See `/tmp/wta-t50/source-b-security-review.md` for B generated-contract proof and its failed third real attempt.

## Verdict

**Source C1 README delta: PASS. Exact C1 eight-class real acceptance: PASS after independent result/XML review below.** B→C1 changes one product-space file, `frontend/packages/api-contracts/README.md`, plus eleven files confined to the current SpecDev change's governance/evidence. No backend or executable frontend/contract input changed. I independently confirmed `git rev-parse B:backend == C1:backend == 507250fd94c75aa15a89506e2a4771aa3497469b`; `git diff B..C1 -- frontend backend` lists only the README. The four generated snapshot/pointer/TypeScript blobs at B and C1 have identical Git object IDs; `git diff --check B..C1` exits 0. This corroborates `/tmp/wta-t50/c1-source-equivalence.json` rather than relying on its assertion alone.

The README removes the now-false hard-coded current backend SHA/path and schema totals. It instructs readers to follow `openapi/current.json` to the active immutable revision's `provenance.json` and `source.json`, which is the actual contract structure. It states the supported new-submit values `ALL/ASYNC/priority=0`, explicit HTTP JSON priority and required generated TS field, and historical enum read-only meaning. Those statements match fixed B's live A3-derived source (`NotificationCommand.required=["priority"]`, strategy/mode optional, `generated/openapi.ts` `priority: number`) and the Ticket's supported-mode contract. Historical revisions remain unchanged. This closes the DOC-003 source-attribution drift reported in the B review without altering runtime behavior.

## Reuse and failed-run boundary

At C1, A3 default/full/live capture is reusable only because backend input is byte-identical; B `openapi:fetch/generate/check`, frontend lint/typecheck/tests/build and generated artifact checks are reusable only because their executable/config/generated inputs are byte-identical. Neither set should be described as having run at C1 HEAD. The failed B third real run `e5e3229eb2500be7` remains failed: Maven exited 0 and eight XML roots contain 135/0 failures/0 errors/0 skips, but the runner's source-after was not clean during concurrent frontend build, so acceptance=false. Later clean B and C1 states cannot retroactively repair that exact-run result.

The recovery procedure recorded in `evidence/T-50-recovery-2026-09-23.md` and `dispatch-T-50-20260923-02.md` is bounded: complete all builds before the acceptance lock, keep the old result/XML verbatim, then run the same frozen eight-class driver serially at clean C1, requiring source-before and source-after at the same SHA/tree, positive zero-skip XML, Maven exit 0 and full owned cleanup. It does not remove any business assertion or source guard. At initial review this C1 recovery-batch first run was in progress. Its completed result is independently checked below. Fresh C1 static/OpenAPI documentation checks subsequently completed and are independently checked below. The ticket's target-environment legacy-mode disposition is separate from this code acceptance.


## C1 recovery-batch first real run: independently verified

`/tmp/wta-t50/runs/e0ba930d8ef45c5d/result.json` records **exit 0 / acceptance true / Maven exit 0**, with source-before and source-after both clean at exact C1 `84ce0a9162dfc507fb4e8339575247477e57684f`, tree `c118348385489be74b55233508ae0b5f40a93ee1`. I independently parsed all eight retained XML suites and compared every suite's counts and SHA-256 to `/tmp/wta-t50/c1-retained-verification.json`: **8 files, 135 tests, 0 failures, 0 errors, 0 skips; all eight count/hash pairs match**. Cleanup has no errors, no live Maven process-group members, no remaining owned containers, captured anonymous volume absent and both loopback ports closed. This is a valid focused real MySQL/Redis T-50 plus T-36/37/38/39 regression result at C1; it replaces the failed B attempt as current acceptance evidence but does not erase that historical failure. The A3 default/full/live and B frontend gates remain input-equivalent reuse only, not newly run at C1. Do not extend this focused pass to target-environment legacy rows; fresh C1 statics have separate evidence below.


## Fresh C1 static gates and equivalent frontend evidence

I read `/tmp/wta-t50/c1-{openapi-check,skill-facts,fullstack-facts,notify-layered,fm,handbooks,diff-check}.json` independently: all seven recorded **exit 0**. `/tmp/wta-t50/c1-static-source-before.json` and `c1-static-source-after.json` both record exact clean C1 SHA/tree. Lead's `/tmp/wta-t50/lead-frontend-evidence-check.json` records B source `50757f9` equivalent to recovery C1 `84ce0a9`, 29 Vitest packages with 628 passes, 108 Node passes, **736 executed**, and **330 current production artifacts individually verified against manifest**; I checked that summary and the B/C1 executable-input blob equivalence above. This is a documented input-equivalent reuse of B frontend gates, not a statement that frontend tests/build ran at C1. Together with the independently verified C1 eight-class real acceptance and unchanged backend/generated inputs, this closes the local security/contract verification in this review. It does not establish target-environment historical-mode remediation or deployment readiness.
