# T-13 evidence

HEAD `f2f166c9` closes AC-013. Registration and captcha recovery were already in the built apps and in `SysRegisterService`. The missing piece was a harness that serves those apps over HTTPS. AC-013 stays in the T-30 combined run.

## Browser journey

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=RegistrationRecoveryIntegrationTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Dregistration.recovery.integration=true \
  -Dnamewta.repo.root=/srv/WTA-plus
```

Exit 0. `RegistrationRecoveryIntegrationTest`: tests=1, failures=0, errors=0, skipped=0. Playwright: 10 passed, 0 skipped, 6.5s.

The cases cover a closed register switch hiding the entry and disabling the page, a failed context that can be rechecked without reload, captcha responses that keep the submit button disabled until applied, keyboard refresh where the older captcha cannot replace the newer uuid, rejected and network failures that clear secrets and allow another submit, and a repeated submit that sends once while a late success does not navigate away after cancel.

## Server rejection and home unit tests

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=SysRegisterServiceRegistrationUnitTest \
  -Dsurefire.failIfNoSpecifiedTests=false
```

Exit 0. tests=7, failures=0, errors=0, skipped=0. A disabled or missing register flag is rejected by the shipped service before captcha consumption or user persistence. Invalid phone numbers are rejected the same way.

```bash
cd frontend && pnpm --filter @namewta/home-web test
```

Exit 0. Test files=7, tests=15, skipped=0.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase. The harness is outside the original ticket write set and is recorded on the ticket. No production source edit.
