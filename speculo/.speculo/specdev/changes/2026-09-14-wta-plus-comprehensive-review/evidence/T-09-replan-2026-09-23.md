# T-09 evidence

HEAD `a139dbc5` closes AC-009. The production build matrix already emitted per-app mode markers and the bundle verifier already enforced the required and excluded modules. One regression assertion still looked for a single outbox insert after fan-out had moved to `insertFanout`; that assertion was updated in this HEAD. No tracked application config or bundle classifier changed. Environment-gated tests that skipped are not counted as passes. AC-009 stays in the T-30 combined run.

## Frontend modes

```bash
cd frontend && pnpm build:dev
cd frontend && pnpm build:prod
```

Both exited 0. `build-mode.json` after `build:dev`: admin-web, home-web, and sso-web were `development`. After `build:prod` the same three apps were `production`.

## Backend suite and bundles

```bash
cd backend && ./mvnw test
```

Exit 0 on `a139dbc5`. Class rows: tests=1191, failures=0, errors=0, skipped=247, so 944 assertions actually ran. The 247 skips are service-gated integration tests whose ports or URLs were not supplied. They are not passes.

```bash
cd backend && ./mvnw clean package -DskipTests
bash scripts/ci/verify-admin-bundle.sh full
cd backend && ./mvnw clean package -Pbundle-core -Dmaven.test.skip=true
bash scripts/ci/verify-admin-bundle.sh core
```

Each exited 0. The verifier printed `full bundle contents verified` and `core bundle contents verified`.

```bash
node --test release-artifacts/tests/build-matrix.test.mjs
```

Exit 0. tests=33, pass=33, fail=0, skipped=0. That includes missing-module, excluded-module, impostor, and retired Snail AI rejections, plus the check that Redis, MySQL, and MinIO images in `scripts/ci/run-external-services.sh` equal `release-artifacts/docker/docker-compose-infrastructure.yml` (`redis:8.6.3`, `mysql:8.4.9`, `quay.io/minio/minio:RELEASE.2025-04-22T22-12-26Z`).

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase. The only source edit is the wake test's fan-out assertion, outside the original T-09 write set, recorded on the ticket.
