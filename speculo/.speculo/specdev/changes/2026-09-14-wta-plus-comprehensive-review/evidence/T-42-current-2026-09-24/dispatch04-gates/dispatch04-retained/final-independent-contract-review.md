# T-42 independent contract review — fixed `3efc5862`

**Verdict: product source has no remaining AC-042 blocker found, but candidate acceptance is blocked by the current default backend gate.** This is a read-only review of `5417c257130216e2b283ca933d9496d76c10f46a..3efc5862e1bfb86ee23e566d1eb05aa1dda24e94` (72 backend/release/frontend changed paths). I used fixed `git show`/`git diff`, the earlier independent [A1 review](/tmp/wta-t42/review-product-a1-contract.md), [02B JDBC review](/tmp/wta-t42/review-02b-jdbc.md), writer [43e5 self-review](/tmp/wta-t42/final-contract-43e5-review.md), and retained run results. I did not build, start services, edit repository files, or inspect credentials. `git diff --check` on the fixed range exited 0.

## Closure of earlier product findings

1. **Old physical MAIL sender in flight versus cancel/reclaim:** `NotifyDispatchResultUseCase.beginMailProviderSend` is a dedicated `@DSTransactional` entry. `NotifyDispatchResultService.beginMailProviderSend` acquires Intent through `lockActive`, then Outbox, Delivery and relations; checks the live lease, deadline, MAIL channel and every READY snapshot; atomically changes every relation's `send_reserved` to true before the physical sender. `DispatchNotificationService` invokes it through the final Mail pre-send gate. A false/unknown transaction commit cannot authorize a send. `NotifyAttachmentSnapshotTransactions.releaseIfSafe` requires every relation's marker explicitly false; true or null conservatively retains both references. This closes the A1 cancel + expired lease + late SMTP release counterexample without treating an empty provider receipt as proof of non-send. The real 27-method suite includes the blocked sender/reclaim and BEFORE/AFTER reservation-commit cases.
2. **Definitely unsent `UNDELIVERABLE` Mail:** `releaseIfSafe` now admits this terminal delivery only with the other strict conditions: all Mail deliveries terminal unsent, no accepted/provider receipt, no active outbox lease, every relation in the allowed state with `send_reserved=false`. Its fresh DB test covers absent email/no Outbox and source-reference release. `COPYING`/`COPY_UNKNOWN`, retryable local failure, or a reserved send remain excluded.
3. **Persistence annotations:** `NotifyIntentAttachment.version` now has `@Version` and `delFlag` has `@TableLogic`. The four manual `version+1` writes were removed; DAO updates still require one affected row. The fresh stale-version/logical-delete test and multi-relation reservation rollback are present.

The A1 authorization, snapshot and unknown-outcome chains remain intact in the final source: the submitting User/Client comes from the trusted session adapter and is persisted with the Intent; System locks/checks the source owner, Client, ACTIVE state, source policy and actual references; the Worker reads that persisted actor rather than adopting its own context. Copy reserves a stable target and both references before bounded external I/O; an uncertain PUT or commit stays `COPY_UNKNOWN` with source/target ownership, and READY is rechecked before sending. Empty attachments still have zero OSS work. The demo-mail no-link exception does not remove notice/workflow path requirements. The background `NotifyContextConfiguration` checks Sa-Token context validity only for optional audit identity; an active request still uses `LoginHelper`, while attachment authorization stays on the stored actor. The later full-context test asserts the live resolver returns the logged-in user and Client.

The public `NotificationCommand.attachmentOssIds` field is optional `List<String>` and defaults to empty; the runtime parses positive decimal Long IDs, rejects invalid strings and preserves order while deduplicating. `OssClient` bounded transfer methods retain default fallbacks for binary entry compatibility and production overrides; the new `OssService` attachment methods are a direct in-repository SPI migration, with no promise of compatibility for out-of-tree implementations under Ticket §8/§10. `backend/README.md:134-138` records the 20-item/10 MiB/25 MiB/30-second limits, 104-table new-database baseline and conservative `COPY_UNKNOWN`/`send_reserved` operations. It does not prescribe deleting an uncertain key. No further public-SPI or documentation blocker was found in this scope.

## Actual verification and blocking gate

- Retained `/tmp/wta-t42/runs/a36d6d66aa5e06bd/result.json` records **27/27 Mail integration methods, 0 failures/errors/skips**, Maven exit 0, exact clean `43e5c2be`, fresh 104-table six-SQL MySQL/Redis/MinIO, and cleanup errors `[]`. I previously checked its sanitized fresh XML and method manifest. The strengthened BEFORE hook at 43e5 requires successful owned-session KILL acknowledgement and entry into the real driver commit; AFTER requires real commit then lost acknowledgement. The earlier 6f5 green result is not used to prove that boundary. `43e5..3efc` changes only the unrelated `NotifyAtomicResultIntegrationTest` fixture binding count (10 to 11), so Mail production and the 27 test methods are byte-identical at the reviewed candidate. The physical SMTP sender is a controlled substitute; this proves the production Spring/Notify/System/OSS/DB wiring and pre-sender bytes, not external SMTP delivery.
- Retained `/tmp/wta-t42/regression-runs/0c1f52d8c3de3c81/result.json` records **135/135 across eight Notify suites, 0 failures/errors/skips**, exit 0, exact clean `cd78c395`, 104-table owned MySQL/Redis, cleanup `[]`. `git diff cd78c395..3efc5862 -- backend release-artifacts frontend` is empty, so this is source-equivalent current-candidate evidence. The prior 134/135 failure at 43e5 remains historical and is not reclassified as pass.
- The latest `3efc5862` default `./mvnw test` did **not** pass. `/tmp/wta-t42/cleanr1-backend-default.json` exits 1; the admin module reports 713 tests, 2 failures and 1 error (242 skips up to that point). These are three real contract/test-sync gaps, not a green gate: `OssStorageReadinessArchitectureUnitTest:24-26` scans the entire `AbstractOssClientImpl` for `putObject(` even though the new bounded *upload* is outside the diagnostic read-only method; constrain the source assertion to diagnostic behavior while retaining the no-policy-write/no-bucket-create guard. `OssNotifyMigrationUnitTest:46` forbids the substring `client_pk` anywhere in `notify_intent`, inadvertently matching the new `attachment_actor_client_pk`; assert absence of the old bare column instead. `BusinessOssOwnerArchitectureUnitTest:39-45` correctly rejects two unregistered persistent carriers, `notify_intent_attachment.source_oss_id` and `.snapshot_oss_id`; register their real owner and lifecycle evidence in `test/resources/oss/business-oss-owners.json`, not in a bypass allowlist. Parent has registered the precise test/manifest write set in revision236 and delegated the fix. Until a fresh exact-source default gate confirms it, candidate **cannot be accepted**. The earlier common-openapi ClassFormatError from pre-existing `target` output was retained separately and clean recovery did not waive this new assertion failure.

Full-JAR HTTP authorization and quoted >2^53 attachment ID, invalid ID rejection, live default-SSE OpenAPI capture plus formal generated-contract check, frontend gates, full package/core/static and final clean/cleanup proof remain pending in this review. Numeric JSON coercion is a schema-outside observation rather than an AC requirement to reject; any accepted numeric request still needs exact source/actor/Client and unique-relation evidence. `COPY_UNKNOWN` recovery intentionally remains conservative and requires controlled human verification of the old PUT, remote bytes and references. These limits do not turn the three current default-gate failures into product defects, but they prevent an overall AC-042 pass at `3efc5862`.

## Fixed Dispatch04 addendum — `c980698b794fba9b02d0041207f57c7b5aa77bb7`

I independently reviewed the exact `3efc5862..c980698b` diff: **four test/manifest files, no production or SQL changes**, `git diff --check` exit 0. **Static verdict: all three default-gate sync gaps above are narrowly addressed; no new blocker found in this delta.** This is a source review, not a claim that the pending targeted run or a fresh default reactor passed.

- `OssStorageReadinessArchitectureUnitTest` now extracts the contiguous `diagnoseAccess(...)` through the next `bucketConfiguration()` declaration. In fixed `AbstractOssClientImpl`, this slice contains the diagnostic entry and its read-only helper methods; bounded upload is at a later method outside it. The positive check still requires policy/ACL reads and bodyless anonymous HTTP probe, and excludes object/bucket mutations. A new negative test injects each `putObject`, `deleteObject`, `putBucketPolicy`, `deleteBucketPolicy` and `createBucket` call into the diagnostic slice and requires the guard to fail. This retains the actual diagnostic no-write rule while permitting the unrelated bounded upload implementation in the same class.
- `OssNotifyMigrationUnitTest` now tests an actual DDL column boundary, positively requires `attachment_actor_client_pk`, and still forbids the bare `client_pk` column in `notify_intent` and `sys_oss_ref`. Its DML assertion uses identifier boundaries, so the new prefixed actor column no longer creates a false match while a standalone legacy `client_pk` still fails.
- `business-oss-owners.json` registers both persistent `notify_intent_attachment.source_oss_id` and `.snapshot_oss_id` as owned carriers with relation PK, owner class, lifecycle and the real Mail integration contract test. `BusinessOssOwnerArchitectureUnitTest` explicitly requires both coordinates and extends caller discovery to `bindNotificationSource`, `reserveNotificationSnapshot` and `releaseNotificationReferences` alongside `reconcileReferences`. Fixed-source caller search confirms the two registered Notify classes are the actual producers; no carrier/caller allowlist entry was added. Unknown future carrier/caller detection remains fail-closed.

The earlier `3efc5862` failed default result remains failed and preserved. Targeted three-class test and complete default/full results for `c980698b` must be read from their own fresh result/XML before changing the overall AC-042 verdict.

## Dispatch04 actual backend verification — later `c980698b` evidence

The pending targeted and backend gates now have fixed-source results. `/tmp/wta-t42/dispatch04-targeted.json` exited 0; its counts file identifies the exact three suites above with **8 tests, 0 failures/errors/skips** at clean `c980698b`/tree `b518062e`. I independently checked `/tmp/wta-t42/dispatch04r2-backend-default.json` (exit 0), its retained 265 fresh suite records and counts: **1,184 tests, 0 failures, 0 errors, 244 environment skips**, hence 940 executed passes. The three affected suites contribute 2+2+4 = 8 current methods with zero skips; `LoginUserAgentUnitTest` contributes 10 current methods with zero skips. Source before and after both record clean `c980698b`/tree `b518062e`. This closes the three default-gate sync gaps as executed tests, while the 244 environment skips remain skips rather than claimed passes.

An earlier same-source Dispatch04 default attempt failed with **9 failures confined to `LoginUserAgentUnitTest`** (`/tmp/wta-t42/dispatch04-backend-default.json` and `dispatch04-default-failure/counts.json`). It remains a failed attempt. After `clean`, Lead temporarily paused an identified shared editor Java language-server process during the exact-source default/full run, and `/tmp/wta-t42/dispatch04r2-jdt-exclusion.json` records `paused=true`, `resumed=true`, `build_exit_code=0`. This is a bounded build-output exclusion and recovery record; it does **not** prove that process authored any earlier class-file modification. No product or test assertion was weakened between the failed and successful runs.

`/tmp/wta-t42/dispatch04r2-full-package.json` records `./mvnw clean package -DskipTests` exit 0 and `/tmp/wta-t42/dispatch04r2-full-bundle.json` records the full bundle verifier exit 0. The package proof binds the clean source/tree to `wta-admin.jar` SHA-256 `d67d1a5dfbfa95655ed3450fb51302ba3f9462853a9474c2c13aead78357b54a`; this is build provenance, not a live HTTP result. Existing Mail27 and Notify135 real gates are reusable only by the explicit `/tmp/wta-t42/dispatch04-real-input-equivalence.json` boundary: Dispatch04 modifies unrelated architecture/migration tests and owner inventory, while Mail/Notify production, SQL and those selected suite sources are unchanged.

**Updated independent verdict:** no outstanding product-source or backend-default blocker found at fixed `c980698b`. Overall AC-042 remains **pending** until the exact-source full-JAR HTTP/authorization and quoted-ID checks, default-SSE OpenAPI capture/formal generation, frontend, core/static and final provenance/cleanup gates finish. The previous 3ef failure and first Dispatch04 failure stay historical failures; neither is rewritten as a pass.
