# T-41 G candidate — independent implementation-axis review

## Fixed source and static decision

G `c21de75f030688d274dd24e9d6075eff1bdbd5a6`, tree `fea078b0dfa5917dc4375311e9aa455f843560cd`; compare F `cb8063b686fcd82d0a91064061e674a59fb6fc14...G`. The sole frontend/backend delta is `frontend/e2e/inbox-paged-real.e2e.ts`. **Implementation-axis review passes: the static correction preserves assertions and G's required real 501 browser case now passes.** No application Java/Vue/TS, generated contract, Python runner or owned-resource cleanup code changed. I did not personally run tests, builds, browsers or services; actual runs below were executed by the Lead.

## F failure and G correction

F's actual owned browser result `/tmp/wta-t41/browser-runs/62047a08743c9ffa/result.json` was `exit1/acceptance=false` at the B shared title assertion, spec line 112:54. It had already shown A page 26/oldest and B login/unread2/two rows; source/JAR before and after matched F and cleanup errors were empty. The seed sets each message's title and summary to the same text, and `InboxPage.vue:22,29` renders them in distinct cells. A page-wide positive `getByText(title)` can therefore match both cells.

G replaces **both** page-wide B positive checks with a loop over the two manifest titles at current spec lines 112–116. Each title is used to filter actual `.el-table__body-wrapper tbody tr`, the filtered result must have count **exactly one**, and that row's **first `td`** must have the exact title text. Since the table's first column is `prop="title"`, this distinguishes title from summary and rejects absent, duplicate-row and wrong-title-cell results. It does not use `.first()` to select an arbitrary matching row: `.first()` applies only after the row count is proven one, to select the table's title column. The preceding two-row count, B unread2, A-only absence, and all later detail/read-all/no-push assertions are unchanged. The two generated titles are distinct for this run, so each filter has a unique intended row. G does not newly assert B row ordering; stable ordering remains covered by the existing backend/current tests, and the A page-26 real assertion remains in this same browser case.

## Prior evidence and exact reuse

`/tmp/wta-t41/g-source-equivalence.json` attributes earlier evidence to the source that actually ran: D backend default 870 executed/201 environmental skips, owned Notify HTTP 4, shared Notify 135 and frontend 751; C production dist 328 artifacts; E Python offline 24 and separate API-controlled Chrome 5/0/0/0; F core bundle/static/OpenAPI gates. D→E changed only two Python browser tool files, E→F and F→G changed only this real-501 Playwright spec. The C production dist was rehashed as 328 equivalent outputs for G. These are unchanged-input reuse statements, **not** claims that those suites were rerun on G. The E five-case preview source/config remains identical; a preview v3 is not needed for G's separate real-501 spec change.

## Actual G acceptance and remaining governance boundary

F's browser gate failed before the A full-detail/read-all, B-after-refresh and preserved prior timestamps checks; that failure remains part of the retained history. Lead's G result `/tmp/wta-t41/browser-runs/61b121b3b258cc3c/result.json` is an actual pass: source before/after equals clean G/tree above; six-SQL initializer exit 0; fresh full JAR before/after SHA-256 `488f4fe2a0b99e09ccd01090ec280e8bd326efa78764f645019619899f6154e3`. The JAR matches `/tmp/wta-t41/g-full-package-proof.json` (clean G source, `./mvnw -B -ntp clean package -DskipTests`, backend cwd, exit 0), and `g-full-bundle.json` exit 0. The Chrome case ran **1 expected, 0 unexpected, 0 skipped, 0 flaky** with one attempt, Playwright exit 0 and `acceptance=true`.

The same real result records A 501/unread481 and B 2/unread2 before browser actions, then A 501/unread0 and B 2/unread2 after. `preexisting_read_and_seen_timestamps_preserved=true` follows exact prior-timestamp snapshot comparison in the frozen runner; a passed single case also reached page 26, the A/B role/identity checks, full detail, top badge and zero-push assertion. Cleanup shows three process groups with no live members, no owned container IDs, both captured anonymous volumes absent, five loopback ports closed and `errors=[]`. The separately recorded G browser TypeScript lint exited 0. `/tmp/wta-t41/g-path-audit.json` binds 52 concrete product paths to all 24 declared scope entries (`all_in_declared_scope=true`).

This closes the implementation-axis and required real-browser evidence for G. Earlier D/C/E/F gates remain explicitly attributed to their actual sources as above; no prior failure is rewritten as a pass. Ticket status and change-level archival remain the Lead's governance decision, outside this read-only review.
