# T-03 当前候选只读审计：可考虑取消重复施工，尚未达到关闭证据

**固定产品点** `617a3693ddb2897851ab46d4131690898df6a690`；当前 T-03 历史实现 commit `214de538267a60fa527b1213f7dd9139622f4a2e` 非空且是此点祖先（`git merge-base --is-ancestor` exit 0）。输入为 Ticket T-03／AC-003、旧 `evidence/T-03.md` 及 `/tmp/wta-t02-t03-current-audit.md`。只读 `git show`、diff、现有清洗计数；本审计未运行 Maven、服务、HTTP、数据库或读私有凭据，也不更改票状态。

**当前判断**：固定源码未见需要再写一遍 T-03 核心产品实现的缺口。历史提交已实现每请求唯一有界原始正文、普通 JSON 与机器入口独立 2 MiB 配置、先捕获后业务／验签、原文签名与独立 XSS 视图、413、上传/SSE 不缓存。关键 `CapturedRequestBody.java`、`RepeatableFilter.java`、`RepeatedlyRequestWrapper.java`、`wta-common-openapi/src/main` 与 `RequestBodyHttpIntegrationTest.java` 对历史实现 commit 的 `git diff --name-only ...617a3693` 为空；`FilterConfig`／`XssFilter`／`XssHttpServletRequestWrapper` 的所见增量仅删作者注释。T-02 当前新增的 `SysLogFilter` 与 `GlobalExceptionHandler` 变更只处理日志路径副本，不改正文捕获或签名输入。若本点必需验证全部通过，可由 Lead 明确裁决“取消重复施工”，把 AC-003 当前合同证据交 T-30；不能把旧 `T-03.md` 的历史 review 或整批 result 当作当前 Done，也不能在本审计直接取消。

## AC-003 对固定源码的静态对应

- **前／等于／超限一字节，未知长度不能绕过**：`backend/wta-common/wta-common-core/src/main/java/org/namewta/common/core/http/CapturedRequestBody.java:39-69` 先拒声明长度大于预算；未知或低报长度每次最多读剩余额度 +1，达到 `maxBytes` 后下一字节抛 `RequestBodyTooLargeException`，完整捕获后才写请求属性。已存 owner 的下一观察者先按自己预算检查长度，`openStream:109-122` 仅新游标，`copy:101-106` 防御复制，日志只取前缀。`RepeatableFilter.java:27-50` 对 JSON/+json 在业务前捕获，超限直接 413；`OpenApiGatewayFilter.java:125-162` 在 authenticate 前构造 `ReplayableOpenApiRequest`，超限 413、未进入授权后业务。
- **原文签名、独立预算和过滤器顺序**：`ReplayableOpenApiRequest.java:18-34` 使用同一 owner，验签用独立防御性字节副本；`OpenApiAutoConfiguration.java:127-134` 的机器入口是 +1，普通 Repeatable 为 +2，日志与 XSS 后置。`XssHttpServletRequestWrapper.java:31-37` 只以 raw owner 生成独立清洗视图，不回写验签原文。`RequestBodyProperties.java:16-31` 与 `OpenApiStartupValidator.java:26-29` 校验预算；`application.yml:32-34,69-72` 普通及机器默认均为 2 MB 且各自可覆盖。
- **容量和可取消性**：`SysLogMediaTypePolicy` 排除 multipart／SSE 正文；`CapturedRequestBody` 缓存流明示仅同步 MVC，异步流本身不进入这个缓存。源码能证明捕获次数受入口 owner 约束，不能凭静态阅读推断生产峰值或目标容量。T-03 旧 `evidence/T-03.md` 有 512 MiB／8 并发×2 MiB 两轮、旧 HTTP 11／11 和旧模块 83／83；这些是旧输入历史而非本点通过。

## 当前可复用的真实执行事实与剩余最小验证

Lead 的 T-02 同候选消费者命令记录在 `/tmp/wta-t02-c1/acceptance-consumers.json`，cwd `backend`、命令 `JAVA_TOOL_OPTIONS=-Xmx512m MAVEN_OPTS=-Xmx1024m ./mvnw -Pdev -pl wta-admin -am -Dtest=LogSanitizerTest,LogAspectRedactionTest,SysLogFilterTest,SysLogFailureRedactionTest,SysLogEventWriterTest,OperationLogRedactionTest,SysLogConfigTest,RequestBodyLimitTest,RequestBodyHttpIntegrationTest,OpenApiAssemblyContextTest,BrowserHttpsTransportIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false -DforkCount=1 -DreuseForks=false -Dnamewta.body-probe.bytes=2097152 -Dnamewta.body-probe.concurrency=4 -Dnamewta.body-probe.expected-heap=536870912 -Dnamewta.body-probe.report=/tmp/wta-t02-c1/body-probe.json test`，退出 0。其日志当前显示 `RequestBodyHttpIntegrationTest` 11／11、`RequestBodyLimitTest` 9／9、`OpenApiAssemblyContextTest` 9／9、`SysLogConfigTest` 4／4，均零 fail/error/skip；11 个 HTTP 方法覆盖定长/chunked 预算边界、大正文假签名 413/小正文拒绝、逐字节 HMAC、独立机器预算、上传/SSE 取消与下一请求恢复。`/tmp/wta-t02-c1/body-probe.json` 实际 JVM 512 MiB、4 并发×2,097,152 字节、8 请求、日志前缀 32 字节，采样峰值 154,189,528 B、held 150,534,048 B、11 次 GC/47 ms；仅证明该隔离 JVM 包含 Jetty/Spring/客户端/签名/XSS的本次有界性，不外推生产容量。

**当前消费者差额**：同次 `BrowserHttpsTransportIntegrationTest` 1 skip（缺 `-Dbrowser.transport.integration=true`），虽然 Maven exit 0，该 HTTPS 消费者须由 Lead 依正确开关和 owned App 构建补跑 1／1 零 skip；不必为 T-03 重跑无关服务。现有消费者报告还需与同 SHA、clean 的运行前后源码点及进程/端口清理绑定，避免把 T-02 或历史结果跨 checkpoint 误认。T-03 的真实 HTTP/堆探针已可复用，不应再次执行同一负载只为产生第二份日志。默认全仓测试及模块门禁的新增证据核对见下方补记；full/core bundle 与最终跨票矩阵如已有当前交付证据可引用，或由 T-30 集成汇总。

### 当前同点全 reactor 门禁补记（Lead 证据，不重复测试）

Lead 的 `/tmp/wta-t02-c1/backend-default-test.json` 记录 cwd `backend`、命令 `./mvnw test`、exit 0；`backend-default-test-counts.json` 记录 head `617a3693`、新生成 Surefire XML 249 类／1000 tests／0 failure／0 error／150 环境 skip（850 实际执行通过）。按 XML 的**物理 module path** 分组：`wta-common-web` 9 类／54 tests／0 skip，`wta-common-openapi` 5 类／14 tests／0 skip；`wta-common-encrypt` 源树无 `src/test`，本次 reactor 日志明确 `No tests to run`，其资源、compile、testCompile、Surefire 阶段及模块结果 SUCCESS。三模块及它们的 `-am` 上游均包含在成功的完整 49 模块 reactor 里。因此 Ticket §8 的三模块 `-am test` 已被**同固定输入的完整 superset 命令**覆盖，没有必须重复执行的模块测试差额；不能把 encrypt 的零测试伪称正数或 required skip。admin 的 `RequestBodyHttpIntegrationTest` 11／11、`OpenApiAssemblyContextTest` 9／9 也在这份 fresh 报告中零 skip。全仓 150 个环境 skip 不等于 T-03 本票选集跳过；其中 Browser HTTPS 1 skip 仍需单独启用补跑，其他无关外部服务 skip 不作为 T-03 新工作。当前剩余仅是这项 HTTPS 消费者以及 Lead 将复用证据绑定到 T-03 当前 checkpoint/Goal 处置；不再建议另跑三模块命令。

**历史/状态边界**：旧 `T-03.md` 记载早期工作树 review、旧 83／83 和全仓 814 项中 97 外部条件 skip；Ticket 现识别真实非空 implementation commit，但旧 result `6c8764c...` 是整批旧出口，不能补造当时 clean exact-HEAD。T-03 `blocked_by: [T-02]`；先由 Lead 完成 T-02 零 skip 消费者和正式状态，再按本点可复用证据、模块门禁及 Goal 历史票处置规范决定是否取消仅重复施工。若新的真实检查出现故障，才据失败症状限定 T-03 已声明写集做修复；目前不建议新增产品改动。
