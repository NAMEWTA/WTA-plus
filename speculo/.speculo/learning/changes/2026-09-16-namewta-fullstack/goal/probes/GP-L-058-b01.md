---
artifact: learning-goal-probe
probe_id: GP-L-058-b01
lesson_id: L-058
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-058 / b01

审问对象是已写成的 Lesson `lessons/L-058-sso-web.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **四格**：`A:parseAuthorizeQuery`、`A:fetchSession`、`A:loginWithPassword`、`A:requestAuthorize`。OBJ-58 原文只点三名：`parseAuthorizeQuery` / `loginWithPassword` / `requestAuthorize`（**没有** `fetchSession`，**没有** `apiUrl`）。档是**解释+走查**。矩阵 (a) **一行五符号** `parseAuthorizeQuery, loginWithPassword, requestAuthorize, fetchSession, apiUrl`（模块栏印 `apps/sso-web`）**已经** `covered`（L-008 + GP-L-008-b01），备注「PKCE 细节 L-058」。矩阵 (a) **没有** `createSsoAuth` / `ssoLoginMethods` / `AuthorizePage` 独行，也没有 `@namewta/domain-sso`。矩阵 (b) **没有** `loginRequired === true` / 门脸 catch 不对称 / 空查询仍出门；SSO (b) 仍 uncovered 的是 `SsoOAuthController.authorize|token|revoke` 与 `SsoSessionController.login/logout`。矩阵 (c) `sso-web` 容器已 covered（L-004）。矩阵 (d) 已有「主路径 SSO 授权码」「失败路径 SSO PKCE」两行，仍 uncovered，备注 L-055 / L-056；**没有**浏览器认人厅菜单流。本批按派单只审问这四支枪在认人厅里的因果链，以及 PKCE 查询串如何作为不验的复印件穿过它们。口试按磁盘：抄写不验、未登录两扇窗、密码只换 HttpOnly 手环、授权 GET 要 JSON 再 `assign`，秘密留在厅堂。不把 L-008 五函数组合面再盖第二次章，不把 `apiUrl` 升成本课格子，也不把 L-055 五层 / L-056 换票 / L-057 Cookie 构造 / L-059 目录提前盖章。`apiUrl` 水管留给已 covered 的 L-008 行；厅堂 `createSsoAuth` 留给 L-008 / platform-auth 对照；`SsoOAuthController.authorize` 留给 L-055；`POST /sso/oauth2/token` 留给 L-056；`SsoSessionController` 三扇与 Redis 键留给 L-057；`SsoClientCatalog` 留给 L-059；厅堂 `/auth/login` 留给 L-014；路牌 `ssoAuthorizeUrl` 留给 L-011；`sso_secret` 信封留给 L-018。

抽查锚点（2026-09-17）：`frontend/apps/sso-web/src/{ssoApi.ts,ssoApi.test.ts,main.ts,views/AuthorizePage.vue}`；包 `package.json`；`AGENTS.md`；`.env.development` / `.env.production`；`vite.config.ts`；`frontend/packages/platform/auth/src/index.ts` `buildSsoAuthorizeUrl` / `createSsoAuth`；`apps/{admin,home}-web/src/application/sso.ts`；`AuthController.clientContext`；`SsoOAuthController.authorize|token`；`SsoAuthorizeVo`；`SsoSessionController.login|session|logout`；`SsoSessionCookie.create`；`SsoAuthorizationService.authorize` / `PkceS256`；`SsoLoginBo`；`e2e/sso-three-gates.spec.ts`；`frontend/packages/domains/` 无 sso 包；`web-domains/` 无 sso 包。

## Q1 澄清

- objective_id: OBJ-58
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-58 只三名；chain L-058 四格无 apiUrl；课 claimed_cells 四格；矩阵 (a) 一行五符号已 covered（L-008），备注 PKCE 细节 L-058；(b) 无本课符号；(c) sso-web 已 covered；(d) 授权码主路径 / PKCE 失败仍 uncovered 且备注 L-055/L-056；课开篇「口试名单就是矩阵 (a) 这一行四格，符号以磁盘为准」+ 负空间 L-008 / L-055…L-057 / L-059 / L-014 / L-011 / L-018
- difficulty: medium

课把浏览器认人这一头收成「保安亭四张纸条：抄胸牌、问手环、换手环、要纸条；秘密不进亭」。计划里至少有四套可数对象，派单点名的是 **chain 这四格 (a)**，它们在矩阵里却已经和 `apiUrl` 印在同一行且被 L-008 盖过章：

1. **OBJ-58 原文。** `course.md`：能口述 sso-web `parseAuthorizeQuery` / `loginWithPassword` / `requestAuthorize`。一句里是 **三支枪走查**，没有把 `fetchSession`、`apiUrl`、`createSsoAuth`、`SsoOAuthController.authorize` 或 `POST /sso/oauth2/token` 写成口试清单。前置 OBJ-55…OBJ-57。档是解释+走查，不是方法性状新行。
2. **chain / 课 `claimed_cells`。** 四格。磁盘符号就是这四个函数。chain **不**写 `apiUrl`，**不**写 `AuthorizePage.vue`，**不**写 `ssoLoginMethods`。课把 `fetchSession` 收进口试名单，并写明 `apiUrl` 是水管不是本课格子。
3. **矩阵 (a) 一行五符号已经 covered。** L-008 + GP-L-008-b01 把组合职责（谁喊谁、哪条 URL、带不带 Cookie、写不写业务 Token）标 covered，备注把 **PKCE 细节**留给本课。Lead 若按「四字又出现一次」把该行再盖一次，是重复章；若凭「已经 covered」宣布本课无对象，会把 OBJ-58 的走查深度（返回值、两扇未登录窗、门脸三枝不对称、复印件不持有 verifier）丢掉。`apiUrl` 在矩阵里、不在本课四格里。
4. **课自己的操作定义。** 本课认：蛇形抄驼峰且 `response_type` 缺省 `'code'`，不验 S256、不持有 `code_verifier` / `sso_secret`；`GET /sso/session` 只把 `code === 200` 收成 `boolean`，未登录是 `false` 不是 throw；`POST /sso/login` 身体只有 `{ username, password }`，成功 `void`，手环靠 `Set-Cookie`；`GET /sso/oauth2/authorize` 带回六键，`loginRequired` 必须 `=== true`，有 `redirectUri` 才 `window.location.assign`，不 302、不换票。页面 `onMounted` 先探测再授权；`submit` 先藏表再授权，两处 catch 不对称。PKCE 深度停在「厅堂写在 URL 上的挑战原样送到授权 GET，认人厅不能也不该持有 verifier」。
5. **邻格。** L-008 已认五函数组合面（含 `apiUrl` 空前缀）与「没有接线板」。L-055 验 Client / 精确回调 / 强制 S256 / 无用户早退 / 写码。L-056 厅堂 `POST /token` 与 revoke。L-057 Redis `sso:session:` 与 Cookie 名/TTL/HttpOnly。L-059 只经 `wta-api` 读用户与 Client。L-014 厅堂 `/auth/login` 写业务 Token。L-011 路牌 `webOrigin + "/authorize"`。L-018 信封密钥不属于查询串。

问：本格闭合标准是「四支枪的 URL/返回值/失败通道 + 两扇未登录窗 + PKCE 复印件 + 门脸三枝不对称，且不重盖 L-008 五行、不升 Vue 矩阵行、不提前盖 L-055…L-057」，还是 OBJ 那句必须先把 `fetchSession` 写进 `course.md`、或必须把矩阵该行改回 uncovered 才给走查盖章？Lead 若凭「L-008 已 covered」跳过 OBJ-58，是否把复印件深度和 catch 不对称假闭合进了组合课？

## Q2 证据

- objective_id: OBJ-58
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: ssoApi.ts 1–68；ssoApi.test.ts 4–33；AuthorizePage.vue 8 / 25 / 28–77；main.ts 6–13；package.json 2 / 19–21；AGENTS.md 3；.env.development 8–9；.env.production 6；vite.config.ts 15–18；platform-auth index.ts 51–113；admin/home application/sso.ts 4–32；AuthController 183–211；SsoOAuthController 55–88；SsoSessionController 51–87；SsoSessionCookie 23–30；SsoAuthorizationService 70–97；SsoOAuthCommands 23–40；SsoLoginBo 15–22；HttpStatus.ERROR=500；e2e sso-three-gates 62–114 / 135–148；sso-web 零 verifier / oauth2/token / logout / setToken / createSsoAuth；domains/ 与 web-domains/ 无 sso 包
- difficulty: high

课的四支枪 + 复印件哪些能在工作树直接复现，哪些只是测试名单比课表窄、或口试把邻格 SQL/Cookie 构造说成本课格子？

**仍能复现（本切片四枪 + 门脸 + 复印件）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 无 `@namewta/domain-sso` / `createSsoService`；无 web-domain-sso；无 `src/application/` | `frontend/packages/domains/` 与 `web-domains/` 目录无 sso；sso-web `src/` 只有 `ssoApi` + `views/AuthorizePage.vue` + `main.ts` |
| 包名 `@namewta/sso-web`；运行时依赖只有 `vue` 与 `vue-router` | `package.json` 2 / 19–21 |
| 三条路由 `/` `/authorize` `/login` 都挂同一张页；`createWebHistory()` | `main.ts` 6–13 |
| `parseAuthorizeQuery` 六字段全 `string`；蛇形进驼峰出；缺键 `?? ''`，唯 `response_type` 缺省 `'code'` | `ssoApi.ts` 3–21 |
| 结果对象**没有** `sso_secret`；测试锁 `not.toHaveProperty('sso_secret')` | 测试 27–32 |
| 不读 hash、不算 SHA-256、源码搜不到 `code_verifier` | `parseAuthorizeQuery` 12–22；sso-web 全树 `verifier` 零命中 |
| `fetchSession`：`GET apiUrl('/sso/session')` + `credentials: 'include'`；只 `body.code === 200`；返回 `boolean` | `ssoApi.ts` 33–37 |
| 后端无会话 `R.fail("未登录")`，业务码 `HttpStatus.ERROR=500`，于是函数得 `false` 不 throw | `SsoSessionController` 65–68；`R.fail(String)`；`HttpStatus` 82 |
| 成功体虽有 `SsoAuthenticatedUser(userId, username)`，页面不读、标题不变 | Controller 70–71；`AuthorizePage.vue` 无 `username` 展示绑定到来自身份接口 |
| `loginWithPassword`：`POST /sso/login`，JSON `{ username, password }`，`Content-Type: application/json`，带 Cookie | `ssoApi.ts` 39–45；测试 11–24 锁 `WTA`/`admin123` |
| `code !== 200` 才 `throw Error(msg \|\| '登录失败')`；成功 `void`；函数内无 `setToken` | 46–47；sso-web 零 `setToken` / `localStorage` |
| 后端 `R.ok()` 无 data；`writeCookie` → `Set-Cookie`；BO 只有 username/password | `SsoSessionController` 51–55；`SsoLoginBo` 15–22 |
| Cookie 默认名 `Sso-Token`；HttpOnly、`path=/`、`SameSite=Lax`；`create` **不** `.secure()` | `SsoProperties` 28；`SsoSessionCookie` 25–30；测试 17–24 |
| `requestAuthorize`：六个驼峰变回蛇形；`URLSearchParams` 编码；fetch 默认 GET；Cookie 照带 | `ssoApi.ts` 50–59 |
| `code !== 200` 抛「授权失败」；`loginRequired` 仅 `=== true`；`redirectUri` 可为 `undefined` | 63–67 |
| 空字符串也会进查询（`state=`、`code_challenge=`），六键都在 | 51–58 的对象字面量，空串仍 `set` |
| 三次 HTTP **都不**看 `response.ok`；`readJson` 即 `response.json()` | 29–31 / 34–36 / 40–47 / 59–63 |
| 页面只 import 四支枪，**不** import `apiUrl` / `ssoLoginMethods` | `AuthorizePage.vue` 25 |
| 初值：`needPassword=false`，`status='正在检查 SSO 会话…'`，用户名 `'WTA'`，密码空，`busy=false`；表 `v-if="needPassword"` | 8 / 28–32 |
| `onMounted`：true →「已有 SSO 会话…」→ `continueAuthorize`；false → 出示表；throw → 与 false 同一张脸，catch **不**读 `error.message` | 63–76 |
| `continueAuthorize`：从 `route.fullPath` 切片（带 `?`）或 `''`，**不是** `route.query` | 35 |
| `loginRequired` → 出示表并 return；有 `redirectUri` → `window.location.assign`；两者都没有 →「授权未返回回调地址」 | 37–46 |
| `submit`：`busy` 只包这一段；登录成功先 `needPassword=false` 再 `continueAuthorize`；失败 `status=error.message`，表还在 | 49–60 |
| 挂载路径授权 throw 被外层 catch 变成密码表；提交路径授权 throw 时表已藏，只改 status | 53–57 vs 63–76 |
| `ssoLoginMethods === ['password']`，测试不含 github/wechat；**不是**运行时 `if` | `ssoApi.ts` 1；测试 4–8；页面零 import |
| 后端有 `POST /sso/logout`；本 App 零 `logout` | Controller 82–87；sso-web 零命中 |
| 本包零 `oauth2/token`；换票在厅堂 `POST /sso/oauth2/token` + `code_verifier` | admin/home `sso.ts` 18–27；platform-auth `handleCallback` 105–109 |
| 厅堂 `startSsoLogin`：32 字节 verifier → SHA-256 BASE64URL challenge；`sessionStorage` 存 `{ clientId, redirectUri, state, verifier }`；URL 写死 `response_type=code` 与 `code_challenge_method=S256` | platform-auth 51–94 |
| pending 键互不相通：`namewta-admin-sso-pending` / `namewta-home-sso-pending` | 两份 `sso.ts` 第 4 行 |
| 路牌：Client 启用 SSO 且 `webOrigin` 非空 → `origin + "/authorize"`；默认 Origin `http://127.0.0.1:4176`；认人厅不读该配置 | `AuthController` 205–211；`SsoProperties` 23 |
| 授权门口仍 `R.ok(SsoAuthorizeVo)`，无 302；无用户 `needsLogin()` **不写码**；有用户才 insert 并把 code/state 拼进回调 | `SsoOAuthController` 67–70；`SsoAuthorizationService` 80–97；`AuthorizeResult.needsLogin` 29–30 |
| 强制 S256 / 精确回调发生在服务层，不在抄写函数 | `SsoAuthorizationService` 74–76 |
| `VITE_SSO_API=` 在 development **和** production 都是空串；dev 代理 `/sso` → `VITE_SSO_API_PROXY` 默认 `18080`，`changeOrigin: true` | `.env.development` 8–9；`.env.production` 6；`vite.config.ts` 15–18 |
| e2e：admin 先走认人厅；同一上下文 home 再点 SSO，密码框计数 0；两张业务票 `client_id` 不同，交叉 getInfo 不是 200 | `sso-three-gates.spec.ts` 62–76 / 102–114 / 135–148 |
| 对照厅堂密码登录：`/auth/login` 要 `clientId` + `grantType: 'password'`；认人厅这一枪没有 | `loginWithPassword` body 44；L-014 |

**课写过、测试名单更窄或口试标签写偏（不是证伪本格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「测试锁死抄写缺省 `'code'`」 | `ssoApi.test.ts` 只锁 `clientId='admin'` 与无 `sso_secret`。缺省 `'code'` / 空键 `''` 的证人是函数体 16–21，无单测 `toEqual` 六字段 |
| 「测试覆盖三支 HTTP」 | 只锁 `loginWithPassword` 的 URL/method/credentials/body。`fetchSession` / `requestAuthorize` / `apiUrl` **无** `describe`（课边界 7 已写） |
| 「Cookie 测试锁死不设 Secure」 | `SsoSessionCookieTest` 锁 HttpOnly / path / Lax，**没有** `isSecure()`。负空间证人是 `create` 25–30 无 `.secure()`，属 L-057 构造，不是本课单测 |
| 「`estimated_minutes: 38`」 | chain 该行估时 35。不是机制错误 |
| 口试把 e2e 夹具 `client_id=admin` 说成种子 Client | 单元测试查询串用过 `client_id=admin`；e2e 用十六进制 Client。课图边界已写 |

问：在四枪 URL/返回值、两扇未登录窗、密码不写 JS Token、授权不 302、verifier 零命中、空前缀 dev/prod 都成立、home 复用认人不共用业务票已经能互证的前提下，(a) 是否还要把「OBJ-58 漏写 fetchSession」「单测未锁 session/authorize」「chain 35 分钟 vs 课 38」写成开篇半句，才算本切片讲完？还是这些已经落在课的表/边界里，Lead 可以把本切片视为已讲、**保持**矩阵该行 covered（证据可追加本 probe），并把 PKCE 验算 / 换票 / Cookie 构造继续留给 L-055…L-057？

## Q3 假设与反例

- objective_id: OBJ-58
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 课反例 1–14 / 误区 1–16 / 边界 1–10；ssoApi.ts 12–67；AuthorizePage.vue 35–76；ssoApi.test.ts 4–32；platform-auth 74–109；SsoOAuthController 55–70；SsoSessionController 51–72；sso-web 零 verifier/token/logout；矩阵 (a) 该行已 covered；(d) 仍 uncovered
- difficulty: high

课的隐含假设若反转，合格读者会把哪段走廊认错？

1. **「`parseAuthorizeQuery` 会拒绝非 S256。」** 它不会。`plain` 也会抄进去。拒绝在 `PkceS256.requireS256`（L-055）。课反例 1。
2. **「`parseAuthorizeQuery` 读 `route.query`。」** 磁盘用 `route.fullPath` 切片。课误区 3。
3. **「`fetchSession` 返回用户对象 / 等于 getInfo。」** 返回 `boolean`。页面不读 `data.username`。课反例 2 / 误区 4。
4. **「未登录等于 `fetchSession` throw。」** 未登录是 `false`。throw 是运输层事故。课反例 3 / 正例 6。
5. **「`loginWithPassword` 往 `localStorage` 写 `Sso-Token` / 返回 token。」** 返回 `void`。钥匙是后端 Cookie。课反例 4 / 误区 5。
6. **「`loginWithPassword` 要带 `clientId` / `grantType: 'password'`。」** body 只有用户名密码。课反例 5。
7. **「`requestAuthorize` 就是换票。」** 换票是厅堂 `POST /sso/oauth2/token`。课反例 6 / 误区 6。
8. **「授权成功靠服务器 302。」** 靠 JSON `redirectUri` + `assign`。课反例 7 / 误区 12。
9. **「`loginRequired` 缺省 true / 就是 HTTP 401。」** 实现 `=== true`；它是 200 里的字段。课反例 8 / 误区 13。
10. **「`ssoLoginMethods` 在提交时拦住社交登录。」** 页面不 import。常量是测试钉。课反例 9 / 误区 8。
11. **「三条路由三张页。」** 一个组件。课反例 10。
12. **「在 sso-web 里 `createSsoAuth` 更干净。」** 会把 verifier 拖进认人厅。课反例 11。
13. **「空 `VITE_SSO_API` 是漏配。」** dev/prod 都是空串=同源 `/sso`。课反例 12。
14. **「认人厅也该有 logout 才算完整。」** 产品面今天就是四支枪。注销窗口在后端。课反例 13。
15. **「`AuthorizePage` 会校验 redirect 白名单。」** 白名单在 `requireExactRedirect`。页面有地址就 `assign`。课反例 14。
16. **「空查询串会在抄写这一关失败关闭。」** 抄出五个 `''` + `'code'` 仍 GET。失败在门口。课边界 1。
17. **「任何失败都会回到密码表。」** 登录成功后授权 throw，表已被藏。课边界 2。
18. **「`credentials: 'include'` 让 JS 读到 Cookie。」** 正好相反。课误区 7。
19. **「`apiUrl` 是 OBJ-58 声称格子。」** chain 四格不含它。课误区 9。
20. **「认人 Cookie 等于 Admin-Token / Home-Token。」** e2e 锁 Client 隔离。课误区 15 / 边界 9。
21. **「L-008 已经讲过这四个名字，本课可以只背口号。」** L-008 覆盖组合职责。本课覆盖返回值、两扇窗、复印件、catch 不对称。课误区 16。
22. **「OBJ-58 包含 `SsoOAuthController.authorize` 五层。」** OBJ-55。课误区 1。
23. **「矩阵要先有 (b) loginRequired 行 / 浏览器 (d) 才算讲完。」** 矩阵无这些行。它们是 (a) 的 explained。已有 (d) 是授权码→换票，仍归 L-055/L-056。
24. **「本课可以把矩阵该行从 L-008 抢走或改回 uncovered。」** 同一行五符号的组合面已 covered。本课是备注里的 PKCE/走查深度，不是第二张组合章。

问：读完后仍说不出的，是不是「四个函数名」，而是 **抄写不验、未登录有两扇窗、密码枪不写 JS Token、授权 GET 要 JSON 再 assign、verifier 永不进亭、提交后授权失败不会自动再出示表、空前缀不是漏配、L-008 的组合章不能代替本课走查**？

## Q4 视角

- objective_id: OBJ-58
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-58 vs L-008 / L-011 / L-014 / L-018 / L-055…L-057 / L-059；chain 四格 vs OBJ 三名 vs 矩阵五符号已 covered
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这四格 (a)**？

| 视角 | 多出来的边 | 是否本课这四格 |
| --- | --- | --- |
| 矩阵维护者 | 一行五符号已 covered（L-008）；本课四格无 `apiUrl`；OBJ 漏 `fetchSession`；无 Vue 行；(b) 无本课符号；(d) 授权码主路径仍 uncovered | **是** 闭合定义。本切片可视为已讲。不要改该行为 uncovered，不要发明 (b)/(d)，不要把 `AuthorizePage` 升成矩阵行才能给走查盖章 |
| 认人厅四枪 | URL/动词/body/返回值；`=== true`；不看 `response.ok`；空键仍出门 | **是** (a) |
| 门脸三枝 | `onMounted` / `submit` / `continueAuthorize`；catch 不对称；`busy` 只管提交；默认用户名 WTA | **是** OBJ 走查，不是矩阵第二格 |
| PKCE 复印件 | 挑战经查询串进 GET；verifier 留厅堂 `sessionStorage`；本包零命中 | **是** L-008 备注交给本课的深度。`PkceS256.verify` **否**（L-055） |
| L-008 组合课 | 五函数谁喊谁、有无接线板、`apiUrl` 空前缀、不换票 | 对照一句 **是**。再盖组合章 **否** |
| 授权门口（L-055） | Client 白名单、精确回调、强制 S256、无用户不写码 | 查询串被送过去、`loginRequired` 被读对 **是** 边界。五层 **否** |
| 换票（L-056） | `POST /token`、verifier 比对、revoke | `assign` 之后结束 **是** 负空间。换票 **否** |
| 域会话（L-057） | Redis 键、Cookie 名/TTL/不设 Secure | 登录枪不写 JS Token、探测把非 200 当 false **是**。构造细节 **否** |
| 目录（L-059） | `SsoClientCatalog` / `SsoIdentityService` | 本课不拆密码怎么进身份口 **是** 边界 |
| 厅堂密码（L-014） | `/auth/login` + session store | 两张表长得像、合同不同 **是** 对照。六枪 **否** |
| 路牌（L-011） | `ssoAuthorizeUrl = webOrigin + "/authorize"` | 人对上路径、亭子不读配置 **是**。context 全表 **否** |
| Client 信封（L-018） | `sso_secret` | 查询对象禁止出现 **是**。旋转密钥 **否** |
| 容器（L-004） | 独立 Origin、瘦依赖 | 包边界一句 **是**。C 格已 covered，不重盖 |
| (b) loginRequired / catch 不对称 | 矩阵无此行 | 本格 explained **是**。不要发明第二 (b) |
| (d) 主路径 SSO 授权码 | 仍 uncovered；备注 L-055, L-056 | ASCII 复印件图 **是** 本课图。不要用本课改 (d) 状态，也不要发明浏览器 (d) |
| Redis 8 / MySQL 8.4 | 会话键 / Client 行 | 不要因本课标容器 covered |

问：Lead 若凭「四函数名已在 L-008 那一行里」宣布 OBJ-58 无事可做，是否还要求读者能指着 **两扇未登录窗、`loginRequired === true`、提交后授权失败表已藏、verifier 零命中、assign 不 302**？这些是不是已经在本格机制链/反例里，而不需要 (b) 新行、也不需要把矩阵该行改回 uncovered？`apiUrl` 是否保持 L-008 的水管，不升本课第五格？`fetchSession` 是否已经是磁盘因果链的第一枪，OBJ 漏名只是半句精度？

## Q5 元问题

- objective_id: OBJ-58
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 一行五符号保持 covered；无本课 (b)；C:sso-web 已 covered；(d) SSO 两行仍 uncovered
- difficulty: low

建议：`defer`

原因：

- **chain 四格与课 claimed 1:1；与矩阵同一行（少 `apiUrl`）对象相同、深度不同。** L 合同章节齐全（宏观保安亭、直觉十二句、定义表、机制链 1–8、带 caption / 文字等价物 / 图边界的三张图、正反例、变式、误区、暂停走查、来源表 S-005/S-006/S-012/S-SSO-01…02/S-L058-01…06）。**主叙事站得住且与源码对齐**：抄写不验、探测把非 200 当 false、密码枪只 POST 用户名密码、授权 GET 读 `loginRequired === true` 再 `assign`、三次 fetch 带 Cookie 且不看 `response.ok`、verifier 不进亭、门脸三枝 catch 不对称、dev/prod 空前缀同源。不必为「函数名字」再挖五问。
- **Lead 不可以把矩阵该行改回 uncovered，也不可以只用本课再盖一次组合章。** GP-L-008 已把五函数组合面标 covered，并把 PKCE 细节交给本课。本切片证据可写入该行备注（「认人厅返回值 / 两扇未登录窗 / 复印件 / catch 不对称已讲」）。`apiUrl` 保持 L-008 水管。`C:sso-web` 不重盖。
- 本批可粘贴的精度项不挡住切片视为已讲：（1）OBJ-58 原文漏 `fetchSession`，磁盘因果链第一枪仍是它，课已 claimed；（2）`ssoApi.test.ts` 未锁 `fetchSession` / `requestAuthorize` / `apiUrl` / 缺省 `'code'`；（3）课 `estimated_minutes: 38` vs chain 35；（4）Cookie `Secure` 负空间的单测在 L-057，本课只认谁写下、谁读不到。不要把授权五层、换票、Redis 键、`SsoClientCatalog`、厅堂 `/auth/login` 塞进本格。改 OBJ 原文加第四名是 A/`course.md` 的半句，不是本课缺机制。
- 不建议 `mine-more`：b01 未证明这四格还有真实矩阵空洞；再五问只会把 L-055 `PkceS256`、L-056 token、L-057 Cookie 构造或 L-014 `/auth/login` 再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；可选半句（OBJ 漏名、单测未锁两支枪）不是第二容器。不建议 `split-lesson`：未满 10 问；四枪+一张页是 chain 原设计的同一 OBJ-58、同一认人厅容器，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b)/(d) 行：两扇窗与复印件图是 (a) 的 explained；已入库 (d) 仍归 L-055/L-056。不建议等 GP-L-055 才给本切片当已讲。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:parseAuthorizeQuery | 蛇形抄驼峰、缺省 `'code'`、不验 S256、无 secret/verifier，与源码对齐。残留：单测只锁 `clientId` 与无 `sso_secret`。 | 可选半句：口试缺省与空键以函数体为准。不要求重写机制链。 | defer（切片已讲） |
| A:fetchSession | `GET /sso/session`、`code === 200` 才 true、未登录 false 不 throw、丢弃 userId/username。OBJ-58 原文漏此名；课与 chain 已立格。无单测。 | 不要为漏名改矩阵。Lead 可将 OBJ 三名旁注「探测枪见 L-058」。单测缺口不挡走查。 | defer（本格可视为已讲；OBJ 漏名不挡） |
| A:loginWithPassword | POST `{ username, password }`、失败 throw、成功 void、Cookie 由浏览器收、无 clientId、无 setToken。测试锁 credentials。 | 保持。写入点一句已在课机制 3。 | defer（已讲） |
| A:requestAuthorize | GET 六键、`loginRequired === true`、有 `redirectUri` 才 assign、不 302、不换票、空键仍出门。无单测。 | 证人是函数体 + 页面。不要假装有 URL 单测。 | defer（已讲） |
| 矩阵 (a) 行 `parseAuthorizeQuery, loginWithPassword, requestAuthorize, fetchSession, apiUrl` | 组合面已 covered（L-008）。本课交付备注所要的 PKCE/走查深度。 | **保持 covered**。证据栏可追加本 probe / L-058。不要改回 uncovered，不要用本课再盖组合章。`apiUrl` 不升本课第五格。 | defer（行已 covered；备注可补 L-058） |
| A:sso-web.ssoApi / apiUrl | 水管：dev/prod 空串同源。本课机制 7 借用，claimed 不含。 | 保持 L-008。 | defer（L-008 已 covered） |
| C:sso-web | 独立 Origin、瘦依赖、三路由同一页。 | 不要因本课再改容器状态。 | defer（L-004 已 covered） |
| createSsoAuth / buildSsoAuthorizeUrl | 厅堂生成挑战与 pending。本课只认复印件如何被抄走。矩阵无此独行。 | 不要发明工厂行。对照保持。 | defer（未入库对照；L-008 / platform-auth） |
| ssoLoginMethods | 模块常量+测试钉，不是第四支枪。 | 不要立格。 | defer（未入库） |
| AuthorizePage.vue 三枝 | 课已走查 catch 不对称。矩阵无 Vue 符号。 | 不要升成矩阵行才盖章。 | defer（未入库对照） |
| A:SsoOAuthController.authorize / UseCase / D:fail-pkce | 门口验 S256、无用户不写码、有用户写码。本课只保证查询串送到、JSON 读对。 | 保持 uncovered 直到 L-055 挖掘。 | defer（L-055） |
| A:SsoOAuthController.token,revoke / B:exchange,revoke | 换票与 revoke 在厅堂 HTTP。本包零 `oauth2/token`。 | 保持。L-056。 | defer（L-056） |
| A:SsoSessionController.login,session,logout / Cookie 构造 | 本课认「谁写下、谁读不到」。TTL/Secure/Redis 键未当本格闭合。后端 logout 无前端枪。 | 保持。L-057。不要因本课标会话行 covered。 | defer（L-057） |
| A:SsoClientCatalog / SsoIdentityService | 密码如何进身份口。 | 保持。L-059。 | defer（L-059） |
| A:IdentityAccessService.login | 厅堂 `/auth/login` 对照。 | 保持。L-014 已 covered。 | defer（L-014 已 covered） |
| A:AuthController.clientContext | 路牌 `{origin}/authorize`。亭子不读。 | 保持。L-011 已 covered。 | defer（L-011 已 covered） |
| sys_client / sso_secret | 查询对象禁止出现密钥。 | 保持。L-018 已 covered。 | defer（L-018 已 covered） |
| (b) loginRequired 严格相等 / catch 不对称 / 空查询仍出门 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 主路径 SSO 授权码 / 失败路径 SSO PKCE | 仍 uncovered。备注 L-055, L-056。本课 ASCII 不是新的 (d)。 | 不要用本课改 (d)，也不要发明浏览器认人流。 | defer（L-055 / L-056） |
| C:Redis 8 / MySQL 8.4 | 会话键 / Client 行不在本格。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-084 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；这四格无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（矩阵同行已 covered，本课把 L-008 推迟的走查/复印件讲完；后端窗有自己的课）；合格读者说得出目的（认人厅把人认完再送回厅堂）、主路径（无环 → 密码 → 授权 GET → assign；有环 → 直接授权）与失败路径（探测 false 出示表、密码 throw 表还在、loginRequired 再出示表、提交后授权 throw 表已藏、无 redirectUri 停在亭里、plain/缺 Client 在门口失败不在抄写层）；把可选半句写进原课或 OBJ 旁注不会超过 40 分钟；L-055…L-057 / L-059 / L-008 / L-014 已有邻课。满 10 问前不拆。四枪虽跨抄写/探测/登录/授权四张纸条，仍是 chain 原设计的同一 OBJ-58、同一 sso-web 容器，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-008 已 covered 的五行组合面、L-004 容器、L-055…L-057 后端窗、L-059 目录、L-014/L-011/L-018 对照、未入库 (b)/Vue 符号以及 MySQL/Redis 容器显式建议 `defer`；对 chain 四格建议视为已讲切片；对矩阵 (a) 同行建议 **保持 covered**、证据可追加本 probe 与 L-058，**不要**改回 uncovered、**不要**用本课再盖组合章、**不要**把 `apiUrl` 升成本课第五格。本课不主张发明浏览器 (d)、把 `ssoLoginMethods` 立格、或等 GP-L-055 才给本切片当已讲。
