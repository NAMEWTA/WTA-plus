---
artifact: learning-goal-probe
probe_id: GP-L-023-b01
lesson_id: L-023
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-023 / b01

审问对象是已写成的 Lesson `lessons/L-023-sys-monitor.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（`claimed_cells` / 矩阵 (a) 四行）：`A:CacheController.getInfo`、`A:SysLoginInfoController.list, export, remove, clean, unlock`、`A:SysOperlogController.list, export, remove, clean`、`A:SysUserOnlineController.list, forceLogout, getInfo, remove`。OBJ-23：口述这四份 Controller。chain 覆盖列把后三份写成 `*`；磁盘公开映射与矩阵点名窗同为 1+5+4+4=14，没有 L-015/L-021 那种漏 `add`/`edit`。矩阵 (b) **没有** unlock/kick/clean/insert 行。矩阵 (c) Redis 8 / MySQL 8.4 仍 uncovered。`createMonitorService` 与 web-domain 四页留给 L-024；`PasswordAuthStrategy` / `online_tokens:` 生产行留给 L-013；人事 `SysUserController.unlock` 留给 L-015；配置/字典 `refreshCache` 留给 L-021；`NotificationMonitorController` 留给 notify 课；Sa-Token 键形留给 L-084；基座整库留给 L-085。

抽查锚点（2026-09-16）：`backend/wta-modules/wta-system/.../controller/monitor/{Cache,SysLoginInfo,SysOperlog,SysUserOnline}Controller.java`（目录正好四份）；`service/impl/{SysLoginInfo,SysOperLog}ServiceImpl.java`；空 `mapper/system/Sys{LoginInfo,OperLog}Mapper.xml`；`domain/{SysLoginInfo,SysOperLog,SysUserOnline}.java`；`wta-api/.../UserOnlineDTO.java`；`CacheNames`；`RedisUtils.keys`；`UserActionListener`；`UserLoginSuccessListener`；`LogAspect`；`Lock4jConfig`；`RepeatSubmit`；`SysUserController.unlock`；`ClientSessionService`；`Constants`；`PageResult.build`；`03-backend-module-modes.md`；`10-cde-base-ddl.sql` `sys_login_info`/`sys_oper_log`；`50-cde-base-dml.sql` 监控菜单；`frontend/packages/domains/system/src/monitor/index.ts`；`web-domains/system/src/monitor/login-info/LoginInfoPage.vue`。

## Q1 澄清

- objective_id: OBJ-23
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-23；chain L-023 覆盖列；L-023 `claimed_cells`；矩阵 (a) 四行点名窗；(b) 无本课符号；(c) Redis 8 / MySQL 8.4 uncovered；课开篇「口试名单就是矩阵 (a) 这四行」；Goal「范围内公开方法的目的都要在函数表里出现」
- difficulty: medium

课把「值班室四扇窗」收成 **四个 (a) 格子、一个 OBJ**。计划里至少有五套可数对象，课没有声明以哪一套当作闭合的操作定义：

1. **OBJ-23 原文。** `course.md`：能口述 `CacheController` / `SysLoginInfoController` / `SysOperlogController` / `SysUserOnlineController`。点名的是**四份类**（解释+走查），没有把 14 个 mapping 名写成口试清单，也没有点名失败句。
2. **Chain。** `A:CacheController.getInfo` + 后三份 `Controller.*`。Cache 不写星号。
3. **Lesson `claimed_cells` / 矩阵 (a) 四行。** 与磁盘公开映射同名：Cache 1、登录 5、操作 4、在线 4。默认档 Unistructural + explained。状态仍 `uncovered`。
4. **矩阵 (b)/(c)/(d)。** 无 unlock / `kickoutByTokenValue` / `lambda().delete` / `recordLoginInfo` 行。Redis 8 备注仍是「会话/锁/限流」。无监控数据流行。课却把事件落库、名牌扫描、错次键、切面回写写成机制主链。
5. **菜单种子里多出来的字。** `monitor:logininfo:query` / `monitor:operlog:query` / `monitor:online:query` / `monitor:online:batchLogout`，以及同一「系统监控」目录的 Admin / SnailJob / SnailAI / Nacos。课声明口试按磁盘、外链不是这四份 Java。

问：本课 (a) 闭合标准是「四份类还在 `controller/monitor`」，还是必须连 14 扇的 HTTP 动词、权限串、读的柜子、写点不在本 Controller 一起算进 OBJ-23？若 Lead 只凭「四份类在 system 房间」把四行标 covered，菜单多出来的 query/batchLogout 以及 Admin/SnailJob 窗，还算不算本格？(b) 若必须看见 `kickoutByTokenValue` / `cleanLoginInfo` / `LogAspect.handleLog` 函数体才算性状闭合，是否已经把 OBJ-23 扩成一张不存在的 Service/Redis 行？

## Q2 证据

- objective_id: OBJ-23
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: 四份 Controller 全文；两份 ServiceImpl 查询/清空/状态映射；`CacheController.getInfo` finally 还连接；`RedisUtils.keys` 573–592；`SysUserOnlineController` 49–76 / 89–94 / 102–117 / 129–139；`UserActionListener` 26–54；`UserLoginSuccessListener` 60–66；`LogAspect` 45–55 / 66–134；`Lock4jConfig` 27–28；`RepeatSubmit.interval` default 5000；`BaseController.toAjax`；`SysOperLogServiceImpl.buildQueryWrapper` 81–82；`PageResult.build(list)` 66–70；DDL 195 / 280；DML 53–54 / 72–73 / 122–133 / 899；`createMonitorService` 无 export；`LoginInfoPage.vue` 259–266
- difficulty: high

课的机制链哪些能在工作树直接复现，哪些被磁盘削薄或说满？

**仍能复现：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 目录正好四份 Java；四块门牌 `/monitor/cache`、`/monitor/loginInfo`、`/monitor/operlog`、`/monitor/online`；`wta-system` classic；无 UseCase/DAO | 四份 Controller；模块内零 `usecase/monitor`；`03-backend-module-modes.md` classic 行 |
| Cache **不**继承 `BaseController`；只有 `GET /monitor/cache`；权限 `monitor:cache:list`；无 `@Log`；内部 public record `CacheListInfoVo` | `CacheController` 24–66 行 |
| Cache 借 `RedissonConnectionFactory` 连接，读 `info("commandstats")` + `info()` + `dbSize()`，`cmdstat_` 削皮，`calls=` 与 `,usec` 之间当 value；`finally` `releaseConnection`；不走 `RedisUtils` / `CacheNames` | 同文件 36–55 行 |
| 登录五扇、操作四扇、在线四扇；矩阵点名窗 = 磁盘映射；无第五份类 | 三份 Controller 映射全表 |
| 登录/操作是 `Controller → ISys*Service → ServiceImpl → Mapper`；XML 只有 namespace；实体 `@TableName` 与 DDL 一致，**不是** `BaseEntity`，无 `@TableLogic` | 两份 Mapper XML；`SysLoginInfo` / `SysOperLog` 全文；DDL 195–224 / 280–295 |
| 登录表 `sys_login_info`（不是 `sys_logininfor`）；HTTP 驼峰 `loginInfo`；权限全小写 `monitor:logininfo:*` | Controller 33 行；DML 73 / 126–129 |
| 操作类名 `SysOperlogController`（log 的 L 小写）；服务/实体 `SysOperLog`；门牌 `/monitor/operlog` | 文件名 vs `ISysOperLogService` |
| list 默认 `orderByDesc(infoId/operId)`；export **POST** + `@Log EXPORT` + 不分页 + Excel 表名「登录日志」/「操作日志」+ `void` | ServiceImpl 121–123 / 63–65 / 147–149 / 119–121；Controller export |
| `remove` 权限 `:remove` + `@Log DELETE` + `toAjax(deleteByIds)`；`clean` **同一颗** `:remove` + `@Log CLEAN` + `@Lock4j` + `mapper.lambda().delete()` + `R.ok()`（不看行数） | 两份 Controller clean/remove；ServiceImpl `clean*` |
| `unlock` 是 **GET** `/unlock/{userName}`，权限 `monitor:logininfo:unlock`，`@Log OTHER` + `@RepeatSubmit`，拼 `pwd_err_cnt:` + 路径字符串，`hasKey` 才删，**零 SQL**；无键也 `R.ok()` | `SysLoginInfoController` 98–107；`CacheNames` 92 |
| 人事窗 `GET /system/user/unlock/{userId}`，权限 `system:user:edit`，先 `selectUserById`，无用户 `R.fail("用户不存在")`，再用档案姓名拼同一把键 | `SysUserController` 308–321 |
| 事件状态词 `Success`/`Logout`/`Register` → 表 `"0"`；`Error` → `"1"`；其它词不进分支 | `Constants` 38–63；`SysLoginInfoServiceImpl` 89–93 |
| `recordLoginInfo` / `recordOper` `@Async @EventListener`；Controller **不** insert；`selectOperLogById` / `insertOperlog` 无 HTTP | 两份 ServiceImpl；`ISysOperLogService` 55 行；登录接口无 byId |
| `LogAspect` URI 255、内容 3800；`proceed` 失败仍 `handleLog` 再抛；组装失败只 error，不改返回值 | `LogAspect` 45–55 / 66–76 / 131–134 |
| 在线无 Mapper、无表；`SysUserOnline` 无 `@TableName`；`UserOnlineDTO` 在 `wta-api`；名牌键 `online_tokens:` | 实体/DTO；`CacheNames` 87 |
| `list`：`RedisUtils.keys(ONLINE_TOKEN_KEY+"*")` → `getKeysStream` chunkSize 1000 再 **collect toList**；`substringAfterLast(:,)`；活性 `< -1` 跳过；与 `ClientSessionService` `>= -1` 同一把尺；IP/用户名内存 **equals AND**；`PageResult.build(list)` 的 total=`rows.size()` | `RedisUtils` 573–592；Online Controller 49–76；`ClientSessionService` 186；`PageResult` 66–70 |
| `getInfo`：不扫名牌，走 `getTokenValueListByLoginId`；无 `@SaCheckPermission`、无 `@Log` | Online 102–117 |
| `forceLogout`：`DELETE /{tokenId}` + `monitor:online:forceLogout` + `@Log FORCE` + `@RepeatSubmit`；`kickoutByTokenValue`；已失效吞 `NotLoginException` 仍 `R.ok()`；方法体**不** `deleteObject` | Online 85–94；`UserActionListener.doKickout` 43–44 |
| `remove`：`DELETE /myself/{tokenId}`，无权限注解；只在自己 token 列表 `ifPresent` 才踢；找不到仍 `R.ok()` | Online 126–139 |
| 菜单有 `*:query`、`monitor:online:batchLogout`；四份 Java 零引用。缓存页 C 菜单挂 `monitor:cache:list` | DML 54 / 122–133 |
| `RepeatSubmit.interval` default 5000；两份 `clean` 抢锁失败 `ServiceException(..., 503)`「业务处理中，请稍后再试...」 | `RepeatSubmit` 20；`Lock4jConfig` 27–28 |
| 名牌写入：timeout==-1 不设 TTL，否则 `Duration.ofSeconds(loginParameter.getTimeout())`；`doLogout`/`doKickout`/`doReplaced` 删键 | `UserLoginSuccessListener` 60–64；`UserActionListener` 34–53 |
| 错次 TTL 配置默认 `user.password.lockTime: 10` 分钟；本课 Controller 只删键 | `application.yml` 215；`SysLoginService` 247 |
| API-005 存量：变更窗是 DELETE（remove/clean/踢人）和 GET（unlock），不是 POST | 四份映射 |

**课写过、磁盘已削薄或说满：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「`Collections.reverse` 再 copy」易被听成按登录时间倒序 | **没有** `sort(loginTime)`。`keys()` 收集的是 scan 顺序，`reverse` 只是把这份 Collection 倒过来。口试若说「最新登录在最前」，磁盘不支持 |
| 「底层是 scan，不是 Redis `KEYS` 命令的字面」 | 命令层成立。`keys()` 仍 `collect(Collectors.toList())`，全站名牌键一次性进堆，再 `virtualSubmitAll`。不是流式分页。和「在线 total=size、无 PageQuery」是同一把内存风险，课只把 Excel 写成运维风险 |
| 「有这颗权限就能踢**名单上出现的**任意 token」 | `forceLogout` **不查名单**，直接 `kickoutByTokenValue(tokenId)`。知道 token 字符串就能踢，包括列表因无名牌而没画出来的活票。查名单的是 `myself` |
| 「条件含单个或数组 `businessType`」 | 单个字段：`.eq(businessType != null && businessType > 0, ...)`。`BusinessType.OTHER` 序数是 **0**，按「其它」筛等于没筛。数组 `businessTypes` 的 `in` 没有 `> 0` 闸 |
| 「前端工厂打的 URL 与本课 HTTP 对齐」（S-L023-08） | `createMonitorService` **没有** `loginInfo.export` / `operationLogs.export`。导出走页面 `runtime.download('monitor/loginInfo/export', ...)`（L-024）。14 扇里缺两扇工厂方法，不是「对齐」 |
| 「登录日志按钮含 list/query/remove/export/unlock」 | `monitor:logininfo:list` 挂在 **C 菜单**（登录日志页），不是 F 按钮。F 按钮是 query/remove/export/unlock。操作/在线/缓存同构。没有独立的 `:clean` 权限串 |
| 「连接或 INFO 失败会冒到全局异常处理，不是 `R.fail` 包一层业务码」 | `throws Exception` 走进 `GlobalExceptionHandler` 的 `Exception`/`RuntimeException`，信封仍是 `R.fail("发生未知异常…[错误编号]")`。不要说成「没有 R 外壳」；要说「没有业务码，是未知异常编号」 |
| 名牌与访客簿都「刚发生可能还看不见」 | 名牌在 `UserLoginSuccessListener` **同步** `setCacheObject`；访客簿/盖章簿才 `@Async`。刚进门：在线名单通常立刻有，登录日志可能还没有 |

问：在 14 扇 HTTP 形状、classic、Cache 只 INFO、unlock 零 SQL、clean 无 WHERE、myself 先过滤、batchLogout 无方法已经能互证的前提下，(a) 是否还要把 **`reverse` 说成时间序、`forceLogout` 说成「只能踢列表里的人」、`keys()` 说成已经分页、工厂说成含 export**，才算把四扇窗讲完？

## Q3 假设与反例

- objective_id: OBJ-23
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 四份映射全表；`forceLogout` 不查 keys；`businessType > 0`；`toAjax` vs `R.ok()`；`PageResult.build(list)`；菜单 query/batchLogout；`createMonitorService` 无 export；名牌同步 vs 日志异步；`SYS_CONFIG.refreshCache` 不在本课
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪只柜子认错？

1. **「这是 layered，监控应有 `MonitorUseCase`。」** 登记表 classic。在线逻辑停在 Controller。课反例 14 覆盖。
2. **「`CacheController` 还能 getNames/clear/FLUSHDB。」** 只有 `getInfo`。课反例 1 / 2 覆盖。
3. **「`GET /monitor/cache` 会列出 `pwd_err_cnt:` / `online_tokens:`。」** 读的是服务器 INFO。课反例 2 覆盖。
4. **「登录表叫 `sys_logininfor` / URL 全小写 `logininfo`。」** 表下划线、URL 驼峰 I、权限小写。课反例 3 / 4 覆盖。
5. **「`unlock` 会删登录行 / 是 POST / 会踢会话 / 会作废临时密码。」** GET、零 SQL、三只柜子。课反例 5–7 / 误区 4 覆盖。
6. **「清空登录日志等于给人开锁。」** clean 只撕 MySQL。课反例 7 覆盖。
7. **「`SysOperlogController` 有 `GET /{operId}`，因为 Service 有 `selectOperLogById`。」** 无映射。课反例 8 覆盖。
8. **「在线用户在表 `sys_user_online`。」** 无表。课反例 10 覆盖。
9. **「`GET /monitor/online` 要 `monitor:online:list`。」** 那是 `/list`。课反例 11 覆盖。
10. **「`DELETE /myself/{tokenId}` 也能踢别人。」** 先按 loginId 过滤。课反例 12 覆盖。
11. **「菜单有批量强退，所以有批量 API。」** 种子有串，Java 无方法。课反例 13 覆盖。
12. **「`clean` 之后操作日志表一定空。」** 方法自己带着 `@Log CLEAN`，异步可能立刻插回一行。课正例/变式 F 覆盖。
13. **「`NotificationMonitorController` / `createMonitorService` 属于 OBJ-23。」** 课反例 15 / 误区 1 覆盖。
14. **「两扇 `getInfo` 是同一个方法。」** Cache 仪表 vs 我的设备。课反例 16 覆盖。
15. **「导出是 GET，因为只读。」** 两份都是 POST + `@Log EXPORT`。课反例 17 覆盖。
16. **「`forceLogout` 只能踢列表里出现的 token。」** 方法体不查名单。课把「名单上出现」写成了限制，磁盘没有这道闸。
17. **「在线列表按 `loginTime` 倒序；`keys()` 已经分页。」** reverse≠时间序；scan 结果全量进堆；`total=size`。
18. **「按 businessType=0（其它）能筛操作日志。」** `> 0` 闸把 OTHER 挡掉。
19. **「`remove` 0 行和 `clean` 0 行都成功。」** `toAjax(0)` → `R.fail()`；`clean` 永远 `R.ok()`。
20. **「刚登录则名单和登录日志都可能还没有。」** 名牌同步、访客簿异步。
21. **「前端 `createMonitorService` 14 扇都打得到。」** 缺两扇 export；页面另走 `runtime.download`。
22. **「`unlock` 的 GET 已经符合 API-005。」** 存量偏差。课误区 16 覆盖。
23. **「配置刷新走 `CacheController`。」** L-021。课变式 G 覆盖。

问：读完后仍说不出的，是不是「四份类的名字」，而是 **14 扇各自读哪只柜子、`forceLogout` 要不要先在名单里、`reverse` 是不是时间序、remove 0 行和 clean 空表的信封差在哪、工厂到底有没有 export**？

## Q4 视角

- objective_id: OBJ-23
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)；OBJ-23 vs L-013/L-015/L-021/L-024/L-084/L-085；`NotificationMonitorController`；`createMonitorService`；API-005
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于这四行 (a) / OBJ-23？

| 视角 | 多出来的边 | 是否本课格子 |
| --- | --- | --- |
| 矩阵维护者 | 四行点名窗 = 磁盘 14 扇；chain `*` 与点名窗一致（无漏 add/edit）；(b) 无行；(c) Redis 8 / MySQL 仍 uncovered | **是** 闭合定义。不要求发明 (b) 就能标 (a) covered；不要把 Redis 8 整容器标 covered |
| 锅炉房（Cache） | 一扇 GET、还连接、只 INFO、无业务键、无 `@Log`、不继承 BaseController | **是** (a) 该行。`refreshCache` **否**（L-021） |
| 访客簿（登录日志） | 五扇；表 `sys_login_info`；unlock 零 SQL；clean 无 WHERE；事件词→`"0"`/`"1"` | **是** (a) 该行。事件如何组包 **否**（L-013 已认生产行，本课只留方向） |
| 盖章簿（操作日志） | 四扇；无详情 HTTP；clean+@Log 可能回写 CLEAN；`businessType==0` 筛不动 | **是** (a) 该行 + list 的筛选失败句。`LogAspect` 源码逐行 **否**（common-log；本课只需「撕本子会留下字」） |
| 名牌（在线） | 四扇；扫 Redis；两处无权限注解；kick 走 Sa-Token；监听器才删键 | **是** (a) 该行。键形/会话丢失整条 **否**（L-084） |
| 登录生产（L-013） | 写 `online_tokens:`、发 `LoginInfoEvent`、清错次 | 对接点一句 **是**。策略/验证码 **否** |
| 人事柜台（L-015） | 另一扇 unlock，按 userId | 对照表 **是**。用户 CRUD 全表 **否** |
| 配置柜（L-021） | `SysConfigController.refreshCache` | **否**。课已划出 |
| 前端厨房（L-024） | `createMonitorService`；页面 download export；外链 Admin/SnailJob/SnailAI/Nacos；`encodeURIComponent` | URL 对照一句 **可以**，且应改口「工厂缺 export」。工厂全表 / 页面 **否** |
| 通知监控 | `NotificationMonitorController.snapshot` | **否** |
| Redis 8 容器 / L-084 | INFO / 名牌 / 错次只是三把钥匙的名字 | 作为 14 扇读哪只柜子 **可以点名**。容器标 covered **否** |
| MySQL 8.4 / L-085 | 两张表列、无审计列、无逻辑删 | 本课用到的表名/主键一句 **是**。基座容器 **否** |
| API-005 | 存量 GET unlock、DELETE 变更 | **否**。本课盘点现状 |
| dao-mapper / L-083 | 空 XML、`BaseMapperPlus`、`lambda().delete()` | 持有关系一句 **是**。SQL 所有权课 **否** |

问：Lead 若凭「四份类在 system、14 个方法名都在函数表里」把四行 (a) 标 covered，是否还要求读者能指着 **Cache 不扫业务键、unlock 不改表、forceLogout 不查名单、clean 可能立刻回写、在线 total=size**？这些是不是已经（或应当）在函数表/反例里，而不需要 (b) 新行、也不需要把 Redis 8 标 covered？

## Q5 元问题

- objective_id: OBJ-23
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；四份类同一 `wta-system` 容器 `controller/monitor`
- difficulty: low

建议：`re-dispatch-L`

原因：

- 四格同属 OBJ-23、同一 C4 容器 `wta-system`。L 合同章节齐全（直觉、定义、机制链 1–4、带 caption / 文字等价物 / 图边界的两张 ASCII、正反例、变式、误区、来源表）。**主叙事站得住**：四份源码四块牌子；classic 无 UseCase；14 扇与矩阵点名窗一致；Cache 只 INFO 且还连接；登录/操作读 MySQL、写入在事件与切面；unlock 擦 `pwd_err_cnt:`；在线无表、踢人走 Sa-Token、监听器摘牌；菜单 query/batchLogout 不是 HTTP；两扇 getInfo、两扇 unlock、两扇踢人不要并成一词。不必为「窗口名字」再挖五问。
- 本批已有可粘贴的磁盘反例，属于**同一课的闭合定义与机制精度**，不是第二容器：① `Collections.reverse` 不是 `loginTime` 倒序，`keys()` 仍全量进堆；② `forceLogout` 不查在线名单；③ 操作日志单个 `businessType==0` 被 `> 0` 挡掉；④ `remove` 的 `toAjax(0)` 失败 vs `clean` 永远成功；⑤ 名牌同步、日志异步；⑥ S-L023-08「工厂对齐」不含两扇 export；⑦ Lock4j 失败是 503 文案，课只写了「避免两个人同时撕」。补丁应短：开篇加一张「点名窗 = 矩阵四行 = 磁盘 14 扇；失败句是这些窗的 explained 性状，不另开 (b)、不认 Redis 8」；在线链改口 reverse/全量 scan/`forceLogout` 不查名单；操作 list 补 `businessType>0`；remove/clean 信封对照半句；来源表把工厂改成「缺 export，页面 download」。不要把 `createMonitorService` 全表、`LogAspect` 逐行、Sa-Token 键形、基座整库塞进 38 分钟课。
- 不建议再开一批问：重复本批证据。`mine-more` 只允许在 b01 已证明**真实矩阵缺口且五问不够写清**时开 b02——本批缺口是同一 OBJ 的口吻精度，一行补丁可写进原课。不建议拆课：未满 10 问；四格是 chain 原设计（四份 monitor Controller），不是两个 C4 容器；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议把 (a) 整格推迟：14 扇在磁盘上，不是空符号。不建议发明 (b) 行：unlock/kick/clean/回写是 (a) 的解释到副作用。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:CacheController.getInfo | 一扇 GET、还连接、只 INFO、削皮 `calls=`、无 `@Log`、不继承 BaseController、不扫业务键，均与源码对齐。残留：失败信封是未知异常编号，不是「没有 R」；INFO 行若缺 `,usec` 则 pie `value` 为 null。 | 失败句改成「走进 `GlobalExceptionHandler`，`R.fail` + 错误编号」。不要把本格扩成 Redis 8。 | re-dispatch-L（半句） |
| A:SysLoginInfoController.list | 分页、默认 `infoId` 倒序、IP like / status eq / userName like / 时间 between、不走 Redis，已对齐。残留：刚登录可能还没有行（`@Async`），但名牌通常已在。 | 与在线 list 对照半句：访客簿异步、名牌同步。 | re-dispatch-L（半句） |
| A:SysLoginInfoController.export | POST、EXPORT、不分页、sheet「登录日志」、`void`，已对齐。工厂没有这扇。 | 本课只认后端。来源表不要写「工厂对齐」。 | re-dispatch-L（来源表）；全表 defer（L-024） |
| A:SysLoginInfoController.remove | DELETE ids、`:remove`、`toAjax`。残留：0 行 `R.fail()`，与 clean 不同。 | 正例钉 `toAjax(rows>0)`。 | re-dispatch-L |
| A:SysLoginInfoController.clean | 同权限 `:remove`、`@Lock4j`、无 WHERE、`R.ok()`。残留：抢锁 503 文案未写；`@Log` 写的是**操作日志**不是登录表。 | 加半句：锁失败「业务处理中…」503；回写落在 `sys_oper_log`。 | re-dispatch-L（半句） |
| A:SysLoginInfoController.unlock | GET、路径用户名、零 SQL、无键也 200、`@RepeatSubmit` 5000ms，已对齐。残留：重复提交走 `ServiceException` 默认 i18n，不是 503。 | 不要和 Lock4j 文案并成一词。人事对照表保持。 | re-dispatch-L（可选半句） |
| A:SysOperlogController.list | 分页、默认 `operId` 倒序、条件表大体对齐。缺口：单个 `businessType==0` 被 `> 0` 跳过。 | 反例钉 OTHER。数组 `businessTypes` 仍可 `in`。 | re-dispatch-L |
| A:SysOperlogController.export | 同登录 export，sheet「操作日志」。工厂无此扇。 | 同登录 export。 | re-dispatch-L（来源表）；defer（L-024） |
| A:SysOperlogController.remove | `toAjax(deleteByIds)`；自己带 `@Log DELETE`，异步可能再插一行。 | 保持「撕本子留字」。0 行信封同登录 remove。 | re-dispatch-L（半句） |
| A:SysOperlogController.clean | `@Lock4j` + `@Log CLEAN` + 无 WHERE + `R.ok()`。回写同一张表。 | 口试钉「HTTP 200 不等于下一毫秒仍为空」。503 同登录 clean。 | re-dispatch-L（已有，保持 + 锁失败） |
| A:SysUserOnlineController.list | 扫名牌、`< -1` 跳过、内存 equals、`total=size`，已对齐。缺口：`reverse`≠时间序；`keys()` 全量进堆；活票无名牌会被丢掉。 | 改口 scan 顺序倒转；补「`getCacheObject` 空则列表没有，token 仍可能活」。 | re-dispatch-L |
| A:SysUserOnlineController.forceLogout | 踢人走 Sa-Token、吞 NotLoginException、监听器删键，已对齐。缺口：课写成「名单上出现的」；方法体不查名单。 | 改成「有权限就能按 token 字符串踢」。 | re-dispatch-L |
| A:SysUserOnlineController.getInfo | 无权限注解、不扫 `online_tokens:*`、走当前 loginId 的 token 列表，已对齐。 | 保持两扇 getInfo 分名。 | — |
| A:SysUserOnlineController.remove | `/myself/`、无权限注解、列表里找不到仍 200，已对齐。 | 保持「200≠踢掉了」。 | — |
| 菜单 query / batchLogout / Admin / SnailJob / SnailAI / Nacos | query/batchLogout 无 HTTP 已讲。同一监控目录还有外链页；Nacos 权限是 `system:nacos:console`。 | 开篇负空间保持。外链全表 L-024。 | defer（L-024） |
| A:createMonitorService / web-domain monitor pages | 课借用 URL；工厂缺 export；页面 `runtime.download`。 | 改口一句。全表 L-024。 | defer（L-024） |
| B:IAuthStrategy / PasswordAuthStrategy / LoginInfoEvent 生产 | 对接点已画。 | 保持。 | defer（L-013） |
| A:SysUserController.unlock | 对照表已有。 | 保持。 | defer（L-015） |
| A:SysConfigController.refreshCache | 变式 G 已划。 | 保持。 | defer（L-021） |
| A:NotificationMonitorController | 反例 15 已划。 | 保持。 | defer（notify 课） |
| Redis 8 容器 / 失败路径会话丢失 | 三把钥匙已点名，未走 Sa-Token 键形。 | 失败句已够。 | defer（Redis 8 / L-084） |
| MySQL 8.4 / mysql-base | 两张表约束已点名。 | 保持。 | defer（L-085） |
| (b) unlock / kick / clean 写点 | 矩阵无此行。课已把副作用写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；缺口落在同一 OBJ-23 的闭合定义与机制精度（reverse/全量 scan、forceLogout 不查名单、businessType>0、toAjax vs clean、工厂缺 export），不是第二容器或讲不完的函数簇；把一张开篇表和四处反例写进原课不会超过 40 分钟；前端工厂、登录生产、人事 unlock、配置刷新、通知监控、Redis 容器已有后续课。满 10 问前不拆。四份 Controller 仍是同一 `wta-system` 容器。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-024 工厂/外链、L-013 生产行、L-015 人事 unlock、L-021 refreshCache、notify 监控、Redis 8 / L-084、L-085 基座以及未入库的 (b) 行显式建议 `defer`；对本课四行 (a) 建议 `re-dispatch-L`。Lead 可在补丁后将矩阵 (a) 四行标 `covered`（证据锚本 probe 与 L-023 来源表）。本课不主张改 Redis 容器、MySQL 基座、前端工厂全表或任何 (b)(c)(d) 格子。
