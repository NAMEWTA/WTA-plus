# T42 owned full-JAR HTTP/OpenAPI runtime review

Verdict: **the v5 owned HTTP/OpenAPI run passed its defined gate** at clean source `c980698b794fba9b02d0041207f57c7b5aa77bb7` (tree `b518062e41cdd42b9be37b53cb6ea110d61d1a40`) and full JAR SHA-256 `d67d1a5dfbfa95655ed3450fb51302ba3f9462853a9474c2c13aead78357b54a`. This is evidence for the HTTP attachment contract and live API capture, not for an actual outgoing email or frontend UI. I did not start services, build, or modify the repository in this review.

The first v4 run `90043be9be226f04` remains a failed checkpoint: its own result has `acceptance=false`, `exit_code=1`, a runner `RuntimeError` before HTTP attachment assertions, and no cleanup errors. It nevertheless captured HTTP 200 OpenAPI 438 paths/450 schemas, a clean unchanged c980 source, and the same unchanged JAR. The specific captcha mismatch is supported by the authentication source and earlier investigation; the sanitized v4 result alone records only the runner error type. Its raw OpenAPI bytes are retained separately and hashed independently; they must not be reclassified as v5 acceptance.

The v5 run `2a1672a910adffea` has `acceptance=true`, `exit_code=0`, `init_exit_code=0`, clean identical source identity before/after, unchanged full JAR before/after, matching package-proof SHA `2158c6feac3fcc27798c4640a295764c7100687a4c68781955da02b16f6f87d2`, and matching helper before/after hash. Its raw live `/v3/api-docs` response was HTTP 200 (512,478 bytes); the retained byte SHA matches `openapi_raw.raw_sha256`. Live validation found 438 paths, 450 schemas, zero missing paths/operations/schemas, optional `string[]` attachment IDs, required priority, and both SSE operations.

Authentication used the owned loopback overlay. GET `/auth/code` actually reported `captchaEnabled=false`; real admin and ordinary password logins both returned HTTP 200/application code 200. The runner rotated fresh owned user passwords, checked the ordinary user lacked submit permission and superadmin role, and never stored tokens in the result. This fixture configuration skips only captcha validation; password, user/Client, permission, and token paths remain live.

For the attachment contract, the runner made a real authorized upload and verified its 19-digit source OSS ID exceeded 2^53, its persisted uploader user/Client metadata, and an exact one-to-one `notify_intent_attachment` plus `sys_oss_ref` relation for the quoted decimal ID. The omitted and explicit empty-list submissions each persisted zero relations. Each submit had zero S3 request delta. Seven invalid string-ID cases returned application code 500 with zero DB/OSS change. This proves a failed-closed write boundary, not a precise client-facing 4xx validation response. No-token and a separately authenticated user without submit permission returned application codes 401 and 403 respectively (HTTP transport status 200), also with zero DB/OSS change. These are application-code assertions, not HTTP 401/403 claims.

The schema-external numeric JSON token was **accepted in this run**. The driver did not require rejection: it verified a fresh response ID, exactly one additional intent/relation/recipient/delivery/outbox/source reference, the exact uploaded source ID, actor user and Client, and no S3 request during submit. This demonstrates exact binding for that observed numeric input; the public generated schema remains `string[]`, so numeric acceptance is not a promised client contract. Final owned counts were four future intents, two attachment relations/source references, four recipients/deliveries/outboxes, zero due outboxes, and zero enabled MAIL/SMS accounts. The run did not attempt real SMTP delivery.

Cleanup records no errors, zero remaining captured owned containers, absent anonymous volumes, closed loopback ports, and no live backend process-group members. The v5 freeze file pins the runner SHA `5b4da905037f4ff82bf2d0a24f5da8269561fafbd93ef5300aaab857e40a7be7`; 10 offline safety checks passed. The v4 and v5 OpenAPI source byte hashes differ because the live owned server address/port differs; each retained source matches its own result's raw SHA. The manifest records source and retained hashes of every staged file. No full JAR, secret environment file, properties file, or raw credential file is copied here.
