# T42 Redisson store ordering: fixed-source review

**Verdict: no blocking source defect found in the two-path correction.** This is a static review of fixed red `2534405fe3a44bad72da458d65648e0d6de1eba2` and green `764820dd90a17e5c6329fca5b5330000058dd1c7`; I did not run Maven, Docker, Redis or the MAIL suite. The MAIL A3 run was pending during this review and remains a separate acceptance gate.

The red commit adds `NotifyIdempotencyAutoConfigurationUnitTest.shouldDiscoverRedissonFromActualAutoConfigurationOrder`. It imports the actual Boot 4 pair `NotifyAutoConfiguration` and `RedissonAutoConfigurationV4` through `AutoConfigurations.of(...)`. Its `BeanFactoryPostProcessor` runs after configuration parsing/condition evaluation: it marks definitions lazy, obtains the existing `redisson` definition and changes only its instance supplier to a mocked `RedissonClient`. It does **not** register a user `RedissonClient` definition before `@ConditionalOnBean` runs, so it cannot make the original missing-store condition pass. The class's older `withUserConfiguration` positive case and no-Redisson fail-closed case remain unchanged.

The red evidence `/tmp/wta-t42/store-order-red-source-and-counts.json` binds a clean `2534405f` tree before/after, exit 1 and exact fresh 3 tests/1 failure/0 error/0 skip. The failing XML identifies only the new real-order test and the missing production-store assertion; the two older cases passed. The green commit changes only the production `NotifyAutoConfiguration` annotation to `@AutoConfiguration(afterName = "org.redisson.spring.starter.RedissonAutoConfigurationV4")`. It leaves `@ConditionalOnBean(RedissonClient.class)`, `@ConditionalOnMissingBean(NotifyIdempotencyStore.class)`, the null-store fail-closed path, public types and bean factories intact. This after-edge names the actual V4 configuration in the local Redisson 4.6.1 Boot 4 starter and compiles without a direct starter-configuration type reference in common-notify.

Green evidence `/tmp/wta-t42/dispatch03-a3-targeted-source-and-counts.json` binds clean `764820dd` tree before/after, exit 0 and exact fresh 18 suites/113 tests/0 failures/0 errors/0 skips. Its Notify auto-configuration XML has 3/0/0/0, including the new real-order case. The after-edge addresses the observed full-context null store without introducing a no-Redis fallback. The test verifies store registration/type under the real auto-configuration order; it does not independently exercise Redis operations or all MAIL behavior. Those remain for Lead's isolated full-context MAIL run and its exact source/cleanup evidence.
