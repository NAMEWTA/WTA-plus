# T-41 Lead 恢复复盘与新批裁决

## 1. 失败模式与历史保留

完整批 B 05d0f34、C eff81f6、D d93eb1d 的三次失败及 d58fc3d blocked 状态保持不变；见 T-41-three-attempt-review-2026-09-23.md 和 recovery-diagnosis/attempt-limit-state.json。B 测试装配、C 缺实际宿主时序覆盖已由 D 修复。D 浏览器未启动，SQL 准备失败，不能视为 UI 失败或成功。

## 2. 根因和补充发现

Lead 在 clean d58fc3d 执行冻结诊断，run dac0c6bfd5c2159b。相同 MySQL8.4.9 和六 SQL 103 表中，用户名 A/B、Client 和 title LIKE 原查询均返回 ERROR1267/HY000。列为 utf8mb4_general_ci，CONVERT 表达式为 utf8mb4_0900_ai_ci；显式 COLLATE utf8mb4_general_ci 对照全部成功。随后两用户口令更新、角色有效权限、501/2 收件关系、502消息/503关系、20条既有read时间及另一用户时间戳基线全部通过。诊断 acceptance=false，仅证明准备阶段根因。before/after clean一致，进程/容器/卷/端口清理成功，无外部副作用。

另有独立静态阻断：runner 登录口令40字符超过实际 PasswordLoginBody 的 max30。它尚未造成 D 的观察失败；应单独生成24字符登录口令，基础设施口令仍40字符，不改生产校验。

## 3. 实质修改和恢复条件

Dispatch02 仅授权既有 frontend/e2e/ 范围内 runner 和对应离线测试：修正三处比较表达式（用户名复用两次）、分离登录口令生成/BCrypt校验、为 SQL 错误增加受控阶段与数字错误号/SQLSTATE诊断。禁止持久化 SQL、原始stderr、口令、token；保持精确源码/JAR、501条、权限/隔离、零skip和资源清理断言。先针对性离线测试，再非空工具修复commit、完整浏览器验收；不原样重跑D。

已有 D 后端/前端结果只能通过逐项输入等价证明复用，注明真实源SHA与skip。新候选重新full打包取得对应proof；实际501 Chrome、五项API受控Chrome、core及静态/生成合同核对仍必需。任何新失败保留证据后诊断，不自动重复。恢复批最多三次，旧三次永久保留。

## 4. 责任与派单

Lead负责治理、唯一提交/构建/服务与集成；cors_audit为唯一产品writer；ops_audit实现轴和legacy_audit安全轴只读审查。下发 evidence/dispatch-T-41-20260923-02.md 后恢复新批计数0；本记录不是验收通过。无push、部署、生产数据操作或归档。
