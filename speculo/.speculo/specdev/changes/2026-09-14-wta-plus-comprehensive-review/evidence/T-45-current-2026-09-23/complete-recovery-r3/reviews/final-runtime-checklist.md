# T45 最终运行证据核对（当前候选 09be6db；实际以新运行的精确 HEAD 为准）

本清单只核现有结果，不增加测试、工具或证据格式。`green04` 已记录 34 类/170 例/零 skip；当前 default/full 仍在运行，不能预记通过。每项先核运行时的 clean HEAD/tree、命令、退出码与原始结果文件；新候选若只有非后端输入变化，复用须有逐输入等价和原运行 SHA，不能把复用写成同 SHA 重跑。

- **受限 full-app/真实 UI**：只接受新批 `/tmp/wta-t45/ui-v5-runs/<run-id>/result.json` 的 `accepted=true`、精确两 Chrome case/零 failed、skip、flaky、retry。核其 `source` clean HEAD/tree、完整 full JAR hash/package proof、Admin 生产 dist 全库存 manifest 在运行前后同值；`cleanup_errors=[]`、三个 owned 容器/full ID、匿名卷、PGID、端口与私有文件全部收敛。`browser_fixture.before` 必须是 status/code200、total=rowcount（v4实测为7）、bad/public/private均在页、unknown=1；精确 owned 删除 rowcount1；`after` bad无、公私仍在、全策略0/2、总数恰减1。原 INVALID_ACCESS_POLICY HTTP 负例、普通用户403/匿名401以及安全日志 canary 仍通过。
- **真实诊断的证据范围**：v5 在同一 owned MinIO/受限 app identity 中承接原 A2 full HTTP 的访问、切换、权限、健康、OpenAPI 和诊断检查；对每次诊断核该桶恰 3 个签名只读请求（对象 HEAD、Policy GET、ACL GET）加 2 个匿名只读请求（HEAD、Range GET），跨桶/多余/写请求为零。Policy/ACL 403 仍为 UNKNOWN；public 单对象可读、private 单对象拒绝，不据此断言全桶安全或匿名写已禁止。v5 若通过，同批原 A2 检查只计一次；A2 历史通过只作来源对照。
- **两类真实 JUnit 兼容回归**：只接受 `/tmp/wta-t45/oss-two-runs/<run-id>/result.json` 的 `acceptance=true`、Maven exit0、精确 ReadinessMinio 与 AccessUrlMinio 两份本次 fresh XML，各 1 执行/0 failure/error/skip、properties marker 本 run、源码 clean HEAD/tree 前后同值、`cleanup.errors=[]` 与 owned 容器/卷/端口/进程收敛。两类以 owned bootstrap 身份验公共 URL、私有签名与匿名边界；Readiness 测试还含 owned 匿名 PUT=403 负控。该 PUT 是隔离兼容测试，**产品诊断本身只读**由上述受限 HTTP 五请求证明；两类不能替代 v5。
- **默认/full/core 与静态**：default Maven 以 fresh XML 统计执行、fail/error/skip，环境 skip 单列不计通过；full clean package/bundle exit0，记录完整 JAR 来源与 hash。core 要有 `core-package`、`core-bundle`、`core-artifact`、`source-before`/`source-after`，JAR 必需/禁用项照现有 bundle verifier；静态 `skill-facts`、`fullstack-facts`、`notify-layered`、`fm`、`handbooks` 各 exit0。前端适用完整门禁和三 App 生产 build 以当前源/产物清单核对，OpenAPI 来自实际 full-app 捕获并通过正式 fetch/generate/check；勿把 TypeScript 编译或 default 环境 skip 写作真实 MinIO 通过。
- **历史隔离与结论**：A1 公开 URL 失败、A2 前端类型失败、C3 旧文案、恢复 R1 row-count、R2 SQL 1267、旧两 JUnit 的 POLICY_READ 失败，各保留原时点 `accepted=false`/失败结果，不能追认。最终只根据固定新候选的实际通过结果及明确输入等价复用结论勾 AC-045；任何运行失败、清理残留、源/构件漂移或 required skip 均不勾。
