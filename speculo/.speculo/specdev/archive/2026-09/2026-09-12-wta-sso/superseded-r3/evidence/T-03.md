# Evidence T-03

- Goal: sso-web password-only; backend Set-Cookie session.
- Commands:
  - `pnpm --filter @namewta/sso-web test` exit 0 (login methods = password only)
  - `pnpm architecture:check` exit 0
- Code: `SsoSessionController` writes HttpOnly `Sso-Token`; `AuthorizePage.vue` has no Mask/GitHub.
- Residual: Cookie owner not observed in a live browser (launcher cannot reach 18080).
