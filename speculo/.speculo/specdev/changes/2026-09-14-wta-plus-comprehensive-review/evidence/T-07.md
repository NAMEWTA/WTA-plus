# Evidence: T-07 — SSO回调与恢复旅程

- Workspace：/srv/WTA-plus，main，单人串行、无子代理、无新worktree。
- Parent before：76dbbe84a34624234e379661a57b232529e34ed3。
- Implementation commit / result SHA：null / null，用户全change暂不提交；0 Done。
- 当前状态：review。本票行为与required浏览器选集已验证，Admin全量typecheck残余8条诊断仍为失败，责任归T-20，未冒充全仓通过。

## 实现合同

后端仍先精确匹配注册redirect，追加HTTP(S)/无userinfo/fragment校验，拒绝既有code/state/error/error_description/error_uri参数（含编码参数名）。使用URI组件构造器保留已有query的raw编码，仅编码新code/state一次；state只校验非空，原值保存和回传，不再trim。SQL主键、PKCE、TTL、Client与单次consume合同保持。

platform-auth接受经校验的App内returnTo；拒绝外域、协议相对、反斜杠、控制字符及编码路径分隔符。回调根据origin和规范context构建，覆盖/admin/与/home/。浏览器临时状态按unknown验证，在任何换票await前清除；错误state、重复响应参数、错Client/畸形Token拒绝。换票失败仅给固定结构化错误，保留安全returnTo，重新授权产生新state/verifier；不重试旧code。

两App的SSO token交换使用本地fetch接缝与10秒取消，携带目标Client头和原协议字段，避免全局Axios弹窗反射一次性凭据或盲目重试。callback先清地址栏code/state，成功写Token到既有session和userStore并恢复returnTo；失败呈现校验/过期/网络/换票分类与重新授权入口。重新授权重新读取当前Client配置，拒绝停用/local-only配置。SSO页提供授权重试，固定错误文案不回显后端原始msg，提交后清密码。现有本地密码登录、系统授权和菜单恢复机制未改。

## 红绿与回归

| 证据 | exit | 实际结果 |
|---|---|---|
| T-07-callback-red.json | 1 | 原后端7项中1失败1错误：复杂state产生非法URI，fragment注册回调未拒绝 |
| T-07-callback-green.json | 0 | 原负向与新增opaque state/query/保留参数共7项通过 |
| T-07-platform-red.json | 1 | 新增11项复现旧状态残留、外域returnTo、冲突参数与错误Client问题 |
| T-07-platform-green-1.json | 混合 | 15项通过/typecheck通过；控制字符正则触发lint，已改显式字符检查 |
| T-07-frontend-1.json / T-07-frontend-2.json | 见各项 | 最终platform-auth17、SSO5、Admin65、Home4，共91项Vitest通过；Home旧固定/profile断言按安全returnTo合同更新，浏览器另验真实行为 |
| T-07-builds.json / T-07-home-build-2.json | 0 | 三App生产模式、隔离API/context配置构建通过；Home复核重建通过 |
| T-07-journey-1.json | 1 | Admin6通过，Home6因测试菜单字段误用componentKey失败；保留失败证据 |
| T-07-journey-2.json | 0 | 33项Maven零跳过；12个真实Chrome旅程全部通过，MySQL最终12行consumed且签发计数12 |
| T-07-cookie-regression.json | 0 | 33项Maven零跳过及T-06两个HTTPS/dev HTTP Cookie/CORS/PKCE场景通过 |
| T-07-reactor.json | 0 | common-web、SSO及依赖完整83项，0失败/错误/跳过，含真实Redis租约回归 |
| T-07-gates.json | 全部0 | E2E独立strict TS、E2E/两App lint、前端36包架构、SSO32文件分层及diff检查 |

完整命令/cwd/退出码见JSON。新E2E使用`playwright.sso.config.ts --grep T-07`，由run-t06-sso.py --journey启动并清理隔离服务。原three-gates/admin-config测试保留，但包含完整System登录/管理写入，尚未在该fixture执行，属于T-30真实整合验收；未连接其默认18080环境。

每App6个场景：登录入口构造带base的注册callback并恢复query/hash；手工输入opaque state的真实SSO页面往返；注入测试时钟推进致授权码过期后重授权；网络换票失败和刷新均不重放旧code；错误verifier由真实SSO拒绝后恢复；错误state在前端换票前拒绝后恢复。失败后pending与Token为空、地址栏无code/state；再次授权的state/challenge指纹不同。系统身份/菜单是Playwright显式fixture，密码校验与业务Token签发是Java测试端口；实际SSO Controller方法/UseCase/Service、Redis会话、MySQL DAO/Mapper和三App构建产物真实执行。未启动完整Spring MVC/LogAspect，也未宣称真实System密码与Sa-Token签发/权限管理E2E。

首次Home失败追到真实ServerMenuNode的component字段后只修测试fixture，未放宽菜单失败关闭。过程中曾误判Home createReplacement丢hash；复核实际源码已带hash，未修改该文件，判断已撤回。旧失败日志中的独占fixture OAuth query值已脱敏，断言/堆栈/结果保留；当前fixture日志在写入前脱敏且不录trace。三轮本票MySQL/Redis共6容器及额外模块回归1个Redis均清理，未触碰现有服务或在线凭据。

## 未闭合的整体验收

Admin完整typecheck仍退出2：Notify ConfigPage的6条DefaultRow槽位类型诊断及System SsoAppPage的2条行类型/ID诊断。两页面字节与HEAD一致，当前改动SSO路径诊断为0，详情T-07-admin-type-diagnostics.json；本票自身SHA BufferSource类型错误已修复，Home/SSO/platform-auth及E2E严格类型检查通过。不得删除断言、放宽类型或忽略此8条错误。后端此前670项消费者选集旧Notify DDL OSS载体错误仍归T-22。

构建采用测试环境显式VITE_APP_ENCRYPT=false、/api与/admin/或/home/，不等同于发布候选；未部署，生产共享私钥问题仍由T-11负责。完整System/发布/双bundle回归由其责任票与T-30完成。共享源码的当前hash见T-07-checkpoint.json，前置T-06检查点及修改前校验见T-07-input-checkpoint.json；所有正式commit/result字段仍空。

## Skill Execution Records

```json
[
  {
    "id": "engineering-standards",
    "phase": "implement",
    "operation": "apply-scope-contract",
    "sha256": "dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9",
    "status": "passed",
    "evidence": [
      "保留精确redirect/PKCE/Client/消费；URI只编码新参数，state不trim；不改业务身份授权或菜单恢复链"
    ]
  },
  {
    "id": "namewta-fullstack-development",
    "phase": "implement",
    "operation": "apply-scope-contract",
    "sha256": "675c053c11d8b22cd394c875f48688242d8e8328dd14e69657f5d5b12f2af68b",
    "status": "passed",
    "evidence": [
      "两App callback、当前context/returnTo、临时PKCE先消费与SSO授权页恢复同批实现；真实菜单协议按当前源码纠正fixture"
    ]
  },
  {
    "id": "engineering-standards",
    "phase": "verify",
    "operation": "verify-affected-contract-and-quality-gates",
    "sha256": "dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9",
    "status": "passed",
    "evidence": [
      "83项模块、33项定向及12个真实Chrome旅程、T-06两场景回归；91项前端单测；Admin剩8条未改页面类型诊断归T-20保留失败"
    ]
  }
]
```

## 后续验证交接

T-09已修复这里记录的八条旧页面类型诊断和旧Notify DDL载体问题；最新三App类型/lint与默认673项Maven见T-09.md。本文件以上结果仍表示T-07当时事实，不回写原失败记录。
