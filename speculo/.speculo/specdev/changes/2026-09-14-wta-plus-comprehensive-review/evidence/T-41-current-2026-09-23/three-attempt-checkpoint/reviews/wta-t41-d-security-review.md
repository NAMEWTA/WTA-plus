# T41 D fixed-source security review and acceptance checkpoint

**Fixed D:** `d93eb1d8d6095464a9606619f4161b3dd4ef35c7`, tree `668f14e67b3e21ac91f76e7c9b64e224ae29e3f5`. **Parent C:** `eff81f642f08a1590f85a1f1f813a592a8232451`. Review scope is the D increment plus unchanged B/C security/session source conclusions in `/tmp/wta-t41-b-security-review.md` and `/tmp/wta-t41-c-security-review.md`. Read-only fixed Git objects and existing safe evidence only; no tests, builds, services or repo changes were performed by this reviewer.

**Static security verdict: PASS for D's increment and inherited B/C session/permission source. Overall T41 acceptance remains PENDING** the real Chrome 501 scenario, core and the five API-controlled cases Lead identified. No pending gate is silently replaced by a different test count.

`git diff C..D -- frontend backend` reports exactly one changed file: `frontend/apps/admin-web/src/router/adminManifestRegistry.test.ts`. The added test at :73-102 captures the **actual Admin host** `inboxSession` port through `createNotifyWebDomain` and drives a reactive user from loaded A, through generation-only pending logout while old token/userId remain, to unloaded then loaded B. It asserts monotonic epoch, inactive suspended/logout and unloaded states, reactivation only after B identity loads, and snapshot text free of the token canary. Its mock of `useUserStore` remains confined to test setup; it calls the actual `createNotifyWebDomain`, not a reimplemented session algorithm. The test is deliberately independent of a particular initial epoch. It strengthens the precise pre-candidate race regression without relaxing B/C product guards. Production frontend, backend, generated API, SQL and build config did not change between C and D. `/tmp/wta-t41/d-source-equivalence.json` records A3 backend production/build equivalence, unchanged generated inputs, and the single test-only difference from C's frontend build input; `/tmp/wta-t41/d-path-audit.json` reports all product files in declared scope. The B security source reasoning is therefore inherited byte-for-byte.

Existing evidence read at this checkpoint:

- `/tmp/wta-t41/d-frontend-{architecture,lint,typecheck,test}.json` each records exit 0. `/tmp/wta-t41/d-frontend-counts.json` binds D commit and reports 643 Vitest + 108 Node tests = **751 executed, 0 failed, 0 skipped**. The prior B frontend test failure remains a failed historical attempt; D is its own clean run. `/tmp/wta-t41/d-frontend-build-reuse.json` explicitly reuses the completed C production builds because D changed only a test fixture; 328 artifact hashes were checked, with no claim of rerunning builds at D.
- `/tmp/wta-t41/d-backend-default.json` records `./mvnw test` exit 0. `/tmp/wta-t41/d-backend-default-counts.json` reports **258 fresh XML classes, 1071 tests = 870 executed + 201 environment skips, 0 failures/errors**. Environment skips are not treated as passing runtime E2E. `/tmp/wta-t41/d-full-package-proof.json` binds the actual D clean `./mvnw -B -ntp clean package -DskipTests` exit 0 to a 214186517-byte JAR, SHA256 `97f1e73bdbaedd6f9f409b30a002ec702c240b31c16d4ee9e175f27a72f10e47`.
- `/tmp/wta-t41/runs/52a850c3db470a58/result.json` records the **exact D** owned MySQL/Redis `NotifyInboxPagingIntegrationTest` run: 4 tests, 0 failures/errors/skips, Maven exit 0, source before/after D clean, retained XML and cleanup errors empty. It covers owner page 501, detail/read-all, orphan/large page and first-interaction times.
- `/tmp/wta-t50/runs/675004b2d820e928/result.json` records the exact D eight-class MySQL/Redis regression: **135 tests, 0 failures/errors/skips**, Maven exit 0, source before/after D clean, owned containers/volume/ports/process group cleared. It supports unchanged T36–T39/T50 backend behavior; it does not substitute for the T41 browser or five API-controlled cases.

**Still required before whole-ticket completion:** the in-progress real Admin Chrome 501/two-user browser runner result with source-before/after and owned resource cleanup; Lead's remaining core and five API-controlled cases (their exact class counts and zero-skip status once available); final generated-contract/provenance and any whole-change gates assigned to Lead. This report does not infer those results from the green frontend or database suites. No new blocking security defect was found in D's one-file increment.
