---
artifact: learning-goal-probe
probe_id: GP-L-072-b01
lesson_id: L-072
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-072 / b01

审问对象是已写成的 Lesson `lessons/L-072-test-leave.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain 覆盖列 **一格** `A:TestLeaveController.list,export,get,add,submitAndFlowStart,edit,remove`；课 `claimed_cells` **三格**，另加 `B:TestLeaveController.submitAndFlowStart` 与 `D:主路径 工作流请假`）：OBJ-72 原文：能口述 `TestLeaveController` 含 `submitAndFlowStart`。档是**解释+走查**。矩阵 (a) 一行把七个名字写成 `list, export, get, add, submitAndFlowStart, edit, remove`，模块 `wta-workflow`，备注「示例流」，仍 `uncovered`；磁盘详情方法是 **`getInfo`**，路径 `GET /{id}`。矩阵 (b) 独行 `TestLeaveController.submitAndFlowStart`，口号「写请假并发起流程」，仍 `uncovered`。矩阵 (d) 「主路径 工作流请假」原文 `TestLeave.submitAndFlowStart → 任务完成`，备注 **L-072, L-070**，仍 `uncovered`。chain **未**把 (b)(d) 写成覆盖列。矩阵 (d) 「失败路径 工作流驳回/终止」已 covered（L-070 + GP-L-070-b01），**不是**本课格子。矩阵 (c) `wta-modules` 业务房间 / `layered-classic` 已 covered（L-002/L-003）；登记表 classic 行就是 `wta-modules/wta-workflow`。MySQL 8.4 / `dao-mapper-ownership` 仍 uncovered。本批按派单审问 **(a) 七扇 HTTP + (b) 写单并发流 + (d) 请假主走廊（申请人首枪办掉、假条 waiting）**。分类树留给 L-067；图纸发布/导入留给 L-068；实例十三扇与 `WorkflowService.*` 整份合同留给 L-069；任务枢纽十六扇与驳回/终止失败留给 L-070（GP-L-070 已把本 (d) 明确让出）；SpEL 目录留给 L-071；厨房 `createWorkflowDefinitionService` / 菜单 `createWorkflowWebDomain` 留给 L-073。不要把 `ITestLeaveService` 升成第二张 (a)，不要把 `eval` 升成第八扇 HTTP 或 L-071 目录行，不要把页面「提交审批」说成本课 (b)，也不要用本课去盖任务窗 `completeTask` 或档案网关 `startCompleteTask`。

抽查锚点（2026-09-17）：`backend/wta-modules/wta-workflow/src/main/java/org/namewta/workflow/controller/TestLeaveController.java`；`service/ITestLeaveService.java`；`service/impl/TestLeaveServiceImpl.java`；`domain/{TestLeave,bo/TestLeaveBo,vo/TestLeaveVo}.java`；`mapper/TestLeaveMapper.java` 与空 XML；`common/ConditionalOnEnable.java`；`service/impl/WorkflowServiceImpl.java`；`liteflow/start/StartExecuteComponent.java`；`liteflow/complete/{CompletePrepare,CompleteExecute}Component.java`；`common/constant/FlowConstant.java`；`common/enums/MessageTypeEnum.java`；`wta-common-core/.../BusinessStatusEnum.java`；`wta-common-redis/.../RepeatSubmit.java`；`wta-common-web/.../BaseController.java`；`wta-api/.../WorkflowService.java`；登记表 `03-backend-module-modes.md` classic 行；DDL/菜单 `release-artifacts/docker/infrastructure/mysql/init/30-cde-workflow.sql`；图纸 `release-artifacts/workflow/leave/{leave1,leave2,leave6}.json`；厨房 `frontend/packages/domains/workflow/src/{index.ts,index.test.ts,leave/index.ts}`；页 `LeaveEditPage.vue` / `LeaveListPage.vue` / `runtime-actions.ts`；e2e `frontend/e2e/workflow-runtime.spec.ts`；对照（格子归邻课）`SpringPersonWorkflowGateway.start`（L-034）、`FlwTaskController`（L-070）、厨房工厂（L-073）。`controller/` 下 **零** UseCase。工作树 **零** `TestLeave*Test` / `TestLeaveController` HTTP 合同测试，**零** `submitAndFlowStart` 行为单测。e2e 请假加单测的是「提交审批」+ `startWorkFlow`，**不是**「后端发起」。

## Q1 澄清

- objective_id: OBJ-72
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-72；chain L-072 覆盖列只 A 七名；L-072 `claimed_cells` 三格 A+B+D；矩阵 (a) 七名一行仍 uncovered、`get`≠`getInfo`；(b) 提交枪仍 uncovered；(d) 请假主路径挂 L-072 与 L-070 但 GP-L-070 不认；负空间 L-067/L-068/L-069/L-070/L-071/L-073；(c) 房间已 covered
- difficulty: medium

课把请假样例收成「一块 `/workflow/leave` 牌子：七扇窗，一枪写单并发流，主走廊把申请人那一格代盖掉」。计划里至少有六套可数对象，派单点名的是 **一格 (a) + 一格 (b) + 一条主路径 (d)**：

1. **OBJ-72 原文。** `course.md`：能口述 `TestLeaveController` 含 `submitAndFlowStart`。档是解释+走查。一句里是 **柜台类名 + 那一枪方法名**，没有把七扇逐个写成 OBJ 清单，也没有写出「主路径 工作流请假」这半句，更没有把 `WorkflowService.*`、`FlwTaskController.completeTask`、厨房 `submitLeave` 写成口试名单。
2. **chain 覆盖列。** 一格：`A:TestLeaveController.list,export,get,add,submitAndFlowStart,edit,remove`。不写 (b)，不写 (d)，不写 `ITestLeaveService`。
3. **课 `claimed_cells` / 矩阵对照。** 课把头扩成三格，把 (b) 钉成同一把提交枪，把 (d) 钉成 `submitAndFlowStart → 任务完成`。矩阵 (a) 那一行七个名字；`get` 是矩阵名，磁盘方法 `getInfo`。矩阵 (b) 口号「写请假并发起流程」对着这一枪，不是对着 add/edit。矩阵 (d) 主走廊挂 L-072 **和** L-070：GP-L-070 已写明「挂名不等于本课认格，汇在 L-072」。失败 (d) 驳回/终止已由 L-070 独盖。Lead 若按 **现印 (a) 行** 标 covered，**不会**把 L-069 合同十一法或 L-070 十六扇假闭合；若凭「startCompleteTask 三字」把 `WorkflowService.*` 一并盖章，则会假闭合 L-069；若凭「任务完成」把 `FlwTaskController.completeTask` 标进 OBJ-72，则会假闭合 L-070。
4. **课自己的操作定义。** (a) 门牌 `/workflow/leave`，磁盘 7 映射；查用 GET，导出/提交用 POST，改用 PUT，删用 DELETE；提交与新增共用 `workflow:leave:add`、同一份 INSERT 日志、同一份 `@RepeatSubmit`（默认 5s）；导出无防重、删除无防重；`@ConditionalOnEnable` 整类随 `warm-flow.enabled=true` 进 Spring。接口 7 法，多出的内部查询是 `queryList`（只给 export）；`eval` 不在接口、不是 HTTP。(b) 同一事务 `insertOrUpdate` 假条，再 `params.ignore=true`、默认 `flowCode=leave1`、喊 `WorkflowService.startCompleteTask`；false 抛「流程发起异常」整单回滚。(d) 以已发布 leave1 为河床：这一枪把申请人节点办掉，门铃把 `test_leave.status` 写成 `waiting`，下一格「组长」待办出现；组长/主管再打 L-070 的 `completeTask` 才到 `finish`。模块登记 **classic**：无 UseCase、无 DAO、空 XML、流程只经 `wta-api`。
5. **邻格。** L-069 对讲机十一法（本课只认请假怎么喊 `startCompleteTask` / `deleteInstance`；合同窗**不**代写 ignore）。L-070 任务十六扇与五支枢纽；HTTP **没有**「启动并办理」对称窗。L-073 厨房六枪对齐六扇、**缺 export**；页面「提交审批」走 `addLeave`/`updateLeave` + `startWorkflow`。L-068 发布闸（本课消费已发布定义）。L-067 抽屉。L-071 暗号本（`eval` 只是 leave6 表达式的 bean 方法）。L-034/L-040 档案网关同一枪但不写 `test_leave`、不写 ignore。厂商 `skip` 内部 deferred(vendor-engine)。
6. **未入库。** 矩阵 **没有** `ITestLeaveService` 独行 (a)、没有 `eval` 符号行、没有「页面提交审批」(d)、没有 leave2/leave6 条件河独行 (d)。查询四扇与 add/edit/remove 是 (a) 的地图，不是第二组 (b)。

问：本三格闭合标准是「七扇 URL/动词/权限/Log/RepeatSubmit/返回形状 + 提交枪落库/ignore/leave1/`startCompleteTask`/失败回滚 + 主走廊把申请人首枪办掉且假条 waiting，且不把合同窗整份、任务窗后续盖章、厨房工厂、页面提交审批算进来」，还是 OBJ 那句必须先等 GP-L-069 的 `startCompleteTask` 实现、或必须先把 L-070 十六扇再走一遍才给 (d) 盖章、或必须等改 chain 印上 (b)(d) 才给这两行盖章？Lead 若只凭「请假和任务都叫完成」把 L-070 任务窗或 L-073 厨房标 covered，是否假闭合了盖章柜台与浏览器工厂？Lead 若因 chain 只印了 (a) 就把矩阵已有的 (b)(d) 留空洞，是否假判成未讲完？

## Q2 证据

- objective_id: OBJ-72
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 33–136；ITestLeaveService 17–74；TestLeaveServiceImpl 51–54 / 62–64 / 109–115 / 125–138 / 147–175 / 185–188 / 197–203 / 212–237 / 250–253 / 262–269；TestLeaveBo 35–98；TestLeave 20–65；TestLeaveVo 21–74；TestLeaveMapper.xml 5–6；ConditionalOnEnable 27；RepeatSubmit 20；BaseController 29–30；WorkflowServiceImpl 187–218；WorkflowService 108；StartExecute 40–57；CompletePrepare 47–63；CompleteExecute 39–48；BusinessStatusEnum 32–37 / 145–156；FlowConstant 94 / 99；MessageTypeEnum 23；modes.md 22；SQL 242–258 / 273–274 / 309–314；leave1.json 28–126；leave2.json 69–77；leave6.json 144–153；kitchen index.ts 198–211 / 470–475；index.test.ts 151–191；LeaveEditPage 90–182；runtime-actions 3–8 / 37–40；e2e 468 / 846–908；PersonGateway 33–44
- difficulty: high

课的 (a) 七扇、(b) 提交枪、(d) 申请人首枪完成哪些能在工作树直接复现，哪些只是测试名单比课表窄、或邻格事实？

**仍能复现（本课 A + B + D）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 门牌 `/workflow/leave`；`@ConditionalOnEnable`；只注入 `ITestLeaveService` | Controller 33–40；ConditionalOnEnable 27 |
| 磁盘 7 个 `@*Mapping`：list/export/getInfo/add/submitAndFlowStart/edit/remove | Controller 50 / 63 / 76 / 91 / 105 / 119 / 132 |
| 矩阵 `get` = Java `getInfo` = `GET /{id}` | Controller 75–79；矩阵 (a) 行写 `get` |
| 提交与新增共用 `workflow:leave:add`、`@Log INSERT`、`@RepeatSubmit()` 默认 5000ms | Controller 88–107；RepeatSubmit 20 |
| 导出 `POST /export` + `workflow:leave:export` + Excel 表名「请假」+ `void`；删除无 RepeatSubmit，走 `toAjax(boolean)` | Controller 61–66 / 130–136；BaseController 29–30 |
| 接口 7 法；`queryList` 只给 export；`eval` 不在接口 | ITestLeaveService 17–74；Impl 62–64 / 98–100 |
| classic：无 usecase 包；登记表 `wta-workflow` = classic，「经公开 Workflow API 接入」 | grep usecase 空；modes.md 22；Impl 53–54 抱 Mapper + `WorkflowService` |
| `AddGroup`：`leaveType`/`startDate`/`endDate` 非空；`id` 只在 EditGroup；`flowCode`/`params`/`leaveDays` 无校验 | TestLeaveBo 35–98 |
| `params` 字段默认 `new HashMap<>()` | TestLeaveBo 98 |
| 查询只认类型等值 + 天数上下限，按 createTime 倒序；不按状态筛 | Impl 109–115 |
| add：闭区间天数 `DAYS.between + 1`；新 `applyCode=毫秒串`；空白状态 → `draft`；**不**喊流程 | Impl 125–138 |
| (b) `@Transactional`；同样算天数；`id==null` 才写 applyCode；`insertOrUpdate`；成功才 `params.put("ignore", true)`；`flowCode` 空则 `"leave1"`；`startCompleteTask`；false →「流程发起异常」；返回 `convert(leave)` 不是 `queryById` | Impl 147–175 |
| edit：不重算天数、不点火 | Impl 185–188 |
| 删除先 `deleteByIds` 再 `deleteInstance`（转字符串） | Impl 197–203 |
| 三只门铃都是 `flowCode.startsWith("leave")`；提交监听无空行保护、submit 强制 `WAITING`；任务创建只 log；删除监听有空保护 | Impl 212–269 |
| 实体表 `test_leave` **没有** `flowCode` 列；flowCode 只活在 BO / 启动 DTO | TestLeave 20–65 vs TestLeaveBo 41 |
| 空 XML | TestLeaveMapper.xml 5–6 |
| 合同 `startCompleteTask`：先 `startWorkFlow` 再 `completeTask(首 taskId)`；消息类型写死 `"1"`；**不** `put("ignore")`；变量原样下传 | WorkflowServiceImpl 203–218；MessageTypeEnum 23 |
| 新开实例 `flowStatus=draft`；首环节任务数 ≠ 1 →「请检查流程第一个环节是否为申请人！」 | StartExecute 44–55 |
| draft/cancel/back 再办塞 `SUBMIT=true`；skip 读 `VAR_IGNORE` | CompletePrepare 61–63；CompleteExecute 42；FlowConstant 99 |
| `checkStartStatus` 挡 waiting/finish/invalid/termination/空白 | BusinessStatusEnum 145–156 |
| leave1：开始 → 申请人 `${initiator}` → 组长角色 → 部门主管角色或 → 结束；`formPath=/workflow/leaveEdit/index` | leave1.json 28–126 |
| leave2 读 `leaveDays` 条件；leave6 点名 `@testLeaveServiceImpl.eval(#leaveDays)` | leave2.json 69–77；leave6.json 153 |
| 可见列表菜单父节点是测试菜单 `1761400000000000005`；隐藏 `leaveEdit` 挂工作流菜单；设计器也误贴 `workflow:leave:edit` | SQL 273–274 / 309 |
| 厨房六枪：list/get/add/`submitLeave`→`/submitAndFlowStart`/update/delete；**无** export URL；`LeaveForm` **无** `flowCode` | index.ts 198–206 / 470–475；test 186–191 |
| 页面三河：`draft`→add/update；`start`→先存再 `startWorkflow({leaveDays,userList,bizExt})`；`direct`→`submitLeave({...form})` 不带下拉 `flowCode` | LeaveEditPage 90–182 |
| 前端可编辑闸 draft/cancel/back；天数毫秒地板除 +1 | runtime-actions 3–8 / 37–40 |
| `eval`：`leaveDays <= 2` | Impl 62–64 |

**测试名单比课表窄（不证伪机制，挡住「单测即格子」）：**

| 课的机制 | 测试实际锁什么 |
| --- | --- |
| 七扇 HTTP 动词/权限/返回形状 | **零** `TestLeaveController` 合同测试。厨房 `index.test.ts` 186–191 锁的是浏览器 URL 拼法（且无 export），格子归 L-073 |
| `submitAndFlowStart` 落库 + ignore + leave1 + 失败回滚 | **零** 行为单测。证人是 Impl 147–175 + WorkflowServiceImpl 203–218 |
| 门铃强制 waiting、返回 Vo 可能旧 | **零** 监听器单测。证人是 Impl 212–237 vs 返回 175 |
| 申请人首枪被 `startCompleteTask` 办掉 | **零** 请假流程集成测试。e2e `leave add calculates days…` 846–908 点的是「提交审批」，断言 `workflowStartBodies` 走 `/workflow/task/startWorkFlow` 且带 `leaveDays`/`userList`/`bizExt` |
| 审批只读页藏起三颗写按钮 | e2e 468 锁的是待办进 `leaveEdit?type=approval`，那是任务河，不是 (b) |

**邻格事实（对照，不是本三格）：**

| 声称 | 磁盘锚点 | 格子 |
| --- | --- | --- |
| 合同窗 `startCompleteTask` / `completeTask(taskId,message)` 才自己 `put ignore`；十一法整份 | WorkflowServiceImpl 187–218 | L-069 |
| 任务 HTTP 启动/办理/驳回/催办/终止十六扇；无「启动并办理」窗 | FlwTaskController（L-070 已 covered） | L-070 |
| 厨房 `submitLeave` 与十六扇任务 URL 写在同一座工厂 | index.ts 413 / 473；test 159–191 | L-073 |
| 档案网关 `startCompleteTask` 变量只有 profileType/snapshotVersion/submissionId，**无** ignore | PersonGateway 33–44 | L-034 |
| leave6 SpEL 目录窗 | `FlwSpelController` | L-071 |
| 定义必须先发布 | `IFlwDefinitionService.publish` | L-068 |

**课写过、口试标签写偏（不是证伪 A/B/D）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「返回 Vo 的 status 可能仍是 null，库里已经 waiting」 | Impl 175 转的是点火前 `leave`；门铃 232 写库。**无** HTTP 测试钉响应体 status |
| 「JSON 显式 `params: null` 会 NPE」 | `bo.getParams().put` 160。默认字段是空 Map；显式 null 才会炸。**无** 合同测试 |
| 「`insertOrUpdate` 失败不点火、不抛，仍返回 Vo」 | Impl 157–175 `if (flag)` 才启动。静默空操作 |
| 「页面后端发起带不走下拉 leave2」 | `flowCode` 是 Vue ref 91；`submitLeave({ ...form })` 139；`LeaveForm` 无该字段 |
| 矩阵 (d) 挂了 L-070 | 课明确：后续盖章借用任务窗，**不**把 `FlwTaskController` 标进本格。GP-L-070 也不认这一行 |

问：在七映射、classic 对讲机、提交枪 ignore+leave1、合同窗不代写 ignore、leave1 申请人首枪、门铃 waiting 已经能互证的前提下，三格是否还要把「零 HTTP 单测」「e2e 测的是另一条河」「返回体 status 未刷新」写成开篇半句才算讲完？chain 没写 (b)(d) 时，能不能凭矩阵已有这两行 + 课 `claimed_cells` + OBJ-72 点名的提交枪盖章，而不是先改 chain？主走廊挂了 L-070，是不是邻格对照（后续 completeTask 枪）而不是本课必须再盖任务窗？

## Q3 假设与反例

- objective_id: OBJ-72
- bloom_level: analyze
- socratic_move: assumptions
- expected_evidence: Controller 50–136；Impl 125–175 / 185–188 / 197–203 / 212–269；WorkflowServiceImpl 187–218；LeaveEditPage 90–182；index.ts 198–206 / 470–475；e2e 846–908；PersonGateway 33–44；leave1.json 28–90；leave2.json 69–77；课反例 1–16 / 误区 1–18 / 变式 A–H
- difficulty: high

课的隐含假设若反转，合格读者会把哪段走廊认错？

1. **「`TestLeaveController.*` 就是提交那一枪。」** 七扇。(a) 含 list/export/getInfo/add/edit/remove。提交枪是 (b)。课误区 5 的镜像。
2. **「`ITestLeaveService` 有的方法 Controller 都有对外名。」** 窗与接口都是 7，但对外 HTTP 没有 `queryList`；`eval` 连接口都没有。
3. **「矩阵 `get` 是 Java 方法名 `get`。」** 方法名 `getInfo`。课反例 3。
4. **「提交有独立权限字 `workflow:leave:submit`。」** 与新增同字。课反例 4。
5. **「`add` 也会启动流程。」** `insertByBo` 零对讲机。课反例 2。
6. **「`submitAndFlowStart` 就是页面『提交审批』。」** 磁盘『提交审批』=`addLeave`/`updateLeave` + `startWorkflow`；『后端发起』才是本枪。e2e 锁的是前一条。课反例 1。
7. **「`startCompleteTask` 自动 ignore。」** 合同窗不写。请假 `params` 写。档案样本不写。`completeTask(taskId, message)` 那一超载才自己 put ignore，本课没喊它。课反例 5。
8. **「(b) 会把 `leaveDays` 放进流程变量。」** 变量就是 `params` 加 ignore。天数只写在假条列。leave1 不读天数仍通；leave2/leave6 要调用方自己放。课反例 6。
9. **「后端发起会带上页面选的 leave2。」** `LeaveForm` 不带 `flowCode`，后端默认 leave1。课误区 8。
10. **「返回 Vo 的 status 一定是 waiting。」** 返回点火前内存对象；门铃写库。课反例 7。
11. **「请假 Controller 注入了 `IFlwTaskService`。」** 只注入 `ITestLeaveService`；实现注入 `WorkflowService`。课反例 8。
12. **「`eval` 是第八扇 HTTP / SpEL 目录行。」** 实现类公开方法，给 leave6 表达式用。课反例 9。
13. **「删除假条不会动流程 / 只删 Excel 行。」** `deleteInstance` 会。反过来删实例可能门铃再删假条。课反例 10。
14. **「`updateByBo` 会重算天数并续流。」** 两件都不做。课反例 13。
15. **「`processHandler` 和删除监听一样空行保护。」** 提交监听没有。课反例 15。
16. **「`RepeatSubmit` 罩住删除和导出。」** 只有 add / submitAndFlowStart / edit。课反例 16。
17. **「主路径在 submitAndFlowStart 返回的那一刻已经 finish。」** 那一刻只办完申请人。finish 在后面的组长/主管盖章。课误区 17。
18. **「列表页撤销打 `/workflow/leave`。」** 打实例 `cancelProcessApply`。课误区 18。
19. **「本课覆盖 `WorkflowService.*` / 厨房工厂 / 任务十六扇。」** L-069 / L-073 / L-070。把假设反转后，Lead 会用假条窗口假闭合总机、厨房或盖章柜台。
20. **「(b)(d) 必须等改 chain 才盖章。」** 矩阵已有这两行。(b) 只挂本课符号；(d) 挂 L-072 与 L-070，但 GP-L-070 已让出。课 `claimed_cells` 已列。OBJ-72 点名提交枪。
21. **「矩阵要先有 `ITestLeaveService` (a) 独行 / leave2 条件河 (d) 课才算 (a) 讲完。」** 矩阵无这些行。接口七法与条件图是 (a)/(b) 的解释到副作用。
22. **「主走廊挂了 L-070，所以本课必须把 `FlwTaskController.completeTask` 标 covered 才算 (d) 讲完。」** 后续盖章枪已在 L-070 的 (b) 讲过。本课认的是示例流怎么点着那条河，不是再盖任务窗。

问：读完后仍说不出的，是不是「七个方法名」，而是 **草稿枪不开火、并发流枪自己写 ignore 且默认 leave1、合同窗不代写 ignore、天数不自动进变量、门铃写 waiting 而响应体可能仍空、页面提交审批是另一条河、申请人首枪完成 ≠ 整单 finish**？

## Q4 视角

- objective_id: OBJ-72
- bloom_level: analyze
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-72 vs L-067…L-073 / L-034 / L-083 / L-085；chain 只 A；课 claimed_cells A+B+D；modes.md classic；GP-L-070 让出本 (d)
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:TestLeaveController` 七扇** 或 **`B:submitAndFlowStart`** 或 **`D:主路径 工作流请假`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 七名一行（`get`≠`getInfo`）+ (b) 提交枪 + (d) 主走廊挂两课；chain 漏印 (b)(d) | **是** 闭合定义。三格可单独 covered。不要等改 chain。不要用本课盖任务窗或厨房 |
| 窗口计数员 | 7 映射；提交=add 权；导出 void Excel；删除 toAjax | **是** (a) |
| 接口计数员 | 7 法；`queryList` 无独立窗；`eval` 不在接口 | 内部查询是 (a) 负空间，不必发明第二张 (a) |
| 写单并发流 | 落库 → ignore → leave1 → startCompleteTask；失败回滚 | **是** (b) |
| 主走廊河床 | leave1 申请人被代盖；门铃 waiting；下一格组长 | **是** (d) 第一段。finish 尾巴借用 L-070 枪，**不**盖任务窗 |
| 页面三按钮 | 保存 / 提交审批 / 后端发起 | 对照 **是**。(b) 只认 direct。start 那条 **否**（L-073+L-070） |
| 总机（L-069） | `WorkflowService.*`；不代写 ignore | 对照 **是**。十一法 **否** |
| 任务柜台（L-070） | 十六扇 + 五支枢纽；驳回/终止失败已 covered | 后续盖章对照 **是**。本 (d) 认格 **否**（GP-L-070 已让出） |
| 厨房（L-073） | 六枪无 export；`submitLeave` URL 对齐 | URL 对照 **是**。工厂 **否** |
| 图纸/分类/SpEL | 发布闸、抽屉、暗号本 | **否**（L-068/L-067/L-071）。leave6 `eval` 只是对照 |
| 档案网关 | 同一枪、不同变量、不写 test_leave | 对照 **是**（变式 F）。格子 **否**（L-034/L-040） |
| 测试夹具行 | 零 Java 单测；e2e 锁提交审批 | 证人窄 **是**。不要把 e2e 或厨房测试名单标成本课 (b) |
| 登记表作者 | classic，不改内部层次 | **是** 房间模式。不要发动 layered 重构 |
| MySQL 8.4 / dao-mapper | `test_leave` DDL + 空 XML | 本窗表举例 **是**。容器/组件课 **否**（L-085/L-083） |

问：Lead 若凭「`TestLeaveController` / `submitAndFlowStart` 四字在函数表里」把 (a)(b)(d) 标 covered，是否还要求读者能指着 **七扇形状、提交枪 ignore+leave1、申请人首枪完成、门铃 waiting、页面提交审批不是本枪**？这些是不是已经在本格机制链/反例里，而不需要把合同十一法、任务十六扇或厨房工厂算进来？(b)(d) 是不是矩阵已有、不必先改 chain？主走廊挂名 L-070 是不是「后续枪在任务楼」而不是「本课不能盖 (d)」？

## Q5 元问题

- objective_id: OBJ-72
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 七扇、(b) 提交枪、(d) 请假主路径挂 L-072 与 L-070；chain 只印 A；课 claimed_cells A+B+D；GP-L-070 已让出本 (d)
- difficulty: low

建议：`defer`

原因：

- **(a)(b) 与主路径 (d) 与矩阵 / OBJ-72 / 课 `claimed_cells` 1:1；chain 覆盖列少印了 (b)(d)，但不缺机制。** L 合同章节齐全（宏观地图、直觉十二句、定义表、机制链 1–5、带 caption / 文字等价物 / 图边界的七扇图与主走廊图、正反例、变式 A–H、误区 1–18、暂停走查、来源表 S-004/S-008/S-010/S-015/S-L072-01…08）。**主叙事站得住且与源码对齐**：七扇 HTTP、classic 无 UseCase、提交与新增同权、草稿不开火、并发流枪自己写 ignore 且默认 leave1、合同窗不代写 ignore、失败回滚、门铃 waiting、返回 Vo 可能旧、页面提交审批是另一条河、申请人首枪完成不是整单 finish。不必为「七个名字」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例/边界或邻格，不挡住把 **(a)(b) 与主路径 (d)** 标 covered：（1）零 `TestLeaveController` HTTP 测试、零提交枪行为单测；（2）e2e 请假加单锁的是「提交审批」+ `startWorkFlow`，不是「后端发起」；（3）厨房 URL 证人属 L-073 且缺 export 枪；（4）返回体 status 与库行不同步无合同测试；（5）`params==null` NPE、`insertOrUpdate` 失败静默；（6）提交监听无空保护。不要把 `WorkflowService.*`、任务十六扇、厨房工厂、档案网关、`eval` 目录行塞进本三格。
- **不要发明 `ITestLeaveService` (a) 行，也不要等 GP-L-069/L-070/L-073 才给假条窗盖章。** 矩阵 (b) 只挂本课符号。矩阵 (d) 挂 L-072 与 L-070，但 GP-L-070 已声明不认格。L-069 只保证总机组合拳；L-070 只保证任务窗与驳回/终止失败；L-073 只保证厨房 URL。本课保证七扇 + 写单并发流 + 示例河第一段。
- **(b)(d) 不必改 chain 才盖章。** 证据锚是矩阵已有这两行 + 课 `claimed_cells` + OBJ-72 点名提交枪 + 课机制链 2–3 + 本 probe。miner 不改 chain。(d) 与 (b) 是同一条河的两岸：枪是写单并发流，河是申请人首枪完成。
- **不要把 (d) 的「任务完成」读成必须再盖 `FlwTaskController.completeTask`。** 课的操作定义是申请人那一格被 `startCompleteTask` 办掉、假条 waiting、下一格待办出现；后续组长/主管盖章走到 finish 借用已 covered 的 L-070 枪。挂名 L-070 是下游对照，不是本行空洞。
- 不建议 `mine-more`：b01 未证明三格还有真实矩阵空洞；再五问只会把 L-069 合同窗、L-070 任务窗、L-073 厨房、leave2 条件变量或档案网关再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；可选半句（测试名单窄、e2e 测邻河、返回体旧照片、params 空指针）不是第二容器。不建议 `split-lesson`：未满 10 问；七扇与提交枪与请假主走廊是 chain 原设计的同一 OBJ-72，同一 `wta-workflow` 容器，不是两个 C4。`split_depth: 0`，本批禁止填 split 表。不建议发明 UseCase 层或把 classic 改 layered。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:TestLeaveController.list, export, get, add, submitAndFlowStart, edit, remove | 七映射、门牌、权限字（提交=add）、Log/RepeatSubmit 分布、导出 void Excel、删除 toAjax、条件装配、矩阵 `get`=`getInfo`——均与源码对齐。残留精度：零 HTTP 合同测试；厨房无 export 枪属 L-073。 | 可选半句：口试七扇以 Controller 映射为准，不以厨房测试名单当本格测试；矩阵 `get` 对磁盘 `getInfo`。Lead 可将本行标 `covered`（证据锚本 probe 与 L-072 来源表 S-L072-01）。 | defer（本行可 covered；精度项不挡盖章） |
| B:TestLeaveController.submitAndFlowStart | 落库 `insertOrUpdate`、自己写 ignore、默认 leave1、喊 `startCompleteTask`、false 抛并回滚、不自动带天数变量、返回内存 Vo——均与源码对齐。残留：零行为单测；e2e 测的是页面提交审批；`params==null` NPE；`insertOrUpdate` 失败静默。 | 可选半句：口试「页面提交审批等于本枪」以 LeaveEditPage `persist('direct')` 为准。Lead 可将本行标 `covered`。 | defer（本行可 covered） |
| D:主路径 工作流请假 | 矩阵口号 `TestLeave.submitAndFlowStart → 任务完成`，挂 L-072, L-070。磁盘：leave1 申请人被代盖、门铃强制 waiting、下一格组长待办；finish 需后续 L-070 `completeTask`。chain 未列本行。GP-L-070 已让出。 | 不要等改 chain。不要把任务十六扇写进这行。不要把 e2e 提交审批河当本走廊证人。Lead 可将本行标 `covered`。 | defer（本行可 covered；证据在本课；L-070 为下游对照） |
| A:FlwTaskController.* / B:IFlwTaskService 五支 / D:失败路径 工作流驳回/终止 | 后续盖章枪与驳回/终止失败。 | 对照「首枪 vs 后续盖章」。已 covered。 | defer（L-070；已 covered） |
| A:FlwInstanceController.* / A:WorkflowService.* | 总机组合拳、不代写 ignore、`deleteInstance`。 | 对照「请假怎么喊对讲机」。L-069 已 covered。 | defer（L-069；已 covered） |
| A:createWorkflowDefinitionService / A:createWorkflowWebDomain | 厨房六枪 + 请假页三按钮。 | URL 对照一句即可。L-073。 | defer（L-073） |
| A:FlwCategoryController.* / A:FlwDefinitionController.* / A:FlwSpelController.* | 抽屉、图纸、暗号本。 | 保持。L-067 已 covered；L-068 已 covered；L-071 仍 uncovered。leave6 `eval` 不是目录行。 | defer（邻课） |
| A:ITestLeaveService / `eval` 升格 (a) 或第二 (b) | 课已写进 (a) 负空间或 leave6 对照。矩阵无独行。 | 不要发明第八扇或 Service 行。 | defer（未入库） |
| D:页面提交审批 / leave2 条件河 | 课已标为变式 C/E，不是矩阵行。 | 不要发明第二 (d)。 | defer（未入库） |
| 档案 `startCompleteTask` | 同一枪、不同变量。 | 对照样本。L-034/L-040。 | defer（邻课） |
| 厂商 `skip` 内部 | 课已 deferred(vendor-engine)。 | 主路径只指 NAMEWTA 闸与门铃。 | defer（vendor-engine） |
| C:MySQL 8.4 / mysql-base / dao-mapper-ownership | `test_leave` 是本窗表。 | 不要因本课标容器/组件 covered。 | defer（(c) 容器 / L-085 / L-083） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；(a)(b) 与主路径 (d) 无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出目的（假条窗口七扇、草稿不开火、后端发起写单并代盖申请人）、主路径（insertOrUpdate → ignore → leave1 → startCompleteTask → 门铃 waiting）与失败关闭（流程 false 抛并回滚；未发布/首环节非申请人由启动链抛，整单回滚；本课不认驳回/终止失败行）；(b)(d) 缺的是 chain 漏印，不是讲义容器；把可选半句写进原课不会超过 40 分钟；合同窗、任务窗、厨房、分类/定义/SpEL 已有邻课。满 10 问前不拆。七扇与提交枪与请假主走廊是 chain 原设计的同一 OBJ-72，同一 `wta-workflow` 容器，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-069 实例/合同（已 covered）、L-070 任务窗与驳回失败（已 covered，且已让出本 (d)）、L-073 厨房、L-067/L-068/L-071、未入库 Service/`eval` 升格与第二 (d)、档案网关、厂商引擎内部、(c) 容器与 dao-mapper 组件显式建议 `defer`；对 `A:TestLeaveController.list,export,get,add,submitAndFlowStart,edit,remove` 与 `B:TestLeaveController.submitAndFlowStart` 与 `D:主路径 工作流请假` 建议 Lead 标 `covered`（证据锚本 probe 与 L-072 来源表 S-L072-01…08）；对 (b)(d) 建议凭矩阵已有行盖章，不要等改 chain；不要发明第二 (d)、不要用假条窗假闭合 `WorkflowService.*` 或任务十六扇、不要把 e2e「提交审批」当成 (b) 证人、不要把厨房 `submitLeave` 标成本课 (a)。本课不主张发明 UseCase、把 `eval` 升成 HTTP、或等 GP-L-073 才给七扇盖章。
