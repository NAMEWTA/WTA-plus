---
artifact: learning-goal-probe
probe_id: GP-L-053-b01
lesson_id: L-053
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-053 / b01

审问对象是已写成的 Lesson `lessons/L-053-notify-domain-frontend.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` 三格：`A:createNotificationService`、`A:notificationDirectory.searchUsers`、`A:usersByIds`。chain 覆盖列写成 **两格**：`A:createNotificationService A:notificationDirectory`。OBJ-53 原文：能说明 `createNotificationService` 与 `notificationDirectory`。档是**解释+对照**。矩阵 (a) **一行**把三符号绑在一起：`createNotificationService, notificationDirectory.searchUsers, usersByIds`（模块栏印 `domains/notify`，仍 uncovered）。矩阵 (a) **九工厂同行**含 `createNotifyWebDomain`（仍 uncovered，备注「manifest 工厂」）。矩阵 (b) **没有**空关键字短路 / 浅层 freeze / `pageNum` 行。矩阵 (c) `admin-web` / `home-web` 容器与 `frontend-direction` 已 covered（L-004 / L-007 / L-008）。矩阵 (d) **没有**浏览器通知流；已 covered 的「主路径 通知提交」仍是 Java `NotificationController.submit → Outbox → claim → 渠道`。本批按派单审问 **厨房工厂 + 厅堂目录**（矩阵那一行三符号）；口试按磁盘：冻住对象只有五块 22 枪，选人是 App 对象字面量两枪，`userTypes` 是同一对象上的邻居第三枪。不把九工厂行整行闭合，不把 L-007 插头再盖第二次章，也不把四张 Vue 升成矩阵行。`NotifyNoticeController` 六扇留给 L-045；收件箱已见/已读留给 L-046；配置账号/场景/试发留给 L-047；收件人端口与 `pageSize` clamp 留给 L-048；监控 snapshot 写死 `includeContent=false` 留给 L-049；供应商回调验签留给 L-050；统一通知 submit/query/retry/cancel 留给 L-051；Outbox 领取留给 L-052；`createNotifyWebDomain` 四键剧本留给 L-054；厅堂插座板全表留给 L-007 / L-008；系统监控四键留给 L-024。

抽查锚点（2026-09-17）：`frontend/packages/domains/notify/src/{transport,transport.test,index,types}.ts`；`src/{monitor,notification,notice,inbox,config,callback}/index.ts`；包 `package.json` `exports` / `dependencies`；`frontend/apps/admin-web/src/application/services.ts` 第 5、13、18–20、70–88 行；`apps/admin-web/src/router/adminManifestRegistry.ts` 与 `.test.ts`；`frontend/packages/web-domains/notify/src/{runtime,index,index.test}.ts`；`NoticePage.vue` / `InboxPage.vue` / `ConfigPage.vue` / `NotificationPage.vue`；`notice/{useRecipientSelection,useRecipientSelection.test,RecipientUserPicker}.ts|.vue`；`layout/components/notice/index.vue`；`utils/push.ts`；`apps/home-web/src/application/services.ts`；`NotifyRecipientController.java` / `NotifyRecipientUseCase.java`；`domains/system/src/service.ts` `userTypes.options`；`api-contracts/generated/openapi.ts` `/notify/notification*`。

## Q1 澄清

- objective_id: OBJ-53
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-53；chain L-053 覆盖列两格 `A:createNotificationService A:notificationDirectory`；课 `claimed_cells` 三格；矩阵 (a) 一行三符号仍 uncovered、模块栏印 `domains/notify`；九工厂同行含 `createNotifyWebDomain` 仍 uncovered；(b) 无本课符号；(c) 两厅堂已 covered；(d) 无浏览器通知流，已 covered 的主路径是 Java 提交走廊；课开篇「口试名单就是矩阵 (a) 这一行三格」+ 负空间 L-045…L-052 / L-054 / L-007 / L-008 / L-024
- difficulty: medium

课把浏览器这一头收成「两张纸条」：厨房打印机打公告/收件箱/配置/监控，厅堂门卫名单夹打收件人两枪。计划里至少有五套可数对象，派单点名的是 **矩阵 (a) 那一行**：

1. **OBJ-53 原文。** `course.md`：能说明 `createNotificationService` 与 `notificationDirectory`。一句里厨房 **和** 目录，没有把 `createNotifyWebDomain`、四张 Vue、`NotificationController.submit` 或 `X-Notify-Signature` 写成口试清单。
2. **chain / 课 `claimed_cells` / 矩阵三套写法。** chain 两格把目录收成对象名。课把目录拆成 `searchUsers` + `usersByIds`。矩阵把三符号印在 **同一行**。`userTypes` 在磁盘上是同一字面量的第三方法，chain/矩阵/claimed **都不**立格。Lead 若按「三格必须三行」去改矩阵，会把本课拆成厨房课和目录课；若按「一行三符号」盖章，必须同时认冻住对象 **没有** `recipients` 和 App 对象 **有** 两枪 HTTP。
3. **模块栏 vs 磁盘所有权。** 矩阵印 `domains/notify`。厨房确实在该包 `transport.ts`。目录是 `apps/admin-web/.../services.ts` 对象字面量，只从 notify 包 **借类型** `NotifyUserCandidatePage`。口形状 `NotifyUserDirectory` 在 web-domain `runtime.ts`。问：盖章这一行，是承认「厨房在 domain 包、目录在厅堂」这一分裂，还是默认读者会以为 `notificationService.searchUsers` 存在？
4. **课自己的操作定义。** 工厂只认 `HttpClient`，`Object.freeze` 五块 22 枪，前缀 `/notify/notice|inbox|config|monitor`。读 GET、写 POST，没有 `method: 'delete'`。只有监控两枪用 `keyof paths`。内部 `request` 无投影闸、无 `encodeURIComponent`。`searchUsers`：`keyword.trim()` 有字才 `GET /notify/recipients/search`，空白本地空页。`usersByIds`：永远 `GET /notify/recipients/by-ids?userIds=` + `join(',')`，空数组不短路。`userTypes` 转 `systemService.userTypes.options()` → `GET /system/userType/options`，用来证明目录不是厨房漏装的抽屉。菜单页走 runtime；铃铛/`push.ts` 直接 import 厅堂单例 `inbox`。
5. **邻格。** L-007 已认插座板上「工厂一行 + 目录对象三方法」，**没有** 22 枪 URL 表。L-054 才认四键怎么扣扳机。L-045…L-051 是后端窗。L-048 已讲 search 无 `pageNum`、`pageSize` clamp、空白空页。L-024 是 `createMonitorService`，必须走 `@namewta/domain-system/monitor`。九工厂行把本课菜单工厂和另外八个 `create*WebDomain` 绑死。

问：本行闭合标准是「厨房 22 枪 URL/动词/body 形状 + 目录两枪短路不对称 + 用户类型拐 system 作对照，且不碰九工厂整行、不重盖 L-007 插头、不升 Vue 矩阵行」，还是 OBJ 那句必须先把 `createNotifyWebDomain` 标 covered、或必须把目录从 `domains/notify` 那行拆到 `admin-web` 才盖章？Lead 若凭「`createNotificationService` 四字在函数表里」把九工厂行或 `admin-web.services` 再盖一次，是否假闭合了另外八个菜单工厂和十二个 `create*`？

## Q2 证据

- objective_id: OBJ-53
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: transport.ts 17–71；transport.test.ts 4–96；package.json 6–8；根 index.ts 3–9；monitor/index.ts 1–6；notification/index.ts 1–6；callback/index.ts 1–5；config/index.ts 1；notice/index.ts 1–3；inbox/index.ts 1–3；types.ts 1–2 / 57–74 / 84；admin services.ts 5 / 13 / 18–20 / 70–88；runtime.ts 11–28；web-domain index.ts 6–83；index.test.ts 8–36；useRecipientSelection.ts 35–68 / 88–97；useRecipientSelection.test.ts 27–37 / 78–97；RecipientUserPicker.vue 12 / 44–51；NoticePage.vue 127–144 / 283 / 302 / 330 / 362 / 387–388；InboxPage.vue 85 / 99；ConfigPage.vue 310 / 323 / 337–351 / 366 / 377 / 411 / 452–456；NotificationPage.vue 84；layout notice 54 / 100 / 114；push.ts 4 / 107；adminManifestRegistry.ts 4–5 / 38–39 / 291–306 / 376；registry 测试 43 / 115–118；home-web services.ts 1–16；NotifyRecipientController.java 24–38；NotifyRecipientUseCase.java 18–33；system service.ts 149–153；generated openapi.ts 2688+；web-domain Vue 零 `@/application/services`
- difficulty: high

课的两张纸条哪些能在工作树直接复现，哪些只是测试名单比课表窄、或邻格事实？

**仍能复现（本行厨房 + 目录）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂签名只有 `http: HttpClient`；内部 `request` 即 `http.request` 断言成 `{ data, code?, msg?, error? }`；无投影、无 `encodeURIComponent`、无 `method: 'delete'`、无 `recipients`、无 `/notify/notification`、无 `callback` | transport.ts 17–71；对该文件三词检索为空 |
| 返回 `Object.freeze`；五块：顶层 `snapshot`/`deliveries` + 嵌套 `notices`/`inbox`/`config` | transport.ts 22–70 |
| 监控两枪 URL 类型是 `keyof paths`；其余 URL 是字符串字面量 | transport.ts 20–21 vs 29–69 |
| `notices` 六枪：list GET `/notify/notice/list`；get GET `.../${id}`；save POST `.../save`；publish/retract POST 独立路径；remove POST `.../remove` 且 `data` 是 **数组** | transport.ts 27–38 |
| `inbox` 四枪：list GET `/notify/inbox` 无 params；seen/read/readAll 皆 POST | transport.ts 39–44 |
| `config` 十枪：accounts GET list + `channel/pageNum/pageSize`；account GET by id；add POST 根；edit POST `.../edit`；changeStatus POST 对象 `{ accountId, enabled }`；removeAccount POST **标量** `data: accountId`；scenes/saveScene；testAccount/testTemplate | transport.ts 45–70 |
| 三类 `recipientType` 都走同一枪 save；publish 另算一枪 | transport.test.ts 5–21 |
| deliveries 保留筛选参数；config 映射 MAIL/SMS 账号与 `testTemplate` | transport.test.ts 22–96 |
| `exports`：`.` / `./monitor` / `./notification` / `./callback` / `./notice` / `./inbox` / `./config`。依赖表 **没有** `@namewta/domain-system` | package.json 6–8 |
| 根 barrel 与 `./monitor`、`./notification` re-export **同一** `createNotificationService`。`./callback` 只有资源标签。`./notice` `./inbox` 有标签不 re-export 工厂。`./config` **只有类型** | 各 `index.ts` |
| 资源标签：`notifyNotificationResource.basePath === '/notify/notification'`；`notifyCallbackResource.basePath === '/notify/callback'`。工厂零枪打这两条前缀 | notification/index.ts 3–6；callback/index.ts 2–5 |
| 名片 `notifyDomainModule`：`id: 'notify'`，`backendModules: ['wta-notify']`，`capabilities: ['notification-control-plane', 'notification-monitor']` | 根 index.ts 3–7 |
| `NotificationChannel` 含 `IN_APP`；`NotifyConfigChannel` 只有 `MAIL \| SMS` | types.ts 1 / 84 |
| 厅堂 `import { createNotificationService } from '@namewta/domain-notify'`；`notificationService = createNotificationService(domainHttp)` | services.ts 5 / 70 |
| `notificationDirectory` 是对象字面量：search 三元短路；by-ids `ids.join(',')`；`userTypes: () => systemService.userTypes.options()` | services.ts 72–88 |
| 空白关键字 **不**发卡车，本地 `{ rows: [], total: 0 }` | services.ts 74–80 |
| 空 `ids` 仍 `GET /notify/recipients/by-ids`，`params.userIds` 是空串 | services.ts 81–86 |
| 口 `NotifyUserDirectory` 三方法；runtime 同时要 `service` 与 `directory`；类型是 `ReturnType<typeof createNotificationService>`，**没有**导出的 `NotificationService` 接口名 | runtime.ts 11–28 |
| registry：`createNotifyWebDomain({ service: notificationService, directory: notificationDirectory, ... })`；`selectedManifestIds` 含 `'web-domain-notify'` | adminManifestRegistry.ts 291–306 / 376 |
| 旧键 `monitor/notify/index` + domain `system` → `undefined`；新键 `notify/monitor/index` + domain `notify` → `NotificationMonitor` | registry 测试 115–118 |
| 选人 composable：空关键字 `if (!value) return`；防抖 300ms；`searchUsers(value, nextPage, 20)`；`status === '0'` 过滤在 composable；`restore` 对空 ids return | useRecipientSelection.ts 35–48 / 58–68 / 88–90 |
| 按钮 `:disabled="!keyword.trim()"`；`total > pageSize` 才渲染分页 | RecipientUserPicker.vue 12 / 44–51 |
| `recipientType=ALL` 只告警，不挂 picker；USER 才 `runtime.directory`；USER_TYPE 才 `directory.userTypes()` 再滤 `status === '0'` | NoticePage.vue 132–144 / 302 |
| 公告页扣 `notices.list/get/save/remove` 与 `publish|retract` | NoticePage.vue 283 / 330 / 362 / 387–388 |
| 收件箱页 `inbox.list` / `inbox.read`；配置页九枪（**无** `config.account(id)`，编辑拷列表行并清空密钥）；监控页只 `deliveries` | InboxPage.vue 85 / 99；ConfigPage.vue 337–351 / 310–456；NotificationPage.vue 84 |
| 四张 web-domain 页 **零** import `@/application/services` | web-domains/notify Vue 检索为空 |
| 铃铛 import 厅堂单例 `inbox.read` / `readAll`；`push.ts` `inbox.list` | layout notice 54 / 100 / 114；push.ts 4 / 107 |
| home-web `services.ts` **无** `createNotificationService` / `notificationDirectory`；`frontend/apps/{home-web,sso-web}` 零命中 | home-web 1–16；apps 检索只 admin-web |
| `userTypes.options` → `GET /system/userType/options` | system service.ts 153 |
| 后端 search **无** `pageNum`；权限是 `notify:notice:add` **或** `edit`，没有 `notify:recipients:*`；空白关键字 UseCase 空页；空白 `userIds` 空列表；非法编号 `R.fail("用户编号格式错误")` | Controller 24–38；UseCase 18–33 |
| generated 合同有 `/notify/notification` submit/retry/cancel/get；厨房方法表对不上其中任何一枪 | openapi.ts 2688+ |
| `createMonitorService` **不**在 `@namewta/domain-system` 根 barrel | system `src/index.ts` 零命中；厅堂从 `@namewta/domain-system/monitor` 进 |

**邻格仍能复现（不计入本行闭合）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| `createNotifyWebDomain` 四键顺序与权限目录含 `notify:inbox:seen`；工厂 **不** throw 缺方法（测试可塞 `service: {} as never`） | web-domain index.ts 6–83；index.test.ts 8–19 |
| 配置试发仍打 `/notify/config/test/*`，不打 `/notify/notification` | ConfigPage.vue 452–456；L-047 |
| 后端 search 把 `pageSize` clamp 1..50 当 limit，`total = rows.size()` | UseCase 18–22；L-048 |

**课写过、测试名单更窄或口试标签写偏（不是证伪本行）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「返回值 `Object.freeze`」易滑成五块抽屉都冻住 | freeze **浅**：顶层不能加键，`notices`/`inbox`/`config` 内层仍可变。无单测锁内层 |
| 22 枪方法表 | `transport.test.ts` 只锁 save+publish、deliveries 筛选、config 账号/场景/`testTemplate`。inbox / snapshot / remove / removeAccount / changeStatus **无**厨房单测 |
| App 空关键字短路 | **无** `services.ts` 单测。选人测试 mock 的是 `NotifyUserDirectory`，打的是 composable 自己的 `if (!value) return`。picker 路径上 App 空白短路是死代码（composable 已 trim 才喊 directory） |
| `usersByIds([])` 仍出门 | 无 App 单测。证人是字面量本身；真正跳过空名单的是 `restore()` |
| 「web-domain 要 service + directory」易滑成运行时 fail-closed | 类型要两根线；`createNotifyWebDomain` 测试用空 `service` 仍能注册四键（L-054） |
| 「只有 `./monitor` 与 `./notification` 能拿到工厂」 | 根 barrel 也能。`./notice` `./inbox` `./callback` `./config` **不能**从子路径拿到工厂 |
| `pageNum` 会发出去 | 厅堂 params 有；Controller 方法签名没有。Spring 忽略多余 query。不是后端分页合同（L-048） |

问：在 22 枪 URL/动词/body、无 recipients、无 submit/callback、目录两枪短路不对称、`userTypes` 拐 system、铃铛碰单例而菜单页不碰、home 不雇、旧监控键 undefined 已经能互证的前提下，本行是否还要把「freeze 是浅的」「厨房单测未锁 inbox/remove」「App 目录无单测」写成开篇半句才算讲完？矩阵模块栏印 `domains/notify` 是否只是分组标签，不必等拆行才给厨房+目录盖章？

## Q3 假设与反例

- objective_id: OBJ-53
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 课反例 1–8 / 误区 1–18 / 边界 1–8；transport.ts 17–71；services.ts 72–88；package.json 6–8；monitor/notification/callback index.ts；NoticePage.vue 132–144；NotificationPage.vue 84；layout notice 54；home-web 零命中；openapi.ts 2688+；Controller 25–26
- difficulty: high

课的隐含假设若反转，合格读者会把哪段走廊认错？

1. **「`notificationDirectory` 是 `createNotificationService` 返回值上的字段。」** freeze 对象没有 `searchUsers`。课误区 1 / 反例 1。TypeScript 也会拦。
2. **「存在 `createNotificationDirectory`。」** 磁盘无此工厂。目录是 App 字面量。课反例 2。
3. **「从 `@namewta/domain-notify/monitor` 进口得到监控专用小厨房。」** 同一只手，五块全在。课反例 3 / 误区 8。姿势与 `createMonitorService` 必须走子路径 **相反**。
4. **「看见 `notifyNotificationResource` 就有 `service.submit`。」** 标签不是方法表。generated 合同有四条 path，厨房零枪。课反例 6 / 误区 9。
5. **「页面自己拼 `/notify/notice/list`。」** 违反 App → web-domain → domain → platform。课反例 4。
6. **「domain-notify 依赖 domain-system 才能选用户类型。」** 包依赖表没有这条边。交叉线在 `services.ts`。课反例 5 / 误区 5。
7. **「空关键字还会打 search。」** App 三元与 composable 双重闸。课正例 2 / 误区 3。不要说「后端返回了零行」。
8. **「`usersByIds([])` 也短路。」** 不会。短路在 `restore()`。课边界 1 / 误区 4。
9. **「`userTypes` 是 `GET /notify/recipients/types`。」** 没有这条 path。真实 URL `/system/userType/options`。课反例 8。
10. **「`NotificationPage.vue` 就是 `/notify/notification`。」** 组件名 `NotificationMonitor`，键 `notify/monitor/index`，方法 `deliveries`。课误区 6。
11. **「布局 `notice/index.vue` 就是公告管理。」** 那是消息盒子，import 厅堂单例。公告管理是 `NoticePage.vue`。课误区 7。
12. **「`capabilities` 有 `notification-control-plane` 所以厨房含 application API。」** 名片不是 URL 表。课误区 12。
13. **「删除用 DELETE；`removeAccount` body 是 `{ accountId }`。」** POST + 标量 ID。`notices.remove` 才是数组。课误区 13–14。
14. **「搜人权限是 `notify:recipients:query`。」** 没有这串。后端是公告 add/edit 的 OR。目录对象 **不**自己查权限串。课误区 16。
15. **「`createMonitorService.deliveries` / 旧键 `monitor/notify/index`。」** 系统监控工厂没有这枪；registry 测试锁死旧键 `toBeUndefined()`。课误区 17 / 边界 6。
16. **「`inbox.list` 要传当前 `userId`。」** 厨房无此参数。课误区 18。
17. **「厨房有 snapshot/seen/`config.account`，所以页面一定扣了。」** 2026-09-17 Vue 零命中。墙上没挂的按钮 ≠ 刀被收了。课边界 3。
18. **「`recipientType=ALL` 会把全站用户拉进前端。」** 页面只显示告警；search 也没有「无关键字列出全部」。课边界 4。
19. **「前端会给回调签 `X-Notify-Signature`。」** 厨房源码搜不到那颗头。课误区 10。
20. **「home-web 对称一份厨房。」** 产品差在 App 插座板。课反例 7 / 误区 11。
21. **「本课覆盖四张页面的按钮剧本。」** 那是 L-054。本课认 URL 与谁注入谁。课误区 15。
22. **「L-007 已经 covered 插座板，所以本行方法表也 covered。」** L-007 只认组合点。22 枪与两枪短路是本课。
23. **「矩阵要先有 (b) 空关键字行 / (d) 浏览器通知流才算讲完。」** 矩阵无这些行。它们是 (a) 的 explained。
24. **「九工厂行可以凭本课标 covered。」** `createNotifyWebDomain` 是 L-054，且与另外八个工厂印在同一行。

问：读完后仍说不出的，是不是「工厂类名」，而是 **冻住对象没有 recipients、目录是厅堂字面量、空关键字不发车而空 ID 发车、`userTypes` 拐 system、标签不是方法、铃铛可碰单例、菜单页不可、home 不雇、submit/callback 不在这张浏览图表上**？

## Q4 视角

- objective_id: OBJ-53
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-53 vs L-007 / L-008 / L-024 / L-045…L-052 / L-054；九工厂行；chain 两格 vs 课三格 vs 矩阵一行
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:createNotificationService`** 或 **`A:notificationDirectory`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | 一行三符号 vs chain 两格 vs 课三格；模块栏印 domain 包但目录在 App；无 Vue 行；九工厂含 `createNotifyWebDomain` | **是** 闭合定义。本行可单独 covered。不要拆成两行才盖章，也不要把 Vue / 九工厂并进本行 |
| 厨房作者 | 22 枪 URL/动词/body；浅 freeze；监控才 `keyof paths`；无投影 | **是** (a) 工厂。submit/callback **否** |
| 厅堂电工（L-007） | `domainHttp` 懒电线、并列单例、目录字面量三方法 | 插头一句 **是**。12 个 `create*` 全表 **否**。本课才认 URL 表 |
| 目录门卫 | 空关键字本地空页；by-ids 逗号串；空数组不短路 | **是** `searchUsers` / `usersByIds`。`userTypes` **对照**，不是本行第三符号 |
| 选人 composable | 防抖、generation、`status==='0'`、restore 跳过空 ids | **对照**（谁在喊）。短路责任不要从 App 字面量挪走，也不要把 composable 升成矩阵行 |
| 菜单页作者（L-054） | 四键扣扳机；监控只用 deliveries；配置不用 `account(id)` | 谁喊哪一枪 **是** 对照。按钮剧本 **否** |
| 厅堂铬 | 铃铛/`push.ts` import 单例 `inbox` | **是** 厨房第二张脸。不是公告管理页 |
| 后端窗（L-045…L-051） | 五层链、clamp、HMAC、Outbox | URL 对齐 **是**。窗内状态机 **否**（多行已 covered） |
| 收件人窗（L-048） | search 无 pageNum；空白空页；非法 ID 文案 | 前端仍带 `pageNum` **是** 精度。后端合同 **否** |
| 系统监控（L-024） | 必须子路径；键 `monitor/{online,cache,operlog,logininfo}/index` | 对照「根有通知工厂、没有 system monitor」**是**。四抽屉 **否** |
| 九工厂行 / L-054 | `createNotifyWebDomain` | 注入 `{ service, directory }` **是** 对照。四键工厂盖章 **否** |
| home / sso | 不雇这座厨房 | 负空间 **是**。不要发明 home 通知行 |
| (b) 短路 / 浅 freeze | 矩阵无此行 | 本格 explained **是**。不要发明第二 (b) |
| (d) 浏览器通知流 | 矩阵无此行；已有 (d) 是 Java 提交走廊 | ASCII 两张纸条 **是** 本课图。不要发明浏览器 (d)，也不要用本课改 Java 主路径 |

问：Lead 若凭「工厂与目录都在函数表里」把矩阵那一行标 covered，是否还要求读者能指着 **无 recipients、两枪短路不对称、`userTypes` 拐 system、标签不是方法、两张脸、home 不雇**？这些是不是已经在本格机制链/反例里，而不需要 (b) 新行、也不需要把四张 Vue 或九工厂算进来？

## Q5 元问题

- objective_id: OBJ-53
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 一行三符号、无 Vue 行、无 (b)；九工厂同行仍 uncovered
- difficulty: low

建议：`defer`

原因：

- **矩阵 (a) 该行与 OBJ-53 / chain / 课 claimed 1:1（写法粒度不同，对象相同）。** L 合同章节齐全（宏观两张纸条、直觉十二句、定义表、机制链 1–5、带 caption / 文字等价物 / 图边界的两张图、正反例、变式、误区、暂停走查、来源表 S-005/S-006/S-010/S-L007-01/S-L053-01…08）。**主叙事站得住且与源码对齐**：工厂只认 `HttpClient`、冻住五块 22 枪、无选人/无 submit/无回调；目录是 App 字面量；空关键字不发车、空 ID 发车；`userTypes` 拐 system；`./monitor` 与 `./notification` 再 export 仍是同一只；铃铛碰单例、菜单页走 runtime；home 不雇。不必为「工厂名字」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例或邻格，不挡住把 **本行** 标 covered：（1）`Object.freeze` 是浅的；（2）厨房单测未锁 inbox/snapshot/remove/removeAccount；（3）App 目录无单测，picker 路径上 App 空白短路与 composable 重复；（4）前端仍传 `pageNum`，后端 search 当没看见；（5）`createNotifyWebDomain` 不 fail-closed 缺方法（L-054）。不要把四张 Vue、九工厂、Java 五层、Outbox、HMAC 塞进本行。
- **不要等拆矩阵行才盖章。** 模块栏印 `domains/notify` 是分组；课已经把目录所有权钉在厅堂。chain 写对象名、课写两方法、矩阵一行三符号，覆盖对象仍是厨房+目录两枪。`userTypes` 保持邻居。
- 不建议 `mine-more`：b01 未证明本行还有真实矩阵空洞；再五问只会把 L-054 按钮剧本、L-048 clamp 或 L-051 submit 再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；可选半句（浅 freeze、App 目录无单测、`pageNum` 会被忽略）不是第二容器。不建议 `split-lesson`：未满 10 问；厨房与目录是 chain 原设计的同一 OBJ-53，同一管理端浏览器柜台，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b)/(d) 行：短路与两张纸条图是 (a) 的 explained。不建议把 Vue 升成矩阵第二格才盖章：课已写明格子按 (a) 这一行；四键剧本是 L-054。不建议用本课标九工厂行或重盖 L-007 插头。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:createNotificationService | 22 枪 URL/动词/body、浅 freeze、无 recipients/submit/callback、监控才 typed path、根与 `./monitor`/`./notification` 同一工厂、home 不雇、铃铛第二张脸——均与源码对齐。残留精度：freeze 浅；厨房单测未锁 inbox/snapshot/remove/removeAccount/changeStatus。 | 可选半句：口试 freeze 只锁顶层键；方法表以 `transport.ts` 为准，不以三则厨房单测为准。不要求重写机制链。Lead 可将矩阵该行标 `covered`（证据锚本 probe 与 L-053 来源表 S-L053-01…08）。 | defer（本行可 covered；精度项不挡盖章） |
| A:notificationDirectory.searchUsers / A:usersByIds | 空关键字本地空页；by-ids `join(',')`；空数组不短路；类型从 notify 借、口在 web-domain、HTTP 在 App。残留：无 App 单测；picker 路径上 App 空白短路与 composable 重复；前端带 `pageNum`。 | 不要另建 userTypes 格，也不要把 composable 升成矩阵行。对照一句「空关键字不发车、空 ID 发车」即可。Lead 可将同一矩阵行一并标 `covered`。 | defer（与厨房同行可 covered） |
| notificationDirectory.userTypes | 同一字面量第三枪，URL 在 system。课用作对照，claimed/chain/矩阵都不立格。 | 保持邻居。不要为本课发明第三符号。 | defer（未入库对照；L-007/L-019） |
| A:createNotifyWebDomain / 九工厂同行 | 注入 `{ service, directory }` 已点。四键顺序、权限目录、页面扣扳机未当本格闭合。九工厂把八个邻居绑死。 | 不要用本课标该行 covered。L-054。 | defer（L-054 / 九工厂） |
| NoticePage / InboxPage / ConfigPage / NotificationPage | 课已讲谁喊哪一枪、ALL 不搜人、监控只用 deliveries、配置不用 `account(id)`、未见 `inbox.seen`。矩阵无 Vue 符号。 | 不要并进本行才能盖章。按钮剧本 L-054。 | defer（未入库对照；L-054） |
| A:admin-web.services / C:admin-web-composition | 插头 `createNotificationService(domainHttp)` + 目录字面量已借用。 | 保持。全表 L-007（已 covered）。本课不重盖。 | defer（L-007 已 covered） |
| A:home-web.services | 负空间：无工厂、无目录。 | 保持。L-008（已 covered）。 | defer（L-008 已 covered） |
| A:createMonitorService | 对照：必须子路径；通知监控键不是 system 第四屏。 | 保持。L-024（已 covered）。 | defer（L-024 已 covered） |
| A:NotifyNoticeController.* / Inbox / Config / Recipient / Monitor / Callback / Notification* / Outbox | 后端窗与领取。本课只保证浏览器打到哪些前缀、以及有标签无方法的门。 | 保持。L-045…L-052（多行已 covered）。 | defer（L-045…L-052 已 covered） |
| (b) 空关键字短路 / 浅 freeze / pageNum 忽略 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 浏览器通知流 | 矩阵无此行。已有 (d) 是 Java 提交走廊。 | 不要发明浏览器 (d)，也不要用本课改 L-051/L-052 主路径。 | defer（未入库） |
| C:Redis 8 / MySQL 8.4 | 铃铛推送 / 用户表不在本格。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-084 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本行无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出目的（两张纸条：厨房打 `/notify/notice|inbox|config|monitor`，目录打收件人两枪）、主路径（有字才 search、save 与 publish 分枪、铃铛与收件箱共用 `inbox.list`）与失败路径（空白不发车、空 ID 仍出门、非法编号由后端失败、缺起草权后端拒搜人）；四键剧本缺的是邻课不是讲义容器；把可选半句写进原课不会超过 40 分钟；后端窗、菜单工厂、厅堂插头、系统监控已有邻课。满 10 问前不拆。厨房与目录虽跨 domain 包与 admin-web 壳，仍是 chain 原设计的同一 OBJ-53，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / split 提案：否（miner 不改矩阵）；split 提案为 none；缺口表对 L-054 九工厂、L-007/L-008 插头（已 covered）、L-024 监控对照、L-045…L-052 后端窗（已 covered）、`userTypes` 邻居、未入库 (b)/(d)/Vue 符号以及 MySQL/Redis 容器显式建议 `defer`；对矩阵 (a) 行 `createNotificationService, notificationDirectory.searchUsers, usersByIds` 建议 Lead 标 `covered`（证据锚本 probe 与 L-053 来源表）。本课不主张拆该行为厨房/目录两行、发明 `createNotificationDirectory`、把 `userTypes` 升格、重盖 L-007、或用本课标九工厂行。
