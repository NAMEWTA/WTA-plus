# T-02 real HTTP + MySQL runner preparation (no service executed)

Fixed review input: `38032d24335c52cafea855b19d51fb36295162ef` and `/tmp/wta-t02-t03-current-audit.md`. New private tool: `/tmp/wta-t02/run-log-redaction-integration.py`. This preparation did not start Docker, Maven, Java, HTTP, MySQL or any other service and did not read production credentials. The T-37 product writer owns the repository; all new files here are under `/tmp/wta-t02/` mode `0600`/`0700`.

## Why the historical harness cannot be reused unchanged

`evidence/run-t02-mysql.py` is a useful historical record of the real Jetty/Servlet→HTTP logger→OperLogEvent→production Mapper chain, but its current code has no exact clean candidate check, uses a fixed root test password in Docker `-e` and Maven `-D...password` argv, stores the complete command/stdout/stderr including that value in JSON, and writes raw output under tracked Evidence. It accepts Maven exit alone, without fresh exact Surefire XML or positive/zero-skip checks. Its single owner label plus mutable container name are insufficient to prove cleanup ownership; Docker volume/process-group/port cleanup is not verified. A `finally` cleanup exception can mask the Maven failure and prevent the result record, while `container_removed=created` records a creation boolean rather than removal evidence. Those limitations make it unsuitable for a new clean-source acceptance claim. Historical T-02.md's 33 tests and 6 HTTP/3 operation/3 SQL observations remain historical, not current coverage.

## New runner and exact current scope

Seven currently present classes are selected by exact simple class names and each is required to produce a **fresh FQCN Surefire XML with tests>0 and zero failure/error/skip**: `LogSanitizerTest`, `LogAspectRedactionTest`, `SysLogFilterTest`, `SysLogFailureRedactionTest`, `SysLogEventWriterTest`, `OperationLogRedactionTest`, `LogRedactionHttpMySqlIntegrationTest`. Exact FQCN→module report paths are constants in the runner. The last class currently has one real Jetty/Servlet+MySQL method `credentialsNeverReachHttpEventsOperationEventsOrMySql`: signed token response remains usable, six HTTP events, three operation events, three SQL rows are nonempty and contain no canary, while ordinary fields/failure metadata remain. It **does not** yet exercise the actual `/monitor/online/{tokenId}` route leakage from `/tmp/wta-t02-t03-current-audit.md`.

The runner therefore has two explicit modes:

- Baseline only: `python3 /tmp/wta-t02/run-log-redaction-integration.py --execute --expected-head <FULL_CLEAN_SHA>`. If seven current classes pass and cleanup succeeds, exit 0 but result has `mode=baseline-only`, `acceptance=false`. It is not T-02 ticket acceptance.
- After the product writer adds a distinct real token-path canary method to `LogRedactionHttpMySqlIntegrationTest`, Lead reviews its HTTP sink/error logger/OperLogEvent/DB assertions, then runs `python3 /tmp/wta-t02/run-log-redaction-integration.py --execute --expected-head <FULL_CLEAN_SHA> --required-canary-method <EXACT_NEW_METHOD_NAME>`. The runner rejects the original baseline method as a canary marker, checks that the new exact method appears in fresh target XML, and permits `acceptance=true` only with all seven class gates and cleanup passing. A new independent canary class instead requires Lead to add its selector, FQCN XML and method check before execution; current runner does not silently count it.

Maven argv from `/srv/WTA-plus/backend` is nonsecret: `./mvnw -B -ntp -Pdev -pl wta-admin -am test -Dtest=<seven simple class names comma-separated> -Dsurefire.failIfNoSpecifiedTests=false -DforkCount=1 -DreuseForks=false -Dlog.mysql.integration.url=jdbc:mysql://127.0.0.1:<owned-port>/namewta_log_test_<runid>?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai -Dlog.mysql.integration.username=t02_<runid> -Dnamewta.sql.root=/srv/WTA-plus/release-artifacts/docker/infrastructure/mysql/init`. No password property is passed. Existing Java fixture defaults its password property to empty, so the runner creates a random-named, **passwordless app test user** with only `CREATE,DROP,INDEX,SELECT,INSERT` on its fresh disposable `namewta_log_test_<runid>` schema. This narrowly scoped, localhost-only synthetic account avoids a secret in JVM system properties/Surefire XML. The MySQL root password is separately random, supplied only through a `0600` Docker env file and in-container `MYSQL_PWD`; neither root nor app uses a production credential. If the product writer later changes the Java fixture to read `T02_MYSQL_PASSWORD` from child environment, Lead can change this runner to a passworded app user without placing a password in Maven argv/JVM system properties.

MySQL `mysql:8.4.9` is a new container with two exact labels `namewta.test.owner=T-02` + `namewta.test.run=<64-bit random runid>` and a Docker-assigned `127.0.0.1` port. The test itself reads only the public `10-cde-base-ddl.sql` fragment and creates/drops `sys_oper_log` in the fresh test DB; the runner hashes that DDL and **does not replay six SQL files, touch an existing database, Redis, MinIO, or external suppliers**. Maven runs in its own process group with one-hour failure bound and full child-group TERM→KILL cleanup even if its leader exited. Cleanup removes only full 64-character captured IDs that also pass both labels; `docker rm -fv`, exact anonymous-volume absence, loopback port closure, no owned containers, source before/after same clean HEAD/tree, and successful log redaction are all verdict conditions. Run output is only a private `0600` cleaned log plus `0600` result JSON under `0700 /tmp/wta-t02/runs/<runid>`; raw Maven log is private during execution then deleted. Known `credential-canary-*` values and root password are redacted from the cleaned log. Failure still records fresh available class counts and an owned cleanup verdict, never a false pass.

The runner's focused class selection does not replace Ticket 02's full affected reactor command or signing/encryption/SSE/default backend regressions. Those are separate current-candidate gates for Lead after the product fix. Old-log disposal and credential rotation have separate authorized evidence; neither is performed here.

## Synthetic checks (no service)

`python3 /tmp/wta-t02/test_driver_safety.py -v` via `/tmp/wta-check.py`: **9 tests passed, exit 0**, record `/tmp/wta-t02/verification/driver-synthetic-no-secret-property-20260923.{json,log}`. Tests cover opt-in/exact-clean refusal, baseline not being accepted as ticket proof, full-ID dual-label failure cleanup and captured-ID loss, anonymous-volume absence, seven exact fresh positive/zero-skip reports, process group after Maven leader exit, secret/canary log redaction and no app secret in Maven property. `python3 -m py_compile` exit 0. Earlier synthetic records under the same directory are preserved; the final record is the no-secret-property version. No real canary token-path test exists at the fixed input, so no T-02 AC is claimed passed by this preparation.
