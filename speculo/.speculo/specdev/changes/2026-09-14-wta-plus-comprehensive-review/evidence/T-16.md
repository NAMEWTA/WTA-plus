# T-16 本地实现与验证证据

revision69，status=review。全部提交按用户指令暂缓；parent_before/HEAD 为 `76dbbe84a34624234e379661a57b232529e34ed3`，implementation_commit/result_sha/candidate 均为空，0 Done。未暂存、提交、推送或部署。

## 实现与 AC-016

- 实际 ProcessActionDialog 使用本地 useProcessActionDialog 管理每次打开的 generation。打开即清空旧任务、节点、附件和选人状态；关闭/卸载立即失效；旧请求的成功、错误和 finally 均不能更新新任务。B 加载失败不会留下可提交的 A。
- 提交在等待确认前同步锁定并复制 taskId、variables、办理人、抄送、附件和意见。确认返回后再次核对 generation，服务端返回后也核对；旧确认不发送请求，已经发出的 A 请求不关闭或完成 B，连续点击只产生一次办理。
- 两个节点读取入口 getNextNodeList/getBackTaskNode 的陌生人读取已由真实 Warm-Flow 红灯运行证实（T-16-engine-both-red）。最终各增加一行既有 checkTaskReadAccess(task)，复用原读取规则；合法发起人、办理人、关系用户、管理权限和超级管理员保持可读。
- 原 Lock4j、Warm-Flow handler/状态校验与 LiteFlow XML 保留。真实占锁拒绝、正常推进一次、过期任务再次办理拒绝且历史不增均通过；没有新增 taskVersion。
- 变量读取保留原 Controller 的 workflow:instance:variableQuery 权限合同，真实 SaInterceptor/MockMvc 验证无权限 403、有权限 200。没有把内部 listVariable 方法写成已确认缺陷，也不声称新增了 owner 合同。

## 实际验证

所有命令串行；详细命令、cwd、退出码、日志与资源清理见对应 JSON。

| 验证 | Evidence | 实际结果 |
|---|---|---|
| 组件红绿与最终定向 | T-16-dialog-red.json、T-16-dialog-focused.json | 原行为 26 项中 12 个竞态红灯；最终 Workflow 包 36/36，通过 typecheck/test/lint，均 exit 0 |
| 真实引擎授权与完整办理 | T-16-engine-matrix.json | `./mvnw -B -ntp -pl wta-admin -am -Dtest=WorkflowTaskEngineIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false -Dworkflow.engine.integration=true … test`，1/1、0 skip、exit 0 |
| 实际组件浏览器竞态 | T-16-dialog-browser-final-scoped.json | `run-t16-dialog-browser.py T-16-dialog-browser-final-scoped`：构建与 Chrome 均 exit 0；10/10，0 skip；owned preview 已关闭 |
| 全前端门禁 | T-16-frontend-second.json | `frontend: corepack pnpm lint`、`typecheck`、`test` 均 exit 0；489 Vitest +108 Node=597 passed，0 skip |
| 三 App 生产构建 | T-16-build-final.json | `frontend: corepack pnpm build:prod` exit 0；Admin/Home/SSO 均完成 |
| 原流程页面浏览器回归 | T-16-workflow-e2e-final.json | 已构建 Admin 上运行 workflow-runtime/definition 两原 suite，14/14 Chrome，0 skip、exit 0；临时配置/预览均清理 |
| 后端消费者 | T-16-backend-final.json | `backend: ./mvnw -B -ntp -pl wta-modules/wta-workflow,wta-admin -am test` exit 0，711 总计、679 pass、32 环境门控 skip、0 failure/error |
| 最终静态门禁 | T-16-static-final.json | 新 E2E/config 严格 tsc、oxlint、Workflow classic、Skill facts、git diff --check 五命令全部 exit 0 |
| 治理一致性 | T-16-governance.json | revision69 goal-plan、ticket-control、diff 的实际结果单列 |

T-16-verification-counts.json 保存前端计数与32条 skip 的精确类/方法/原因。常规 reactor 中新引擎用例默认按属性门控跳过；required 用例已在 engine-matrix 显式启用并通过，不将 skip 计为通过。最后仅修改 E2E 拦截器的静态路径排除，随后10浏览器/14原回归及严格 tsc/oxlint 已复核；生产代码与全前端/三 App 构建时相同。

## 接缝与残余边界

引擎用例使用实际 Warm-Flow 1.8.9、MyBatis Mapper、六份当前基座 SQL 初始化的 MySQL 8.4.9、SaToken JWT/WorkflowPermissionHandler、DynamicRoutingDataSource 事务代理、实际 LiteFlow completeTaskChain XML/组件和 Lock4j/Redisson。锁竞争通过同一测试中的外部逻辑 owner 持锁复现，不启动并发 agent；重复推进检查数据库历史行数。System 办理人展开使用固定合法 ID 替身，完整业务 WorkflowGlobalListener/真实外部通知不在本票证据范围。自建容器/网络/卷前后库存相同。

浏览器竞态宿主位于拥有组件与依赖的 workflow 包，直接渲染正式 SFC/正式 domain service，使用真实 Chrome UI 和确认弹窗，API 响应由 Playwright 控制时序。不能将它冒充浏览器直连真实引擎的整链证据；后端真实引擎和前端竞态分别覆盖各自不变量。原14个 Admin 场景包含上传/退回和变量编辑，沿用原受控 API 接缝。整体同候选与外部供应商验证仍归 T-30/原责任票。

## 检查点

输入 T-16-input-checkpoint.json 的343路径已核对。最终 T-16-checkpoint.json 为10路径；342条非重叠上游哈希完全不变，仅 playwright.config.ts 为已有共享改动。原 DefinitionPage.vue 未改。所有 changes 仍未提交，正式 commit/direct-parent 出口暂缓。

## Skill Execution Records

| Skill / phase / operation | 实际输入、执行与输出 |
|---|---|
| engineering-standards / implement / apply-scope-contract | Vue 生命周期、快照/取消/资源和权限硬约束落实；保持 Workflow classic/现有锁，fixture 迁回实际 owner 修复真实架构违规，passed |
| namewta-fullstack-development / implement / apply-scope-contract | 实际 SFC/domain 服务与后端节点读取贯通；权限/变量 HTTP、原 Admin 行为和真实引擎分别验证，不扩大公开字段或兼容路由，passed |
| wta-module-guide / implement / apply-scope-contract | 回读 Workflow 能力/集成图及当前源码，复用 checkTaskReadAccess、Warm-Flow、Lock4j、LiteFlow 正式入口；B-10由运行红灯确认，passed |
| engineering-standards / verify / verify-affected-contract-and-quality-gates | 36定向、597前端、三App、24浏览器、679消费者/32明确skip、1真实引擎及最终静态/哈希审计，passed |

Skill 入口 SHA 未变化，以票据 frontmatter 为准。以下保留实施历史，历史中的 remaining/未运行不代表最终状态。

# T-16 实施过程历史（当时状态，以下最终结论优先）

revision67，单人串行，全部提交暂缓。343 条上游最新哈希全部核对；input见 T-16-input-checkpoint.json。原有 workflow/DefinitionPage.vue 是上游改动，本票尚未修改。

前端：从 ProcessActionDialog 提取本地生命周期状态到 useProcessActionDialog，先保持原行为复现，再修复。T-16-dialog-red 中 26 项有12个真实竞态失败；T-16-dialog-green 与扩展后的 T-16-dialog-focused 36项全部通过，后者 typecheck/test/lint 均 exit0。正式组件已接入，切换/关闭/卸载立即失效，任务/选择/附件状态清空，确认前固定 payload，确认与响应后校验当前 generation。浏览器/全前端/最终构建尚未执行，不标通过。

真实 Warm-Flow 1.8.9 + 真实 MyBatis Mapper/MySQL 六文件基座 + 实际 WorkflowPermissionHandler/SaToken 登录的 fixture 已运行。第一轮漏 User-Agent，停于测试登录；第二轮漏真实 ChartService，停于引擎启动；第三轮实际生成申请/审批任务，发起人/办理人两个节点读取都成功，陌生人 getNextNodeList 竟成功导致负向断言失败。B-10 此入口已运行证实，getBackTaskNode 尚待独立红灯。所有失败 owned MySQL/Redis 容器/卷均恢复。

## Integration attempt review

已达三次集成失败门槛，先记录再推进：前两次共同模式是最小夹具缺必要装配（请求头、图形服务），均有具体补齐后重跑；第三次是产品授权缺口的预期红灯，不是环境失败。下一次具体改变：同时捕获两个节点读取异常并在最后用 assertAll 核对，避免首条失败遮蔽第二入口及真实引擎拒绝陌生办理/重复推进的既有行为。取证后在原检查接入点复用 checkTaskReadAccess，不加新 taskVersion/授权系统。恢复入口 run-t16-engine.py；不删除断言或放宽权限。

## Remaining

两入口完整运行证据与最小修复、现有 Lock4j/完整办理链验证、真实浏览器快速切换/确认/重复动作、整体前端与消费者门禁、最终哈希/Skill/治理与review。当前 T-16 in_progress，未宣称AC完成。

T-16-engine-both-red：两入口均未抛预期授权异常；实际引擎已拒绝陌生办理及同任务再次推进，历史行数未增长。现仅在两入口加载任务成功后调用既有 checkTaskReadAccess(task)，不改变引擎锁/状态字段或读取规则，随后验证绿色。

完整办理链的三次失败复核：chain-first 是夹具 DynamicRoutingDataSource 构造漏 List 参数（0测试）；chain-second 是 LiteFlow SPI/config 未先装配导致 FlowBus 静态初始化失败；chain-third 已运行实际办理链，但占锁夹具将真实 key 的 `#` 分隔符错写为 `:`，因此没有占到实际任务锁。已读当前 LockInterceptor 字节码确认，下一次仅修正测试 key，保留原锁异常/历史不增长断言；同时保留实际 LiteFlow XML、事务与引擎，不改产品锁策略。

浏览器第二轮：10/10 passed。第一轮宿主未提供中文确认文案，1失败/1通过/1中断/7未执行；发现原因后主动中断避免重复失败，补宿主同产品的按钮文案，断言不改。全前端首轮 lint 正确拒绝根 e2e fixture 跨 workspace 私有相对 import 与缺声明依赖；已将宿主移动到实际拥有组件/依赖的 workflow 包内测试 fixture，E2E 只通过浏览器访问，未扩大 allowlist 或修改架构规则。移动后重跑整体门禁和浏览器。

迁移宿主后的 browser-final 首例未挂载：测试拦截器 `**/workflow/**` 同时命中了新增静态宿主路径中的包名 workflow，把 HTML 当 API 返回。已将拦截限定为根路径 /workflow/，其他静态请求 continue；保持产品代码与测试断言不变后再运行。
