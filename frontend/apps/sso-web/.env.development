# SSO Web 开发环境公开配置。全部 VITE_* 会进入浏览器产物，禁止写入密钥。
# 生产构建使用同目录 .env.production。本机私有覆盖使用 .env.development.local。

VITE_APP_TITLE=WTA SSO
VITE_APP_PORT=4176
VITE_APP_CONTEXT_PATH=/
# 空字符串表示走同源 /sso，由下方代理转到后端
VITE_SSO_API=
VITE_SSO_API_PROXY=http://127.0.0.1:18080
