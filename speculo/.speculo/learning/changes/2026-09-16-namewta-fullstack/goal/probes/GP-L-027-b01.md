---
artifact: learning-goal-probe
probe_id: GP-L-027-b01
lesson_id: L-027
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-027 / b01

审问对象是已写成的 Lesson `lessons/L-027-oss-upload.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（`claimed_cells`）：`A:SysOssUploadController.init,signParts,parts,complete,abort`、`B:SysOssUploadController.complete / abort`、`D:主路径 OSS 直传`、`D:失败路径 OSS abort`。OBJ-27：能口述直传五方法与失败回滚（`course.md` 档是**方法性状**）。chain 覆盖列**只写 (a)**；矩阵 (a)(b) 与两条 (d) 均仍 `uncovered`。(d) 主路径备注 `L-027, L-029`；失败路径备注只有 `L-027`。本批按派单审问 **complete/abort 性状 + 两条 OSS 数据流**；五扇窗是 (a) 地图，不是把 `createOssUploadClient` 算进来。`SysOssConfigController` 留给 L-025；`SysOssController` 四扇留给 L-026；`SysOssMigrationController` 留给 L-028；浏览器 `createOssUploadClient.upload` / IndexedDB / `AbortSignal` 留给 L-029；厅堂 `services.ts` 插头留给 L-007；头像消费者留给 L-022；MinIO 容器与 Redis 8 容器仍是 (c) uncovered。

抽查锚点（2026-09-16）：`backend/wta-modules/wta-system/src/main/java/org/namewta/system/controller/system/SysOssUploadController.java`；`oss/upload/{OssUploadService,OssUploadTicket,RedisOssUploadTicketStore,DefaultOssUploadMetadataStore,DefaultOssUploadObjectStore,DefaultOssUploadIdentityResolver,OssUploadCleanupTask,OssUploadError,OssUploadContracts}.java`；`controller/system/SysOssController.java`；`backend/wta-admin/src/test/java/org/namewta/test/oss/upload/{OssUploadHttpContractUnitTest,OssUploadServiceUnitTest}.java`；`backend/wta-admin/src/main/resources/application.yml` `oss.direct-upload`；`frontend/packages/domains/system/src/resource-service.ts`；`03-backend-module-modes.md` classic 行。

## Q1 澄清

- objective_id: OBJ-27
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-27；chain L-027 覆盖列只 (a)；L-027 `claimed_cells` 四格；矩阵 (a) 五方法一行、(b) complete/abort 一行、(d) 主路径「complete → 业务引用」挂 L-027+L-029、(d) 失败路径只挂 L-027；(c) MinIO/OSS 与 Redis 8 仍 uncovered
- difficulty: medium

课把票房收成「五扇 JSON 窗 + complete/abort 回滚 + 两条 D」。计划里至少有四套可数对象，派单点名的是 (b) 与两条 (d)：

1. **OBJ-27 原文。** `course.md`：能口述 `SysOssUploadController.init/signParts/parts/complete/abort` 与失败回滚。档是方法性状。点名的是五方法 + 回滚，没有把 PUT xhr、IndexedDB、`sys_oss` 列表窗写成口试清单。
2. **chain 覆盖列。** 只有 `A:SysOssUploadController.init,signParts,parts,complete,abort`。不写 (b)，不写两条 (d)。
3. **Lesson `claimed_cells` / 矩阵四格。** (a) 与磁盘五映射 1:1；(b) 原文「complete 落引用；abort 清票据」；(d) 主路径原文 `init ticket → 浏览器 PUT 对象 → complete → 业务引用`；(d) 失败路径原文 `complete 失败 → abort，不留业务引用`。
4. **课自己的操作定义。** (a) 文件字节不进这扇门；(b) complete 插入 `sys_oss` **临时行**（`isTemp=Y`，`url=""`），校验失败服务端 `cleanupAfterFailure`，不必再打 DELETE；HTTP abort 未完成才清对象+撕票，COMPLETED 只摘补偿索引；(d) 主路径把「业务引用」改口成「目录上出现临时行，调用方拿到 ossId；用户表还没改」；(d) 失败路径把矩阵那句改口成「服务端 complete 失败路径已经做了和 abort 相同的 Provider 清理」。
5. **邻格。** 矩阵 (d) 主路径同时挂 L-029。L-029 `claimed_cells` 是 `A/B:createOssUploadClient.upload`，并写明 complete 校验失败**不会**由 adapter 再打 DELETE。

问：Lead 盖章时，「业务引用」是 `registerTemporary` 那一行临时 `sys_oss`，还是头像/富文本表挂 ossId？(d) 主路径是否必须等 GP-L-029 把浏览器 PUT 走完才能标 covered，还是 Relational 走廊（init JSON → PUT 不进 Controller → complete 临时行）本课已经够？chain 只写 (a) 时，标 (b)/(d) covered 的证据锚是 `claimed_cells` 还是要先改 chain 覆盖列？

## Q2 证据

- objective_id: OBJ-27
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: `SysOssUploadController.java` 17–62；`OssUploadService.java` 50–174 / 211–246 / 388–395 / 415–439；`OssUploadHttpContractUnitTest.java` 35–76；`OssUploadServiceUnitTest.java` 69–239；`RedisOssUploadTicketStore.java` 20–73；`DefaultOssUploadMetadataStore.java` 28–59；`DefaultOssUploadObjectStore.java` 42–47 / 98–108；`application.yml` 73–79 / 81–98 / 139–148；`resource-service.ts` 33–34 / 179–204
- difficulty: high

课的 complete/abort 与两条 D 哪些能在工作树直接复现，哪些只是测试名单比课表窄、或邻格事实？

**仍能复现（本课四格）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 门牌 `/resource/oss/uploads`；正好五映射 + 一只 `@ExceptionHandler`；无 `BaseController`、零 `@Log` | Controller 17–62；合同测试 35–43 钉死五条 path；`SysOssConfigController`/`SysOssController`/`SysOssMigrationController` 才有 `@Log` |
| init POST 根（PostMapping 值为空）；signParts POST `/{uploadToken}/parts/sign`；parts GET `/{uploadToken}/parts`；complete POST `/{uploadToken}/complete`；abort DELETE `/{uploadToken}` | 合同测试 38–42；Controller 25–57 |
| 五扇都是 `system:oss:upload`；策略还有 `requiredPermission`（`richtext-*` = `common:richtext:upload`） | Controller 每方法注解；`application.yml` 139–148 |
| Java `parts` → `uploadService.resume`；GET 且 `@NotBlank fingerprint` | Controller 38–43；Service 130–154 |
| complete `R<String>` 走 `R.data`；body `required=false`；null → 空 parts | Controller 45–50；Service 156–157；合同测试 43 / 47–56 data=`"9001"` |
| abort `R.ok()` 无 body | Controller 52–57 |
| `handleUploadException`：`data.error` 是枚举名，`msg` 是中文 | Controller 59–62；合同测试 67–76 `FINGERPRINT_MISMATCH` |
| 五个方法没有任何文件参数；prepare 才 `presignPut` / `createMultipartUpload` | Controller 全文；ObjectStore 42–47 |
| wta-system = classic；`Controller → OssUploadService`，无 UseCase；metadata 自己持 `SysOssMapper` | 登记表 classic 行；Service `@Service` 25–27；MetadataStore 20–24 |
| init 就绪/权限/文件校验在 `prepare` 与 `ticketStore.create` **之前**；未就绪 `prepareCalls=0`、tickets 空 | Service 50–59 / 94–104；单测 69–85 |
| create 失败 `cleanupPrepared` 再 `STATE_STORE_FAILURE` | Service 83–88 / 449–456 |
| 票冻结 `service`；改 yaml 后 abort 仍打旧路由 | Service 78–81；单测 88–101 `lastCleanupService=minio` |
| `general` AUTO：max-size 10MiB < threshold 100MiB → 合法文件走不了 MULTIPART | `application.yml` 81–96 |
| signParts 去重排序、≤ `max-sign-parts` 20、票改 `UPLOADING`、URL 不写回 Redis；`SignedPart` 五字段无嵌套 request | Service 106–121；Contracts 42–55；合同测试 59–64；单测 133–134 `[1,2]` |
| resume **不** `saveTicket`、**不** `locked`；指纹比原串；COMPLETED 不再 `presignSingle`；SINGLE 未完成新签 PUT；MULTIPART `presignedRequest=null` | Service 130–154；单测 140–141 / 168–176 / 179–196 |
| complete 早退：已 COMPLETED+ossId → 摘 cleanup 返回旧 id；`findByObject(service, objectKey)` 已有行 → `markCompleted` | Service 213–221；单测 114–121 `registerCalls=1`；226–238 Redis 写下失败后靠 findByObject 找回 `"1001"` |
| `markCompleted` 在 try **外面**：Redis 失败 `STATE_STORE_FAILURE`，**不** `cleanupAfterFailure` | Service 244–245 vs 237–242；单测 226–238 `registerCalls` 仍 1 |
| 校验/HEAD/魔数/register 失败 → `cleanupAfterFailure`（`propagateFailure=false`） | Service 237–239 / 415–417 |
| 坏 PNG 魔数：`COMPLETE_VALIDATION_FAILED`，`deleteCalls=1`，`registerCalls=0`，`tickets.get(token)=null` | Service 284–297；单测 210–223 |
| SINGLE 带 parts → `INVALID_PARTS`「SINGLE Complete 不接受 Part」 | Service 230–231 |
| MULTIPART 无对象才 `completeMultipart`；ETag 剥引号 | Service 226–229 / 488–493；单测 145–148 `"etag-1"` |
| `registerTemporary`：查重 `(service, fileName=objectKey)`；insert `url=""`、`isTemp="Y"`、`ext1.source=directUpload`、`expireTime=now+tempRetention`（Java 默认 24h） | MetadataStore 38–59；`OssLifecycleProperties` 33 |
| HTTP abort：票 null → 空成功；COMPLETED → 只 `removeCompletedCleanup`；否则 `cleanupSession(..., true)` | Service 160–173 |
| Provider abort 仅 MULTIPART 且有 uploadId；随后 `delete(objectKey)` | ObjectStore 98–108 |
| Redis 三键一锁一索引；create `setIfAbsent`；票记录注释「不包含可长期复用的签名 URL」 | TicketStore 20–23 / 35–47；Ticket.java 1–4 |
| HTTP abort 主路径是 `removeSession` 删键，不是写成 `ABORTED`；工作树只有 `requireOwned` **读** ABORTED/EXPIRED | Service 69–73 / 343–345；全仓无 `withState(ABORTED)` |
| 身份 `LoginHelper.getLoginUser()`，无 userId → `ACCESS_DENIED`；后续 `userId+clientPk` | IdentityResolver 15–20；Service 349–354；单测 153–165 / 200–207 |
| 定时清理默认关：`cleanup-enabled: false` 且 `cleanup-dry-run: true`；COMPLETED 或已有 metadata **禁止**删对象 | yml 78–79；CleanupTask 21–24；Service 183–190；单测 242–254 / 257–273 |
| 前端五枪 URL 对齐；`resumeUpload` 对 GET parts；token `encodeURIComponent` | `resource-service.ts` 33–34 / 179–204；`resource-service.test.ts` 129–137 |

**邻格仍能复现（不计入本行闭合）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| `/resource/oss` 是 `SysOssController`（list/listByIds/download-url/remove），不是本课 | `SysOssController.java` 31–32 |
| 厨房 `createOssUploadClient.upload` / IndexedDB / 取消才 `abortUpload` | L-029；adapter 不在 OBJ-27 |
| `OssLifecycleManager` / `OssTempCleanupTask` 清 **已登记仍 isTemp** 的行 | `oss/service/OssLifecycleManager.java`；与 `OssUploadCleanupTask` 不是同一 cron |

**课写过、测试名单更窄或图比机制段更满（不是证伪）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 失败路径图：complete 校验失败 → 票键消失 | 单测坏魔数确实 `tickets.get==null`。但 `cleanupSession` 在 Provider `abort/delete` 抛错时 `propagateFailure=false` 会 `return`，**不** `removeSession`，只 `scheduleCleanup`（Service 419–429）。机制段写了不把 Provider 异常再抛给 complete，图没写「票可能还在」 |
| HTTP abort COMPLETED 只摘索引、不删对象 | 源码 167–170 清楚。单测没有「complete 成功后再 abort」的 `deleteCalls=0` 夹具；冻结路由那则 abort 的是未完成票 |
| abort 票不存在 → 200 空成功 | 源码 163–165。无单测 |
| complete 的 `SESSION_EXPIRED` / `SESSION_NOT_FOUND` / `SESSION_OWNER_MISMATCH` 也算「失败 → abort」 | `requireOwned` 在 try **外**（211–212）。过期/无票/别人的票 **不会** `cleanupAfterFailure`。无票再 complete 是 `SESSION_NOT_FOUND`，不是幂等成功（与 abort 空成功不对称） |
| 锁表：signParts / complete / abort / cleanupExpired 都经 `locked` | 真。init 与 resume **不**进锁。课没把 resume 写成例外 |
| abort 不看 `expiresAt` | abort 用 `get`+`requireOwner`，不是 `requireOwned`。过期但仍在 Redis 的票，主人仍能撕 |

问：在五扇 path、complete 幂等/魔数回滚/`markCompleted` 在 try 外、HTTP abort 三分支、主路径三跳（JSON → PUT 边界 → 临时行）已经能互证的前提下，(b) 与 (d) 是否还要把「Provider 清理失败票还在」「complete 过期不扫桶」「abort COMPLETED 无单测」写成开篇半句，才算性状讲完？还是这些已经落在机制段/边界，Lead 可以直接给 (a)(b) 和 (d) 失败路径盖章？

## Q3 假设与反例

- objective_id: OBJ-27
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: Controller 无 MultipartFile；parts≠上传分片；complete=`R.data`；临时行≠业务表；cleanupAfterFailure 不必前端 abort；COMPLETED abort 不删对象；classic 非 UseCase；零 `@Log`；abort 是 DELETE；AUTO 不进票；service 冻结；handler 不是第六 API；resume 比原串；双权限；createOssUploadClient 属 L-029
- difficulty: high

课的隐含假设若反转，合格读者会把哪一跳认错？

1. **「字节从 `SysOssUploadController` 进 Spring 再转存 MinIO。」** 五方法无文件参数。课反例 1 / 误区。
2. **「`parts` 用来上传分片 / Java 方法叫 `resume`。」** GET 续传查询。课反例 2–3。
3. **「complete 用 `R.ok`，和 init 一样。」** 源码 `R.data`。课反例 4。
4. **「complete 等于头像/公告附件已经挂上。」** 临时行 `url=""`。课反例 5。矩阵 (d) 若把「业务引用」读成用户表，会把 L-022/L-026 消费者算进本课。
5. **「complete 失败必须再打 abort 才删桶。」** `cleanupAfterFailure` 已删（Provider 成功时）。课反例 6；L-029 对齐「校验失败不由 adapter DELETE」。
6. **「abort 已完成的票会把 MinIO 对象删掉。」** 只摘 cleanup。课反例 7。
7. **「`/resource/oss` 和 `/resource/oss/uploads` 是同一个 Controller。」** 课反例 8。
8. **「这是 layered UseCase。」** classic + `OssUploadService`。课反例 9。
9. **「五扇都有 `@Log` / abort 是 POST 因为 API-005。」** 零 `@Log`；`@DeleteMapping`。课反例 10–11。存量偏差，本课不发动传输 Ratchet。
10. **「票上会出现 `AUTO` / init 之后改存储键续传跟新桶。」** `resolveMode` 之后只有 SINGLE/MULTIPART；`service` 冻结。课反例 12–13。
11. **「`handleUploadException` 是第六个业务 API。」** 课反例 14。
12. **「清理任务默认会扫桶。」** `cleanup-enabled: false`。课反例 15。过期未 complete 的对象默认靠 Bucket Lifecycle（课声明不覆盖运维门闩）。
13. **「指纹校验用 SHA-256 做 resume。」** resume 原串；complete HEAD 用 `fingerprintDigest`。课反例 16。
14. **「richtext-image 只需 `common:richtext:upload`。」** 还要跨过方法上的 `system:oss:upload`。课反例 17。
15. **「`createOssUploadClient` 属于 OBJ-27。」** OBJ-29。课反例 18。
16. **「矩阵 (d) 失败路径 = 前端必须打 DELETE。」** 矩阵原文确实写 `complete 失败 → abort`。课把 abort 解释成服务端内部清理。若 Lead 按字面要看见 HTTP abort 才盖章，会把已写清的 `cleanupAfterFailure` 判成未覆盖。
17. **「complete 对过期票也会 `cleanupAfterFailure`。」** 不会。`requireOwned` 先抛 `SESSION_EXPIRED`。对象留下，除非主人再 DELETE abort（abort 不看过期）或打开定时清理。

问：读完后仍说不出的，是不是「五扇的名字」，而是 **complete 落的是临时行不是用户表、校验失败不必再打 DELETE、COMPLETED abort 不删对象、(d) 主路径第四跳不在五方法里、过期 complete 不扫桶**？

## Q4 视角

- objective_id: OBJ-27
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)(b)(c)(d)；OBJ-27 vs L-025/L-026/L-028/L-029/L-007/L-022/L-005；API-005；Redis 键；MinIO 容器
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于本课四格？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | chain 只 (a)；课认四格；(d) 主路径挂 L-027+L-029；「业务引用」一词两义；(c) MinIO/Redis 仍 uncovered | **是** 闭合定义。(a)(b) 可单独 covered。(d) 失败路径本课 1:1。(d) 主路径 Relational 走廊本课可作证；PUT xhr 不要塞进本行 |
| 票房作者 | 五映射、权限双锁、R.data vs R.ok、零 `@Log`、DELETE abort | **是** (a) |
| complete/abort 作者 | 幂等两早退；try 内失败才 `cleanupAfterFailure`；`markCompleted` 失败留对象+行；HTTP abort 三分支；`propagateFailure` 真假 | **是** (b) 与 (d) 失败路径 |
| 数据面搬运工（L-029） | xhr PUT、`requiredHeaders`、IndexedDB 续传、`AbortSignal` 才 `abortUpload`、SIGN_WINDOW | PUT **不进 Controller** 这一句 **是**。(a) 工厂/`upload()` **否** |
| 卡片柜（L-026） | list/download-url/remove；临时行 `url=""` 不能当永久地址 | 对照一句 **是**。四扇 **否** |
| 换默认桶（L-025） | 就绪表 SERVING / accessPolicy | init 门闩 **是**。切默认配置 **否** |
| 搬家批（L-028） | PRIVATE→PUBLIC_READ 批次 | **否** |
| 厅堂电工（L-007） | `gateway: systemService.resources.oss` | 厨房五枪打得到本 Controller **是**。12 工厂全表 **否** |
| 头像消费者（L-022） | complete 后再 `updateProfile({ avatar: ossId })` | 第四跳不在五方法 **是** 负空间。资料页 **否** |
| API-005 | 变更目标 POST+`@Log`；本课 POST 无日志 + DELETE abort | 盘点存量 **是**。不发动 Ratchet **否** |
| Redis 8 容器 | `oss:upload:ticket/cleanup/lock/expire-index` | 票键作为 (b) 机制 **是**。会话/限流容器课 **否**（L-084） |
| MinIO/OSS 容器 | 预签名 PUT 落在桶里 | 数据面边界 **是**。(c) 容器画像 **否** |
| 两条 cron | `OssUploadCleanupTask` 清未 complete；`OssLifecycleManager` 清已登记 isTemp | 分界一句 **是**。Lifecycle 实现 **否**（L-026） |

问：Lead 若凭「五扇窗都在函数表里」把 (a) 标 covered，是否还要求读者能指着 **complete 临时行、校验失败服务端 abort、COMPLETED abort 只摘索引、主路径 PUT 不进 Spring**？这些是不是已经在 (b)/(d) 的图和反例里，而不需要把 L-029 的 `upload()` 算进来、也不需要发明第二条 (b)？

## Q5 元问题

- objective_id: OBJ-27
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a)(b) 与两条 (d) 仍 uncovered；(d) 主路径同时挂 L-029
- difficulty: low

建议：`defer`

原因：

- **(a) 与磁盘五映射 1:1；(b) 与 `completeLocked` / `abort` / `cleanupAfterFailure` 对齐；(d) 失败路径本课独挂。** L 合同章节齐全（直觉、定义、五窗机制链、带 caption / 文字等价物的主/失败两张图、正反例、变式、误区、来源表）。**主叙事站得住且与源码对齐**：控制面 JSON vs 数据面 PUT；complete 校验通过才 `registerTemporary`；校验失败服务端清理，不靠前端第二枪；HTTP abort 未完成清桶撕票、已完成只摘索引；幂等 complete 不二次 insert；`markCompleted` 失败走 `findByObject`。不必为「五扇的名字」再挖五问。
- 本批可粘贴的精度项（Provider 清理失败时票可能还在、complete 对过期票不扫桶、HTTP abort COMPLETED/空票无单测、resume 不进锁）已经落在机制段或源码边界。它们不是第二容器，也不是「说不出 complete/abort 目的」。可选图注/半句不挡住把 **(a)(b) 与 (d) 失败路径** 标 covered。不要把 `createOssUploadClient`、IndexedDB、列表窗、迁移批、切桶塞进 OBJ-27。
- **(d) 主路径** 矩阵挂 L-027+L-029。本课已把 Relational 走廊讲完，并把「业务引用」操作定义成临时 `sys_oss` 行。浏览器 PUT 实现与取消时 `abortUpload` 是 L-029 的函数级格子。Lead 可用本 probe 作该行的**后端半边证据**；整行盖章等 GP-L-029，或接受课的操作定义后先标 covered、L-029 只补 xhr。miner 不选。不要把第四跳（用户表挂 id）写进本课才能盖章。
- 不建议 `mine-more`：b01 未证明本课四格还有真实矩阵空洞；再五问只会把 L-029 adapter、L-026 卡片柜或 Bucket Lifecycle 再问一遍。不建议 `re-dispatch-L`：不缺动机/机制/边界/例子；图比 catch 分支更满不是缺章。不建议 `split-lesson`：未满 10 问；四格是同一 OBJ-27 的窗/性状/主失败路径，不是两个 C4 容器；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明新 (b) 行：幂等、魔数、摘索引已是本行副作用。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:SysOssUploadController.init, signParts, parts, complete, abort | 五映射、门牌、权限注解、`parts`→`resume`、complete `R.data`、abort DELETE、零 `@Log`、无文件参数、classic 非 UseCase——均与源码/合同测试对齐。 | 可选半句：init/resume 不进 `locked`。Lead 可将本行标 `covered`（证据锚本 probe 与 L-027 来源表 S-L027-01/02）。 | defer（本行可 covered） |
| B:SysOssUploadController.complete / abort | 落临时行、两处早退、try 内失败 `cleanupAfterFailure`、`markCompleted` 在 try 外、HTTP abort 三分支、坏魔数不留行——源码可复现。残留精度：(1) Provider 清理失败不 `removeSession`；(2) `SESSION_EXPIRED` 不走 cleanup；(3) abort COMPLETED/空票无单测。 | 失败路径图加半句：Provider 抛错时只拨 expire-index，票可能还在。不要重写机制链。Lead 可将本行标 `covered`。 | defer（本行可 covered；精度项不挡盖章） |
| D:失败路径 OSS abort | 矩阵只挂 L-027。课把「complete 失败 → abort」解释成服务端 `cleanupAfterFailure`，HTTP DELETE 是取消门。与 L-029「校验失败不由 adapter DELETE」对齐。过期 complete 不扫桶是边界，不是另一条失败路径。 | Lead 可将本行标 `covered`。不要要求口试必须再打一枪 HTTP abort。 | defer（本行可 covered） |
| D:主路径 OSS 直传 | 本课三跳（init 票 → PUT 不进 Controller → complete 临时行）已讲清。矩阵原文「业务引用」被课收成临时 `sys_oss`；第四跳业务表、xhr PUT/IndexedDB 在邻课。矩阵备注 `L-027, L-029`。 | 不要把 adapter `upload()` 并进 OBJ-27。Lead：要么等 GP-L-029 再盖整行，要么接受临时行操作定义后标 covered、L-029 只补数据面函数级。chain 覆盖列若要当证据，补上本行名字。 | defer（整行等 L-029 或 Lead 采纳课的操作定义；本课不 mine-more） |
| A:createOssUploadClient.upload / B:同符号 | 课只保证五条 URL 对得上。进度、IndexedDB、`AbortSignal` 未认格。 | 保持。 | defer（L-029） |
| A:SysOssController.list, listByIds, downloadUrl, remove | 对照门牌 `/resource/oss`。临时行 `url=""` 不能当下载地址。 | 保持。 | defer（L-026） |
| A:SysOssConfigController / B:updateOssConfigStatus | init 的 SERVING 门闩已借用。切默认桶未讲。 | 保持。 | defer（L-025） |
| A:SysOssMigrationController.* | 负空间。 | 保持。 | defer（L-028） |
| A:admin-web.services / C:admin-web-composition | 厨房五枪对照已借用。 | 保持。全表 L-007。 | defer（L-007） |
| A:SysProfileController / 头像 | 变式 A 一句：complete 后再 `updateProfile`。 | 保持。 | defer（L-022） |
| C:MinIO/OSS | 数据面 PUT 边界已画。容器画像未认格。 | 不要用本课假闭合 (c)。 | defer（(c) 仍 uncovered） |
| C:Redis 8 | 票四键是 (b) 机制，不是会话容器课。 | 保持。 | defer（L-084） |
| API-005 变更 POST+`@Log` | 存量 POST 无 `@Log` + DELETE abort。 | 本课盘点，不发动 Ratchet。 | defer（传输 Ratchet） |
| OssLifecycleManager 临时对象过期 | 课已与 UploadCleanupTask 拆开。 | 保持。 | defer（L-026） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本行无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出五扇目的、complete 主路径（HEAD/魔数/临时行）与失败路径（`cleanupAfterFailure` / HTTP abort 三分支）；(d) 主路径缺的是与 L-029 的盖章时序和「业务引用」一词，不是讲义容器；把可选图注写进原课不会超过 40 分钟；列表窗、切桶、迁移、adapter、头像、Redis 会话、MinIO 容器已有后续课或范围外 defer。满 10 问前不拆。五窗与两条 D 虽跨控制面/数据面，仍是 chain 原设计的同一 OBJ-27，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-029 / L-026 / L-025 / L-028 / L-007 / L-022 / L-084 / (c) MinIO / API-005 / OssLifecycleManager 显式建议 `defer`；对 `A:SysOssUploadController.init,signParts,parts,complete,abort`、`B:SysOssUploadController.complete / abort`、`D:失败路径 OSS abort` 建议 Lead 标 `covered`（证据锚本 probe 与 L-027 来源表）；对 `D:主路径 OSS 直传` 建议 Lead 按操作定义二选一（等 GP-L-029 或接受「业务引用=临时行」后盖章）。本课不主张改列表窗、切桶、迁移、adapter 工厂或任何 (c) 格子。
