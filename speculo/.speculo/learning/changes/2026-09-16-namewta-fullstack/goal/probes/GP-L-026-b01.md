---
artifact: learning-goal-probe
probe_id: GP-L-026-b01
lesson_id: L-026
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-026 / b01

审问对象是已写成的 Lesson `lessons/L-026-oss-object.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / 矩阵 (a) 一行：`A:SysOssController.list,listByIds,downloadUrl,remove`。OBJ-26：口述 `SysOssController` 列表 / 下载 URL / 删除。chain 覆盖列多写了 **`B:ISysOssService`**（整接口）。矩阵 (b) **没有** `ISysOssService` / `resolveAccessUrl` / `markDeletePending` 行。矩阵 (c) MinIO/OSS 仍 uncovered。矩阵 (d) 直传主路径 / abort 留给 L-027 / L-029。配置切换留给 L-025；直传五扇留给 L-027；迁移批留给 L-028；厨房 `createSystemService.resources.oss` / `createOssUploadClient` 留给 L-019 / L-029。

抽查锚点（2026-09-16）：`backend/wta-modules/wta-system/.../controller/system/SysOssController.java`（公开映射正好四扇）；`SysOss{Config,Upload,Migration}Controller.java` 三块隔壁门牌；`service/ISysOssService.java`；`service/impl/SysOssServiceImpl.java`；`oss/service/OssLifecycleManager.java`；`mapper/SysOssMapper.xml`；`domain/{SysOss,bo/SysOssBo,vo/SysOssVo}.java`；`wta-api/.../OssService.java`；`CacheNames.SYS_OSS`；`OssLifecycleProperties`；`GlobalExceptionHandler`；`ThreadUtils.virtualSubmitAll`；`03-backend-module-modes.md` classic 行；`10-cde-base-ddl.sql` `sys_oss` / `sys_oss_ref`；`50-cde-base-dml.sql` 文件管理菜单；`OssProtocolCutoverUnitTest`；`OssAccessUrlArchitectureUnitTest`；`OssAccessUrlServiceUnitTest.managementListNeverReturnsAccessUrl`；`OssAccessUrlContractUnitTest`；`OssReleaseContractionUnitTest`；`OssLifecycleManagerUnitTest`；`frontend/packages/domains/system/src/{oss/index.ts,resource-service.ts}`。

## Q1 澄清

- objective_id: OBJ-26
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-26；chain L-026 覆盖列 `A:SysOssController.list,listByIds,downloadUrl,remove B:ISysOssService`；L-026 `claimed_cells` 只认 (a) 一行；矩阵 (a) 该行与磁盘四扇同名；(b) 无本课符号；(c) MinIO/OSS uncovered；(d) 直传流是 L-027；Goal「范围内公开方法的目的都要在函数表里出现」
- difficulty: medium

课把「卡片柜四扇窗」收成 **一个 (a) 格子、一个 OBJ**。计划里至少有五套可数对象，课没有声明以哪一套当作闭合的操作定义：

1. **OBJ-26 原文。** `course.md`：能口述 `SysOssController` 列表 / 下载 URL / 删除。点名的是**一份类 + 三件事**，没有把 `listByIds` 写成第四个口试名词，也没有点名失败句（引用拦截、PENDING 拒下载、涂黑 URL）。
2. **Lesson `claimed_cells` / 矩阵 (a) 一行。** `list, listByIds, downloadUrl, remove`。与磁盘公开映射 1:1。默认档 Unistructural + explained。状态仍 `uncovered`。
3. **Chain。** 同一四扇 **加上** `B:ISysOssService`。接口磁盘还有 `getById`、`upload(File, SysOssExt)`，本 Controller **零 HTTP**。
4. **矩阵 (b)/(c)/(d)。** 无 `deleteWithValidByIds` / `resolveAccessUrl` / `markDeletePending` 行。MinIO/OSS 容器备注仍是「直传对象」。直传 init→PUT→complete 不是本课路径。课却把管家盖章、引用拦截、访问分类写成机制主链。
5. **菜单种子里多出来的字。** `system:oss:upload` 挂在隔壁 `SysOssUploadController`；组件路径 `system/oss/index`，HTTP 前缀 `/resource/oss`。课声明口试按磁盘四扇，upload 不是本类。

问：本课 (a) 闭合标准是「四扇 HTTP 还在 `SysOssController`」，还是必须连涂黑 URL、出门条才给地址、删除只盖 PENDING、引用拦截、两根导线一份实现一起算进 OBJ-26？若 Lead 只凭「类名四方法在函数表里」把该行标 covered，chain 多出来的 `B:ISysOssService`（含 `upload(File)`）还算不算本格？(b) 若必须看见 `OssLifecycleManager.deleteObjects` 函数体才算性状闭合，是否已经把 OBJ-26 扩成一张不存在的 Service/管家行，或把 MinIO 容器标 covered？

## Q2 证据

- objective_id: OBJ-26
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: `SysOssController` 31–86；`ISysOssService` 18–61；`SysOssServiceImpl` 51 / 67–93 / 152–164 / 173–176 / 235–239 / 291–305；`OssLifecycleManager` 82–85 / 124–148 / 174–216 / 239–276；`OssService` 28–42 / 76 / 110–114；`SysOssBo` 全文无校验注解；`SysOssVo` 44–48 / 86–89；`CacheNames` 67；`SysOssMapper.xml` 32–38；`10-cde-base-ddl.sql` 304–321；`GlobalExceptionHandler` 158–163；`SysOssUploadController` 59–61；`ThreadUtils` 64–81；`resource-service.ts` 157–177；`OssProtocolCutoverUnitTest` 24–37；`OssAccessUrlArchitectureUnitTest` 14–30；`OssAccessUrlServiceUnitTest` 69–81；`OssLifecycleManagerUnitTest` 74–86 / 135–161 / 249–280 / 333–354；`OssReleaseContractionUnitTest` 41–45 / 60–64；`03-backend-module-modes.md` classic 行
- difficulty: high

课的机制链哪些能在工作树直接复现，哪些被磁盘削薄或说满？

**仍能复现：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 公开映射正好四扇；无 `upload` / `download` / `MultipartFile` / `ResponseEntity` 字节流 | `SysOssController` 45–86；`OssProtocolCutoverUnitTest` 24–37 |
| 门牌 `/resource/oss`，不是 `/system/oss`；四扇均有 `@SaCheckPermission` | 类 31；`OssReleaseContractionUnitTest` 41–45 / 60–64；菜单组件 `system/oss/index`（DML 65） |
| 两根导线一份实现：`ISysOssService ossService` 管 list / listByIds / remove；`OssService publicOssService` 只服务 `downloadUrl`；`SysOssServiceImpl implements ISysOssService, OssService` | Controller 34–36 / 70–71；Impl 51 |
| `wta-system` classic；无 UseCase 目录；管家 `OssLifecycleManager` 仍是 `@Service`，不是 layered | `03-backend-module-modes.md` classic 行；模块内零 `usecase` |
| 四邻门牌：`/resource/oss/config`、`/uploads`、`/migrations` | 三份 Controller `@RequestMapping` |
| `list`：`GET /list` + `system:oss:list` + 无 `@Log` + `QueryGroup` + `PageQuery` | Controller 45–48 |
| `buildQueryWrapper`：fileName/originalName like；suffix/url/service/isTemp eq 文本；createBy `eqIfPresent`；时间 `beginCreateTime/endCreateTime`；`orderByAsc(ossId)`；**不**滤 `delete_state` | Impl 152–164 |
| `managementView`：拷贝 VO → `setUrl(null)` → `snapshot` 填 `referenceCount`（`references().size()`）和 `references` | Impl 298–305；单测 `managementListNeverReturnsAccessUrl` |
| `listByIds`：`GET /listByIds/{ossIds}` + `system:oss:query` + `@NotEmpty` + `Arrays.asList`；AOP `getById` `@Cacheable SYS_OSS#30d`；空 VO 变 null 后 `removeAll(singleton(null))` | Controller 57–62；Impl 82–93 / 173–176；`CacheNames` 67 |
| `downloadUrl`：`GET /{ossId}/download-url` + `system:oss:download` + `resolveAccessUrl`；无匿名 `public/{ossId}` | Controller 68–71；架构测试 28–30 |
| PUBLIC_READ → `OssAccessUrl("PUBLIC", publicUrl, null, originalName)`，不 presign；否则私有签名，默认 TTL 2 分钟（1–10），`expiresAt` 用 Provider 值 | Manager 133–148 / 267–276；Properties 36–42；单测 249–280 / 348–352 |
| `presignDownload` 对公共对象 `PUBLIC_PRESIGN_FORBIDDEN`；管理窗不走这扇 | Manager 155–160；单测 283–291 |
| `remove`：`DELETE /{ossIds}` + `system:oss:remove` + `@Log DELETE` + `deleteWithValidByIds(..., true)`；`isValid` 空块 | Controller 80–86；Impl 235–239 |
| `deleteObjects`：空集 `false`；id 去重排序 + `FOR UPDATE`；行数不够 `OBJECT_NOT_FOUND`；有 `sys_oss_ref` 有效行 `OBJECT_REFERENCED` 且 `verifyNoInteractions(objectStore)`；否则只 `markDeletePending` | Manager 174–194；XML 32–38；单测 135–161 |
| `markDeletePending`：`is_temp='Y'`、`expire_time=now`、`delete_state='PENDING'` | XML 32–38 |
| 清理默认关、dry-run 默认 true；HTTP **不**调 `cleanupExpired`；已 PENDING 才 `objectStore.delete` + `deleteById` | Properties 48–51；Manager 198–216；单测 163–212 / 348–354 |
| `OssService.selectUrlByIds` / `selectByIds` `@Deprecated(since="6.0.0")`；本 Controller 无映射 | `OssService` 28–42；Controller 全文 |
| `ISysOssService.upload(File)` 仍在接口；本类零调用 | `ISysOssService` 52；Cutover 禁止 MultipartFile |
| 权限四颗：list / query / download / remove；菜单另有 upload | Controller 四注解；DML 142–145 |
| API-005 存量：变更窗是 DELETE + `@Log`，不是 POST | Controller 80–82 |
| 基座 `CREATE TABLE sys_oss` 有 `is_temp` / `expire_time`，**没有** `delete_state`；实体 / VO / XML 认该列 | DDL 304–321；`SysOss` 70；`SysOssVo` 89；XML 27 / 36 / 47 |
| 前端锚点 `{ controller: 'SysOssController', basePath: '/resource/oss' }`；厨房 `listByIds` 再打 download-url 填 url；`list` 把 url 写成 `''` | `oss/index.ts` 3；`resource-service.ts` 165–177 |

**课写过、磁盘已削薄或说满：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「异常（引用占用、缺行）会冒成生命周期错误」 | Java 层确是 `OssLifecycleException` + `OssLifecycleError`。本 Controller **没有** `@ExceptionHandler`（隔壁 `SysOssUploadController` 59–61 才把 `OssUploadException` 收成带 error 名的 `R.fail`）。`OssLifecycleException extends RuntimeException`，走进 `GlobalExceptionHandler` 158–163：`R.fail("发生未知异常…[错误编号]")`。口试若说 HTTP 回 `OBJECT_REFERENCED` 业务码，磁盘不支持 |
| 「列表却不按 PENDING 过滤，卡片可能还在柜上」易听成「看不出待撕」 | `managementView` **不**抹 `deleteState` / `isTemp` / `expireTime`。`list` 走 `selectVoPage`，PENDING 行 JSON 里仍带 `deleteState=PENDING`。涂黑的是 url，不是状态栏 |
| 「`listByIds` 可能仍吃到删除前的 VO，再经 snapshot 补引用」 | 补引用成立。未写明：`@Cacheable` 30d、删除路径**零** `@CacheEvict`，缓存 VO 的 `deleteState` 仍可能是 `ACTIVE`。两扇不是同一份生命周期视图：`list` 能看见 PENDING，`listByIds` 可能撒谎 |
| 「缺号就丢、不 404」 | 仅 `getById` 返回 null。`managementView`→`snapshot`→`require` 在**行已物理删除、缓存仍命中**时抛 `OBJECT_NOT_FOUND`；`virtualSubmitAll` 再包成 `RuntimeException("线程执行异常：…")`，整包失败，不是丢掉那一个 id |
| 「盖了章就领不出门条」 | `requireDownloadable` 见 PENDING 即拒，成立。未写：`bind` / `reconcileReferences` 会 `updateLifecycle(..., delete_state='ACTIVE')`。单测 `existingBindShouldCancelPendingDeletion`。PENDING 不是锁死，再借出去会擦章 |
| 「`toAjax(false)` 只在空集合」 | Controller `@NotEmpty`，HTTP 空路径进不了管家。缺行 / 占用走异常（再变成未知异常信封）。空集 false 几乎不是管理窗能打到的信封 |
| 「H2 集成测试 DDL 都认 `delete_state`」 | 工作树只在 `OssStorageMigrationIntegrationTest` 内联 CREATE 见到该列，不是本课 Controller 夹具。冲突主语仍是 **10 号基座缺列 vs Java/XML 在写它** |
| 「`OssAccessUrl.fileName`」未钉来源 | PUBLIC/PRIVATE 都用 `oss.getOriginalName()`（私有经 `OssDownloadUrl.fileName`）。列表 VO 的 `fileName` 才是对象键。出门条文件名 ≠ `sys_oss.fileName` |
| 「部分 OSS 对象不存在」 | 任一 id 行锁对不上，**整批不盖章**。不是「缺的跳过、其余 PENDING」 |

问：在四扇 HTTP 形状、classic、列表涂黑、出门条分类、删除只 PENDING、引用拦截已经能互证的前提下，(a) 是否还要把 **HTTP 失败信封说成生命周期业务码、listByIds 说成能看见 PENDING、盖章后不能再被 bind 救回**，才算把四扇窗讲完？

## Q3 假设与反例

- objective_id: OBJ-26
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 四扇映射；`managementView` 只 `setUrl(null)`；`SysOssVo.deleteState`；`getById` `@Cacheable` 无 evict；`bind` 擦 PENDING；`GlobalExceptionHandler` vs `SysOssUploadController.handleUploadException`；`deleteObjects` 整批行数闸；`List.of`/`Arrays.asList`；菜单 upload；API-005
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪只柜子认错？

1. **「这是 layered，OSS 应有 `OssUseCase`。」** 登记表 classic。课反例 12 覆盖。
2. **「`SysOssController` 还有 upload / 字节 download。」** 方法名测试禁止。课反例 1 覆盖。
3. **「列表 VO 的 url 就是下载地址。」** `managementView` 置 null。课反例 2 覆盖。
4. **「`listByIds` 缺一个 id 就整包 404。」** 缺的丢掉。课反例 3 覆盖。**但** snapshot 抛错会整包未知异常——课没写成反例。
5. **「`GET /resource/oss/{id}` 是详情。」** 没有这扇。课反例 4 覆盖。
6. **「删除 = `objectStore.delete` = 行消失。」** 只 `markDeletePending`。课反例 5 覆盖。
7. **「被引用也能删，反正是管理员。」** 先数引用。课反例 6 覆盖。
8. **「业务页面也可以打 download-url，因为已登录。」** 权限是 `system:oss:download`。课反例 7 覆盖。
9. **「`selectUrlByIds` 是本课第四扇。」** deprecated，无映射。课反例 8 覆盖。
10. **「HTTP 前缀 `/system/oss`。」** 类上 `/resource/oss`。课反例 9 覆盖。
11. **「四扇都有 `@Log`。」** 只有 `remove`。课反例 10 覆盖。
12. **「`list` 和 `listByIds` 同一颗权限。」** `:list` vs `:query`。课反例 11 覆盖。
13. **「`ISysOssService.upload` 没了。」** 接口还在，本课 HTTP 没有。课反例 13 覆盖。
14. **「匿名 `GET /resource/oss/public/{ossId}`。」** 架构测试禁止。课反例 14 覆盖。
15. **「公共对象也要签名。」** `resolveAccessUrl` 走 `publicUrl`。课误区 8 覆盖。
16. **「`OssService` 和 `ISysOssService` 是两个类。」** 一个实现。课误区 9 覆盖。
17. **「四扇都符合 API-005 的 POST 变更。」** 变更窗是 DELETE。课误区 12 覆盖。
18. **「`isValid=true` 会做完整校验。」** 空块。课误区 13 覆盖。
19. **「10 号基座已经有 `delete_state`。」** CREATE 没有。课误区 14 / 基座冲突覆盖。
20. **「前端 listByIds 填了 url，所以后端也返回 url。」** 厨房再打 download-url。课误区 15 覆盖。
21. **「HTTP 删除失败会返回 `OBJECT_REFERENCED` 业务码。」** 本窗没有专用 handler，信封是未知异常编号。课写成「冒成生命周期错误」，磁盘只在 Java 枚举层成立。
22. **「列表看不出待撕，因为 url 都是 null。」** `list` 的 `deleteState` 仍在 VO 上。待撕标志不是空 url。
23. **「`listByIds` 与 `list` 对 PENDING 一样诚实。」** 30d 缓存、删除不 evict；`listByIds` 可能仍报 `ACTIVE`。
24. **「盖了 PENDING 就不能再被引用。」** `bind` 会把 `delete_state` 写回 `ACTIVE`。
25. **「删除一批时缺一个 id 会删掉其余。」** 行数对不上整批拒绝，零盖章。
26. **「chain 的 `B:ISysOssService` 已随本课闭合。」** 课 `claimed_cells` 没认它；矩阵 (b) 无行；`upload(File)` / `getById` 不是本课 HTTP。

问：读完后仍说不出的，是不是「四扇的名字」，而是 **url 涂黑不等于看不见 PENDING、`listByIds` 缓存会撒谎、HTTP 失败不是生命周期业务码、PENDING 可被 bind 擦掉、缺 id 整批不删**？

## Q4 视角

- objective_id: OBJ-26
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-26 vs L-025/L-027/L-028/L-029/L-019；`OssLifecycleManager.bind`；`GlobalExceptionHandler`；API-005；10 号 DDL
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:SysOssController.list,listByIds,downloadUrl,remove`**？

| 视角 | 多出来的边 | 是否本课格子 |
| --- | --- | --- |
| 矩阵维护者 | (a) 一行 = 磁盘四扇；chain 多 `B:ISysOssService`；(b) 无行；(c) MinIO/OSS uncovered；(d) 直传流不是本课 | **是** 闭合定义。不要求发明 (b) 就能标 (a) covered；不要把 MinIO 容器或 chain B 整接口假闭合 |
| 卡片柜（本类） | 四扇、四权限、仅 remove `@Log`、两根导线、classic | **是** (a) |
| 目录两扇 | 涂黑 url、不滤 PENDING、`list` 能带 `deleteState`；`listByIds` 缓存 30d 不 evict | **是** (a) 的 explained 失败句 / 副作用。不要另开缓存行 |
| 出门条 | `resolveAccessUrl` 分类；PENDING / 停业拒绝；PUBLIC 不签名；TTL 默认 2 分钟 | **是** `downloadUrl`。`presignDownload` 内部路 **否**（无本课 HTTP） |
| 待撕章 | 引用拦截、只 PENDING、不砸箱；缺 id 整批拒绝 | **是** `remove`。清理循环 / `objectStore.delete` **否**（默认关；不是本课 HTTP） |
| 管家 bind | PENDING 可被擦回 ACTIVE | 一句负空间 **可以**（解释「待撕不是锁死」）。`reconcileReferences` 全表 **否** |
| 配置柜（L-025） | 换默认仓；旧对象 `service` 名牌不搬家 | 下载依赖 `requireServing(service)` 一句 **是**。`changeStatus` **否** |
| 直传票房（L-027） | `/uploads` 五扇；`system:oss:upload`；有专用异常 handler | 对照「本类零 upload、失败信封不同」 **是**。init/complete **否** |
| 搬家批（L-028） | `/migrations`；XML `compareAndSetService` 要求 `delete_state=ACTIVE` | **否**。H2 内联 DDL 有 `delete_state` 只证明冲突主语 |
| 厨房（L-029 / L-019） | `listByIds` 再打 download-url；`list` 写空串 | URL 对照一句 **是**。工厂全表 **否** |
| MinIO/OSS 容器 | 箱子还在，直到清理 | 作为 remove 副作用点名 **是**。容器标 covered **否** |
| MySQL 8.4 / L-085 | 10 缺 `delete_state`；`sys_oss_ref` 在 | 本课用到的表/冲突一句 **是**。基座容器 **否** |
| API-005 | 存量 DELETE + `@Log` | 盘点现状 **是**。不发动传输 Ratchet **否** |
| dao-mapper / L-083 | `SysOssMapper.xml` 有行锁 / 盖章 SQL，不是空壳 | 持有关系一句 **是**。SQL 所有权课 **否** |

问：Lead 若凭「四方法名都在函数表里」把该行 (a) 标 covered，是否还要求读者能指着 **列表涂黑但 `list` 仍带 `deleteState`、`listByIds` 缓存可能仍是 ACTIVE、download-url 才给地址、删除不砸箱、HTTP 失败是未知异常编号**？这些是不是已经（或应当）在函数表/反例里，而不需要 (b) 新行、也不需要把 MinIO 标 covered、也不需要闭合 chain 的整份 `ISysOssService`？

## Q5 元问题

- objective_id: OBJ-26
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；一份 Controller 同一 `wta-system` 容器
- difficulty: low

建议：`re-dispatch-L`

原因：

- 一格同属 OBJ-26、同一 C4 容器 `wta-system`。L 合同章节齐全（直觉、定义、机制链 1–5、带 caption / 文字等价物 / 图边界的 ASCII、正反例、变式、误区、来源表）。**主叙事站得住**：磁盘正好四扇；classic 无 UseCase；两根导线一份实现；列表涂黑、出门条才给 `OssAccessUrl`；删除只盖 PENDING、引用拦截、桶后删；upload/config/migrations 是隔壁门牌；10 号基座缺 `delete_state` 要报冲突。不必为「窗口名字」再挖五问。
- 本批已有可粘贴的磁盘反例，属于**同一课的闭合定义与机制精度**，不是第二容器：① HTTP 失败信封是 `GlobalExceptionHandler` 未知异常编号，不是 `OssLifecycleError` 业务码（对比 uploads 窗有专用 handler）；② `list` 的 VO 仍带 `deleteState`，空 url 不是待撕标志；③ `listByIds` 30d 缓存、删除不 evict，PENDING 可能仍显示 `ACTIVE`；④ 缺 id 整批不盖章；⑤ `bind` 可把 PENDING 擦回 ACTIVE；⑥ 出门条 `fileName` 是 `originalName`。补丁应短：开篇钉「点名窗 = 矩阵 (a) 一行 = 磁盘四扇；失败句是这些窗的 explained 性状，不另开 (b)、不认 MinIO 容器、不闭合 chain 的整份 `ISysOssService`」；remove/downloadUrl 失败句改口未知异常编号；list vs listByIds 加半句缓存撒谎；PENDING 加半句可被 bind 救回。不要把直传五扇、配置切换、厨房全表、清理循环、基座整库塞进 38 分钟课。
- 不建议再开一批问：重复本批证据。`mine-more` 只允许在 b01 已证明**真实矩阵缺口且五问不够写清**时开 b02——本批缺口是同一 OBJ 的口吻精度，一行补丁可写进原课。不建议拆课：未满 10 问；不是两个 C4 容器；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议把 (a) 整格推迟：四扇在磁盘上，不是空符号。不建议发明 (b) 行：涂黑 / PENDING / 引用拦截是 (a) 的解释到副作用。chain `B:ISysOssService` 请 Lead 改覆盖列或诚实 defer，不是 miner 再问 `upload(File)`。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:SysOssController.list | `GET /list`、`:list`、无 `@Log`、`managementView` 涂黑 url、不滤 PENDING、`QueryGroup` 空壳、条件表、`orderByAsc(ossId)`，均与源码对齐。残留：VO **保留** `deleteState`/`isTemp`/`expireTime`；口试若把「卡片还在」说成「看不出待撕」会错。 | 正例钉「url 恒 null，状态栏仍可能是 PENDING」。不要把本格扩成 MinIO。 | re-dispatch-L（半句） |
| A:SysOssController.listByIds | `:query`、缓存 `getById`、缺号丢 null、同样涂黑，已对齐。缺口：30d 无 evict，删除后 `deleteState` 可能仍是 ACTIVE；`snapshot` 抛错会整包 `RuntimeException`，不是再丢一个 null。 | 反例钉「两扇不是同一份生命周期视图」。缺号静默 ≠ 缓存命中已删行。 | re-dispatch-L |
| A:SysOssController.downloadUrl | 专用窗、`:download`、`resolveAccessUrl`、PUBLIC 不签名 / PRIVATE 默认 2 分钟、PENDING 与停业拒绝、无字节流，已对齐。残留：HTTP 信封是未知异常编号；`fileName` 是 `originalName`。 | 失败句改成走进 `GlobalExceptionHandler`。对照 uploads 窗有 error 名。 | re-dispatch-L（半句） |
| A:SysOssController.remove | DELETE、`:remove`、`@Log`、空 `isValid`、引用拦截、只 PENDING、不砸箱，已对齐。残留：① HTTP 失败不是业务码；② 缺任一 id 整批零盖章；③ PENDING 可被后续 `bind` 擦掉；④ `@NotEmpty` 下 `toAjax(false)` 几乎打不到。 | 补三句：未知异常信封；整批行数闸；待撕可被引用救回。清理默认关保持。 | re-dispatch-L |
| B:ISysOssService | chain 声称整接口。课只借用 `queryPageList` / `listByIds` / `deleteWithValidByIds`。`getById` 无 HTTP；`upload(File)` 无本课映射。矩阵 (b) **无此行**。 | 不要并进 (a)。Lead 收窄 chain 覆盖列，或 defer 到无格子。不要为本课发明 (b) 行。 | defer（未入库 / chain 过宽） |
| A:SysOssConfigController.* / B:updateOssConfigStatus | 课已划四邻门牌与 `requireServing(service)`。 | 保持。 | defer（L-025） |
| A:SysOssUploadController.init,signParts,parts,complete,abort | 菜单 `system:oss:upload`、专用异常 handler 已作对照。 | 保持。失败信封对照一句可进本课补丁。全表 L-027。 | defer（L-027） |
| A:SysOssMigrationController.* | 门牌已划。H2 内联 DDL 有 `delete_state` 属迁移夹具。 | 基座冲突主语保持 10 vs Java/XML。 | defer（L-028） |
| A:createOssUploadClient / createSystemService.resources.oss | 厨房 `listByIds` 再打 download-url、`list` 写 `''` 已借用。 | 改口保持「后端仍涂黑」。全表 L-029 / L-019。 | defer（L-029 / L-019） |
| MinIO/OSS 容器 / (d) 直传流 | 箱子在清理前仍在；本课 HTTP 不触发清理。 | 点名即可。 | defer（矩阵 (c)/(d)；L-027） |
| MySQL 8.4 / mysql-base | 10 缺 `delete_state`；`sys_oss_ref` 在。 | 保持报冲突。 | defer（L-085） |
| API-005 变更 POST | 存量 DELETE + `@Log`。 | 本课盘点，不发动 Ratchet。 | defer（传输 Ratchet） |
| (b) resolveAccessUrl / markDeletePending | 矩阵无此行。课已把分类与盖章写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；缺口落在同一 OBJ-26 的闭合定义与机制精度（HTTP 未知异常信封、list 仍带 deleteState、listByIds 缓存撒谎、整批行数闸、PENDING 可被 bind 擦掉），不是第二容器或讲不完的函数簇；把开篇闭合句和四处反例写进原课不会超过 40 分钟；配置 / 直传 / 迁移 / 厨房 / MinIO 容器已有后续课或独立格子。满 10 问前不拆。四扇仍是同一 `SysOssController`。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-025 / L-027 / L-028 / L-029 / L-019、MinIO 容器、直传数据流、L-085 基座、API-005、未入库 (b) 以及 chain 过宽的 `B:ISysOssService` 显式建议 `defer`；对本课一行 (a) 建议 `re-dispatch-L`。Lead 可在补丁后将该行标 `covered`（证据锚本 probe 与 L-026 来源表）。本课不主张改 MinIO 容器、MySQL 基座、直传流、配置/上传/迁移窗或任何 (b)(c)(d) 格子。
