# T41 E fixed recovery security review — runtime pending

**Fixed source:** E `a0dcbac8fd33a754c74c154551f7090a8878fb48`, tree `0edf8653fede967b034662afd95559283efe08e7`. Compared to the governance checkpoint `f8edabc50f2df03a4b89da8f23f3c321bd277174`, exactly `frontend/e2e/run-inbox-paged-real.py` and `frontend/e2e/test_run_inbox_paged_real.py` changed. Runner SHA256 from fixed Git object: `ad7dd09ba26f479d3e70060858b5a8a4f5aa8a8439c13ac4e952b459dca0e901`. Review uses fixed `git show`/diff and safe offline result only. No test/build/service/repo mutation by this reviewer.

**Security delta verdict: static PASS for E recovery. T41/browser acceptance remains PENDING Lead's exact-E full package and real 501 Chrome run.** The prior B/C/D attempts retain their original failed/incomplete verdicts.

The two diagnosed fixture failures are closed narrowly:

- `run-inbox-paged-real.py:120-145,785-788` retains 40-character alphanumeric infrastructure credentials, introduces a separate 24-character alphanumeric generator only for A/B login, and validates that exact length in the BCrypt `$2a$` helper before the one-row update. This fits the backend `PasswordLoginBody` 5–30 bound. Passwords remain generated within the owned run, stored only in private child environment; SQL receives only the BCrypt hash encoded as hex, and no credential enters argv/reports.
- `:406-429,501-509` adds `compared_hexsql` with explicit `utf8mb4_general_ci` only for the two username lookups, Admin Client ID lookup and owned title `LIKE` verification. Generic `hexsql` for inserts and updates is unchanged. These are exactly the four expressions that isolated diagnosis found to fail with MySQL 1267; no table/schema/collation migration or production query changed. User/Client lookups still require a single enabled, non-deleted row; title count remains exactly 502.

SQL failure diagnostics now use a closed stage allowlist and `OwnedSqlError` (`:43-65,272-289,1014-1019`). The runner passes SQL bytes via stdin, returns only stage, exit code and numeric MySQL error/SQLSTATE, and suppresses raw stderr, SQL text and timeout content; an unknown stage fails before command execution. The original cleanup, exact source/JAR/package preflight, one Chrome case/one attempt, zero skip/fail/flaky and A/B SQL before/after assertions are unchanged. The new offline tests exercise length separation, comparison scoping, missing/duplicate identity and canary-bearing SQL error/timeout without leaking canary. `/tmp/wta-t41/dispatch02-offline-handoff.json` records exit 0 and its log reports **24 tests passed**; this is offline validation only.

Fixed Git objects show backend `wta-admin/src/main` unchanged (`abb6b35faf193d6b63d0e5fcbd0b3c227bb30e12` on D and E), Admin frontend runtime `src` unchanged (`7d04b884ee9e3113c967d3dd1b3b540725a76290`), and api-contracts unchanged (`f1de7c78a44dde47560c04f82a7b1a1f9922148b`). `git diff --check f8edabc5..E` exited 0. This supports reuse of earlier runtime/static reasoning; it does not relabel earlier D browser or C production build as an E real run.

**Pending final evidence:** exact clean E full-JAR package proof/hash; E runner result with SQL stage and login actually passed, exactly one Chromium 501 case with 1 expected/0 unexpected/0 skipped/0 flaky, A/B post-DB counts and preserved timestamps, source-before/after E clean, unchanged JAR hash, and fully cleared owned PGIDs/containers/volumes/ports. Lead also owns the outstanding core and five API-controlled acceptance cases. Update this report only from their completed safe results, without reading credentials or raw browser/SQL logs.

## Actual E browser attempt 1 — failed, retained (2026-09-23)

Lead's safe `/tmp/wta-t41/browser-runs/969f43a52216aebb/result.json` reports **acceptance false, exit 1**. Its exact Playwright identity is the intended Chromium case, one attempt, but counts are `expected=0, unexpected=1, skipped=0, flaky=0`. The numeric failure location is `frontend/e2e/inbox-paged-real.e2e.ts:90:57`, the top-detail “关闭” button click. Since the preceding sequential body assertion at :89 completed before this click, the run established fresh six-SQL seed counts A 501/481 and B 2/2, two owned login rotations, Admin login, top-ten list and top detail/body through that point. It **did not** reach page 26, global read-all, post-browser SQL preservation, or final one-case pass; `seed_after` is absent. The safe result alone does not establish why the close-button locator failed, so do not label it a product or selector defect until the component structure/allowed diagnostic evidence is reviewed.

Both source snapshots are exact clean E/tree `0edf8653fede967b034662afd95559283efe08e7`; full JAR SHA256 before/after is `1e95f6f074ba99ecd7c9efc9211d8067524be8aa4c41b3f74373872408d5c77d`. Browser/Vite/backend process groups have no live members, owned containers and captured anonymous volumes are gone, all five loopback ports are closed, and cleanup errors are empty. This clean failure closes the two deterministic D fixture diagnoses at least through initial browser detail, while preserving the E browser gate as **failed**. Any correction requires a new fixed source/attempt and exact final-run evidence; the original E result must remain unchanged.

Static follow-up for the failed click: fixed E has a footer button with text “关闭” in both notification dialogs (`layout/components/notice/index.vue:54-57`, `InboxPage.vue` footer), while the local Element Plus zh-cn locale assigns the dialog header close button `aria-label="关闭此对话框"` (`es/locale/lang/zh-cn.mjs:116`, `es/components/dialog/src/dialog-content...mjs:61`). Playwright role-name matching without `exact:true` can therefore select both buttons for `getByRole('button', { name: '关闭' })`. A narrower footer scope or exact-name test locator is a bounded fixture correction that keeps an ordinary user click. This is static evidence for ambiguity, not a claim about the removed raw reporter's exact exception text; the E result remains failed.
