# T41 F fixed-source security review — real 501 pending

**Fixed F:** `cb8063b686fcd82d0a91064061e674a59fb6fc14`, tree `2ee339a27589ea393baca0d4131f598981f5abae`. **Compare E:** `a0dcbac8fd33a754c74c154551f7090a8878fb48`. Read-only fixed Git diff and safe retained result; no tests/builds/services/repo writes by this reviewer. Prior E review: `/tmp/wta-t41-e-security-review.md` (its first real 501 run remains failed).

**Static F security result: PASS for this exact two-line test-selector correction. Whole T41 acceptance remains PENDING F full package and real 501 Chrome result.** `git diff E..F -- frontend backend` has one path, `frontend/e2e/inbox-paged-real.e2e.ts`, and exactly two changes at :90 and :124: both detail footer “关闭” role locators now require `exact:true`. This resolves the documented selector overlap with Element Plus's zh-cn header close aria-label “关闭此对话框” while preserving normal Playwright click, HTTP/detail/body assertions, two-user seed, page 26, global read-all, zero-push assertion, one Chromium attempt and zero-skip gate. No `force`, `dispatchEvent`, screenshot/trace persistence or lowered expectation was introduced. `git diff --check E..F -- frontend/e2e` exited 0.

`/tmp/wta-t41/f-source-equivalence.json` states E→F only this isolated real-501 spec changed, backend/runtime/generated/config/lock and C production-dist inputs unchanged; 328 dist artifacts were rehashed. E's separate API-controlled Chrome regression `/tmp/wta-t41/api-controlled-runs-v2/2fe330f006dff4ae/result.json` has acceptance true/exit 0, exact E source before/after clean, **5 expected / 0 unexpected / 0 skipped / 0 flaky**, each one attempt, unchanged 328-artifact preview proof and clean preview/Playwright groups plus closed loopback port. These five T34 shared regression cases do not execute the T41 real-501 spec; they may be reused by byte-equivalent inputs, not relabelled as an F execution. The 24 offline runner tests are also E evidence on unchanged Python files, not a fresh F run.

Still missing at this checkpoint: F clean full package/JAR proof; F exact real-501 result showing the two button clicks, page 26, all 501 A read, B untouched, pre-existing timestamps preserved, one passing Chromium case with zero skips, F source/JAR before-after equality, and owned process/container/volume/port cleanup. Lead retains final core/whole-ticket acceptance. Do not convert E's failed browser run or the E five-case preview result into F 501 acceptance.

## Actual F real-501 attempt — failed, retained

Lead's safe `/tmp/wta-t41/browser-runs/62047a08743c9ffa/result.json` records **acceptance false, exit 1**, exact intended Chromium test one attempt, `expected=0/unexpected=1/skipped=0/flaky=0`. Failure location is `frontend/e2e/inbox-paged-real.e2e.ts:112:54`, the positive B shared-title visibility assertion. The sequential assertions before that line reached A's page-26 one-row/oldest/foreign-negative checks and B login, unread 2 and two visible rows. It did **not** establish B shared/B-only positive assertions, A detail/read-all, B unchanged afterward, or final SQL preservation; `seed_after` is absent. Do not infer a passing ticket from the progressed browser route.

A static source explanation for the failed locator is concrete: `seed_plan` writes each message's title and summary text identically, and `InboxPage.vue` renders both title and summary in separate table cells. `b.getByText(manifest.shared.title)` can therefore match two elements in one row; the next B-only positive `getByText` has the same shape. A row-scoped positive assertion on the existing two-row table, for each shared/B-only title, would preserve the two-user contract and ordinary browser interaction. The safe result contains only numeric failure location, so the precise Playwright exception text is not claimed.

F source before/after is the exact clean commit/tree above; JAR SHA256 before/after is `486989a5a86ac601f5587236e44e0856754057a3bd63c5a21d8d62cca95d95e3`. Playwright/Vite/backend process groups have no live members; all owned containers and captured anonymous volumes are gone, all five loopback ports are closed, cleanup errors empty. The F real-501 gate remains **failed** and needs a new fixed source plus fresh real run after any bounded selector correction.

## F snapshot finalized before G

Additional safe F records are complete and remain valid as *F results*: `/tmp/wta-t41/f-full-package.json` and `f-full-package-proof.json` show clean F full-JAR package exit 0 and the same `486989a5…95e3` artifact observed unchanged through the failed browser run; `f-openapi-check.json` exit 0. The `fcore-{core-package,core-bundle,skill-facts,fullstack-facts,notify-layered,fm,handbooks}.json` records each have exit 0, and `fcore-source-before/after.json` both bind exact clean F/tree `2ee339a27589ea393baca0d4131f598981f5abae`. These checks do not change the decisive F real-501 browser `acceptance:false` result. **Final F attempt disposition: rejected for incomplete browser acceptance, with clean resource/source closure.** A future G selector-only revision needs its own fixed diff, source-equivalence and real browser result; do not overwrite or relabel F's snapshot.
