# T-43 evidence

Implementation commit `9267ecb52cd8ec8c663a41f06b10eb7e0b3fe6ca`. Probe file unchanged from baseline, SHA256 `94ae510b968474abc30a34fbd8a8a7a9431b90ecfd9fcfc0ed36b0f64e78186a`.

## Commands

Same-probe after measurement, one fresh MySQL/Redis JVM per case. Each command exited 0. The runner's `acceptance` field is false in smoke mode; `smoke_passed` was true and each case `accepted` was true. No test was skipped.

```bash
python3 /tmp/wta-t43/run-notify-fanout-measurement-v2.py --execute \
  --expected-head 9267ecb52cd8ec8c663a41f06b10eb7e0b3fe6ca \
  --expected-test-probe-sha256 94ae510b968474abc30a34fbd8a8a7a9431b90ecfd9fcfc0ed36b0f64e78186a \
  --mode smoke --size <100|1000|10000> --method <measurePublishedAllInAppFanout|measureBoundedInAppResultAggregation|failedFanoutInsertRollsBackNoticeAndWake>
```

`node .agents/skills/namewta-fullstack-development/scripts/validate-module-mode.mjs backend/wta-modules/wta-notify --mode layered` exited 0.

## Before / after

Before is three repetitions at `7933bdff61a5dbd620b869be7171b61b83fed845` (`a-baseline/summary.json`). After is one repetition at `9267ecb52cd8ec8c663a41f06b10eb7e0b3fe6ca`. JDBC `executeUpdate` counts are client calls, not network round trips. `executeBatch` stayed 0 because the candidate uses one multi-row INSERT per chunk of 500, on the current SqlSession. Lock-wait delta was 0 in every sample; that is absence of contention in the sample, not a production lock SLA. No SLA is set.

| size | before updates (rep 1) | before publish ms (rep 1) | after updates | after publish ms | after rows recipient/delivery/outbox |
|---|---:|---:|---:|---:|---|
| 100 | 305 | 584.2 | 8 | 272.1 | 100/100/100 |
| 1000 | 3005 | 3111.1 | 11 | 1015.2 | 1000/1000/1000 |
| 10000 | 30005 | 22664.6 | 65 | 4359.5 | 10000/10000/10000 |

Publish is fewer statements and shorter on this probe. Aggregation was not rewritten. Ten result commits still lock-read every delivery: after for-update rows were 1030 / 10030 / 100030, and JDBC updates stayed 71. After result times were 1000.3 / 935.3 / 1381.4 ms versus before rep-1 times 710.4 / 585.4 / 890.1 ms. That is not a speedup, so no new counter state machine was added.

## Rollback and limits

10000-recipient insert failure at `9267ecb5`: 1 JDBC rollback, 0 commit, 0 wake, recipient/delivery/outbox rows 0. The owned test also required the notice to stay DRAFT and the intent count to stay 0.

ALL 100000 reaches the first intent insert and 100001 is rejected before any business write. Surefire at the baseline commit: tests=2, failures=0, errors=0, skipped=0. Copy: `T-43-current-2026-09-24/b-candidate/all-recipient-limit-surefire.xml`. `resolveUsers` still throws before `insert` / `insertFanout`. That surefire file was not re-executed on `9267ecb5`.

`NotifyDispatchResultService` and claim SQL were not edited. The aggregation probe re-ran the real result lock and `NotificationAggregatePolicy` assertion. The mock lease unit test was not re-run.

## Skill

engineering-standards notification and layered-module rules were applied: DAO owns the mapper batch, the use case transaction boundary is unchanged, and external provider I/O stays outside this submit write.
