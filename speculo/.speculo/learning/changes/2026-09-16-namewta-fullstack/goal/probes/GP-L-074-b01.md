---
artifact: learning-goal-probe
probe_id: GP-L-074-b01
lesson_id: L-074
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-074 / b01

审问对象是已写成的 Lesson `lessons/L-074-test-demo.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **一格**：`A:TestDemoController.list,page,importData,export,getInfo,add,edit,remove`。OBJ-74 原文：能口述 `TestDemoController` classic CRUD（含导入导出）作为成熟样例。档是**解释+走查**。矩阵 (a) **有独行**，八名与磁盘 1:1：`list, page, importData, export, getInfo, add, edit, remove`，模块 `wta-demo`，仍 uncovered，备注「成熟 CRUD」。矩阵 (a) **没有** `ITestDemoService` / `saveBatch` HTTP / `DemoPage.vue` 行。厨房同行 `createDemoService, createRichTextService` 仍 uncovered（L-077）。九工厂行里的 `createDemoWebDomain` 仍 uncovered。矩阵 (b) **没有** 两本分页 / 导入丢 `saveBatch` / 点名删除 / `@DataPermission` 五枪独行；已印的 (b) 不挂本课符号。矩阵 (c) `wta-modules` / `layered-classic` 已 covered（L-002 / L-003）；登记表 classic 行就是 `wta-modules/wta-demo`。MySQL 8.4 / Redis 8 / `dao-mapper-ownership` 仍 uncovered。矩阵 (d) **没有** 单表 CRUD 走廊；已 uncovered 的存储点仍是「业务表只进 10/50 基座」（挂 L-085）。本批按派单只审问这一格 (a) 切片；口试按磁盘：同一门牌 `/demo/demo` **正好 8** 个公开映射，classic 三跳，门卫只抱 `ITestDemoService`，`/list` 走 wrapper、`/page` 才进 XML，导入 multipart 且丢掉 `saveBatch` 布尔，导出是 **POST** 流、返回 `void`，`getInfo` 可空且不滤部门，`add` 用 `ValidatorUtils` + 2 秒防重，`edit` 用 `EditGroup` + 默认 5000ms 防重，`remove` 点名一票否决后逻辑删。厨房五枪 + `runtime.download` ≠ 八扇。不要把 Service/Mapper 升成第二张 (a)，不要发明 (b)/(d)，也不要把 L-075 树表、L-076 富文本、L-077 厨房/三键、或 deferred 玩具柜算进本切片。

抽查锚点（2026-09-17）：`TestDemoController.java`；`ITestDemoService.java`；`TestDemoServiceImpl.java`；`TestDemoMapper.java` 与 XML 仅 `customPageList`；`TestDemo` / `TestDemoBo` / `TestDemoVo` / `TestDemoImportVo`；`PageQuery`；`RepeatSubmit` / `RepeatSubmitAspect`；`BaseController.toAjax`；`R.ok(String)`；`ExcelBuilder.ReadBuilder` / `DefaultExcelListener` / `DefaultExcelResult.getAnalysis`；`BaseMapperPlus.selectVoById` / `insertBatch`；登记表 `03-backend-module-modes.md`；`wta-admin/pom.xml` `bundle-full` / `bundle-core`；DDL `test_demo`；DML 菜单 1761400000000001500–1505 与十三行种子；对照 `TestBatchController` / `TestEncryptController` / `TestDemoEncrypt` / `TestExcelController` / `TestTreeController.export`；厨房 `domains/demo/src/{index,index.test,transport,test-demo/index}.ts`；web-domain `createDemoWebDomain` 与 `DemoPage.vue`；厅堂 `services.ts` / `http.ts` `download`；`downloadWithAxios` `client.post`；OpenAPI `/demo/demo**`。工作树 **零** `TestDemo*Test`（`wta-demo` 测试目录几乎只见 `TestRichTextListAutoMapperTest`）。

## Q1 澄清

- objective_id: OBJ-74
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-74；chain L-074 覆盖列只 `A:TestDemoController.list,page,importData,export,getInfo,add,edit,remove`；L-074 `claimed_cells` 一格；矩阵 (a) 八名独行仍 uncovered、无 Service/Vue 行；厨房同行与九工厂行仍 uncovered（L-077）；(b) 无本课符号；(c) 房间与登记表已 covered；(d) 无单表 CRUD 流，已 uncovered 的是 10/50 基座；课开篇八扇 + 负空间 L-075 / L-076 / L-077 / L-003 / deferred 玩具柜
- difficulty: medium

课把单表抽屉收成「一块牌子、八扇窗、classic 三跳、样板 ≠ 模板」。计划里至少有四套可数对象，派单点名 **一格 (a) 切片**：

1. **OBJ-74 原文。** `course.md`：能口述 `TestDemoController` classic CRUD（含导入导出）作为成熟样例。一句里是 **走查八扇 HTTP**，不是树表六扇、不是富文本 OSS、也没有把厨房五枪写成第二张矩阵行。课把 OBJ 扩成 classic 抽屉 + 两本分页 + 数据权限贴点，那是走查，不是第二格。
2. **chain / 课 `claimed_cells`。** 一格。符号就是八个方法名。矩阵把它们印成独行，与磁盘公开映射 1:1。chain **不**写 Service、不写 Vue、不写 (b)。`ITestDemoService.saveBatch` **没有**自己的 HTTP。
3. **矩阵 (a) 对齐。** 本课有独行，Lead 按现印八名标 covered **不会**把树表/富文本或厨房假闭合。不要把 `ITestDemoService` 或 `DemoPage.vue` 另立一行才给本切片盖章。厨房同行与九工厂行仍 uncovered，本课不得当第二张前端章。
4. **课自己的操作定义。** 门牌 `/demo/demo`；GET 三 / POST 三 / PUT 一 / DELETE 一；无类上 `@SaIgnore` / `@ConditionalOnEnable`。八扇都贴权限字；`list` 与 `page` 同为 `demo:demo:list`。门卫只注入 `ITestDemoService`，不抱 Mapper。`export` 在 Java+菜单+页面都有扳机，但厨房对象上没有 `exportDemo`。`page` / `importData` 在 Java+OpenAPI，管理页没扳机。
5. **导入导出不是第二张 claimed 格。** OBJ 要「含导入导出」。课把「后端有 import 窗、种子/权限组/页面没收」和「导出走 download 管、动词是 POST」收进对照走查。不要为了盖章把 `createDemoService` 再立一行，也不要把无页导入降成「本课不认这扇窗」。
6. **邻格。** L-075 树表六扇（`list` 扁、GET 导出）。L-076 富文本与 OSS。L-077 厨房五枪 / `createRichTextService` / 三键菜单。L-003 已认 classic 且禁止把 demo 当 layered 反例，本课不得再盖 `C:layered-classic`。Redis/MQTT/MCP/Excel 模板等 Controller 保持 `deferred(capability-demo-not-product-room)`。`TestBatchController` 是越层反例，不标 covered。

问：本格闭合标准是「八扇路径/动词/权限/信封 + classic 三跳门卫不抱 Mapper + `/list` 无 XML vs `/page` 有 XML + 导入丢写库成败 + 导出 POST 流 + `getInfo` 不滤 + 删前点名一票否决 + 厨房五枪没收 page/import、导出走 download，且不升 Service/Vue 矩阵行、不发明 (b)、不提前盖 L-077」？还是 OBJ 那句必须把八个方法拆成八行、或必须等 GP-L-077 厨房缺枪才给本切片盖章？Lead 若凭「demo 房间还没挖完」把这一行留空洞，是否假判成未讲完？Lead 若用本课把厨房或树表标 covered，是否假闭合了 L-077 / L-075？

## Q2 证据

- objective_id: OBJ-74
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 42–48 / 53–56 / 62–65 / 73–82 / 88–97 / 105–109 / 115–123 / 129–134 / 142–147；Service 18–85；ServiceImpl 32 / 41–43 / 53–56 / 67–70 / 80–81 / 90–97 / 107–114 / 124–127 / 135–137 / 147–156 / 165–167；Mapper 24–94；XML 5–9；Entity 18–66；Bo 21–67；Vo 26–117；ImportVo 17–53；PageQuery 54–59 / 68–69；RepeatSubmit 20–25；Aspect 52–55 / 69；BaseController 19–30；R 64–98；ExcelBuilder 653–658 / 724；Listener 47；DefaultExcelResult 84–95；BaseMapperPlus 143–145 / 206–223；modes.md 24；pom 150–204；DDL 1691–1707；DML 158–163 / 358–370；Batch 22–30；Encrypt 16–17 / 24；Excel 29；Tree 35 / 55；厨房 index.ts 84–96 / 109–134；index.test.ts 14–48 / 51–64；transport 6–14；资源标签 3；web-domain 29–81；index.test.ts 32–36 / 51–52；DemoPage 32–57 / 162–169 / 208 / 244–259 / 266–283；services.ts 69；http.ts 93–96；downloadWithAxios 261–265；OpenAPI 766–783 / 2968–3001 / 6989–7020；零 TestDemo*Test
- difficulty: high

课的八扇 + classic 抽屉哪些能在工作树直接复现，哪些只是测试名单比课表窄、或口试把邻课窗说成本格第九扇？

**仍能复现（本切片）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 类上 `@Validated` `@RequiredArgsConstructor` `@RestController` `@RequestMapping("/demo/demo")`，继承 `BaseController`；**无** `@ConditionalOnEnable` / `@SaIgnore` | Controller 42–46；类文件 grep 空 |
| 构造只注入 `ITestDemoService`；不抱 Mapper / `LoginHelper` | Controller 48 |
| 八扇：`GET /list`、`GET /page`、`POST /importData` multipart、`POST /export`、`GET /{id}`、`POST /`、`PUT /`、`DELETE /{ids}` | Controller 53–147 |
| `list` / `page` 权限都是 `demo:demo:list`，都回 `PageResult`；入参 `@Validated(QueryGroup.class)` 但 Bo **无** QueryGroup 约束 | Controller 53–65；Bo 21–67 无 QueryGroup |
| `list` → `queryPageList` → `selectVoPage`；**不执行** XML | ServiceImpl 53–56；XML 只有 `customPageList` |
| `page` → `customPageList` → XML `SELECT * FROM test_demo ${ew.customSqlSegment}` | ServiceImpl 67–70；XML 7–8；OpenAPI 6989–7000 GET + list 权限 |
| `buildQueryWrapper`：`deptId`/`userId` eq；`testKey` **like**；`value` **eq**；`orderByAsc(id)` | ServiceImpl 90–97 |
| 不带 `pageSize` 等于整表：`DEFAULT_PAGE_SIZE = Integer.MAX_VALUE`（注释「默认查全部」） | PageQuery 56–59 / 68–69 |
| `importData` 权限 import、`@Log IMPORT`、**无** `@RepeatSubmit`；`validate(true)` 后再 `doRead`；`saveBatch` 返回值丢掉；`R.ok(getAnalysis())` | Controller 73–82 |
| ReadBuilder 默认 `validate=true`、`failFast=true`；监听默认 failFast | ExcelBuilder 653–658；Listener 47 |
| 分析句只统计读 Excel：空「读取失败，未解析到数据」；全成功「恭喜您，全部读取成功！共N条」 | DefaultExcelResult 84–95 |
| `R.ok(String)` 重载把分析句放进 **msg**、data 为 null（声明 `R<Void>`） | R 97–98；Controller 76 / 82 |
| `saveBatch` → `insertBatch` → MP `Db.saveBatch`；**不**走 `insertByBo` / 空 TODO | ServiceImpl 165–167 / 135–137；BaseMapperPlus 143–145 |
| ImportVo 校验在默认组；`orderNum` 是 `Long`；无 id / version | ImportVo 23–53 |
| `export` 权限 export、`@Log EXPORT`、返回 `void`、`queryList` → `selectVoList` → sheetName「测试单表」；雪花 id 循环已注释 | Controller 88–97；OpenAPI 2988–3001 **post** |
| Vo `@ExcelIgnoreUnannotated`；`userId` `index = 5`；`version` 无 Excel 注解 | Vo 26 / 50 / 114–117 |
| `getInfo` 权限 query、路径 `@NotNull`、`R.ok(queryById)`；`selectVoById` → `selectById`，**没贴**数据权限；无行不 fail | Controller 105–109；ServiceImpl 41–43；BaseMapperPlus 206–223；Mapper 无 `selectById` override |
| `add` 权限 add、`@Log INSERT`、`@RepeatSubmit(interval=2, SECONDS)`、`ValidatorUtils.validate(bo, AddGroup)`、`toAjax(insertByBo)` | Controller 115–123 |
| `edit` 权限 edit、`@Log UPDATE`、`@RepeatSubmit` 默认 5000ms、`@Validated(EditGroup)`、`toAjax(updateByBo)` | Controller 129–134；RepeatSubmit 20–25 |
| 防重间隔换算后 `< 1000ms` 直接抛；键含 URL + token + 参数 MD5；`setObjectIfAbsent` | Aspect 52–69 |
| `insertByBo` 成功把 id 写回 Bo；HTTP 仍 `R<Void>`；`insert` **没贴**数据权限 | ServiceImpl 107–114 |
| `updateById` **贴了**数据权限；0 行 → `toAjax` fail | Mapper 90–94；BaseController 29–30 |
| `remove` 权限 remove、`@Log DELETE`、**无** `@RepeatSubmit`；路径 `Long[]` + `@NotEmpty`；`isValid` 写死 `true` | Controller 142–147 |
| 点名：`selectByIds`（`joinStr=AND`）`list.size() != ids.size()` → `ServiceException("您没有删除权限!")`，短路零删除；随后 `deleteByIds` **没贴**数据权限 | ServiceImpl 147–156；Mapper 76–81 |
| Mapper **五个** `@DataPermission`：`customPageList` / `selectVoPage` / `selectVoList` / `selectByIds` / `updateById`；类上无总开关 | Mapper 33–94 |
| 实体 `@TableName("test_demo")` `@Version` `@TableLogic`；`orderNum` `@OrderBy(asc=false, sort=1)` | Entity 18–66 |
| Bo Add/Edit：`deptId`/`userId`/`orderNum` `@NotNull`，`testKey`/`value` `@NotBlank`；id 只在 Edit；`version` 无校验 | Bo 31–67 |
| classic：Impl 唯一字段 `demoMapper`；登记表「不得作为新模块 layered 反例」 | ServiceImpl 32；modes.md 24 |
| 电闸在装配：`bundle-full`（默认激活）依赖 `wta-demo`；`bundle-core` **没有**这一行；不是运行时 `@ConditionalOnEnable` | pom 150–171 / 191–204 |
| 种子 C + F：list/query/add/edit/remove/export；**没有** `demo:demo:import` 菜单字；十三行练习数据 | DML 158–163 / 358–370；全仓 import 权限只在 Controller + OpenAPI |
| 厨房封面：list/get/add/update/delete；**没有** `pageDemo` / `importDemo` / `exportDemo` | index.ts 84–96 / 112–134 |
| 厨房测试锁 `/demo/demo/list` GET、详情编码、POST/PUT `/demo/demo`、删除 `demo%2F7,demo%208,demo%2C9` | index.test.ts 37–42 |
| 投影只留 id/deptId/userId/orderNum/testKey/value；缺字段 `''` / `0`；**丢掉** version / 审计列 | transport 6–14；DemoVO 34–41；AuditFields 9–15 也无 version |
| 页：`listDemo`；编辑先 `getDemo`；提交永远 toast「修改成功」；删除可多选；导出 `runtime.download('demo/demo/export', …)` | DemoPage 208 / 244–259 / 266–283 |
| 工具栏有新增/修改/删除/导出，**没有**导入钮；初始表单无 version | DemoPage 32–57 / 162–169 |
| download 管 `client.post` + `x-www-form-urlencoded`，对得上后端 POST | http.ts 93–96；downloadWithAxios 261–265 |
| web-domain `demo-table` 五串：list/add/edit/remove/export；**不含** query / import；测试整表锁死；缺托盘 throw `'DemoWebRuntime is required'` | web-domain 40–49；index.test.ts 32–36 / 51–52 |
| 资源标签 `controller:'TestDemoController'` `basePath:'/demo/demo'` | test-demo/index.ts 3 |
| 厅堂 `demoService = createDemoService(domainHttp, createRichTextService(...))` | services.ts 69 |
| 树表导出是 **GET** `/demo/tree/export`；单表才是 POST | TestTreeController 35 / 55 |
| `TestBatchController` `/demo/batch` **直持** `TestDemoMapper` | Batch 22–30 |
| `TestDemoEncrypt` 也 `@TableName("test_demo")` 且 extends `TestDemo`；Excel 模板另一门牌 `/demo/excel` | Encrypt 16–17；Excel 29 |
| 无 Java `TestDemo*Test` | 测试目录几乎只见富文本 AutoMapper |

**课写过、测试名单更窄或口试把邻课说偏（不是证伪本格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「测试锁死厨房五枪」 | 锁 URL + 逗号删除编码 + 投影缺字段。**没有** `pageDemo` / `importDemo` 不存在的 `not.toContain` |
| web-domain「权限组五串不含 query/import」 | 测试把五串整表 `toEqual`，query/import 缺席靠字面量 |
| `R.ok(excelResult.getAnalysis())`「回执是分析句」 | 因 `ok(String)` 比 `ok(T)` 更具体，分析句进 **msg** 不是 data。课没点 overload，信封仍是成功 + 分析句 |
| 「练习卡不跑流程」 | 真。不要读成「`wta-demo` 里没有流程课」——请假在 `wta-workflow` |
| 课 `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |
| 「厨房把 Vo 投影成 DemoVO」 | 只投影六列。`DemoForm` 继承的 `AuditFields` 也没有 version，和 L-077 对照句一致 |

问：在八扇映射、classic 三跳、两本分页 SQL 不同、导入丢写库成败、导出 POST 流、详情不滤、点名一票否决、厨房五枪 + download、种子无 import 已经能互证的前提下，(a) 是否还要把「`ok(String)` 进 msg」「厨房测试没写 not.toContain page」写成开篇半句，才算本切片讲完？还是这些已经落在课的表/反例里（overload 除外），Lead 可以把本切片视为已讲、并保持厨房/九工厂/树表/富文本 uncovered？

## Q3 假设与反例

- objective_id: OBJ-74
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无第九扇 HTTP saveBatch；无 GET /export；厨房无 page/import/export 方法；菜单无 import；getInfo 无数据权限；deleteByIds 无数据权限；validEntityBeforeSave 空 TODO；QueryGroup 挡不住空查询；add 用 ValidatorUtils 不是 AddGroup 注解；防重 2s vs 5s；导入 failFast 抛且成功读后不管 insert；TestBatch 直持 Mapper；Encrypt 同表；OBJ-74 不含厨房；bundle-core 整栋不进包
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪道闸认错？

1. **「八扇都走 XML。」** 只有 `page` 走 XML。`list` 是 `selectVoPage`。课反例 1 / 误区 3 覆盖。
2. **「`/list` 和 `/page` 权限不同。」** 都是 `demo:demo:list`。课反例 2 / 误区 4 覆盖。
3. **「导出是 GET，和树表一样。」** 单表 POST。树表才是 GET（L-075）。课反例 3 / 误区 5 覆盖。
4. **「页面能导入、能打 `/page`。」** 2026-09-17 都不能。课反例 4 / 误区 6 覆盖。
5. **「厨房八枪。」** 五枪 + 下载管。page/import 不在 `DemoService`。课反例 5 / 误区 7 覆盖。
6. **「这是 layered，Service 不许碰 Mapper。」** 登记 classic；Impl 必须能指到 `demoMapper`。课反例 6 / 误区 14 覆盖。
7. **「新模块就该抄这一份。」** 登记表禁止把 demo 当 layered 反例。课反例 7 / 误区 14 覆盖。
8. **「`TestBatchController` 也是成熟 CRUD。」** 越层入口；门卫直持 Mapper。课反例 8 / 误区 15 覆盖。
9. **「`getInfo` 也滤部门。」** `selectById` 没注解。课反例 9 / 误区 8 覆盖。
10. **「`deleteWithValidByIds` 会在 SQL 里再滤一次。」** 删语句没贴；Valid 是点名比大小。课反例 10 / 误区 9 覆盖。
11. **「导入失败会 `R.fail`。」** 读失败抛；读成功后 insert 成败被丢掉，仍成功信封 + 分析句。课反例 11 / 误区 13 覆盖。
12. **「`validEntityBeforeSave` 会查唯一 key。」** 空 TODO。课反例 12 / 误区 10 覆盖。
13. **「QueryGroup 会挡住空查询。」** Bo 上没有 QueryGroup 约束。课反例 13 覆盖。
14. **「OBJ-74 包含 `createDemoService`。」** 那是 OBJ-77。课反例 14 / 误区 1 / 20 覆盖。
15. **「加密实体是另一张表。」** `TestDemoEncrypt` 也是 `test_demo`。课反例 15 / 误区 16 覆盖。
16. **「`add` 用了 `@Validated(AddGroup)`。」** 用的是 `ValidatorUtils`。树表才是注解（L-075）。课误区 12 覆盖。
17. **「add 和 edit 防重一样长。」** 2 秒 vs 默认 5000ms。课边界 6 / 误区 11 覆盖。
18. **「第九扇 HTTP `saveBatch`。」** 只给导入打电话。课开篇对照表覆盖。
19. **「`bundle-core` 还留着 demo 窗。」** 整份模块不进 jar。课正例 10 / 误区 19 覆盖。
20. **「前端授权。」** `v-hasPermi` 藏按钮；REST 仍是 `@SaCheckPermission`。`query`/`import` 连权限组都没收。课边界 7 / 误区 17 覆盖。
21. **「不带 PageQuery 也只回一页。」** 默认页大小 `Integer.MAX_VALUE`。课边界 2 覆盖。
22. **「两列都是模糊搜。」** 只有 `testKey` like；`value` eq。课正例 2 / 边界 3 覆盖。

问：读完后仍说不出的，是不是「Controller 的名字」，而是 **八扇合同 ≈ 磁盘映射、缺的是厨房没收的 page/import 与菜单缺的 import、导出是 POST 不是 GET、详情不滤、点名失败整票作废、样板 ≠ 模板、九工厂/厨房行不能因本课闭合**？

## Q4 视角

- objective_id: OBJ-74
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-74 vs L-003/L-015/L-067/L-075…L-077；claimed_cells 八扇 vs 厨房五枪 / 一张 DemoPage；厨房同行仍 uncovered
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这一格 (a) 切片**？

| 视角 | 多出来的边 | 是否本课这一格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 八名独行 vs chain 八名；无 Service/Vue 行；(b) 无本课独行；(d) 无单表 CRUD 流 | **是** 闭合定义。本切片可视为已讲。独行可标 covered。不要发明 (b)/(d)，不要把一张 Vue 升成新行才能给切片盖章，不要用本课盖厨房章 |
| 单表门卫 | 八扇路径/动词/八串权限、`list`/`page` 同权不同 SQL、导入丢布尔、导出 POST `void`、2s vs 5s、点名 `true` | **是** 切片里的 `TestDemoController.*` |
| classic 厨师 | 两分页、空 TODO、点名比大小、`insertBatch`、wrapper like/eq | **是** 走查后果，不是矩阵第二格 |
| Mapper / XML | 五个 `@DataPermission`；XML 只服务 `/page`；详情/insert/deleteByIds 没贴 | **是** 对照。XML 保持 covered-by-parent，不要另开课 |
| 导入窗 | Java+OpenAPI 有 `demo:demo:import`；种子无 F；权限组无；页无钮；分析句不管 insert | **是** 对照。不要把「没按钮」说成「没这扇窗」 |
| 导出窗 | Java+菜单+页按钮都有；厨房无 `exportDemo`；download 走 POST | **是** 对照。和分类课「有窗无钮」相反，不要抄错 |
| 厨房作者（L-077） | 五枪 URL；投影丢 version；权限组漏 query/import；toast「修改成功」 | 本课借用封面形状与没收窗负空间 **是**。工厂盖章 **否**（L-077） |
| 树表（L-075） | 六扇、扁 List、GET 导出、无 import/`/page` | 导出动词对照 **是**。树表格子 **否** |
| 富文本（L-076） | OSS 资产口 | 负空间 **是**。资产解析 **否** |
| 用户柜台（L-015） | 专用 Listener 写密码；数据权限闸在 Service | 导入对照 **是**。用户房间 **否** |
| 分类窗（L-067） | 扁 List、601 黄灯、运行时电闸、无分页 | 信封/电闸对照 **是**。分类七扇 **否** |
| 登记表（L-003） | classic 允许 Impl 抱 Mapper；禁止抄新模块；Batch 是拧松 | 不改登记 **是**。房间画像 **否**（已 covered） |
| 装配 | `bundle-full` 才进 admin；`bundle-core` 整栋实验室不进包 | 电闸对照 **是** (a)。job bundle 格子 **否**（L-079） |
| MySQL 8.4 | 表 `test_demo` + 十三行种子 + 菜单无 import | 种子对照 **是** (a)。容器 **否** |
| Redis 8 | 防重 `setObjectIfAbsent` | 副作用一句 **是**。容器 **否** |
| 玩具柜 | Redis/MQTT/Excel 模板/加密同表 | 负空间 **是**。保持 deferred **否**当 covered |

问：Lead 若凭「八个方法名在独行里」把该行标 covered，是否还要求读者能指着 **八扇合同、classic 三跳、两本分页、导入丢写库、导出 POST、详情不滤、点名一票否决、厨房五枪 + download、样板 ≠ 模板**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要等 L-077 才给**本切片**当已讲？厨房同行是否必须保持 uncovered，本课只往备注写「单表八扇消费已讲」？

## Q5 元问题

- objective_id: OBJ-74
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 八名独行仍 uncovered、厨房同行仍 uncovered、无本课 (b)/(d)
- difficulty: low

建议：`defer`

原因：

- **`A:TestDemoController.list,page,importData,export,getInfo,add,edit,remove` 是 chain 切片，也是矩阵独行。** L 合同章节齐全（宏观地图、定义表、机制链 1–10、带 caption / 文字等价物的三张图、正反例、变式、误区、暂停走查、来源表 S-004/S-008/S-010/S-L074-01…09）。能复现：八扇映射与权限字、`/list` 无 XML vs `/page` 有 XML、导入 multipart 丢 `saveBatch`、导出 POST 流、`getInfo` 可空且无数据权限、`add` 工具校验 + 2 秒防重、`edit` 注解校验 + 5 秒防重 + 更新带数据权限、`remove` 点名一票否决后逻辑删、classic 门卫不抱 Mapper、厨房五枪没收 page/import、导出走 download、种子无 import、`bundle-core` 整栋不进包。不必为「Controller 的名字」再挖五问。
- **Lead 可以把本课这一行标 `covered`。** 八名与磁盘 1:1，不会绑死树表/富文本/厨房。**厨房同行与九工厂行保持 uncovered**，等 L-077，不要用本课开第二张前端章。不要发明单表 (b) 行：两本分页 / 导入丢布尔 / 点名删除 / 五枪数据权限已是 (a) 的解释到副作用。
- 本批可粘贴的精度项不挡住切片视为已讲：（1）`R.ok(String)` 让分析句进 msg 不是 data；（2）厨房测试锁五枪 URL，没有 `not.toContain` page/import；（3）课估时 38、chain 35。不要把树表六扇、OSS 资产、厨房 URL 全表塞进本格。
- 不建议 `mine-more`：b01 未证明本切片还有真实矩阵空洞；再五问只会把 L-077 厨房、L-075 树表或 L-076 富文本再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；overload 是半句精度，不是第二容器。不建议 `split-lesson`：未满 10 问；八扇是 chain 原设计的同一 OBJ-74，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行。不建议等 GP-L-077 才给本切片当已讲。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:TestDemoController.list, page, importData, export, getInfo, add, edit, remove | 课按磁盘讲了八扇路径/动词/权限/信封、classic 三跳、两本分页 SQL 不同、导入丢写库成败、导出 POST 流、详情不滤、点名一票否决、厨房五枪 + download、种子无 import、样板 ≠ 模板。残留精度：`ok(String)` 进 msg；厨房测试没写 page/import 缺席断言；课估时 38 vs chain 35。 | 可选半句：导入回执因 `R.ok(String)` 落在 msg。不要求重写机制链。Lead **可以**改本行为 covered。 | defer（切片已讲；独行可闭合） |
| A:createDemoService, createRichTextService | 厨房同行仍 uncovered。本课只借用五枪 URL、投影丢字段、download 导出与没收窗负空间。 | 不要用本课盖厨房章。备注可写「单表八扇消费已讲」。 | defer（L-077） |
| A:createDemoWebDomain（九工厂行） | 本课只用 `demo-table` 五串权限 + 无 query/import 当切片证据。其余工厂未因本课讲完。 | 保持 uncovered。 | defer（L-077 / 九工厂行保持 uncovered） |
| A:TestTreeController.list, export, getInfo, add, edit, remove | GET 导出对照已借用。 | 保持。L-075。 | defer（L-075） |
| A:TestRichTextController.list, create, update, remove, assets, get | OSS 负空间已点名。 | 保持。L-076。 | defer（L-076） |
| C:layered-classic / wta-modules | 登记表 classic 行已 covered。 | 不要用本课再盖房间章，也不要发动迁 layered。 | defer（L-003 已 covered） |
| demo Redis/MQTT/MCP/Excel/ES/WebSocket/限流/加密/敏感/SaToken文档/邮件短信演示/队列 Controller | 模式由本课 covered-by-parent；格子保持 deferred。 | 不要把玩具柜标 covered。 | defer（deferred(capability-demo-not-product-room)） |
| TestBatchController | 不是矩阵行。课用作越层反例。 | 不要发明第九扇或第二张 (a)。 | defer（未入库） |
| (b) 两本分页 / 导入丢 saveBatch / 点名删除 / 五枪数据权限 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 单表 CRUD 流 | 矩阵无此行。 | 不要发明前端单表数据流行。 | defer（未入库） |
| Mapper XML / BO/VO getter | `customPageList` 与校验组已走查。 | 保持 covered-by-parent。 | defer（covered-by-parent） |
| 测试夹具 TestDemo*Test | 工作树没有这类测试。 | 保持 deferred(test-fixture)。证据走 Controller/SQL/厨房测试。 | defer（deferred(test-fixture)） |
| MySQL 8.4 / Redis 8 | 种子表与防重缓存是 (a) 对照，不是容器闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本切片无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（厨房/树表/富文本是邻课格子，课内已把八扇讲完并显式禁止整行假闭合）；合格读者说得出函数目的（实验室八扇窗、classic CRUD 含导入导出）、主路径（wrapper 分页 / XML 分页 / Excel 读入 insertBatch / Excel 流出 / 可空详情 / 过校验新增 / 带数据权限更新 / 过点名逻辑删）与失败路径（failFast 整次中止、点名越权整票作废、更新 0 行 fail、防重抛、关装配 404、空 TODO 不挡重复 key）；把可选半句写进原课不会超过 40 分钟；邻课已规划。满 10 问前不拆。八扇虽跨读写/导入导出/两本分页，仍是 chain 原设计的同一 OBJ-74，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对厨房/九工厂、L-075 / L-076 邻窗、L-003 已 covered 的登记表、deferred 玩具柜、未入库的 Batch/(b)/(d)、covered-by-parent 的 XML、deferred 的测试夹具、以及 MySQL/Redis 容器显式建议 `defer`；对 `A:TestDemoController.list,page,importData,export,getInfo,add,edit,remove` 建议视为已讲切片、**可以**把矩阵八名独行标 `covered`、**不要**用本课盖仍 uncovered 的厨房同行与九工厂行。本课不主张改 Java 窗、发明 Vue 矩阵行、或把 `saveBatch` 升成第九扇 HTTP。
