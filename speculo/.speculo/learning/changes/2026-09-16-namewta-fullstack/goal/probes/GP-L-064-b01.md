---
artifact: learning-goal-probe
probe_id: GP-L-064-b01
lesson_id: L-064
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-064 / b01

审问对象是已写成的 Lesson `children/2026-09-14-wta-third/lessons/L-005-third-gateway-spi.md`（chain 全局 ID L-064 / OBJ-64，子课号仍是 L-005 / OBJ-05）与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（派单 **B:ThirdPartyGateway.execute + D:fail-third-http + D:third outbound happy path**；chain 覆盖列 **只印两格** `B:ThirdPartyGateway.execute D:fail-third-http`）：矩阵 (b) `ThirdPartyGateway.execute` 口号「出站 HTTP、限流、记 invocation」仍 `uncovered`。矩阵 (d) 「主路径 third 出站」原文 `ThirdPartyGateway.execute → 限流/凭证/HTTP/SPI → 记录`，备注 **只挂 L-064**，仍 `uncovered`；chain **未**把这条主走廊写成覆盖列。矩阵 (d) 「失败路径 third HTTP」原文 `超时/拒绝/凭据缺失 → 失败分类入库`，备注 **只挂 L-064**，仍 `uncovered`。OBJ-64 原文：能口述 `ThirdPartyGateway.execute` → Adapter → SPI → 记录。档是**方法性状**。child 课 OBJ-05 把同一窗收成一次出站：`ThirdPartyGateway` → `ThirdGatewayAdapter` → 限流/凭证/HTTP/SPI `ThirdProviderAdapter` → 记录。child 头 **未**列 `claimed_cells`。矩阵 (a) **没有** `ThirdPartyGateway.execute` 独行（无管理 HTTP）。矩阵 (c) `wta-modules` 业务房间 / `wta-api` 已 covered（L-002/L-003）；MySQL 8.4 / Redis 8 / `dao-mapper-ownership` 仍 uncovered。本批按派单审问 **(b) 出站管道 + (d) 主路径出站 + (d) 失败分类信封**。Provider 六扇留给 L-060；Endpoint 六扇与选门键留给 L-061；Credential 加密/不回显留给 L-062；invocation/statistics 查询窗留给 L-063；厨房 `createThirdService` 留给 L-065；四键页留给 L-066。不要把 Registry/Factory/`createTyped` 升成第二张 (a)，也不要用本课去盖 Redis 容器。

抽查锚点（2026-09-17）：`backend/wta-api/src/main/java/org/namewta/third/api/{ThirdPartyGateway,ThirdPartyRequest,ThirdPartyResponse,ThirdPartyFailureCategory}.java`；`backend/wta-modules/wta-third/src/main/java/org/namewta/third/adapter/gateway/ThirdGatewayAdapter.java`；`spi/{ThirdProviderAdapter,ThirdProviderAdapterRegistry,ThirdProviderAdapterStartupValidator,ThirdAdapterRequest,ThirdAdapterResponse}.java`；`adapter/{store/ThirdConfigCacheAdapter,resilience/ThirdResiliencePolicyAdapter,observability/ThirdInvocationRecorderAdapter,security/ThirdCredentialCryptoAdapter}.java`；`http/{ThirdHttpClientFactory,ThirdTypedHttpExchange}.java`；`support/{ThirdEndpointSecurity,ThirdRejectedException,ThirdLimitLease}.java`；`port/{ThirdConfigSnapshot,ThirdConfigSnapshotPort,ThirdResiliencePort,ThirdInvocationRecorderPort,ThirdCredentialStore,ThirdCredentialCryptoPort,ThirdOutboundAttempt}.java`；`dao/ThirdCredentialDao.java`；测试 `adapter/gateway/{ThirdGatewayAdapterTest,ThirdGatewayApplicationContextTest}.java`、`adapter/resilience/ThirdResiliencePolicyAdapterTest.java`、`adapter/observability/ThirdInvocationRecorderAdapterTest.java`、`http/ThirdHttpClientFactoryTest.java`、`spi/ThirdProviderAdapterRegistryTest.java`；旁证 `wta-admin/.../ThirdRedisIntegrationTest.java`（限流 IT，格子仍是本课机制不是 Redis 容器）；旁证（格子归邻课）`controller/admin/Third{Provider,Endpoint,Credential,Observability}Controller.java`（L-060…L-063）；`frontend/packages/domains/third`（L-065）；登记表 `03-backend-module-modes.md` **未逐行点名** `wta-third`；`wta-third/AGENTS.md`；Skill `wta-module-guide` third 索引。`controller/` 下 **零** `execute` HTTP。工作树除测试外 **零** 业务模块注入 `ThirdPartyGateway`。**零** 过期凭证网关单测。

## Q1 澄清

- objective_id: OBJ-64
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-64；child course OBJ-05；chain L-064 覆盖列只 B+fail-third-http；矩阵 (b) execute 仍 uncovered；(d) 主路径 third 出站与失败路径 third HTTP 只挂 L-064；负空间 L-060/L-061/L-062/L-063/L-065/L-066；(a) 无 execute 行
- difficulty: medium

课把出站收成「寄信只能交出站收发室」。计划里至少有三套可数对象，派单点名的是 **一格 (b) + 两条 (d)**：

1. **OBJ-64 原文。** `course.md`：能口述 `ThirdPartyGateway.execute` → Adapter → SPI → 记录。档是方法性状。一句里是 **统一出口 + 适配器管道 + 可选 SPI + 尽力记录**，没有把 Provider/Endpoint/Credential 管理六扇、invocation 查询窗、厨房工厂写成口试清单，也没有把 `createTyped` / `@HttpExchange` 写成第二根管子。
2. **chain / 子课。** 覆盖列两格：`B:ThirdPartyGateway.execute`、`D:fail-third-http`。不写 happy (d)，不写 (a)，不写 L-063 查询。子课号仍是 L-005，child 头无 `claimed_cells`。前置 L-061、L-062。
3. **矩阵对照。** (b) 口号「出站 HTTP、限流、记 invocation」与磁盘 `executeInternal` 1:1。(d) 主走廊原文就是这根管子，备注只挂本课——和 L-052 那种「主路径还要邻课」不同，**本课可以独盖**。(d) 失败口号「超时/拒绝/凭据缺失 → 失败分类入库」：磁盘分类是 `ThirdPartyFailureCategory` 信封；「凭据缺失」对不上空列表（空列表继续发信），应对「过期 / 解密失败 → `CONFIG_UNAVAILABLE`」；「入库」是尽力 `record`，不是强一致。(a) 没有 execute 行：本切片无管理 HTTP。
4. **课自己的操作定义。** 公开合同在 `wta-api` 三个 `execute`；实现类 `ThirdGatewayAdapter` 七个 `private final`：`configCache`、`adapterRegistry`、`resiliencePolicy`、`invocationRecorder`、`credentialStore`、`credentialCrypto`、`clientFactory`。顺序：快照 → `validateSnapshot` → Registry.find → 供应商状态再门状态 → `acquire` 租约 → 拼装/凭证/prepare → `clientFactory.create` → 循环 `byte[]` → decode/map/convert → `record` → finally `lease.close()`。失败尽量收成 `ThirdPartyResponse`，分类靠 category 不靠文案。
5. **邻格。** L-060 登楼、`evict(code, null)`。L-061 登门、选门键、保存期安检。L-062 加密入库、列表不回显、管理窗不 `decrypt`。L-063 才读 invocation/statistics。L-065/L-066 厨房与页。本课是管子本身。
6. **未入库。** 矩阵 **没有** `ThirdGatewayAdapter` (a) 行、没有 Registry/Factory/`createTyped` 独行、没有「失败路径 管理保存」第二 (d)、没有业务模块调用方行。Skill 把 gateway 写进 service、typed 走 `@HttpExchange`；Java 以 Adapter + `RestClient` 为准。

问：本三格闭合标准是「`wta-api` 三 `execute` + 七字段直管道 + 状态门先于租约 + 声明式拼装 + 空凭证可发信、过期/解密才关闸 + 禁重定向 `byte[]` + 4xx/5xx 不重试 + 可选 SPI + 分类信封 + 尽力记录，且不把 Factory/Registry 升成第二张 (a)」，还是 OBJ 那句必须先把 L-061 选门、L-062 加密窗、L-063 查询标 covered，或必须把 `createTyped` 画进主路径，或必须等 Redis 8 容器课才给限流盖章？chain 没写 happy (d) 时，标「主路径 third 出站」的证据锚是矩阵已有该行 + OBJ-64 + 课机制链，还是要先改 chain？Lead 若只凭「`ThirdPartyGateway` 四字在函数表里」把 Endpoint 六扇或 invocation 列表标 covered，是否假闭合了 L-061 / L-063？

## Q2 证据

- objective_id: OBJ-64
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Gateway 6–16；Request 11–33；Response 4–15；Category 4–16；Adapter 51–58 / 60–75 / 82–209 / 212–221 / 223–247 / 249–258 / 260–275 / 277–338 / 341–354 / 360–418；Factory 14–27；Typed 6–8；Resilience 27–76；Registry 15–42；SPI 6–30；Startup 11–19；Cache 66–93 / 114–116 / 131–133；Dao 26–30；Security 22–25 / 96–112；Rejected 5–16；Lease 3–6；Attempt 10–23；Recorder 47–115；AdapterTest 120–191；ContextTest 124–211；ResilienceTest 14–27；FactoryTest 35–56；RegistryTest 16–39；RecorderTest 32–89；RedisIT 85–128；AGENTS.md 3；Skill third 14–24；邻格 L-060…L-063 / L-065
- difficulty: high

课的 (b) 出站管道、(d) 主路径、(d) 失败分类哪些能在工作树直接复现，哪些只是口号写偏、测试名单窄、或邻格事实？

**仍能复现（本课 B + 两条 D）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 公开合同三 `execute`；无 url 字段；编码空则构造即炸 | Gateway 8–15；Request 19–32 |
| 实现类七字段；`@Component` 在 `adapter/gateway`，不是 `service/` | Adapter 49–58；Skill 把 gateway 写进 service，以 Java 为准 |
| 无类型重载进 `executeInternal(request, Class)`；`ParameterizedTypeReference` 先拒 null 再 `Object.class` + `getType()` | Adapter 60–78 |
| 快照失败 / `validateSnapshot` 失败 / Registry 抛错 → `CONFIG_UNAVAILABLE`，attempts=0，不发 HTTP | Adapter 87–103；AdapterTest 184–190；ContextTest 161–165 |
| 缓存键 `third:config:{provider}:{endpoint}`，TTL 10 分钟；Redis 读/写失败抛「第三方配置缓存不可用」 | Cache 66–93 / 114–116 / 131–133 |
| 供应商 `status != "0"` → `PROVIDER_DISABLED` 优先；否则门停用 → `ENDPOINT_DISABLED`；均在 `acquire` 之前 | Adapter 104–105；AdapterTest 139–145 |
| 租约：供应商/门 QPS 键、并发键；限额 `<=0` 跳过；QPS 超 `RATE_LIMITED`；信号量拿不到 `REJECTED`；Redisson 抛错 `CONFIG_UNAVAILABLE`「limit service unavailable」；成功 lambda，`finally close` | Resilience 27–50；Adapter 106–111 / 207–209；RedisIT 85–128 |
| `maxAttempts`：非幂等恒 1；幂等 `1 + min(max(retryCount,0), 3)`（retryCount=2 → 3；99 → 4） | Resilience 48–50；ResilienceTest 14–27 |
| 路径模板未知/缺失、query/body 未声明 → `IllegalArgumentException` → `REJECTED` | Adapter 114–116 / 195–197 / 260–275 / 341–354 |
| 头四层：调用方（`validateHeaderName`，禁 `authorization` 等）→ 共享 `putIfAbsent` → 门 override 覆盖 → 凭证头再覆盖（`validateConfiguredHeaderName` 不禁 `authorization`） | Adapter 117–120 / 277–306；Security 22–25 / 96–112 |
| `findByScopes` 取供应商级（endpointId 空）+ 该门级；按 type 先 `putIfAbsent` 再让门级覆盖；过期 `CONFIG_UNAVAILABLE`「credential expired」；解密失败同分类「credential is unavailable」；只合并 JSON 顶层 `headers` | Adapter 314–338；Dao 26–30 |
| 空凭证列表 **不** 抛：AdapterTest 仓恒 `List.of()`，JSON POST 仍 200 / `NONE` | AdapterTest 121–136 / 216–218 |
| SPI 默 `prepare` 原样、`mapResponse` 返回 null；`adapterCode()` 默认等于 `providerCode()`；`supportsEndpoint` 默认 true | SPI 14–29 |
| Registry：无 Bean 且未配 adapterCode → null（纯 HTTP）；无 Bean 但配了 → 炸；Bean 与 adapterCode 不属同一供应商 → 炸；重复 `providerCode` 启动炸 | Registry 15–42；RegistryTest 16–39；ContextTest 184–211；AdapterTest 184–190 |
| 主路径 HTTP：`clientFactory.create`（连接/读超时至少 100ms，`followRedirects(NEVER)`），`retrieve().toEntity(byte[].class)`，2xx 再按 BYTES/TEXT/JSON decode，无 map 则 `convertValue` | Factory 15–22；Adapter 123–186 / 212–221 / 249–258 |
| `createTyped` **只**出现在工厂与 `ThirdHttpClientFactoryTest`；`execute` 零调用 | Factory 25–27；FactoryTest 35–56；Adapter 全文件 |
| 循环：每次先 `recordAttempt(completed=false)`；成功 completed=true 并 break；`RestClientResponseException` 记 `HTTP` 后抛、不 `continue`；`ResourceAccessException` 记 TIMEOUT/TRANSPORT，未到上限才再试 | Adapter 126–166；ContextTest 147–180 |
| 503 即使幂等 `retryCount=3` 仍只打 1 次；302 分类 `HTTP`、不打 `/target`；非幂等 `retryCount=3` 超时只 1 次；幂等 `retryCount=1` 第一次超时第二次 200，attempts=2，attempt 事件 4 条 | ContextTest 148–180；AdapterTest 172–180 |
| SPI `mapResponse` 非 null 则强转返回（可把 2xx 标成 `PROVIDER` 且 `providerMessage` 有文案）；否则 convert | Adapter 176–186；ContextTest 140–144 / 346–360 |
| `isSuccess()` = `NONE` 且 2xx | Response 13–15 |
| 其余 catch：`ThirdRejectedException` 用自带 category；`IllegalArgumentException` → `REJECTED`；其他 `RestClientException` → `TRANSPORT`；`ThirdDecodeException`（私有内部类）→ `DECODE`；剩下 `RuntimeException` → `PROVIDER` | Adapter 187–206 / 387–389 |
| `failure(...)` 调方把 message 传 `null`；分类靠 category；成功失败都 `record`，记录异常忽略 | Adapter 360–375 / 408–417 |
| 逻辑行 `SUCCESS/FAILURE` + 门级/供应商级两行 statistic；尝试事件 `THIRD_HTTP_ATTEMPT_START/FINISH`；脱敏 | Recorder 52–105；RecorderTest 32–89 |
| 业务只依赖 wta-api；本切片无 `/third/**` execute 映射 | AGENTS.md 3；controller 包零 execute |

**邻格仍能复现（不计入本三格闭合）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 管理保存才 `encrypt`；列表 Vo 无秘密 | L-062；本课只 `decrypt` |
| 选门是 `get(providerCode, endpointCode)` 点名，不是搜索 | L-061；本课从快照之后起算 |
| invocation/statistics **查询 HTTP** | L-063；本课只保证写 |
| 厨房工厂不发第三方 HTTP | L-065 |

**课写过、口号或口试标签写偏（不是证伪 B/D）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 误区 2「没配 adapterCode 时允许 null，走纯 HTTP」 | 纯 HTTP 的条件是 **该供应商没有 Bean**。有 Bean 时空白 adapterCode 仍 `find` 到适配器（默认 `supportsEndpoint=true`）。ContextTest 注册了 `qichachaAdapter`，JSON 2xx 仍会进 `mapResponse`（返回 null 再 convert） |
| 矩阵「凭据缺失」 | 空 `findByScopes` 继续发信。失败句是过期/解密 `CONFIG_UNAVAILABLE`。**无** 过期凭证网关单测 |
| 矩阵「失败分类入库」 | `record`/`recordAttempt` 吞异常；分类信封已决定。不是强一致入库 |
| Skill「fixed typed contracts use `@HttpExchange`」 | 主路径永远 `byte[]`。typed 是工厂旁路 + 标记接口 |
| `failure` 的 message | 管道自己的 failure 常传 `null`；有文案的是 SPI `mapResponse` 或 `ThirdRejectedException` 自己（但 Adapter 仍把 message 丢成 null） |
| `ParameterizedTypeReference` null / 空白编码 | 在进 `executeInternal` **之前**抛，不会收成 `REJECTED` 信封 |

问：在三 `execute`、七字段顺序、状态门先于租约、空凭证可发信、过期/解密才 `CONFIG_UNAVAILABLE`、4xx/5xx 不重试、只 `ResourceAccessException` 可能再试、禁重定向、SPI 可空、记录失败不改分类 已经能互证的前提下，三格是否还要把「空白 adapterCode+Bean 仍走 SPI」「failure 文案常 null」「无过期凭证单测」「`createTyped` 旁路」写成开篇半句才算讲完？chain 没写 happy (d) 时，能不能凭矩阵已有行 + OBJ-64 盖章而不是先改 chain？(d) 失败口号里的「凭据缺失 / 入库」是不是精度项，不是缺机制？

## Q3 假设与反例

- objective_id: OBJ-64
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: Adapter 87–209 / 314–338；Factory 15–27；Registry 26–42；SPI 14–29；Resilience 48–50；Security 96–112；ContextTest 147–180；AdapterTest 139–190；课反例 1–3 / 正例 1–3 / 误区 1–6 / 变式 B–C / 图边界
- difficulty: high

课的隐含假设若反转，合格读者会把哪段走廊认错？

1. **「Gateway 是 `Third*Service` 的一个方法 / 有 `/third/execute` HTTP。」** 实现类在 `adapter/gateway`，实现 wta-api。课误区 1。厨房也没有试连通枪（L-065）。
2. **「业务模块可以 `RestClient.create()` 或传完整 URL。」** 课反例 1 / 误区 6。请求对象没有 url；origin 来自快照。
3. **「调用方 header 带 `Authorization` 就能过。」** `validateHeaderName` 列入 CALLER_BLOCKED。凭证贴纸走 `validateConfiguredHeaderName`。课反例 2。
4. **「空白 adapterCode = 纯 HTTP。」** 无 Bean 才 null。有 Bean 仍 prepare/map。课误区 2 半句滑。
5. **「Registry.find 找不到适配器就调不成。」** 未配且无 Bean 允许 null。课误区 2 正方向。
6. **「HTTP 500 / 503 会按 `retryCount` 重试。」** 只有 `ResourceAccessException` 会 continue。ContextTest `/status` 打 1 次。课误区 3。
7. **「非幂等门写了 `retryCount=3` 就会试 4 次。」** `maxAttempts` 非幂等恒 1。ResilienceTest。
8. **「记录器挂了调用就失败。」** `record`/`recordAttempt` catch 忽略。课误区 4。
9. **「`createTyped` / `@HttpExchange` 就是 execute 内部在用的。」** 主路径 `RestClient` + `byte[]`。课误区 5；图边界。
10. **「`ParameterizedTypeReference` 会把 genericType 传进 HTTP 层。」** HTTP 仍 `byte[]`；泛型只事后 convert。课反例 3。
11. **「3xx 会跟随 Location 改 host。」** `NEVER`；302 分类 `HTTP`，`/target` 计数 0。课边界。
12. **「没有凭证行就会 `CONFIG_UNAVAILABLE`。」** 空列表继续发。过期/解密才关闸。矩阵口号「凭据缺失」反转后会发明第三种失败。
13. **「限额 0 表示拒绝一切。」** `positive` 才建闸；0 是关闭该闸。课边界。
14. **「缓存组件不可用就降级打裸 HTTP。」** fail-closed `CONFIG_UNAVAILABLE`。课边界。
15. **「供应商停用仍会看门状态 / 仍会 `acquire`。」** 供应商分支先 return，attempts=0。AdapterTest 停用楼 `requests=0`。
16. **「FORM 能原样传嵌套 JSON。」** 拍扁 `asText()`。课边界。
17. **「`mapResponse` 返回类型可以和 `Class<T>` 不一致。」** 未检查强转。课边界。
18. **「本课覆盖 Endpoint 六扇 / Credential 加密窗 / invocation 列表，所以那三行也可 covered。」** 燃料与仪表是邻课。把假设反转后，Lead 会用管子假闭合 L-060…L-063。
19. **「矩阵要先有 Registry / Factory / `createTyped` 独行才算 (b) 讲完。」** 矩阵无这些行。它们是 (b) 的 explained。
20. **「happy (d) 必须等改 chain 才盖章。」** 矩阵已有该行且只挂 L-064。OBJ-64 就是这根管子。

问：读完后仍说不出的，是不是「Gateway 类名」，而是 **七字段顺序、状态门先于租约、空凭证可发、过期才关闸、4xx/5xx 不重试、禁重定向、SPI 可空、分类信封、记录失败不改结果、typed 不是主路径**？

## Q4 视角

- objective_id: OBJ-64
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-64 vs L-060…L-066 / L-083 / L-085；chain 只 B+fail-third-http；Skill third 14–24 与 Java 冲突以 Java 为准
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`B:ThirdPartyGateway.execute`** 或 **`D:fail-third-http`** 或 **`D:主路径 third 出站`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (b)+两条 (d) vs chain 漏印 happy (d)；(a) 无 execute 行；无 Factory 独行 | **是** 闭合定义。三格可单独 covered。不要等改 chain 才盖 happy (d)。不要发明 (a)/第二 (d) |
| 出站合同 | `wta-api` 三 `execute` + Request/Response/Category | **是** (b)。无 HTTP 映射是负空间，不必发明 (a) |
| 管道作者 | 快照安检 → SPI 查找 → 启停 → 租约 → 拼装/凭证/prepare → RestClient → map/convert → 尽力记录 | **是** (b) 与 (d) 主路径同一条河 |
| 失败分类作者 | TIMEOUT / REJECTED / RATE_LIMITED / CONFIG_UNAVAILABLE / HTTP / TRANSPORT / DECODE / PROVIDER / DISABLED；空凭证不是失败 | **是** (d) fail-third-http。口号「缺失/入库」精度 **是** 半句，不是缺格 |
| 限流邮差 | 四把 Redis 闸；0 跳过；失败不发 HTTP | **是** (b)「限流」。Redis 8 容器 **否** |
| 凭证锁盒 | 出站才 `findByScopes` + `decrypt`；只贴 `headers` | **是** (b) 汇半段。加密入库 **否**（L-062） |
| SPI 专科医生 | 可选；默认 no-op；启动期重复/缺 Bean 可炸上下文 | **是** (b)「SPI」。某个供应商签名算法 **否**（课图边界） |
| HTTP 工厂 | `create` 禁重定向；`createTyped` 旁路 | `create` **是**。(typed) **否** 升格 |
| 记录器 | 逻辑行 + 尝试事件，吞异常 | 写半段 **是**。(d)「入库」别读成强一致。查询窗 **否**（L-063） |
| 选门柜员（L-061） | 编码点名 + 保存期安检 | 快照那一行对照 **是**。六扇 **否** |
| 加密柜员（L-062） | encrypt；列表不回显 | decrypt 对照 **是**。管理三扇 **否** |
| 监控室（L-063） | invocation/statistics HTTP | 读 **否** |
| 厨房/页（L-065/L-066） | 配置 CRUD 枪 | **否** |
| Redis 8 / MySQL 8.4 / dao-mapper | 限流键、config 缓存、invocation upsert | 本窗机制举例 **是**。容器/组件课 **否**（L-084 也不是这些键；L-085/L-083） |
| 测试夹具行 | `*Test.java` 矩阵已 deferred | 证人 **是**。不要把夹具行标 covered |
| Skill 作者 | gateway 写进 service；typed `@HttpExchange` | 冲突句 **是** 课已拆。不要用 Skill 盖过 Java |

问：Lead 若凭「`execute` 三字在函数表里」把 (b) 标 covered，是否还要求读者能指着 **七字段顺序、状态门、空凭证可发、过期关闸、不重试 4xx/5xx、禁重定向、SPI 可空、尽力记录**？这些是不是已经在本格机制链/反例里，而不需要把管理六扇、查询窗或 Redis 容器算进来？两条 (d) 是不是同一条河的两岸，可以凭本课盖章而不是等 L-061/L-063？happy (d) 是不是明确「矩阵已有、只挂本课、不必先改 chain」？

## Q5 元问题

- objective_id: OBJ-64
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (b) execute、(d) 主路径 third 出站、(d) 失败路径 third HTTP 只挂 L-064；chain 只印 B+fail-third-http
- difficulty: low

建议：`defer`

原因：

- **(b) 与两条 (d) 与矩阵 / OBJ-64 / 子课 L-005 1:1；chain 覆盖列少印了 happy (d)，但不缺机制。** 子课合同章节齐全（宏观地图、直觉、定义表、机制链 1–14、带 caption / 文字等价物 / 图边界的直管道图、正反例、变式、误区、暂停走查、来源表 S-3RD-07/S-3RD-08/S-3RD-09/S-3RD-06）。**主叙事站得住且与源码对齐**：三 `execute`、七字段、快照安检、可选 SPI、启停门、租约 finally、声明式拼装、凭证只在网关解密、`RestClient`+`byte[]`、禁重定向、非幂等不重试、4xx/5xx 不重试、分类信封、尽力记录。不必为「Gateway 类名」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例/边界或邻格，不挡住把 **(b) 与两条 (d)** 标 covered：（1）纯 HTTP = 无 Bean，不是空白 adapterCode；（2）空凭证列表不是失败，过期/解密才 `CONFIG_UNAVAILABLE`，且无过期凭证网关单测；（3）`failure` 文案常 null；（4）矩阵「入库」= 尽力 upsert；（5）`createTyped` 旁路；（6）进管子前的构造期异常不收信封；（7）登记表未逐行点名 `wta-third`；（8）工作树除测试外无业务调用方。不要把 Provider/Endpoint/Credential 管理窗、invocation 查询、厨房工厂、Redis 容器塞进本三格。
- **不要发明 Registry/Factory/`createTyped` (a) 行，也不要等 GP-L-061/L-063 才给出站盖章。** 矩阵两条 third 流只挂 L-064。L-061 只保证选门键；L-062 只保证加密入库；L-063 只保证读回放。本课保证管子。
- **happy (d) 不必改 chain 才盖章。** 证据锚是矩阵已有该行 + OBJ-64 + 课机制链 + 本 probe。miner 不改 chain。失败 (d) 与 (b) 是同一条河的另一岸。
- 不建议 `mine-more`：b01 未证明三格还有真实矩阵空洞；再五问只会把 L-061 六扇、L-062 加密窗、L-063 查询或 Redis 容器再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；可选半句（空白 adapterCode+Bean、空凭证≠失败、文案常 null、typed 旁路）不是第二容器。不建议 `split-lesson`：未满 10 问；出站主路径与失败分类是 chain 原设计的同一 OBJ-64，同一 `wta-third` 容器，不是两个 C4。`split_depth: 0`，本批禁止填 split 表。不建议发明 execute (a) 行：无管理 HTTP 是 (b) 的负空间。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| B:ThirdPartyGateway.execute | 三 `execute`、七字段直管道、限流租约、声明式拼装、可选 SPI、`RestClient`+`byte[]`、尽力 `record`——均与源码对齐。残留精度：空白 adapterCode+Bean 仍 SPI；`createTyped` 旁路；进管子前异常不收信封；无业务调用方。 | 可选半句：口试「纯 HTTP」要问有没有 Bean，不是只看 adapterCode 空。Lead 可将本行标 `covered`（证据锚本 probe 与 L-005 来源表 S-3RD-07/S-3RD-08/S-3RD-09）。 | defer（本行可 covered；精度项不挡盖章） |
| D:主路径 third 出站 | 矩阵口号 `execute → 限流/凭证/HTTP/SPI → 记录`。磁盘：限额 0 跳过、凭证可空、SPI 可空、2xx + `NONE` + `isSuccess`、attempt START/FINISH、逻辑 SUCCESS。chain 未列本行。 | 不要等改 chain。限流/凭证/SPI 三步都是可选闸，不是「每步必须发火」。Lead 可将本行标 `covered`。 | defer（本行可 covered；证据在本课） |
| D:失败路径 third HTTP / D:fail-third-http | 矩阵口号「超时/拒绝/凭据缺失 → 失败分类入库」。磁盘：TIMEOUT/TRANSPORT/HTTP/REJECTED/RATE_LIMITED/CONFIG_UNAVAILABLE/DISABLED/DECODE/PROVIDER 信封；空凭证不是失败；过期/解密才 CONFIG_UNAVAILABLE；记录尽力。残留：无过期凭证网关单测。 | 可选半句：口试「凭据缺失」改成「过期或解密失败」；「入库」= 尽力。不要另建「凭证缺失」行。Lead 可将本行标 `covered`。 | defer（本行可 covered） |
| A:ThirdProviderController.* / A:ThirdProviderUseCase | 登楼、`evict(code, null)`。 | 对照「燃料不是管子」。L-060。 | defer（L-060） |
| A:ThirdEndpointController.* / A:ThirdEndpointUseCase | 选门键、保存期安检。 | 快照对照一句即可。L-061。 | defer（L-061） |
| A:ThirdCredentialController.* / A:ThirdCredentialUseCase / B:ThirdCredential 保存 | 加密入库、列表不回显。 | decrypt 对照一句。L-062。 | defer（L-062） |
| A:ThirdObservabilityController.invocation/list, statistics/list | 读回放。 | 本课只保证写。L-063。 | defer（L-063） |
| A:createThirdService / A:createThirdWebDomain | 厨房与页。 | 保持。L-065/L-066。 | defer（L-065/L-066） |
| Registry / Factory / `createTyped` / StartupValidator 升格 (a) | 课已写进 (b) 解释。矩阵无此符号。 | 不要发明第二法。typed 保持旁路。 | defer（未入库） |
| (a) ThirdPartyGateway.execute | 矩阵无此行。无管理 HTTP。 | 不要发明。负空间已在 (b)。 | defer（未入库） |
| C:Redis 8 / MySQL 8.4 / mysql-base / dao-mapper-ownership | 限流键、`third:config:`、invocation upsert 是本窗机制。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085 / L-083） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；(b) 与两条 (d) 无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出目的（业务只走出站大门）、主路径（三 `execute` → 快照安检 → 可选 SPI → 启停 → 租约 → 拼装/凭证 → RestClient `byte[]` → map/convert → 尽力记录）与失败路径（配置/停用/限流/拒绝/超时/传输/HTTP/解码/供应商信封，空凭证不是失败，记录失败不改分类）；happy (d) 缺的是 chain 漏印，不是讲义容器；把可选半句写进原课不会超过 40 分钟；Provider、Endpoint、Credential、Observability、厨房已有邻课。满 10 问前不拆。出站主路径与失败分类是 chain 原设计的同一 OBJ-64，同一 `wta-third` 容器，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-060 Provider、L-061 Endpoint、L-062 Credential、L-063 查询、L-065/L-066 前端、未入库 Registry/Factory/`createTyped`/(a) execute 行、(c) 容器与 dao-mapper 组件显式建议 `defer`；对 `B:ThirdPartyGateway.execute` 与 `D:主路径 third 出站` 与 `D:失败路径 third HTTP`（`D:fail-third-http`）建议 Lead 标 `covered`（证据锚本 probe 与 L-005 来源表 S-3RD-07/S-3RD-08/S-3RD-09/S-3RD-06）；对 happy (d) 建议凭矩阵已有行盖章，不要等改 chain；不要发明第二 (d)、不要用管子假闭合管理六扇或 invocation 列表、不要因限流键盖 Redis 8 容器。本课不主张发明 execute (a)、把 `createTyped` 画进主路径、或等 GP-L-063 才给记录写半段盖章。
