# T-44 AC-044 independent contract review — fixed `fc50c1e1227d42a46f8e25b3e19949baeccb89e0`

**Earlier checkpoint verdict (before the seven-consumer run): no blocking source or evidence inconsistency found in the implemented T-44 paths; final acceptance remained pending seven existing OSS integration consumers under real MySQL/Redis/MinIO.** This is a read-only review. I ran no Maven, service, browser, or build command. Do not count the default suite's environmental skips as those consumers passing. The final verdict is in the addendum below.

## Source and contract

`git diff 958aad6174c880fbf8c7afbfb8f657d37c968979...fc50c1e` changes only four generated OpenAPI files. `/tmp/wta-t44/a2-backend-evidence-reuse.json` records identical backend, release-artifacts, `.agents`, and scripts trees. The source at `fc50c1e` keeps local object `ACTIVE`/service checks and actual access-policy validation in `OssLifecycleManager.requireDownloadable/accessPolicy`, policy/permission/Client checks in `OssUploadService`, and actual object service binding. The upload, download, and migration routes no longer call `readinessRegistry.requireServing`; diagnostic freshness is not a business prerequisite. The retained registry reference is not used as such a gate.

`SystemApplicationRunner` now invokes only configuration-cache initialization. `SysOssConfigServiceImpl.init` first reads DB, clears the dedicated OSS cache and old Redis default pointer, then populates locally valid configurations and sets a unique valid PRIVATE default. A DB read error is not converted to a successful empty configuration. Invalid optional `OssStorageReadinessProperties` values are parsed at diagnosis time, so lexical Duration errors do not abort the core. `OssStorageReadinessService.diagnoseOne` is a single-ID admin observation with a revision check; `SysOssConfigController` uses POST, `system:ossConfig:list`, positive ID validation, and `@Log` with request/response persistence disabled. The returned VO has only status/reason/checkedAt. The configured network timeout is 100 ms–3 s per step, with at most five serial network steps; it is not a whole-request 3 s claim. Config changes invalidate diagnostic snapshots.

`application.yml` makes readiness `readinessState,db,redis`, core `db,redis`, and optional `ossdiagnostics` separate. `NamewtaApplication` retains global scheduling for Notify polling. These changes implement the AC-044 diagnostic decoupling without claiming T-45's policy-UNKNOWN/403 interpretation change. Access to a wrong policy, non-ACTIVE object, missing service/config, or unauthorized user remains fail-closed.

## Evidence checked

- `logging-green01-source-and-counts.json`: clean `958aad6` before/after, 40 fresh suites, 191 tests, zero failure/error/skip. Earlier A1 MinIO log leakage and red regressions remain historical failures; A2's `run 570b67d1325ff08b` reports 18 canaries at zero hits, including dynamic upload tokens and S3 signature material; source and owned resources cleaned. The SQL logger now emits fixed command/mapper/timing/error-class metadata rather than SQL text, bound values, or raw exception messages; HTTP log sanitization hides signed URLs and upload-token route/JSON copies while leaving ordinary public URLs and business codes.
- `a2-backend-default-counts.json`: 262 fresh classes, 1121 tests = 905 executed and 216 environmental skips, zero failure/error. `a2-full-package-proof.json` records a clean full JAR build at backend `958aad6`, SHA-256 `5055eaf2e8e8358057918188050cde497eef0a3767d8d2e372d3f64e2ab633e2`.
- `a2-real-summary.json`: eight owned scenarios have command exit 0, accepted/acceptance true and cleanup errors empty. MinIO verifies startup remote-call count zero; PRIVATE A and B actual upload/HEAD/download, PUBLIC read without a canary, least-privilege bucket-policy/ObjectACL denial, invalid ID/ordinary-user denial, policy/status/config failure, diagnostic target isolation, STALE observation while download still works, and historical A still routed to A after default changes to B. Six core degraded-config/startup cases and Redis-PUBLISH-denied Notify fallback also pass on the same backend/JAR, with no hidden external SMS/MAIL assertion. These are synthetic owned environments, not production operations.
- Supplementary v8 run `/tmp/wta-t44/runs/1cde81b827e2b971/result.json`: accepted true, clean source, cleanup empty, 18 canary hits zero. It uses default `message.enabled=true`/SSE and captured 438 paths; `schema_capture` confirms the diagnosis path and `/resource/message` plus `/resource/message/close`. The earlier v7 436-path capture had messaging disabled and is not substituted for the default HTTP contract. `a2-openapi-generation-proof.json` binds the formal generated revision to the v8 raw SHA-256 `8c21f523b8f46fc283c70dc7b8d0c1232e16f36ae59c054e674cc02f1af6eade`; provenance reports 438 paths, 449 schemas, 77 tags. OpenAPI check/types exit 0.
- At `fc50c1e`, frontend 760 tests pass, lint/typecheck/architecture/OpenAPI checks exit 0, and three production apps yield 329 hashed artifacts. Core package/bundle and skill/fullstack/notify/fm/handbook checks exit 0. `/tmp/wta-t44/a2-write-scope-and-ancestry.json` records base ancestry, all 45 product paths inside the 26 pre-authorized roots, and matching Skill hashes. These frontend/core facts are at the generated-contract head; backend real runs reuse the byte-identical backend inputs, not a falsely relabeled `fc50c1e` JAR build.

## Pending required verification

Seven existing OSS integration consumers each show one environmental skip in the fresh default suite and **have not yet been shown running with real dependencies**: `OssAccessUrlMinioIntegrationTest`, `MinioOssClientIntegrationTest`, `OssStorageMigrationIntegrationTest`, `OssStorageReadinessMinioIntegrationTest`, `BrowserUploadLifecycleIntegrationTest`, `OssUploadStorageRoutingMinioIntegrationTest`, and `RedisOssUploadTicketStoreIntegrationTest`. Lead/ops are preparing a separate owned run. Require fresh per-class XML with tests positive, zero fail/error/skip, exact source/JAR binding and owned cleanup before the final T-44 acceptance verdict. No current code blocker is inferred solely from their pending status.

## Final addendum — seven-consumer evidence received

The preceding pending section records the earlier review checkpoint; it is now resolved. I independently read `/tmp/wta-t44/oss-seven-runs/023e9db8eab34cec/result.json` and parsed all seven retained sanitized XML files. The result has `acceptance=true`, Maven exit 0, and seven `fresh=true` suites, each with 1 test, 0 failures, 0 errors, 0 skips, and the private test-property marker true. Each XML's parsed counts match the result record and its retained SHA-256. The `BrowserUploadLifecycleIntegrationTest` XML's safe system output contains indices `[1/10]` through `[10/10]`, `10 passed`, and its owned MinIO byte-verification completion marker; the runner reports browser 10 passed/0 skipped.

The run's source before/after is the same clean `fc50c1e` tree `c39bb13afcbf07459b2fb07c1c2179f3198fc807`; all three frontend dist manifest hashes also match before/after. Its Maven process group and owned Node/Chrome/Java members have no survivors; three owned containers, two anonymous volumes, and three loopback ports are gone; cleanup errors are empty. Retained XML and Maven log are sanitized with original and sanitized hashes/redaction counts recorded; I did not read private properties, raw logs, or credential values.

**Final AC-044 review verdict: PASS, no remaining blocker in the authorized T-44 contract and evidence.** This conclusion is limited to the fixed source, generated contract, and owned isolated tests above. It does not assert a production deployment, nor does it preempt T-45 policy-UNKNOWN/403 semantics. The earlier default-suite skip count and A1/logging failures remain recorded as historical facts, not retroactively relabeled as passes.
