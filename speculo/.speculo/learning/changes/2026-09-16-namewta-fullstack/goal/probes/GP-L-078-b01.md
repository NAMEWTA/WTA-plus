---
artifact: learning-goal-probe
probe_id: GP-L-078-b01
lesson_id: L-078
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-078 / b01

审问对象是已写成的 Lesson `lessons/L-078-job-executors.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` **九格工人类**：`A:AlipayBillTask`、`A:WechatBillTask`、`A:SummaryBillTask`、`A:TestAnnoJobExecutor`、`A:TestBroadcastJob`、`A:TestClassJobExecutor`、`A:TestMapJobAnnotation`、`A:TestMapReduceAnnotation1`、`A:TestStaticShardingJob`。OBJ-78 原文：能指出 `wta-job` 的 SnailJob 执行器类是什么入口、**不是** HTTP Controller。档是**解释+指类**。几何要对齐三份清单，不要把 chain 脊柱误读成矩阵第二行：

- chain 覆盖列只印 **脊柱四类** `AlipayBillTask / WechatBillTask / SummaryBillTask / TestAnnoJobExecutor`。
- 课 `claimed_cells` 与矩阵 (a) **同一行九个名字**：上列四类再加 `TestBroadcastJob, TestClassJobExecutor, TestMapJobAnnotation, TestMapReduceAnnotation1, TestStaticShardingJob`，模块 `wta-job`，仍 uncovered，备注「Job 入口不是 HTTP」。
- 矩阵 (a) **没有** `JobController` / `createJobService` / `web-domain-job` / `BillDTO` / `SnailJobConfig` 独行。厨房与九工厂行都不含 job。
- 矩阵 (b) **没有** 账单 DAG / 广播抛异常 / MapReduce / `wfContext` 独行。
- 矩阵 (c) `wta-modules` 业务房间 / `layered-classic` 已 covered（L-002 / L-003）；登记表 classic 行就是 `wta-modules/wta-job`。`bundle-full-job` 仍 uncovered（L-079）。`wta-common-*` 叶子里的 job 已 `deferred(not-on-happy-path-hub)`。
- 矩阵 (d) **没有**「SnailJob DAG / 点名执行器」走廊；已 uncovered 的存储点仍是「业务表只进 10/50 基座」（L-085）。`20-cde-job.sql` 是上游快照，不是那条基座。

本批按派单审问 **九个执行器入口**；口试按磁盘：`snailjob/` 九个 `@Component`，**零** `@RestController` / `@RequestMapping`，八个贴纸 `@JobExecutor(name=...)` + 一个继承 `AbstractJobExecutor`，花名 ≠ 类名 ≠ 种子 `job_name`，账单三件套只改 `wfContext` 字符串且金额写死，广播失败扔异常，静态分片吃逗号并 `Thread.sleep`，Map 只切 1…200，MapReduce 再把各片当 String 加总。不要把 `C:bundle-full-job` 标进本格，不要把 iframe `monitor/snailjob/index` 说成工人的脸，不要把 `SnailAiController` 的那一扇 HTTP 抄进这间无窗房，也不要把九个名字拆成九行才给切片盖章。

抽查锚点（2026-09-17）：`backend/wta-modules/wta-job/src/main/java/org/namewta/job/snailjob/{AlipayBillTask,WechatBillTask,SummaryBillTask,TestAnnoJobExecutor,TestBroadcastJob,TestClassJobExecutor,TestMapJobAnnotation,TestMapReduceAnnotation1,TestStaticShardingJob}.java`；`entity/BillDTO.java`；`package-info.java`；`wta-job/pom.xml`；模块无 `src/main/resources`、无 `src/test`、无 UseCase/Mapper；`wta-common-job` `SnailJobConfig.java` 与 `AutoConfiguration.imports`；`application-{local,dev,prod}.yml` 的 `snail-job` 段；`20-cde-job.sql`；`NamewtaApplication.java`；`SnailJobServerApplication.java`；登记表 `03-backend-module-modes.md` 第 23 行；模块地图 `wta-job` 行；`wta-admin/pom.xml` `bundle-full` / `bundle-core`；父 POM `snailjob.version`；`StreamUtils.groupByKey`；`DateUtils` 继承 Hutool `DateUtil`；对照 `domains/system/src/monitor/index.ts` `externalIntent('snail-job')`、`views/monitor/external/index.vue`、`adminManifestRegistry.ts`；frontend `packages/domains` 与 `web-domains` **无** job 包。

## Q1 澄清

- objective_id: OBJ-78
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-78；chain L-078 覆盖列脊柱四类；课 `claimed_cells` 九类；矩阵 (a) 九名同行仍 uncovered、无 Controller/厨房/Vue 行；(b) 无本课符号；(c) 房间与登记表已 covered、`bundle-full-job` 仍 uncovered；(d) 无 job DAG 流；课开篇「chain 前四个当脊柱 / 矩阵九个同一行 / 九个都要能指类」+ 负空间 L-024 / L-079 / L-080 / L-003
- difficulty: medium

课把后车间收成「九个等人拍肩膀的工人，墙上没有 HTTP 窗」。计划里至少有五套可数对象，派单点名 **执行器类**，但清单长度不一致：

1. **OBJ-78 原文。** `course.md`：能指出 `wta-job` 的 SnailJob 执行器类是什么入口、不是 HTTP Controller。一句是 **指类、指入口**，不是 `/job/list` 方法表，不是 pom profile，也不是 iframe 四键。课把 OBJ 扩成九个工人的挂号方式、账单袋子、广播/分片/Map 形状，那是走查，不是第二张矩阵行。
2. **chain 覆盖列 vs 课 `claimed_cells`。** chain 只写脊柱四类。课与矩阵把其余五类印在**同一行**。磁盘上这九个文件都在 `org.namewta.job.snailjob`，外加一张 `BillDTO`。chain **不**写 `SnailJobConfig`，**不**写 `C:bundle-full-job`，**不**写 `createMonitorService.externalIntent`。
3. **矩阵 (a) 一行九名。** Lead 若按该行标 covered **会**同时盖脊柱四类和后五类——本课九个都讲了，这是 1:1，不是假闭合。Lead 若因 chain 只印四个就把该行留空洞，是假判「未讲完」。矩阵 **没有** 把九个名字拆成九行，也没有 `JobController` / `createJobService` 行。
4. **课自己的操作定义。** 入口是类，不是路径。八个类同时 `@Component` + `@JobExecutor(name=...)`；`TestClassJobExecutor` 只 `@Component`，继承 `AbstractJobExecutor`，方法是 `doJobExecute`。主任点的是注解 `name`（种子里是 `testJobExecutor`），不是 Java 文件名，也不是 `job_name='demo-job'`。扫到 Bean ≠ 被点名。
5. **三道电闸是对照，不是第四格。** 类在不在 jar → L-079；客户端醒不醒 → `snail-job.enabled`（三份 yml 默认 false）；主任在不在 → extend 进程。本课只认工人。`SnailJobConfig` 上的 `@EnableScheduling` 不是这九个工人被点名的原因。
6. **邻格。** L-002 变式 D：工人在 admin，主任在 extend。L-003：classic 列不是一种家具；job 无 Controller/Mapper/resources，脚本缺目录退出码 1 不改登记。L-024：iframe 看主任楼。L-079：`C:bundle-full-job`。L-080：ai **有** `SnailAiController`。L-067…L-073：Warm-Flow 的 `flow_*` 不是 SnailJob `sj_workflow*`。

问：本九格闭合标准是「矩阵 (a) 九名同行按磁盘指类：无 HTTP 窗、八贴纸+一继承、花名/类名/任务名三套嘴、账单 DAG 写死金额且缺键当 0、广播扔异常、静态分片逗号+sleep、Map 只切、MapReduce 再收 String，且不升 `BillDTO`/`SnailJobConfig` 矩阵行、不提前盖 L-079、不把 iframe 当工人」，还是 OBJ 那句必须先把同行拆成脊柱四行+后五行、或必须等 GP-L-079 pom 对照才给工人盖章？Lead 若凭「chain 只写了四个名字」把九名同行留空洞，是否假判？Lead 若用本课把 `C:bundle-full-job` 或 `createMonitorService` 标 covered，是否假闭合了 L-079 / L-024？

## Q2 证据

- objective_id: OBJ-78
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: AlipayBillTask 21–43；WechatBillTask 21–44；SummaryBillTask 20–50；BillDTO 13–18；TestAnnoJobExecutor 16–30；TestClassJobExecutor 12–24；TestBroadcastJob 18–44；TestStaticShardingJob 16–42；TestMapJobAnnotation 24–64；TestMapReduceAnnotation1 26–78；wta-job pom 12–31；package-info 仅包声明；模块 grep 空 RestController/RequestMapping/UseCase/Mapper；无 resources、无 test；SnailJobConfig 21–40；AutoConfiguration.imports 1；application-local 39–47；application-dev 15–27；application-prod 18–30；20-cde-job.sql 18–19 / 41–42 / 279–320 / 525–539；NamewtaApplication 25–26；SnailJobServerApplication 12–21；modes.md 23；01-module-map 55；wta-admin pom 149–204；wta-modules pom 20；backend pom 46 / 305–322；StreamUtils 207–213；DateUtils 21；monitor index.ts 21 / 39–43 / 146–148；external/index.vue 15–28；adminManifestRegistry 308–311；common-job pom 12–47；frontend 无 domains/job、无 web-domains/job
- difficulty: high

课的九个工人哪些能在工作树直接复现，哪些只是文案比分支更宽、或口试把邻课插座/玻璃窗说成本格第十个工人？

**仍能复现（脊柱四类）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| `AlipayBillTask` `@Component` + `@JobExecutor(name = "alipayBillTask")`；`jobExecute` 声明 `throws InterruptedException`，方法体无 sleep | AlipayBillTask 21–32 / 全文无 sleep |
| 清算日 `jobArgs.getWfContext().get("settlementDate")`；`"sysdate"` → `DateUtils.now()` | AlipayBillTask 34–37；DateUtils 21 继承 `DateUtil` |
| 写死 `BillDTO(23456789L, "alipay", settlementDate, 2345.67)`；`appendContext("alipay", JsonUtils.toJsonString)`；REMOTE 打整袋；`success(billDTO)` | AlipayBillTask 38–42；BillDTO 13–18 |
| JSON 走项目 `JsonUtils`，不是厂商 `JsonUtil` | AlipayBillTask 9 vs TestAnnoJobExecutor 5 |
| `WechatBillTask` 花名 `wechatBillTask`；同一套日期规则；写死 `123456789L` / `"wechat"` / `"1234.56"`；上下文键 `wechat` | WechatBillTask 21–43 |
| `SummaryBillTask` 花名 `summaryBillTask`；`(String) jobArgs.getWfContext("wechat")` / `getWfContext("alipay")` ——按键取值，不是先拿整袋再 `.get` | SummaryBillTask 20–45 |
| 空白键金额 0，不抛；`wechatAmount.add(alipayAmount)`；REMOTE「总金额」；`success(totalAmount)`；两边都在时 `2345.67 + 1234.56 = 3580.23` | SummaryBillTask 33–49 |
| `TestAnnoJobExecutor` 花名 **`testJobExecutor`** ≠ 类名；`jobExecute` 用厂商 `JsonUtil` 打 LOCAL+REMOTE；`success("测试成功")`；不读上下文、不睡 | TestAnnoJobExecutor 16–30 |
| 种子唯一 `sj_job`：`namespace_id='dev'`，`biz_id`/`job_name='demo-job'`，`group_name='wta_group'`，`executor_info='testJobExecutor'`，`task_type=1`（注释：1 集群 / 2 广播 / 3 切片），`trigger_type=2`，`trigger_interval='60'` | 20-cde-job.sql 289–295 / 320 |
| 类名 / 花名 / 任务名三套嘴 | TestAnnoJobExecutor 17 vs SQL 320 |

**仍能复现（后五类，与脊柱同行）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| `TestClassJobExecutor` **无** `@JobExecutor`；`extends AbstractJobExecutor`；覆盖 `doJobExecute`；固定 `success("TestJobExecutor测试成功")` | TestClassJobExecutor 12–24 |
| `TestBroadcastJob` 花名 `testBroadcastJob`；`@Value("${snail-job.port}")`；`RandomUtil.randomInt(100)`；`< 50` 抛 `RuntimeException`；否则 `success`；slf4j + REMOTE | TestBroadcastJob 18–43 |
| 广播注释写「相加的两个数」，方法体不读 `jobArgs` 做加法；失败信封是**扔异常**，不是 `ExecuteResult.failure` | TestBroadcastJob 35–43 |
| `TestStaticShardingJob` 花名 `testStaticShardingJob`；`Convert.toStr(jobArgs.getJobParams()).split(",")` → `fromId,toId`；LOCAL 开始；REMOTE「加密」；`Thread.sleep(3000)`；中断 → `failure("任务执行失败")`；正常 → `success("执行分片任务完成")`；无 Mapper | TestStaticShardingJob 16–42 |
| `TestMapJobAnnotation` 花名 `testMapJobAnnotation`；无 `taskName` 的 `@MapExecutor` 方法名 **`doJobMapExecute`**；1…200 按 `(i-1)/50` 切四片；`doMap(..., "doCalc")`；REMOTE 打 `server.port` 不是 `snail-job.port` | TestMapJobAnnotation 24–47 |
| `@MapExecutor(taskName = "doCalc")` 把 `getMapResult()` 当 `List<Integer>` 求和；`ThreadUtil.sleep(3, SECONDS)`；**没有** `@ReduceExecutor` | TestMapJobAnnotation 55–64；类文件无 Reduce |
| `TestMapReduceAnnotation1` 花名 `testMapReduceAnnotation1`；根方法名 **`rootMapExecute`**；`doCalc` 几乎同一份；`@ReduceExecutor reduceExecute`：`Integer.parseInt((String) i)` 再 sum | TestMapReduceAnnotation1 26–78 |
| 分组底层 `Collectors.groupingBy` + `LinkedHashMap` | StreamUtils 207–213 |

**仍能复现（房间形状 / 负空间，服务本格指类）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| Java 11 个文件：空 `package-info`、`BillDTO`、九个工人；`src/main` 只有 `java/` | find 11；package-info 仅 `package org.namewta.job;` |
| 全模块无 `@RestController` / `@Controller` / `@RequestMapping` / `@GetMapping` / `@PostMapping`；无 UseCase / Mapper / ServiceImpl | wta-job grep 空 |
| POM 恰好 `wta-common-json` + `wta-common-job`；无 web / mybatis / satoken / `wta-api` | wta-job pom 18–31 |
| `DateUtils` / `StreamUtils` / `StringUtils` 经 `wta-common-job` → `wta-common-core` 传递，不是第三行直接依赖 | common-job pom 44–47 |
| `NamewtaApplication` 在 `org.namewta`，默认扫到 `org.namewta.job.snailjob` | NamewtaApplication 1 / 25–26 |
| 客户端电闸：`@ConditionalOnProperty(prefix="snail-job", name="enabled", havingValue="true")` + `@EnableSnailJob` + `@EnableScheduling`；启动事件挂 `SnailLogbackAppender` | SnailJobConfig 21–40；imports 登记该类 |
| local/dev/prod 都是 `enabled: false`，`group: wta_group`，`server.port: 17888`，`port: 2${server.port}`；local `namespace: local`；dev/prod `namespace: ${spring.profiles.active}` | application-local 39–47；dev 15–27；prod 18–30 |
| `sj_namespace` 只有 `dev` / `prod`；`sj_job_executor` 有 DDL、**无** INSERT | SQL 18–19 / 525–539 |
| 主任另一进程：本仓包装类 `org.namewta.snailjob.SnailJobServerApplication` | SnailJobServerApplication 12–21 |
| iframe 键 `'snail-job'` → `VITE_APP_SNAILJOB_ADMIN`；权限 `monitor:snailjob:list`；组件 `monitor/snailjob/index` | external/index.vue 15–28；monitor 21 / 41 / 146–148；registry 310 |
| frontend **无** `domains/job`、**无** `web-domains/job`、**无** `createJobService` | packages 目录清单；全仓 grep 空 |
| classic 登记「既有任务业务能力；新增独立业务另登 layered」；模块地图「业务任务执行器 / package surface / none」 | modes.md 23；01-module-map 55 |
| `bundle-full` 才插 `wta-job`；`bundle-core` 只有两份档案 | wta-admin pom 149–204 |
| 父 POM `snailjob.version=2.0.2` | backend pom 46 / 305–322 |
| 无 `AlipayBillTaskTest` 等模块测试 | 无 `src/test` |

**课写过、文案比分支更宽或口试数字写偏（不是证伪本九格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 广播成功「随机数大于 50」 | 条件是 `< 50` 才抛；`randomInt == 50` 走 success，文案仍写「大于 50」 |
| Map「睡 3 秒」 | Map 两份是 Hutool `ThreadUtil.sleep`；静态分片才是 `Thread.sleep(3000)` 且能被中断收成 `failure` |
| 「启动类 `SnailJobServerApplication`」 | 包装类 main **run** 的是厂商 `com.aizuda.snailjob.server.SnailJobServerApplication` |
| 课 `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |
| 微信注释「否则取管理页面设置的值」 | 本仓没有 job 管理页；值来自主任下发的 `wfContext` |
| 「无模块测试」 | 真。证据走生产源码 + SQL + yml，不要假装有夹具 |

问：在九个类/花名/入口方法、零 HTTP 注解、账单袋子与写死金额、广播扔异常 vs 分片 `failure`、Map 根方法名不同、Reduce 按 String parseInt、种子只点 `testJobExecutor`、yml 电闸默认关 已经能互证的前提下，(a) 是否还要把「==50 文案」「ThreadUtil vs Thread.sleep」「包装类委托厂商 main」写成开篇半句，才算本九格讲完？还是这些已经落在课的表/反例里（广播文案的 50 边界除外），Lead 可以把 **九名同行** 视为可 covered，并保持 `C:bundle-full-job` / iframe / ai 窗 uncovered？

## Q3 假设与反例

- objective_id: OBJ-78
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无 JobController；无九个都贴 @JobExecutor；无类名=花名=job_name；无账单调支付 SDK；无 Summary 缺键失败；无广播 ExecuteResult.failure / R.fail；无静态分片真加密；无 Map 根方法叫 jobExecute；无 iframe 直调 AlipayBillTask；无 enabled:false 时类从磁盘消失；无本课覆盖 bundle-full；无 sj_job_executor 九行 INSERT；无 createMonitorService 算 OBJ-78；无 OBJ-78 HTTP 方法表
- difficulty: high

课的隐含假设若反转，合格读者会把哪个工人或哪道闸认错？

1. **「`wta-job` 有一套 `/job` CRUD。」** 没有 Controller。课反例 1 / 误区 1 / 15 覆盖。
2. **「执行器名就是类名。」** `TestAnnoJobExecutor` ≠ `testJobExecutor` ≠ `demo-job`。课反例 2 / 误区 3 覆盖。
3. **「九个都贴了 `@JobExecutor`。」** 继承那份没有。课反例 3 覆盖。
4. **「账单工人会调支付宝 / 微信 SDK。」** 构造函数写死 `BigDecimal`。课反例 4 / 误区 5 覆盖。
5. **「这是 Warm-Flow 的第四种节点。」** 类注释 DAG 属于 SnailJob；流程楼是 L-067…L-073。课反例 5 覆盖。
6. **「Map 和静态分片是同一种切法。」** 静态分片吃 `jobParams` 逗号；Map 自己生成 1…200。课反例 6 覆盖。
7. **「广播注释说加法，所以会把两个参数加起来。」** 磁盘只掷骰子。课反例 7 覆盖。
8. **「失败都走 `ExecuteResult.failure`。」** 广播走抛异常；账单缺键走 0；静态分片中断才 `failure`。课反例 8 / 误区 7 覆盖。
9. **「iframe 任务调度页就是这些工人的前端。」** 那是主任楼的玻璃，L-024。课反例 9 / 误区 10 覆盖。
10. **「本课覆盖 bundle-full。」** OBJ-79。课反例 10 / 误区 12 覆盖。
11. **「没有 Mapper 就不是 classic。」** 登记表有这一行。课反例 11 / 误区 2 覆盖。
12. **「给这间房加 UseCase/DAO 才算现代化。」** 新的独立业务另行登记 layered。课反例 12 覆盖。
13. **「`SnailJobLog.REMOTE` 就是 logback 控制台。」** REMOTE 要客户端启动时挂上的 appender。课反例 13 覆盖。
14. **「`sj_job_executor` 种子已经登记九个花名。」** 表有，INSERT 没有。课反例 14 / 误区 4 覆盖。
15. **「OBJ-78 包含 `createMonitorService.externalIntent`。」** L-024。课反例 15 覆盖。
16. **「`enabled: false` 时九个类不会被编译 / 不会进 Spring。」** 编译是 Maven；电闸贴在 `SnailJobConfig` 上，不是贴在这九个类上。`@Component` 仍可能被扫到。课正例 10 / 误区 11 / 类比失效边界覆盖。
17. **「`@EnableScheduling` 就是这九个工人被点名的原因。」** 它在 `SnailJobConfig` 上。工人等的是 SnailJob 点名。课边界 9 覆盖。
18. **「MapReduce 的根方法也叫 `jobExecute`。」** 叫 `rootMapExecute` / `doJobMapExecute`。课误区 9 覆盖。
19. **「Summary 读不到键会失败。」** 当 0。课误区 6 覆盖。
20. **「`wta-job` 可以 import `UserService`。」** POM 没有 `wta-api`。课误区 13 覆盖。
21. **「九个都会在种子里被点名。」** 只有 `demo-job` → `testJobExecutor`。课类比失效边界 / 误区 4 覆盖。
22. **「local 配置能点到 demo-job。」** 种子命名空间 `dev`/`prod`，local yml 写 `local`。课边界 5 覆盖：不声称已连上实跑。
23. **「ai 同样瘦所以也没有 HTTP。」** L-080 有 `SnailAiController`。课变式 6 覆盖。
24. **「声明了 `throws InterruptedException` 就是会睡。」** 账单三份签名有、方法体无 sleep。课边界 1 覆盖。

问：读完后仍说不出的，是不是「九个类的文件名」，而是 **入口是类不是路径、花名才是主任要点的字符串、八贴纸 vs 一继承、账单不发外网、失败信封三种、Map 根方法名按磁盘、三道电闸分开说、九名同行不能因 chain 只印四个就留空洞、bundle/iframe/ai 窗不能因本课闭合**？

## Q4 视角

- objective_id: OBJ-78
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-78 vs L-002/L-003/L-024/L-079/L-080；chain 脊柱四类 vs 矩阵九名同行；claimed_cells 九类 vs BillDTO/SnailJobConfig；chain 估时 35 vs 课 38
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这九格 (a)**？

| 视角 | 多出来的边 | 是否本课这九格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 九名同行可一起盖章；chain 脊柱四类不是第二行；(b) 无本课符号；(d) 无 job DAG 流 | **是** 闭合定义。九名同行可视为已讲并可 covered。不要发明 (b)/(d)，不要把 `BillDTO` / `SnailJobConfig` 升成新行才能给切片盖章 |
| 贴纸工人 | 八个 `@JobExecutor(name=...)` + `jobExecute` 或 Map 注解方法 | **是** 八格 |
| 继承工人 | 无注解；`doJobExecute` | **是** `A:TestClassJobExecutor` |
| 账单 DAG | 上下文键 `settlementDate` / `alipay` / `wechat`；写死金额；缺键 0；取值 API 不对称 | **是** 脊柱三格的走查，不是 (b) 新行 |
| 最小正常任务 | LOCAL+REMOTE；种子只点它 | **是** `A:TestAnnoJobExecutor` |
| 广播 / 静态分片 / Map / MapReduce | 扔异常 vs `failure`；逗号范围；1…200 切四片；Reduce 当 String | **是** 后四格（加继承共五格）的形状 |
| 装配电工（L-079） | `bundle-full` 插 `wta-job`；`bundle-core` 拔掉 | 对照「类在不在 jar」**是**。格子 **否** |
| 客户端电闸 | `enabled: false`；`@EnableSnailJob`；REMOTE appender | 对照「扫到 ≠ 点名」**是**。common-job 叶子保持 deferred **否**当 covered |
| 主任进程 | extend 独立启动类 | 对照「另一栋楼」**是**。拆 filter/SecurityConfig **否** |
| 玻璃窗（L-024） | iframe `snail-job` + `monitor:snailjob:list` | 负空间 **是**。厨房/四页 **否**（已 covered） |
| 登记表（L-003） | classic 允许没有 ServiceImpl 链 | 不改登记 **是**。房间画像 **否**（已 covered） |
| 装配总图（L-002） | 变式 D：工人 admin / 主任 extend；最小 common | 一句 **是**。admin 组装全表 **否**（已 covered） |
| AI 窗（L-080） | 同样瘦、同样 classic，但有 HTTP | 对照「job 零扇」**是**。`registerCurrentUser` **否** |
| 流程楼（L-067…L-073） | `flow_*` vs `sj_workflow*` | 「哪栋楼」**是**。请假大厅 **否** |
| MySQL 8.4 | `20-cde-job.sql` 种子一行；非 10/50 基座 | 种子对照 **是** (a)。容器 / 基座 **否**（L-085） |
| 前端厨房 | 无 job 包 | 负空间 **是**。不要发明 Vue 行 |

问：Lead 若凭「四个脊柱名在 chain 覆盖列里」只想盖四个、却面对矩阵一行九名，是否还要求读者能指着 **无窗、八贴纸+一继承、三套嘴、账单袋子、三种失败信封、Map 根方法名、三道电闸**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要等 L-079 才给 **九名同行** 当已讲？`C:bundle-full-job` 是否例外——必须保持 uncovered？Lead 若因矩阵把九个印在同一行，就把后五类判成「没有格子」，是否假判？

## Q5 元问题

- objective_id: OBJ-78
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 九名同行仍 uncovered、`bundle-full-job` 仍 uncovered、无本课 (b)/(d)
- difficulty: low

建议：`defer`

原因：

- **九名同行与课 `claimed_cells` / 矩阵 (a) 1:1。** L 合同章节齐全（宏观无窗地图、直觉十二句、定义表、机制链 1–7、带 caption / 文字等价物的三张图、正反例、变式、误区、暂停走查、来源表 S-004/S-007/S-008/S-010/S-L002-01/S-L003-01/S-L078-01…08）。能复现：九个 `@Component`、零 HTTP 注解、八贴纸花名 + 一继承 `doJobExecute`、`testJobExecutor` 对上种子 `demo-job`、账单写死金额与缺键 0、广播扔异常、静态分片逗号+3 秒、Map 切四片无 Reduce、MapReduce `parseInt((String) i)`、yml 电闸默认关、无 job 厨房。不必为「类的名字」再挖五问。Lead 可将该行标 `covered`。
- **chain 脊柱四类不是拆行理由。** 课开篇已写明：前四个当脊柱，九个都要能指。后五类不是第二容器，是同一 OBJ-78 的入口形状。不要为了迎合 chain 覆盖列把矩阵一行拆开，也不要因此留空洞。
- 本批可粘贴的精度项不挡住盖章：（1）广播 `randomInt == 50` 成功但文案写「大于 50」；（2）Map 睡的是 `ThreadUtil.sleep`，静态分片才是 `Thread.sleep`；（3）本仓 `SnailJobServerApplication.main` 委托厂商启动类；（4）课估时 38、chain 35。不要把 bundle pom、iframe 四键、`SnailAiController`、厂商引擎内部塞进本格。
- 不建议 `mine-more`：b01 未证明这九格还有真实矩阵空洞；再五问只会把 L-079 插头、L-024 玻璃或 L-080 HTTP 再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；广播 50 文案是半句精度，不是第二容器。不建议 `split-lesson`：未满 10 问；九个工人是 chain 原设计的同一 OBJ-78（矩阵本来就印在一行）；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：DAG 袋子 / 三种失败信封 / Map 切法已是 (a) 的解释到副作用。不建议等 GP-L-079 才给工人同行当已讲。不建议用本课改 (c) `bundle-full-job` 或揭开 deferred 的 common-job 叶子。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:AlipayBillTask, WechatBillTask, SummaryBillTask, TestAnnoJobExecutor, TestBroadcastJob, TestClassJobExecutor, TestMapJobAnnotation, TestMapReduceAnnotation1, TestStaticShardingJob | 课按磁盘讲了九个入口类、无 HTTP 窗、八贴纸+一继承、三套嘴、账单 DAG 写死金额且缺键当 0、广播扔异常、静态分片逗号+sleep、Map 只切、MapReduce 按 String 加总、种子只点 `testJobExecutor`、三道电闸分开说。残留精度：广播 ==50 文案；Map `ThreadUtil.sleep` vs 分片 `Thread.sleep`；包装启动类委托厂商 main；课 38 vs chain 35。 | 可选半句：广播成功分支含 50，文案仍写「大于 50」；Map 睡眠 API 以磁盘为准。不要求重写机制链。Lead 可将矩阵该行标 `covered`（证据锚本 probe 与 L-078 来源表 S-L078-01…06）。不要因 chain 只印脊柱四类就拆行或留空洞。 | defer（本行可 covered；精度项不挡盖章） |
| C:bundle-full-job | 本课只用 full 才进 classpath、core 拔掉当对照。格子仍 uncovered。 | 不要用本课盖装配章。 | defer（L-079） |
| A:createMonitorService / iframe `snail-job` | 玻璃窗负空间已点名。该厨房已 covered（L-024）。 | 不要把 iframe 说成第九+1 个工人。 | defer（L-024 已 covered） |
| A:SnailAiController.registerCurrentUser | 「同样瘦但有 HTTP」对照已借用。该行仍 uncovered。 | 不要用本课盖 ai 窗。 | defer（L-080） |
| A:createAiService / A:createAiWebDomain | 未因本课讲完。 | 保持。 | defer（L-081） |
| C:layered-classic / wta-modules | 登记表 classic 行与业务房间已 covered。 | 不要用本课再盖房间章，也不要发动迁 layered。 | defer（L-002 / L-003 已 covered） |
| wta-common-job 叶子 / `SnailJobConfig` | 电闸+REMOTE appender 已作对照。矩阵将该 common 叶子标 deferred。 | 不要因本课揭 deferred。 | defer（deferred(not-on-happy-path-hub)） |
| wta-extend/wta-snailjob-server 内部 | 启动类对照已点。filter/SecurityConfig 不拆。 | 保持范围外。 | defer（Goal 范围外） |
| (b) 账单 DAG / 广播抛异常 / MapReduce / wfContext | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) SnailJob DAG / 点名执行器流 | 矩阵无此行。已 uncovered 的存储点仍是 10/50 基座。 | 不要发明 job 数据流行，也不要用本课改 L-085。 | defer（未入库） |
| MySQL 8.4 / 20-cde-job.sql | 种子一行与 `sj_job_executor` 无 INSERT 是 (a) 对照；文件是上游快照。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |
| 测试夹具 `*Job*Test` | 工作树没有这类测试。 | 保持 deferred(test-fixture)。证据走生产源码 + SQL + yml。 | defer（deferred(test-fixture)） |
| BillDTO / package-info | 账单 record 与空包声明已走查。 | 保持 covered-by-parent，不要升成 (a) 行。 | defer（covered-by-parent） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；九名同行无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（bundle/iframe/ai 是邻课格子，课内已把工人讲完并显式禁止整行假闭合装配/玻璃窗）；合格读者说得出函数目的（九个执行器类就是入口，不是 HTTP）、主路径（主任按花名点 `jobExecute` / `doJobExecute` / `@MapExecutor`，交 `ExecuteResult.success`）与失败路径（广播扔异常、分片中断 `failure`、账单缺键当 0、客户端未醒点不着、种子没写的花名不会被 demo-job 点到）；把可选半句写进原课不会超过 40 分钟；装配、监控 iframe、ai 窗、登记表已有邻课。满 10 问前不拆。脊柱四类与后五类虽形状不同，仍是 chain 原设计的同一 OBJ-78，矩阵本来就印在一行，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / split 提案：否（miner 不改矩阵）；split 提案为 none；缺口表对 `C:bundle-full-job`、L-024 玻璃窗（已 covered）、L-080/L-081 ai、L-002/L-003 已 covered 的房间与登记表、deferred 的 common-job 叶子、范围外的主任内核、未入库 (b)/(d)、MySQL 容器、测试夹具以及 `BillDTO` covered-by-parent 显式建议 `defer`；对矩阵 (a) 行九个执行器名建议 Lead 标 `covered`（证据锚本 probe 与 L-078 来源表 S-L078-01…06）。本课不主张发明 HTTP/Vue 矩阵行、用本课盖 bundle 或 iframe、把 chain 脊柱四类理解成「后五类未讲」、或等 GP-L-079 才给工人同行盖章。
