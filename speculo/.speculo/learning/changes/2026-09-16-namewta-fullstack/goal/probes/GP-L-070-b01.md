---
artifact: learning-goal-probe
probe_id: GP-L-070-b01
lesson_id: L-070
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-070 / b01

审问对象是已写成的 Lesson `lessons/L-070-flw-task.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain 覆盖列 **两格** `A:FlwTaskController.*` `B:IFlwTaskService.startWorkFlow,completeTask,backProcess,urgeTask,terminationTask`；课 `claimed_cells` **三格**，另加 `D:失败路径 工作流驳回/终止`）：OBJ-70 原文：能口述 `FlwTaskController` / `IFlwTaskService` 启动、完成、驳回、催办、终止。档是**方法性状**。矩阵 (a) 一行把十六个映射写全，模块 `wta-workflow`，仍 `uncovered`。矩阵 (b) 同五支枢纽，口号「流程副作用」，仍 `uncovered`。矩阵 (d) 「失败路径 工作流驳回/终止」原文 `backProcess / terminationTask`，备注 **只挂 L-070**，仍 `uncovered`。矩阵 (d) 「主路径 工作流请假」原文 `TestLeave.submitAndFlowStart → 任务完成`，备注 **L-072, L-070**，仍 uncovered；chain **未**把这条主走廊写成覆盖列，课 `claimed_cells` **也未**列它。矩阵 (c) `wta-modules` 业务房间已 covered（L-002/L-003）；MySQL 8.4 / `dao-mapper-ownership` 仍 uncovered。本批按派单审问 **(a) 十六扇 HTTP + (b) 五支枢纽副作用 + (d) 任务窗驳回/终止失败关闭**。分类树留给 L-067；图纸发布/导入留给 L-068；实例十三扇与 `WorkflowService.*`（含按 businessId 的幂等 `terminateInstance`）留给 L-069；SpEL 目录留给 L-071；请假 `submitAndFlowStart` 留给 L-072；厨房 `createWorkflowDefinitionService` / 菜单 `createWorkflowWebDomain` 留给 L-073。不要把 `setCopy` / `isTaskEnd` 升成第二张 (a)，不要把 `taskOperation` 升成第六支 (b)，也不要用本课去盖厂商 `skip` / `termination` 内部或 Redis 容器。

抽查锚点（2026-09-17）：`backend/wta-modules/wta-workflow/src/main/java/org/namewta/workflow/controller/FlwTaskController.java`；`service/IFlwTaskService.java`；`service/impl/FlwTaskServiceImpl.java`；`resources/liteflow/task-chain.el.xml`；`liteflow/start/{StartPrepareRequest,StartResume,StartPrepareInstance,StartExecute}Component.java`；`liteflow/complete/{CompletePrepare,CompleteExecute,CompleteAutoPass}Component.java`；`oss/WorkflowHistoryOssOwner.java`；`handler/FlowExceptionHandler.java`；`listener/WorkflowGlobalListener.java`；`domain/bo/{StartProcessBo,CompleteTaskBo,BackProcessBo,FlowTerminationBo,FlowUrgeTaskBo}.java`；`mapper/{FlwTaskMapper,FlwHisTaskMapper}.java`；`common/ConditionalOnEnable.java`；`common/constant/FlowConstant.java`；`common/enums/TaskAssigneeType.java`；`service/impl/{WorkflowServiceImpl,FlwInstanceServiceImpl,FlwCommonServiceImpl}.java`；`wta-common-core/.../BusinessStatusEnum.java`；测试 `backend/wta-admin/src/test/java/org/namewta/test/oss/owner/workflow/WorkflowHistoryOssOwnerUnitTest.java`、`.../profile/contract/WorkflowTerminationContractTest.java`；旁证（格子归邻课）`WorkflowService.terminateInstance` / `startCompleteTask`（L-069）、`processInvalid`（L-069）、厨房 `frontend/packages/domains/workflow/src/index.test.ts`（L-073）；登记表 `03-backend-module-modes.md` classic 行。`controller/` 下 **零** UseCase。工作树 **零** `FlwTaskController` HTTP 合同测试，**零** `backProcess` / `terminationTask` / `urgeTask` / `startWorkFlow` 行为单测。

## Q1 澄清

- objective_id: OBJ-70
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-70；chain L-070 覆盖列只 A+B；L-070 `claimed_cells` 三格 A+B+D；矩阵 (a) 十六映射一行仍 uncovered；(b) 五支枢纽仍 uncovered；(d) 驳回/终止只挂 L-070；(d) 请假主路径挂 L-072 与 L-070 但本课未认；负空间 L-067/L-068/L-069/L-071/L-072/L-073；(c) 房间已 covered
- difficulty: medium

课把任务柜台收成「一块 `/workflow/task` 牌子：十六扇窗，五支会改世界的枪，驳回/终止失败停在闸前」。计划里至少有六套可数对象，派单点名的是 **一格 (a) + 一格 (b) + 一条失败 (d)**：

1. **OBJ-70 原文。** `course.md`：能口述 `FlwTaskController` / `IFlwTaskService` 启动、完成、驳回、催办、终止。档是方法性状。一句里是 **柜台类名 + 服务类名 + 五支枢纽动词**，没有把十六扇逐个写成 OBJ 清单，也没有写出「失败路径 工作流驳回/终止」这半句，更没有把 `WorkflowService.terminateInstance`、`TestLeave.submitAndFlowStart`、厨房工厂写成口试名单。
2. **chain 覆盖列。** 两格：`A:FlwTaskController.*`、`B:IFlwTaskService.startWorkFlow,completeTask,backProcess,urgeTask,terminationTask`。不写 (d)，不写请假主走廊，不写 `WorkflowService.*`。
3. **课 `claimed_cells` / 矩阵对照。** 课把头扩成三格，把 (d) 钉成 `backProcess / terminationTask`。矩阵 (a) 那一行十六个名字与磁盘 `@*Mapping` 1:1。矩阵 (b) 口号「流程副作用」对着五支枪，不是对着查询十一扇。矩阵 (d) 失败行只挂本课——和 L-052「主路径还要邻课」不同，**失败 (d) 可以独盖**。矩阵 (d) 请假主走廊挂 L-072 **和** L-070：那是邻格对照，chain / `claimed_cells` 都没把它写成覆盖列。Lead 若按 **现印 (a) 行** 标 covered，**不会**把 L-069 十三扇实例或合同十一法假闭合；若凭「terminate 三字」把 `WorkflowService.terminateInstance` 一并盖章，则会假闭合 L-069。
4. **课自己的操作定义。** (a) 门牌 `/workflow/task`，磁盘 16 映射；写操作几乎全 POST，换人是唯一 PUT；全楼两扇要 `workflow:task:list`，催办/换人要 `workflow:task:edit`；启动/办理/驳回/终止方法上无权限字但仍过登录；`@ConditionalOnEnable` 整类随 `warm-flow.enabled=true` 进 Spring。接口 23 法，多出的 7 个（`setCopy` / `selectByIdList` / `selectHisTaskById` / `selectByInstId(s)` / `isTaskEnd` / `getByNodeCode`）无 HTTP。(b) 启动锁 `flowCode+businessId`、按 **businessId** 续写或新开 `draft`；办理锁 `taskId`、LiteFlow `skip(PASS)`、成功恒 `true`；驳回无 LiteFlow、无 `@Lock4j`、`skip(REJECT)`、状态闸 `checkBackStatus`；催办只 `sendMessage`，可 `false`；终止 `taskService.termination(taskId)`，无 `ignore(true)`，无业务 ID 锁，状态闸 `checkInvalidStatus`。(d) 任务空/实例空/非法状态/引擎 `FlowException`/附件对不上都抛；**不是**合同窗 `NO_ACTIVE_INSTANCE`。已退回可终止、不可再驳回。模块登记 **classic**：无 UseCase、无 DAO。
5. **邻格。** L-069 大厅十三扇 + `WorkflowService.*`（启动/办理只是 `BeanUtil.toBean` 再喊本课两枪；`terminateInstance` 认 businessId、`.ignore(true)`、锁 `'workflow:terminate:' + #businessId`、缺实例或已终态返回 `NO_ACTIVE_INSTANCE`）。L-072 请假一枪写单并发流。L-073 厨房 URL 几乎对齐十六扇。L-068 发布闸。L-067 抽屉标签。L-071 暗号本。厂商 `skip` / `termination` 内部是矩阵 deferred(vendor-engine)。
6. **未入库。** 矩阵 **没有** `taskOperation` 独行 (b)、没有 `checkTaskReadAccess` 独行、没有「主路径 任务办理」第二 (d)、没有 `startProcessChain` 符号行。查询十一扇是 (a) 的地图，不是第二组 (b)。

问：本三格闭合标准是「十六扇 URL/动词/权限/RepeatSubmit/返回形状 + 五支枢纽锁/事务/LiteFlow 有无 + 驳回/终止失败关闭（taskId 钥匙、非幂等、无 ignore）且不把合同窗终止、作废、请假主走廊、厨房工厂算进来」，还是 OBJ 那句五动词必须先等 GP-L-069 的 `terminateInstance`、或必须先把请假 (d) 标 covered、或必须等改 chain 印上 (d) 才给失败行盖章？Lead 若只凭「`terminationTask` 与 `terminateInstance` 都叫终止」把 L-069 合同十一法或 L-072 请假七枪标 covered，是否假闭合了总机与示例流？

## Q2 证据

- objective_id: OBJ-70
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 32–240；IFlwTaskService 24–217 / 59–66；FlwTaskServiceImpl 86–91 / 117–137 / 201–230 / 299–332 / 343–360 / 476–492 / 515–520 / 749–768 / 791–802；task-chain.el.xml 10–60；StartPrepareRequest 32–50；StartResume 36–46；StartExecute 38–57；StartPrepareInstance 32–41；CompletePrepare 42–64；CompleteExecute 34–56；CompleteAutoPass 70–85；BusinessStatusEnum 106–215；WorkflowServiceImpl 61–87 / 162–218；TerminationContractTest 31–105；OssOwnerUnitTest 118–121；OssOwner 52–79；FlowExceptionHandler 26–30；BackProcessBo 42–45 / 60；FlowTerminationBo 13–16；FlowUrgeTaskBo 12–37；FlwTaskMapper 50–60；FlwHisTaskMapper 53–63；ConditionalOnEnable 27；modes.md 22；FlowConstant 69 / 79；applyNodeCode 186–191；processInvalid 450–462；setCopy 147–157；listener 117–132 / 252–281；kitchen test 159–174
- difficulty: high

课的 (a) 十六扇、(b) 五支枢纽、(d) 驳回/终止失败哪些能在工作树直接复现，哪些只是测试名单比课表窄、或邻格事实？

**仍能复现（本课 A + B + D）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 门牌 `/workflow/task`；`@ConditionalOnEnable`；只注入 `IFlwTaskService`；无 `@SaIgnore` | Controller 32–39；ConditionalOnEnable 27 |
| 磁盘 16 个 `@*Mapping`：start/complete/五扇列表/getTask/next/termination/taskOperation/updateAssignee/back/backNodes/users/urge | Controller 49 / 63 / 75 / 87 / 100 / 113 / 125 / 136 / 147 / 160 / 174 / 189 / 202 / 214 / 225 / 236 |
| 换人是唯一 PUT；催办无 `@RepeatSubmit`；终止窗 **未** `@Validated(AddGroup)`；终止返回 `R.ok(Boolean)` 不是 `toAjax` | Controller 189 / 236–240 / 160–162；BaseController `toAjax(boolean)` 29–30 |
| 启动/办理/驳回 AddGroup：`businessId`/`flowCode` 非空，`taskId` 非空；催办 BO 有 AddGroup 但窗上没启用 | StartProcessBo 30–37；CompleteTaskBo 28–29；BackProcessBo 29–30；FlowUrgeTaskBo 25–37 |
| 全楼两扇 `workflow:task:list`；催办/换人 `workflow:task:edit`；启动/办理/驳回/终止方法上无权限字 | Controller 99 / 112 / 188 / 238 vs 47–65 / 158–162 / 200–204 |
| 接口 23 法；无 HTTP 的 7 个：`setCopy` / `selectByIdList` / `selectHisTaskById` / `selectByInstId` / `selectByInstIds` / `isTaskEnd` / `getByNodeCode` | IFlwTaskService 24–217 |
| `pageByTaskFinish` 接口 JavaDoc 写成「当前租户所有待办」；实现走已办历史 | IFlwTaskService 59–66；FlwTaskServiceImpl 215–217；FlwHisTaskMapper 36–63 |
| classic：无 usecase 包；登记表 `wta-workflow` = classic | grep usecase 空；modes.md 22 |
| LiteFlow 三条链：`startProcessChain` / `completeTaskChain` / `taskOperationChain`；**没有** back/terminate/urge 链 | task-chain.el.xml 10–60 |
| 启动 `@Transactional` + `@Lock4j(flowCode + businessId)`；查找实例只 `eq(FlowInstance::getBusinessId)`，不问 flowCode | FlwTaskServiceImpl 117–122；StartPrepareRequest 47–49 |
| 已有实例 → `checkStartStatus` 后续写；无任务 →「流程实例缺少任务…」；无实例 → 未发布抛、`flowStatus=draft`、任务数 0/多于 1 抛 | StartResume 38–46；StartPrepareInstance 32–34；StartExecute 40–57 |
| `checkStartStatus` 挡 waiting/finish/invalid/termination/空白；放行 draft/back/cancel | BusinessStatusEnum 145–156 |
| 办理 `@DSTransactional` + `@Lock4j(taskId)`；LiteFlow 不抛则 **恒 `return true`** | FlwTaskServiceImpl 132–137 |
| 任务空文案「流程任务不存在或任务已审批！」；draft/cancel/back 再办塞 `submit=true` | CompletePrepare 47–63 |
| `skipType=PASS`，`ignore` 从变量读；capture → skip → reconcileCreated | CompleteExecute 39–56 |
| autoPass 只过当前登录人后续待办；留言改成「流程引擎自动审批！」；不继承抄送/通知 | CompleteAutoPass 70–85 |
| 驳回 `@DSTransactional`、**无** `@Lock4j`；mapper `selectById` 空 →「任务不存在！」；实例空 →「流程实例不存在」；`checkBackStatus`；`.nodeCode(bo.getNodeCode())`；目标=申请人 → 实例 `back` 否则 `waiting`；`hisStatus=back` | FlwTaskServiceImpl 299–332 |
| BO 注释「nodeCode 目前未使用」；实现用了。`variables` 字段 **未** 塞进 `FlowParams.variable`（只塞 messageType/notice） | BackProcessBo 42–45 / 60；FlwTaskServiceImpl 317–323 |
| `applyNodeCode` = 定义上第一个 BETWEEN；缺则「流程定义缺少申请人节点」 | FlwCommonServiceImpl 186–191 |
| 终止 `@Transactional`（不是 DS）、无 Lock4j；`taskService.getById` 空 →「任务不存在！」；实例非空才 `checkInvalidStatus`；实例空仍 `termination(taskId)`；FlowParams 只有 message/flowStatus/hisStatus，**无** `.ignore(true)` | FlwTaskServiceImpl 476–492 |
| `checkBackStatus` 额外挡 back 与 cancel；`checkInvalidStatus` 只挡 finish/invalid/termination/空白，放行 draft/waiting/back/cancel | BusinessStatusEnum 185–215 |
| 催办无事务无锁；空 `taskIdList` 或看不见办理人 → `false` → `toAjax` 变 `R.fail()`；`sendMessage(..., PATH_TASK_WAITING)`；不调 `taskService` | FlwTaskServiceImpl 791–802；FlowConstant 69 |
| 我的待办把 userId 传入且 SQL 在 userId 非空时锁 `flow_status=waiting`；全楼待办传 `null`，waiting 锁消失 | FlwTaskServiceImpl 201–230；FlwTaskMapper 59–60 |
| 待办办理人类型 `"1"|"2"|"3"`；抄送类型 `"4"` | FlwTaskMapper 51；TaskAssigneeType 19–37 |
| `getTask`：任务空返回 null，Controller `R.ok(null)`；非空走读闸 | FlwTaskServiceImpl 515–520；Controller 136–138 |
| 读闸：超管或 `workflow:task:list`/`edit` 或审批/转办/委托/抄送关系人或发起人；否则 `NotPermissionException("无权访问该流程任务")` | FlwTaskServiceImpl 749–768 / 86–91 |
| 办理/驳回附件对账失败抛「未生成预期的历史任务 / 附件与办理请求不一致 / 未持久化附件」 | WorkflowHistoryOssOwner 52–79 |
| OSS 单测锁死 `completeTask` 与 `backProcess` 带 `@DSTransactional`；**不**断言 `terminationTask` | OssOwnerUnitTest 118–121 |
| 引擎 `FlowException` → `R.fail(message)` | FlowExceptionHandler 26–30 |
| 退回到申请人：`shouldSendTaskMessage` 为 false，避免待办+结果两封 | WorkflowGlobalListener 252–281 |
| `getBackTaskNode`：任务空抛；已有 DEPUTE 关系人则返回当前节点列表；`anyNodeSkip` 则只返回那个固定点 | FlwTaskServiceImpl 343–360 |
| `setCopy` 找不到历史行 →「流程历史任务不存在，无法添加抄送记录」 | FlwTaskServiceImpl 147–157 |
| `FlowTerminationBo` record 的 `taskId` 带 AddGroup `@NotNull`，窗上没启用分组校验 | FlowTerminationBo 13–16 vs Controller 160–161 |
| `FlowUrgeTaskBo` 类注释写成「流程变量参数」；字段是 `taskIdList` / `messageType` / `message` | FlowUrgeTaskBo 12–37 |

**测试名单比课表窄（不证伪机制，挡住「单测即格子」）：**

| 课的机制 | 测试实际锁什么 |
| --- | --- |
| 十六扇 HTTP 动词/权限/返回形状 | **零** `FlwTaskController` 合同测试。厨房 `index.test.ts` 159–174 锁的是浏览器 URL 拼法，格子归 L-073 |
| `backProcess` 对象闸 / `checkBackStatus` / `nodeCode` 决定 back vs waiting | **零** 驳回行为单测。证人是 ServiceImpl 299–332 + enum 185–198 |
| `terminationTask` 非幂等、「任务不存在」、无 ignore、无业务 ID 锁 | **零** 任务窗终止单测。`WorkflowTerminationContractTest` 锁的是 **`WorkflowServiceImpl.terminateInstance`**（L-069）：`NO_ACTIVE_INSTANCE`、`.ignore(true)`、锁 `'workflow:terminate:' + #businessId`、`@DSTransactional` |
| 催办空名单 `false`、不写任务行 | **零** `urgeTask` 单测 |
| 启动按 businessId 续写、锁键拼了 flowCode | **零** `startWorkFlow` 单测。证人是 StartPrepareRequest 47–49 + 方法注解 118 |
| 办理成功恒 true、失败抛 | **零** `completeTask` 行为单测。OSS 测试只钉事务注解 |

**邻格事实（对照，不是本三格）：**

| 声称 | 磁盘锚点 | 格子 |
| --- | --- | --- |
| 合同窗 `terminateInstance(businessId, reason)`：缺实例或 `finishStatus` → `NO_ACTIVE_INSTANCE` 且 **不**调引擎；运行态 `terminationByInsId` + `ignore(true)` + 业务 ID 锁 | WorkflowServiceImpl 61–87；TerminationContractTest 54–105 | L-069 |
| 合同 `startWorkFlow` / `completeTask` 只 `toBean` 再喊本课；`completeTask(taskId, message)` 自己塞 `ignore=true`；`startCompleteTask` 启动后立刻办第一枪 | WorkflowServiceImpl 162–218 | L-069 |
| 实例窗作废 `processInvalid`：`terminationByInsId` + `flowStatus=invalid` + `ignore(true)` | FlwInstanceServiceImpl 450–462 | L-069 |
| 厨房十六扇 URL 含 `post /workflow/task/terminationTask` 与 `urgeTask`；同一测试还打了 instance/leave | index.test.ts 159–191 | L-073；leave 段 L-072 |
| 请假 `submitAndFlowStart` | TestLeave 控制器（未本批拆） | L-072 |

**课写过、口试标签写偏（不是证伪 A/B/D）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「任务窗终止 `getById(null)` 走任务不存在，不是 Bean Validation 400」 | Controller 未 `@Validated(AddGroup)`；`taskService.getById(null)` 是否 NPE 还是空，属引擎/空值行为。课把失败收成「任务不存在」是 NAMEWTA 闸的口试句，**无** HTTP 测试钉 400 vs 500 |
| 「锁键 `flowCode+businessId` 与查找只认 businessId 是裂缝」 | 注解 118 与查询 47–49 确实不是同一把钥匙。**无** 并发单测锁这道裂缝 |
| 矩阵 (d) 请假主走廊挂了 L-070 | 课明确不认 `submitAndFlowStart`。不要用本三格去盖那一行 |

问：在十六映射、三条 LiteFlow、启动按袋号续写、办理恒 true、驳回用 `nodeCode`、终止无 ignore 非幂等、催办只按铃、两套状态闸已经能互证的前提下，三格是否还要把「零 HTTP 单测」「锁键≠查找键」「终止 null taskId 的 HTTP 状态码」写成开篇半句才算讲完？chain 没写 (d) 时，能不能凭矩阵已有失败行 + 课 `claimed_cells` + OBJ-70 五动词里的驳回/终止盖章，而不是先改 chain？请假主走廊挂了 L-070，是不是邻格对照而不是本课第二张 (d)？

## Q3 假设与反例

- objective_id: OBJ-70
- bloom_level: analyze
- socratic_move: assumptions
- expected_evidence: Controller 160–162 / 236–240；FlwTaskServiceImpl 117–137 / 299–332 / 476–492 / 791–802；StartPrepareRequest 47–49；CompletePrepare 47–48；BackProcessBo 42–45；BusinessStatusEnum 145–215；WorkflowServiceImpl 61–87 / 187–218；task-chain.el.xml 10–60；OssOwnerUnitTest 118–121；TerminationContractTest 54–105；IFlwTaskService 59–66；课反例 1–18 / 误区 1–18 / 变式 A–H
- difficulty: high

课的隐含假设若反转，合格读者会把哪段走廊认错？

1. **「`FlwTaskController.*` 就是五支枢纽。」** 十六扇。(a) 含查询、next、backNodes、taskOperation、换人。五支是 (b)。课误区 1。
2. **「`IFlwTaskService` 有的方法 Controller 都有。」** 少 7 个内部法。`setCopy` 由副作用监听器调用。课反例 1。
3. **「启动每次 insert 新实例。」** 同一 `businessId` 走 `startResume`。课反例 2。
4. **「续提交会核对这次带来的 `flowCode`。」** 查找键只有 businessId。锁键才拼了 flowCode。课反例 3 / 误区 3。
5. **「`completeTask` 失败返回 `false`，单据半通过。」** 失败抛；成功恒 true；`@DSTransactional` 回滚。`false` 出现在催办空名单和空换人列表。课反例 4。
6. **「办理任务空的文案和驳回一样。」** 办理是「流程任务不存在或任务已审批！」；驳回/终止是「任务不存在！」。
7. **「驳回总是退回申请人，`nodeCode` 没用。」** `.nodeCode(bo.getNodeCode())`；注释过期。课反例 5。
8. **「`POST /terminationTask` 就是 `WorkflowService.terminateInstance`。」** 钥匙 taskId vs businessId；引擎 `termination` vs `terminationByInsId`；无 ignore vs ignore；无业务锁 vs `'workflow:terminate:'`；抛 vs `NO_ACTIVE_INSTANCE`；`R.ok(Boolean)` vs 合同结果枚举。课反例 6。
9. **「终止已退回的单会 `checkBackStatus`。」** 终止走 `checkInvalidStatus`，back **允许**终止。课反例 7 / 误区 17。
10. **「催办会把任务标成超时/催办状态。」** 只 `sendMessage`。课反例 8。
11. **「我的待办和全楼待办只差 userId / 只差权限字。」** 我的待办还有 `eq flow_status waiting`。课反例 9。
12. **「`getTask` 找不到会抛 / 404。」** `selectById` 返回 null，`R.ok(null)`。课反例 10。
13. **「任务窗终止也 `@DSTransactional` 并协调 OSS。」** 普通 `@Transactional`，不调 `WorkflowHistoryOssOwner`。OSS 单测名单里没有它。课反例 11。
14. **「LiteFlow 也编排驳回/终止/催办。」** XML 三条链：start / complete / taskOperation。课误区 14。
15. **「作废和终止是同一扇 HTTP。」** 作废在实例柜台 `processInvalid`，终态 `invalid`。本课终态 `termination`。课反例 13。
16. **「`pageByTaskFinish` 按接口注释是全租户待办。」** 注释错。实现是已办。课反例 14。
17. **「HTTP 任务柜台有启动并办理。」** 没有。合同窗 `startCompleteTask` 或请假 `submitAndFlowStart`。课变式 F。
18. **「浏览器办理默认 `ignore=true`。」** 办理链读变量；后台 `completeTask(taskId, message)` 才塞 ignore。课变式 A。
19. **「本课覆盖 `WorkflowService.*` / 厨房工厂 / 请假七枪。」** L-069 / L-073 / L-072。把假设反转后，Lead 会用任务窗假闭合总机、示例流或厨房。
20. **「失败 (d) 必须等改 chain 才盖章。」** 矩阵已有该行且只挂 L-070。课 `claimed_cells` 已列。OBJ-70 五动词含驳回/终止。
21. **「矩阵要先有 `taskOperation` (b) 独行 / 厂商 skip 内部课才算 (a) 讲完。」** 矩阵无这些行。操作四码是 (a) 地图；厂商内部 deferred。
22. **「请假主走廊挂了 L-070，所以本课也可以把 `submitAndFlowStart → 任务完成` 标 covered。」** 本课不认那一行。汇在 L-072。

问：读完后仍说不出的，是不是「十六个方法名」，而是 **启动认袋号、盖章认号票、驳回闸与终止闸不是同一组状态、任务窗终止非幂等且无 ignore、催办不写库、办理/驳回才走 OSS 动态事务、合同窗终止是另一扇门**？

## Q4 视角

- objective_id: OBJ-70
- bloom_level: analyze
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-70 vs L-067…L-073 / L-083 / L-085；chain 只 A+B；课 claimed_cells A+B+D；modes.md classic；厂商 skip deferred
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:FlwTaskController.*`** 或 **`B:IFlwTaskService` 五支** 或 **`D:失败路径 工作流驳回/终止`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 十六映射一行 + (b) 五支 + (d) 失败只挂本课；chain 漏印 (d)；请假主走廊挂了 L-070 | **是** 闭合定义。三格可单独 covered。不要等改 chain 才盖 (d)。不要用本课盖请假 (d) |
| 窗口计数员 | 16 映射；PUT 只有换人；催办无 RepeatSubmit；终止 `R.ok(Boolean)` | **是** (a) |
| 接口计数员 | 23 法，7 个无窗 | 多出的 7 个是 (a) 负空间，不必发明第二张 (a) |
| LiteFlow 编排 | 三条链；启动续写；办理 PASS+autoPass | **是** (b) 启动/办理。驳回/终止/催办 **不是** 链 |
| 驳回闸门 | 对象空、`checkBackStatus`、`nodeCode`、OSS 回滚 | **是** (b)+(d) |
| 终止闸门 | taskId、`checkInvalidStatus`、无 ignore、非幂等 | **是** (b)+(d)。合同窗幂等 **否**（L-069） |
| 催办按铃 | 只消息；可 false；路径 `/task/taskWaiting` | **是** (b)。不是 (d) 失败走廊 |
| 总机（L-069） | `WorkflowService.*`；`terminateInstance` 认袋子 | 对照 **是**。(a)/(b) 十一法 **否** |
| 作废柜员（L-069） | `processInvalid` → `invalid` | **否** |
| 请假示例（L-072） | `submitAndFlowStart` | 主走廊挂名 **否** 当本课 covered |
| 厨房（L-073） | 十六扇 URL + instance/leave | URL 对齐对照 **是**。工厂 **否** |
| 图纸/分类/SpEL | 发布闸、抽屉、暗号本 | **否**（L-068/L-067/L-071） |
| OSS 邮差 | 办理/驳回 capture-reconcile | 办理/驳回 **是** (b)。终止 **否** |
| 厂商引擎 | `skip` / `termination` 内部图 | deferred。失败时能指的是 NAMEWTA 闸 **是** (d) |
| 测试夹具行 | `*Test.java` 矩阵已 deferred | 证人 **是**。不要把夹具行或邻课 `WorkflowTerminationContractTest` 标成本课 (b) |
| 登记表作者 | classic，不改内部层次 | **是** 房间模式。不要发动 layered 重构 |
| MySQL 8.4 / dao-mapper | WarmFlow 表 + 本模块 Mapper | 本窗 SQL 举例 **是**。容器/组件课 **否**（L-085/L-083） |

问：Lead 若凭「`FlwTaskController` / `terminationTask` 四字在函数表里」把 (a)(b)(d) 标 covered，是否还要求读者能指着 **十六扇形状、五支锁与事务、两套状态闸、任务窗终止非幂等无 ignore**？这些是不是已经在本格机制链/反例里，而不需要把合同十一法、请假七枪或厨房工厂算进来？失败 (d) 是不是矩阵已有、只挂本课、不必先改 chain？请假主走廊是不是明确「挂名不等于本课认格」？

## Q5 元问题

- objective_id: OBJ-70
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 十六映射、(b) 五支枢纽、(d) 驳回/终止只挂 L-070；chain 只印 A+B；课 claimed_cells A+B+D
- difficulty: low

建议：`defer`

原因：

- **(a)(b) 与失败 (d) 与矩阵 / OBJ-70 / 课 `claimed_cells` 1:1；chain 覆盖列少印了 (d)，但不缺机制。** L 合同章节齐全（宏观地图、直觉十二句、定义表、机制链 1–7、带 caption / 文字等价物 / 图边界的十六扇图与失败闸图、正反例、变式 A–H、误区 1–18、暂停走查、来源表 S-004/S-008/S-015/S-L070-01…08）。**主叙事站得住且与源码对齐**：十六扇 HTTP、classic 无 UseCase、三条 LiteFlow、启动按 businessId 续写或 draft 新开、办理 PASS+autoPass+OSS、驳回 REJECT+`nodeCode`+OSS、催办只消息、终止 `termination(taskId)` 无 ignore、两套状态闸、失败抛而不是合同窗幂等枚举。不必为「五支名字」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例/边界或邻格，不挡住把 **(a)(b) 与失败 (d)** 标 covered：（1）零 `FlwTaskController` HTTP 测试、零驳回/终止/催办/启动行为单测；（2）`WorkflowTerminationContractTest` 是 L-069 总机证人，不是 `IFlwTaskService.terminationTask`；（3）启动锁键拼了 flowCode、查找只认 businessId；（4）终止窗未启用 AddGroup，null `taskId` 的 HTTP 状态码无合同测试；（5）`BackProcessBo.variables` 不下传；（6）接口 `pageByTaskFinish` JavaDoc 写偏。不要把 `WorkflowService.*`、请假七枪、厨房工厂、厂商 skip 内部、`taskOperation` 第六支 (b) 塞进本三格。
- **不要发明 `taskOperation` (b) 行，也不要等 GP-L-069/L-072/L-073 才给任务窗盖章。** 矩阵失败 (d) 只挂 L-070。L-069 只保证总机认袋子；L-072 只保证示例流写单并发流；L-073 只保证厨房 URL。本课保证窗口条与五支枪。
- **失败 (d) 不必改 chain 才盖章。** 证据锚是矩阵已有该行 + 课 `claimed_cells` + OBJ-70 五动词中的驳回/终止 + 课机制链 4–5 + 本 probe。miner 不改 chain。失败 (d) 与 (b) 的 back/termination 是同一条河的两岸。
- **请假主走廊不要凭本课标 covered。** 矩阵挂了 L-070 只说明任务完成会碰到本课办理枪；认格的是 L-072。
- 不建议 `mine-more`：b01 未证明三格还有真实矩阵空洞；再五问只会把 L-069 合同窗、L-072 请假、L-073 厨房、`taskOperation` 四码或厂商 skip 内部再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；可选半句（测试名单窄、锁键≠查找键、终止校验未启用、variables 不下传）不是第二容器。不建议 `split-lesson`：未满 10 问；十六扇与五支枢纽与驳回/终止失败是 chain 原设计的同一 OBJ-70，同一 `wta-workflow` 容器，不是两个 C4。`split_depth: 0`，本批禁止填 split 表。不建议发明 UseCase 层或把 classic 改 layered。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:FlwTaskController.* | 十六映射、门牌、权限字分布、RepeatSubmit、终止 `R.ok(Boolean)`、催办无防重、`getTask` 空 data、条件装配——均与源码对齐。残留精度：零 HTTP 合同测试；厨房 URL 证人属 L-073。 | 可选半句：口试十六扇以 Controller 映射为准，不以厨房测试名单当本格测试。Lead 可将本行标 `covered`（证据锚本 probe 与 L-070 来源表 S-L070-01）。 | defer（本行可 covered；精度项不挡盖章） |
| B:IFlwTaskService.startWorkFlow, completeTask, backProcess, urgeTask, terminationTask | 启动续写/draft、办理恒 true+OSS+autoPass、驳回 REJECT+nodeCode、催办只消息可 false、终止无 ignore 无业务锁——均与源码对齐。残留：零五支行为单测；锁键≠查找键；`variables` 不下传；OSS 单测不钉终止。 | 可选半句：口试「终止也走动态事务/OSS」以 `terminationTask` 方法体为准（普通 `@Transactional`、不调 OssOwner）。Lead 可将本行标 `covered`。 | defer（本行可 covered） |
| D:失败路径 工作流驳回/终止 | 矩阵口号 `backProcess / terminationTask`。磁盘：对象空抛、两套状态闸不同、引擎 `R.fail`、驳回附件回滚、终止非幂等无 `NO_ACTIVE_INSTANCE`、已退回可终止不可再驳回。chain 未列本行。 | 不要等改 chain。不要把合同窗幂等终止写进这行。Lead 可将本行标 `covered`。 | defer（本行可 covered；证据在本课） |
| D:主路径 工作流请假 | 矩阵挂 L-072, L-070。本课办理枪是对照，不是该行符号。 | 不要用本课改该行。汇在 L-072。 | defer（L-072；本课不认格） |
| A:FlwInstanceController.* / A:WorkflowService.* | 总机终止、作废、启动办理转交、`startCompleteTask`。 | 对照「号票 vs 袋子」。L-069。 | defer（L-069） |
| A:TestLeaveController.* | `submitAndFlowStart`。 | 保持。L-072。 | defer（L-072） |
| A:createWorkflowDefinitionService / A:createWorkflowWebDomain | 厨房十六扇 URL。 | URL 对照一句即可。L-073。 | defer（L-073） |
| A:FlwCategoryController.* / A:FlwDefinitionController.* / A:FlwSpelController.* | 抽屉、图纸、暗号本。 | 保持。L-067/L-068/L-071。 | defer（邻课） |
| `taskOperation` / `updateAssignee` / `setCopy` / `isTaskEnd` 升格 (b) 或第二 (a) | 课已写进 (a) 地图或内部法负空间。矩阵无独行。 | 不要发明第六支枢纽。 | defer（未入库） |
| 厂商 `skip` / `termination` 内部 | 课已 deferred(vendor-engine)。 | 失败时只指 NAMEWTA 闸。 | defer（vendor-engine） |
| C:MySQL 8.4 / mysql-base / dao-mapper-ownership | 待办 SQL waiting 锁是本窗机制。 | 不要因本课标容器/组件 covered。 | defer（(c) 容器 / L-085 / L-083） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；(a)(b) 与失败 (d) 无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出目的（窗口条盖章、启动认袋号、办理/驳回/终止认号票、催办只按铃）、主路径（续写或 draft 新开 → skip PASS；驳回 skip REJECT 到 `nodeCode`；终止 `termination(taskId)`）与失败路径（对象空/状态闸/引擎或附件抛，任务窗非幂等，不是 `NO_ACTIVE_INSTANCE`）；失败 (d) 缺的是 chain 漏印，不是讲义容器；把可选半句写进原课不会超过 40 分钟；实例合同、请假、厨房、分类/定义/SpEL 已有邻课。满 10 问前不拆。十六扇与五支枢纽与驳回/终止失败是 chain 原设计的同一 OBJ-70，同一 `wta-workflow` 容器，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-069 实例/合同、L-072 请假主走廊、L-073 厨房、L-067/L-068/L-071、未入库 `taskOperation` 升格、厂商引擎内部、(c) 容器与 dao-mapper 组件显式建议 `defer`；对 `A:FlwTaskController.*` 与 `B:IFlwTaskService.startWorkFlow,completeTask,backProcess,urgeTask,terminationTask` 与 `D:失败路径 工作流驳回/终止` 建议 Lead 标 `covered`（证据锚本 probe 与 L-070 来源表 S-L070-01…08）；对失败 (d) 建议凭矩阵已有行盖章，不要等改 chain；不要发明第二 (d)、不要用任务窗假闭合 `WorkflowService.*` 或请假七枪、不要把 `WorkflowTerminationContractTest` 当成任务窗终止证人。本课不主张发明 UseCase、把 `taskOperation` 升成 (b)、或等 GP-L-072 才给五支枢纽盖章。
