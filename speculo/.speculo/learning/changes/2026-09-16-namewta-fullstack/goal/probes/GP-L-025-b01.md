---
artifact: learning-goal-probe
probe_id: GP-L-025-b01
lesson_id: L-025
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-025 / b01

审问对象是已写成的 Lesson `lessons/L-025-oss-config.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain / `claimed_cells`）：`A:SysOssConfigController.*`、`B:ISysOssConfigService.updateOssConfigStatus`。OBJ-25：能口述 `SysOssConfigController` 配置切换副作用。矩阵 (a) 一行点名六扇：`list, getInfo, add, edit, remove, changeStatus`（与磁盘 `*` 1:1，没有 export/refreshCache）。矩阵 (b) 一行：`ISysOssConfigService.updateOssConfigStatus`（切换默认存储）。矩阵 (c) MinIO/OSS、(d) 直传主/失败路径仍 uncovered，留给 L-027/L-029。本批按派单审问这两格；`OssFactory` / `OssConfigChangeListener` / readiness 只作 B 的下游副作用，不另开格子。对象窗、直传、迁移、浏览器 adapter 留给 L-026…L-029；`createSystemService.resources` 15 键留给 L-019；L-021 `refreshCache` 扫帚不搬来。

抽查锚点（2026-09-16）：`backend/wta-modules/wta-system/.../controller/system/SysOssConfigController.java`；`service/ISysOssConfigService.java`；`service/impl/SysOssConfigServiceImpl.java`；`mapper/SysOssConfigMapper.java` 与空 `SysOssConfigMapper.xml`；`event/OssConfigChangeEvent.java`；`listener/OssConfigChangeListener.java`；`runner/SystemApplicationRunner.java`；`oss/readiness/OssStorageReadinessService.java`；`domain/bo/SysOssConfigBo.java`；`domain/vo/SysOssConfigVo.java`；`OssConstant` / `CacheNames.SYS_OSS_CONFIG` / `OssFactory` / `OssProperties` / `AccessPolicy` / `RedisUtils.setCacheObject` / `RepeatSubmit` / `BaseController.toAjax`；`OssConfigHttpContractUnitTest` / `OssConfigGovernanceUnitTest` / `OssStorageReadinessSchedulingUnitTest`；`10-cde-base-ddl.sql` `sys_oss_config`；`50-cde-base-dml.sql` 种子五行 + `NAMEWTA-OSS-ACCESS-DML-001`；`frontend/packages/domains/system/src/{service.ts,resource-service.ts,resource-service.test.ts}`；`web-domains/system/src/oss-config/OssConfigPage.vue`；`frontend/e2e/oss-config-access-policy.spec.ts`；登记表 `03-backend-module-modes.md` classic 行。

## Q1 澄清

- objective_id: OBJ-25
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-25；chain L-025 覆盖列；L-025 `claimed_cells`；矩阵 (a) 六扇全表；矩阵 (b) `updateOssConfigStatus` 独行；(c) MinIO/OSS 与 (d) 直传流仍 uncovered
- difficulty: medium

课把仓库名牌柜台收成「六扇窗 + 换正门」。计划里至少有四套可数对象，派单点名两格：

1. **OBJ-25 原文。** `course.md`：能口述 `SysOssConfigController` **配置切换副作用**。点名的是切换句，不是「把 ConfigController 九扇抄过来」。
2. **chain / 课 `claimed_cells`。** 两格：`A:SysOssConfigController.*`（磁盘六扇，无 export/refresh）+ `B:ISysOssConfigService.updateOssConfigStatus`（`changeStatus` 唯一调用的切换方法）。
3. **矩阵 (a) 一行。** 符号把六扇写全：`list, getInfo, add, edit, remove, changeStatus`。与 `*` 1:1。Lead 若按行标 covered，**不会**把 `SysOssController` / 直传 / 迁移假闭合。
4. **矩阵 (b) 一行。** 性状栏「切换默认存储」。listener / Redis 正门 / `OssFactory` 早退是这行的 explained 副作用，矩阵没有 `OssFactory.remove` 或 `OssConfigChangeListener` 独行。
5. **Goal 逐方法。** 默认档 Unistructural + explained。OBJ-25 的失败句在 B：公开桶写库前停、update 0 行整段回滚、`useDefault` 不拆钥匙串、已存 `sys_oss.service` 不搬家。

问：本课闭合标准是「(a) 六扇 HTTP/权限/动词 + (b) 切换副作用链（忽略 body `status`、只发 `useDefault`、不 put/evict 图纸墙）」就够，还是 OBJ-25 那句必须连 `OssFactory` 源码、readiness 定时、Vue 开关文案一起算进 `SysOssConfigController.*` 才能盖章？若是前者，Lead 标两行 covered 是否必须把 MinIO 容器、(d) 直传流、`createOssUploadClient` 留给我课，而不是把 L-021 的 `refreshCache` 或对象迁移塞进本行？

## Q2 证据

- objective_id: OBJ-25
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 36–120；ServiceImpl 254–275 / 121–175 / 200–234；Listener 32–54；Event 26–48；Mapper 19–34；空 XML；HttpContract / Governance 单测；OssFactory 34–72；OssConfigPage 96–127 / 414–427；resource-service 210–236
- difficulty: high

课的机制链哪些能在工作树直接复现，哪些只是测试名单比课表窄、或邻格事实？

**仍能复现（本格）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 前缀 `/resource/oss/config`；六扇：GET list / GET `{id}` / POST add / POST `/edit` / POST `/remove/{ids}` / POST `/changeStatus`；零 Put/Delete | Controller 35–120；`OssConfigHttpContractUnitTest` 扫全部方法 Put/Delete 为 null |
| classic：Controller → `ISysOssConfigService` → `SysOssConfigServiceImpl` 持有 Mapper；无 UseCase/DAO；XML 只有 namespace | 登记表 `wta-system` classic；模块内零 UseCase；`SysOssConfigMapper.xml` 5–7 行空壳 |
| `list` **和** `getInfo` 都是 `system:ossConfig:list`；`changeStatus` 挂 **edit**；`remove` 挂 remove | Controller 47–59 / 100 / 114 |
| 写窗都有 `@Log`。add/edit/changeStatus `isSaveRequestData=false`；remove 才走默认 `true` | Controller 73 / 87 / 101 / 115；`Log.isSaveRequestData()` default true；HttpContract `assertWrite` |
| add/edit/changeStatus `@RepeatSubmit()` 默认 5000ms；remove 没有 | Controller 74 / 88 / 116 vs 100–106；`RepeatSubmit.interval()` default 5000 |
| `changeStatus` **没有** `@Validated` / 分组；BO 上 `@NotBlank configKey` / `@Pattern status` 管不到这扇 | Controller 118 vs add 76 / edit 90；Bo 40–41 / 91–92 |
| 接口注释仍写「启用禁用状态」；实现忽略 `bo.status`，永远 `config.setStatus(YES)` | `ISysOssConfigService` 59–62；ServiceImpl 268–273 |
| `updateOssConfigStatus`：空 id → 停；`selectByIdForUpdate`；先 `validateAccessPolicy` 再 PRIVATE 闸；`clearOtherDefaultStatuses`；`updateById` ≠1 →「切换失败」；只发 `useDefault` | ServiceImpl 254–275；Mapper 19–20 / 33–34 |
| 公开行切默认：抛「默认OSS配置必须为PRIVATE」，源码顺序在 clear **之前** | ServiceImpl 263–267 vs 268 |
| 四段写路径都有 `@DSTransactional` | Governance `everyConfigMutationUsesDynamicDatasourceTransaction` |
| 监听器是 `@DsTxEventListener`，注解集里没有 Spring `TransactionalEventListener` | Listener 31–32；HttpContract `secretIsNotSerializedAndCacheListenerUsesDsAfterCommitHook` |
| `useDefault`：`defaultConfig=true`，json/oldKey 空；监听器写 Redis 正门后 **return**，不 put/evict `SYS_OSS_CONFIG`，不 `OssFactory.remove`；`finally` 仍 `readiness.refresh()` | Event 46–48；Listener 34–36 / 52–54 |
| 正门键 `global:sys_oss:default_config`；`setCacheObject(key,value)` 走 `bucket.set`，不带 Duration | `OssConstant.DEFAULT_CONFIG_KEY`；`GlobalConstants.GLOBAL_REDIS_KEY`；`RedisUtils` 137–164 |
| 图纸墙名 `sys_oss_config` 光板 → TTL=0 走 `createMap`；本地 Caffeine `expireAfterWrite(30s)` | `CacheNames` 82；`PlusSpringCacheManager` 179–180 / 261–266；`CacheConfig` 32–34 |
| `OssFactory.instance()` 每次现读 Redis 正门；`instance(configKey)` 读 JSON 墙；墙缺则「配置信息不存在」；`OssProperties` **无** `status` 字段 | OssFactory 34–53；OssProperties 11–57 |
| 未指定上传 `SysOssServiceImpl.upload` 走无参 `instance()` | `SysOssServiceImpl` 193 |
| `toAjax(int)`：`rows>0` 才 `R.ok()`；成功至少 `activated=1` | `BaseController` 19–20；ServiceImpl 274 |
| `init()` 不是 HTTP：默认个数 ≠1 直接抛；`ApplicationRunner` 先 init 再 readiness | ServiceImpl 55–70；`SystemApplicationRunner` 30–32 |
| VO `secretKey` `@JsonIgnore` | `SysOssConfigVo` 46–47；HttpContract |
| 旁路：add/edit 带 Y 会 clear + **save** ± useDefault；当前 Y 改 N 抛「请使用默认配置切换操作」；正门/内置四 id/引用不可删 | ServiceImpl 129–135 / 161–174 / 207–225；`OssConstant.SYSTEM_DATA_IDS` 四行 |
| 种子五行（minio/qiniu/aliyun/qcloud/image）；内置 id 只前四；`image` 可删。DDL `access_policy` 注释已是 0/2，**无** `config_key` 唯一索引、无 `del_flag` | `50` 348–352；DDL 327–348 |
| `NAMEWTA-OSS-ACCESS-DML-001`：默认个数 ≠1 则插两行同主键哨兵；随后把非 `0` 的 `access_policy` 打回 `0` | `50` 834–853 |
| 枚举只有 `PRIVATE(0)` / `PUBLIC_READ(2)`，没有 type=1 | `AccessPolicy` 18–28 |
| 前端 `changeStatus` POST body `{ ossConfigId, status, configKey }`；页开关无 `v-hasPermi`，改/删按钮有；「取消默认」文案仍调用同一枪；公开行 disabled + 拨回 N | `resource-service.ts` 235–236；`OssConfigPage.vue` 96–127 / 414–427 |
| e2e 认 changeStatus/edit/remove 都是 POST | `oss-config-access-policy.spec.ts` 164–168 |
| readiness 默认行标 `DEFAULT`；他节点定时默认 `PT1M` | `OssStorageReadinessService` 67–68；Scheduling 单测 26–27 |

**邻格仍能复现（不计入本行闭合，只证明课没把柜台讲成搬运/直传）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 厅堂 `createSystemService().resources = createSystemResourceService(http)`；页用 `runtime.service.resources.ossConfigs` | `service.ts` 186；`OssConfigPage.vue` 227–234 |
| 列表/详情把 `'0'`/`'2'` 投影成 `PRIVATE`/`PUBLIC_READ`；add/edit 再编码回去。`changeStatus` **不**走 `encodeOssConfig` | `resource-service.ts` 84–102 / 235–236 |
| 对象列表/下载/删除是另一扇 `/resource/oss`（L-026）；直传 init/sign/complete/abort（L-027）；迁移窗（L-028）；`createOssUploadClient`（L-029） | 各课符号；本 Controller 无这些 mapping |

**课写过、测试名单更窄或邻课才钉死（不是证伪）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 公开不能当默认的单测「钉死：**此时还没有** `clearOtherDefaultStatuses`」 | `referencedOrDefaultConfigCannotBeDeletedAndPublicCannotBecomeDefault` 抛的是 PRIVATE 文案，随后只 `verify never deleteByIds`。**源码顺序**确在 clear 前；该测试**没有** `never().clearOtherDefaultStatuses` |
| `defaultSwitchFailureAfterClearingDefaultsMustAbortTheTransaction`「必须整段 abort」 | 直接 `new SysOssConfigServiceImpl(mapper)`，证明 clear 之后 update 0 行会抛。真正回滚靠同类的 `@DSTransactional` 注解单测 + 动态数据源拦截器，不是这一则 mock 关掉了数据库 |
| e2e 当 POST 证据 | 夹具在收到 changeStatus 后把 `privateStatus` 拨成 **`'N'`**（108–111）。这是前端契约测试，**不是**服务端永远 Y 的规格。课正文已把永远 Y 写进 B |
| 「图纸墙 TTL=0、本地 30s」 | `CacheConfig` **注释**写「写入或访问」，**代码** `expireAfterWrite`。访问不续命。同一 caffeine 豆 L-021 已挖过，本窗主链不 clear 这组 |
| `OssProperties` 无 status | 字段表确实无 status。`accessPolicy` **注释**仍写 `0private 1public 2custom`，与运行时闸 0/2、枚举无 type=1 打架。课用的是 BO/枚举，没点这行过期注释 |
| 厨房单测 `changeStatus('oss/1', '0', 'primary')` | body 的 status 甚至是 `'0'`（像桶权限）。服务端仍只认主键。证明「忽略 status」比课的 Y/N 例子更狠，但是夹具不是产品窗 |

问：在六扇动词/权限、`changeStatus` 无分组校验、切换忽略 status、PRIVATE 闸在写库前、`useDefault` 早退只写 Redis、不拆 `OssFactory`、list 不读正门、前端「取消默认」仍 POST 同一枪——已经能互证的前提下，(a)(b) 是否还要把「公开单测没 verify clear」「abort 单测没套事务代理」「e2e 夹具拨 N」「OssProperties 过期注释」写成开篇半句，才算柜台讲完？还是这些已经落在课的表/反例里，Lead 可以直接盖章？

## Q3 假设与反例

- objective_id: OBJ-25
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 课反例 1–14；Controller/Service 注释「启用」；前端取消默认；useDefault 早退；SYSTEM_DATA_IDS；getInfo=list；POST 非 PUT；classic；init 修不了两个 Y
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪只柜子认错？

1. **「`changeStatus` 和用户/角色窗一样，传 `N` 就停用。」** 用户窗读 status。本窗忽略，永远 Y。课反例 1 / 误区 1–3 覆盖。接口 javadoc 仍写「启用禁用」，是源码自己的陷阱，课已点名。
2. **「取消默认会变成零个正门。」** 关当前开关 = 再指定当前行为正门。课反例 2 覆盖。页成功文案仍可能是「取消默认成功」。
3. **「PUBLIC_READ 也能当默认，前端灰掉就够。」** 服务端硬闸，失败在 clear 前。课反例 3 覆盖。
4. **「切换会 `CacheUtils.put` 新 JSON / `OssFactory.remove`。」** 那是 save/remove。`useDefault` 早退。课反例 4 / 误区 4–5 覆盖。
5. **「切换会把旧文件迁到新桶。」** `sys_oss.service` 不动。课反例 5。搬家是 L-028。
6. **「监听器用 `@TransactionalEventListener(AFTER_COMMIT)`。」** 磁盘 `@DsTxEventListener`。课反例 6。
7. **「`getInfo` 要 `system:ossConfig:query`。」** 源码 list。课反例 7。
8. **「改配置是 PUT，删配置是 DELETE，和字典一样。」** 写窗全 POST。课反例 8。对象窗 DELETE 是 L-026，不要并进本行。
9. **「这是 layered，切换应该写 UseCase。」** 登记表 classic。课反例 9。
10. **「`init` 失败还能先切一把再启动。」** 默认个数不对进程起不来。运行期 changeStatus 才是修理扳手。课反例 10。
11. **「body 带 `accessPolicy=0` 就能把公开行升成默认。」** 读的是 FOR UPDATE 行。课反例 11。
12. **「正门 Redis 和 `sys_oss_config` 是同一组 Spring Cache。」** 正门 `RedisUtils` 直写独立键。课反例 12。
13. **「内置四行不能被设为默认。」** 只禁删除。种子默认本来就是 minio 那行内置 id。课反例 13。
14. **「矩阵没有 export/refresh 就是漏了。」** 磁盘就六扇。课反例 14。不要按 L-021 九扇去补。
15. **「e2e 把 privateStatus 拨成 N，所以服务端取消了默认。」** 夹具不是 B 的规格。课没写成反例。
16. **「SQL 插入一行从未上墙的配置再 changeStatus，热路径立刻能上传。」** `useDefault` 不 put JSON。`instance()` 会「配置信息不存在」。课变式 B 只写了 SQL 改 **status** 没有事件，没单独钉「新行未 save/init」。
17. **「四页 / `createOssUploadClient` 也在 `SysOssConfigController.*` 里，因为 OBJ-25 写了 Controller。」** 课自己把对象/直传/迁移划走。矩阵 (a) 只有配置六扇。

问：读完后仍说不出的，是不是「六扇的名字」，而是 **`changeStatus` 不是启停、`useDefault` 不拆钥匙、取消默认仍会再写 Y、公开单测没把 clear 钉成 never、SQL 新行未上墙时指针和图纸会分家**？

## Q4 视角

- objective_id: OBJ-25
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-25 vs L-019/L-021/L-026/L-027/L-028/L-029/L-083/L-085；classic 登记表；OssFactory 无参/有参
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:SysOssConfigController.*` 或 `B:updateOssConfigStatus`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 六扇全表 vs OBJ-25 只点切换；(b) 已有切换行；(c) MinIO/OSS 未覆盖；(d) 直传流未覆盖 | **是** 闭合定义。两行可单独 covered。不要把直传/MinIO 并进本行 |
| 柜台作者（A） | 六扇动词/权限/POST/`@Log`/`@RepeatSubmit`；getInfo=list；changeStatus 无分组 | **是** (a) |
| 换正门作者（B） | 忽略 status、FOR UPDATE、PRIVATE、clear+Y、失败回滚、只 useDefault | **是** (b)。listener/Redis/工厂早退是 B 的副作用，不必发明第二行 (b) |
| 钥匙串 / 图纸墙 | `OssFactory.CLIENT_CACHE`；`SYS_OSS_CONFIG` 集群作废 | 早退「不动」**是** B。`CacheUtils.clear` 扫帚、Caffeine 访问续命 **否**（L-021） |
| 对象窗（L-026） | `sys_oss.service` 名牌、list/downloadUrl/remove | 不搬家一句 **是**。对象 HTTP **否** |
| 直传（L-027） | init/sign/complete/abort；(d) 主/失败路径 | **否** |
| 迁移（L-028） | dryRun/start/retry/rollback | **否**。本课只说切换不搬家 |
| 浏览器 adapter（L-029） | `createOssUploadClient.upload` | **否** |
| 厅堂电工（L-019） | `createSystemService.resources.ossConfigs` | 插头一句 **是**。resources 全表 **否** |
| 参数抽屉（L-021） | `refreshCache` DELETE + 集群回执 | 对照「本窗没有那扇」**是**。扫帚实现 **否** |
| 启动器 | `init()` + 两个 Y 起不来 | 负空间 / 修理扳手对照 **是**。不是 HTTP 窗，不进 (a) 点名表 |
| MySQL 基座（L-085） | 表列、无唯一索引、无逻辑删、ACCESS DML 哨兵 | 本课用到的列/前置一句 **是**。容器标 covered **否** |
| Redis 8 / L-084 | 正门键无 TTL | 键名/无 TTL **是** B。会话容器 **否** |
| dao-mapper（L-083） | 空 XML、注解 SQL、`BaseMapperPlus` | 持有关系一句 **是**。基类课 **否** |
| API-005 | 查询 GET、变更 POST、POST `@Log` | 盘点现状 **是**。不发动传输 Ratchet **否** |

问：Lead 若凭「六扇都在函数表、切换方法在 B 行」把两格标 covered，是否还要求读者能指着 **忽略 body status、useDefault 早退、不搬家、公开闸在写库前**？这些是不是已经在本格函数表/反例里，而不需要把 OssFactory 升成新 (a) 行、也不需要把 MinIO 容器算进来？

## Q5 元问题

- objective_id: OBJ-25
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 六扇一行、(b) 切换一行
- difficulty: low

建议：`defer`

原因：

- **`(a)` 与磁盘六扇 1:1，`(b)` 与 `updateOssConfigStatus` 1:1。** L 合同章节齐全（直觉、定义、机制链 1–5、带 caption / 文字等价物 / 图边界的两张图、正反例、变式、误区、来源表）。**主叙事站得住且与源码对齐**：`status` 不是启停；`changeStatus` 只认主键、永远 Y、只发 `useDefault`；公开桶写库前停；clear 与点亮同一 `@DSTransactional`；监听器提交后早退只写 Redis 正门；不 put/evict 图纸墙、不 `OssFactory.remove`、不改 `sys_oss.service`；list 读表不读正门；无 export/refresh；classic 无 UseCase。不必为「窗口名字」再挖五问。
- 本批可粘贴的精度项（公开单测没 verify never-clear；abort 单测没套事务代理；e2e 夹具把 status 拨 N；`OssProperties` 注释仍写 type=1；SQL 新行未上墙时 `instance()` 会找不到 JSON；监听器 Redis 失败则表已提交、热路径仍信 Redis、readiness 信表）落在测试夹具、过期注释或课已写的「SQL 无事件 / 监听器失败」边界上。它们不是第二容器，也不是「说不出六扇目的或切换失败路径」。可选一行补丁不挡住把**两行**标 covered。不要把对象窗、直传、迁移、`createOssUploadClient`、L-021 扫帚、MinIO 容器塞进柜台格。
- 不建议 `mine-more`：b01 未证明本行还有真实矩阵空洞；再五问只会把 L-026 对象名牌、L-027 票据或 Vue 投影再问一遍。不建议 `re-dispatch-L`：不缺动机/机制/边界/例子；没有把源码讲反。不建议 `split-lesson`：未满 10 问；A+B 是 chain 原设计（窗 + 切换枢纽），不是「一课两个 C4 却讲不完」；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明第二行 (b)：listener / 工厂早退已经是本行 (b) 的 explained 性状。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:SysOssConfigController.* | 六扇路径/动词/权限、POST+`@Log`、getInfo=list、changeStatus 无分组校验、无 export/refresh、classic 空 XML——均与源码对齐。残留精度：接口/方法 javadoc 仍写「启用」；e2e 夹具把 changeStatus 模拟成 status=`N`。 | 可选半句：口试以磁盘实现为准，不以 javadoc「启用禁用」或 e2e mock 拨 N 为准。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-025 来源表 S-L025-01/06/08）。 | defer（本行可 covered；精度项不挡盖章） |
| B:ISysOssConfigService.updateOssConfigStatus | 忽略 body status、FOR UPDATE、PRIVATE 闸在 clear 前、失败抛错、只发 useDefault、早退不拆钥匙、不搬家——均与源码对齐。残留精度：（1）公开单测未 `verify never clear`；（2）abort 单测未套 `@DSTransactional` 代理；（3）SQL 插入从未上墙的行再切，Redis 指针换了但 `instance()` 仍要 JSON 墙；（4）监听器 Redis 写失败：库已提交，热路径信 Redis，readiness `finally` 信表。 | 可选半句：修理扳手假定目标行已经 init/save 上墙；验收热路径用无参 `OssFactory.instance()` 的 clientId，不要用 e2e 夹具的 N。不要求重写图 2。Lead 可将本行标 `covered`（S-L025-02/03/05/06）。 | defer（本行可 covered；精度项不挡盖章） |
| A:SysOssController.list,listByIds,downloadUrl,remove | 本课只钉 `sys_oss.service` 不随正门搬家。对象 HTTP 未讲。 | 保持。L-026。 | defer（L-026） |
| A:SysOssUploadController.init,signParts,parts,complete,abort / D:OSS 直传 | 未讲票据。 | 保持。 | defer（L-027） |
| A:SysOssMigrationController.* | 真搬家。 | 保持。 | defer（L-028） |
| A:createOssUploadClient.upload | 浏览器 adapter。 | 保持。 | defer（L-029） |
| A:createSystemService.resources / ossConfigs | 插头 `resources.ossConfigs` 已借用；页投影 0/2↔枚举名。 | 保持。全表 L-019；页细节不必为本课建行。 | defer（L-019 / 未入库前端格） |
| A:SysConfigController.refreshCache | 课已否「把扫帚打到 OSS 配置」。 | 保持。 | defer（L-021） |
| C:MinIO/OSS 容器 | 类比用了仓库，未验收进程/桶内部。 | 不要因本课标 covered。 | defer（(c) 容器；后续 OSS 课） |
| D:主路径 OSS 直传 / 失败 abort | 矩阵仍 uncovered。 | 保持。 | defer（L-027, L-029） |
| Redis 8 / L-084 | 正门键名与无 TTL 已点名。 | 失败句已够。 | defer（L-084） |
| MySQL 8.4 / mysql-base / L-085 | 表列、无唯一索引、ACCESS DML 哨兵已点名。 | 保持。 | defer（L-085） |
| C:dao-mapper-ownership / L-083 | 空 XML + 注解 SQL 已点名。 | 保持。 | defer（L-083） |
| init() / SystemApplicationRunner | 非 HTTP；两个 Y 起不来、运行期扳手已讲。 | 不要塞进 (a) 点名六扇。 | defer（启动器；本格负空间已写） |
| (b) OssFactory.remove / listener 独行 | 矩阵无此行。课已把早退写进 (b)。 | 不要为本课发明第二行 (b)。 | defer（未入库） |
| API-005 变更 POST | 本窗写操作已是 POST+`@Log`。 | 本课盘点，不发动 Ratchet。 | defer（传输 Ratchet） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；两行无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出六扇目的、切换主路径（指定唯一 Y + Redis 正门）与失败路径（空 id、公开桶、update 0 行回滚、useDefault 不拆钥匙）；把可选半句写进原课不会超过 40 分钟；对象/直传/迁移/adapter、L-021 扫帚、基座容器已有后续课。满 10 问前不拆。A 与 B 虽是 Controller 与枢纽方法，仍是 chain 原设计的同一 OBJ-25，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-026 / L-027 / L-028 / L-029 / L-019 resources / L-021 refreshCache / MinIO 容器 / 直传数据流 / L-084 / L-085 / L-083 / 启动器 init / 未入库第二行 (b) / API-005 显式建议 `defer`；对 `A:SysOssConfigController.*` 与 `B:ISysOssConfigService.updateOssConfigStatus` 建议 Lead 标 `covered`（证据锚本 probe 与 L-025 来源表）。本课不主张改对象窗、直传、迁移、浏览器 adapter、MinIO 容器或任何 (c)(d) 格子。
