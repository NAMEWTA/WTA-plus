# T-41 F candidate — independent implementation-axis review

## Fixed scope and conclusion

F source `cb8063b686fcd82d0a91064061e674a59fb6fc14`, tree `2ee339a27589ea393baca0d4131f598981f5abae`; compared with E `a0dcbac8fd33a754c74c154551f7090a8878fb48`. **The two static selector changes are narrower, but F's actual 501 browser gate failed; no overall T-41 pass.** `git diff E...F -- frontend backend` contains only `frontend/e2e/inbox-paged-real.e2e.ts`. No production Java, Vue, TS application, generated contract, browser runner or Python safety-test input changed. I did not run repository tests, builds, services or a browser.

## Locator correction preserves the assertion

E's owned real browser run `/tmp/wta-t41/browser-runs/969f43a52216aebb/result.json` reached the A top message, real detail GET200 and detail body, then failed at `inbox-paged-real.e2e.ts:90` on the dialog's `关闭` button selector. F changes only that selector and the corresponding later full-page detail selector from `getByRole('button', { name: '关闭' })` to `getByRole('button', { name: '关闭', exact: true })` (current lines 90 and 121). Both dialogs have a real footer `<el-button>关闭</el-button>` in `frontend/apps/admin-web/src/layout/components/notice/index.vue:54-57` and `frontend/packages/web-domains/notify/src/InboxPage.vue:71-75`. Exact accessible-name matching targets that button more narrowly; it does not remove the click, replace real UI with a mock, relax the preceding GET/detail assertions, or skip the page-26/read-all/B-isolation checks. The E sanitized result proves the failure location, not the precise DOM ambiguity; F still needs real execution to establish that this locator resolves it.

## Evidence reuse boundaries

- `/tmp/wta-t41/f-source-equivalence.json` records D→E only two Python fixture files and E→F only this Playwright spec. I independently checked C→F frontend changed paths: the D registry test, E runner/test, and F spec only. I independently rehashed the frozen C production-dist manifest and all current 328 outputs; all file hashes/sizes and exhaustive inventory match (Admin 310, Home 14, SSO 4). This proves production dist equivalence, not a new F frontend build.
- The separate **E** API-controlled five-case Chrome run `/tmp/wta-t41/api-controlled-runs-v2/2fe330f006dff4ae/result.json` is an actual pass: five expected, zero unexpected/skipped/flaky, one attempt each, source E clean before/after, exact C production-dist proof before/after, process groups/port cleared and `cleanup.errors=[]`. The controlled five-case spec, runner, preview config and production dist are unchanged in F. It is valid unchanged-input regression evidence attributed to E and reused for F; it is **not** relabelled as an F execution. No preview v3 is needed solely for F's distinct 501-browser selector change.
- E's 24 Python offline safety tests, D's backend/default/owned HTTP/shared-notify results and C production builds likewise retain their original source/run attribution. Their unmodified executable inputs are identified in `/tmp/wta-t41/f-source-equivalence.json`; this review does not report them as fresh F runs.
- I checked the Lead's fixed F `fcore-*` records: core package/bundle, skill facts, fullstack facts, layered notification, FM and handbook checks each report exit 0 with F source before/after, and `f-openapi-check.json` reports exit 0. These passing gates do not override the required browser failure below.

## F real browser result and remaining selector audit

The E real 501 case was `acceptance=false`, source clean and cleanup clean. Lead's F run `/tmp/wta-t41/browser-runs/62047a08743c9ffa/result.json` also reports **exit1/acceptance=false**, one Chrome case unexpected, zero skip/flaky, failure at `inbox-paged-real.e2e.ts:112:54` (`b.getByText(manifest.shared.title).toBeVisible()`). The run's six-SQL initializer exited 0; seed A501/unread481 and B2/unread2 passed. The exact F source/tree and full JAR SHA `486989a5…` matched before/after, all owned containers/anonymous volumes/process groups/ports were cleared, `cleanup.errors=[]`. Earlier real A login, top detail, page 26/oldest/negative B-only and B login/unread2/two rows were reached before this locator. The failed run did **not** reach full-detail/read-all/B-after-refresh/preserved-time checks.

The fixture at `run-inbox-paged-real.py:466-469` sets each notification's `title`, `message` and `content` to the same generated text. `InboxPage.vue:22` renders `title`, and line 29 renders `message` as a separate summary cell in the same table row. Thus the page-level positive `getByText(manifest.shared.title)` at spec line 112 and the immediately following `getByText(manifest.bOnly.title)` at line 113 are deterministically susceptible to multiple matching elements; the sanitized reporter does not preserve the raw Playwright ambiguity message, so the exact failure class at line 112 remains an inference from source. Repair both with row-scoped, single-row assertions. Since the two B relationships share the fixture timestamp and the public order is message ID descending, checking the title cell of row 0 equals `shared.title` and row 1 equals `bOnly.title` would preserve and strengthen the sorting/isolation assertion. A unique row filter plus `toHaveCount(1)` is also valid; `.first()` on a page-wide text locator would hide ambiguity and should not be used.

I checked the remaining selectors against current markup. The negative `toHaveCount(0)` checks at lines 106/114 are absence checks and cannot fail from duplicate positive title/summary cells. The `oldest` row is scoped before its detail button at lines 104/119, and both dialog close buttons now use an exact name. The page header unread counts and current page's `全部已读`/`刷新` buttons are presently single elements in `InboxPage.vue:5-12`; scoping the two action buttons to the card header would make the test more robust if the shell later introduces same-named controls, but no current deterministic duplicate is established for them. The B positive title checks are the clear remaining static ambiguity. A subsequent fixed candidate must preserve its real page-26, cross-user, full-read, timestamp and zero-push checks, then pass the full owned browser gate. No final ticket pass or archive claim follows from F.
