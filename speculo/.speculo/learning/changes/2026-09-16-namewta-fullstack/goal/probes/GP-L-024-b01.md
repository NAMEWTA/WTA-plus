---
artifact: learning-goal-probe
probe_id: GP-L-024-b01
lesson_id: L-024
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-024 / b01

审问对象是已写成的 Lesson `lessons/L-024-monitor-frontend.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain / `claimed_cells`）：`A:createMonitorService`、`A:system web-domain monitor pages`。OBJ-24：能说明 `createMonitorService` 与 web-domain monitor 页面。矩阵 (a) **只有** `createMonitorService` 一行（`domains/system/monitor`，仍 uncovered）。矩阵 (a) 九工厂行是 `createAdminWebDomain, createSystemWebDomain, …`，**没有** `createMonitorWebDomain`。矩阵 (b) **没有** monitor 工厂/四页/Intent 行。本批按派单审问 **`A:createMonitorService`**；第二格只作邻格对照，不把四页塞进本行才能标 covered。Java 四窗留给 L-023；`createSystemWebDomain` 15 键留给 L-020；厅堂 `services.ts` 组合点留给 L-007；通知监控留给 L-049；OpenAPI 厨房留给 L-033；iframe / `wta-extend` 留给范围外 defer。

抽查锚点（2026-09-16）：`frontend/packages/domains/system/src/monitor/{index.ts,index.test.ts,transport.ts,transport.test.ts,types.ts,cache,login-info,online,operlog}/index.ts`；`domains/system/{package.json,src/index.ts}`；`frontend/apps/admin-web/src/application/services.ts`；`frontend/apps/home-web/src/application/services.ts`；`frontend/packages/web-domains/system/src/monitor/{index.ts,index.test.ts,runtime.ts,pages.ts}` 与四份 Vue；`web-domains/system/{package.json,src/index.ts,src/pages.ts}`；`frontend/apps/admin-web/src/router/{adminManifestRegistry.ts,adminManifestRegistry.test.ts}`；`views/monitor/external/index.vue`；`views/system/user/profile/{index,onlineDevice}.vue`；四份 Java `controller/monitor/*`；`adapters/axios-browser` `downloadWithAxios`。

## Q1 澄清

- objective_id: OBJ-24
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-24；chain L-024 覆盖列；L-024 `claimed_cells`；矩阵 (a) `createMonitorService` 独行；九工厂行无 `createMonitorWebDomain`；矩阵 (b) 无本课符号
- difficulty: medium

课把保安亭收成「厨房 + 四块屏」。计划里至少有四套可数对象，派单只点名厨房这一格：

1. **OBJ-24 原文。** `course.md`：能说明 `createMonitorService` 与 web-domain monitor 页面。一句里两件东西，没有把 Intent 尺、导出旁路、iframe 写成口试清单。
2. **chain / 课 `claimed_cells`。** 两格：`A:createMonitorService`（子路径工厂、四抽屉、两把不发 HTTP 的尺）+ `A:system web-domain monitor pages`（`createMonitorWebDomain`、四键四页、`web-domain-system-monitor`）。
3. **矩阵 (a) 一行。** 符号就是 `createMonitorService`，模块 `domains/system/monitor`。与厨房 1:1。Lead 若按行标 covered，**不会**把另外八个 web-domain 工厂假闭合——那些在另一行，且该行点名的是 `createSystemWebDomain` 不是本课第二工厂。
4. **矩阵没有第二格。** chain 的 `system web-domain monitor pages` / 磁盘符号 `createMonitorWebDomain` 都不在 (a) 九工厂行，也不在 (b)。课已经把两格拆开写；计划还没给四页一张可盖章的格子。
5. **Goal 逐方法。** 厨房公开 HTTP 12 枪（`cache.get`；`loginInfo.list/delete/unlock/clean`；`online.list/forceLogout/current/removeCurrent`；`operationLogs.list/delete/clean`）+ 两把 Intent。课口试口径是「按抽屉看形状 + 例外，不默写测试每一行」。`binds the complete monitor surface` 只钉了 **10** 行 `METHOD url`，缺的两枪在 encoding 用例。

问：本格 (a) 闭合标准是「子路径工厂四抽屉 URL/动词 + 两尺失败关闭 + 投影/空页例外」，还是 OBJ-24 那句必须连四页 runtime 一起算进 `createMonitorService` 才能盖章？若是前者，Lead 标本行 covered 是否必须先给 `createMonitorWebDomain` 单独立行（或诚实把第二格留 uncovered），而不是把四页塞进九工厂行、也不是把 iframe / 资料页设备列表算进本行？

## Q2 证据

- objective_id: OBJ-24
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: `monitor/index.ts` 39–162；`index.test.ts` 5–128；`transport.ts` 6–34；`types.ts` 32–43 / 115–128；`package.json` `./monitor`；根 `src/index.ts` 无 re-export；admin `services.ts` 8 / 18–20 / 89；home `services.ts` 无 monitor；四份 Java 映射；四页 Vue 只作邻格
- difficulty: high

课的厨房机制链哪些能在工作树直接复现，哪些只是测试名单比课表窄、或邻格事实？

**仍能复现（本格）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂 `createMonitorService(http: HttpClient)`，只认 `http`，返回 `Object.freeze` 的 `MonitorService`；四个抽屉各自再 freeze | `index.ts` 104–110 / 81–102 |
| 必须走子路径 `@namewta/domain-system/monitor`。根 `src/index.ts` 只导出 `createSystemService` / `createOpenApiService` 等，搜不到本工厂 | `package.json` `"./monitor"`；根 `index.ts` 1–49；全文件无 `createMonitorService` |
| **没有** `@namewta/domain-monitor` 包；**没有** `createSystemService.monitor` | 仓库无该包名；`service.ts` 八口不含 monitor |
| 厅堂 `domainHttp` 懒电线后 `export const monitorService = createMonitorService(domainHttp)` | admin `services.ts` 8 / 18–20 / 89 |
| home-web **整仓**无 `monitor` / `createMonitorService` | `home-web` grep 空；`services.ts` 只有 system / identity / profile |
| `segment`：单值或数组 → `encodeURIComponent(String(item))` → 逗号连接。`'a/b', 2` → `a%2Fb,2`；`token/value` → `token%2Fvalue` | `index.ts` 45–46；`index.test.ts` 5–14 |
| 监控枪 `request({ url, method, params? })`，**无 `data` 体**，`method` 小写 | `index.ts` 110–145 |
| `cache.get` → **GET** `/monitor/cache`，无 params | 110；Java `CacheController` `@GetMapping()` |
| `loginInfo` 前缀 `/monitor/loginInfo`：list GET `/list` + params；delete DELETE `/{ids}`；unlock **GET** `/unlock/{names}`；clean DELETE `/clean` | 111–118；Java `SysLoginInfoController` 同动词；unlock 仍 `@GetMapping` |
| `online` 前缀 `/monitor/online`：list GET `/list` + params；forceLogout DELETE `/{tokenId}`；current GET 根路径；removeCurrent DELETE `/myself/{tokenId}` | 119–126；Java `list` / `forceLogout` / `getInfo` / `remove` |
| `current` 厨房不查权限。Java `GET /monitor/online` **无** `@SaCheckPermission`（list 才要 `monitor:online:list`） | Java 102–118 vs 45–47 |
| `removeCurrent` 只踢当前登录者自己的 token（Java 先 `getTokenValueListByLoginId` 再 filter）；监控页强退走 `forceLogout` | Java 128–136；`OnlinePage` 只用 `list` + `forceLogout`；资料页 `removeCurrent` |
| `operationLogs` 前缀 `/monitor/operlog`：list GET `/list` **先投影再返回**；delete / clean DELETE | 127–145；Java `SysOperlogController` |
| 投影丢掉 `serverOnly`；缺的 `businessType`/`requestMethod`/`status`/`costTime` 补 0 或 `''`；`tenantId` **恒** `''` | `transport.ts` 6–34；`transport.test.ts` 5–26 |
| 空页：`data` 缺 rows → `[]`；`total` 非 number → 0；连 `data` 都没有（`{ code: 204 }`）仍回 `{ rows: [], total: 0 }` 并保留其它元数据。另外三个 list **没有**这层衣服 | `index.ts` 128–141；`index.test.ts` 117–128 |
| 工厂 **没有** `export()` / `batchLogout`。后端 export 是 `@PostMapping("/export")`；`batchLogout` 只在菜单 SQL + 权限目录 | 工厂方法表；Java loginInfo/operlog export；`50-cde-base-dml.sql` 1761400000000001047；全仓无批量踢人窗 |
| 两把尺不发 HTTP。`allowed === false` 先抛 `MonitorSecurityError('permission-denied')`，测试 `/无权/`；缺 URL `missing-url`「未配置运维入口地址」 | 106–108 / 50–51 / 146–158；`index.test.ts` 82–90 |
| `safeUrl` 拒绝非 http(s)、协议相对、带 userinfo、空白/反斜杠/控制字符/残缺百分号、根相对以 `//` 开头；根相对用假 origin `https://monitor.invalid` 丢掉 `..` | 47–79；`index.test.ts` 44–80 |
| `monitorPermissions.nacos === 'system:nacos:console'`，不是 `monitor:nacos:list` | `index.ts` 39–44 / 162；测试 92–102 |
| `externalIntent` 的 `allowed` 由**调用方**传入，工厂不读 `Admin-Token`、不 `hasPermission` | 146–148；产品入口 `views/monitor/external/index.vue` 24–27 |
| `attachmentIntent`：`target: 'notify-attachment'`，`mode: 'download'`。生产调用点目前只有测试；runtime 仍准备了 `openDownload` | 150–158；grep 仅 `index.ts` + `index.test.ts` |
| 资源锚点 `systemMonitor*Resource` 冻 `{ controller, basePath }`，不是 HTTP 客户端 | `cache/login-info/online/operlog/index.ts` |
| `capabilities` 有 `monitor-cache` 等四个名字，在根 `systemDomainModule`，**不是**本工厂字段、也不是 `componentKey` | 根 `index.ts` 150–153 |
| `types.ts` 的 `NotifyQuery` / `Notify*VO` 被 `export * from './types'` 带出；工厂实现 **不引用** | `types.ts` 115–185；`index.ts` import 列表 5–17 |

**邻格仍能复现（不计入本行闭合，只证明课没把厨房讲成屏）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 第二工厂 `createMonitorWebDomain`，id `web-domain-system-monitor`，`domainId: 'system'`，四键顺序 online/cache/operlog/logininfo | `web-domains/system/src/monitor/index.ts` 10–45；测试 20–25 |
| 包根导出工厂；`package.json` 有 `./monitor/cache` 等四页、**没有** `./monitor` 指向工厂；根 `src/pages.ts` 不含监控 | `src/index.ts` 14–19；`package.json` 13–16；`src/pages.ts` |
| 四页只认 `runtime.service.*`。Online 本地 `slice`、pagination **无** `@pagination="getList"`；LoginInfo/Operlog `@pagination="getList"` + `runtime.download('monitor/loginInfo/export' \| 'monitor/operlog/export')` | 三份 Vue；`downloadWithAxios` `client.post` + `x-www-form-urlencoded` + blob |
| Cache 页无 `v-hasPermi`；强退文案「删除成功」；`onBeforeUnmount` `dispose` | `CachePage.vue`；`OnlinePage.vue` 148–150 / 230–232 |
| 权限目录含 `monitor:online:batchLogout`；四页无批量钮 | `monitor/index.ts` 37–38 vs Vue |
| `selectedManifestIds` 含 `web-domain-system-monitor` 与 `admin-external-monitor`；registry：online→`Online`，admin→`MonitorAdmin`，`notify/monitor`→`NotificationMonitor`，`monitor/notify`/`monitor/report` 为 `undefined` | `adminManifestRegistry.ts` 366–377；测试 113–128 |
| 资料页直接 `import { monitorService }`，走 `current` / `removeCurrent` | `profile/index.vue` 139；`onlineDevice.vue` 32 / 48 |

**课写过、测试名单更窄或邻课才钉死（不是证伪）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 暂停「把 complete monitor surface 那 10 行读一遍」 | 10 行缺 `loginInfo.delete`、`online.removeCurrent`。两枪在 encoding 用例，工厂表有。口试若把 10 行当成 HTTP 全集，会漏删登录日志和「踢自己」 |
| `safeUrl` 拒绝清单 | `it.each` 还锁了 `https://example.test:99999/x`（非法端口）和残缺 IPv6 `https://[2001:db8::1/x`。代码另拒 `parsed.password`。课列的是主形状，不是夹具全集 |
| `unlock(userNames: string \| readonly string[])` | 页把 `selectName`（`userName[]`）整袋传入；按钮 `:disabled="single"`，点下去是长度 1 的数组。Java `@PathVariable userName` **不按逗号拆**。工厂类型看起来能批量，接线只保证单名 |
| `online.list` + 本地切片 | 厨房仍把 `pageNum`/`pageSize` 放进 **params** 打出去。Java `list(ipaddr, userName)` 忽略分页、整袋 `PageResult.build`。不打第二枪的是页上缺 `@pagination`，不是厨房省略 params |
| `LoginInfoVO` 没有 `clientKey`/`deviceType`，页上有这两列 | 厨房 `loginInfo.list` **不投影**，多出来的 JSON 字段会活到表格。只有 `operationLogs.list` 会丢掉多余字段。类型缺口在 `types.ts`，是本模块合同，不只是页的事 |

问：在子路径、四抽屉 URL/动词、unlock 仍是 GET、operlog 投影+空页、两尺不发 HTTP、无 export/batchLogout 已经能互证的前提下，(a) 是否还要把「surface 测试 10≠12」「unlock 类型像批量」「list 透传 vs 投影」写成开篇半句，才算厨房讲完？还是这些已经落在课的表/反例里，Lead 可以直接盖章？

## Q3 假设与反例

- objective_id: OBJ-24
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 根入口无工厂；`systemService.monitor` 不存在；surface 10 行；`operationLogs` 字段名；unlock GET；`batchLogout` 无枪；Intent 不 fetch；Nacos 权限串；home 无雇；Notify* 残留；资料页 ≠ OnlinePage
- difficulty: high

课的隐含假设若反转，合格读者会把哪把枪或哪座包认错？

1. **「`createMonitorService` 从 `@namewta/domain-system` 根入口来 / 是 `systemService.monitor`。」** 根不导出；厅堂并列工厂。课反例 1 / 误区 1 覆盖。
2. **「有独立的 `@namewta/domain-monitor` 包。」** 没有。课开篇包边界表覆盖。
3. **「工厂字段叫 `operlog` / `logininfo`。」** 叫 `operationLogs` / `loginInfo`。URL 才是 `/monitor/operlog`、`/monitor/loginInfo`。课反例 3 / 误区 5 覆盖。
4. **「导出也是 `service.loginInfo.export`。」** 工厂无此方法。页走 `runtime.download`，axios **POST** blob。课正例 3 / 误区 6 覆盖。属邻格吃法，本格负空间成立。
5. **「`unlock` 已改成 POST，因为 API-005。」** 仍是 GET。课误区 14 覆盖。存量，本课不改合同。
6. **「`monitor:online:batchLogout` 能批量踢人。」** 目录有，工厂无，Java 无，四页无钮。课反例 5 覆盖。
7. **「`externalIntent` 会帮你 fetch 运维控制台。」** 只返回安全 URL。课误区 12 覆盖。iframe 页是对照门，不是本行产品页。
8. **「Nacos 权限是 `monitor:nacos:list`。」** 磁盘 `system:nacos:console`。课误区 13 覆盖。
9. **「`current` 也要 `monitor:online:list`。」** Java 根 GET 无该注解；资料页用它。课机制例外覆盖。
10. **「资料页在线设备就是 `OnlinePage`。」** 资料页 import 厅堂单例，走 `current`/`removeCurrent`。课反例 8 覆盖。
11. **「home-web 只要菜单种子带监控键就能开缓存图。」** 门户不创建本工厂、不选 monitor manifest。课反例 10 覆盖。home 源码零命中。
12. **「`types.ts` 里的 Notify* 说明本工厂管站内信。」** 残留形状。课反例 7 覆盖。
13. **「`capabilities` 的 `monitor-online` = 本工厂能 list。」** 能力清单不是 HTTP 客户端。课反例 9 覆盖。拼写还多一刀：能力名 `monitor-login-info`，权限组 id `monitor-logininfo`。
14. **「complete surface 的 10 行 = 工厂 HTTP 全集。」** 漏 delete 登录日志、漏 `removeCurrent`。课在机制段补了表，暂停仍说「10 行」。读者若只背测试标题会漏两枪。
15. **「`unlock` 传数组 = Java 按逗号拆多名。」** 类型允许数组；窗是单个 `{userName}`。页用 `:disabled="single"` 把风险按住。课没写成反例。
16. **「四页也在 `createMonitorService` 格子里，因为 OBJ-24 写了一句。」** 课自己拆成两格。矩阵只有厨房行。把假设反转后，Lead 会用本行假闭合 `createMonitorWebDomain`。

问：读完后仍说不出的，是不是「工厂的名字」，而是 **本行不含四页、surface 10 行不是 12 枪、unlock 看起来像批量、loginInfo.list 透传所以表格能显示 VO 没有的列**？

## Q4 视角

- objective_id: OBJ-24
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)；OBJ-24 vs L-007/L-020/L-023/L-033/L-049；admin-external-monitor；资料页；API-005；九工厂行
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:createMonitorService`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 厨房独行 vs chain 两格；四页符号未入库；九工厂行不含 `createMonitorWebDomain`；(b) 无 Intent/投影行 | **是** 闭合定义。厨房行可单独 covered。四页不要塞进本行或九工厂行 |
| 厨房作者 | 12 HTTP + 2 Intent；segment；operlog 投影/空页；unlock GET；无 export | **是** (a) 逐方法。Java Redis/踢人语义 **否**（L-023） |
| 四页作者 | 四键、`runtimeView`、本地切片、`runtime.download` POST blob、目录宽于按钮 | **否** 本行。chain 第二格 / 磁盘 `createMonitorWebDomain`。本课已讲，矩阵尚未建行 |
| 厅堂电工（L-007） | `domainHttp` 懒电线、单例、`selectedManifestIds` 十项 | 插头一句 **是**。11 个 `create*` 全表 **否** |
| 导航 host（L-020） | 15 键不含 `monitor/...`；`inferDomainId` 得 `'monitor'`，manifest.domainId 仍 `'system'`，查找只认键 | 对照一句 **是**。15 键/恢复链 **否** |
| 值班室 Java（L-023） | 四份 Controller；`GET /online` 无 list 权限；unlock 擦 `PWD_ERR_CNT_KEY`；list 忽略分页整袋 | URL/动词对齐 **是**。Redis/`StpUtil` 实现 **否** |
| iframe 运维 | `admin-external-monitor` + `VITE_APP_*` + `externalIntent` | 尺的失败关闭 **是** 本格。四 iframe 页/进程内部 **否** |
| 资料页（L-022 邻） | `current` / `removeCurrent` 厅堂单例 | 两枪在工厂方法表 **是**。不要把 Vue 算进四页格 |
| 通知监控（L-049） | `notify/monitor/index`、`/notify/monitor` | **否**。负空间一句即可 |
| OpenAPI 厨房（L-033） | 同包另一工厂，根入口导出 | **否**。对照「根有 OpenAPI、没有 monitor」 |
| API-005 | 变更目标 POST；本厨房 DELETE + unlock GET + 导出 POST | 盘点现状 **是**。不发动传输 Ratchet **否** |
| Redis 8 / Sa-Token | 在线名单、踢人、错次键 | **否**（L-023 / L-084） |

问：Lead 若凭「子路径工厂四抽屉都在函数表里」把 `createMonitorService` 标 covered，是否还要求读者能指着 **两尺不发 HTTP、operlog 才投影、工厂没有 export、current≠forceLogout**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要把四页算进来？

## Q5 元问题

- objective_id: OBJ-24
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 厨房独行、无 pages 行、无 (b)
- difficulty: low

建议：`defer`

原因：

- **`A:createMonitorService` 与矩阵 (a) 该行 1:1。** L 合同章节齐全（直觉、定义、机制链 1–4、带 caption / 文字等价物 / 图边界的两张图、正反例、变式、误区、来源表）。**主叙事站得住且与源码对齐**：子路径工厂不是 `systemService` 字段；四抽屉 URL/动词与 Java 窗对得上；unlock 仍是 GET；`operationLogs.list` 投影+空页，其它 list 透传；两把 Intent 失败关闭且不发 HTTP；无 export / batchLogout；freeze；home 不雇；Notify* 残留无调用点。不必为「工厂名字」再挖五问。
- 本批可粘贴的精度项（surface 测试 10≠12、`safeUrl` 夹具比课表多非法端口/残缺 IPv6、unlock 类型像批量但 Java 不拆、online.list 仍带 page params）已经落在课的表、encoding 用例或邻格。它们不是第二容器，也不是「说不出四抽屉目的」。可选一行补丁不挡住把**本行**标 covered。不要把四页 runtime、iframe 四键、Java Redis、`createSystemWebDomain` 15 键塞进厨房格。
- 不建议 `mine-more`：b01 未证明本行还有真实矩阵空洞；再五问只会把四页、L-023 窗内实现或 iframe 再问一遍。不建议 `re-dispatch-L`：厨房不缺动机/机制/边界/例子。不建议 `split-lesson`：未满 10 问；两格是 chain 原设计（厨房 + 四页），不是「一课两个 C4 却讲不完」；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：Intent 失败关闭、投影丢字段是 (a) 的解释到副作用。
- 第二格 `A:system web-domain monitor pages` 课内已讲完且源码可复现，但**矩阵没有对应行**。这是 Lead 改矩阵（新建 `createMonitorWebDomain` / 保留 chain 名），不是 miner 再问、也不是把四页并进本行。九工厂行不要因为本课而标 covered。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:createMonitorService | 子路径工厂、四抽屉 URL/动词、segment、unlock GET、operlog 投影+空页、两尺不发 HTTP、无 export/batchLogout、非 `systemService` 字段、home 不雇——均与源码对齐。残留精度：（1）surface 测试 10 行不是 12 枪；（2）`unlock` 类型为数组、Java 单路径变量、页用 `single` 卡住；（3）`loginInfo.list`/`online.list` 透传，只有 operlog 投影；（4）`safeUrl` 夹具比课列的形状多。 | 可选半句：口试 HTTP 以工厂方法表为准，不以那则 10 行 `toEqual` 为准；unlock 点下去是单名。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-024 来源表 S-L024-01/02/03）。 | defer（本行可 covered；精度项不挡盖章） |
| A:system web-domain monitor pages | 课已讲四键顺序、runtime 注入、本地切片、download 旁路、目录含 `batchLogout`、无独立 web-domain-monitor 包、iframe/资料页是隔壁门。源码可复现。矩阵 (a)/(b) **无此符号**；九工厂行也 **无** `createMonitorWebDomain`。 | 不要并进 `createMonitorService`。请 Lead 单独立行（建议符号 `createMonitorWebDomain` 或保留 chain 名）后再标 covered。不要改九工厂行。本批不把四页当本行缺口。 | defer（未入库符号；Lead 补行，不走 b02） |
| A:CacheController / SysLoginInfo / SysOperlog / SysUserOnline | 本课只对 URL/动词。窗内 Redis、错次键、`kickoutByTokenValue`、list 忽略分页 未讲。 | 保持。L-023。 | defer（L-023） |
| A:createSystemWebDomain / B:navigation host | 课已否 15 键含监控；`inferDomainId` 得 `monitor` 仍能解析。 | 保持。L-020。 | defer（L-020） |
| A:admin-web.services / C:admin-web-composition | 插头 `createMonitorService(domainHttp)` 已借用。 | 保持。全表 L-007。 | defer（L-007） |
| A:createOpenApiService | 根入口对照「有 OpenAPI、无 monitor」。 | 保持。 | defer（L-033） |
| A:NotificationMonitorController / createNotificationService | 键 `notify/monitor/index`，前缀 `/notify/monitor`。 | 保持。 | defer（L-049） |
| admin-external-monitor / wta-extend | Intent 尺本格已讲；iframe 页与进程内部未认格。 | 保持对照边界。 | defer（范围外 / `deferred(separate-process)`） |
| A:SysProfile / 资料页设备 | `current`/`removeCurrent` 在工厂方法表；Vue 是厅堂私有例外。 | 保持。不要抄回四页。 | defer（L-022 邻；本格负空间已写） |
| API-005 变更 POST | 存量 DELETE + unlock GET + 导出 POST。 | 本课盘点，不发动 Ratchet。 | defer（传输 Ratchet） |
| (b) Intent / 投影 / 踢人副作用 | 矩阵无此行。课已把失败关闭与投影写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本行无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出四抽屉目的、主路径（list/get）与失败路径（Intent 拒绝、operlog 空页）；第二格缺的是矩阵行名不是讲义；把可选半句写进原课不会超过 40 分钟；Java 四窗、15 键、通知监控、OpenAPI、iframe 已有后续课或范围外 defer。满 10 问前不拆。厨房与四页虽跨两个 npm 包，仍是 chain 原设计的同一 OBJ-24，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-023 / L-020 / L-007 / L-033 / L-049 / L-022 资料页邻接 / iframe 与 `wta-extend` / API-005 / 未入库 (b) 以及未入库的 pages 符号显式建议 `defer`；对 `A:createMonitorService` 建议 Lead 标 `covered`（证据锚本 probe 与 L-024 来源表）。本课不主张改 Java 四窗、九工厂行、通知监控或任何 (b)(c)(d) 格子。
