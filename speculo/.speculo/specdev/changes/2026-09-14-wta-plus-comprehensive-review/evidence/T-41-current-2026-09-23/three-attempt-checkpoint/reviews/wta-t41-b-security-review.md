# T41 B fixed-source security/session review

**Input:** fixed commit `05d0f3443dc3364917a8149ce9290f7dc6785670`, tree `f1672c953958ec94d3c20546d0b1a4e336e78825`; backend A3 reference `9d748aa`. All product readings below used `git show` or fixed-commit `git diff`, not the working tree. Scope: session ownership, readonly/write permissions, page/detail concurrency, global count/ID boundary, and backend A3 equivalence. No source/governance edits, tests, builds or services were run by this reviewer.

**Static recommendation: PASS for this security/session axis, subject to Lead's exact-candidate gates and real browser acceptance.** The previously observed old-callback race at pre-candidate hash `1bc955…` is closed in fixed B. This is a code-review conclusion, not an assertion that the in-progress frontend gates or browser scenario passed.

## Findings and source checks

1. **Old readAll/read result isolation is closed.** `frontend/apps/admin-web/src/layout/components/notice/index.vue:141-149` captures the session token, notice identityVersion, `userStore.sessionGeneration`, and `identityLoaded`. Detail result/error/finally (:152-169), single read refresh/error/finally (:171-182), and readAll refresh/error/finally (:191-202) gate on this snapshot. `userStore.logout()` changes sessionGeneration before awaiting remote logout (`store/modules/user.ts:82-95`), so even while the old token/version remain unchanged, the old completion cannot refresh the box, show an old error, or clear a newer loading state. Synchronous watcher (:102-103) also clears visible detail. `index.test.ts:94-117` covers held-token generation-only old readAll success/failure/finally; :128-138 covers old single-read completion; :67-92 covers detail clearing/late success. The tests exist in B but their execution is pending Lead.
2. **Page identity fencing and active state are coherent.** The Admin host `router/adminManifestRegistry.ts:268-299` derives epoch from token, generation, userId and loaded state, and suspends the page when only generation changes while the old token/userId/loaded fields remain. `packages/web-domains/notify/src/InboxPage.vue:100-128,141-162,174-226,242-268` clears list/count/detail/mutation state on epoch or active change, rejects late list/detail/mutation completion by captured epoch and generation, and releases subscriptions on unmount. Page turn invalidates old detail. This means a pending remote logout does not reactivate the old page merely because its token has not yet been removed. Component tests cover page 26 of 501, old list/detail/read callbacks, page turn, error recovery, readonly mutation denial and unmount; the host snapshot integration itself is a source invariant to be observed in final real/browser acceptance.
3. **Readonly and global-count boundary.** Both top component and full page only expose or start read mutations when `notify:inbox:read` is present (`index.vue:87,171,191`; `InboxPage.vue:102,174-205,227`). Backend `NotifyInboxController` still requires login for list/detail and checks this permission for read/read-all, so UI guards are not the authorization authority. Full page takes `total` and `unreadTotal` from the server page response, not the 20 current rows (`InboxPage.vue:149-153`); top store returns server `unreadTotal` as the badge and treats ten rows as a recent preview (`store/modules/notice.ts:21-44`, `utils/push.ts:101-115`). Top readonly detail test is at `index.test.ts:119-126`.
4. **Identifier precision.** `packages/domains/notify/src/transport.ts:22-43,82-98` rejects unsafe numeric IDs and preserves decimal string IDs in list/detail/read URLs. The test `transport.test.ts:5-21` uses `9000000000000000001`. Backend Jackson's existing Long serializer emits a string outside JS safe integer range. Generated OpenAPI describes an int64 number, so final HTTP/browser proof remains material; this reviewer did not execute it.
5. **Backend A3 equality is production-only, precisely stated.** `git rev-parse A3:backend/wta-modules/wta-notify` and `B:backend/wta-modules/wta-notify` both return `b096e70f5cb8947d990d8b0826d1c1d711b89a15`; `backend/wta-admin/src/main` both return `abb6b35faf193d6b63d0e5fcbd0b3c227bb30e12`. One backend test file does change: `BrowserHttpsTransportIntegrationTest.java:267` replaces its `/notify/inbox` empty-list stub with the real page shape `{rows:[],total:0,unreadTotal:0}`. This test-fixture change is necessary for the new transport shape; it is not a production backend deviation. The exact B frontend, fixture and generated-contract gates must still be evaluated separately.

`git diff --check 9d748aa..B` over the reviewed frontend product paths exited 0. No additional concrete blocking security/session finding was found in the fixed scope. Unverified here: Lead's frontend test/typecheck/lint/build counts, generated OpenAPI provenance, exact real Admin two-user browser behavior/cleanup, and whole-change acceptance; no pass is inferred from test declarations or the earlier A3 run.
