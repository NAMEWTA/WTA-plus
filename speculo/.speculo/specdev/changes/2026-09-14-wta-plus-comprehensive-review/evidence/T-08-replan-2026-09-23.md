# T-08 evidence

Current HEAD `ecc163ed` already meets AC-008. No new implementation was required. This cancels only the repeated construction. AC-008 stays owned by T-30. The adjacent OIDC change was not modified.

## Three-origin journey

```bash
node release-artifacts/tests/fixtures/sso-release-origin.mjs --evidence <owned-json>
```

Exit 0. The fixture built admin-web, home-web, and sso-web in production mode under `/admin-app/`, `/home-app/`, and `/sso-app/`, then served them with the tracked Nginx templates, isolated MySQL 8.4.9, and Redis 7.4-alpine. Containers and the owned network were removed, and the Docker inventory matched the pre-run snapshot.

`SsoHttpsSessionIntegrationTest`: tests=1, failures=0, errors=0, skipped=0. Playwright: 5 passed, 0 skipped, 10.3s. The test printed `T-08: 5 real Chrome scenarios passed through production Nginx templates with Redis/MySQL SSO`. It consumed 6 authorization codes, matching the issued count. `AuthClientContextSsoUnitTest`: tests=3, failures=0, errors=0, skipped=0.

The Chrome cases covered admin and home HTTPS login, callback, and refresh; signing in again after the SSO session expired and the business token was removed; three `/healthz` routes; a host-only Secure HttpOnly SSO cookie that is absent from the app origins and from `document.cookie`; cross-origin session reads rejected with 403; and the test clock control hidden from the app origins with 404.

## Release contract

```bash
bash release-artifacts/scripts/verify-release.sh
```

Exit 0 on the same HEAD. tests=128, pass=128, fail=0, skipped=0. Missing, corrupt, and mixed stage inputs leave current unchanged. Compose parsing ran. A test whose name contains "skipped" asserts the external-service report gate; the runner skipped nothing.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase. No production source edit.
