---
artifact: learning-goal-probe
probe_id: GP-L-051-b01
lesson_id: L-051
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-051 / b01

审问对象是已写成的 Lesson `children/2026-09-14-wta-notify/lessons/L-007-notify-application.md`（chain 全局 ID L-051 / OBJ-51，子课号仍是 L-007 / OBJ-07）与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、verdict、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain 覆盖列 **两格 (a)**；派单另点名 **(d)**）：`A:NotificationController.submit, query, retry, cancel`、`A:NotificationApplicationService`。OBJ-51 原文：能口述 `/notify/notification` submit/query/retry/cancel。档是**解释+走查**。chain 覆盖列写成 `A:NotificationController.submit,query,retry,cancel A:NotificationApplicationService`，**不**写 (b)、(d)。矩阵 (a) 两行模块都是 `wta-notify`，均仍 `uncovered`：Controller 行四方法与磁盘 1:1；第二行原文 `NotificationApplicationUseCase / NotificationApplicationService`，备注「wta-api + UseCase」。矩阵 (b) **有**独行 `NotificationController.submit/retry/cancel`（口号「写 Outbox / 取消」），仍 `uncovered`；chain **未**把 (b) 写进覆盖列。矩阵 (c) `wta-modules` 业务房间已 covered（L-002/L-003）；MySQL 8.4 / `dao-mapper-ownership` 仍 uncovered。矩阵 (d) 「主路径 通知提交」原文 `NotificationController.submit → Outbox → claim → 渠道`，备注 **L-051, L-052**，仍 uncovered。本批按派单审问 **两格 (a) + (d) 的源半段**（submit 写四表 + READY Outbox + 事务内唤醒事件）；口试按磁盘四扇 HTTP + 接口字段 + UseCase 事务章 + Runtime 校验/幂等/解析/回执公式。(b) 用本课机制链判断能否盖章，不要等改 chain。claim SQL / Worker / `dispatch(outbox)` 留给 L-052；`NotifyClient`/`NotifyDispatcher` 留给 L-082；公告六扇留给 L-045；配置试发调用方留给 L-047；监控 snapshot 留给 L-049；回调验签留给 L-050；厨房 `createNotificationService`（**没有**这四枪）留给 L-053。

抽查锚点（2026-09-17）：`backend/wta-modules/wta-notify/src/main/java/org/namewta/notify/controller/admin/NotificationController.java`；`usecase/NotificationApplicationUseCase.java`；`service/runtime/{NotificationApplicationRuntimeService,DispatchNotificationService}.java`；`dao/NotifyNotificationDao.java`；`support/outbox/NotifyOutboxWakeRequestedEvent.java`；`resources/mapper/notify/NotifyOutboxMapper.xml` `requeue`；`domain/policy/NotificationAggregatePolicy.java`；`wta-api/.../notify/api/{NotificationApplicationService,NotificationCommand,NotificationQuery,NotificationReceipt,NotificationSnapshot,NotificationRetryCommand,NotificationCancelCommand,RetryReceipt,CancelReceipt,NotificationMode,NotificationStatus}.java`；测试 `service/runtime/NotifyOutboxWakePublisherTest.java`、`domain/policy/NotificationAggregatePolicyTest.java`（**没有** `NotificationController*Test` / `NotificationApplicationUseCase*Test` / 独立 Runtime submit 单测）；DDL `10-cde-base-ddl.sql` `notify_intent|recipient|delivery|outbox`；DML `50-cde-base-dml.sql` 四串 `notify:notification:*`；规范 `notification.md`「调用入口」「后端分层」；登记表 `03-backend-module-modes.md` layered 行；旁证 `service/{NotifyNoticePublisherService,NotifyTestSendService}.java`（格子归 L-045/L-047）；厨房 `frontend/packages/domains/notify/src/{transport.ts,notification/index.ts}`（格子归 L-053）；`frontend/packages/api-contracts/generated/openapi.ts` `/notify/notification**`。

## Q1 澄清

- objective_id: OBJ-51
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-51；chain L-051 覆盖列两格 (a)；子课 L-007 开篇四扇 + 接口字段；矩阵 (a) 四方法 + UseCase/Service 行仍 uncovered；(b) 已有 submit/retry/cancel 独行；(c) notify 房间已 covered；(d) 通知提交走廊仍 uncovered 且挂 L-051/L-052；负空间 L-045/L-047/L-049/L-050/L-052/L-053/L-082
- difficulty: medium

课把应用柜台收成「同一张 `NotificationCommand`、柜台盖章后信件进发件箱」。计划里至少有四套可数对象，派单点名的是 **两格 (a) + (d)**：

1. **OBJ-51 原文。** `course.md`：能口述 `/notify/notification` submit/query/retry/cancel。四个动词是口试主题，**没有**把 `NotifyIntent` 四表、`queued` 公式、`selectAllActiveUsers`、claim SQL 写成第三张矩阵行。子课 OBJ-07 同句，课号不要写成 L-007。
2. **chain 覆盖列。** 两格 (a)。Controller 点名四方法；第二格只写接口名 `NotificationApplicationService`。磁盘唯一实现是 `NotificationApplicationUseCase implements NotificationApplicationService`。chain **不**写 Runtime、不写 DAO、不写 (b)、不写 (d)、不写厨房。
3. **矩阵 (a) 两行。** Controller 行四名与磁盘 `@PostMapping` ×3 + `@GetMapping("/{notificationId}")` 1:1。第二行把 UseCase 与接口绑在同一格（备注 wta-api + UseCase）。Lead 若按这两行标 covered，**不会**把 L-052 claim 或 L-053 厨房假闭合。
4. **矩阵 (b) 已有独行。** `NotificationController.submit/retry/cancel` = 写 Outbox / 取消。chain 没把它写进覆盖列，但本课机制链 1–8 / 10–11 与正例 1–3 / 反例 3 **就是**这台机器（submit 只写 READY Outbox；cancel 改 PENDING、不 finish Outbox）。不要等 L-052 才承认「提交阶段不执行 Provider I/O」。也不要为 (b) 另开一课才给两格 (a) 盖章。
5. **矩阵 (d) 与派单。** 「主路径 通知提交」原文含 **claim → 渠道**，备注 L-051 **和** L-052。本课图边界写明不画 claim SQL、短信 SDK。派单要审问的是 **源半段**：HTTP/Java `submit` → 四表 → READY Outbox → 事务内 `NotifyOutboxWakeRequestedEvent`。AFTER_COMMIT Redis、领取、`dispatch(outbox)` 不是本两行 (a) 的闭合条件，也 **不够**单独把 (d) 整行标 covered。
6. **课自己的操作定义。** 门牌 `/notify/notification`。Controller 字段类型是接口 `NotificationApplicationService notificationService`，不是 UseCase 类名。UseCase 唯一字段 `runtimeService`；`submit/retry/cancel` 带 `@DSTransactional`，`query` 不带。Runtime javadoc：提交阶段不执行 Provider I/O。`queued`（回执字段名 `outboxQueued`）= `mode==ASYNC` 且存在 PENDING。规范「UseCase 负责幂等和目标解析」与磁盘冲突：这两件事在 Runtime。
7. **Runtime / Dispatch 不是第三张 (a)。** `findDuplicate` / `resolveUsers` / 写四表 / `requestOutboxWake` / 空篮 `refreshAggregate`，课都讲了。矩阵 **没有** Runtime 行。`DispatchNotificationService.dispatch` 是领取后的下一步（L-052/L-082）。口试认它们是本窗解释到的运行时后果，不要升格成第三法，也不要用 submit 去盖 claim。
8. **邻格。** L-045 公告 Publisher 调同一 `submit`（`USER_TYPE` 先压成 USER）。L-047 试发 `mode=SYNC` 也只 submit。L-049 监控 snapshot ≠ 本窗 query。L-052 Outbox 领取。L-053 厨房 22 枪 **没有**这四扇。L-082 同步渠道入口。

问：本两格 (a) 闭合标准是「四扇 URL/动词/四串权限 + Controller 只持接口 + UseCase 事务章与 Runtime 委托 + 校验/幂等/解析/四表/UNDELIVERABLE 不进篮 + `queued` 公式 + query 忽略 `includeContent` + retry 只捡 FAILED/UNKNOWN + cancel 拒终态只改 PENDING，且不把 Runtime 升成第三张 (a)」，还是 OBJ 那句必须先把 (d) 整行（含 claim）标 covered、或必须把接口行拆成 UseCase 与 Runtime 两行、或必须等 GP-L-052/L-053 才给四枪盖章？Lead 若只凭「`NotificationController` 四字在函数表里」把 L-052 claim 或 L-053 厨房标 covered，是否假闭合了领取走廊与前端工厂？Lead 若用本课把 (d) 整行标 covered，是否假闭合了 claim → 渠道？

## Q2 证据

- objective_id: OBJ-51
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 21–60；UseCase 20–44；API 6–14；Command 27–40；Query 9；RetryCommand 11；Receipt 14–16；Snapshot 14–15；Runtime 29–32 / 36–41 / 43–138 / 140–155 / 157–193 / 195–210 / 217–219 / 221–254 / 256–261 / 263–292 / 294–300 / 302–310 / 312–323；Dao 16–24 / 27–29 / 70–77 / 93；Outbox XML 40–48；Dispatch 43–62 / 209–221；Policy 20–34；WakeEvent 8；WakeTest 136–225；Publisher 24 / 39–40 / 61–65；TestSend 83–89；DDL 1451–1577；DML 1529–1531 / 1564；modes.md 19；notification.md 7–19 / 21–35；Log 35–40；transport.ts 17–71；notification/index.ts 3–6；openapi 2688–2747 / 5631–5642；测试树无 Controller/UseCase 单测
- difficulty: high

课的四扇窗 + 接口实现哪些能在工作树直接复现，哪些只是课把回执字段叫 `queued`、或把邻格领取说成本课第三扇？

**仍能复现（两格 (a) + (d) 源半段 + (b) 写盒/取消）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 一份类、根路径 `/notify/notification`；继承 `BaseController`；只注入 `NotificationApplicationService notificationService` | Controller 21–22 |
| 四扇映射：POST `/` submit、GET `/{notificationId}` query、POST `/{id}/retry`、POST `/{id}/cancel`；无 PUT/DELETE | Controller 25–60；openapi 2688–2747 / 5631–5642 |
| 权限四串 `notify:notification:submit\|query\|retry\|cancel` 与 DML 1:1 | Controller 26 / 33 / 42 / 53；DML 1529–1531 / 1564 |
| submit/retry/cancel 有 `@Log`（INSERT/UPDATE/UPDATE）；query **无** `@Log` | Controller 25 / 41 / 52 vs 33–38 |
| query `includeContent` 默认 false，放进 `NotificationQuery` | Controller 36–37；Query 9 |
| retry/cancel body `required = false`；空则 path id + reason=`manual` | Controller 45–47 / 56–58 |
| 接口四方法；模块内 **唯一** `implements` 是 UseCase | API 6–14；全树仅 UseCase 20 |
| UseCase 唯一字段 `runtimeService`；submit/retry/cancel `@DSTransactional`；query 无 | UseCase 21–44 |
| Runtime 四字段：dao / userService / dispatchService / events | Runtime 38–41 |
| javadoc：提交只写事实和 Outbox，不执行 Provider I/O | Runtime 29–32 |
| `validate`：appId/scene/template/channels 非空；类型只 ALL/USER/PHONE/EMAIL；ALL 不得带 ID；非 ALL 必须有 ID；USER 必须正整数；expires 不能早于 scheduled | Runtime 221–254 |
| 空白幂等键跳过 `findDuplicate`；有键则 `dao.intentByIdempotency(appId, key)` | Runtime 256–261；Dao 27–29 |
| insert Intent `status=QUEUED`；`DuplicateKeyException` 再查一次 | Runtime 67–80 |
| 每用户 Recipient（电话邮箱进 `targetSnapshotJson`）；每渠道 Delivery；目标 blank → `UNDELIVERABLE` + `TARGET_UNAVAILABLE`，**不**建 Outbox；否则 PENDING + Outbox READY、`maxAttempts=5` | Runtime 85–130 |
| 目标：IN_APP=userId 字符串，SMS=phone，MAIL=email | Runtime 294–300 |
| 空 Outbox → `dispatchService.refreshAggregate`；否则 `events.publishEvent(NotifyOutboxWakeRequestedEvent(firstId))` | Runtime 132–136 / 217–219 |
| 回执 `outboxQueued = ASYNC 且存在 PENDING`；`followUpRequired` = 非 IN_APP 且 ACCEPTED/UNKNOWN | Runtime 302–305；Receipt 14–16 |
| 对外状态：Delivery `PENDING`/`READY` 映射成 `QUEUED` | Runtime 312–323 |
| query **不读取** `includeContent`；Snapshot **无**正文字段 | Runtime 140–155；Snapshot 14–15 |
| retry 只过滤 Delivery `FAILED`/`UNKNOWN`；`markDeliveryForRetry` 必须 1 行；`requeue` 0 行则新建 READY | Runtime 165–185；Dao 70–77；XML 40–48 只收 `WAITING_RECEIPT`/`DEAD_LETTER` |
| retry 成功入队才 `requestOutboxWake`；hint 允许 null（requeue 命中时不改 hint） | Runtime 167 / 183–191；WakeEvent 8 |
| cancel：意图已是 DELIVERED/CANCELLED/FAILED → `当前通知状态不允许取消`；否则意图 CANCELLED + 批量 PENDING→CANCELLED；**不** finish Outbox、**不**发 wake | Runtime 203–209；WakeTest 195–225 |
| Worker 侧：Delivery 非 PENDING 则 Outbox 标 DONE（本课只点到；领取格子归 L-052） | Dispatch 59–62 |
| 空篮聚合：`refreshAggregate` → `NotificationAggregatePolicy`（全 UNDELIVERABLE → FAILED） | Dispatch 209–221；Policy 20–34 |
| ALL 分页 `selectAllActiveUsers(offset, 1000)`，超过 100000 拒绝；空列表「没有可接收通知的正常用户」 | Runtime 50–51 / 270–281 |
| `resolveUsers` 额外认识 SMS/MAIL 别名，但 `validate` 先挡，HTTP 合法请求进不了别名分支 | Runtime 231–233 / 285–289 |
| Command compact：`mode` 空则 `ASYNC` | Command 38 |
| 表 `notify_intent` unique `(app_id, idempotency_key)`；Outbox unique `(delivery_id, status)`；max_attempts 缺省 5 | DDL 1477 / 1565 / 1574 |
| `wta-notify` layered；Controller 不 import Runtime | modes.md 19 |
| 规范：业务只依赖该接口；同一事务写意图/接收者/Outbox；默认异步 | notification.md 7–19 / 29–35 |
| 唤醒单测：submit/retry 在 afterCommit 前 **不**发 Redis；cancel 不登记唤醒 | WakeTest 136–225 |
| 厨房 `createNotificationService` **零** submit/query/retry/cancel；`./notification` 只贴 `basePath` | transport.ts 17–71；notification/index.ts 3–6 |

**邻格仍能复现（不计入本两行 (a) 闭合；用来判断走廊能不能单独盖章）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 公告 Publisher 持同一接口；`USER_TYPE` 先解析再压成 `USER` 再 submit；幂等键带快照版本 | Publisher 24 / 33–40 / 61–65（L-045） |
| 配置试发 `mode=SYNC`、`recipientType=EMAIL/PHONE`，HTTP 只回状态名 | TestSend 83–89（L-047） |
| 真正 Redis 发布 / claim `for update skip locked` / `dispatch(outbox)` | L-052 XML 4–12；Dispatch 52 |
| 监控 snapshot 是另一扇 GET `/notify/monitor/snapshot` | L-049；厨房 transport 20–26 |

**课写过、测试名单更窄或口试标签写偏（不是证伪 (a)）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 回执字段口试叫 `queued` | Java 记录字段是 `outboxQueued`（Receipt 15）；公式仍是 ASYNC∧PENDING |
| 「`@Valid NotificationCommand`」 | 记录 **没有** Bean Validation 注解；真闸是 Runtime `validate` |
| retry body 可带 `deliveryId` / `idempotencyKey` | Runtime **零**读取这两字段；过滤器永远是该意图下全部 FAILED/UNKNOWN |
| 非空 retry/cancel body | Controller 直接把 body 交给接口，**不**把 path id 写回 command；body 缺 id 会走「通知编号不能为空」，即使 path 有号 |
| retry「意图改回 QUEUED」 | **无**终态闸。0 条 FAILED/UNKNOWN 仍 `dao.update` 成 QUEUED，且不 wake |
| 「规范 UseCase 负责幂等和目标解析」 | 课已标冲突、跟 Java。证人是 Runtime，不是 UseCase 行的第三张章 |
| `@Log` 未关 `isSaveRequestData` | 默认 true（Log 35–36）。配置窗 L-047 显式 false；本窗 Command 可含电话/邮箱。规范「不要写入日志」与磁盘不一致 |
| 无 Controller / UseCase 单测 | 四扇映射与事务分叉的证人是源码 + 唤醒单测 + 邻课 mock submit |
| 合同测试 / MockMvc | 不存在。OpenAPI 四 path 可旁证 |

问：在四扇 URL/权限、接口字段、UseCase 事务章、校验/幂等/解析、四表与 UNDELIVERABLE 不进篮、`queued` 公式、SYNC 仍写 Outbox、query 忽略正文开关、retry 只捡 FAILED/UNKNOWN、cancel 拒终态且不 wake 已经能互证的前提下，两格 (a) 是否还要把「回执字段名 `outboxQueued`」「`@Valid` 是空闸」「retry 不读 deliveryId」「0 条匹配仍把意图写成 QUEUED」「无 Controller 单测」写成开篇半句才算讲完？(d) 是否因为原文含 claim，所以本课只能盖源半段、不得把整行标 covered？(b) 写盒/取消是不是已经在机制链里，不必等改 chain？

## Q3 假设与反例

- objective_id: OBJ-51
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: Controller 22 / 36–58；UseCase 21–44；Runtime 32 / 132–136 / 140–155 / 157–193 / 203–209 / 231–233 / 256–261 / 302–305；XML 40–48；Dispatch 59–62 / 209–221；Publisher 39–40；TestSend 85；transport.ts 无四枪；WakeTest 195–225
- difficulty: high

课的隐含假设若反转，合格读者会把哪把枪或哪条走廊认错？

1. **「Controller 字段类型是 UseCase。」** 字段是接口。课误区 1。唯一实现才是 UseCase。
2. **「SYNC 等于现在打供应商。」** Runtime submit 无供应商分支；`outboxQueued` 才看 mode。课误区 2 / 正例 3。试发 L-047 是调用方，不是第二扇 submit。
3. **「幂等在 UseCase。」** `findDuplicate` 在 Runtime。课误区 3。规范跟代码冲突时跟代码。
4. **「cancel 会删 inbox / 抽回 Outbox。」** 只改意图与 PENDING 投递。已 persist 的消息不在这个方法里删。Worker 见非 PENDING 才把 Outbox 标 DONE。课误区 4。
5. **「retry 重试所有渠道 / 按 body.deliveryId 精确打一枪。」** 过滤器只有 FAILED 与 UNKNOWN；`deliveryId` 字段闲置。UNDELIVERABLE 要先补资料再重新 submit。课误区 5。
6. **「query `includeContent=true` 会回正文。」** Runtime 忽略该布尔；Snapshot 无 content。课变式 B。
7. **「`recipientType=USER_TYPE` 能进应用 API。」** `validate` 直接「接收者类型不受支持」。USER_TYPE 只活在公告草稿，Publisher 压成 USER。课反例 1。
8. **「ALL 同时塞 userIds 会当指定名单发。」** 「全部用户通知不能同时指定接收者编号」。课反例 2。
9. **「在 Controller 里 `dispatch(outbox)`。」** 分层禁止；submit 成功路径是 wake 或 `refreshAggregate`。课反例 3。
10. **「缺联系方式的渠道仍进待寄篮。」** blank 目标 UNDELIVERABLE，不 insert Outbox。课机制 6。
11. **「唯一渠道 UNDELIVERABLE 时意图永远停在 QUEUED。」** 空篮走 `refreshAggregate`，政策可聚成 FAILED。课暂停题。
12. **「cancel 已 ACCEPTED 的投递也会改成 CANCELLED。」** 只改 PENDING。课边界。
13. **「retry 拒绝已 DELIVERED 的意图。」** 没有这道闸。0 条匹配仍把意图写成 QUEUED。课未点名为反例。
14. **「非空 retry body 以 path id 为准。」** 磁盘以 body.notificationId 为准。课未点名。
15. **「缺幂等键会 400。」** 空白键跳过查重，仍 insert。规范要求稳定键，Runtime 不强制。
16. **「厨房 `createNotificationService.submit` 是本四枪。」** 工厂零枪打 `/notify/notification`。标签 `basePath` 不是方法表。L-053。
17. **「本课覆盖 claim，所以 (d) 整行可 covered。」** 矩阵该行挂 L-051/L-052。本课只保证写出 READY 并在事务内登记唤醒。把假设反转后，Lead 会用本行假闭合 L-052。
18. **「监控 snapshot 就是本窗 query。」** 另一扇门、另一把权限。L-049。
19. **「公告 retract 会调 cancel。」** 撤回链零引用本窗 cancel。L-045。
20. **「UseCase 是空转发，可以删。」** 事务注解除了它没处盖；Controller 也不得直连 Runtime。
21. **「PHONE+IN_APP 也能进篮。」** PHONE 解析无 userId，IN_APP 目标 null → UNDELIVERABLE。
22. **「`@Valid` 已经挡住空渠道。」** 记录无约束注解；空渠道是 Runtime 句子「通知应用、场景、模板和渠道不能为空」。

问：读完后仍说不出的，是不是「四个方法名」，而是 **Controller 只持接口、写路径事务章在 UseCase、幂等/解析在 Runtime、SYNC≠当场发送、缺地址不进篮、空篮才 refreshAggregate、query 不回正文、retry 只捡 FAILED/UNKNOWN 且可不带 hint、cancel 不 wake、厨房没有这四枪、(d) 的 claim 半段不在本课**？

## Q4 视角

- objective_id: OBJ-51
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-51 vs L-045/L-047/L-049/L-052/L-053/L-082；chain 无 (b)/(d)；UseCase 事务章；Runtime queued；厨房零枪
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:NotificationController.submit,query,retry,cancel`**、**`A:NotificationApplicationService`** 或派单的 **(d) 源半段**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 两行 vs (b) 已有写盒/取消行；(d) 仍 uncovered 且挂 L-051/L-052 | **是** 闭合定义。两行 (a) 可单独 covered。(b) 可用本课机制链盖章，**不要**等改 chain。(d) 整行 **否**，只盖源半段 |
| 四扇窗作者 | 四映射、四权限、query 无 Log、retry/cancel body 可空 | **是** 第一格 |
| 接口电工 | Controller 字段是接口；全树唯一 UseCase 实现；业务模块不打 HTTP | **是** 第二格。wta-api 整包目录 **否**（L-002 已 covered） |
| 用例电工 | 三枪 TX、query 无 TX；只委托 Runtime | **是** 第二格。Runtime 升格第三法 **否** |
| 提交走廊作者 | 源：四表+READY+wake 事件；中：AFTER_COMMIT Redis；汇：claim/dispatch/渠道 | 源半段 **是** (d) 派单。领取/渠道 **否**（L-052/L-082）。不要把 (d) 改挂本课独有 |
| 写盒/取消（(b)） | submit 写 Outbox；retry 重入 READY；cancel 改 PENDING 不 finish | **是** 已入库 (b)。不要另开一课 |
| 公告柜员（L-045） | Publisher 调 submit；retract 不 cancel | 出口对照 **是**。六扇 **否** |
| 试发柜员（L-047） | SYNC 试发仍 submit | 调用方对照 **是**。十扇配置 **否** |
| 监控柜员（L-049） | snapshot/deliveries | **否** |
| 回调柜员（L-050） | HMAC / 单向升级后再 `refreshAggregate` | 对照「submit 成功≠回执」**是**。验签窗 **否** |
| 领取（L-052） | claim 短事务、hint 不直投、失败保持待领取 | 上游开关 `requestOutboxWake` **是**。领取格子 / fail-outbox **否** |
| 厨房（L-053） | 22 枪无这四扇；标签有 `basePath` | URL 负空间 **是**。工厂格子 **否** |
| Dispatcher（L-082） | `NotifyClient.send` | submit 注入 dispatch 只为 refreshAggregate **是**。同步入口 **否** |
| MySQL 8.4 / dao-mapper | 四表 unique；DAO 持 Mapper；Outbox 有 XML | 本窗 unique 意图 **是**。容器课 **否**（L-085/L-083） |
| (b) includeContent / deliveryId 闲置 | 矩阵无此行 | 课已写进 (a) 变式。不要发明第二 (b) |

问：Lead 若凭「四个方法名都在函数表里」把两行 (a) 标 covered，是否还要求读者能指着 **接口字段、UseCase 事务章、Runtime 幂等/解析、四表、UNDELIVERABLE 不进篮、queued 公式、SYNC 仍写 Outbox、cancel 不 wake**？这些是不是已经在本格函数表/反例里？(b) 写盒/取消是不是同一台机器、可以凭本课盖章而不是第二容器？(d) 是不是明确「源半段本课、claim 归 L-052、不得整行假闭合」？

## Q5 元问题

- objective_id: OBJ-51
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 两行、(b) 已有写盒/取消独行、(d) 通知提交挂 L-051/L-052
- difficulty: low

建议：`defer`

原因：

- **两格 (a) 与矩阵 / chain / OBJ-51 1:1。** 子课 L-007 合同章节齐全（宏观地图、直觉、定义表、机制链 1–11、带 caption / 文字等价物 / 图边界的 submit 图、正反例、变式、误区、暂停走查、来源表 S-N-07/S-N-09/S-N-10/S-N-11）。**主叙事站得住且与源码对齐**：四扇 HTTP、Controller 只持接口、UseCase 唯一实现且写路径盖事务章、Runtime 做校验/幂等/解析/四表、SYNC 不等于当场发送、缺地址不进篮、query 忽略 `includeContent`、retry 只捡 FAILED/UNKNOWN、cancel 拒终态且不登记唤醒。不必为「四个方法名」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例或邻格，不挡住把 **两行 (a)** 标 covered：（1）回执字段名是 `outboxQueued`；（2）`@Valid` 对无约束记录是空闸；（3）retry 不读 `deliveryId`；（4）retry 无终态闸，0 条匹配仍把意图写成 QUEUED；（5）非空 body 不以 path id 为准；（6）无 Controller/UseCase 单测；（7）`@Log` 默认存请求体。不要把 claim、厨房工厂、监控 snapshot、回调验签、Dispatcher 塞进本两行。
- **(b) `NotificationController.submit/retry/cancel` 不要另开一课，也不要等 GP-L-052。** 矩阵口号「写 Outbox / 取消」；磁盘就是 submit 写 READY、retry 重入篮、cancel 改 PENDING 且不 wake。chain 覆盖列只写 (a)；标 (b) covered 的证据锚是本课机制链 + 本 probe + 矩阵已有该行，不是先改 chain。miner 不改 chain。
- **不要用本课把 (d) 整行标 covered，也不要等 (d) 才给两行 (a) 盖章。** 矩阵「主路径 通知提交」明确挂 L-051/L-052，原文含 claim → 渠道。本课只保证源半段：Command → 四表 → READY Outbox → 事务内唤醒事件。AFTER_COMMIT Redis、领取、供应商 I/O 是邻格。GP-L-052 尚未落盘，不阻塞本两行 (a) 与 (b)。
- 不建议 `mine-more`：b01 未证明两行还有真实矩阵空洞；再五问只会把 L-052 claim、L-053 厨房或 L-082 Dispatcher 再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；可选半句（字段名 `outboxQueued`、retry 终态、path vs body、Log 存请求体）不是第二容器。不建议 `split-lesson`：未满 10 问；四枪与接口是 chain 原设计的同一 OBJ-51，同一 `wta-notify` 容器；(d) 跨 L-052 是矩阵原设计的走廊切分，不是「超过 1 个容器却讲不完」。`split_depth: 0`，本批禁止填 split 表。不建议发明 Runtime (a) 行，也不建议发明 includeContent (b)。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:NotificationController.submit, query, retry, cancel | 门牌四映射、四权限、query 无 Log 且忽略正文开关、retry/cancel body 可空——均与源码对齐。残留精度：（1）`@Valid` 空闸；（2）非空 body 不回写 path id；（3）`@Log` 默认存请求体；（4）无 MockMvc/合同测试。 | 可选半句：真闸在 Runtime `validate`；retry/cancel 以 body.notificationId 为准。Lead 可将本行标 `covered`（证据锚本 probe 与 L-007 来源表 S-N-07）。 | defer（本行可 covered；精度项不挡盖章） |
| A:NotificationApplicationUseCase / NotificationApplicationService | 接口四方法；唯一实现 UseCase；三枪 TX / query 无 TX；委托 Runtime 做校验/幂等/解析/四表/`queued` 公式——均与源码对齐。残留：无 UseCase 单测；规范把幂等写在 UseCase，课已标冲突。 | 可选半句：口试字段类型以接口为准；幂等/解析以 Runtime 为准。Lead 可将本行标 `covered`（S-N-07/S-N-09/S-N-10）。 | defer（本行可 covered） |
| B:NotificationController.submit/retry/cancel | 矩阵口号「写 Outbox / 取消」。磁盘：submit 写 READY；retry 重入 WAITING_RECEIPT/DEAD_LETTER 或新建；cancel 改 PENDING、不 finish、不 wake。chain 未列 (b)。 | 不要等 L-052，也不要拆第二课。课已拆「SYNC≠发送 / 缺地址不进篮 / cancel≠删消息」。Lead 可将本行标 `covered`。 | defer（本行可 covered；证据在本课，不改 chain 也能盖） |
| D:主路径 通知提交（NotificationController.submit → Outbox → claim → 渠道） | 源半段（四表 + READY + 事务内 wake 事件）本课已讲。汇半段 claim → 渠道未认。矩阵仍 uncovered，备注 L-051、L-052。 | 不要用本课改该行整行。对照一句「submit 只写盒、SYNC 不打供应商」即可。等 L-052 后再盖。 | defer（L-052；本课只是源半段） |
| B:NotifyOutboxClaimUseCase / D:fail-outbox | AFTER_COMMIT Redis、claim 租约、失败保持待领取。本课只登记唤醒。 | 保持。L-052。 | defer（L-052） |
| B:NotifyClient.send / NotifyDispatcher.send | `dispatch(outbox)` 在领取事务外。本课只可能 `refreshAggregate`。 | 保持。L-082。 | defer（L-082） |
| A:NotifyNoticeController.* / A:NotifyNoticeUseCase | Publisher 调 submit；retract 不 cancel。 | 保持。L-045。 | defer（L-045） |
| A:NotifyConfigController.testAccount, testTemplate | 试发 SYNC 调用方。 | 保持。L-047。 | defer（L-047） |
| A:NotificationMonitorController.snapshot, deliveries | 另一扇 GET。 | 保持。L-049。 | defer（L-049） |
| A:ProviderCallbackController.callback | 回执升级后再 refreshAggregate。 | 保持。L-050。 | defer（L-050） |
| A:createNotificationService / A:notificationDirectory | 厨房零枪打本前缀；标签不是方法表。 | 保持。L-053。 | defer（L-053） |
| Runtime / DAO / AggregatePolicy 升格 (a) | 课已写进第二格解释。矩阵无此符号。 | 不要发明第三法。 | defer（未入库） |
| (b) includeContent 闲置 / retry.deliveryId 闲置 / retry 无终态闸 | 矩阵无此行。课变式 B 已写 includeContent。 | 可选半句进原课。不要发明 (b)。 | defer（未入库） |
| C:MySQL 8.4 / mysql-base / dao-mapper-ownership | 四表 unique、DAO 持 Mapper、Outbox 有 XML 是本窗机制。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085 / L-083） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本两行 (a) 无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出四扇窗的目的（交单据 / 查回执 / 只捡失败渠道再入篮 / 停掉还没出门的 PENDING）、主路径（校验 → 幂等 → 解析 → 四表 → wake 或 refreshAggregate）与失败路径（类型不受支持、ALL 带 ID、空用户、缺地址不进篮、终态不可取消、SYNC 仍不打供应商）；(d) 缺的是矩阵把 claim 半段挂在 L-052，不是讲义容器；把可选半句写进原课不会超过 40 分钟；公告、试发、监控、回调、领取、厨房、Dispatcher 已有邻课。满 10 问前不拆。四枪与一份接口是 chain 原设计的同一 OBJ-51，同一 `wta-notify` 容器，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-052 领取、L-082 Dispatcher、L-045 公告、L-047 试发、L-049 监控、L-050 回调、L-053 厨房、未入库 Runtime/(b) 闲置字段、(c) 容器与 dao-mapper 组件显式建议 `defer`；对 `A:NotificationController.submit, query, retry, cancel` 与 `A:NotificationApplicationUseCase / NotificationApplicationService` 建议 Lead 标 `covered`（证据锚本 probe 与 L-007 来源表）；对 `B:NotificationController.submit/retry/cancel` 建议凭本课标 `covered`，不要等 chain 补写 (b) 或等 L-052；对矩阵「主路径 通知提交」建议保持 uncovered 并继续挂 L-051/L-052，只承认本课源半段，不要发明第二 (d)、不要用四枪假闭合 claim。本课不主张发明 Runtime (a)、把厨房标签算进本两行、或等 GP-L-052 才给 (a)/(b) 盖章。
