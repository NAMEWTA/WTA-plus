# T-50 three-run retrospective: B real-gate source identity

Read-only finding: the third real run is **not accepted** even though Maven and all eight selected classes passed. The fixed driver correctly reports `acceptance=false` for a failed final source identity check. The most likely cause is concurrent frontend production build writing tracked generated declaration files while the real driver checked `git status`; this is a high-confidence interference mechanism, but the exact dirty path at that instant was not captured, so the precise cause remains unproven. No product or driver gate should be weakened on this evidence.

## Three retained attempts

| Run | Fixed source | Behavior | Disposition |
| --- | --- | --- | --- |
| `35e14468040352c5` | A1 `9bb2e2888b5d2b0563beb7164c8c0a2165547e2c` | Maven exit 1: real HTTP optional primitive priority request expected 200, got 400; source before/after clean; cleanup errors empty. | Genuine test expectation failure in A1. Later fixed as a test-contract correction; do not recast as accepted. |
| `e29ae3ce94c6942c` | A2 `1dfdcbca831ad7391d725d7f001acb2b9ba1c88f` | Maven exit 0, 135 tests / 0 failures / 0 errors / 0 skips; source before/after clean; cleanup errors empty. | Accepted for A2 only, not a B candidate run. |
| `e5e3229eb2500be7` | B `50757f9b872b0b9b0131e4b2a4538bff7473f74d` | Maven exit 0; 135 / 0 / 0 / 0 in eight classes, with retained XML and method counts. Final source identity check failed and `source_after` was not saved. Containers, one captured anonymous volume, Maven process group and both loopback ports cleaned successfully; sole cleanup error is `source_after_not_clean_exact_head`. | `acceptance=false`, exit 1. The fresh XML proves test behavior, but not the full real-gate acceptance contract. |

All times below are UTC on 2026-09-23. B real run was 15:06:21.140–15:09:43.665. The independently recorded frontend production build was 15:08:27.990–15:09:52.777, so the final identity check occurred while that build was still active. Its script runs workspace dependency builds, then admin-web Vite, then home-web and sso-web. The admin-web Vite configuration registers `unplugin-auto-import` and `unplugin-vue-components` with declaration outputs at `frontend/apps/admin-web/src/types/auto-imports.d.ts` and `components.d.ts`; both are Git tracked. The installed plugins write these paths through direct `fs.promises.writeFile`, not an atomic replacement. That gives a concrete window in which `git status --porcelain --untracked-files=all` can see partial or intermediate content.

Filesystem metadata corroborates the overlap: among tracked frontend files, only those two declaration files have modification times inside the build interval, respectively **15:09:42.731** and **15:09:46.739**. The first is 0.934 seconds before the B run's finished timestamp, and the second is 3.074 seconds after. The driver's `source_identity` checks exact HEAD plus a completely clean `git status`, but its `finally` catches any exception and writes only `source_after_not_clean_exact_head`, not the failing command or dirty path. Thus we cannot distinguish a transient declaration write, a transient untracked file, or a Git inspection failure from that historical result alone. The declaration-write race has **high confidence as a mechanism and probable cause; medium confidence as the exact unobserved failure**. There is no evidence of a persistent product-source diff.

After the build completed, both declaration files' current Git object hashes equal the B HEAD objects and `git status --porcelain --untracked-files=all` is empty. Separately recorded B source checks at 15:10:39 and 15:11:34 show the same B head/tree clean. This later state supports transient interference, but it cannot retroactively satisfy the failed run's source-after proof.

## Controlled recovery and falsification

1. Finish or stop all workspace-writing gates first, including the already-started core/static run; do not overlap a frontend Vite build, code generation, checkout, commit, or another product writer with the real gate. Keep frozen B HEAD `50757f9b872b0b9b0131e4b2a4538bff7473f74d` and tree `01927241641aceef0fb13ffea2a3f28a748bdaf7`.
2. Immediately before the run, record `git rev-parse HEAD`, `git rev-parse HEAD^{tree}`, and `git status --porcelain --untracked-files=all` as a clean exact checkpoint. Confirm the two declaration files hash to their B HEAD objects. This is read-only; do not reset or rewrite files to force cleanliness.
3. Run the **unchanged frozen** `/tmp/wta-t50/run-notify-supported-mode-integration-v2.py` alone against its exact B `--expected-head`. Retain the new run's eight fresh XML files and method/count manifest. Require Maven 0, 135 tests or the source-derived actual counts with all eight classes positive and zero failure/error/skip, `source_before == source_after` at clean B HEAD/tree, and empty cleanup errors including process, full container IDs, captured volume and ports. Do not promote run `e5e3229eb2500be7` as acceptance.
4. If the serialized retry still gets `source_after_not_clean_exact_head`, stop and capture a prompt read-only Git status/HEAD/tree and driver stderr/owned cleanup into private `/tmp` evidence. The current driver does not expose the exception, so such a repeat would require a new diagnostic candidate or sidecar evidence that **retains** the exact-clean gate, never a bypass. Investigate any actual changed path or Git failure before another real attempt.

Evidence inspected: three `/tmp/wta-t50/runs/*/result.json` records; B retained XML manifest; `/tmp/wta-t50/b1-frontend-build.json`; `/tmp/wta-t50/b1-source-{before,after}.json`; frozen driver `source_identity`/`finally`; admin-web Vite plugin configuration and installed plugin write paths; current Git status, tracked-file mtimes and object hashes. No Docker, Maven, frontend build, or service was started during this review.
