# T-50 private runner preparation and diff audit

**Versioned handoff:** `/tmp/wta-t50/version-manifest.json` is the authoritative frozen-path map. The table below records the pre-versioning byte identities: original v1 `dd14e75e...` is preserved at `run-notify-supported-mode-integration-v1.py`; final v2 `518a0c19...` is preserved at `run-notify-supported-mode-integration-v2.py` and byte-identical to the unversioned snapshot. `test_driver_safety.py` now imports the explicit v2 path, changing only that test-file hash to `193317e03fa27616280609336517a1364ce76285d0a44e032d16fd04be499644`. Historical `offline-synthetic-v1/v2` results retain their original runner bytes; both versioned drivers and the capture passed `py_compile`, and v2-copy byte equality was verified without rerunning the unchanged 27 assertions.

Prepared under `/tmp/wta-t50/` only. No repository file, Docker container, Maven build, JVM, HTTP service, or `/srv/ops` content was touched. The T-50 product class was absent when this preparation began, so real integration execution is intentionally impossible until source preflight recognizes its exact class, flag, private password and owned fixture seam.

| Artifact | SHA-256 | Parent and controlled delta |
|---|---|---|
| `run-notify-supported-mode-integration.py` | `518a0c1946ba026d84f5e637e9ce00cc125306134f077f2d310a0614e09fe5a9` | From frozen T39 v4 `3f4b5eb24cb4281f61853f783968fe9d1294010df8ff748fb7d4fff55486d2d0`; full diff in `integration-driver.diff` |
| `capture-live-openapi.py` | `267f825eb6cb57a93413622aff2d9d5b3c33b231f73fa39c14c93b1cccfa9176` | From T38 capture v2 `9ae7cfcd28e8b5ba8ee0bff271a76f656ca7bb2eb421caa5dbdfdb1d0452909e`; full diff in `openapi-capture.diff` |
| `test_driver_safety.py` | `5d1b8d0fed9db0f78e05644125ec63bb50a6a5c33f24a5b43c59f11cef395ff4` | Fourteen isolated T50 driver tests; no service launch |
| `test_openapi_capture_safety.py` | `7c7aaf84bbdece73d10673801a0b8143593a12a29c7b1e9cd22d007877d7180e` | Thirteen isolated T50 capture tests; no service launch |

The integration delta changes the owner/run directory, labels and container names, test user prefix, one selected class/report from Common Redis to T50 supported mode, its opt-in and private password alias. It also avoids Docker discovery when source preflight fails before the first Docker operation, while preserving cleanup discovery after any attempted Docker operation. The seven retained existing classes are Deadline 15, Enterprise 8, Wake 1, Atomic 66, ManualRetry 13, SMS 10 and Redis idempotency 3, total **116** currently known methods; T50 class must contribute positive fresh methods. Each class requires its exact fresh XML, positive tests and zero fail/error/skip. Source head/tree must match the explicitly supplied clean 40-hex SHA both before and after. The MySQL/Redis random-loopback six-SQL/103-table setup, app-only database user, secret-free Maven argv, fresh fork, dual-label/full-ID cleanup, captured anonymous volume verification, port closure, process-group kill and redacted logs are retained unchanged. `T50_MYSQL_PASSWORD` and the older `T39/T36/T38/T35` aliases receive the **same owned random password only in the Maven child environment**. New source preflight checks the exact class/flag and either direct owned DB/Redis properties or the writer-confirmed `NotifyAtomicResultIntegrationTest.open()` fixture before any Docker call.

The capture delta changes only the T50 owner/run directory, owned resource names/user prefixes and nonsecret KEK version. It retains the frozen T38 owned-resource helper at `/tmp/wta-t38/run-notify-manual-retry-integration.py` with expected SHA `d068610f75a9c65cabf77d69d230884b33edd9c9f3d64c73c73296655066c31e`; the helper is imported for primitives, **not** for T38 owner labels. Capture requires explicit `--execute`, exact clean source SHA, full JAR SHA-256 and package proof binding a successful clean full package command, JAR path/size/hash, build-log hash and JAR mtime. The full-bundle module manifest check, owned authenticated Redis, pinned MinIO, six-SQL no-provider-work preflight, private prod overlay, direct bounded HTTP bytes saved before loss validation, source/JAR identity after capture, and complete owned cleanup remain unchanged. The old RetryReceipt/path/schema no-loss guard remains; any T50-specific generated schema assertion must be reviewed against the actual writer diff before first real capture. Source A's JAR and package proof must be preserved until capture; generated candidate B follows capture and must bind the raw bytes/provenance rather than substitute an old snapshot.

Offline evidence after the final runner fix: `python3 /tmp/wta-check.py /tmp/wta-t50 offline-synthetic-v2 /srv/WTA-plus python3 -m unittest discover -s /tmp/wta-t50 -p 'test_*safety.py' -v` exited **0**, 27/27 synthetic tests passed (14 integration-driver, 13 capture), log `/tmp/wta-t50/offline-synthetic-v2.log`, machine record `/tmp/wta-t50/offline-synthetic-v2.json`. The prior v1 synthetic result is retained and is not the final code's result. Tests mock Docker/HTTP where necessary; this is safety-tool verification, **not** T50 product acceptance. Neither runner has been invoked with `--execute`; Lead must separately review and freeze their hashes against the committed T50 source and exact candidate SHA before any real run.
