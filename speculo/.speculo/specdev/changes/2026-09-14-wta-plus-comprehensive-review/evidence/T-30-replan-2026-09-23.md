# T-30 evidence

Source checkpoint `6002e7ad` (`6002e7ad1abd3ff7b3c0d4e4f2c6e8838605d46d`). The worktree was clean before these commands. The other 49 tickets were already terminal. This run does not change production code. AC-030 stays the integration record. The change is not archived, pushed, or deployed. Windows remains `user-waived/not-run` and is not a pass.

## Same-candidate real services

Isolated MySQL `mysql:8.4.9` (`sha256:c36050afdca850f23cef85703f84c7531a5ae155a11b5ee1c60acb09937c4084`), Redis `redis:8.6.3`, and MinIO `quay.io/minio/minio:RELEASE.2025-04-22T22-12-26Z`. Each case used its own loopback port and database. Credentials were the existing test defaults or a generated test-only password and are not copied here. Containers were removed after each case. System Chrome ran the browser cases. Every counted case below is failures=0, errors=0, skipped=0.

| Case | Tests | Notes |
|---|---|---|
| Notify atomic + wake + enterprise transfer | 75 | 66 + 1 + 8. Includes SQL/lease rollback, slow provider, lost wake, Redis pause, and queued transfer failure recovery. |
| Profile materials + workflow engine | 2 | Browser case also printed Playwright 8 passed. Workflow rejected a stranger and a duplicate transition. |
| Department tree | 14 | Cycle, concurrent move, and subtree rollback. |
| Demo tree | 15 | Illegal parent, cycle, and child-delete rejection. |
| Third permits + limit refresh | 12 | Killed JVM, Redis pause, and two-node configuration refresh. |
| CRUD HTTP | 1 | `PUT`/`DELETE` return 405. Unauthorized and invalid writes do not change rows. |
| SSO HTTPS journey | 1 | Playwright 12 passed, 18.2s. An earlier attempt on a preloaded schema failed because the test creates `sso_authorization_code` itself. That attempt is not counted. The counted run used an empty `namewta_sso_test_*` database. |

## Release candidate

```bash
bash release-artifacts/scripts/verify-release.sh
cd frontend && pnpm build:prod
cd backend && rm -rf wta-admin/target && ./mvnw -pl wta-admin -am package -Pbundle-full -Dmaven.test.skip=true
bash scripts/ci/verify-admin-bundle.sh full
cd backend && rm -rf wta-admin/target && ./mvnw -pl wta-admin -am package -Pbundle-core -Dmaven.test.skip=true
bash scripts/ci/verify-admin-bundle.sh core
```

All exited 0. Release tests=128, pass=128, fail=0, skipped=0. `build:prod` built admin, home, and sso (`built in` 5.54s, 713ms, 125ms). Earlier full/core verifies that reported a missing or unexpected `wta-job` reused a stale `wta-admin/target` and are not the pass. On `7aca0c86`, which changes no product source after `6002e7ad`, each profile was packaged only after `rm -rf backend/wta-admin/target`. `verify-admin-bundle.sh full` printed `full bundle contents verified` and exited 0. The target was deleted again, then `verify-admin-bundle.sh core` printed `core bundle contents verified` and exited 0. Transcripts: `evidence/T-30-bundle-full-7aca0c86.out` and `evidence/T-30-bundle-core-7aca0c86.out`.

The 100-recipient publish probe was re-run because T-43's evidence HEAD is not this HEAD. `NotifyFanoutMeasurementIntegrationTest#measurePublishedAllInAppFanout` on a six-file schema of 104 tables: tests=1, skipped=0. Metrics: recipient/delivery/outbox rows 100, intent rows 1, JDBC `executeUpdate` calls 8, rollbacks 0. Production fan-out code was not changed after `9267ecb5`.

## Not run, and not counted as passes

`./mvnw test` without the service properties was not re-run. Its historical gated skips are not this proof. Default `pnpm test:e2e` was not run because it asks for the missing Playwright headless shell; the SSO and Profile cases above used system Chrome. `scripts/ci/run-external-services.sh` was not used as a substitute. Archive, deploy, credential rotation, and permanent-knowledge promotion were not authorized and were not performed. `.status.json` stays `archived: false`.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase, operation `verify-affected-contract-and-quality-gates`, status passed. No production source edit.
