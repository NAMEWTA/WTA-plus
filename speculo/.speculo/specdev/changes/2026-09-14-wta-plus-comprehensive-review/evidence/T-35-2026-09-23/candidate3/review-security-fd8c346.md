# T-35 C3 fixed-source security/specification delta review

**Source verdict: PASS for the C3 three-path delta; final ticket acceptance remains pending Lead's new gates.** Fixed parent `0b14311c9652b4d765c4db8c8898d28662eb5adb`, C3 `fd8c34644d2cb36f96deccce81fe35f586701f94`, tree `ae8068dde29ef30364f540006aac73edb44836af`; C3 is the direct child. Lead reported clean before at 09:35:58Z. Review used fixed `git diff 0b14311..fd8c346 -- backend` and `git show fd8c346:<path>`; the diff contains only `DispatchNotificationService.java`, its unit test and `NotifySmsDispatchIntegrationTest.java`. No repo, service, or test action by this reviewer. AC-035/T-35 revision152 and the C2 corrected finding (`/tmp/wta-t35/review-security-0b14311.md`) are the decision boundary.

## C2 ACQUIRE blocker closed by source

`NotifyDispatcher.send` still calls `beginIdempotency` before `adapter.send` (`NotifyDispatcher.java:73-78,98-100`). `NotifyIdempotencyCoordinator.begin` wraps Redis acquire failure in `NotifyIdempotencyUnavailableException("ACQUIRE")` (`NotifyIdempotencyCoordinator.java:30-41`). C3 now catches this exact type before the generic RuntimeException catch and only for SMS/phase `ACQUIRE` maps it to fixed `FAILED/PREPARATION_RETRYABLE` (`DispatchNotificationService.java:171-183`). `NotifyDispatchResultService.java:86-95,152-156` treats that category as nonterminal, so the delivery becomes `PENDING` and Outbox `READY` with existing bounded backoff/max attempts, rather than `UNKNOWN/WAITING_RECEIPT`. It does not persist `exception.getMessage()`, `cause`, original request ID, phone or OTP. The new real-Dispatcher unit case asserts zero supplier calls and this state (`DispatchNotificationServiceTest.java:251-265`); the new isolated Redis case corrupts its owned idempotency bucket, checks the first zero-call READY disposition, repairs that bucket and checks one accepted supplier call (`NotifySmsDispatchIntegrationTest.java:369-389`). Those assertions still require execution on this exact C3 candidate.

## Post-I/O and existing-claim safety

`NotifyIdempotencyCoordinator.complete` wraps completion/CAS failure as phase `COMPLETE` (`:44-49`), after `NotifyDispatcher` has called the adapter. C3's non-ACQUIRE branch keeps `UNKNOWN/DISPATCH_ERROR` (`DispatchNotificationService.java:178-182`), which remains `WAITING_RECEIPT` and is not automatically requeued. The new unit case asserts one supplier call and UNKNOWN/WAITING (`DispatchNotificationServiceTest.java:267-283`). The new real Redis integration case damages only the owned bucket during the supplier call to force the completion CAS error, then invokes dispatch against the waiting Outbox and asserts the supplier count remains one (`NotifySmsDispatchIntegrationTest.java:391-405`). This tests the no-blind-resend boundary, not a real SMS vendor.

`NotifyDispatcher.java:79-84` still emits distinct `NotifyInProgressException` and `NotifyIdempotencyConflictException` when an existing claim is found. Both continue through the unchanged generic runtime catch after client entry, retaining conservative UNKNOWN/WAITING; the C3 typed catch does not match them. Their exception messages are fixed and `DispatchNotificationService` persists only fixed `DISPATCH_ERROR` text, not `originalRequestId`. C3 does not introduce a public/log/event path for that original ID. A partial Redis acquire may leave a claim despite throwing; a later attempt would see InProgress/Completed/Conflict rather than blindly invoke the supplier. No new test directly asserts the runtime mapping for InProgress/Conflict, but the reviewed branches are unchanged and separately typed.

## Reused security scope and evidence limit

The C1 REDACT event/log/target and management query/receipt/monitor protections, FULL behavior, internal provider ID correlation and fixed SMS provider error categories are unchanged in C3; see `/tmp/wta-t35/review-security-41882b5.md`. The C2 pre-client quota classification and best-effort confirmed-hold release are likewise unchanged; see `/tmp/wta-t35/review-security-0b14311.md` and its explicit C2 addendum. C3 resolves that addendum's typed ACQUIRE blocker without broadening the generic retry path.

Lead is about to run the 38-class unit selection and eight real isolated MySQL/Redis scenarios. Their exit codes, exact Surefire XML counts/zero skips, owned-resource cleanup and final clean HEAD must be checked separately before AC-035 is marked done. This source review does not claim those runs passed.
