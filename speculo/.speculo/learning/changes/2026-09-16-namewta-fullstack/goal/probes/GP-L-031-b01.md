---
artifact: learning-goal-probe
probe_id: GP-L-031-b01
lesson_id: L-031
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-031 / b01

审问对象是已写成的 Lesson `lessons/L-031-openapi-credential.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（`claimed_cells`）：`A:SysOpenApiCredentialController.*`、`B:SysOpenApiCredentialController.create/reset`。OBJ-31：能口述 `SysOpenApiCredentialController` 创建/重置/启停/删除（明文不回显）；档是**方法性状**。chain 覆盖列**只写 (a)** `A:SysOpenApiCredentialController.*`。矩阵 (a) 把十三方法写全：`getSelfCredential, createSelf, resetSelf, enableSelf, disableSelf, deleteSelf, users, getUserCredential, createUser, resetUser, enableUser, disableUser, deleteUser`（与磁盘 `*` 1:1，没有 export / PUT / DELETE / `/interfaces`）。矩阵 (b) 一行：`create/reset`，性状栏「密文存储、列表不回显」。两行均仍 `uncovered`。本批按派单审问 **十三扇窗 + create/reset 密文/不回显**。启停/删除的作废会话是 (a) 的 explained 副作用，矩阵**没有** `enable/disable/delete` 或 `invalidateByUserId` 独行。`SysOpenApiCatalogController` 四扇留给 L-030；HMAC / 机器会话 / nonce / 限流留给 L-032；`createOpenApiService` / `openApi.vue` 留给 L-033；SSO `rotateSecret` / BCrypt 留给 L-018；OSS `changeStatus` Y/N 留给 L-025。矩阵 (c) MySQL 8.4 / Redis 8、(d) 无 OpenAPI 凭据数据流行。

抽查锚点（2026-09-16）：`backend/wta-modules/wta-system/.../controller/system/openapi/SysOpenApiCredentialController.java`；`openapi/credential/{service/SystemOpenApiCredentialService,service/SystemOpenApiCredentialResolver,mapper/SysOpenApiCredentialMapper,domain/SysOpenApiCredential,model/OpenApiCredential{Issued,Summary,CreateRequest,UserSummary,UserRow},crypto/OpenApiCredentialCrypto,crypto/PropertyOpenApiKekProvider,crypto/OpenApiCredentialCryptoException}.java`；`SysOpenApiCatalogController.java` 同前缀四扇 GET；`03-backend-module-modes.md` classic 行；`10-cde-base-ddl.sql` `NAMEWTA-OPENAPI-CREDENTIAL-DDL-001` 约 516–544 行；`50-cde-base-dml.sql` `DML-001` 六串 + `system/openApi/index`；`application.yml` `openapi:` 52–60 行；`OpenApiProperties.enabled` 字段默认 false；`SysOpenApiCredentialControllerContractTest` / `SystemOpenApiCredentialServiceTest` / `OpenApiCredentialCryptoTest` / `OpenApiCredentialSqlContractTest`；`GlobalExceptionHandler.handleServiceException`；`frontend/packages/api-contracts/generated/openapi.ts` Issued/Summary 注释；`domains/system/src/open-api/{service.ts,transport.ts}`（仅旁证，格子归 L-033）。

## Q1 澄清

- objective_id: OBJ-31
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-31；chain L-031 覆盖列只 (a)；L-031 `claimed_cells` 两格；矩阵 (a) 十三方法独行、(b) create/reset「密文存储、列表不回显」；(c) MySQL/Redis uncovered；(d) 无凭据流
- difficulty: medium

课把钥匙柜台收成「十三扇窗 + 签发盒只给一次」。计划里至少有四套可数对象，派单点名的是 **Controller 全表 + (b) create/reset**：

1. **OBJ-31 原文。** `course.md`：能口述 `SysOpenApiCredentialController` 创建/重置/启停/删除（明文不回显）。档是方法性状。点名的是四类写动作 + 不回显，没有把 GET 详情 / `users` 写成口试清单，也没有把 HMAC 解密写成口试清单。
2. **chain 覆盖列。** 只有 `A:SysOpenApiCredentialController.*`。不写 (b)。
3. **Lesson `claimed_cells` / 矩阵两格。** (a) 与磁盘十三映射 1:1；(b) 原文「密文存储、列表不回显」。课把 create/reset 四扇窗收成同一对 Service 方法。
4. **课自己的操作定义。** (a) 前缀 `/system/openApi`、零 PUT/DELETE、自有六扇 `:self`、他人七扇 list/query/add/edit/remove **且** `requireSuperAdmin()`、GET 走 Summary、create/reset 走 Issued + `no-store`、delete 走 `R<Void>`；(b) 表无 `app_secret` 列、AES-256-GCM 四列 NOT NULL、签发明文来自内存不是 decrypt、列表/GET/启停永不回显、create **不** invalidate、reset 保持 `appKey`。
5. **邻格。** `/self/interfaces` 是另一份类（L-030）。解密发生在 `SystemOpenApiCredentialResolver`（L-032）。厨房 `createOpenApiService` 与 Summary 投影拒 `secret` 键是 L-033 的篱笆，本课只作旁证。

问：Lead 盖章时，「明文不回显」是 (b) 那句「表/Summary/列表不出现 `appSecret`」，还是 OBJ 那句连 create 响应也不能带明文？(a) 是否必须包含 GET/`users`（课用来对照 Issued vs Summary），还是 OBJ 四类写窗就够？chain 只写 (a) 时，标 (b) covered 的证据锚是 `claimed_cells` 还是要先改 chain 覆盖列？启停/删除的 `invalidateByUserId` 是 (a) 的 explained 副作用，还是一张不存在的 (b)？

## Q2 证据

- objective_id: OBJ-31
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 34–172；Service 49–186；Mapper 17–57；实体 19–45；Issued/Summary/CreateRequest；Crypto 43–131；KekProvider 12–52；CryptoException 7–13；DDL 519–544；DML 791–826；yml 52–60；ContractTest 41–140；ServiceTest 36–137；CryptoTest 34–90；SqlContractTest 15–34；Handler 64–70；transport.ts 117–141
- difficulty: high

课的十三扇与 create/reset 哪些能在工作树直接复现，哪些被表/信封说薄或测试名单更窄？

**仍能复现（本课两格）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 一份类、十三扇、根路径 `/system/openApi`；**不**继承 `BaseController`；只注入 `SystemOpenApiCredentialService`；`@ConditionalOnProperty(openapi.enabled=true)` | Controller 34–43；无 PUT/DELETE 注解 |
| 同前缀另一份类才是 catalog 四扇 GET `/self/interfaces*`、`/users/{id}/interfaces*` | CatalogController 24–54 |
| `wta-system` classic；无 UseCase、无 DAO、无 `ISysOpenApiCredentialService`、无 Mapper XML；Service 直接持 Mapper | 登记表 classic 行；模块内零 `usecase/openapi`；Mapper 接口 `@Select` |
| 自有六扇权限全是 `system:openApi:self`；owner **只**来自 `LoginHelper.getUserId()` | Controller 45–93；合同测试 `selfOwnerComesOnlyFromLoginContextAndAdminRechecksSuperAdmin`：`create(41L, request)` |
| 他人七扇：`users`=`list`，GET 凭据=`query`，create=`add`，reset/enable/disable=`edit`，delete=`remove`；每一扇再 `requireSuperAdmin()` | Controller 95–159 |
| 十扇写窗全是 POST，路径后缀 `/credential/{create\|reset\|enable\|disable\|delete}`；`@Log` 且 `isSaveRequestData=false` **且** `isSaveResponseData=false`；无 `@RepeatSubmit` | 合同测试 `everyWriteUsesPostExactPermissionAndSafeOperationLogging` |
| GET `/self/credential`、`/users/{id}/credential` 返回 `Summary`；空行 Controller `requiredCredential` → `ServiceException(..., NOT_FOUND)` | Controller 47–49 / 167–171；合同测试 `missingCredentialUsesNotFoundContract` `code=404` |
| create/reset 四扇返回 `Issued`，并 `Cache-Control: no-store` | Controller 55–67 / 114–130 |
| delete 两扇 `R.ok()` 无 data 盒子 | Controller 90–92 / 155–158 |
| `CreateRequest` 只有 `appName`（`@NotBlank` `@Size(100)`）/ `expiresAt`（`@Future`）/ `remark`（`@Size(500)`），**无 owner** | CreateRequest 12–19 |
| `OpenApiCredentialSummary` 注释 *safe for repeated transport*；字段无 `appSecret`、无密文四列 | Summary 5–18 |
| `OpenApiCredentialIssued` 注释 *AppSecret is never part of a summary model*；只比 Summary 多 `appSecret` | Issued 5–19；生成 openapi.ts 9366 / 9398 同行注释 |
| `users` HTTP 默认 `limit=50`；Service `Math.max(1, Math.min(requestedLimit, 100))`；SQL 不选密文列；`where` 只滤 `u.del_flag='0'`，**不过** `u.status` | Controller 97–101；Service 53–57；Mapper 42–54 |
| create：`@DSTransactional`；owner 空 → 404；`existsActiveOwner` 失败 → 404 且 `crypto.generate` **never**；已有活行 → 409；`generate` 后 `applyEncrypted` 只写四列；`insert!=1` 或 `DuplicateKeyException` → 409；返回 `issued(..., generated.appSecret())`；**不**调 invalidator | Service 60–89；单测 `createRejectsAnUnknownOrDisabledOwnerBeforeGeneratingMaterial` / `createReturnsSecretOnceAndConcurrentUniqueLoserGetsConflict` |
| reset：`generateSecret()` **不**新 `appKey`；`encrypt(owner, 旧 appKey, 新 secret)` 走现行 `activeVersion()`；`updateAndInvalidate`；再 `issued` | Service 91–98；单测 `resetKeepsAppKeyAndEveryStateMutationInvalidatesMachineSession` `appKey` 仍 `app-key`、新秘密 `new-secret` |
| enable/disable → `changeStatus` 无「已是该状态则跳过」；即使再 enable 一次仍 `updateById` + invalidate；返回 `summary` | Service 100–124 |
| delete：`deleteById`（`@TableLogic`）`!=1` → 409；然后 `invalidateByUserId`；create **不**走这条 | Service 110–117 vs 60–88 |
| reset+disable+enable+delete 合计 **4** 次 `invalidateByUserId` | ServiceTest 76–92 |
| `get` / `users` 无 `@DSTransactional`、不 decrypt、不 invalidate；`get` 空行是 `null`，404 在 Controller | Service 49–57；`everyLifecycleWriteIsTransactionBound` 只钉 create/reset/enable/disable/delete |
| 表 `sys_open_api_credential`：生成列 `active_owner_user_id`；`uk_..._active_owner`；`uk_..._app_key`（含已删）；密文四列 NOT NULL；无 `app_secret`；`status` 默认 `'0'` | DDL 519–544；实体 `FieldStrategy.NEVER` + `@Version` + `@TableLogic` |
| `selectByOwnerUserId` 是 `select *` 但不进 HTTP 模型；`selectUserSummaries` 投影无密文；`existsActiveOwner` 要求用户 `status='0' AND del_flag='0'` | Mapper 17–40；单测 `get()` record 组件名不含 `appSecret` / 密文四列 |
| `selectUsableByAppKey` **不在**本 Controller 调用链；SQL 不滤凭据 `status`，Java Resolver 见 `status!='0'` 则 decrypt **never** | Mapper 25–33；Resolver 35–40；单测 `resolverRejectsUnknownDisabledExpiredAndCryptoFailuresWithOneCategory` |
| AES-256-GCM、nonce 12、tag 16、KEK 必须 32 字节；AAD 含 `NAMEWTA-OPENAPI-CREDENTIAL-V1` + owner + appKey + version；失败文案单一 `OpenAPI credential crypto unavailable`；`finally` 擦 KEK/明文 | Crypto 24–75 / 111–135；CryptoException 7–13；CryptoTest 34–90 |
| Java `PropertyOpenApiKekProvider` **无**源码默认密钥；旧版走 `openapi.keks.<ver>`；yml 给了 `OPENAPI_KEK` 开发默认值；`OpenApiProperties.enabled` 未绑定前是 `false`；运行配置 `OPENAPI_ENABLED:true` | KekProvider 12–38；yml 52–60；Properties 13 |
| 菜单种子六串 + `system/openApi/index`；`NAMEWTA-OPENAPI-CREDENTIAL-DML-001` | DML 791–826；SqlContractTest 29–34 |
| GET JSON 无 `appSecret` / `secretCiphertext`；POST create 有 `$.data.appSecret=one-time-secret` 且 `Cache-Control=no-store` | ContractTest `transportShowsSecretOnlyForCreateAndUsesNoStore` 108–118 |
| 前端 Summary 投影：任一键名含 `secret` 即抛；Issued 只允许 `appSecret` 且空串拒（L-033 篱笆） | `transport.ts` 117–141；`service.ts` `NO_STORE` + `repeatSubmit: false` |

**课写过、表/信封说薄或测试名单更窄（不是证伪两格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「null → 404 / 冲突 → 409」 | `ServiceException.code` 确是 `HttpStatus.NOT_FOUND=404` / `CONFLICT=409`。`handleServiceException` **没有** `@ResponseStatus`：HTTP 仍 200，JSON `code` 才是 404/409。合同测试断言的是异常 `getCode()`，不是 MockMvc HTTP 404 |
| 「超管闸这扇没写 404 码」 | `new ServiceException("OpenAPI credential management is unavailable")` 的 `code=null` → Handler 走 `R.fail(msg)` → JSON `code=500`（`HttpStatus.ERROR`）。不是 403，也不是 404。合同测试只断言文案，不断言 code |
| 十三扇表：自有 enable/disable/delete 写「会作废会话」，他人三扇只写「否」 | 他人窗同样 `credentialService.enable/disable/delete(userId)` → 同一 `changeStatus` / `delete` → `updateAndInvalidate`。表列省略，机制段 4 已写 |
| 「作废失败则库回滚」 | 单测 `invalidationFailureFailsTheTransactionalWriteExplicitly` 只证明 `IllegalStateException("redis down")` 冒出；mapper 是 mock，**没有**集成事务回滚夹具。回滚靠方法上的 `@DSTransactional` 推断 |
| AAD = ASCII 标签 + owner + appKey + version | 实际 `ByteBuffer`：CONTEXT + `putLong(owner)` + `0` + appKey UTF-8 + `0` + version ASCII。口试「换 owner / 改 tag 关死」仍成立 |
| 暂停「圈出 GET 两扇是 Summary」 | GET **三**扇只读：两扇 Summary + `users` 返回 `List<UserSummary>`（嵌套仍是 Summary）。总结段已写三扇 |
| 测试名 `transportShowsSecretOnlyForCreateAndUsesNoStore` | MockMvc 只打了 GET + create。`resetSelf`/`resetUser` 返回类型同样是 `Issued` 且源码设 `no-store`。课误区 16 已补 |

问：在十三扇路径/权限/POST+安全 `@Log`、Issued vs Summary、create 先查用户再 generate、reset 保持 appKey、密文四列 NOT NULL、GET JSON 缺 `appSecret` 已经能互证的前提下，(a)(b) 是否还要把 **「HTTP 404」「超管闸 403」「他人 enable 不作废」** 写成开篇半句，才算讲完？还是这些已经（或应当）落在反例/边界，Lead 可以直接给两行盖章？

## Q3 假设与反例

- objective_id: OBJ-31
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 十三映射全表；Issued vs Summary；CreateRequest 无 owner；create 不 invalidate；reset 不换 appKey；无明文列；status `'0'`/`'1'`；超管闸且权限；零 PUT/DELETE；classic 无 UseCase；catalog `/interfaces` 另一类；BCrypt≠GCM
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪只盒子认错？

1. **「明文不回显 = create 也不返回 `appSecret`。」** 签发盒必须带。课误区 1 / 正例 1。这是 (b) 的主反例。
2. **「reset 会换 `appKey`。」** 只换 secret 和密文四列。课误区 2 / 正例 2。
3. **「GET 会 decrypt 再藏起来。」** GET 构造 Summary，不碰 Crypto。课误区 3。
4. **「列表 SQL 只是忘了选 secret 列。」** 模型层就没有该字段；合同测试检查 JSON 缺省。课误区 4。
5. **「`status` 是 OSS 那种默认正门 Y/N。」** 这里是 `'0'`/`'1'` 启停。课误区 5；对照 L-025。
6. **「超管可以读出密钥。」** 超管也只在代 create/reset 时收信封。课误区 6。
7. **「有 `system:openApi:query` 就能打他人窗。」** 还要 `isSuperAdmin()`。课误区 7 / 反例 4。
8. **「写操作是 PUT/DELETE。」** 全是 POST，路径里带动词。课误区 8。
9. **「create 也会踢机器会话。」** 不会。课误区 9 / 反例 6。
10. **「作废会话失败库已经改完了。」** 同一 `@DSTransactional`。课误区 10。单测只证明异常冒出。
11. **「逻辑删除会释放 `app_key`。」** 只释放 `active_owner_user_id`。课误区 11。
12. **「这是 layered，该有 UseCase。」** 登记 classic。课误区 12。
13. **「`/self/interfaces` 也是凭据课。」** Catalog，L-030。课误区 13。
14. **「跟 L-018 一样是 BCrypt。」** 这里是 AES-256-GCM，因为网关以后要还原明文。课误区 14。
15. **「本地 yml 有默认 KEK，所以 Java 也写死了密钥。」** Provider 从配置读。课误区 15。
16. **「合同测试名叫 OnlyForCreate，所以 reset 不回密钥。」** 看返回类型。课误区 16。
17. **「第二次 create 会覆盖。」** 已有活行 409。课反例 2。
18. **「给停用用户办新钥匙会先 generate。」** 404 且 generate never。课反例 3。
19. **「超管闸是 HTTP 403。」** 文案 management unavailable，`code=null` → JSON 500。课只写「没写 404 码」，没写 500。
20. **「空 GET 的 HTTP 状态是 404。」** JSON `code=404`，HTTP 200。课写「404」易听成状态码。
21. **「`users` 名单不含停用账号。」** SQL 不过 `u.status`。课机制 1 已写。
22. **「停用账号的活凭据超管不能 reset。」** `selectByOwnerUserId` 不 join 用户状态；机器路径 `selectUsableByAppKey` 才要求用户启用。课边界已写。
23. **「已经 enable 再 enable 不会踢会话。」** `changeStatus` 无短路。课机制 4 已写。
24. **「`createOpenApiService` 属于 OBJ-31。」** OBJ-33。课开篇负空间。

问：读完后仍说不出的，是不是「十三方法名」，而是 **Issued 只给 create/reset 一次、表里没有明文列、reset 不换编号、create 不 invalidate、GET 不解密、权限串且超管闸、JSON 404/409 不是 HTTP 状态**？

## Q4 视角

- objective_id: OBJ-31
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)(b)(c)(d)；OBJ-31 vs L-030/L-032/L-033/L-018/L-025；API-005；DDL 两 unique；Redis 作废
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:SysOpenApiCredentialController.*` 或 `B:SysOpenApiCredentialController.create/reset`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | chain 只 (a)；课认两格；OBJ 点名四类写 + 不回显；(c) MySQL/Redis uncovered；(d) 无凭据流 | **是** 闭合定义。(a)(b) 可单独 covered。不要发明 enable 的 (b)，不要把 Redis 容器标 covered |
| 钥匙柜台（本 Controller） | 十三扇：路径/动词/六+五权限/十扇 POST+安全 `@Log`/四扇 `no-store`/超管闸；无 export、无 `/interfaces` | **是** (a) 逐方法 |
| create/reset 作者 | 先查用户再 generate；密文四列；Issued 内存明文；reset 保持 appKey + invalidate；create 不 invalidate | **是** (b) |
| 启停/删除 | Summary/Void；同一事务踢会话；逻辑删释放活唯一键不释放 `app_key` | **是** (a) explained。不要另开 (b) 行 |
| 目录窗（L-030） | 同门牌 `/system/openApi` 另一份类四扇 GET | 负空间一句 **是**。四扇全表 **否** |
| 网关（L-032） | Resolver.decrypt；HMAC；nonce；机器会话结构 | 停用不解密 / reset 要作废的对象 **是** 下游一句。Signer/会话实现 **否** |
| 厨房（L-033） | `createOpenApiService`；Summary 拒 secret 键；前端 `no-store` + `repeatSubmit: false` | 旁证运输箱 **是**。工厂/页面全表 **否** |
| SSO 轮换（L-018） | BCrypt 哈希 + `ssoSecretOnce` | 对照一句 **是**。Client/SsoApp 全表 **否** |
| OSS 正门（L-025） | `status=Y/N` 忽略 body N | 对照「这里真写 `'0'`/`'1'`」**是**。配置切换 **否** |
| API-005 | 十扇变更 POST+`@Log`；GET 只读；无 PUT/DELETE | 与规范同向 **是**。不发动传输 Ratchet **否** |
| MySQL 8.4 / L-085 | 表名、生成列、两 unique、密文四列 | 本课用到的表/键 **是**。基座容器 **否** |
| Redis 8 / L-084 | `invalidateByUserId` 函数接口 | 写成功之后才调 **是**。(c) 会话容器 **否** |
| 身份邻居 | `SysUserServiceImpl` / `SysRoleServiceImpl` 等也会 invalidate | 本课 HTTP 不是唯一踢入口 **是** 边界。用户/角色 CRUD **否**（L-015/L-016） |
| 全局异常信封 | 有 code 的 404/409；超管闸 null code → JSON 500 | 失败句精度 **是**。不要发明业务码合同 |

问：Lead 若凭「十三方法名都在函数表里」把 (a) 标 covered、凭「Issued 带一次明文」把 (b) 标 covered，是否还要求读者能指着 **表无明文列、GET 不解密、create 先 404 再 generate、reset 不换 appKey、create 不踢会话、权限且超管闸**？这些是不是已经在 (b) 性状和 (a) 反例里，而不需要 L-032 的 HMAC、也不需要发明第二条 (b)？

## Q5 元问题

- objective_id: OBJ-31
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；单一 C4 容器 `wta-system` 的一份 Controller + 其 create/reset 性状
- difficulty: low

建议：`defer`

原因：

- **(a) 与磁盘十三映射 1:1；(b) 与 `create`/`reset` 密文四列、Issued 一次明文、Summary/列表永不回显对齐。** L 合同章节齐全（直觉、定义、机制链 1–5、带 caption / 文字等价物 / 图边界的两张 ASCII、正反例、变式、误区、来源表）。**主叙事站得住且与源码对齐**：classic 无 UseCase；自有 owner 只来自登录；他人窗权限串 **且** 超管闸；十扇 POST+双 false `@Log`；create 先查用户再 generate、409 拒重、不作废会话；reset 保持 `appKey`、现行 KEK 重加密、update + invalidate；GET/列表不解密；删除逻辑删释放活唯一键不释放 `app_key`；`openapi.enabled` 关掉则 Bean 不注册。不必为「窗口名字」再挖五问。
- 本批可粘贴的精度项已经落在课的表/误区/边界，或只需半句、不挡住把**两行**标 covered：（1）404/409 是 JSON `code`，HTTP 仍 200；（2）超管闸 `code=null` → JSON 500，不是 403；（3）他人 enable/disable/delete 同样 invalidate（表列省略）；（4）invalidator 抛错的库回滚靠 `@DSTransactional` 推断，单测无集成夹具；（5）AAD 有 0 字节分隔；（6）暂停「GET 两扇」应为三扇只读。不要把 catalog `/interfaces`、HMAC 验签、`createOpenApiService`、SSO BCrypt、OSS 正门塞进 38 分钟课。
- 不建议 `mine-more`：b01 未证明本课两格还有真实矩阵空洞；再五问只会把 Resolver.decrypt、Sa-Token 机器会话或 L-033 投影再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；信封精度与表列省略是可选半句，不是第二容器。不建议 `split-lesson`：未满 10 问；十三扇与 create/reset 性状是 chain 原设计的同一 OBJ-31、同一份 Controller，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 enable/delete 的 (b) 行：作废会话与逻辑删是 (a) 的解释到副作用。(b) 原文就是「密文存储、列表不回显」。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:SysOpenApiCredentialController.getSelfCredential, createSelf, resetSelf, enableSelf, disableSelf, deleteSelf, users, getUserCredential, createUser, resetUser, enableUser, disableUser, deleteUser | 十三扇路径/动词/权限、自有 owner 来自登录、他人窗超管闸、十扇 POST+安全 `@Log`、GET Summary / create-reset Issued+`no-store` / delete Void、classic 无 UseCase、无 `/interfaces`——均与源码对齐。残留精度：（1）空 GET 的 404 是 JSON `code`；（2）超管闸 JSON 500；（3）他人启停/删除表列没写 invalidate；（4）暂停「GET 两扇」vs 总结三扇。 | 可选半句：信封说「`R.fail(code)`，HTTP 仍 200」；超管闸「无 code → 500」；十三扇表他人写窗补「会作废会话」。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-031 来源表 S-L031-01…08）。 | defer（本行可 covered；精度项不挡盖章） |
| B:SysOpenApiCredentialController.create/reset | 密文四列 NOT NULL、无 `app_secret` 列、Issued 内存明文一次、Summary/GET/列表无秘密字段、create 先 404 再 generate、reset 保持 appKey、create 不 invalidate、reset 走 `updateAndInvalidate`、`no-store`——源码/单测可复现。残留：MockMvc 测试名只写 create；invalidator 失败回滚无集成夹具。 | 课误区 16 已钉 reset 返回类型。可选半句：回滚靠 `@DSTransactional`。Lead 可将本行标 `covered`。 | defer（本行可 covered；精度项不挡盖章） |
| A:SysOpenApiCatalogController.selfInterfaces, selfInterface, userInterfaces, userInterface | 同门牌另一份类。课已划负空间。 | 保持。 | defer（L-030） |
| B:OpenApiSigner / OpenApiCanonicalizer；B:SaTokenOpenApiMachineSessionOperations；B:RedissonOpenApiNonceStore / RedissonOpenApiRateLimiter | Resolver.decrypt 与 invalidate 函数接口只作下游。 | 不要并进 OBJ-31。 | defer（L-032） |
| A:createOpenApiService | 厨房 URL / `no-store` / Summary 拒 secret 已作旁证。 | 保持。全表 L-033。 | defer（L-033） |
| A:SysSsoAppController.rotateSecret / SysClientController.rotateSsoSecret | BCrypt vs GCM 对照一句。 | 保持。 | defer（L-018） |
| A:SysOssConfigController.* / B:ISysOssConfigService.updateOssConfigStatus | `status` Y/N 正门对照一句。 | 保持。 | defer（L-025） |
| C:MySQL 8.4 / mysql-base | 表/生成列/两 unique 已点名。 | 不要因本课标 covered。 | defer（L-085） |
| C:Redis 8 | invalidate 是写路径副作用，不是会话容器课。 | 保持。 | defer（L-084 / L-032） |
| SysUserServiceImpl / SysRoleServiceImpl invalidate | 邻居踢会话入口。 | 边界一句已有。 | defer（L-015 / L-016） |
| (d) OpenAPI 凭据流 | 矩阵无此行。 | 不要发明。签发/回显是 (b)，机器验签是 L-032。 | defer（未入库） |
| (b) enable/disable/delete 或 invalidateByUserId | 矩阵无此行。课已把副作用写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| API-005 变更 POST+`@Log` | 十扇符合；无 PUT/DELETE。 | 本课盘点，不发动 Ratchet。 | defer（传输 Ratchet） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本行无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出十三扇目的、create 主路径（校验用户 → generate → 密文落库 → Issued）与失败路径（停用用户 404 且不 generate、已有活行 409）、reset 主路径（保持 appKey、再给信封、踢会话）；把可选信封半句写进原课不会超过 40 分钟；目录窗、HMAC、前端工厂、SSO 轮换、OSS 正门、MySQL/Redis 容器已有后续课或范围外 defer。满 10 问前不拆。十三扇与 create/reset 仍是同一 `wta-system` 容器的一份 Controller。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-030 目录、L-032 网关、L-033 工厂、L-018 SSO、L-025 OSS 正门、L-015/L-016 邻居踢会话、MySQL/Redis 容器、未入库 (b)/(d) 以及 API-005 显式建议 `defer`；对 `A:SysOpenApiCredentialController.*` 与 `B:SysOpenApiCredentialController.create/reset` 建议 Lead 标 `covered`（证据锚本 probe 与 L-031 来源表）。chain 覆盖列若要当 (b) 的证据，由 Lead 补上本行名字。本课不主张改 catalog 窗、HMAC、前端工厂、SSO/OSS 邻格或任何 (c)(d) 格子。
