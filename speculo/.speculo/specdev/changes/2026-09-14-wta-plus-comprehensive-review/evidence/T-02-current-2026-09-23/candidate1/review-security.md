# T-02 C1 安全／规格轴固定源码评审

**固定输入**：base `c16966167526f9b6ab6eb213265034b3bbe53e46` → head `617a3693ddb2897851ab46d4131690898df6a690`，tree `a8d1181dfc950d6edf6d94a9cc1c42e10e88da00`；中间测试先行提交 `8a88965363ca295122efadabf0bc97e99c6704c4`。实际 `git diff c169661...617a3693 -- backend` 为 `/tmp/wta-t02-c1/path-audit.json` 的 7 个产品／测试路径，`git diff --check` 退出 0。只用固定 SHA 的 `git show`/diff 审产品，不混当前治理工作树。本轴根据 Ticket T-02、Packet revision161、AC-002 及 engineering-standards 安全规则审查；未运行 Maven、MySQL、HTTP 或服务。

**安全／规格源码结论：PASS，无已证实的 C1 阻断 finding。** 这个结论覆盖固定源码对真实 tokenId、两类 URI 日志副本和原业务路径的修正；Lead 后续已完成同 SHA 的 7 类真实 HTTP/MySQL 验收，39 tests／0 failure／0 error／0 skip、隔离资源全清、source 前后 clean（详见下节）。11 类消费者（含 HTTPS／签名／SSE）仍在运行，不能据 39 项结果称整票 Done。旧 T-02.md 已按 Lead 记录另存原字节历史，本审查没有改历史或重判旧时点。

## AC-002 源码核对

1. **有效 token 的响应日志副本**：`backend/wta-common/wta-common-json/src/main/java/org/namewta/common/json/utils/LogSanitizer.java:21-29` 新增规范化名 `tokenid`。同类已有 `token`、`accesstoken` 等。`LogSanitizer.redact:153-160` 在独立 JSON 树副本中置 `[REDACTED]`；`SysLogBodySanitizer` 与 `SysLogFilter` 既有 JSON 响应捕获调用该策略。原 `UserLoginSuccessListener` 将签发 tokenValue 写入在线 DTO，`SysUserOnlineController` 正常两个 GET 会返回 `SysUserOnline.tokenId`；这些未改业务类的 HTTP 响应仍保留可操作 token，而日志正文不再复制它。`LogSanitizerTest:105-116` 与 `SysLogFilterTest:99-128` 同时断言 canary 从日志消失、普通 `userName`／业务 `code` 保留、原对象和响应保留 token。
2. **两条正常强退 URL**：`LogSanitizer.java:118-151` 仅在应用内 `/monitor/online/<token>` 与 `/monitor/online/myself/<token>` 单动态段遮蔽 token；静态 `/monitor/online/list`、`/monitor/online` 和无关路径保留。`backend/wta-common/wta-common-web/src/main/java/org/namewta/common/web/logging/SysLogFilter.java:299-315,354-370` 只在 HTTP 请求／响应事件的 `path` 值调用该函数；其保存的原应用路径继续供正文敏感字段策略及业务 Filter/Servlet 使用。`LogSanitizerTest` 含尾斜线、token 后 matrix 参数和普通路径断言；`SysLogFilterTest` 验证两事件遮蔽、业务 request URI 原值不变。方法未改 `HttpServletRequest`、Controller 路由、踢除 token、签名输入或实际 HTTP 正文。
3. **异常 URI 与 context path**：`backend/wta-common/wta-common-web/src/main/java/org/namewta/common/web/handler/GlobalExceptionHandler.java:52-173,255-287` 将原来直接写入 logger 的 URI 改为 `safeRequestUri`；它先验证并剥离实际 `request.getContextPath()`，对应用内路径调用同一策略，再将 context 作为日志前缀接回；URI/context 不一致时固定 `[REDACTED]`，故不会把含 token 的未解析 URI回退原文。`SysLogFailureRedactionTest` 用 `/app` 测两个动态路由及静态 list；在真实根 context 的 HTTP/MySQL组合测试中还捕获两个错误日志。`GlobalExceptionHandler:137-145` 的 IOException SSE `contains(message.path)` 判断明确仍使用 `request.getRequestURI()` 原值，只有日志参数改安全副本。处理方法的响应内容／状态代码未被本次改动。
4. **OperLogEvent 与数据库**：既有 `LogAspect:96-98` 使用 `HandlerMapping.BEST_MATCHING_PATTERN_ATTRIBUTE`，两在线 POST 的 `@Log` 关闭请求／响应数据保存；本次没有把完整请求 URI 添加到事件。`LogRedactionHttpMySqlIntegrationTest:305-356` 的隔离用例断言两个 `OperLogEvent.operUrl` 是 `/{tokenId}` 模板、MySQL 行无 canary 且操作者／标题／耗时可观测。无证据需为了这一源路径改 `SysOperLogServiceImpl` 或 LogAspect；不存在“HTTP URL 泄漏必然已在 OperLog DB”的推断。

## 测试证据与限制

`/tmp/wta-t02/red-behavior-assessment.json` 是 **8a88965 测试先行时**的 clean 红灯：35 项、1 failure、0 error、0 skip，目标 `onlineDeviceTokenFromRealIssuerNeverReachesHttpOrErrorAudit` 第 336 行在 HTTP 审计仍含真实有效 token 后失败；之前有效会话与踢除正控制已过。首个失败会中止后面的错误日志、OperLogEvent 与 DB 断言，因此红灯不能声称这些后续 sink 当时执行过。C1 增量代码针对该失败源和另外两条已由源码证明的出口；**目前没有 C1 真实绿灯证据**。

Lead 提供的 `/tmp/wta-t02-c1/real-http-mysql-counts.json` 记录固定 C1 run `7c481ffc58536e6c`：7 类合计 39 tests／0 failure／0 error／0 skip，包含原红灯用例及后续 `OperLogEvent`、真实 MySQL、错误日志断言；source_before/source_after 均为 `617a3693`／tree `a8d1181` 且 clean，owned Maven 进程组、容器、匿名卷和 loopback 32815 均清理，result `acceptance=true`。这是 Lead 的实际运行证据，本轴未自行重跑。新增集成测试使用 Sa-Token `LoginHelper.login` 签发并通过 `StpUtil.getLoginIdByToken`／踢除前后状态证明 token 有效，在 HTTP 响应中先正向断言 canary，随后断言 HTTP/事件/DB/错误日志没有 canary；所有敏感值均为隔离合成。测试的 Jetty servlet 手工装配 `SysUserOnline` 响应、调用真实 Controller 的 LogAspect 代理，未运行真实 `UserLoginSuccessListener` 从 Redis 取 DTO，也未经过完整 Spring MVC 权限／RepeatSubmit 拦截链。该差异不否定已核实的生产源码数据流，但不能把此测试单独称为整套认证 E2E。异常 context path 另由 `SysLogFailureRedactionTest` 覆盖；原有 SSO 换票、回执验签、SSE、加解密与正常响应是否回归须看 Lead 当前 11 类消费者实际计数。`T02_MYSQL_PASSWORD` 环境缺失时集成测试会 assume-skip，不能计入 required 通过；本次 39 项无 skip。

**残余边界**：本策略只针对正常有效的两条在线 token 路由，未把任意用户可控 `upstreamRequestId`／普通元数据拼接 secret 的假设变成无限脱敏要求。单段匹配对多余路径段返回原路径；当前前端 `monitor/index.ts:121-124` 构造的是精确单段 URL，未见生产来源会把有效 token 加多余段后当作正常强退请求。`/monitor/online/list;...` 可能被当作动态段而少记静态路径，但本次已覆盖应用正常 `/list`，这不是凭据外泄。旧日志处置及现场凭据轮换不由本票源码证明。
