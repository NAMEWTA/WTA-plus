---
artifact: learning-goal-probe
probe_id: GP-L-063-b01
lesson_id: L-063
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-063 / b01

审问对象是已写成的 Lesson `children/2026-09-14-wta-third/lessons/L-004-third-observability.md`（chain 全局 ID L-063 / OBJ-63，子课号仍是 L-004 / OBJ-04）与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（派单 **Controller + UseCase**；chain 覆盖列 **只印一格 (a)**）：`A:ThirdObservabilityController.invocationList,statisticsList`。OBJ-63 原文：能口述 **invocation/statistics 查询**。档是**解释+走查**。child 课 OBJ-04 把同一窗收成 `ThirdObservabilityUseCaseImpl` → `ThirdObservabilityService` → invocation/statistic DAO。child 头 **未**列 `claimed_cells`。矩阵 (a) **两行**均仍 `uncovered`：Controller 行写成 `invocation/list, statistics/list`（与磁盘两扇 URL 1:1，但 Java 方法名是 `invocations` / `statistics`，**没有** `invocationList()`）；第二行只写类名 `ThirdObservabilityUseCase`，模块 `wta-third`。矩阵 (b) **没有**观测独行；已有 `ThirdPartyGateway.execute`（口号「出站 HTTP、限流、记 invocation」）仍 `uncovered`，挂 **L-064**；`ThirdCredential 保存` 已 covered（L-062）。矩阵 (c) `wta-modules` 业务房间 / `layered-classic` 已 covered（L-002/L-003）；登记表 **未点名** `wta-third`（未登记默认 layered；S-3RD-10）；MySQL 8.4 / `dao-mapper-ownership` 仍 uncovered。矩阵 (d) 「主路径 third 出站」「失败路径 third HTTP」仍 uncovered，备注 **L-064**。本批按派单审问 **两扇只读 GET + UseCase 只读合同**；口试性状以磁盘 `observabilityService` 类型是 UseCase、根路径 `/third` 不含 `observability`、invocation 近 7 天/200、statistic 200 不截 7 天、读路径从不 insert 为准。写路径（记录器/脱敏/健康）是对照门，**不要**等改 chain，也 **不要**用本课把 (d) 或 `execute` 标 covered。Provider 六扇留给 L-060；Endpoint 六扇留给 L-061；Credential 三扇留给 L-062；`execute` / `record` / 失败分类留给 L-064；厨房 `listInvocations|listStatistics` 留给 L-065；两页 `editable===false` 留给 L-066。通知看板 L-049 是另一模块。

抽查锚点（2026-09-17）：`backend/wta-modules/wta-third/src/main/java/org/namewta/third/controller/admin/ThirdObservabilityController.java`；`usecase/ThirdObservabilityUseCase.java`；`usecase/impl/ThirdObservabilityUseCaseImpl.java`；`service/ThirdObservabilityService.java`；`dao/{ThirdInvocationDao,ThirdStatisticDao}.java`；`mapper/{ThirdInvocationMapper,ThirdStatisticMapper}.java`；`domain/{ThirdInvocation,ThirdStatistic,vo/ThirdInvocationVo,vo/ThirdStatisticVo}.java`；`port/{ThirdInvocationRecorderPort,ThirdInvocationStore,ThirdStatisticStore,ThirdOutboundAttempt}.java`；旁证（格子归 L-064）`adapter/observability/{ThirdInvocationRecorderAdapter,ThirdInvocationLogHealthIndicator}.java`、`adapter/log/ThirdLogSanitizerAdapter.java`、`adapter/gateway/ThirdGatewayAdapter.java` 360–374；`wta-api/.../third/api/{ThirdPartyResponse,ThirdPartyFailureCategory}.java`；测试 `controller/admin/ThirdAdminPermissionHttpTest.java`（**零** `ThirdObservabilityController*Test` / `ThirdObservabilityUseCase*Test` / `ThirdObservabilityService*Test`；401 夹具打了 **两扇** GET）；旁证 `adapter/observability/ThirdInvocationRecorderAdapterTest.java`（L-064 写路径）；DDL `10-cde-base-ddl.sql` `third_invocation` / `third_statistic`；DML `50-cde-base-dml.sql` 两串 `third:invocation:list` / `third:statistics:list`；`resources/mapper/third/ThirdMapper.xml` 只服务 Provider；登记表 `03-backend-module-modes.md` **未逐行点名** `wta-third`；Skill `modules/third/index.md` 把 observability 写进 service；旁证（格子归 L-065/L-066）`frontend/packages/domains/third/src/index.ts` `listInvocations|listStatistics`、`web-domains/third/src/index.ts` 两页只冻 `list` 权；`frontend/packages/api-contracts/generated/openapi.ts` `/third/invocation/list` / `/third/statistics/list`。

## Q1 澄清

- objective_id: OBJ-63
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-63；child course OBJ-04；chain L-063 覆盖列只 A:invocationList,statisticsList；矩阵 (a) Controller 行 + UseCase 行仍 uncovered；(b) 无观测独行、execute 挂 L-064；(d) 出站/失败 HTTP 只挂 L-064；负空间 L-060/L-061/L-062/L-064/L-065/L-066/L-049
- difficulty: medium

课把观测柜台收成「监控室只能看回放，不能替你打电话」。计划里至少有五套可数对象，派单点名的是 **Controller 两扇 + UseCase**：

1. **OBJ-63 原文。** `course.md`：能口述 invocation/statistics **查询**。档是 **解释+走查**，不是 OBJ-62 那种方法性状，也不是 OBJ-64 的 `execute`。一句里是 **两扇只读列表**，没有把 POST 重放、分页 API、出站失败分类、厨房两枪、两页 `editable` 写成口试清单。
2. **chain / 子课。** 覆盖列一格：`A:ThirdObservabilityController.invocationList,statisticsList`。不写 UseCase，不写 (b)，不写 fail-third-http，不写 createThirdService。子课号仍是 L-004，child 头无 `claimed_cells`。名字陷阱：chain 驼峰 **`invocationList`/`statisticsList`**，矩阵斜杠 **`invocation/list`/`statistics/list`**，Java 方法与 OpenAPI 操作名都是 **`invocations`/`statistics`**，HTTP 是 `GET /third/invocation/list` 与 `GET /third/statistics/list`。URL **不**出现 `observability`。
3. **矩阵对照。** (a) Controller 行两段路径与磁盘两扇 1:1，**不会**把 L-060 六扇或 L-064 `execute` 假闭合。(a) UseCase 行只写类名；磁盘接口两方法 `invocations`/`statistics`，唯一实现 `ThirdObservabilityUseCaseImpl`。矩阵 **没有** `findRecent` / 7 天 / 200 / 脱敏落库独行。(b) `ThirdPartyGateway.execute` 口号含「记 invocation」，那是 **写** 路径，挂 L-064。(d) 出站走廊挂 L-064，不是本课覆盖列。
4. **课自己的操作定义。** 门牌根路径 `/third`（不是 `/third/observability`）。Controller 字段叫 **`observabilityService`**，类型是 `ThirdObservabilityUseCase`。两映射：`GET /invocation/list` 权限 `third:invocation:list`；`GET /statistics/list` 权限 `third:statistics:list`。都无 `@Log`，都 `providerCode` `required=false`，都回 `R.ok(...)`。UseCase 注释 *Read-only management contract*。UseCaseImpl 字段 `service`；**无** `@DSTransactional`。Service 把空白 `providerCode` 收成 `null`；invocation 另截 `now-7d` 且 SQL `limit 200`；statistic 只 `limit 200`、**不**截 7 天。读路径 `new Vo(...)`，从不 insert。
5. **邻格。** L-060/L-061 供应商与门是六扇、字段 `useCase`、有 get/status/`edit`。L-062 凭据三扇、字段 `credentialUseCase`、写路径有事务。L-064 才 `execute` → `record`/`recordAttempt` → 失败分类入库。L-065 厨房 `listInvocations`/`listStatistics` 类型上 `providerCode: string` 必填，页面空着也传 `''`。L-066 两页 `editable===false`，不画 7 天/200 筛选器。L-049 通知看板是 `/notify/monitor` 另一模块。
6. **未入库。** 矩阵 **没有** `ThirdObservabilityService` 独行、没有 Recorder/Sanitizer/Health (a) 行、没有 `findRecent` (b)、没有「失败路径 管理读」第二 (d)、没有 `third:observability:*` 权限串。

问：本两格 (a) 闭合标准是「两扇 URL/动词/两串权限 + 字段名 `observabilityService` 类型是 UseCase + Java `invocations` 对 chain `invocationList` + 根路径 `/third` 不含 observability + UseCase 只读无事务 + 读路径从不 insert + invocation 7 天/200、statistic 200 不截 7 天，且不把 Service/记录器升成第三张 (a)」，还是 OBJ 那句必须先把 (d) 出站走廊标 covered、或必须把 UseCase 行拆成 Impl 与 Service 两行、或必须等 GP-L-064/L-065 才给两扇盖章？Lead 若只凭「`ThirdObservabilityController` 四字在函数表里」把 L-064 `execute`/`record` 或 L-065 厨房标 covered，是否假闭合了出站走廊与前端工厂？Lead 若因 chain 没写 UseCase 就拒盖矩阵第二行，是否把派单的第二格空着？

## Q2 证据

- objective_id: OBJ-63
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 12–28；UseCase 8–12；Impl 12–25；Service 13–32；InvocationDao 14–19；StatisticDao 13–18；InvocationMapper 16–24；StatisticMapper 16–23；InvocationVo 5–8；StatisticVo 5–7；Invocation 11–24；Statistic 12–21；RecorderPort 8–19；Store 5–8 / 5–7；OutboundAttempt 9–22；Recorder 27–123；Sanitizer 14–15 / 66–76 / 88–96；Health 8–18；Gateway 360–374；ThirdPartyResponse 13–15；FailureCategory 4–16；DDL 1387–1410；DML 1649–1650；PermissionHttpTest 31–52；RecorderAdapterTest 32–111；ThirdMapper.xml 1–8；modes.md 12；skill third index 12–14；openapi 3924–3990 / 22364–22487；frontend index.ts 24–26 / 33 / 55–56；web-domain index.ts 13–20；L-066 259–262
- difficulty: high

课的两扇窗 + UseCase 哪些能在工作树直接复现，哪些只是课把写路径说成本课第三扇、或把邻格 `execute` 算进查询窗？

**仍能复现（两格 (a) + 查询窗 7 天/200）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 一份类、根路径 `/third`；**不**继承 `BaseController`；只注入 `ThirdObservabilityUseCase observabilityService` | Controller 12–16 |
| 两扇：GET `/invocation/list`、GET `/statistics/list`；无 POST/PUT/DELETE、无 `/{id}`、无 `/save`、无 `/observability` | Controller 18–28；openapi 3924–3990 |
| 权限两串 `third:invocation:list` / `third:statistics:list` 与 DML 菜单行 1:1；无第三串 `observability` | Controller 19 / 25；DML 1649–1650 |
| 两扇都 `providerCode` `required=false`；无 `@Log`；回 `R<?>` | Controller 18–27 |
| 未登录打两扇都在进 UseCase 前 401；`verifyNoInteractions(observabilityUseCase)` | PermissionHttpTest 49–52 |
| 接口两方法 `invocations`/`statistics`；注释只读合同；模块内唯一 Impl | UseCase 8–12；Impl 14 |
| Impl 唯一字段 `service`；两方法各一行委托；**无** `@DSTransactional` | Impl 15–25 |
| Service 两字段：`invocationDao` / `statisticDao`；`normalize`：null 或 blank → null，否则 `strip` | Service 16–17 / 30–32 |
| invocation：`findRecent(code, now.minusDays(7))` → 12 组件 Vo；**不映射** `providerErrorCode` | Service 19–22；Vo 5–8；Entity 20 有该列 |
| statistic：`findRecent(code)` **不传时间**；9 组件 Vo；**没有** `statisticId` | Service 25–27；Vo 5–7；Entity 12 有 id |
| `findRecent` 是 DAO 额外方法；Store 端口 **只有** `upsert` | InvocationDao 16–19；Store 6–8；StatisticDao 16–18 |
| Mapper `@SelectProvider` 拼串：供应商 null 不加 where；invocation 仍有 `create_time >= #{from}` + `limit 200`；statistic 全表最近 200 | InvocationMapper 19–20；StatisticMapper 18–19 |
| XML 命名空间是 Provider，**没有** invocation/statistic 语句 | ThirdMapper.xml 3 |
| 表 `third_invocation`：`uk_third_invocation_request(request_id)`；JSON 列是已脱敏 | DDL 1387–1398 |
| 表 `third_statistic`：生成列 `endpoint_dimension`；唯一 `(provider_code, endpoint_dimension, stat_date)`；check 禁止空串门码 | DDL 1400–1410 |
| 厨房 `Invocation`/`Statistic` 字段与 Vo 对齐（无 `providerErrorCode` / 无 `statisticId`）；两枪 GET 打本前缀 | index.ts 24–26 / 55–56（格子归 L-065） |
| 两页只冻 `list` 权；`kind` invocations/statistics 时 `editable===false` | web-domain 19–20；L-066 259–262（格子归 L-066） |

**邻格仍能复现（不计入本两行 (a) 闭合）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| Provider/Endpoint 字段叫 `useCase`；六扇含 get/status/`edit` | ProviderController（L-060）；EndpointController（L-061） |
| Credential 字段 `credentialUseCase`；写路径有事务；list 必填 `providerCode` | CredentialController（L-062） |
| 出站 `record` 先 upsert 调用行再 upsert **两行**统计（门级 + `endpointCode=null` 供应商级）；`quotaValue` 恒 `0L` | Recorder 52–65 / 116–122（L-064） |
| `recordAttempt` 只写 `THIRD_HTTP_ATTEMPT_START/FINISH` 日志事件，不写 invocation 表 | Recorder 82–105；OutboundAttempt 9–22 |
| 成功定义：`category == NONE` 且 HTTP 2xx | ThirdPartyResponse 13–15 |
| timeout 计数仅 `TIMEOUT`；rejected 计数 `RATE_LIMITED` 或 `REJECTED` | Recorder 120–121；FailureCategory 4–16 |
| 网关 catch：*must not change the synchronous gateway result* | Gateway 360–374 |
| 健康 Bean 名 `thirdOutboundLog`；只看 `logSinkFailureCount`，不读 DAO | Health 8–18；RecorderAdapterTest 97–111 |
| 脱敏 BLOCKED 子串 + 额外精确字段名；截断 16KiB；`jsonValue` 才入库 | Sanitizer 14–15 / 66–76 / 92–96 |
| 厨房 TypeScript `providerCode: string` 必填；OpenAPI query 也写成必填 string | index.ts 33；openapi 22364–22461（L-065 合同 smearing） |

**课写过、测试名单更窄或口试标签写偏（不是证伪 A）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「Skill 把 observability 写进 service」 | Skill index 12–14 把 gateway **和** observability 都写进 service。Java **读**是 `ThirdObservabilityService`，**写**是 `adapter/observability/ThirdInvocationRecorderAdapter`。课已说「按 Java 教」 |
| 「登记表 layered 行」 | `03-backend-module-modes.md` **没有** `wta-third` 行；未登记默认 layered（S-3RD-10；modes 12） |
| 「管理读会看到脱敏 JSON」 | Service 只 `new Vo(...)`。脱敏发生在写入。Recorder 测试钉 `***`，**不是** ObservabilityService 测试 |
| 「同一次逻辑调用多次 record 会覆盖」 | Mapper upsert **不**更新 `sanitized_request_json` / `provider_error_code` / `create_time`（InvocationMapper 23）。课没写成第一句 |
| 「实体有 `providerErrorCode`，Vo 丢掉」 | Recorder `record` **从不** `setProviderErrorCode`，列多半是 null。丢掉是读模型事实；写入侧更窄 |
| OpenAPI / 厨房把 `providerCode` 画成必填 | Java `required=false`；空白进 Service 变 null。省略查询参数在绑定阶段仍进 UseCase（与 L-062 list 必填相反） |
| 无 Controller / UseCase / Service 单测 | 两扇映射证人是注解 + OpenAPI；401 夹具覆盖两扇 GET，**没**打空白 `providerCode`、没断言 7 天 SQL |
| 返回类型 `R<?>` / OpenAPI `RObject` | 不是 `R<List<ThirdInvocationVo>>`。口试不要把生成合同当成分页 Page |

问：在两扇 URL/权限、`invocationList`≠`invocations` 命名、字段 `observabilityService`、根路径不含 observability、UseCase 无事务、Store 端口只有 upsert、Vo 丢掉 `providerErrorCode`/`statisticId`、7 天只绑 invocation、statistic 仍 limit 200 已经能互证的前提下，两格 (a) 是否还要把「OpenAPI 把 providerCode 画成必填」「upsert 不改请求 JSON」「记录器从不写 providerErrorCode」写成开篇半句才算讲完？7 天/200 是不是已经在机制链里，不必发明 (b)、也不必等 L-064 的 `record`？(d) 出站走廊是否根本不在本课覆盖列？

## Q3 假设与反例

- objective_id: OBJ-63
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: Controller 14–28；UseCase 8–12 vs Impl 15–25；Service 19–32；Dao 16–19 vs Store 6–8；Vo 5–8 / 5–7；Mapper 19–23 / 18–23；Health 8–18；Gateway 360–374；Recorder 52–105 / 122；openapi 22364–22461；index.ts 33 / 55–56；modes.md 12
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪条走廊认错？

1. **「Controller 字段和其他切片一样叫 `useCase`。」** 本窗叫 `observabilityService`。课误区 1。类型是 UseCase；真注入 Service 就跳过管家层。
2. **「矩阵写 `invocation/list`，所以 Java 有 `invocationList()` / 根路径 `/third/observability`。」** 方法是 `invocations`/`statistics`；根是 `/third`。chain 驼峰是第三套名字。
3. **「statistics URL 也是单数 `statistic`。」** HTTP、权限、厨房、菜单组件都是 **statistics** 复数。课 L-065 已钉拼写；本窗必须能指着磁盘。
4. **「`findRecent` 是分页 API。」** 是 `limit 200` 截断列表，没有 page/size。课误区 2。
5. **「统计墙也截近 7 天。」** 只有 invocation 传 `from`。statistic SQL 无时间下界，旧日行只会被 200 条挤出。课变式 C。
6. **「不传 providerCode 会 400。」** Java `required=false`；normalize 后 SQL 不加供应商条件。厨房/OpenAPI 把参数画成必填，是邻格 smearing，不是本窗绑定闸。
7. **「传空白字符串与省略参数不同：一个全局、一个失败。」** 两者都变 `null`。与 L-062「省略 providerCode 到不了 Service」相反。
8. **「管理读会再脱敏一次。」** Service 只构造 Vo。打码在 Recorder + Sanitizer。课误区 4。
9. **「Vo 里能看到 `providerErrorCode` / `statisticId`。」** 实体有，读模型丢掉。
10. **「给观测 Controller 加 POST 重放。」** 工作树没有。重放等于新的 `ThirdPartyGateway.execute`，走 L-064 再写新回放。课反例 1。
11. **「UseCaseImpl 还要开事务。」** 读路径无 `@DSTransactional`。写入在记录器各自 upsert，不经过 Observability UseCase。课反例 2。
12. **「DAO 只实现 Store，所以 `findRecent` 在端口上。」** Store 只有 `upsert`。管理读走 DAO 额外方法。写协作方看到的是端口类型，读协作方看到的是 DAO 类型。
13. **「一次物理重试会在管理列表里变成三行。」** 管理 invocation 仍是 **一行**，`attemptCount` 反映逻辑次数；START/FINISH 只在日志槽。课变式 B。
14. **「统计表按次覆盖。」** upsert 对计数列是 `+ values(...)` 累加；`quota_value` 取新值且恒写 0。课误区 3。
15. **「列表里看见 `quotaValue=0` 表示对接了供应商配额 API。」** 记录器硬编码 `0L`。课边界。
16. **「记录器失败应让调用失败，否则丢审计。」** 网关合同相反：出站分类稳定优先。课误区 5。
17. **「健康 down 表示表没写上。」** 只表示 `sinkFailures != 0`。DAO upsert 失败走另一条 warn，不进该计数。
18. **「本课覆盖 `execute`/`record`，所以 (d) 出站可 covered。」** 写路径只为指认「另一扇门」。把假设反转后，Lead 会用本行假闭合 L-064。
19. **「厨房 `listInvocations` 是第三扇管理窗。」** 工厂格子归 L-065。HTTP 仍是本两扇；类型要求 `providerCode: string` 正好旁证「页面空着也传 `''`」。
20. **「两页能选日期范围 / 改 200 上限。」** L-066 不画这些控件。截断是后窗合同。
21. **「登记表没有 `wta-third` 行，所以本窗是 classic、Controller 可直连 Mapper。」** 未登记默认 layered；磁盘五层完整。S-3RD-10。
22. **「矩阵要先有 Service/Recorder (a) 独行才算讲完。」** 矩阵无此行。7 天/200/只读是 (a) 的 explained；`record` 是 L-064 的 explained。
23. **「通知 `NotificationMonitorController` 也是监控室，可以并进本课。」** L-049 是 `/notify/monitor`。本窗是 `/third/invocation|statistics`。
24. **「同一天同一门两行统计是 bug。」** 记录器写门级一行 + 供应商级一行（`endpointCode=null`，生成列空维度）。课正例 2。

问：读完后仍说不出的，是不是「两个方法名」，而是 **`invocationList` 对 `invocations`、字段 `observabilityService`、根路径不含 observability、两串权限资源名不同、UseCase 无事务、Store 不承载 findRecent、invocation 7 天/200 而 statistic 只有 200、Vo 无秘密也无 providerErrorCode、读路径从不 insert、`execute`/`record` 不在本窗、(d) 出站不在本课**？

## Q4 视角

- objective_id: OBJ-63
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-63 vs L-060/L-061/L-062/L-064/L-065/L-066/L-049/L-083/L-085；chain 只 A:invocationList,statisticsList
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:ThirdObservabilityController.invocation/list, statistics/list`** 或 **`A:ThirdObservabilityUseCase`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) Controller 两路径 + UseCase 类名；(b) 无观测独行；execute「记 invocation」挂 L-064；(d) 出站挂 L-064；chain 没写 UseCase | **是** 闭合定义。两行 (a) 可单独 covered。不要发明 (b)。(d) 整行 **否** |
| 两扇窗作者 | 两映射、两权限、无 `@Log`、字段 `observabilityService`、根 `/third`、无 POST | **是** 第一格。只读门牌半句 **是** |
| 用例电工 | 两方法委托；无 TX；只持 `service`；接口注释只读 | **是** 第二格。Service/DAO/Mapper 升格第三法 **否** |
| 查询窗（7 天/200） | invocation `from=now-7d` + limit 200；statistic 无时间窗 + limit 200；normalize 空白→null | **是** 已写入机制链的 explained。不要发明 (b) |
| 读模型柜员 | Vo 12/9 组件；丢掉 `providerErrorCode`/`statisticId`；JSON 已是 sanitized | **是** (a) 列表形状。写入打码 **否**（L-064） |
| 写路径对照 | Recorder upsert ×1 调用 + ×2 统计；Sanitizer；Health sinkFailures | 指认「另一扇门」**是**。Recorder/(d)/execute 盖章 **否** |
| 供应商柜员（L-060） | 六扇、字段 `useCase`、有 get/status/`edit` | 对照「本窗没有那些扇」**是**。六扇 **否** |
| 门牌柜员（L-061） | Endpoint 六扇；`sensitiveFieldsJson` 作为额外脱敏字段名 | 字段名来源对照 **是**。Endpoint CRUD **否** |
| 凭据柜员（L-062） | 三扇、`credentialUseCase`、写路径有事务、list 必填 providerCode | 对照「本窗 optional + 无事务」**是**。加密/软删 **否** |
| 出站大门（L-064） | `execute` → 限流/凭证/HTTP/SPI → `record`/`recordAttempt`；失败分类 | 对照一句「读不写、写失败不改响应」**是**。走廊盖章 / fail-third-http **否** |
| 厨房（L-065） | `listInvocations` / `listStatistics`；TS 必填 providerCode；无投影器 | URL 负空间与类型旁证 **是**。工厂格子 **否** |
| 两页（L-066） | `editable===false`；不画 7 天/200 筛选 | 页面负空间 **是**。菜单格子 **否** |
| 通知看板（L-049） | `/notify/monitor` snapshot/deliveries | 只读监控类比一句 **是**。并课 **否** |
| MySQL 8.4 / dao-mapper | 两表、request_id 唯一、生成列维度、DAO 持 Mapper、无 XML | 本窗表/SQL **是**。容器课 **否**（L-085/L-083） |
| 测试夹具行 | `*Test.java` 矩阵已 deferred | 401 两扇 + Recorder 脱敏证人 **是**。不要把夹具行标 covered |
| (b) OpenAPI 必填 smearing / upsert 不改请求 JSON | 矩阵无此行 | 课已写 optional/覆盖。不要发明第二 (b) |

问：Lead 若凭「两个方法名都在函数表里」把第一行 (a) 标 covered，是否还要求读者能指着 **`invocationList` 对 `invocations`、`observabilityService`、根路径不含 observability、两串权限、无 POST/无事务、Vo 无 providerErrorCode、7 天只绑 invocation、读路径从不 insert**？这些是不是已经在本格机制链/反例里？第二行 UseCase 是不是同一条河的只读章，可以凭本课盖章而不是等改 chain？7 天/200 是不是同一台机器、不必等 L-064？(d) 是不是明确「本课只保证管理读窗，出站归 L-064、不得整行假闭合」？

## Q5 元问题

- objective_id: OBJ-63
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 两行、无观测 (b)、(d) 出站挂 L-064；chain 只印 Controller 驼峰两名
- difficulty: low

建议：`defer`

原因：

- **两格 (a) 与矩阵 / OBJ-63 / 子课 L-004 1:1；chain 覆盖列少印了 UseCase 且用了第三套方法名，但不缺机制。** 子课合同章节齐全（宏观地图、直觉、定义表、机制链两条 GET + 写路径对照、带 caption / 文字等价物 / 图边界的读写分叉图、正反例、变式、误区、暂停走查、来源表 S-3RD-04/S-3RD-05/S-3RD-06/S-3RD-08）。**主叙事站得住且与源码对齐**：两扇 GET、字段 `observabilityService`、Java `invocations`/`statistics` 对 chain `invocationList`/`statisticsList`、根路径 `/third` 不含 observability、UseCase 只读无事务、Store 端口只有 upsert、Vo 丢掉 `providerErrorCode`/`statisticId`、invocation 近 7 天/200、statistic 200 不截 7 天、读路径从不 insert、写失败不改网关响应。不必为「两个方法名」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例或邻格，不挡住把 **两行 (a)** 标 covered：（1）OpenAPI/厨房把 `providerCode` 画成必填，Java `required=false`；（2）invocation upsert 不更新 `sanitized_request_json`；（3）记录器从不 `setProviderErrorCode`；（4）登记表未逐行点名 `wta-third`；（5）无 Controller/UseCase/Service 单测，401 夹具覆盖两扇 GET；（6）Skill 把 observability 写进 service，Java 写在 adapter。不要把 `execute`、厨房工厂、Provider/Endpoint/Credential 六扇/三扇、通知看板塞进本两行。
- **不要为本课发明 (b) 行，也不要等 GP-L-064。** 矩阵没有「7 天/200 / 只读 / 脱敏落库」独行。磁盘就是 Service `minusDays(7)` + Mapper `limit 200`、读路径 `new Vo`、写入才 sanitizer。chain 覆盖列只写 (a)；7 天/200 的证据锚是本课机制链 + 本 probe，不是先改 chain。miner 不改 chain。`ThirdPartyGateway.execute`「记 invocation」是写路径，保持 L-064。
- **不要用本课把 (d) 整行标 covered，也不要等 (d) 才给两行 (a) 盖章。** 矩阵「主路径 third 出站」「失败路径 third HTTP」明确挂 L-064。本课只保证管理读半段：可选 providerCode → findRecent → Vo；列表已是脱敏 JSON；没有 POST。出站 `execute` / `record` / 失败分类 / 吞异常是邻格。
- 不建议 `mine-more`：b01 未证明两行还有真实矩阵空洞；再五问只会把 L-064 出站、L-065 厨房或 L-066 两页再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；可选半句（三套方法名、OpenAPI 必填 smearing、upsert 不改请求 JSON、死列 providerErrorCode、登记表）不是第二容器。不建议 `split-lesson`：未满 10 问；两扇与一份 UseCase 是同一 OBJ-63，同一 `wta-third` 容器；(d) 跨 L-064 是矩阵原设计的走廊切分，不是「超过 1 个容器却讲不完」。`split_depth: 0`，本批禁止填 split 表。不建议发明 Service/Recorder (a) 行，也不建议发明「失败路径 管理读」第二 (d)。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:ThirdObservabilityController.invocation/list, statistics/list | 门牌两映射、两权限、字段 `observabilityService`、Java `invocations` 对 chain `invocationList`、根路径 `/third` 不含 observability、无 POST/无 `@Log`、`providerCode` optional——均与源码对齐。残留精度：（1）OpenAPI 把 query 画成必填；（2）返回 `R<?>` / `RObject`；（3）无 MockMvc 合同测试，401 夹具只证明未登录。 | 可选半句：真闸在 Service.normalize；chain 驼峰不是 Java 方法名。Lead 可将本行标 `covered`（证据锚本 probe 与 L-004 来源表 S-3RD-04）。 | defer（本行可 covered；精度项不挡盖章） |
| A:ThirdObservabilityUseCase | 接口两方法；唯一 Impl；无 TX；委托 Service 做 normalize/7 天/Vo；Store 端口不承载 findRecent——均与源码对齐。残留：无 UseCase 单测；chain 覆盖列没写本行。 | 不要等改 chain。口试字段名以 `observabilityService` 为准。Lead 可将本行标 `covered`（S-3RD-05/S-3RD-06）。 | defer（本行可 covered；证据在本课） |
| B:ThirdPartyGateway.execute（出站 HTTP、限流、记 invocation） | 管理读半段本课已讲。写半段 `record`/`recordAttempt`/脱敏/两行统计未认。矩阵仍 uncovered，挂 L-064。 | 不要用本课改该行。对照一句「读不写、写失败不改响应」即可。 | defer（L-064） |
| D:主路径 third 出站 / D:失败路径 third HTTP | 读窗不是出站走廊。超时/拒绝/凭据缺失如何分类入库未认。 | 不要用本课改该两行。 | defer（L-064；本课只是管理读窗） |
| A:ThirdProviderController.* / A:ThirdProviderUseCase | 六扇、字段 `useCase`、有 get/status/`edit`。 | 对照命名差即可。L-060。 | defer（L-060） |
| A:ThirdEndpointController.* / A:ThirdEndpointUseCase | 敏感字段名在本窗作为额外脱敏输入。 | 保持。L-061。 | defer（L-061） |
| A:ThirdCredentialController.* / A:ThirdCredentialUseCase | list 必填 providerCode、写路径有事务。 | 对照 optional/无事务一句。L-062。 | defer（L-062；已 covered） |
| A:createThirdService | 厨房两枪打本前缀；TS 必填 providerCode。 | URL 旁证一句即可。L-065。 | defer（L-065） |
| A:createThirdWebDomain | 两页 `editable===false`；不画 7 天/200。 | 保持。L-066。 | defer（L-066） |
| A:NotificationMonitorController.snapshot, deliveries | 另一模块只读看板。 | 类比一句即可。L-049。 | defer（L-049；已 covered） |
| Service / DAO.findRecent / Recorder / Sanitizer / Health 升格 (a) | 课已写进第二格与写路径对照。矩阵无此符号。 | 不要发明第三法。 | defer（未入库） |
| (b) 7 天/200 / OpenAPI 必填 smearing / upsert 不改请求 JSON | 矩阵无此行。课机制链/边界已写截断与 optional。 | 可选半句进原课。不要发明 (b)/(d)。 | defer（未入库） |
| C:MySQL 8.4 / mysql-base / dao-mapper-ownership | 两表、request_id 唯一、生成列维度、DAO 持 Mapper、无 invocation XML 是本窗机制。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085 / L-083） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；两行 (a) 无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出两扇窗的目的（按可选供应商倒带回放 / 按日看累加计数）、主路径（optional providerCode → normalize → findRecent → Vo）与失败路径（未登录 401、读路径没有 POST、记录失败不改网关、健康 down 不等于表没写上、`execute` 不在本窗）；(d) 缺的是矩阵把出站挂在 L-064，不是讲义容器；把可选半句写进原课不会超过 40 分钟；Provider、Endpoint、Credential、Gateway、厨房、两页、通知看板已有邻课。满 10 问前不拆。两扇与一份 UseCase 是同一 OBJ-63，同一 `wta-third` 容器，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-064 出站/execute、(d) 出站/失败 HTTP、L-060 供应商、L-061 Endpoint、L-062 凭据、L-065 厨房、L-066 页面、L-049 通知看板对照、未入库 Service/DAO/Recorder/(b) 精度项、(c) 容器与 dao-mapper 组件显式建议 `defer`；对 `A:ThirdObservabilityController.invocation/list, statistics/list` 与 `A:ThirdObservabilityUseCase` 建议 Lead 标 `covered`（证据锚本 probe 与 L-004 来源表 S-3RD-04/S-3RD-05/S-3RD-06/S-3RD-08）；对 7 天/200/只读 建议凭本课机制链解释，不要发明 (b)，不要等 chain 补写 UseCase 或等 L-064；对矩阵「主路径 third 出站」「失败路径 third HTTP」建议保持 uncovered 并继续挂 L-064，只承认本课管理读窗，不要发明第二 (d)、不要用两扇 GET 假闭合 `record`/`execute`。本课不主张发明 Service (a)、把厨房两枪算进本两行、或等 GP-L-064 才给 (a) 盖章。
