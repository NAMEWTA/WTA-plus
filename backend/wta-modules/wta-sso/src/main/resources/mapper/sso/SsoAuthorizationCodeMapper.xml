<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.namewta.sso.mapper.SsoAuthorizationCodeMapper">

    <update id="consumeIfUnconsumed">
        update sso_authorization_code
        set consumed = 1,
            version = version + 1,
            update_time = now()
        where authorization_code = #{authorizationCode}
          and consumed = 0
          and del_flag = '0'
          and version = #{version}
    </update>

</mapper>
