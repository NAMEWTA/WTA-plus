# T45 A1 anonymous diagnostic URL — fixed-source read-only review

Inspected fixed product `081c75ff92b147bd338ca98a041182973596bca2` and retained `/tmp/wta-t45/runs/e9853018a23b0d59/result.json`. A1 says `accepted=false`, phase `public`, fixed reason `single-object anonymous fact differs`, and `cleanup_errors=[]`; startup MinIO count was zero. No service, build or repository edit was performed here.

The 404 has a concrete source-chain explanation. `AbstractOssClientImpl.anonymousReadFacts` (`:1285–1286`) always builds the anonymous probe from `config.getBucketUrl(bucket) + '/' + encodedKey`. `OssClientConfig.getBucketUrl` (`:220–230`) appends the bucket for path-style access even when `domain` already represents a bucket-bound public URL such as `http://proxy/bucket`; the resulting probe goes to `/bucket/bucket/<key>`. `DefaultOssObjectStore.publicUrl` (`:34–44`) instead chooses `config.getDomainUrl()` whenever a custom domain is present, otherwise `config.getBucketUrl()`, trims its trailing slash and appends the encoded object key. `OssAccessUrlProviderUnitTest.publicUrlUsesBucketBoundDomainAndStructurallyEncodesObjectKey` already locks this domain contract. The actual A1 combination of signed HEAD 200 and policy/ACL 403 with anonymous HEAD/GET 404 therefore reflects a diagnostic-only URL mismatch, not proof that the named object is anonymously missing.

Minimum product correction, confined to `AbstractOssClientImpl`: in `anonymousReadFacts`, select the same public base as `DefaultOssObjectStore.publicUrl`—nonblank configured domain ⇒ `getDomainUrl()`, otherwise `getBucketUrl(bucket)`—then trim a trailing slash, reject URI userinfo/query/fragment as the existing public URL path does, and append the existing `SdkHttpUtils.urlEncodeIgnoreSlashes(key)`. Keep `HttpClient.Redirect.NEVER`, HEAD/GET classification and bounded requests unchanged. Do **not** alter `OssClientConfig.getBucketUrl`: other upload/S3 paths rely on its current bucket construction. A malformed domain should yield UNKNOWN facts through the existing diagnostic failure boundary rather than a guessed bucket URL.

Place the regression in the existing `OssAccessDiagnosticUnitTest` root: start a loopback server that serves only `/bucket/diagnostic/canary.txt`, configure path-style=true and `domain=127.0.0.1:<port>/bucket`, mock signed HEAD success plus policy/ACL 403, then assert both anonymous HEAD and Range GET are ALLOWED, `observedMethods` is exactly HEAD/GET, and no `/bucket/bucket/...` request occurred. Retain a no-custom-domain case proving `/bucket/<key>` remains correct and the existing public URL provider test untouched. A full owned MinIO rerun is still required; this is a static correction proposal, not acceptance of A1.
