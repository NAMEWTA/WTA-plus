---
artifact: learning-goal-probe
probe_id: GP-L-085-b01
lesson_id: L-085
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-085 / b01

审问对象是已写成的 Lesson `lessons/L-085-mysql-baseline.md` 与当前工作树 `/srv/WTA-plus`（2026-09-17），不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` 三格：`C:mysql-base`、`C:MySQL 8.4`、`D:存储点 MySQL`。chain 覆盖列只印 **两格**：`C:mysql-base D:happy-store-mysql`。`D:happy-store-mysql` **就是** 矩阵 (d) 行「存储点 MySQL / 业务表只进 10/50 基座」，不要发明第二张存储行。OBJ-85 原文：能指出 MySQL `10-cde-base-ddl.sql` / `50-cde-base-dml.sql` 是自有表唯一基座，以及动态数据源 `@DS` 的所有权。档是**解释+指文件**。前置 L-005。矩阵几何 **三行仍 uncovered**，本课可以一次问完：

- (c) `MySQL 8.4 基座` | Container | 备注「10-cde-base-ddl / 50-cde-base-dml」
- (c) `mysql-base` | Component | 备注「L-085」
- (d) `存储点 MySQL` | 业务表只进 10/50 基座 | 备注「L-085」

矩阵 (c) `public-contracts HTTP/SQL/OpenAPI/wta-api` 与 (d) `合同顺序` 已 covered（L-005 + GP-L-005-b01）。MinIO/OSS 容器与直传走廊已 covered（L-025…L-029）。`dao-mapper-ownership` / `BaseMapperPlus` 仍 uncovered（L-083）。`Redis 8`、`LoginHelper / StpUtil`、失败路径「会话丢失」仍 uncovered（L-084）。`NotifyClient.send / NotifyDispatcher.send` 仍 uncovered（L-082）。本批按派单只审问 **这三格 MySQL**；口试按磁盘：init 目录恰好六份、自有 DDL 只在 10、自有 DML（含回填与 `_preflight` 临时表）只在 50、活门牌只有 `master`、已有库禁止重放六份。不要发明第七份 `migrate/V2026*.sql`，不要把 20/30/40/60 当自有产品表登记处盖章，也不要用本课再盖 L-005 四张公共合同。

抽查锚点（2026-09-17）：`release-artifacts/docker/infrastructure/mysql/init/{10,20,30,40,50,60}-*.sql`；`docker-compose-infrastructure.yml` `image: mysql:8.4.9` / `container_name: namewta-mysql`；`backend/wta-admin` `application-{dev,prod,local}.yml` `spring.datasource.dynamic`；`backend/pom.xml` `dynamic-ds.version=4.5.0`；`wta-common-mybatis/pom.xml` starter；`TestTreeServiceImpl` / `TestBatchController` 注释 `@DS("slave")`；`NotifyNoticeUseCase` `@DSTransactional`；`NotifyOutboxWakePublisher` / `OssConfigChangeListener` `@DsTxEventListener`；`SqlBaselinePaths.java`；`release-manage.sh` `stage_mysql_init`；`init-mysql-container.sh`；`release-artifacts/tests/mysql-baseline-contract.test.mjs`；`00-project-profile.md`；PERSIST-006；`backend/` 根下无 `script/`；init 旁无 `migrate/`。

## Q1 澄清

- objective_id: OBJ-85
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-85；chain L-085 覆盖列 `C:mysql-base D:happy-store-mysql`；课 `claimed_cells` 三格；矩阵 (c) Container「MySQL 8.4 基座」与 Component `mysql-base` 仍 uncovered；(d)「存储点 MySQL」仍 uncovered、别名即 happy-store-mysql；`public-contracts` 已 covered；课开篇「口试名单就是这三格」+ 负空间 L-005 / L-083 / L-084 / L-082 / L-025…L-029
- difficulty: medium

课把宏观仓库收成「柜子 + 自有货架图纸/货物清单 + 新货往哪张纸上写」。计划里至少有六套可数对象，派单点名 **三格**，但 chain 覆盖列只印两格：

1. **OBJ-85 原文。** `course.md`：能指出 `10-cde-base-ddl.sql` / `50-cde-base-dml.sql` 是自有表唯一基座，以及动态数据源 `@DS` 的所有权。一句里是 **10=自有 DDL、50=自有 DML、门牌归谁**，不是 OpenAPI 指针、不是 Mapper 谁持有、不是 Redis 会话、不是 `NotifyDispatcher`。档是解释+指文件，不是方法性状。
2. **chain / 课 `claimed_cells` 不对齐。** chain 两格：`C:mysql-base` + `D:happy-store-mysql`。课三格：再加上 `C:MySQL 8.4`。矩阵把 Container 印成「MySQL 8.4 基座」，Component 印成 `mysql-base`，(d) 印成「存储点 MySQL」。`happy-store-mysql` **不是** 第四格。Lead 若按 chain 只盖两格、把 Container 留空洞，是假判——镜像、库名、六份 init、compose 挂载就是这一行。Lead 若把 `happy-store-mysql` 再立一行，是发明格子。
3. **10 ≠ 50，且 50 不是 INSERT-only。** 10 是形状（70 张 `CREATE TABLE`，0 条 INSERT，0 条 `ALTER TABLE`）。50 是货物：平台种子 + 带 `NAMEWTA-*-DSL/DML-*` 标识的回填 + 四张 `create temporary table …_preflight`。GP-L-005 已经 defer「50 含临时表与 DSL，不是十三行 INSERT」给本课。本课把那句走成口试：看见 `CREATE` 先问产品表还是预检夹具。
4. **活门牌只有 `master`。** `@DS` 是换门牌的字，名字必须先写进 admin yaml；`@DSTransactional` 是同一趟采购的收据。所有权不在 Mapper，也不在 Controller。PERSIST-001 允许被代理的 public service/mapper **携带** `@DS` 去做切换，但发明数据源名字的人是 yaml，开收据的人是 layered UseCase / classic ServiceImpl。今天生产代码里没有活的 `@DS("…")`。
5. **已有库禁止重放六份。** 50 文件头写死：历史 DSL 标识只留追溯，不是升级步骤。空库按 `10→20→30→40→50→60` 长出来；已有库只比两枚 Git Tag，差异稿进被忽略的 `temp/release/`，不进 init 当第七份。
6. **邻格。** L-005 四张公共合同（HTTP / SQL 存储格 / OpenAPI / `wta-api`）已 covered；那边只钉「自有产品表进 10、产品数据进 50」，升级/DSL/`@DS` 推给本课。L-001 只说柜子是 MySQL 8.4。L-025 的 `sys_oss_config` 是 10/50 的例子，不是再盖 MinIO 容器。L-083 才是谁持 Mapper。不要用本课 restamp `C:public-contracts`。

问：本三格闭合标准是「Container = 镜像 8.4.9 + 库名 `wta-plus`/`nacos` + 恰好六份 init；Component = 自有权威 10/50 + yaml 门牌 `primary: master` + `@DS`/`@DSTransactional` 所有权；Store = 新表/新列进 10、新种子/菜单/回填进 50、已有库不重放、20/30/40/60 只是邻居」，且 **不必** 把 L-005 四张合同再盖一次、**不必** 等 GP-L-083 才给 10 里的表盖章？Lead 若因 chain 只印两格而拒绝给 Container「MySQL 8.4 基座」盖章，是否假判？Lead 若凭「L-005 已经说过 10/50」把 `public-contracts` 再标一次、或把 `happy-store-mysql` 立成第二张 (d)，是否假闭合？

## Q2 证据

- objective_id: OBJ-85
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: init 六份 listing；10 L2–L4 / 70 张 CREATE / 0 INSERT / 0 ALTER / `test_demo` 1691–1707 / `sys_oss_config` 327–348 / `sys_user` 无 version；20 CREATE+INSERT 混排；30 `test_leave` 242–258 + 工作流菜单 INSERT；40 `sai_*`；50 L3 / L391–L396 / 用户 WTA L21 / 四张 `_preflight` / OSS 种子 348–352 + DML-001 828–853；60 L3–16 `CREATE DATABASE nacos`；compose L8–L30；`stage_mysql_init` 424–455；`mysql-baseline-contract.test.mjs` 12–29；`init-mysql-container.sh` 9–19 / 115–140 / 177–191；admin yaml `primary: master` `strict: true`；零活 `@DS(`；`NotifyNoticeUseCase` 23–33；`SqlBaselinePaths.java` 8–47；`backend/` 无 `script/`；无 `mysql/migrate/`
- difficulty: high

课的六份清单 + 门牌哪些能在工作树直接复现，哪些只是断言数字过期、或口试把「邻居必须跑」说成「邻居也是自有登记处」？

**仍能复现（Container `C:MySQL 8.4`）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| init 目录恰好六份，文件名 `10/20/30/40/50/60-cde-*.sql`，无第七份 | `release-artifacts/docker/infrastructure/mysql/init/` listing；`mysql-baseline-contract.test.mjs` 12–29 `assert.deepEqual(actual, sqlFiles)` |
| Compose 镜像 `mysql:8.4.9`；库名默认 `wta-plus`；charset/collation/`lower_case_table_names=1`；init 只读挂 `/docker-entrypoint-initdb.d` | `docker-compose-infrastructure.yml` 8–30 |
| 容器服务名 `namewta-mysql` | compose 10 |
| 项目画像：六份是唯一事实源；只支持并验收 MySQL 8.4 | `00-project-profile.md` 43 / 53–55；PERSIST-006 / PERSIST-007 |
| `stage-mysql` 按数组顺序点名六份；`find … -name '[0-9][0-9]-*.sql' \| sort` 必须 1:1；多一份失败；不复制、不改写、不执行 | `release-manage.sh` 424–455；合同测试 43–48 跑两遍 digest 不变 |
| 河 B 拒绝非 `wta-plus`、拒绝已存在库/账号；按六份 `mysql < file`；失败只 DROP 本次新建对象 | `init-mysql-container.sh` 115–140 / 151–180 |
| 60 自己 `CREATE DATABASE nacos; USE nacos;`，十张表不进 `wta-plus` | `60-cde-nacos.sql` 15–16 / 37–190 |

**仍能复现（Component `C:mysql-base` + Store `D:存储点 MySQL`）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 10 文件头「完整最新基座，直接修改本文件」；来源句含「ALTER 已折入」 | `10-cde-base-ddl.sql` 2–4 |
| 10 产品 `CREATE TABLE` **70** 张（`rg -c` 71 是因为 L4 注释也含 `CREATE TABLE`）；前缀 26 `sys_*` / 24 `profile_*` / 11 `notify_*` / 5 `third_*` / 3 `test_*` / 1 `sso_*` | 10 全文件 CREATE 清单 |
| 10 零 `INSERT`、零 `ALTER TABLE` | 对 10 的 `insert into` / `ALTER TABLE` 空 |
| `test_demo` 七字段齐，是 PERSIST-003 尺子；主键仍是裸 `id`（命名尺子是 `flow_category.category_id`，别混） | 10 1691–1707 |
| `sys_dept` / `sys_user` **没有** `version`；棘轮现场，不是新表样板 | 10 41–60 / 63–88 |
| `sys_oss_config.access_policy` 注释只有 `0=PRIVATE` / `2=PUBLIC_READ` | 10 338 |
| 50 文件头同样「完整最新基座」；L391–L396「仅用于全新数据库初始化 / 禁止重放」 | `50-cde-base-dml.sql` 3 / 391–396 |
| 50 **没有** 产品 `CREATE TABLE`；四张 `create temporary table …_preflight`（password / runtime-gen / nacos-console / admin-runtime） | 50 554–555 / 645 / 865 / 918；对产品 `create table` 空 |
| 管理员 `WTA` 种子在 50，不是 Nacos、不是 `CommandLineRunner` | 50 21 `user_id=1761100000000000001` |
| OSS 五行种子曾写 `access_policy='1'`；同文件 `NAMEWTA-OSS-ACCESS-DML-001` 哨兵 INSERT + 把非 0 打成 `'0'` | 50 348–352 / 828–853 |
| 20 是 SnailJob 上游：`sj_*` CREATE **与** INSERT 混排，无「直接修改本文件」产品头 | `20-cde-job.sql` 3 起；18–19 INSERT |
| 30 是 Warm-Flow 上游；夹带 `flow_category` / `test_leave` / 工作流 `insert into sys_menu` | `30-cde-workflow.sql` 1–5 / 168 / 242–258 / 260 起 |
| 40 是 Snail AI `sai_*` 快照 | `40-cde-ai.sql` 1–5 / 14 起 23 张 |
| 60 头写上游 Tag `2.5.4` 与 SHA-256；禁止本地改一列 | 60 3–13 |
| 活数据源只有 `master`；`strict: true`；`slave` / oracle / postgres / sqlserver 在 dev/prod 整段注释；local 连注释块都没有 | `application-dev.yml` 91–131；`application-prod.yml` 94–134；`application-local.yml` 8–17 |
| JDBC 指向库 `wta-plus` | 三份 yaml `url: jdbc:mysql://…/wta-plus` |
| starter 4.5.0 由 `wta-common-mybatis` 引入；父 POM 锁版本 | `backend/pom.xml` 32；`wta-common-mybatis/pom.xml` 37–41 |
| 生产代码 **零** 活 `@DS(`；只在 `TestTreeServiceImpl` 类上/方法上、`TestBatchController` 三处 **注释** | 全仓 `rg '@DS\('` 仅这两文件 |
| Mapper 上 **零** `@DSTransactional` | 模块内 `*Mapper*.java` 空 |
| layered 收据在 UseCase 命令：`NotifyNoticeUseCase` 查询不贴、`save/publish/retract/remove` 贴 | `NotifyNoticeUseCase.java` 19–33；third / profile UseCaseImpl 同构 |
| 提交后副作用 `@DsTxEventListener`；Outbox 显式 `AFTER_COMMIT` | `NotifyOutboxWakePublisher.java` 52；`OssConfigChangeListener.java` 31 |
| 存量 Spring `@Transactional` 在 workflow Controller/ServiceImpl，是棘轮不是样板 | `FlwDefinitionController` 89 起；`FlwTaskServiceImpl` / `TestLeaveServiceImpl` |
| 后端无 `script/` 副本；测试用 `SqlBaselinePaths` 从 `mvnw` 上溯父仓 `release-artifacts/…/mysql/init`；可用 `-Dnamewta.sql.root` | `backend/` listing 无 `script/`；`SqlBaselinePaths.java` 8–47；契约测试 `file("10-…")` / `file("50-…")`；`BusinessOssOwnerArchitectureUnitTest` `Files.walk(root())` 扫全部 `.sql` |
| 无 `mysql/migrate/`、无 `V2026*.sql` | init 旁 listing；release README 120 |

**课写过、数字/名单更窄或口试会写偏（不是证伪本三格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「脚本断言 `EXPECTED_TABLES=125`；CREATE 清单 10+20+30+40=127」 | `init-mysql-container.sh` 10–11 / 187–191 仍写 125。60 的 10 张在 `nacos` 不计入。口试数 CREATE，不要把断言背成 schema |
| 「默认容器名 `namewta-data-mysql`；compose 是 `namewta-mysql`」 | 脚本 9 vs compose 10。调用必须 `--container` 对上 |
| 「10 约 1725 行 / 50 约 1805 行」 | `wc -l` 为 1724 / 1804。约数，不是机制错误 |
| chain 估时 35、课 `estimated_minutes: 38` | 与 L-081 同类。不是机制错误 |
| 「`SqlBaselinePaths` 读六份」 | helper **定位目录**，不硬编码六文件名。六份 1:1 门禁在 `stage-mysql` 与 `mysql-baseline-contract.test.mjs`。Java 测试主要 `file("10")`/`file("50")`，少数把 20 当邻居反例 |
| PERSIST-006「10 与 50 不得互混语句类型」 | 50 的 `CREATE` 全是 session 级 `_preflight`。课已教「先问产品表还是夹具」。不要把这条张力写成规则已破 |
| PERSIST-001 允许 mapper 携带 `@DS` | 活树 Mapper 上没有。课的所有权句是 yaml 发明名字、UseCase/ServiceImpl 开收据。不要把 skill 的「可贴位置」读成「Mapper 拥有数据源」 |

问：在 六份 1:1、10=70 张 DDL 且无 INSERT/ALTER、50=种子+DSL+四张预检临时表且无产品表、yaml 只有 `master`、零活 `@DS`、UseCase 持 `@DSTransactional`、已有库脚本拒绝重放、无 `backend/script/`、无 `migrate/` 已经能互证的前提下，(c)/(d) 是否还要把「断言 125≠127」「容器名分叉」「helper 不硬编码六文件名」写成开篇半句，才算这三格讲完？还是这些已经落在课的边界表里，Lead 可以把 **三行都标 covered**，并保持 L-005 / MinIO / L-083 / L-084 / L-082 不动？

## Q3 假设与反例

- objective_id: OBJ-85
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 30 里 `test_leave` 不是新表许可证；50 产品 CREATE 空、只有 `_preflight`；无 `V2026` / `mysql/migrate/`；Mapper 零 `@DS`；解开 slave 注释会因 `strict: true` 失败；Controller 开 Spring 事务是棘轮；已有库拒绝；Nacos 表不进 `wta-plus`；不要 restamp public-contracts
- difficulty: high

课的隐含假设若反转，合格读者会把哪张纸或哪扇门认错？

1. **「新产品表写进 `30-cde-workflow.sql`，因为 `test_leave` / `flow_category` 已经住在那儿。」** 那是上游快照夹带。新 NAMEWTA 表仍只进 10。课反例 8 / 误区 11 / 边界 1、9 覆盖。
2. **「DDL 写进 50，反正 50 里已经有 `CREATE`。」** 50 的 CREATE 是 `_preflight` 临时表，会话结束即消失。产品表仍只在 10。课反例 2 / 误区 3 覆盖。GP-L-005 已把这句张力推给本课，本课接住了。
3. **「加 `release-artifacts/…/init/V2026__add_color.sql` 或 `mysql/migrate/`。」** PERSIST-006 禁止第七份。`stage-mysql` 与合同测试会因多一份 `[0-9][0-9]-*.sql` 失败。已有库差异稿去 `temp/release/`。课反例 3 / 误区 4 覆盖。
4. **「`@DS` 贴在 Mapper 上才算用了动态数据源。」** 活树 Mapper 没有这枚章。门牌目录在 admin yaml；今天连第二扇活门都没有。课反例 5 / 误区 6 / 边界 5 覆盖。
5. **「解开 `@DS("slave")` 就是读写分离。」** yaml 没挂 `slave`，`strict: true` 下第一查询爆炸。课反例 6 / 误区 20 覆盖。
6. **「Controller 开 `@DSTransactional` / Spring `@Transactional` 更直观。」** 登记表不让。`FlwDefinitionController` 上的 Spring 事务是棘轮。课反例 7 / 误区 8 覆盖。
7. **「已有 `wta-plus` 再跑 `init-mysql-container.sh` 或重放 50 就升级了。」** 脚本拒绝已存在库；文件头禁止重放。课反例 4 / 误区 4、13 覆盖。
8. **「六份都可以加 NAMEWTA 新表 / OBJ-85 是六份内部全 covered。」** 自有基座只有 10/50。20/30/40/60 必须跑，但不是登记处。课误区 1、2 / 边界 1 覆盖。
9. **「yaml 注释里的 postgres/oracle 说明还支持那些方言。」** 产品基座只有 MySQL 8.4。课反例 9 / 误区 10 覆盖。
10. **「Nacos 表建进 `wta-plus` 更省事。」** 60 明确另一间库房。课误区 15 覆盖。
11. **「`EXPECTED_TABLES=125` 就是表权威。」** 先数 CREATE（127）。课边界 2 / 误区 12 覆盖。
12. **「改了 10 就等于已有库加列。」** 只等于下一台空库有列。课误区 19 / 变式 10 覆盖。
13. **「本课要把 `public-contracts` 再盖一次，因为又讲了 10/50。」** L-005 已经认四张纸和「表先行」。本课认怎么改、怎么初始化、怎么升级、门牌归谁。课变式 1 覆盖。
14. **「OSS 种子是公开桶，所以 MinIO 容器还没讲。」** 列注释 0/2；50 后半打成 0；河 B 再强制 minio PRIVATE。对象直传走廊已由 L-025…L-029 covered。课正例 4 / 边界 图 / 误区 16 覆盖。
15. **「运行时 Mapper XML 才是基座。」** 运行时 SQL 是 L-083。两套抽屉不要抄错。课边界 10 覆盖。

问：读完后仍说不出的，是不是「10 和 50 的文件名」，而是 **自有 DDL 只进 10、自有 DML（含回填/预检）只进 50、20/30/40/60 是邻居不是许可证、活门牌只有 master、收据在 UseCase/ServiceImpl 不在 Mapper/Controller、已有库禁止重放六份、chain 别名 happy-store-mysql 不是第二张 (d)、L-005 四张合同不要 restamp**？

## Q4 视角

- objective_id: OBJ-85
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (c)/(d) 三行 vs chain 两格；OBJ-85 vs L-005/L-025/L-083/L-084/L-082/L-001；PERSIST-006；MinIO 已 covered；`NotifyDispatcher` / `LoginHelper` / `BaseMapperPlus` 仍 uncovered
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这三格**？

| 视角 | 多出来的边 | 是否本课这三格 |
| --- | --- | --- |
| 矩阵维护者 | (c) Container 与 Component 两行 + (d) 存储点一行；chain 只印后两格符号；别名 `happy-store-mysql` = (d) 同行 | **是** 闭合定义。三行都可视为已讲并可 covered。不要发明第二张 store 行，不要因 chain 漏印 Container 而假判 |
| 仓库电工 | 镜像 8.4.9、compose 挂 init、库名 `wta-plus`/`nacos`、容器名分叉、断言 125 | **是** `C:MySQL 8.4`。断言过期是边界半句，不是第二容器 |
| 货架管理员 | 10=70 张形状；50=货物+DSL+预检；新列/新菜单落点；ftl 片段并进 50 | **是** `C:mysql-base` + `D:存储点 MySQL` |
| 门牌管理员 | yaml `primary: master` / `strict: true`；`@DS` 换牌；`@DSTransactional` 收据；`@DsTxEventListener` 提交后 | **是** `C:mysql-base` 的 `@DS` 所有权。今天没有第二扇活门 |
| 发布门禁 | `stage-mysql` 六份 1:1；多一份失败；不执行 SQL | **是** Container/Component 的点名机制，不是「执行升级」格 |
| L-005 合同官 | 四张纸（HTTP/SQL/OpenAPI/`wta-api`）与 `fetch → 指针 → generate → domain` | **否** 已 covered。本课只把 SQL 纸拆成 10/50/顺序/差异稿/门牌。不要 restamp |
| L-025 名牌柜台 | `sys_oss_config` CREATE 在 10、五行种子+ACCESS 回填在 50；默认私桶 | 表落点 **是** (d) 例子。MinIO 容器 / 直传走廊 **否**（已 covered） |
| L-083 DAO | 谁持 Mapper / `BaseMapperPlus` / QueryBuilder / 运行时 XML | 划界一句 **是**。所有权课 **否** |
| L-084 会话 | Sa-Token 写 Redis；登录成功但会话丢失 | 本课不画 Redis 键。**否** |
| L-082 同步入口 | `NotifyClient.send` / `NotifyDispatcher.send` | UseCase 收据活样本 **是** 对照。Dispatcher **否** |
| L-001 大楼 | 柜子标签是 MySQL 8.4 | 开门内部 **是** 本课。五房间 **否**（已 covered） |
| 厂商引擎 | 20/30/40 字段、WarmFlow/SnailJob/SnailAI 内部 | 邻居身份 **是**。引擎内部 **否**（`deferred(vendor-engine)`） |
| 独立进程 | `wta-extend` / snailai-server / snailjob-server 自己的 yaml 数据源 | **否**（`deferred(separate-process)`）。admin 门牌才是本格 |

问：Lead 若凭「chain 覆盖列只有两格」只盖 `mysql-base` 与存储点、把 Container 留着，是否还要求读者能指着 **镜像 8.4.9、六份 init、库名、compose 挂载**？这些是不是已经在本格文件表里，而不需要等别的课？Lead 若凭「又讲了 10/50」把 `public-contracts` 再标 covered，是否 restamp？Lead 若把 `sys_oss_config` 当理由重开 MinIO 容器，或把 `@DSTransactional` 活样本当理由去盖 `NotifyDispatcher` / `BaseMapperPlus` / Redis 8，是否假闭合？

## Q5 元问题

- objective_id: OBJ-85
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；同一 OBJ 缺机制/边界/例子才 `re-dispatch-L`；满 10 问仍不清才拆课；本课累计 5/10；`split_depth: 0`；矩阵三行仍 uncovered；邻格 Redis/DAO/Notify/MinIO/public-contracts 不要用本课改状态
- difficulty: low

建议：`defer`

原因：

- **三格与 OBJ-85 / 课 `claimed_cells` / 矩阵三行对齐。** L 合同章节齐全（宏观地图、直觉、定义表、机制链 1–6、带 caption 的两张图、正反例、变式、误区、暂停走查、来源表 S-009/S-010/S-L005-01/S-L085-01…11）。能复现：六份 1:1、10=自有 DDL、50=自有 DML（含回填与预检，不是 INSERT-only）、活门牌 `master`、已有库不重放。不必为「文件名」再挖五问。Q2 与磁盘匹配。Lead 可将 (c) `MySQL 8.4 基座`、(c) `mysql-base`、(d) `存储点 MySQL` 标 `covered`（证据锚本 probe 与 L-085 来源表）。`D:happy-store-mysql` 不要另立一行。
- **chain 漏印 Container 不是空洞。** 课开篇已经把 Container 写成口试第一格；compose / 画像 / `stage-mysql` / 河 A 河 B 都在。不要因为覆盖列只写了 Component+Store 就 `mine-more`。
- 本批可粘贴的精度项不挡住三格盖章：（1）`EXPECTED_TABLES=125` vs CREATE 127；（2）容器名 `namewta-mysql` vs 脚本默认 `namewta-data-mysql`；（3）`SqlBaselinePaths` 定位目录、六份 1:1 门禁在 release 合同测试；（4）行数约数；（5）chain 35 vs 课 38。课边界 2、3、7 已经写了前三项。
- 不建议 `mine-more`：b01 未证明这三行还有真实矩阵空洞；再五问只会把 L-083 Mapper、L-084 Redis、L-082 Dispatcher、L-005 OpenAPI 指针或 MinIO 直传再问一遍。不建议 `re-dispatch-L`：不缺动机/机制/边界/例子；断言过期与容器名分叉是半句精度，不是第二容器，也不是同一 OBJ 的事实写错。不建议 `split-lesson`：未满 10 问；三格是同一 OBJ-85（柜子+货架+落点）；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议 restamp L-005。不建议发明第七份 migrate 课。不建议把 20/30/40/60 内表拆成新课才给邻居身份盖章。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| C:MySQL 8.4 基座（Container） | 镜像 8.4.9、库名、六份 init、compose 挂载、`stage-mysql` 点名——均与源码对齐。残留精度：断言 125≠127；容器名分叉。chain 覆盖列未印此格。 | 可选半句：口试表数以 CREATE 为准，脚本断言以 `EXPECTED_TABLES` 为准；`--container` 对上真实名字。不要求重写机制链。Lead 可将矩阵该行标 `covered`（证据锚本 probe 与 L-085 来源表 S-L085-05）。 | defer（本行可 covered；精度项不挡盖章） |
| C:mysql-base（Component） | 自有权威 10/50、yaml 只有 `master`、零活 `@DS`、收据在 UseCase/classic ServiceImpl、测试读父仓六份目录——均与源码对齐。 | 可选半句：`SqlBaselinePaths` 定位目录，六文件名 1:1 以 `stage-mysql` / `mysql-baseline-contract.test.mjs` 为准。Lead 可将该行标 `covered`。 | defer（本行可 covered） |
| D:存储点 MySQL（= chain `D:happy-store-mysql`） | 新表/列进 10，新种子/菜单/回填进 50；已有库 Tag 差异、禁止重放；20/30/40/60 不是登记处。课已用 `test_leave`、50 `_preflight`、禁止 `V2026` 作反例。 | 不要另立 `happy-store-mysql` 行。Lead 可将该行标 `covered`。 | defer（本行可 covered；别名同行） |
| C:public-contracts / D:合同顺序 | 本课借用「10/50 是 SQL 纸」并把 DSL/升级/门牌走完。四张合同与 OpenAPI 工序已由 L-005 盖章。 | 不要 restamp。GP-L-005 当时 defer 给本课的 50 语句类型，本课已接住。 | defer（L-005 已 covered；禁止 restamp） |
| C:dao-mapper-ownership / B:BaseMapperPlus | 运行时 SQL ≠ 初始化基座，课只划抽屉。 | 不要用本课盖谁持 Mapper。 | defer（L-083） |
| C:Redis 8 / B:LoginHelper / D:失败路径 会话丢失 | Outbox 唤醒会发 Redis，那是提交后副作用样本，不是会话容器。 | 不要因 `@DsTxEventListener` 标 Redis covered。 | defer（L-084） |
| B:NotifyClient.send / NotifyDispatcher.send | `NotifyNoticeUseCase` 收据是门牌活样本，不是同步渠道入口。 | 不要用本课盖 Dispatcher。 | defer（L-082） |
| C:MinIO/OSS / D:OSS 直传 | `sys_oss_config` 在 10/50 是存储点例子；默认私桶是 50+河 B 的数据事实。 | 不要重开 MinIO 容器。L-025…L-029 已 covered。 | defer（已 covered） |
| 20/30/40/60 内部字段 | 课只认邻居/夹带/另一间库房。 | 保持 `deferred(vendor-engine)`；不要发明第七份文件课。 | defer（邻居身份已讲；内部不 covered） |
| EXPECTED_TABLES=125 | 与 CREATE 127 不一致。课已写进边界，明确「不要用本课去改脚本」。 | 不挡三格盖章。 | defer（精度；非矩阵空洞） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；三格无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出 10/50 分工、六份顺序、活门牌 `master`、已有库不重放、邻居不是许可证；把可选半句写进原课不会超过 40 分钟；Mapper / Redis / Dispatcher / MinIO / 四张公共合同已有邻课或已 covered。满 10 问前不拆。Container + Component + Store 虽跨 compose/SQL/yaml，仍是 chain 原设计的同一 OBJ-85，不是「超过 1 个容器却讲不完」。不要把 20/30/40/60 拆成新课，也不要把 `@DS` 所有权拆成独立课。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / split 提案：否（miner 不改矩阵）；split 提案为 none；缺口表对 Redis/DAO/NotifyDispatcher/MinIO/L-005 restamp、20/30/40/60 内部、断言 125 显式建议 `defer`；对矩阵 (c) 行「MySQL 8.4 基座」、(c) 行 `mysql-base`、(d) 行「存储点 MySQL」建议 Lead 标 `covered`（证据锚本 probe 与 L-085 来源表 S-L085-01…11；Q2 与 2026-09-17 磁盘匹配）。本课不主张发明第二张 `happy-store-mysql` 行、发明第七份 migrate 文件、把 20/30/40/60 当自有登记处盖章、用本课 restamp `public-contracts`、或等 GP-L-083 才给 10 里的表当已讲。
