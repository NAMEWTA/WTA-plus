---
artifact: learning-goal-probe
probe_id: GP-L-033-b01
lesson_id: L-033
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-033 / b01

审问对象是已写成的 Lesson `lessons/L-033-openapi-frontend.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain / `claimed_cells` / 矩阵 (a) **一行**）：`A:createOpenApiService`。OBJ-33 原文：能说明 `createOpenApiService` **与** admin-web `openApi.vue`。档是**解释+对照**。chain 覆盖列只写工厂。矩阵 (a) 符号就是 `createOpenApiService`，模块 `domains/system`，仍 `uncovered`。矩阵 (b) **没有** 工厂投影 / `NO_STORE` / 信封行。矩阵 (c) `admin-web` 容器已 covered（L-004）；`wta-common` 工具间含 openapi 已 covered（L-002）。矩阵 (d) **没有** 管理端钥匙/货架流，也没有机器 HMAC 调用流。本批按派单审问 **`A:createOpenApiService`**；`openApi.vue` / `OpenApiWorkspace` / `OpenApiAdminPage` 是 OBJ 点名的对照门脸，不是另两行符号，不把两张 Vue 塞进本行才能标 covered。`SysOpenApiCatalogController` 四扇留给 L-030；凭据十三条窗与密文四列留给 L-031；HMAC / nonce / 机器会话留给 L-032；厅堂 `services.ts` 组合点留给 L-007；菜单键 `system/openApi/index` 与 `runtime.openApi` 口留给 L-020；九工厂行 `createSystemWebDomain, …` 留给 L-020 那一行；资料页其它页签留给 L-022；监控子路径工厂留给 L-024。

抽查锚点（2026-09-16）：`frontend/packages/domains/system/src/open-api/{service,transport,types,index,index.test}.ts`；根 `src/index.ts` re-export + `systemDomainModule.capabilities`；`package.json` `"./open-api"`；`frontend/apps/admin-web/src/application/{services,http}.ts`；`views/system/user/profile/{openApi.vue,openApi.test.ts,index.vue}`；`frontend/packages/web-domains/system/src/open-api/{workflow,workflow.test,OpenApiWorkspace,OpenApiAdminPage,index}.ts`；`web-domains/system/src/{index.ts,index.test.ts,runtime.ts}`；`apps/admin-web/src/router/adminManifestRegistry.ts`；`frontend/apps/home-web` 零命中；`frontend/packages/adapters/axios-browser/src/index.ts` `successCode` / `repeatSubmit`。

## Q1 澄清

- objective_id: OBJ-33
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-33；chain L-033 覆盖列只 `A:createOpenApiService`；L-033 `claimed_cells` 一格；矩阵 (a) 该行仍 uncovered；矩阵 (b) 无本课符号；(c) admin-web / common-openapi 已 covered；(d) 无管理端钥匙流；课开篇「格子仍只认工厂」+ 负空间 L-030/L-031/L-032
- difficulty: medium

课把浏览器这一头收成「独立厨房 + 两扇柜台 + 两张薄门」。计划里至少有四套可数对象，派单只点名厨房这一格：

1. **OBJ-33 原文。** `course.md`：能说明 `createOpenApiService` 与 admin-web `openApi.vue`。一句里两件东西，没有把 `OpenApiAdminPage`、`workflow.ts`、HMAC 头、密文四列写成口试清单。
2. **chain / 课 `claimed_cells`。** 一格：`A:createOpenApiService`。chain 不写 Vue 符号。
3. **矩阵 (a) 一行。** 符号就是 `createOpenApiService`，模块 `domains/system`。与厨房 1:1。Lead 若按行标 covered，**不会**把九个 `create*WebDomain` 假闭合——那些在另一行，点名的是 `createSystemWebDomain` 不是本工厂。
4. **课自己的操作定义。** 工厂只认 `HttpClient`，冻住 `currentUser`（八枪 `/self/`）+ `targetUser`（九枪，除 `listUsers` 外必须先有合法 `userId`）。读 GET 写 POST（删除也是 POST `.../delete`）。create/reset 独带 `NO_STORE`。Summary 拒任何 `secret*` 键；Issued 只允许非空 `appSecret`。`openApi.vue` 是写死 `current-user` 的薄壳；超管页先 `list` 再 `target-user`。格子仍只认工厂。
5. **邻格。** L-007 已认厅堂插头 `openApiService = createOpenApiService(domainHttp)`。L-020 已认菜单键与 `runtime.openApi` 口，HTTP 全表留给本课。L-030/L-031 是后端两份 Controller。L-032 是机器五颗签名头。L-024 对照「根 barrel 有 OpenAPI、没有 monitor」。

问：本格 (a) 闭合标准是「独立工厂两扇柜台 URL/动词/投影/no-store + 非 `systemService` 字段 + 卡车是登录票」，还是 OBJ-33 那句必须把 `openApi.vue`（外加工作区/超管页）升成第二格才能盖章？若是前者，Lead 标本行 covered 是否必须先给 Vue 单独立行（或诚实把门脸留作对照），而不是把两张页塞进九工厂行、也不是把 HMAC / 密文四列算进本行？

## Q2 证据

- objective_id: OBJ-33
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: `service.ts` 13–14 / 16–60 / 62–137；`transport.ts` 19–21 / 93–107 / 114–141 / 171–217；`types.ts` 32–35 / 91–120；`index.test.ts` 53–184；根 `index.ts` 15–26 / 137–154；`package.json` 19；admin `services.ts` 7 / 18–20 / 34–35；`http.ts` 11 / 56 / 88–90；`openApi.vue` 1–23；`openApi.test.ts` 5–13；`profile/index.vue` 88–90；`workflow.ts` 71–85 / 116–161 / 224–248；`workflow.test.ts` 42–129；`OpenApiAdminPage.vue` 34–77；`OpenApiWorkspace.vue` 73–80 / 98 / 145–153 / 260–293；web-domain `index.ts` 51 / 76；`index.test.ts` 30 / 49–51 / 108–114；registry 187–189；axios `repeatSubmit` 157；`successCode` 213–228；home-web 零命中
- difficulty: high

课的厨房机制链哪些能在工作树直接复现，哪些只是测试名单比课表窄、或邻格事实？

**仍能复现（本格）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂 `createOpenApiService(http: HttpClient)`，只认 `http`，返回 `Object.freeze` 的 `OpenApiService`；两口各自再 freeze | `service.ts` 62–70 / 71 / 102 |
| 根 barrel **也** re-export；子路径 `./open-api` 同时存在。厅堂实际走根：`from '@namewta/domain-system'` | 根 `index.ts` 15–26；`package.json` 19；admin `services.ts` 7 |
| **没有** `@namewta/domain-openapi` 包；**没有** `createSystemService.openApi` | 仓库无该包名；`service.ts` 八口不含 openApi |
| 厅堂懒电线后并列 `export const openApiService = createOpenApiService(domainHttp)`，不是 `systemService` 字段 | `services.ts` 18–20 / 34–35 |
| home-web **整仓**无 `createOpenApiService` / `openApiService` / `openApi` | `home-web` grep 空 |
| `BASE_PATH = '/system/openApi'`；`method` 小写 `'get'`/`'post'` | `service.ts` 13 / 73–133 |
| `currentUser` 八枪，URL 都带 `/self/`；编译期 **没有** `userId` 参数 | 71–101；`types.ts` 91–100；测试 113–131 |
| `targetUser` 九枪：`listUsers` → GET `/users`；其余 `/users/{userId}/...` | 102–135；测试 157–176 |
| 读 GET，写 POST；删除也是 POST `.../credential/delete`，没有 `method: 'delete'` | 93–94 / 126–127；测试 128 / 173 |
| create/reset 独有 `headers: NO_STORE` = 冻住 `{ 'Cache-Control': 'no-store', repeatSubmit: false }`；GET / 启停 / 删除不加 | 14 / 67 / 79 / 86 / 111 / 119；测试 118 / 124 vs 114 / 126–128 |
| `createInput` 只留下 trim 后 `appName`（1–100）和可选 `expiresAt`/`remark`（≤500）。测试塞 `ownerUserId`/`appSecret`，`data` 里没有这两样 | 27–44；测试 99–119 |
| `userSegment` 只放 `/^[1-9]\d*$/`，否则 `TypeError('OpenAPI target userId is invalid')`，不把垃圾拼进 URL | 16–19 |
| `interfaceSegment` 拒绝空白；`'order/query'` → `order%2Fquery` | 22–25；测试 111 / 130 / 175 |
| `userQuery.limit` 整数 1–100；超管页调用写死 `limit: 50` | 53–55；`OpenApiAdminPage.vue` 64 |
| 运输错误原样拒绝：`kind: 'business', code: 403` 的对象 `rejects.toBe(forbidden)`，工厂不包一层 | `service.ts` 63–66；测试 179–184 |
| Summary：任一键名小写含 `secret` → `unavailable()`（文案固定 `OpenAPI 响应不可用`）。GET 里 `appSecret: 'must-not-be-present'` 整份扔掉 | `transport.ts` 19–21 / 118–119 / 135–137；测试 53–60 |
| Issued：允许且只允许键名恰好 `appSecret`；空串拒 | 119 / 131–132 / 139–141；测试 62–68 |
| 目录项：method 枚举无 `CONNECT`；`permissions.values` 不允许空数组；`location` 只认 `header\|path\|query`；`responseSchema` 必须非空字符串 | 93–107 / 57 / 83 / 165；测试 70–77 |
| `projectOpenApiEmpty`：delete 的 `data` 必须是 `null`/`undefined` | 214–217 |
| `groupOpenApiCatalog` 按 `path` 第一段分组，**不改** item 身份；`groups[0].items[0]` 同一引用 | 183–194；测试 79–89 |
| `resolveOpenApiCredentialState`：过期优先于启停；`status==='0'` → enabled，否则 disabled。不是 OSS 的 `Y`/`N` | 196–202；`types.ts` 2–3 |
| `OpenApiCredentialIssued` 类型注释：*A command-only value. Never retain this object in a store or cache.* | `types.ts` 32–35 |
| `systemDomainModule.capabilities` 列到 oss/social/monitor-*，**没有** `openApi` | 根 `index.ts` 137–154 |
| 厨房源码搜不到 `X-App-Key` / `X-Signature` / `X-OpenAPI-*`。卡车是 `Authorization: Bearer ${getToken()}` + `clientid` | open-api 目录零命中；`http.ts` 88–90 |
| `repeatSubmit: false` 被 axios adapter 读成「关掉 500ms 连点闸」，不是 HTTP 缓存语义 | axios-browser `index.ts` 157–169 |

**邻格仍能复现（不计入本行闭合，只证明课没把厨房讲成 HMAC 门或菜单页）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 资料页壳全文三件：import 工作区、import 厅堂单例、手搓六字段 runtime；scope 字面量 `{ kind: 'current-user' }`；测试锁死 **没有** `userId:` | `openApi.vue` 1–23；`openApi.test.ts` 5–13 |
| 页签静态：`label="OpenAPI"` `name="openApi"` `<open-api />`；无 `v-hasPermi`。闸在工作区 `requireAction('view')` → `system:openApi:self` | `profile/index.vue` 88–90；`workflow.ts` 71–85 / 141–142 |
| 超管页：无 `system:openApi:list` 则文案「当前账号没有目标用户查询权限」，**不打** `/users`；有权限才 `listUsers({ keyword, limit: 50 })`；未选人只空状态，不挂工作区 | `OpenApiAdminPage.vue` 33–39 / 56–70 |
| 选中后 `:key="String(selectedUser.userId)"` + `kind: 'target-user'`；`userLabel` = `` `${userName} / ${nickName \|\| userId}` `` | 34–39 / 73–74 |
| current-user 测试：create/load 只碰 `currentUser.*`，`targetUser.getCredential` 零次。target-user 测试：load/reset/disable/delete 全部带 `'41'` | `workflow.test.ts` 42–75 |
| 加载 `Promise.allSettled([getCredential(), listInterfaces()])`；凭据 404 且目录成功 → `credential=null`、`error=null`；双 404 → `error='disabled'` | `workflow.ts` 146–161；测试 77–89 |
| `classifyOpenApiError`：403→forbidden，404→disabled，409→conflict，其余 unavailable | 229–235；测试 119–129 |
| 只有 list 权限时 target create **零次**调用，`error='forbidden'` | 测试 105–117 |
| `applyIssued` 把 `appSecret` 解构丢掉再写入 `state.credential`；名牌区无密钥行；密钥只活在一次性对话框 | `workflow.ts` 116–120 / 224–227；`OpenApiWorkspace.vue` 50–71 / 145–175 |
| 双关闭路径：`@update:model-value="closeIssuedSecret"` 与 `@closed="controller.dismissIssuedSecret"`；`close-on-click-modal=false` | Workspace 145–153 / 291–293；web-domain `index.test.ts` 108–114 |
| 工作区源码 `not.toMatch(/localStorage\|sessionStorage/)` | `index.test.ts` 114 |
| 工作区**不调用** `getInterface`。抽屉 `showInterface(row)` 吃列表行；表格 `:data="[...state.catalog]"` 只拆 freeze，无分组 UI | Workspace 98 / 113 / 287–289；`workflow.test.ts` mock 有 `getInterface` 但用例零调用 |
| 过期时启停钮不渲染（`credentialState !== 'expired'`），重置和删除仍在 | Workspace 73–84 |
| 菜单登记：id `system-open-api`，键 `system/openApi/index`，组件名 `OpenApi` → `OpenApiAdminPage.vue`。权限目录含 self/list/query/add/edit/remove | web-domain `index.ts` 51 / 76；测试 30 / 49–51 |
| 超管页拿到整份 `SystemWebRuntime`（registry `openApi: openApiService`）；资料页只手搓 `OpenApiWorkspaceRuntime` 六字段 | `runtime.ts` 32–58 vs 17–23；registry 187–189；`openApi.vue` 11–23 |
| 创建表单空备注送 `null`：`remark: createForm.remark \|\| null` | Workspace 260–266 |
| JSON `code !== successCode` 时 axios 先 `reject({ kind, code })`，厨房 `projectOpenApiResponse` **看不到** 404 信封，只看到抛错 | axios-browser 213–228；`service.ts` 63–66 |

**课写过、测试名单更窄或邻课才钉死（不是证伪）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 口试表把动词写成 GET/POST | 厨房 `method: 'get'/'post'` 小写（axios 合同）。语义对，字符串不是大写 |
| 「非法 userId（0、小数、空串、带字母）发车前抛 TypeError」 | `userSegment` 源码有；`index.test.ts` **没有** 这组 `it`。target 用例全用 `'41'` |
| 「空白 interfaceId 同样先抛」 | `interfaceSegment` 源码有；无单测。空白校验用 `trim()`，真正上路的是**未 trim** 的原串 |
| `userQuery.limit` 卡 1–100 | 源码有；测试 `limit: 20` 合法值，未打 0/101 |
| 「再用 generation 丢弃过期响应」 | `workflow.ts` 69 / 143 / 147 有计数器；测试无并发 load 夹具 |
| Issued 多一个非 `appSecret` 的 `secret*` 键 | 源码 `secretKeys.some(key => key !== 'appSecret')` 会关闸；测试只打了空串 `appSecret` 与 Summary 夹带 |
| `projectOpenApiResponse` 要求 `code` 是整数，**不**要求 `code===200` | 171–180。信封非 200 的失败关闭在 axios interceptor（邻格 L-007），不是厨房第二道码闸 |
| 工作区 create 传入 `' Operations '`（两端空格） | 测试 47–50。trim 发生在厨房 `createInput`，不在剧本 |

问：在根导出、两扇柜台 URL/动词、create 剥多余字段、`order%2Fquery`、create/reset 独带 no-store、Summary 拒 secret、403 原样拒绝、非 `systemService` 字段、home 不雇已经能互证的前提下，(a) 是否还要把「动词小写」「userId TypeError 无单测」「JSON 404 先被 axios 扔掉」写成开篇半句，才算厨房讲完？还是这些已经落在课的表/反例或邻格里，Lead 可以直接盖章？

## Q3 假设与反例

- objective_id: OBJ-33
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无 `systemService.openApi`；根有工厂；无 HMAC 头；POST delete；Summary 拒 secret；资料页无 `userId:`；权限按 scope 换字典；404≠forbidden；home 不雇；`getInterface` 非 UI 枪；capabilities 无 openApi；`repeatSubmit` 只在 issued；JSON code 404 走 interceptor
- difficulty: high

课的隐含假设若反转，合格读者会把哪把枪或哪座包认错？

1. **「`systemService.openApi` / 独立包 `@namewta/domain-openapi`。」** 没有。课开篇包边界表 / 误区 1 覆盖。
2. **「只能走子路径，像监控那样。」** 半对。OpenAPI 根 barrel **和** `./open-api` 都能 import；监控才是根找不到。课变式 1 / L-024 对照覆盖。
3. **「资料页就是菜单页 `OpenApi`。」** 菜单组件名 `OpenApi` = `OpenApiAdminPage.vue`。资料页文件小写 `openApi.vue`，scope 永远 current-user。课误区 2 覆盖。
4. **「前端会 HMAC / 会写 `X-App-Key`。」** 厨房目录零命中。卡车是 Bearer 登录票。课反例 1 / 误区 3 覆盖。属 L-032 负空间。
5. **「明文不回显 = create 也不返回密钥。」** Issued 必须有非空 `appSecret`；不回显指 GET / 列表 / 启停 / 关掉对话框之后。课误区 4 覆盖。与 L-031 同一句话。
6. **「删除用 DELETE / PUT。」** POST `.../delete`。课误区 5 覆盖。对齐 API-005 存量「变更 POST」。
7. **「工作区把密钥写入 sessionStorage 方便刷新后再看。」** 测试禁止这两类 API。课误区 6 覆盖。
8. **「点接口详情等于 `getInterface`。」** 不等于。抽屉吃列表行。课边界 2 / 误区 7 覆盖。
9. **「`openApi.vue` 会搜用户。」** 不会。`listUsers` 只出现在 AdminPage 与 `targetUser`。课误区 8 覆盖。
10. **「权限全是 `system:openApi:self`。」** 只对 current-user。target-user 四动作拆 query/add/edit/remove，外加选人 list。课误区 9 覆盖。
11. **「404 就是没权限。」** 403 才是 forbidden。凭据 404 且目录成功 = 空柜子。课边界 1 / 误区 10 覆盖。
12. **「home-web 也有资料页 OpenAPI。」** 门户不雇这座厨房。课误区 11 覆盖。home 源码零命中。
13. **「`capabilities` 有 openApi 所以厨房在领域模块清单里。」** 清单没有这一项。课边界 4 / 误区 12 覆盖。
14. **「`repeatSubmit: false` 出现在每一枪。」** 只在 create/reset 的 issued headers。课误区 13 覆盖。
15. **「本课覆盖机器网关失败关闭。」** 那是 L-032。本课失败关闭是投影拒收畸形 JSON 和缺权限不发命令。课误区 14 覆盖。
16. **「资料页传 `target-user` / 自助室填别人 userId。」** 壳测试禁止 `userId:`。owner 由后端 `LoginHelper.getUserId()` 决定（L-031）。课反例 2 覆盖。
17. **「只有 list 权限就替别人 create。」** 工作区进厨房前失败关闭。课反例 4 / 测试 105–117 覆盖。
18. **「`groupOpenApiCatalog` 说明页面按 path 第一段分了手风琴。」** 厨房导出，表格没用它。课边界 3 覆盖。
19. **「两张门脸也在 `createOpenApiService` 格子里，因为 OBJ-33 写了一句。」** 课自己写明「格子仍只认工厂」。把假设反转后，Lead 会用本行假闭合 `OpenApiAdminPage` / 九工厂行。
20. **「工作区读到的 404 是 HTTP 状态行。」** 管理端成功码闸在 axios：JSON `code=404` 已变成抛出的 `{ kind, code }`。课把四态写成 `status`/`code`/`response.status` 的并集，没有把「信封先被 adapter 拆掉」写成反例。口试若说「厨房自己看 JSON code」，会把 L-007 的拦截器算进本工厂。

问：读完后仍说不出的，是不是「工厂的名字」，而是 **本行不含两张 Vue、动词在源码里是小写、JSON 404 先被 axios 扔掉、备用枪 `getInterface` 不是抽屉数据源、权限按 scope 换字典**？

## Q4 视角

- objective_id: OBJ-33
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-33 vs L-007/L-020/L-022/L-024/L-030/L-031/L-032；九工厂行；API-005；home-web；capabilities
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:createOpenApiService`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 厨房独行 vs OBJ 一句两件；Vue 符号未入库；(b) 无投影/NO_STORE 行；(d) 无管理端钥匙流 | **是** 闭合定义。厨房行可单独 covered。两张页不要塞进本行或九工厂行 |
| 厨房作者 | 17 枪 URL/动词；`createInput` 白名单；`userSegment`；Issued/Summary 投影；no-store 只在 create/reset | **是** (a) 逐方法。密文四列 / HMAC **否** |
| 资料页作者 | 薄壳 current-user、静态页签、手搓六字段 runtime | **否** 本行符号。OBJ 对照门。本课已讲，矩阵未建 Vue 行 |
| 超管页 / 柜员剧本 | list 闸、`:key` 换人、权限字典、allSettled 空柜子、信封 dismiss、抽屉不打详情枪 | **否** 本行。共用剧本是对照。不要发明 `createOpenApiWorkspaceController` 行 |
| 厅堂电工（L-007） | `domainHttp` 懒电线、并列单例、axios Bearer | 插头一句 **是**。12 个 `create*` 全表 **否** |
| 导航 host（L-020） | 键 `system/openApi/index` 名 `OpenApi`；runtime 口 `openApi` | 对照一句 **是**。15 键/恢复链 **否** |
| 目录窗（L-030） | 四扇 GET `/self\|users/*/interfaces` | URL 对齐 **是**。过滤规则 / Registry **否** |
| 钥匙柜台（L-031） | 十三扇 POST/GET；密文四列；create 不作废会话 | 运输箱 / 不传 owner / 明文只在签发盒 **是** 边界。窗内加密 **否** |
| 机器门（L-032） | 五颗 `X-OpenAPI-*` 头 | **否**。负空间一句即可 |
| 监控厨房（L-024） | 必须子路径；根不导出 | 对照「根有 OpenAPI、没有 monitor」**是**。四抽屉 **否** |
| 资料页邻签（L-022） | 同文件其它 tab；本页签无 `v-hasPermi` | 静态插入 **是** 对照。社交/改密 **否** |
| API-005 | 变更目标 POST；本厨房删除已是 POST `.../delete` | 盘点现状 **是**。不发动传输 Ratchet **否** |
| axios 信封（adapter） | JSON `code!==200` 先 reject；`repeatSubmit` 是连点闸 | 厨房看得到的是抛错对象 **是** 边界。拦截器实现 **否**（L-007） |
| Redis 8 / MySQL 8.4 | 机器会话 / 凭据表 | **否**（L-031 / L-032 / L-084 / L-085） |
| 九工厂行 | `createSystemWebDomain` 等 | **否**。不要因为本课标那一行 covered |

问：Lead 若凭「两扇柜台都在函数表里」把 `createOpenApiService` 标 covered，是否还要求读者能指着 **非 systemService 字段、create/reset 独带 no-store、Summary 拒 secret、卡车是登录票、资料页无 userId**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要把两张 Vue 算进来？

## Q5 元问题

- objective_id: OBJ-33
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 厨房独行、无 Vue 行、无 (b)
- difficulty: low

建议：`defer`

原因：

- **`A:createOpenApiService` 与矩阵 (a) 该行 1:1。** L 合同章节齐全（直觉十二句、定义表、机制链 1–5、带 caption / 文字等价物 / 图边界的两张图、正反例、变式、误区、暂停走查、来源表 S-005/S-006/S-L033-01…08）。**主叙事站得住且与源码对齐**：独立工厂不是 `systemService` 字段；根 barrel 与 `./open-api` 双导出；两扇柜台 8+9 枪 URL/动词与 L-030 四扇 + L-031 十三扇对得上；删除是 POST；create/reset 独带 `NO_STORE`；`createInput` 剥 `ownerUserId`/`appSecret`；Summary 拒 secret 键、Issued 只要非空 `appSecret`；403 原样拒绝；freeze；home 不雇；厨房无 HMAC 头。不必为「工厂名字」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例或邻格，不挡住把**本行**标 covered：（1）源码动词小写 `'get'`/`'post'`；（2）`userSegment` / 空白 `interfaceId` / `limit` 1–100 源码有、厨房单测未打非法值；（3）JSON `code=404/409` 先被 axios 变成抛错，厨房投影不看信封码；（4）`generation` 丢弃过期响应无并发夹具；（5）工作区 create 传入未 trim 的 `appName`，trim 在厨房。不要把两张 Vue、HMAC 网关、密文四列、`createSystemWebDomain` 15 键塞进厨房格。
- 不建议 `mine-more`：b01 未证明本行还有真实矩阵空洞；再五问只会把工作区剧本、L-031 密文或 L-032 五颗头再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；半句精度不是第二容器。不建议 `split-lesson`：未满 10 问；工厂与两张门脸是 chain 原设计的同一 OBJ-33、同一管理端浏览器柜台，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：投影失败关闭、`NO_STORE`、信封不进 storage 是 (a) 的解释到副作用。不建议把 Vue 升成矩阵第二格才盖章：课已写明格子只认工厂；OBJ 的 `openApi.vue` 是对照走查。
- `openApi.vue` / `OpenApiWorkspace` / `OpenApiAdminPage` 课内已讲完且源码可复现，但**矩阵没有对应行**。这是 Lead 改矩阵（若要坚持 OBJ 双名）或把对照留在本行证据备注，不是 miner 再问、也不是把两张页并进九工厂行。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:createOpenApiService | 根+子路径双导出、两扇柜台 URL/动词、create 白名单、`order%2Fquery`、create/reset 独带 no-store、Summary 拒 secret、Issued 一次性、403 原样拒绝、非 `systemService` 字段、home 不雇、无 HMAC 头——均与源码对齐。残留精度：（1）动词小写；（2）非法 userId/interfaceId/limit 无厨房单测；（3）信封非 200 在 axios 先 reject；（4）generation 无并发夹具；（5）剧本传入未 trim 的 appName。 | 可选半句：口试动词以厨房 `method` 小写为准；404/409 四态看到的是 adapter 抛错上的 `code`，不是厨房第二道信封闸。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-033 来源表 S-L033-01…08）。 | defer（本行可 covered；精度项不挡盖章） |
| openApi.vue / OpenApiWorkspace / OpenApiAdminPage | 课已讲薄壳 current-user、超管先 list 再 target-user、权限字典、allSettled 空柜子、信封双关闭、抽屉不打 `getInterface`、无 localStorage。源码可复现。矩阵 (a)/(b) **无此符号**；九工厂行也 **无** Vue 页。 | 不要并进 `createOpenApiService` 才能盖章，也不要改九工厂行。若 Lead 要把 OBJ 双名落成格子，另建行后再标 covered。本批不把门脸当本行缺口。 | defer（未入库对照；Lead 可选补行，不走 b02） |
| A:SysOpenApiCatalogController.* | 本课只对 self/users GET + encode。过滤/烙印未讲。 | 保持。L-030。 | defer（L-030） |
| A:SysOpenApiCredentialController.* / B:create/reset | 本课只保证不传 owner、Issued 亮一次、Summary 拒 secret。密文四列 / 超管闸 / 作废会话未讲。 | 保持。L-031。 | defer（L-031） |
| B:OpenApiSigner / 机器会话 / nonce | 厨房无签名头。 | 保持。L-032。 | defer（L-032） |
| A:admin-web.services / C:admin-web-composition | 插头 `createOpenApiService(domainHttp)` 已借用。 | 保持。全表 L-007。 | defer（L-007） |
| A:createSystemWebDomain / B:navigation host | 键 + `runtime.openApi` 已点。15 键全表未讲。 | 保持。L-020。 | defer（L-020） |
| A:createMonitorService | 根有 OpenAPI、无 monitor 的对照。 | 保持。L-024。 | defer（L-024） |
| A:SysProfile / 资料页其它签 | OpenAPI 页签静态插入；社交/改密/在线设备未认。 | 保持。L-022。 | defer（L-022） |
| API-005 变更 POST | 本厨房删除已是 POST `.../delete`。 | 本课盘点，不发动 Ratchet。 | defer（传输 Ratchet） |
| (b) 投影 / NO_STORE / 信封不进 storage | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 管理端钥匙/货架流 | 矩阵无此行。ASCII 图不是新的 (d)。 | 不要发明浏览器 OpenAPI 数据流行。 | defer（未入库） |
| MySQL 8.4 / Redis 8 | 凭据表 / 机器会话不在本格。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085 / L-084） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本行无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出工厂目的（管理端浏览器两扇柜台）、主路径（self GET 名牌+货架 / POST create 亮信封）与失败路径（投影拒 secret、缺权限不发命令、凭据 404 且目录成功=空柜子）；门脸缺的是矩阵行名不是讲义；把可选半句写进原课不会超过 40 分钟；目录/凭据/HMAC/厅堂插头/菜单键已有邻课。满 10 问前不拆。工厂与两张门脸虽跨 domain 包、web-domain 包与 admin-web 壳，仍是 chain 原设计的同一 OBJ-33，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-030 目录、L-031 凭据、L-032 网关、L-007 厅堂、L-020 导航、L-024 监控对照、L-022 资料页邻签、API-005、未入库 (b)/(d)/Vue 符号以及 MySQL/Redis 容器显式建议 `defer`；对 `A:createOpenApiService` 建议 Lead 标 `covered`（证据锚本 probe 与 L-033 来源表）。本课不主张改 Java 窗、九工厂行、HMAC 或任何 (b)(c)(d) 格子。
