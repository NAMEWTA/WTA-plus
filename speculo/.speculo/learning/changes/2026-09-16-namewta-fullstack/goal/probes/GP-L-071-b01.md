---
artifact: learning-goal-probe
probe_id: GP-L-071-b01
lesson_id: L-071
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-071 / b01

审问对象是已写成的 Lesson `lessons/L-071-flw-spel.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **一格**：`A:FlwSpelController.*`。OBJ-71 原文：能口述 `FlwSpelController`。档是**解释+走查**。矩阵 (a) **有独行**，五名与磁盘 1:1：`list, getInfo, add, edit, remove`，模块 `wta-workflow`，仍 uncovered，备注空。矩阵 (a) **没有** `IFlwSpelService` / `queryList` / `selectSpelByTaskAssigneeList` / `selectRemarksBySpels` / `SpelPage.vue` 行。厨房 `createWorkflowDefinitionService` 与九工厂行里的 `createWorkflowWebDomain` 仍 uncovered（L-073）。矩阵 (b) **没有** `viewSpel` 唯一闸 / TODO 删除 / 垃圾预览独行；workflow 已印的 (b) 是 `IFlwTaskService` 五支副作用（已 covered，挂 L-070）与 `TestLeaveController.submitAndFlowStart`（仍 uncovered，挂 L-072）。矩阵 (c) `wta-modules` / `layered-classic` 已 covered（L-002 / L-003）；登记表 classic 行就是 `wta-modules/wta-workflow`。MySQL 8.4 / Redis 8 / `dao-mapper-ownership` 仍 uncovered。矩阵 (d) **没有** SpEL CRUD 走廊；已 uncovered 的「主路径 工作流请假」原文仍是 `TestLeave.submitAndFlowStart → 任务完成`（挂 L-072, L-070）；「失败路径 工作流驳回/终止」已 covered（L-070）。本批按派单只审问这一格 (a) 切片；口试按磁盘：同一门牌 `/workflow/spel` **正好 5** 个公开映射，classic 三跳，门卫只抱 `IFlwSpelService`，**没有** `/export`，`list` 是 `PageResult`，删除是 `Long[] ids`，唯一键是 `viewSpel` 字符串，目录**不执行**表达式。不要把 Service/Mapper/设计器两枪升成第二张 (a)，不要发明 (b)，也不要把 L-067…L-070 / L-072 / L-073 邻课算进本切片。

抽查锚点（2026-09-17）：`FlwSpelController.java`；`IFlwSpelService.java`；`FlwSpelServiceImpl.java`；`FlwSpelMapper.java` 与空 XML；`FlowSpel` / `FlowSpelBo` / `FlowSpelVo`；`ConditionalOnEnable`；`application.yml` `warm-flow`；对照 `FlwCategoryController` / `FlwDefinitionController` / `FlwInstanceController` / `FlwTaskController` / `TestLeaveController`；`RepeatSubmit` / `BaseController.toAjax` / `R.fail` / `HttpStatus.ERROR` / `GlobalExceptionHandler`；`SystemConstants.NORMAL`；`FlowConstant.INITIATOR*`；`TaskAssigneeEnum`；`FlwTaskAssigneeServiceImpl`；`WorkflowPermissionHandler`；`SpelRuleComponent`；`TaskAssigneeDTO.convertToHandlerList`；登记表 `03-backend-module-modes.md`；`wta-admin/pom.xml` `bundle-core`；`30-cde-workflow.sql`；请假 JSON `leave4.json` / `leave6.json`；厨房 `domains/workflow/src/{index,index.test,spel/index}.ts`；`SpelPage.vue`；web-domain `permissions` `workflow-spel`；e2e `workflow-definition.spec.ts`；OpenAPI `/workflow/spel**`。工作树 **零** `FlwSpel*Test`，模块 **零** usecase 包，Controller **零** `ExcelUtil` / `/export`。

## Q1 澄清

- objective_id: OBJ-71
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-71；chain L-071 覆盖列只 `A:FlwSpelController.*`；L-071 `claimed_cells` 一格；矩阵 (a) 五名独行仍 uncovered、无 Service/Vue/设计器行；厨房与九工厂行仍 uncovered（L-073）；(b) 无本课符号、任务已 covered 挂 L-070、请假挂 L-072；(c) 房间与登记表已 covered；(d) 无 SpEL CRUD 流，已 uncovered 的主路径是请假、失败路径已由 L-070 盖章；课开篇五扇 + 负空间 L-067…L-070 / L-072 / L-073 / L-003 / vendor-engine
- difficulty: medium

课把表达式柜台收成「一块牌子、五扇窗、classic 目录本、存字符串不演算」。计划里至少有四套可数对象，派单点名 **一格 (a) 切片**：

1. **OBJ-71 原文。** `course.md`：能口述 `FlwSpelController`。档是解释+走查。一句里是 **走查**，不是 `FlwDefinitionController.publish`、不是 `WorkflowService.startWorkFlow`、不是 `SpelRuleComponent.selectDeptLeaderById`，也没有把厨房五枪写成第二张矩阵行。课把 OBJ 扩成 classic 目录本 + 唯一闸 + 批量删，那是走查，不是第二格。
2. **chain / 课 `claimed_cells`。** 一格。符号就是 `FlwSpelController.*`。矩阵把 `.*` 展开成五个方法名，与磁盘公开映射 1:1。chain **不**写 Service、不写 Vue、不写 (b)。前置是 L-068，不是 L-070。
3. **矩阵 (a) 对齐。** 本课有独行，Lead 按现印五名标 covered **不会**把定义十三扇、任务十六扇或厨房假闭合。不要把 `IFlwSpelService`、`queryList` 或设计器两枪另立一行才给本切片盖章。厨房独行与九工厂行仍 uncovered，本课不得当第二张前端章。
4. **课自己的操作定义。** 门牌 `/workflow/spel`；GET 二 / POST 一 / PUT 一 / DELETE 一；无类上 `@SaIgnore`；**五扇全贴**权限字。门卫只注入 `IFlwSpelService`，不抱 `DefService`。`list` 走 `queryPageList` + `PageResult`，不是扁 `List`、也不是接口上的 `queryList`；**没有** `POST /export`；`getInfo` 无行仍 `R.ok(null)`；`add`/`edit` 唯一闸按 `viewSpel` 抛 `ServiceException` 变 `R.fail`（默认 500）+ `@RepeatSubmit` 默认 5000ms；`edit` 的 `id` **没有** `@NotNull`；`remove` 是 `Long[]`，Valid 只有 TODO，然后逻辑删。电闸 `@ConditionalOnEnable` = `warm-flow.enabled=true`。
5. **目录不是计算器，导出不是第六扇。** OBJ 要「能口述五扇」。课把「菜单有 `workflow:spel:export`、Java/厨房/页上都没有」和「设计器 SPEL 页签走 `selectSpelByTaskAssigneeList`，不是第六扇 HTTP」收进对照走查。不要为了盖章把 `SpelPage.vue` 再立一行，也不要把无页导出升成「本课不认这扇窗」——本课认的是**没有这扇窗**。
6. **邻格。** L-067 分类七扇（扁列表、有 export、单个删、树无权限字、删前 601）。L-068 发布/导入；`permissionFlag` 是抄走的字符串，不是本表外键；跳转 `spel@@` 写在边上。L-069 实例与 `WorkflowService`。L-070 任务枢纽。L-072 请假 `submitAndFlowStart`。L-073 厨房 URL 与十二键菜单。L-003 已认 classic，本课不得再盖 `C:layered-classic`。Warm-Flow 对 `$` / `#` 的求值保持 `deferred(vendor-engine)`。

问：本格闭合标准是「五扇路径/动词/权限/信封 + classic 三跳门卫不抱引擎 + list 分页 / 无 export / 批量 `/{ids}` + 唯一键是 `viewSpel` + 目录 ≠ 求值 ≠ `spel@@`，且不升 Service/Vue/设计器矩阵行、不发明 (b)、不提前盖 L-073」？还是 OBJ 那句必须把五个方法拆成五行、或必须等 GP-L-073 厨房缺枪才给本切片盖章？Lead 若凭「workflow 房间还没挖完」把这一行留空洞，是否假判成未讲完？Lead 若用本课把厨房、请假走廊或厂商求值标 covered，是否假闭合了 L-073 / L-072 / vendor-engine？

## Q2 证据

- objective_id: OBJ-71
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 31–104；IFlwSpelService 20–87；ServiceImpl 37–43 / 52–54 / 64–68 / 77–80 / 88–98 / 107–114 / 124–128 / 135–144 / 155–159 / 169–182 / 192–202；Mapper 13–15；XML 5–7；Entity 20–66；Bo 31 / 51–68；Vo 23–77；ConditionalOnEnable 26–27；yml 408–410 / 423；RepeatSubmit 20；BaseController 19–30；R 119–131；HttpStatus 82；GlobalExceptionHandler 64–70；SystemConstants 13；FlowConstant 14 / 24；TaskAssigneeEnum 44 / 131–137；AssigneeImpl 132 / 257–258 / 278–279 / 311–312；PermissionHandler 62–68；SpelRuleComponent 19–38；TaskAssigneeDTO 60–75；modes.md 22；admin pom 195；SQL 199–217 / 268 / 303–307；Category Controller 46–65 / 122–136；Category Bo 30；Definition Controller 39–40；leave4.json 32；leave6.json 153；厨房 index.ts 327 / 368–372；index.test.ts 69–73 / 88 / 93；spel/index.ts 3；web-domain 90–96；web-domain 测试 35 / 42；SpelPage 42–64 / 183–187 / 238–259 / 305–310 / 332–336 / 344–386；e2e 29–30 / 126–127 / 164–168 / 281–304；OpenAPI 22–39 / 3355–3387 / 7312–7326；零 FlwSpel*Test；零 usecase；零 ExcelUtil /export
- difficulty: high

课的五扇 + classic 目录本哪些能在工作树直接复现，哪些只是测试名单比课表窄、或口试把邻课门卫/侧门说成第六扇 HTTP？

**仍能复现（本切片）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 类上 `@ConditionalOnEnable` `@Validated` `@RequiredArgsConstructor` `@RestController` `@RequestMapping("/workflow/spel")`，继承 `BaseController` | Controller 31–36 |
| 构造只注入 `IFlwSpelService`；无 `DefService` / `LoginHelper` / Excel / `@SaIgnore` / `@DataPermission` | Controller 38；全文无 `ExcelUtil` / `/export` |
| 五扇：`GET /list`、`GET /{id}`、`POST /`、`PUT /`、`DELETE /{ids}` | Controller 47–104 |
| `list` 权限 `workflow:spel:list`，回 `R.ok(PageResult)`，入参带 `PageQuery` | 47–51；对照分类扁 `List` 无 `PageQuery` 46–50 |
| `getInfo` 权限 query、路径 `@NotNull`、`R.ok(queryById)`，无行不 fail | 59–63；ServiceImpl 52–54 |
| `add` 权限 add、`@Log INSERT`、`@RepeatSubmit()`、AddGroup；`toAjax(insertByBo)` | 71–77 |
| `edit` 权限 edit、`@Log UPDATE`、`@RepeatSubmit()`、EditGroup；`toAjax(updateByBo)` | 85–91 |
| `remove` 权限 remove、`@Log DELETE`、**无** `@RepeatSubmit`；路径 `Long[] ids` + `@NotEmpty` | 99–104；对照分类单个 `Long` 122–136 |
| 五扇全贴 `@SaCheckPermission`；title 都是「流程spel表达式定义」 | 47 / 59 / 71–72 / 85–86 / 99–100 |
| `@RepeatSubmit` 默认间隔 5000ms | RepeatSubmit 20 |
| `toAjax(boolean)`：true → `R.ok()`，false → `R.fail()` | BaseController 29–30 |
| `R.fail()` 默认 `HttpStatus.ERROR=500`；`ServiceException` 无 code 时同样 `R.fail(msg)` | R 119–131；HttpStatus 82；Handler 64–70 |
| 电闸 `warm-flow.enabled=true`；LiteFlow `enable` 跟着同一把 | ConditionalOnEnable 27；yml 408–410 / 423 |
| classic：Impl 只抱 `FlwSpelMapper`；另实现设计器两枪；无 usecase 包 | ServiceImpl 37–43；模块 usecase grep 空；modes.md 22 |
| 接口 8 法；HTTP 只用 5 个委托（list 用 `queryPageList` 不是 `queryList`） | IFlwSpelService 28–86；Controller 50 / 62 / 76 / 90 / 103 |
| `buildQueryWrapper`：`orderByAsc(id)`；like 组件/方法/备注；eq 参数/预览/状态；**不读** `bo.params` | ServiceImpl 88–98 |
| 唯一闸：`viewSpel` 非空时 `eq` + `neIfPresent(id)` + `exists` → 「SpEL表达式已存在，请勿重复添加」 | 135–144 |
| `insertByBo` 成功回写 `bo.setId`；`updateByBo` 走 `updateById` | 107–114 / 124–128 |
| `deleteWithValidByIds`：`isValid==true` 只有 TODO；然后 `deleteByIds` | 155–159 |
| 设计器侧门：new Bo，`viewSpel=handlerCode`、`remark=handlerName`、`status=NORMAL`，params 塞 begin/end 但 wrapper 不用 | 169–178；SystemConstants 13 |
| `convertToHandlerList`：storageId=`viewSpel`，handlerCode=`""`，handlerName=`remark`，groupName=`""`，createTime=`createTime` | ServiceImpl 180–181；DTO 60–75 |
| `selectRemarksBySpels`：空列表空 Map；备注空则 `""` | 192–202 |
| `@TableName("flow_spel")` + `@TableLogic delFlag` + `@TableId id` | Entity 20 / 29 / 65 |
| Add/Edit 组：`viewSpel`、`status` `@NotBlank`；`id` **无** `@NotNull`；`params` 默认空 `HashMap` | Bo 31 / 51–68；对照分类 Edit 组 id 必填 Bo 30 |
| Vo 有 `@ExcelProperty`（id/组件/方法/参数/预览/状态字典/备注/创建时间），Controller **没有**导出窗 | Vo 23–77 vs Controller 零 `/export` |
| Mapper 空接口 + XML 空壳 | Mapper 13–15；XML 5–7 |
| 种子两行：部门负责人 `#{@spelRuleComponent.selectDeptLeaderById(#initiatorDeptId)}`；发起人 `${initiator}`（组件方法都 NULL） | SQL 216–217；FlowConstant 14 / 24 |
| 菜单 C 型 `workflow/spel/index` perms list，备注「流程**达**式定义菜单」；F 型五串含孤儿 `workflow:spel:export` | SQL 268 / 303–307 |
| 登记表 classic：`wta-modules/wta-workflow`，不改内部层次 | modes.md 22 |
| `bundle-core` 注释「不装配工作流引擎」 | pom 195 |
| 定义门卫另抱 `DefService`；本课门卫单字段 | Definition 39–40；Spel 38 |
| SPEL 枚举描述「SpEL表达式」，**code 前缀空串**；`$` 或 `#` 开头认成 SPEL | TaskAssigneeEnum 44 / 131–137 |
| 设计器页签 `case SPEL -> selectSpelByTaskAssigneeList`；回显走 `selectRemarksBySpels`；`fetchUsers` 的 SPEL 分支 **空用户列表** | AssigneeImpl 132 / 257–258 / 278–279 |
| `convertPermissions` 把办理人标识交给 `fetchUsersByStorageIds`，SPEL 因此展开成空 | PermissionHandler 62–68 |
| `SpelRuleComponent` 同样 `@ConditionalOnEnable`；没负责人抛「当前部门未设置负责人…」 | 19–38 |
| 厨房五枪：list/get/add/put/delete；**没有** `exportSpel`；`spel-admin` 能力键 | index.ts 327 / 368–372；frontend 搜空 `exportSpel` |
| `deleteSpel(['s1','s2'])` 测成 `/workflow/spel/s1,s2`；`getSpel('spel?#')` 编成 `/workflow/spel/spel%3F%23` | index.test.ts 73 / 88 / 93 |
| 资源标签 `controller:'FlwSpelController'` `basePath:'/workflow/spel'` | spel/index.ts 3 |
| web-domain `workflow-spel` 五串 **不含** export | web-domain 90–96 |
| 页工具栏新增/改/删，无导出钮；预览是只读 `span`；`queryParams.status` 默认 `'0'`，搜索表单只有组件+方法；rules **只**校验 status | SpelPage 42–64 / 183–187 / 10–30 / 248–259 |
| `updateViewSpel`：全空变空串；无组件无方法且恰好 1 参 → `${param}`；缺一半 → 「请填写组件名称和方法名」；组件+方法 → `#{@comp.method(#a,#b)}` | 344–386 |
| 编辑先 `getSpel` 填表；删除走 `row.id` 或勾选数组 `ids` | 305–310 / 332–336 |
| e2e 成功 body：`resolveOwner` / `deptId` / `#{@spelRuleComponent.resolveOwner(#deptId)}` / `status:0`；**无** remark、**无** id | e2e 291–300 |
| OpenAPI：`POST/PUT /workflow/spel`；`GET /{id}` 详情；`GET /list` 写「分页」；`DELETE /{ids}` 写「批量」；**无** `/export` | 22–39 / 3355–3387 / 7312–7326 |
| 请假图纸把 `${initiator}` 抄进 `permissionFlag`；`spel@@#{@testLeaveServiceImpl.eval(#leaveDays)}` 在边上，不在 `flow_spel` | leave4.json 32；leave6.json 153 |
| 无 Java `FlwSpel*Test` | 工作树搜空 |

**课写过、测试名单更窄或口试把邻课侧门说偏（不是证伪本格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「厨房逗号删这次后端绑得上」 | 路径形状是 `Long[]`，数字逗号串能绑。厨房测试夹具是 `'s1,s2'`，**不会**变成 Long。图边界已写「不要把 e2e 夹具 id `s1` 当成生产主键」。口试应说**形状**对齐，不要说 `s1` 能过 `@PathVariable Long[]` |
| 「测试锁死厨房五枪」 | 锁 URL + 逗号删除 + `segment` 编码。**没有** `exportSpel` 不存在的 `not.toContain`，也 **没有** 锁 `id==='web-domain-workflow'` |
| web-domain「权限组五串不含 export」 | 测试只 `toContain('workflow:spel:query')`。五串全量与 export 缺席靠字面量，测试没点名 |
| 暂停第 3 步「`convertToHandlerList` 四个函数参数」 | 机制链写的是 **五个**抽取器（storageId / handlerCode / handlerName / groupName / createTime）。口试以机制链为准 |
| 课 `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |
| 「管理页默认只看见正常行」 | `queryParams.status:'0'` 在数据里；搜索表单没有状态项，也没有 `viewSpel` / `methodParams` 筛选项。状态默认才是会藏停用行的那条 |

问：在五扇映射、classic 三跳、list 分页、无 export、批量 `Long[]`、唯一键 `viewSpel`、Edit 组 id 非必填、Valid 空壳、设计器两枪非 HTTP、厨房五枪没收 export、页面只读预览与垃圾字符串可入库已经能互证的前提下，(a) 是否还要把「夹具 `s1` 不是 Long」「暂停四个函数」「权限数组测试只锁 query」写成开篇半句，才算本切片讲完？还是这些已经落在课的表/图边界里（暂停措辞除外），Lead 可以把本切片视为已讲、并保持厨房/九工厂行 uncovered？

## Q3 假设与反例

- objective_id: OBJ-71
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无第六扇 HTTP export；无 queryList 映射；无单个 DELETE /{id}；无扁 List；无 Edit 组 id 必填；无保存时试跑 SpelRuleComponent；无 permissionFlag FK；无 spel@@ 入本表；无 FlwSpelUseCase；门卫不注入 DefService；无 601；停用≠运行时失效；OBJ-71 不含厨房
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪道闸认错？

1. **「五扇之外还有 export，和分类一样。」** 分类课后端**有** `POST /export`。本课后端**没有**。两边菜单都有 export 串。课反例 1 / 误区 3 覆盖。
2. **「`list` 是扁 `List`，和分类一样。」** `PageResult`。课反例 2 / 误区 4 覆盖。
3. **「删除是单个 id，厨房逗号是前端 bug。」** 本课数组是合同。分类课才是单个 Long。课反例 3 / 误区 5 覆盖。
4. **「`queryList` / 设计器两枪也是 `FlwSpelController.*`。」** 接口有、映射无。`.*` 按 Controller 公开方法数。课反例 4 / 误区 11 覆盖。
5. **「保存时会调用 `SpelRuleComponent` 试算。」** 唯一闸只比字符串。部门没负责人要等到运行时。课反例 5 / 误区 7 覆盖。
6. **「这是 layered，Service 不许碰 Mapper。」** 登记 classic；Impl 必须能指到 `spelMapper`。课反例 6 / 误区 14 覆盖。
7. **「`permissionFlag` 就是 SpEL 表外键。」** 节点上存的是抄走的 `viewSpel` 文本，没有 FK。删目录行不会级联擦图纸。课反例 7 / 误区 8 覆盖。
8. **「`skipCondition` 的 `spel@@#{@testLeaveServiceImpl.eval(#leaveDays)}` 也在这张表。」** 不在。请假 JSON 种子，Bean 也不是 `spelRuleComponent`。课反例 8 覆盖。
9. **「停用后设计器和运行时都算不了。」** 设计器侧门滤 `status=0`。运行时读节点上已抄的字。课反例 9 / 误区 17 覆盖。
10. **「Edit 组会校验 id。」** 不会。分类课会；本课 BO 没有那条。课反例 10 / 误区 10 覆盖。
11. **「唯一闸按组件+方法。」** 按 `viewSpel`。同一 Bean 方法只要拼出不同预览就能并存。课反例 11 覆盖。
12. **「设计器时间筛选能缩小 SpEL 列表。」** `beginTime`/`endTime` 进了 `params`，wrapper 不读。课反例 12 / 误区 9 覆盖。
13. **「`convertPermissions` 会把 `${initiator}` 展开成发起人工号。」** SPEL 分支用户列表为空。课反例 13 / 误区 13 覆盖。
14. **「OBJ-71 包含 `createWorkflowDefinitionService`。」** 那是 OBJ-73。课反例 14 / 误区 12 覆盖。
15. **「表名 `wf_spel`。」** 注解和 DDL 都是 `flow_spel`。课误区 6 覆盖。
16. **「页面校验了 viewSpel。」** rules 只有 status。预览只读，靠拼接。课误区 18 覆盖。
17. **「关电闸还能 GET /list 拿空分页。」** Bean 不注册，是没有这栋楼。课正例 7 覆盖。
18. **「`${initiator}` 和 `#{@bean.method}` 是两种 Controller。」** 同一张表、同一扇 add。课误区 16 覆盖。
19. **「Vo 有 Excel 注解所以有导出窗。」** 死元数据。没有映射就没有窗。课边界 11 覆盖。
20. **「厨房能拼逗号 = 夹具 `s1` 能过 Long[]。」** 形状是合同；`s1` 不是生产主键。课图边界覆盖。
21. **「跨模块读表达式走本课 REST。」** 跨模块合同是 `WorkflowService`，不含 SpEL CRUD。课误区 15 覆盖。
22. **「给目录加五层才算现代化。」** 登记表禁止借机把 classic 房间改 layered。课误区 14 覆盖。

问：读完后仍说不出的，是不是「Controller 的名字」，而是 **五扇合同 ≈ 磁盘映射、缺的是菜单孤儿 export、list 分页不是扁列表、删除是 ids 数组、唯一键是预览字符串、设计器侧门不是 HTTP、目录不演算、九工厂/厨房行不能因本课闭合**？

## Q4 视角

- objective_id: OBJ-71
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-71 vs L-003/L-067/L-068…L-073；claimed_cells 五扇 vs 厨房五枪 / 一张表达式页；厨房独行仍 uncovered
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这一格 (a) 切片**？

| 视角 | 多出来的边 | 是否本课这一格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 五名独行 vs chain `.*`；无 Service/Vue/设计器行；(b) 无 SpEL 独行；(d) 无表达式 CRUD 流，请假走廊挂 L-072 | **是** 闭合定义。本切片可视为已讲。独行可标 covered。不要发明 (b)/(d)，不要把一张 Vue 升成新行才能给切片盖章，不要用本课盖厨房章 |
| 表达式门卫 | 五扇路径/动词/五串权限、500 唯一闸、批量 `Long[]`、`RepeatSubmit` 只罩 add/edit、无 601、无 export | **是** 切片里的 `FlwSpelController.*` |
| classic 厨师 | `viewSpel` 唯一、TODO 删除、wrapper 忽略 params、空 XML、设计器两枪、备注回显 | **是** 走查后果，不是矩阵第二格 |
| 导出窗 | 菜单 F 型有；Java 无；厨房无枪；页无钮；Vo Excel 是死注解 | **是** 对照。本课认的是没有这扇窗，不是「有窗但页上没扳机」（那是分类课） |
| 页面拼暗号 | 只读预览；单参 `${}`；组件+方法 `#{}`；缺一半变中文提示仍能过 NotBlank | **是** 对照。不要升格成前端格子 |
| 厨房作者（L-073） | 五枪 URL；`segment()` 编码；逗号删除；权限组漏 export | 本课借用封面形状与没收窗负空间 **是**。工厂盖章 **否**（L-073） |
| 分类窗（L-067） | 扁列表、有 export、单个删、树无 perm、删前 601、Edit 组 id 必填 | 对照一句 **是**。七扇 CRUD **否** |
| 定义窗（L-068） | 发布闸只问 `permissionFlag` 空不空，**不解析** `#{}`；`spel@@` 在边上 | 对照一句 **是**。发布/导入 **否** |
| 实例/任务（L-069/L-070） | 启动办理不打 `/workflow/spel`；合同认 businessId；任务认 taskId | 负空间 **是**。大厅/枢纽 **否** |
| 请假（L-072） | `submitAndFlowStart`；`eval(leaveDays)` 不是本目录 | 负空间 **是**。示例流 **否** |
| 登记表（L-003） | classic 允许 Impl 抱 Mapper；经公开 Workflow API 接入 | 不改登记 **是**。房间画像 **否**（已 covered） |
| 办理人枚举 / 权限处理器 | SPEL 前缀空、靠 `$`/`#`；`convertPermissions` 空用户 | 对照 **是**。求值内部 **否**（vendor-engine） |
| 规则 Bean | `SpelRuleComponent.selectDeptLeaderById` 运行时才炸 | 对照 **是**。不是第六扇 REST **是**。部门房间 **否** |
| MySQL 8.4 | 表 `flow_spel` + 两行种子 + 菜单孤儿 export + 备注掉字 | 种子对照 **是** (a)。容器 **否** |
| bundle-full-job | 模块只进 full；core 不装配引擎 | 装配对照 **是**。容器行 **否**（L-079） |

问：Lead 若凭「五个方法名在独行里」把该行标 covered，是否还要求读者能指着 **五扇合同、classic 三跳、分页/无 export/批量 ids、唯一键 `viewSpel`、目录≠计算器、厨房没收 export、设计器侧门非 HTTP**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要等 L-073 才给**本切片**当已讲？厨房独行是否必须保持 uncovered，本课只往备注写「表达式五扇消费已讲」？

## Q5 元问题

- objective_id: OBJ-71
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 五名独行仍 uncovered、厨房独行仍 uncovered、无本课 (b)/(d)
- difficulty: low

建议：`defer`

原因：

- **`A:FlwSpelController.*` 是 chain 切片，也是矩阵独行。** L 合同章节齐全（宏观地图、定义表、机制链 1–7、带 caption / 文字等价物的三张图、正反例、变式、误区、暂停走查、来源表 S-004/S-008/S-010/S-L071-01…08）。能复现：五扇映射与权限字、list 分页、无 export、`getInfo` 可空、add/edit 按 `viewSpel` 唯一闸 + 5 秒防重、remove 批量逻辑删且 Valid 空、classic 门卫不抱引擎、接口 8 法里 3 个无 HTTP、设计器两枪非 REST、表名 `flow_spel`、两行种子两种暗号、厨房五枪没收 export、页面只读预览与默认 `status=0`。不必为「Controller 的名字」再挖五问。
- **Lead 可以把本课这一行标 `covered`。** 五名与磁盘 1:1，不会绑死定义/任务/厨房。**厨房独行与九工厂行保持 uncovered**，等 L-073，不要用本课开第二张前端章。不要发明表达式 (b) 行：唯一闸 / TODO 删除 / 垃圾预览已是 (a) 的解释到副作用。
- 本批可粘贴的精度项不挡住切片视为已讲：（1）暂停第 3 步「四个函数」以机制链五个抽取器为准；（2）厨房测试 `/s1,s2` 证明形状不是生产 Long；（3）web-domain 测试只锁 `spel:query`；（4）课估时 38、chain 35。不要把发布闸、`WorkflowService`、任务十六扇、请假七枪、厨房 URL 全表、厂商 `$`/`#` 求值塞进本格。
- 不建议 `mine-more`：b01 未证明本切片还有真实矩阵空洞；再五问只会把 L-073 厨房、L-068 发布、L-067 分类对照或 L-072 请假再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；暂停措辞是半句精度，不是第二容器。不建议 `split-lesson`：未满 10 问；五扇是 chain 原设计的同一 OBJ-71，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行。不建议等 GP-L-073 才给本切片当已讲。不建议重盖 L-003 classic。不建议把 SPEL 空用户列表升成厂商引擎 covered。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:FlwSpelController.list, getInfo, add, edit, remove | 课按磁盘讲了五扇路径/动词/权限/信封、classic 三跳、list 分页、无 export、批量 `/{ids}`、唯一键 `viewSpel`、Edit 组 id 非必填、Valid 空壳、设计器两枪非 HTTP、厨房没收 export、页面只读预览与垃圾字符串可入库。残留精度：暂停第 3 步「四个函数」；厨房夹具 `s1` 不是 Long；权限测试只锁 query；课估时 38 vs chain 35。 | 可选半句：口试 `convertToHandlerList` 以五个抽取器为准；厨房逗号删认路径形状，不认夹具 `s1`。不要求重写机制链。Lead **可以**改本行为 covered。 | defer（切片已讲；独行可闭合） |
| A:createWorkflowDefinitionService | 厨房独行仍 uncovered。本课只借用五枪 URL 与没收窗负空间。 | 不要用本课盖厨房章。备注可写「表达式五扇消费已讲」。 | defer（L-073） |
| A:createWorkflowWebDomain（九工厂行） | 本课只用 spel 五串权限 + 无 export 当切片证据。其余工厂未因本课讲完。 | 保持 uncovered。 | defer（L-073 / 九工厂行保持 uncovered） |
| A:FlwCategoryController.* | 扁列表、有 export、单个删、601 三道黄灯已借用对照。 | 保持。L-067。 | defer（L-067） |
| A:FlwDefinitionController.* / B:IFlwDefinitionService.publish,importJson,removeDef | 发布闸不解析 `#{}`；`permissionFlag` 不是本表外键。 | 保持。L-068。 | defer（L-068） |
| A:FlwInstanceController.* / A:WorkflowService.* | 启动办理不打 `/workflow/spel`；跨模块合同不含表达式 CRUD。 | 保持。L-069。 | defer（L-069） |
| A:FlwTaskController.* / B:IFlwTaskService.startWorkFlow,completeTask,backProcess,urgeTask,terminationTask | 任务窗认 taskId；暗号本不启流。五支 (b) 已由 L-070 盖章。 | 不要并进本目录。 | defer（L-070 已 covered） |
| A:TestLeaveController.list, export, get, add, submitAndFlowStart, edit, remove / D:主路径 工作流请假 | `eval` 不是 HTTP；`spel@@` 不在 `flow_spel`；(d) 走廊不是表达式 CRUD。 | 不要并进五扇。 | defer（L-072） |
| D:失败路径 工作流驳回/终止 | 本柜台无 backProcess / terminationTask。 | 保持已 covered（L-070）。 | defer（L-070 已 covered） |
| C:layered-classic / wta-modules | 登记表 classic 行已 covered。 | 不要用本课再盖房间章，也不要发动迁 layered。 | defer（L-003 已 covered） |
| (b) viewSpel 唯一闸 / TODO 删除 / 垃圾预览 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 表达式 CRUD 流 | 矩阵无此行。已有的工作流走廊仍是请假。 | 不要发明前端表达式数据流行。 | defer（未入库） |
| Mapper XML / BO/VO getter | 空 XML 与校验组已走查。 | 保持 covered-by-parent。 | defer（covered-by-parent） |
| 测试夹具 FlwSpel*Test | 工作树没有这类测试。 | 保持 deferred(test-fixture)。证据走 Controller/SQL/e2e。 | defer（deferred(test-fixture)） |
| WarmFlow 厂商引擎内部 | `$` / `#` 求值只点到包边界；`convertPermissions` 对 SPEL 返回空用户。 | 保持 deferred(vendor-engine)。不要把空用户列表说成「已经讲完引擎」。 | defer（deferred(vendor-engine)） |
| MySQL 8.4 / bundle-full-job | 种子表与菜单孤儿 export 是 (a) 对照；模块只进 full。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-079 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本切片无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（厨房/定义/请假/厂商求值是邻课或 deferred 格子，课内已把五扇讲完并显式禁止整行假闭合）；合格读者说得出函数目的（柜台上的暗号本，不是翻译机）、主路径（分页列表 / 可空详情 / 过唯一闸新增改名 / 批量逻辑删）与失败路径（`viewSpel` 撞车 500、空 id 先撞唯一或 0 行 fail、无行详情仍 200、关电闸整栋消失、停用不擦已抄走的图纸、垃圾预览可入库）；把可选半句写进原课不会超过 40 分钟；邻课已规划。满 10 问前不拆。五扇虽跨读写/分页/批量删，仍是 chain 原设计的同一 OBJ-71，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对厨房/九工厂、L-067/L-068/L-069/L-072 邻窗、L-070 已 covered 的任务格、L-003 已 covered 的登记表、未入库 (b)/(d)、covered-by-parent 的 XML、deferred 的测试夹具与厂商引擎、以及 MySQL/bundle 容器显式建议 `defer`；对 `A:FlwSpelController.*` 建议视为已讲切片、**可以**把矩阵五名独行标 `covered`、**不要**用本课盖仍 uncovered 的厨房独行与九工厂行。本课不主张改 Java 窗、发明 Vue 矩阵行、把设计器两枪升成第六扇 HTTP、或把 `SpelRuleComponent` 当成计算器窗。
