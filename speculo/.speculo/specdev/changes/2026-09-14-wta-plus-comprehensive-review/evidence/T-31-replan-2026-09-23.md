# T-31 evidence

Current HEAD `851662d7` already meets AC-031. No new implementation was required. This cancels only the repeated construction. AC-031 stays owned by T-30. The adjacent OIDC change was not modified.

## Real Profile to Notify

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=EnterpriseQueuedNotificationIntegrationTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Dprofile.notify.integration=true \
  -Dnotify.mysql.integration.url='jdbc:mysql://127.0.0.1:33143/namewta_notify_test_t31b?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC' \
  -Dnotify.redis.integration.port=33144
```

Exit 0. Isolated MySQL `mysql:8.4.9` (`sha256:c36050afdca850f23cef85703f84c7531a5ae155a11b5ee1c60acb09937c4084`) on `127.0.0.1:33143`, database `namewta_notify_test_t31b`, loaded `10-cde-base-ddl.sql` through `50-cde-base-dml.sql`. Redis `redis:8.6.3` on `127.0.0.1:33144`. The MySQL password was supplied only through the test's existing environment variable and is not recorded. Both containers were removed after the run.

`EnterpriseQueuedNotificationIntegrationTest`: tests=8, failures=0, errors=0, skipped=0, 8.854s. It uses the real profile transfer use case, the real notification runtime, and a real outbox worker. The supplier and the public identity lookup are the only replaced edges. A queued submission waits for that worker and transfers once. A record write failure rolls back the notification and revokes the staged challenge. An outer rollback leaves only an unconfirmable Redis challenge with its original TTL. A consumed code with rolled-back bindings is recovered by sending a new challenge. A Redis consume failure rolls back the binding and event writes. A changed source version rejects an accepted notification even with the correct code. An expired association and a typed-unsent terminal cannot activate, but a resend is allowed. A generic failure without a single target stays unknown and cannot activate.

## Profile MySQL and Redis, plus unit contract

`EnterpriseTransferMySqlRedisE2ETest` is `@Tag("e2e")`, so the default dev profile does not run it. It was run with `-Dprofiles.active=e2e` and `PROFILE_MYSQL_E2E_URL` / `PROFILE_REDIS_E2E_ADDRESS` on `127.0.0.1:33141` / `33142`, database `namewta_transfer_test_t31`. tests=1, failures=0, errors=0, skipped=0, 4.022s. Send returns `QUEUED`. Confirm after the notification snapshot is `ACCEPTED` transfers once; a second confirm returns `ENTERPRISE_TRANSFER_CHALLENGE_INVALID`. The target can then unbind. That test still stubs the notification port, so it is not the real notify proof above.

```bash
cd backend && ./mvnw -pl wta-modules/wta-profile/wta-profile-enterprise -am test \
  -Dtest=EnterpriseTransferServiceTest,EnterpriseTransferPersistenceTest,EnterpriseTransferHttpContractTest \
  -Dsurefire.failIfNoSpecifiedTests=false
```

Exit 0. tests=16, failures=0, errors=0, skipped=0. These include queued submit without activation, delivery failure without a confirmable challenge, queued notification cannot be consumed, failed notification can be resent, and a consume race rolls back instead of transferring twice.

`NotifyCallerMigrationContractUnitTest`: tests=1, failures=0, errors=0, skipped=0. It reads the current sources and checks that captcha uses scene `auth-captcha`, person rebind uses `person-rebind`, enterprise transfer uses `enterprise-transfer`, and the demo mail/SMS controllers use `NotificationApplicationService` instead of the old builders. It does not execute those flows.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase, operation `verify-affected-contract-and-quality-gates`, status passed. No production source edit.
