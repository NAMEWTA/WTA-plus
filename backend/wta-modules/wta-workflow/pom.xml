<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>org.namewta</groupId>
        <artifactId>wta-modules</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>wta-workflow</artifactId>

    <description>
        工作流模块
    </description>

    <dependencies>

        <!-- 消息推送模块 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-push</artifactId>
        </dependency>

        <!-- 接口模块 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-doc</artifactId>
        </dependency>

        <!-- 统一通知服务 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-notify</artifactId>
        </dependency>

        <!-- 数据库服务 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-mybatis</artifactId>
        </dependency>

        <!-- web服务 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-web</artifactId>
        </dependency>

        <!-- 日志记录 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-log</artifactId>
        </dependency>

        <!-- excel -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-excel</artifactId>
        </dependency>

        <!-- 翻译模块 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-translation</artifactId>
        </dependency>

        <!-- 安全模块 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-security</artifactId>
        </dependency>

        <!-- api模块 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-api</artifactId>
        </dependency>

        <!-- Warm-Flow 工作流引擎 -->
        <dependency>
            <groupId>org.dromara.warm</groupId>
            <artifactId>warm-flow-mybatis-plus-sb4-starter</artifactId>
        </dependency>

        <!-- Warm-Flow UI 插件 -->
        <dependency>
            <groupId>org.dromara.warm</groupId>
            <artifactId>warm-flow-plugin-ui-sb-web</artifactId>
        </dependency>

        <!-- LiteFlow 规则编排 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-liteflow</artifactId>
        </dependency>

    </dependencies>

</project>
