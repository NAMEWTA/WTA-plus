# T44 v6 private backend-log diagnostic handoff

Fixed parent evidence: `/tmp/wta-t44/runs/16f1337c6c560efe/result.json` records v5 `accepted=false`, failure `postcheck/owned backend raw log contains credential material`, source clean `e11c1b6f3a4a4a8bdc1746044d65fe8445c0865c`, JAR SHA-256 `835b4de84b911ff77ba20ba003218f30af015559dccfadcdbc30fdf1d8d069a8`, and `cleanup_errors=[]`. Its HTTP/MinIO/diagnostic/STALE/default-switch/OpenAPI results are useful partial evidence, not ticket acceptance. The v5 raw backend log was deleted by its `finally` cleanup; this review did not recover or read it.

Derived `/tmp/wta-t44/run-minio-real-v6.py` SHA-256 `e0ede6bc885f092941e03066338b862449150bd10aeed7eb3548ce2ae615d676`; it has not been run against services. It preserves v5's strict leak gate and adds the tenth known canary, the generated monitor password. All ten fixed categories receive exact occurrence counts. For matching log lines it retains only raw-log line number, bounded logger/exception class names, optional Java stack file/line, and category counts; no raw message text or URL is retained. It also redacts the two issued session tokens, generic Authorization values, JWT-shaped tokens, and complete HTTP/S3/JDBC URLs in memory before computing a sanitized-log hash. Both raw snapshot SHA-256 and sanitized snapshot SHA-256, byte/line counts and replacement counts go into a 0600 JSON summary under an owned 0700 `diagnostics` directory; no log context is saved. The raw JVM log remains deleted by the existing cleanup. `result.json` contains only the private summary path, hashes and counts. A leak still raises the original fixed failure and leaves `accepted=false`.

Safety checks: `python3 -m py_compile /tmp/wta-t44/run-minio-real-v6.py` exited 0. Synthetic-only `/tmp/wta-t44/test-v6-log-diagnostic.py` exited 0, verifying two category hits, exception/stack locations, JWT/URL/token redactions, no canary bytes in the saved summary, and directory/file modes 0700/0600. No Maven, Docker, JVM, HTTP service or production data was used in preparation.

Lead should review this exact v6 hash before an owned rerun. A new run must be judged on its own `accepted`, full source/JAR proof and cleanup. If it still finds a canary, inspect only the private safe summary to locate the logger and code path; do not copy raw logs into public evidence or turn the strict gate off.
