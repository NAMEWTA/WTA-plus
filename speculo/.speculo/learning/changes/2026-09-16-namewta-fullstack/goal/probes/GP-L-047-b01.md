---
artifact: learning-goal-probe
probe_id: GP-L-047-b01
lesson_id: L-047
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-047 / b01

审问对象是已写成的 Lesson `children/2026-09-14-wta-notify/lessons/L-003-notify-config.md`（chain 映射为本单元 L-047）与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain 覆盖列 / 矩阵 (a) **两行**）：`A:NotifyConfigController.*`、`A:NotifyConfigUseCase.*`。OBJ-47 原文：能口述 `/notify/config` 账号/场景/测试发送。档是**解释+走查**。child 课 OBJ-03 把同一窗收成 `NotifyConfigUseCase` → `NotifyConfigService` / `NotifyTestSendService`。矩阵 (a) Controller 行把十方法写全：`listAccounts, getAccount, addAccount, editAccount, changeStatus, removeAccount, listScenes, saveBinding, testAccount, testTemplate`（与磁盘 `*` 1:1，没有 PUT/DELETE/`IN_APP` 账号窗）。矩阵 (a) UseCase 行只写类名，不列方法。两行均仍 `uncovered`。矩阵 (b) **没有**密钥只写 / SYNC 试发 / 场景目录独行。矩阵 (c) `wta-modules` 业务房间已 covered（L-002/L-003）；MySQL 8.4 / `dao-mapper-ownership` 仍 uncovered。矩阵 (d) 「主路径 通知提交」仍 uncovered，备注 L-051、L-052；课 `claimed_cells` **未**列 (d)。本批按派单只审问 **十扇配置窗 + UseCase 两字段分叉**。公告六扇留给 L-045；收件箱留给 L-046；收件人搜索留给 L-048；监控 snapshot/deliveries 留给 L-049；供应商回调留给 L-050；`NotificationController.submit` / Runtime 写 Outbox 留给 L-051；Outbox 领取留给 L-052；厨房 `createNotificationService` 留给 L-053；配置页 Vue / `createNotifyWebDomain` 留给 L-054。

抽查锚点（2026-09-17）：`backend/wta-modules/wta-notify/src/main/java/org/namewta/notify/controller/admin/NotifyConfigController.java`；`usecase/NotifyConfigUseCase.java`；`service/{NotifyConfigService,NotifyTestSendService}.java`；`dao/NotifyConfigDao.java`；`mapper/{NotifyChannelAccountMapper,NotifySceneBindingMapper}.java`（无对应 XML）；`support/{NotifySceneCatalog,NotifyTemplateRenderer}.java`；`domain/{bo/{NotifyChannelAccountBo,NotifySceneBindingBo,NotifyTestSendBo},vo/{NotifyChannelAccountVo,NotifySceneBindingVo},entity/{NotifyChannelAccount,NotifySceneBinding}}.java`；`port/SmsBlendRegistryPort.java`；`adapter/provider/Sms4jBlendRegistry.java`；`service/runtime/NotificationApplicationRuntimeService.java`（旁证，格子归 L-051）；`usecase/NotificationApplicationUseCase.java`（旁证）；`wta-api/.../NotificationCommand.java`；`wta-common-mail/.../MailAccountResolver.java`；`wta-common-log/.../Log.java`；`src/test/.../controller/admin/NotifyConfigControllerContractTest.java`；`service/{NotifyConfigServiceTest,NotifyTestSendServiceTest}.java`；`03-backend-module-modes.md` layered 行；`notification.md`「目标与渠道」「前端与权限」；`10-cde-base-ddl.sql` `notify_channel_account` / `notify_scene_binding`；`50-cde-base-dml.sql` `notify:config:*` 六串 + 场景种子；`frontend/packages/domains/notify/src/{transport,types,transport.test}.ts`（旁证，格子归 L-053）；`web-domains/notify/src/ConfigPage.vue`（旁证，格子归 L-054）。

## Q1 澄清

- objective_id: OBJ-47
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-47；chain L-047 覆盖列两格 (a)；child L-003 开篇三组映射；矩阵 (a) Controller 十方法 + UseCase 类名仍 uncovered；(b) 无本课符号；(c) notify 房间已 covered、MySQL 未 covered；(d) 通知提交走廊仍 uncovered 且挂 L-051/L-052；负空间 L-045…L-046 / L-048…L-054
- difficulty: medium

课把配置柜台收成「三组 HTTP + 一份 UseCase + 两个下游字段」。计划里至少有六套可数对象，派单点名的是 **两格 (a)**：

1. **OBJ-47 原文。** `course.md`：能口述 `/notify/config` 账号/场景/测试发送。档是解释+走查。一句里三组窗，没有把 `MailAccountResolver.resolve`、Outbox Worker、厨房 `config.testTemplate`、供应商回调写成口试清单。
2. **chain 覆盖列。** 两格 (a)：`NotifyConfigController.*` 与 `NotifyConfigUseCase.*`。不写 (b)，不写 (d)。
3. **矩阵 (a) 两行。** Controller 行十个方法名与磁盘 1:1（`listAccounts` 不是 UseCase 的 `pageAccounts`；`editAccount` 不是 UseCase 的 `updateAccount`）。UseCase 行只写类名。Lead 若按行标 covered，**不会**把 L-051 submit、L-052 领取或 L-054 配置页假闭合。
4. **课自己的操作定义。** 门牌 `/notify/config`。账号六扇 + 场景两扇走 `configService`；试发两扇走 `testSendService`。Controller **只**注入 `NotifyConfigUseCase configUseCase`。写路径（含试发）`@DSTransactional` 盖在 UseCase，不在 Service。密钥详情只给 `mailPassSet` / `accessKeySecretSet`。场景骨架是 `NotifySceneCatalog.sceneCodes()`，不是绑定表全表扫描。试发构造 `NotificationCommand`（`appId=notify`，`bizType=NOTIFY_CONFIG_TEST`，`mode=SYNC`）交给 `NotificationApplicationService.submit`，HTTP 只回 `receipt.status().name()` 或 `"UNKNOWN"`。
5. **Service 副作用不是第二张 (a)。** `NotifyConfigService implements MailAccountResolver`、`@PostConstruct loadEnabledSmsAccounts`、限额字段落库，课都讲了。矩阵 **没有** 这些符号。口试认它们是本窗解释到的运行时后果，不要升格成 (b)，也不要用它们去盖 L-051 的提交走廊。
6. **邻格。** L-045 公告发布。L-046 收件箱。L-048 找人。L-049 监控。L-050 回调验签。L-051 统一 submit / `queued` 公式。L-052 Outbox 领取。L-053 厨房 URL。L-054 配置页变量插入 / 短信无 textarea。

问：本两格闭合标准是「十扇 URL/动词/权限 + UseCase 两字段分叉 + 写路径事务章 + 密钥布尔不回显 + 场景来自代码目录 + 试发走统一 submit 且 SYNC 不等于当场打供应商」，还是 OBJ 那句必须先把 (d) 通知提交标 covered、或必须把 `MailAccountResolver` 升成矩阵行、或必须等 GP-L-051/L-054 才给配置窗盖章？Lead 若只凭「`NotifyConfigController` 四字在函数表里」把 L-051 或 L-054 标 covered，是否假闭合了应用通知四枪与配置页工厂？

## Q2 证据

- objective_id: OBJ-47
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 34–164；UseCase 21–137；ConfigService 36–387 / 390–426；TestSend 26–96；Dao 20–120；Catalog 19–79；Renderer 29–41；AccountVo 11–31；AccountBo 11–33；SceneBo 12–26；TestBo 10–16；ContractTest 19–40；ConfigServiceTest 38–211；TestSendServiceTest 32–82；Runtime 32 / 43–137 / 285–290 / 302–303；ApplicationUseCase 20–27；Command 27–39；Log 35–40；DDL 1617–1666；DML 1534 / 1566–1570 / 1577–1610；modes.md 19；notification.md 28 / 41；transport.ts 45–69；types.ts 84–108；Mapper 接口无 XML
- difficulty: high

课的十扇窗 + UseCase 分叉哪些能在工作树直接复现，哪些只是课 HTTP 表用了 UseCase 方法名、或测试名单比课表窄？

**仍能复现（两格 (a)）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 一份类、根路径 `/notify/config`；继承 `BaseController`；只注入 `NotifyConfigUseCase configUseCase` | Controller 32–36 |
| 十扇映射：三 GET（`/account/list`、`/account/{accountId}`、`/scene/list`）+ 七 POST；**无** PUT/DELETE | Controller 45–163；合同测试 21–33 |
| `listAccounts` GET + `notify:config:list` → `pageAccounts`；`getAccount` GET + `query`；`addAccount` POST `/account` + `add` INSERT；`editAccount` POST `/account/edit` + `edit` UPDATE；`changeStatus` POST `/account/changeStatus` + `edit` UPDATE；`removeAccount` POST `/account/remove` + `remove` DELETE；`listScenes` GET + `list`；`saveBinding` POST `/scene/save` + `edit` UPDATE；`testAccount` / `testTemplate` POST `/test/{account\|template}` + `test` OTHER | Controller 45–163 |
| 七扇写窗（含试发、含 `changeStatus`/`saveBinding`）都是 `@Log(..., isSaveRequestData = false)`；GET 三扇无 `@Log` | Controller 70 / 83 / 96 / 109 / 134 / 147 / 160；合同测试 34–37 `>= 7` |
| `changeStatus` **无** `@Valid`，只用 `bo.getAccountId()` / `getEnabled()`；`removeAccount` body 是裸 `Long`，不是 BO | Controller 98–112 |
| 试发拆字段下传，不把 `NotifyTestSendBo` 整份交给 UseCase；账号试发 **不**传 channel | Controller 150 / 163 |
| UseCase 两个下游字段：`configService` + `testSendService` | UseCase 22–23 |
| 读三枪（`pageAccounts` / `getAccount` / `listScenes`）无事务注解；写五枪 + 试发两枪每一枪 `@DSTransactional`；Service 方法本身无此注解 | UseCase 33–137 |
| 账号/场景写走 `configService.*`；试发走 `testSendService.sendAccount` / `sendTemplate` | UseCase 54–56 / 65–67 / 77–79 / 88–90 / 109–111 / 121–124 / 134–137 |
| `wta-notify` layered；Controller 不 import service；DAO `@Repository` 持两个 Mapper；账号/绑定 Mapper 是空的 `BaseMapperPlus`，**没有** XML | modes.md 19；Dao 18–22；`resources/mapper/notify/` 只有 Notice/Outbox XML |
| Vo **没有** `mailPass` / `accessKeySecret` 字段；只有 `mailPassSet` / `accessKeySecretSet` 布尔；`accessKeyId` / `mailUser` **会**回显 | AccountVo 21–25；`toAccountVo` 377–381；单测 `accountVoOmitsSecretFields` JSON 无 `"mailPass":` / `"accessKeySecret":` 也无明文 |
| 更新时空串/空白密钥拷贝旧值 | ConfigService 129–134；单测 `blankSecretOnEditKeepsPreviousValue` |
| 渠道闸只有 `MAIL`/`SMS`；`IN_APP` 会 `渠道仅支持 MAIL 或 SMS` | ConfigService 317–320 |
| 新增：`(channel, configKey)` 冲突拒绝；`minuteMax < 1` 拒绝；`enabled` 空则 `'N'`；启用才 `validateEnabledCredentials`；然后 insert + `registerSms` | ConfigService 93–109 |
| 启用 MAIL 要 host/port/from/pass；启用 SMS 要 supplier/accessKeyId/secret；缺则不 insert/不 update | ConfigService 328–341；单测 `cannotEnableMailAccountWithoutSecret` / `cannotEnableSmsAccountWithoutSecret` |
| `changeStatus` 只接受 `Y`/`N`；停用 SMS 会 `smsBlendRegistry.remove`，不 `upsert` | ConfigService 151–160 / 298–307；单测 `disablingSmsAccountUnregistersBlend` |
| 删除：`countBindings > 0` → `账号仍被场景绑定，不能删除`；SMS 成功删除后 `remove(configKey)` | ConfigService 169–178；Dao 64–67 |
| `listScenes`：先 `validateChannel`，再对 Catalog 五个 sceneCode 各 `findBinding`，**不**调用 `listBindings()` | ConfigService 187–194；Catalog 19–21；Dao `listBindings` 108–112 存在但本窗列表不用 |
| Catalog 五码：`auth-captcha` / `person-rebind` / `enterprise-transfer` / `workflow-task` / `notice-published`；`person-rebind` 变量列表为空 | Catalog 19–21 / 51 |
| `saveBinding`：未知 sceneCode 拒绝；账号必须同渠道且 `enabled=Y`；`templateMinuteMax` 不能超过账号 `minuteMax`；MAIL 已绑定或填了主题/正文才 `NotifyTemplateRenderer.validate`（主题+正文拼接，必填变量可拆开写）；SMS 绑定账号时模板码必填，必填变量必须出现在 mapping，禁止未声明键 | ConfigService 203–248；Renderer 29–41；单测 `templateMinuteMaxCannotExceedAccountCap` / `mailBindingRejectsRenamedRequiredTokenAndAcceptsMovedToken` |
| 无现有行 insert，有则 update；`restricted` 空则 `'N'`；未绑账号时 `templateMinuteMax` 缺省 60 | ConfigService 209 / 249–268 |
| `resolve(providerKey)` 读 `dao.findAccount("MAIL", providerKey)`，停用或缺失返回 `null`；YAML 不是这条路径 | ConfigService 277–296；notification.md 28 |
| 启动 `loadEnabledSmsAccounts`：缺表 catch + warn，不 `upsert`/`remove`，进程继续 | ConfigService 46–58；单测 `missingChannelAccountTableDoesNotAbortStartup` |
| 账号试发：存在且 `enabled=Y`、target 非空；`listBindingsByAccount`；空 sceneCode 取第一条；否则按 sceneCode 过滤；没有绑定 → `账号未绑定逻辑场景` | TestSend 38–55 |
| 模板试发：`findBinding` 必须已有 `accountId`，账号启用 | TestSend 65–76；单测 `unboundTemplateTestSendFailsClosed` / `disabledAccountTestSendFailsClosed` |
| `submit`：MAIL → `recipientType=EMAIL` + `NotificationChannel.MAIL`；否则 PHONE + SMS；params 来自 `NotifySceneCatalog.examples`；`appId=notify`，`bizType=NOTIFY_CONFIG_TEST`，`mode=SYNC`，`strategy=ALL`，`priority=20`；幂等键含 `currentTimeMillis`；`metadata.audit=TEST`；**不**接收 `providerKey` | TestSend 78–89；Command 27–32；单测 `templateTestSendSubmitsSceneVariablesWithoutProviderKey` |
| HTTP 试发回 `R<String>` = `receipt.status().name()` 或 `"UNKNOWN"` | TestSend 89；Controller 150 / 163 |
| 表 `notify_channel_account` unique `(channel, config_key)`；`notify_scene_binding` unique `(scene_code, channel)` + account 索引；渠道注释写 MAIL SMS | DDL 1617–1666 |
| 菜单六串 `notify:config:list\|query\|add\|edit\|remove\|test`；配置页组件 `notify/config/index` | DML 1534 / 1566–1570；notification.md 41 |
| 场景 DML 十行（五场景 × MAIL/SMS）`account_id` **全是 null**；`auth-captcha` MAIL 已有 `${code}` / `${expireMinutes}` 文案 | DML 1577–1610 |
| 厨房十枪 URL/动词与本窗对齐；`remove` 传裸 `accountId`；`NotifyConfigChannel = 'MAIL' \| 'SMS'` | transport.ts 45–69；types.ts 84 |

**邻格仍能复现（不计入本两行 (a) 闭合；用来判断走廊能不能单独盖章）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| Runtime `submit` 只写 Intent/Recipient/Delivery/Outbox 并登记唤醒，javadoc 写明不执行 Provider I/O | Runtime 32 / 43–137 |
| `queued = mode==ASYNC 且存在 PENDING`；SYNC 试发即使有 PENDING，回执 `queued` 仍为 false | Runtime 302–303 |
| 试发 `recipientType=EMAIL/PHONE` 时 Runtime 把 `recipientIds` 收成外部联系人，不走用户表 | Runtime 285–290 |
| 生产 `NotificationApplicationUseCase.submit` 自己也盖 `@DSTransactional` | ApplicationUseCase 23–27 |
| 厨房 `mailPass?` / `accessKeySecret?` 仍在写入类型上；页面用 `mailPassSet` 决定占位「留空保持原密码」 | types.ts 96–102；ConfigPage.vue 142 / 160（L-053/L-054） |
| 绑定上的 `restricted` / `recipientMinuteMax` / `recipientDayMax` 本窗只负责存进去 | ConfigService 260–262；真正扣减在 `NotifySendPlanner` + `NotifyQuotaPort`（L-051） |

**课写过、测试名单更窄或口试标签写偏（不是证伪 (a)）：**

| 课的句子 | 磁盘 |
| --- | --- |
| HTTP 表把 `GET /account/list` 标成 `pageAccounts` | Controller 方法名是 `listAccounts`（47 行）；UseCase/Service 才是 `pageAccounts`。矩阵 (a) 用的是 Controller 名 |
| HTTP 表把 `POST /account/edit` 标成 `updateAccount` | Controller `editAccount` → UseCase `updateAccount` |
| 合同测试钉了 `notify:config:list` 与 `notify:config:test` | 源码还有 `query`/`add`/`edit`/`remove`；合同测试 **没有** 逐串断言 |
| 合同测试是读源码字符串，不是 MockMvc | 十扇映射、无 PUT/DELETE、`isSaveRequestData` 条数仍能互证 |
| 「试发返回 ACCEPTED」易从单测滑出口试 | 单测 mock `NotificationReceipt(..., ACCEPTED, ...)`（TestSendServiceTest 70–78）。生产 Runtime 插入意图时 `status=QUEUED`（Runtime 67），HTTP 回的是 `receipt.status().name()`，常见字符串是 `QUEUED` 不是 `ACCEPTED` |
| `@Log` 「避免日志里出现密码」 | 只关了 `isSaveRequestData`；`isSaveResponseData` 默认仍 `true`（Log.java 35–40）。写窗 `toAjax` 无密钥盒子；试发回的是状态名 |
| 无 `NotifyConfigUseCase` 单测 | 两字段分叉只在 UseCase 源码与 Controller 注入上能看见 |
| Mapper XML「本窗 SQL」 | 账号/绑定没有 XML，DAO 用 MP Wrapper。不要用本课去盖 `dao-mapper-ownership` 组件行 |

问：在十扇 URL/权限/Log 不存请求体、UseCase 两字段分叉、写路径事务章、密钥布尔、目录骨架、试发 Command 不含 `providerKey`、SYNC 仍进 Outbox 已经能互证的前提下，两格 (a) 是否还要把「合同测试没钉 query/add/edit/remove」「UseCase 无单测」「HTTP 表用了 UseCase 方法名」「试发生产回执字面是 QUEUED」写成开篇半句才算讲完？(d) 通知提交是否根本不在本课覆盖列，因而不必等 GP-L-051 才能给本两行盖章？

## Q3 假设与反例

- objective_id: OBJ-47
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 密钥不回显；空串保持原值；enabled=Y 先补密钥；绑定中账号不能删；场景列表来自 Catalog；试发不是 ConfigService SMTP；SYNC≠当场发送；Controller 只有一份 UseCase；无 IN_APP 账号；试发不收 providerKey；连点两次因毫秒键成两意图；YAML 不是 resolve 来源；缺表不阻止启动；限额本窗只落库
- difficulty: high

课的隐含假设若反转，合格读者会把哪把枪或哪条走廊认错？

1. **「详情会把密码回显，前端再原样 POST。」** Vo 根本没有 `mailPass` 字段；正确留空表示不改。课反例 1。
2. **「空密码写入会把库里的 SMTP 密码清掉。」** `isBlank` 则拷贝 `current.getMailPass()`。课暂停段 / 单测 `blankSecretOnEditKeepsPreviousValue`。
3. **「可以先 `enabled=Y` 再补 host/pass。」** `validateEnabledCredentials` 挡 add/update/changeStatus。课正例 1 / 误区 4。
4. **「删除仍被场景引用的账号会级联解绑。」** `账号仍被场景绑定，不能删除`。课反例 2。
5. **「场景列表来自绑定表全表扫描，历史 scene 也会出现。」** 骨架是 `sceneCodes()`；`listBindings()` 本窗不用。课误区 2 / 边界。
6. **「测试发送是 ConfigService 私有 SMTP / `JavaMailSender.send`。」** 字段是 `NotifyTestSendService`，终点是 `notifications.submit`。课误区 1。
7. **「`NotificationMode.SYNC` 等于在 UseCase 线程里打供应商。」** Runtime submit 无供应商分支；`queued` 公式还要求 ASYNC。课类比失效处 / L-007 正例 3。这是邻格对照，**不要**用它把 (d) 标 covered。
8. **「Controller 注入了 Config / Test 两个 UseCase。」** 只有 `configUseCase`。分叉在 UseCase 字段。课误区 5。
9. **「可以给 IN_APP 建渠道账号。」** `validateChannel` 只 MAIL/SMS。课边界。
10. **「试发 body 可传 `providerKey` 或完整短信句子。」** `NotifyTestSendBo` 只有 accountId/sceneCode/channel/target；Command params 来自 Catalog examples。课反例 3。
11. **「连点两次试发会撞上同一幂等键。」** 键含 `currentTimeMillis()`。课暂停段。对照公告 `notice-published:{id}:{version}` 是 L-045，不是本格副作用清单。
12. **「YAML 里还能写发件人兜底，`resolve` 找不到就用 yml。」** `resolve` 读账号表，找不到返回 `null`。课误区 3 / notification.md 28。
13. **「短信表不存在时进程不得启动。」** `@PostConstruct` catch 后 warn。课变式 B。
14. **「保存绑定上的 `recipientMinuteMax` 会立刻扣 Redis。」** 本窗只 `dao.insert/update`。扣减在 Planner（L-051）。课迁移到限额。
15. **「`listScenes?channel=IN_APP` 会列出站内信绑定。」** 先被渠道闸拦住。
16. **「未绑账号也可以对 SMS 只存 mapping、不填模板码，再拿去试发。」** `saveBinding` 在 `accountId==null` 时不强制模板码；`sendTemplate` 仍要求 `accountId != null`。种子十行全是未绑账号——试发会失败关闭，不是种子已可试发。
17. **「`person-rebind` 试发会带验证码参数。」** Catalog 变量空，`examples` 空 Map。课边界。
18. **「`accessKeyId` 也是只写布尔。」** Vo 回显 `accessKeyId`；只把 `accessKeySecret` 收成 `accessKeySecretSet`。
19. **「本课覆盖 `NotificationController.submit`，所以通知提交走廊可 covered。」** 矩阵 (d) 该行挂 L-051/L-052。试发只是那个走廊的一个调用方。把假设反转后，Lead 会用本行假闭合 L-051。
20. **「配置页 Vue / `createNotificationService.config` 是本两格。」** URL 对齐是旁证。工厂与页面是 L-053/L-054。
21. **「`changeStatus` 走 `notify:config:status` 或独立 (b) 行。」** 权限是 `edit`。矩阵无 status 独行。
22. **「UseCase 是空转发，可以删。」** 事务注解除了它没处盖；试发分叉也在它身上。课误区 5 的对称句。

问：读完后仍说不出的，是不是「十个方法名」，而是 **Controller 只有一份 UseCase、写路径事务章在 UseCase、密钥布尔不回显、场景来自 Catalog、试发走统一 submit 且 SYNC≠当场发送、试发成功≠供应商回执、YAML 不是运行时发件人**？

## Q4 视角

- objective_id: OBJ-47
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-47 vs L-045…L-054；chain 无 (d)；UseCase 两字段；Catalog；TestSend Command；Runtime queued；前端 types 旁证
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:NotifyConfigController.*`** 或 **`A:NotifyConfigUseCase.*`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 两行 vs 无 (b) 密钥行； (d) 通知提交仍 uncovered 且挂 L-051/L-052 | **是** 闭合定义。两行可单独 covered。不要发明 (b)。不要用试发去盖 (d) |
| 配置窗作者 | 十映射、六权限、Log 不存请求体、`changeStatus` 无 `@Valid`、`remove` 裸 Long | **是** 第一格 |
| 用例电工 | 两字段分叉；读三枪无 TX、写+试发有 TX；Controller↛Service | **是** 第二格。整个 notify 房间画像 **否**（L-003 已 covered） |
| 密钥柜员 | Vo 布尔、空串拷贝、启用凭据闸、SMS 启停登记/注销 blend | **是** 第一格解释到的 Service 后果。SMS4J 工厂内部 **否** |
| 目录作者 | 五码代码播种；`person-rebind` 空变量；未知 scene 拒绝 | **是** 场景两扇。公告 `notice-published` 发布动作 **否**（L-045） |
| 试发柜员 | Command 形态、毫秒幂等键、`audit=TEST`、HTTP 只回状态名 | **是** 试发两扇。Outbox 领取 **否**（L-052） |
| 提交走廊作者 | 源：配置试发也会 `submit`；中：Intent+Outbox；汇：Worker/供应商 | 试发半段 **是** 副作用。走廊盖章 **否**（L-051/L-052）。矩阵 (d) 不要改挂本课 |
| 邮件解析器 | `MailAccountResolver.resolve` 读库 | 解释「YAML 不再是运行时」**是**。投递适配器 **否** |
| 前端厨房（L-053） | `service.config.*` 十枪 | URL 对齐旁证 **是**。工厂格子 **否** |
| 配置页（L-054） | 短信无 textarea、变量插入、`mailPassSet` 占位 | UI 篱笆 **否**。后端模板闸 **是** |
| 回调柜员（L-050） | HMAC / eventId | 对照一句「试发成功≠回执」**是**。验签窗 **否** |
| 收件人/监控/公告 | `/notify/recipients`、snapshot、notice 六扇 | **否**（L-048/L-049/L-045） |
| MySQL 8.4 容器 | 两表 unique | 本窗 unique 意图 **是**。容器课 **否**（L-085） |
| dao-mapper-ownership | DAO 持 Mapper、本窗无 XML | 本窗举例 **是**。组件行 **否**（L-083） |
| (b) 密钥只写 / SYNC 试发 | 矩阵无此行 | 课已写进 (a)。不要发明 (b) |

问：Lead 若凭「十扇都在函数表里」把两行标 covered，是否还要求读者能指着 **UseCase 两字段、密钥布尔、Catalog 骨架、试发 Command、SYNC 仍写 Outbox、YAML 不是 resolve 来源**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要把 L-051 submit 算进 (a)、更不需要等配置页 Vue？

## Q5 元问题

- objective_id: OBJ-47
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 两行、无 (b)；矩阵 (d) 通知提交挂 L-051/L-052
- difficulty: low

建议：`defer`

原因：

- **两格 (a) 与矩阵 / chain / OBJ-47 1:1。** child L-003 合同章节齐全（宏观地图、直觉、定义表、机制链 1–10、带 caption / 文字等价物 / 图边界的分叉图、正反例、变式、误区、暂停走查、来源表 S-N-03/S-N-09/S-N-10）。**主叙事站得住且与源码对齐**：十扇 HTTP、一份 UseCase、两个下游字段、写路径事务章、密钥只写不读、场景来自 Catalog、试发走 `NotificationApplicationService.submit` 且 `SYNC` 不等于当场发送。不必为「十个方法名」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例或邻格，不挡住把 **两行 (a)** 标 covered：（1）课 HTTP 表用 UseCase 名 `pageAccounts`/`updateAccount`，磁盘 Controller 是 `listAccounts`/`editAccount`；（2）合同测试只钉 list/test 权限串与源码字符串，不是 MockMvc；（3）无 UseCase 单测；（4）试发生产回执字面常见 `QUEUED`，单测 mock 的 `ACCEPTED` 不是生产字符串；（5）`@Log` 未关 `isSaveResponseData`。不要把 submit 四枪、Outbox 领取、厨房工厂、配置页 Vue、回调验签塞进本两行。
- **不要发明密钥 (b) 行，也不要等 GP-L-051 才给本两行盖章。** 矩阵「主路径 通知提交」明确挂 L-051/L-052。本课只保证试发是那个走廊的调用方：构造 Command → `submit` → 返回状态名。`queued` 公式、Worker 领取、供应商 I/O 是邻格。GP-L-051 尚未落盘，不阻塞本两行。
- **不要把配置页留给本课，也不要把本课留给 L-054。** L-054 符号是 `createNotifyWebDomain`。OBJ-47 已经把 `/notify/config` 十扇钉在本课；拆走会制造矩阵空洞，且未满 10 问、未超 40 分钟。
- 不建议 `mine-more`：b01 未证明两行还有真实矩阵空洞；再五问只会把 L-051 submit、L-052 领取、L-053 厨房或 L-054 配置页再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；可选半句（Controller 方法名 vs UseCase 方法名；试发回执字面；合同测试是源码扫描）不是第二容器。不建议 `split-lesson`：未满 10 问；两格是 chain 原设计的同一 OBJ-47（配置窗 + 配置用例），同一模块同一容器。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：密钥布尔、SYNC 试发、Catalog 骨架是 (a) 的解释到副作用。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:NotifyConfigController.listAccounts, getAccount, addAccount, editAccount, changeStatus, removeAccount, listScenes, saveBinding, testAccount, testTemplate | 门牌十映射、六权限、七扇 Log 不存请求体、`changeStatus` 无 Valid、`remove` 裸 Long、密钥 Vo 布尔、场景三/两扇、试发两扇走统一 submit——均与源码对齐。残留精度：（1）课 HTTP 表用了 UseCase 方法名；（2）合同测试未钉 query/add/edit/remove 且非 MockMvc；（3）试发生产回执字面常见 QUEUED。 | 可选半句：口试方法名以 Controller 为准（`listAccounts`/`editAccount`）；试发 HTTP 回 `receipt.status().name()`，不以单测 mock 的 ACCEPTED 为准。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-003 来源表 S-N-03/S-N-09/S-N-10）。 | defer（本行可 covered；精度项不挡盖章） |
| A:NotifyConfigUseCase | 两字段 `configService` / `testSendService`；读三枪无 TX、写五枪+试发两枪 `@DSTransactional`；Controller 只注入这一份——均与源码对齐。残留精度：无 UseCase 单测，分叉证人是源码+Controller 注入。 | 可选半句：口试分叉以 UseCase 字段为准，不要发明第二份 UseCase。Lead 可将本行标 `covered`。 | defer（本行可 covered） |
| D:主路径 通知提交（NotificationController.submit → Outbox → claim） | 矩阵仍 uncovered，备注 L-051、L-052。本课试发半段是对照，不是该行符号。 | 不要用本课改该行。对照一句「配置试发同样只 submit、SYNC 不打供应商」即可。 | defer（L-051/L-052） |
| B:密钥只写 / SYNC 试发 / Catalog | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| MailAccountResolver.resolve / loadEnabledSmsAccounts | 课已讲 YAML 不是运行时、缺表不阻止启动。矩阵 (a) **无此符号**。 | 保持对照，不升第三法。 | defer（未入库对照） |
| A:NotificationController.submit, query, retry, cancel / A:NotificationApplicationService | 试发调用了 submit。四枪 HTTP 未认格。 | 保持。L-051。 | defer（L-051） |
| B:NotifyOutboxClaimUseCase / D:fail-outbox | 领取未讲。 | 保持。L-052。 | defer（L-052） |
| A:createNotificationService / A:notificationDirectory | 十枪 URL 旁证已借用。 | 保持。L-053。 | defer（L-053） |
| A:createNotifyWebDomain | 配置页 UI 未认格。 | 保持。L-054。 | defer（L-054） |
| A:NotifyNoticeController.* / A:NotifyInboxController.* / A:NotifyRecipientController.search,byIds / A:NotificationMonitorController.snapshot,deliveries / A:ProviderCallbackController.callback | 邻窗。 | 保持。L-045/L-046/L-048/L-049/L-050。 | defer（邻课） |
| C:MySQL 8.4 / mysql-base | 两表 unique 是本窗机制，不是容器课。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |
| C:dao-mapper-ownership | 本窗 DAO 持 Mapper、无 XML 是举例。 | 保持。L-083。 | defer（L-083） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本两行 (a) 无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出三组窗的目的（管钥匙 / 把场景钉到钥匙和信纸 / 用同一柜台交一封会进 Outbox 的试发信）、主路径（账号 CRUD → 场景 list/save → 试发 submit）与失败路径（启用缺密钥、绑定中不能删、未知 scene、未绑不能试发、SYNC 仍不打供应商）；(d) 缺的是矩阵把提交走廊挂在 L-051/L-052，不是讲义容器；把可选半句写进原课不会超过 40 分钟；公告、收件箱、收件人、监控、回调、应用通知、Outbox、厨房、配置页已有邻课。满 10 问前不拆。十扇与一份 UseCase 是 chain 原设计的同一 OBJ-47，同一 `wta-notify` 容器，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-051 submit、L-052 领取、L-053 厨房、L-054 配置页、L-045/L-046/L-048/L-049/L-050 邻窗、未入库 `MailAccountResolver`/(b)/试发走廊、(c) 容器与 dao-mapper 组件显式建议 `defer`；对 `A:NotifyConfigController.*` 与 `A:NotifyConfigUseCase.*` 建议 Lead 标 `covered`（证据锚本 probe 与 L-003 来源表）；对矩阵「主路径 通知提交」建议保持 uncovered 并继续挂 L-051/L-052。本课不主张发明密钥 (b)、把试发升成 (d)、或把配置页 Vue 算进本两行。
