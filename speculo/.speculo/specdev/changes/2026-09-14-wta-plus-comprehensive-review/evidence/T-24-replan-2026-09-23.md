# T-24 evidence

Current HEAD `3db6c6d0` already meets AC-024. No new implementation was required. This cancels only the repeated construction. AC-024 stays owned by T-30. The adjacent OIDC change was not modified.

## Redis recovery and MySQL configuration

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=ThirdResilienceRecoveryIntegrationTest,ThirdLimitConfigurationIntegrationTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Dthird.redis.integration.port=33133 \
  -Dthird.redis.integration.container=namewta-t24-redis \
  -Dthird.configuration.integration=true \
  -Dthird.mysql.integration.url='jdbc:mysql://127.0.0.1:33134/namewta_third_test_t24?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai'
```

Exit 0. Isolated Redis `redis:8.6.3` on `127.0.0.1:33133`, container `namewta-t24-redis`, label `namewta.test.owner=T-24`. Isolated MySQL `mysql:8.4.9` (`sha256:c36050afdca850f23cef85703f84c7531a5ae155a11b5ee1c60acb09937c4084`) on `127.0.0.1:33134`, database `namewta_third_test_t24`, loaded `10-cde-base-ddl.sql` through `50-cde-base-dml.sql`. The database password is the existing test default and is not copied here. Both containers were removed after the run.

`ThirdResilienceRecoveryIntegrationTest`: tests=11, failures=0, errors=0, skipped=0, 8.424s. `ThirdLimitConfigurationIntegrationTest`: tests=1, failures=0, errors=0, skipped=0, 3.280s. Combined tests=12, skipped=0.

The recovery cases are: a killed child JVM's permit expires without deleting the Redis key; lowering concurrency waits for the other client's existing owners; an endpoint rejection returns the provider permit and a late close cannot release the next owner; an expired owner cannot release the replacement; raising the rate keeps requests already spent and an old snapshot cannot restore quota; lowering the rate does not grant quota from a partially expired old window; changing unlimited concurrency to one counts in-flight calls and the endpoint limit refreshes; Redis down is a closed failure even when both limits are unlimited; an interrupted configuration update is repaired without restoring the old quota; pausing the owned Redis rejects the request with `Third-party limit service unavailable` and does not leak the connection failure; real HTTP success, HTTP failure, and bounded timeout retries release both permits. The configuration case commits a save that refreshes two cache nodes, a rollback that does not, and a failed refresh that can be recovered.

## Module regression

The ticket command `./mvnw -pl wta-modules/wta-third -am test` uses the default `dev` profile. The third module tests are `@Tag("local")`, so that command executes tests=0 in `wta-third`. It is not counted as a pass. The same reactor with `-Plocal` is the run that executes them:

```bash
cd backend && ./mvnw -pl wta-modules/wta-third -am test -Plocal
```

Exit 0. `wta-third`: tests=33, failures=0, errors=0, skipped=0. Upstream modules that have no `local` tag reported tests=0, skipped=0. `ThirdResiliencePolicyAdapterTest` is 2 of those 33 and does not replace the Redis run above.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase, operation `verify-affected-contract-and-quality-gates`, status passed. No production source edit.
