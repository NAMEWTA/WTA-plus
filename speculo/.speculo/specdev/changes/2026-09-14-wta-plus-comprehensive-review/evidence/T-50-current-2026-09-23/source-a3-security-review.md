# T-50 Source A3 public-JSON-contract security review

Fixed A2 `1dfdcbca831ad7391d725d7f001acb2b9ba1c88f` → A3 `0b33ff361820249f947a2767674356dabab59774`, tree `f13b1dce91dd61a46115dd6acc3ca246dc87cc3f`. Read-only `git diff`/`git show` and local dependency-source review. I did not run Maven, build, HTTP, Docker, or frontend tests. A2 production/transaction scope is inherited from `/tmp/wta-t50/source-a-security-review.md`; A2 focused acceptance is `/tmp/wta-t50/source-a2-security-review.md`.

## Verdict

**Static security/API delta: PASS, pending A3 and final B live verification.** A2→A3 changes exactly two files (+6/−2): `backend/wta-api/src/main/java/org/namewta/notify/api/NotificationCommand.java` adds `@JsonProperty(required = true)` to the existing `int priority` record component and documents “HTTP JSON 必须显式传入 0”; `backend/wta-admin/src/test/java/org/namewta/test/notify/NotificationModeOpenApiTest.java:22` asserts generated schema `required` contains `priority`. No Controller, record type/order, canonical constructor body, runtime service, retry, transaction, or other production path changed.

The Java constructor descriptor remains the same (`..., NotificationStrategy, NotificationMode, int, Instant, ...`); existing `new NotificationCommand(...)`, accessors, Java source calls, and already compiled callers have no source/binary signature change. Null strategy/mode still normalize to `ALL/ASYNC` in the unchanged compact constructor; priority remains primitive and only 0 passes T-50 runtime validation. Annotation's empty name does not rename the JSON field. It changes Jackson property metadata: a JSON client must explicitly provide priority. Under the deployed Jackson 3.1.4 mapper, that requirement is already observable—A2 real HTTP omitted-priority request got 400, while omitted enums plus explicit 0 got 200. Other separately configured, previously lenient ObjectMappers could newly reject omission; that is an intentional stricter JSON contract, so check external clients if any, but it is not a defect in this fixed delta. Existing browser/frontend requests need generated type update at Source B; no generated output is reviewed here.

Local dependency source supports the chosen mechanism: `com.fasterxml.jackson.annotation.JsonProperty` 2.21 has runtime retention and PARAMETER/FIELD/METHOD targets, and `required()` defaults false; Jackson Databind 3.1.4 `JacksonAnnotationIntrospector.hasRequiredMarker` returns true for `required=true`; swagger-core-jakarta 2.2.47 `ModelResolver` adds required properties when `propDef.isRequired()`. `backend/wta-api/pom.xml` already depends on `wta-common-json`, so no new POM dependency was added. The new test uses `ModelConverters` to cover schema projection; it does not replace a fresh Springdoc HTTP OpenAPI capture.

## Retained A2 gates, with source boundary

At clean **A2** (`1dfdcbc`, tree `6021ef9`), the focused owned MySQL/Redis eight-class run has 135 tests, 0 failures/errors/skips, Maven exit 0 and resource cleanup errors `[]` (independently verified in the A2 report). A2 default `./mvnw test` exit 0 has 257 fresh XML classes, 1,067 total tests, 197 explicit environment skips and 870 executed passes (`/tmp/wta-t50/a2-backend-default-counts.json`); A2 full package and full bundle scripts exited 0. A2 owned full-JAR live OpenAPI capture `/tmp/wta-t50/openapi-live/63c7980e7727c426/` exited 0 with clean source before/after, cleanup errors `[]`, and 436 paths/445 schemas. Its real `POST /notify/notification` request schema had the new strategy/mode/priority descriptions but **no `required` list**. That mismatch is the reason for A3; none of these A2 gates prove the A3 annotation or final generated TypeScript.

For A3, require positive current `NotificationModeOpenApiTest`/default test counts and clean source; for final B, regenerate and inspect the actual live `NotificationCommand.required` array containing `priority` while strategy/mode remain optional, confirm front-end generated types and contract gates, and rerun the required eight-class real HTTP assertions (explicit 0 accepted, omitted priority rejected without DB writes, omitted enums normalized, unauthorized request rejected). If the live schema still lacks `required` or Java record binding changes unexpectedly, this static PASS must be revised. Current A3 default/full and final B evidence are pending as of this report.
