# Evidence: T-06 — FE/docs owned rename + no upstream URL

- **状态:** done
- `@namewta/*` npm scope **kept**. Workspace `name`: `wta-vue-plus`.
- `package.json` `repository.url` → `https://github.com/NAMEWTA/WTA-plus.git`
- pom `<url>` → `https://github.com/NAMEWTA/WTA-plus`
- Admin titles `WTA-Plus`. README is monorepo (no submodule delivery).
- `pnpm architecture:check` exit 0.
- `utils/ruoyi.ts` → `utils/wta.ts` with import updates.
- Live admin home / WTADoc: `https://github.com/NAMEWTA/WTA-plus` only — no `plus-doc.dromara.org` or `github.com/dromara/*`.
- README clone is `git clone https://github.com/NAMEWTA/WTA-plus.git` (no `--recurse-submodules`).
