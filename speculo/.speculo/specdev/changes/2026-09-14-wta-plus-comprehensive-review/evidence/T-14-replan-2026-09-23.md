# T-14 evidence

Current HEAD `5039d361` already meets AC-014. No new implementation was required. This cancels only the repeated construction. AC-014 stays owned by T-30. The adjacent OIDC change was not modified.

## Browser, MySQL, Redis, and MinIO

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=ProfileSelfMaterialsBrowserIntegrationTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Dbrowser.profile.integration=true \
  -Dnamewta.repo.root=/srv/WTA-plus \
  -Dprofile.mysql.integration.url='jdbc:mysql://127.0.0.1:33126/namewta_profile_test_t14?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai' \
  -Dprofile.redis.integration.port=33127 \
  -Doss.minio.integration.endpoint='http://127.0.0.1:33128'
```

Exit 0. Isolated MySQL `mysql:8.4.9` (`sha256:c36050afdca850f23cef85703f84c7531a5ae155a11b5ee1c60acb09937c4084`) on `127.0.0.1:33126`, database `namewta_profile_test_t14`, loaded `10-cde-base-ddl.sql`, `20-cde-job.sql`, `30-cde-workflow.sql`, `40-cde-ai.sql`, and `50-cde-base-dml.sql`. Redis `redis:8.6.3` on `127.0.0.1:33127`. MinIO `quay.io/minio/minio:RELEASE.2025-04-22T22-12-26Z` on `127.0.0.1:33128`. All three containers were removed after the run. Credentials were the existing test defaults and are not copied here. Home dist served from `frontend/apps/home-web/dist` with base `/assets/` and API `/prod-api`.

`ProfileSelfMaterialsBrowserIntegrationTest`: tests=1, failures=0, errors=0, skipped=0, 31.08s. Playwright `playwright.profile.config.ts` on system Chrome: 8 passed, 0 skipped, 24.4s. The test printed that Profile/MySQL transactions, immutable snapshots, OSS metadata, and private MinIO bytes were verified. Login issuance, providers, and workflow start are fixtures. The permission failure log for `profile:material-tag:query` is the denied-session case, and the upload diagnostic is the expired-ticket rejection.

The eight Chrome cases are: a new person `CN_RESIDENT_ID` missing the portrait is rejected with `MISSING_REQUIRED_MATERIAL:PERSON_ID_CARD_PORTRAIT`, then both sides upload, a private preview renders, refresh keeps the file, and submit reaches 审核中; an enterprise whose agent is the legal representative submits the license and legal document; an enterprise whose agent is not the legal representative focuses the missing authorization letter before that file is uploaded and the application submits; another account cannot read, attach, or detach the owner's material (`MATERIAL_ACCESS_DENIED`) and cannot reuse the owner's object (`MATERIAL_OSS_OWNER_MISMATCH`), while a session without material permissions receives 403; a direct-upload network failure and a cancel do not register a material, the draft remains, and a later upload succeeds; a lost registration response is recovered by query, replacement keeps the previous file until the new one is registered, detach only clears the reference, and the original bytes remain readable; an expired Redis ticket rejects complete, then a refresh and a new ticket succeed; a workflow failure after the submission inserts rolls the application, immutable snapshot, and OSS references back, and a refresh retry reaches `WAITING`.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase, operation `verify-affected-contract-and-quality-gates`, status passed. Inputs were this ticket and the fresh integration result. Output is this file. No production source edit.
