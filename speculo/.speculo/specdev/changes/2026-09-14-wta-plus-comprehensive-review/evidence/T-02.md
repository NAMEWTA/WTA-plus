# Evidence: T-02 — 当前在线会话凭据日志闭环

## 1. 实现摘要

当前生产链 UserLoginSuccessListener 把真实已签发 tokenValue 写入 UserOnlineDTO.tokenId，在线 GET 返回 SysUserOnline.tokenId，设备操作 POST 把该令牌带在路径中。此前 HTTP 响应日志字段策略缺 tokenid，SysLogFilter 路径和 GlobalExceptionHandler 原始 URI 也复制令牌。新增共享 LogSanitizer.path 精确遮盖管理端/本人两种在线操作路径，并将 tokenid 加入日志 JSON 字段策略；只改日志副本，原业务响应、URL、会话操作和流行为保持。LogAspect 已取匹配路由模板，没有把未证实的 OperLog/DB 原路径漏洞作为缺陷。

## 2. Lead Dispatch And Candidate Return

Packet dispatch-T-02-20260923-01.md。base c16966167526f9b6ab6eb213265034b3bbe53e46 → 测试红灯 checkpoint 8a88965363ca295122efadabf0bc97e99c6704c4 → 非空生产修复/result 617a3693ddb2897851ab46d4131690898df6a690，tree a8d1181dfc950d6edf6d94a9cc1c42e10e88da00。current/main/direct-parent，native gpt-6-sol/xhigh 的 cors_audit 单产品writer，Lead独占治理/提交/服务/验收。formal source candidates=1；开发红灯不冒充成功集成。该候选消费者第一次命令含一项skip，完整复核失败；补启用后才通过，原记录保留，不把跳过算pass。

历史 canonical 原字节保留于 T-02-history-before-2026-09-23.md；旧日志不变。本文是新修复与当前验收，不补造旧逐票 clean 时点。

## 3. 修改范围与路径所有权

7个产品/测试路径全部在预登记写集，candidate1/path-audit.json。生产仅 common-json LogSanitizer 与 common-web SysLogFilter/GlobalExceptionHandler 三类；四个测试路径。engineering按日志安全、Java/Spring、失败关闭与资源规则核对；common沿既有JSON/shared日志入口，无新模块/依赖或反向调用。没有HTTP API/DTO/数据库迁移/前端业务修改。

共享路径策略保留静态 list/myself、普通路由，处理尾斜线与动态单段；异常日志按真实servlet context剥离application path后遮盖再还原context。异常context关系失败关闭。IOException的SSE判别仍用原URI，仅logger使用安全副本。固定输入断言业务对象/response不变，未把所有可控metadata任意字符串安全化作为本票目标。

## 4. 验收与合同映射

| AC-002 | 当前证据 |
|---|---|
| 正常凭据链可观察红灯 | 8a88965 真实MySQL run4e81e6d59c798f07：35项、1failure、0error/skip，实际签发/踢出断言先通过，HTTP审计仍包含有效canary；首失败后续sink断言未执行，不扩大红灯范围 |
| HTTP/OperLogEvent/MySQL/error无令牌 | 617a369 run7c481ffc58536e6c：7类39项全部零失败/错误/skip，两个真实HTTP/SQL方法确已执行；在线方法非空HTTP8、OperLog2、SQL2、error logger2及正文/URL秘密均检查；原签名基线也保持 |
| 业务响应与审计有用信息保留 | 真实测试会话 LoginHelper/SaToken 签发，先他端踢出再本人退出，原调用身份和目标失效断言成立；GET业务token仍可用，日志保持actor/耗时/结果/静态路径 |
| 共享消费者与请求体/签名/流 | 当前10类70项零skip，加单独启用HTTPS Java1/Chrome6零skip；真实定长/chunked边界、HMAC逐字节、SSE/上传/取消与512MiB堆自检 |

在线GET使用真实SysUserOnline类型组装测试列表，没有调用真实Redis枚举；会话DAO为隔离内存实现。POST调用生产controller代理和实际LogAspect，路由模板由fixture设置，未执行完整Spring MVC权限拦截旅程。异常handler为真实类的故障注入。HTTP/生产日志组件/真实MySQL持久化接缝成立，但这些替身边界不能冒充生产身份/Redis/ACL全链。

## 5. Workspace Verification

完整argv/cwd/时间/exit/方法/计数在 T-02-current-2026-09-23。current-workspace clean候选验证：

- 真实日志驱动v2 SHA cff4e66c3f108c71a212623fdab88f88783415f977d33803ed2bf2180024f452；精确canary方法必需，7类39项零skip。MySQL8.4.9只建本票sys_oper_log，随机loopback端口/最小应用账号，随机密码只在子进程环境，不放JVM属性/argv/XML。10项驱动合成保护通过。
- acceptance-consumers初始11类包含HTTPS1skip，不能验收；已执行10类70项零skip单独记录，随后当前三App productionbuild-v2并启用BrowserHttpsTransportIntegrationTest，Java1与Chrome6全部通过且零skip，组成完整消费者回归。未覆盖的外围HTTP夹具返回404如实保留，不声称完整系统集成。
- body-probe自证heapMaxBytes=536870912，4并发×2MiB、2轮8请求；包括Jetty/客户端/签名/XSS的单JVM探测，不能推断生产容量/SLO。
- 当前默认完整 `./mvnw test`：249类1000项，850执行通过、150环境skip，0fail/error。外部skip不算验收；本票required真实日志和HTTPS分别启用零skip通过。
- 当前 `./mvnw clean package -DskipTests` + `verify-admin-bundle.sh full`，再 `./mvnw clean package -Pbundle-core -Dmaven.test.skip=true` + `verify-admin-bundle.sh core` 均exit0；先保存测试计数及full产物SHA/清单再core clean。不把skipTests打包当测试。
- Skill facts、handbooks及产品diff-check均通过。前端本次无产品改动，productionbuild为HTTPS消费者输入；既有typecheck不冒称本次重跑。T30最终同源全change验收仍未完成。

## 6. 双轴审查

legacy_audit 安全/规格、ops_audit 功能/工程固定617a369与base差异，均未发现阻断问题。报告保留当时消费者pending，不将静态pass当最终执行；Lead另核后续真实39、消费者70+1/Chrome6、完整测试及bundle结果。报告明确真实session发行和GET/ACL/Redis测试边界。

## 7. Integration Verification

source-before/source-after为同617a369/tree a8d1181，完全clean。真实红/绿驱动也逐次验证同SHA/clean并按owner清理。绿色run pgid72280无活成员、owned容器无残留、捕获匿名卷086b99ee6dda9a6dc06e2a93dc77191b3e75029e1a2abd02b59a46c521506a5d逐名不存在、32815关闭，cleanup.errors=[]；红灯同样有逐名清理证明。当前result不随治理提交更换。没有空实现commit或新worktree。

## 8. 偏差与决策

开发第一次普通mvn选择器遗漏failIfNoSpecifiedTests=false，在上游无用例处停止，仅为执行配置错误；第二次compile有环境skip，只算编译。随后统一./mvnw。测试fixture在有效真实红灯前先修正踢出顺序和logger捕获：避免用已失效caller或空root日志造成假证据。测试-only真实红35项1失败，保留原始故障和新绿39，不伪称红阶段已检查后续sink。

首消费者命令未启用HTTPS导致1skip，严格计数器拒收；补跑使用新鲜生产App构建。首次App构建被环境PATH中pnpm11与仓库固定10.34.5冲突拒绝，在本票临时PATH加入调用corepack的shim后通过，未修改锁文件/版本/绕过版本校验。完整backend默认150环境skip明确列在计数，不作为全部集成通过。原始日志含Maven尾空格按字节保存；源文件diff-check与原始日志分开，不为全量空格绿灯改写日志。

## 9. 残余风险与交付定位

只保证已识别正常凭据字段、URL和共享日志消费者；未声称阻止攻击者把任意秘密伪装成任意元数据。真实生产Redis/完整在线权限链、生产部署与供应商不在本票隔离验收声明。外部凭据处置门已独立关闭，不重跑轮换或删除历史日志/业务表。T03当前无新实现复验与T38精确retry入口随后处理；整个change仍未完成，未推送/部署/归档。

## Skill Execution Records

```json
[
  {
    "id": "engineering-standards",
    "phase": "implement",
    "operation": "apply-scope-contract",
    "sha256": "dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9",
    "status": "passed",
    "evidence": [
      "<Path>{roots.state}/specdev/changes/2026-09-14-wta-plus-comprehensive-review/evidence/T-02.md</Path> §§3–9; T-02-current-2026-09-23/candidate1/path-audit.json and acceptance counts"
    ]
  },
  {
    "id": "wta-common-modules-guide",
    "phase": "implement",
    "operation": "apply-scope-contract",
    "sha256": "d7b7e105c37499e0e0f8ad9b1e2dbf379100df5b6a47e0d6affb04483f8b162c",
    "status": "passed",
    "evidence": [
      "<Path>{roots.state}/specdev/changes/2026-09-14-wta-plus-comprehensive-review/evidence/T-02.md</Path> §§3–9; T-02-current-2026-09-23/candidate1/path-audit.json and acceptance counts"
    ]
  },
  {
    "id": "engineering-standards",
    "phase": "verify",
    "operation": "verify-affected-contract-and-quality-gates",
    "sha256": "dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9",
    "status": "passed",
    "evidence": [
      "<Path>{roots.state}/specdev/changes/2026-09-14-wta-plus-comprehensive-review/evidence/T-02.md</Path> §§3–9; T-02-current-2026-09-23/candidate1/path-audit.json and acceptance counts"
    ]
  }
]
```

治理校验：`validate-specdev --stage implement` 当前exit0，0 errors/302共享路径warnings；后续产品仍单writer串行，原始记录见T-02-current-2026-09-23/candidate1/governance-close.json/log。
