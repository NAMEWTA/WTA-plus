# T-37 C1 functional / engineering review

- Fixed base: `38032d24335c52cafea855b19d51fb36295162ef`; candidate: `e2907c41f4b1bef442c68b95a0fc09aa9c3b273a`; command: `git diff 38032d2...e2907c4`; one implementation commit. Scope: the 22 product paths in `/tmp/wta-t37-c1/path-audit.json`. This is an independent read-only review of the immutable candidate, not a review of later working-tree edits.
- Contract: change Spec AC-037, ticket/37 revision 159, engineering-standards notification reference and applicable Java/API/module rules. Outcome: **request-changes**. The static design is directionally consistent with AC-037, but required real acceptance fails in two exact classes, and one failure is in the new Redis state transition.

## Findings

1. **Blocker — Redis `RETRYABLE` re-claim cannot complete under the production CompositeCodec/NameMapper.** New script `backend/wta-common/wta-common-notify/src/main/java/org/namewta/common/notify/idempotency/RedisNotifyIdempotencyStore.java:112-115` rewrites the value using the bucket codec. In the real CompositeCodec test, the second owner is acquired but `complete(second)` at line 81 returns false (`Redis 通知幂等占位已失效`), so the candidate cannot complete a legitimate retry; StringCodec alone is insufficient. Actual fixed-candidate run `/tmp/wta-t37/runs/b0d200e1eaf3766e/result.json`: `RedisNotifyIdempotencyStoreIntegrationTest.retryableOwnerCasRetainsTtlAndReadsLegacyStateWithProductionCompositeCodec` errors at test line 156; 3 tests, 1 error, 0 skips. Determine the actual wire-byte/codec or mapped-key mismatch, keep atomic CAS/PTTL/KEEPTTL and legacy JSON compatibility, then show the exact production-codec path completes and stale-owner/digest/TTL tests remain green. Do not clear old keys.
2. **Blocker to required evidence — the new real SMS path fails before the provider boundary.** In `backend/wta-admin/src/test/java/org/namewta/test/notify/NotifySmsDispatchIntegrationTest.java:480-498`, the new scenario installs an SMS variable mapping requiring `code` and `expireMinutes`, then submits `command("t37-code")`; submission throws `短信模板缺少必填变量映射 code`. The run reports `exactSingleAttemptTencentRateLimitReclaimsAndCallsTheSelectedBlendTwice` as an error at line 498, before its first provider call, so its two physical sends and Outbox retry assertions have not been exercised (10 tests, 1 error, 0 skips). Align fixture parameters/mapping while retaining the real Registry→Resolver→Dispatcher→MySQL/Redis chain and all physical-call assertions, then rerun clean candidate.

## Checked behavior and remaining limits

- The new public status variants distinguish confirmed `UNSENT_RETRYABLE`, confirmed terminal rejection, accepted, and unknown; old `FAILED` does not gain retry authority. `NotifyDispatcher` transitions only when **all** target results are retryable and current owner is acquired; transition failure escapes, mixed/unknown results complete conservatively. Redis state includes a nonce, digest guard, and KEEPTTL script. No cache flush or key deletion was introduced.
- Runtime single-target mapping gives only confirmed retryable a bounded `READY` path through the existing Outbox result transaction; accepted closes, unknown waits for receipt, terminal rejection closes. SMS and MAIL exceptions become outcome unknown. The SMS allowlist checks the selected blend identity plus structured Tencent single-recipient 30-second rejection; SMS4J 3.3.5 `TencentSmsImpl.getSmsResponse` source confirms maxRetries=0 stops its internal retry loop. Registry captures the actual proxied instance; default resolver has no retry authority.
- Tests added for stale owner, 20 concurrent claimants, digest conflict, retained TTL, legacy stored state, ambiguous responses, mixed targets, unknown result, and transition ACK loss. This reviewer did **not** run Maven or real services. Lead's first real run exited 1 with 13 tests / 2 errors / 0 skips. The original runner also misclassified Docker's lowercase `no such volume` stderr as cleanup failure; its separate read-only audit and frozen v2 runner are under `/tmp/wta-t37/`. Current container/port/process cleanup is confirmed, historical exact anonymous volume names were not recorded and cannot be reconstructed; this runner issue does not explain either Java test error.
