# T-21 本地实现与验证证据

revision82，status=review；parent/HEAD=`76dbbe84a34624234e379661a57b232529e34ed3`。用户要求本change全部实现暂不提交，implementation_commit/result_sha/candidate均为空，0 Done；没有暂存、提交、推送或部署。

## 实现与 AC-021

Home App定义共享Login实际消费的 --client-* 主题变量，沿用原青绿色品牌；补足正文/按钮的实际渲染对比。Login/Register卡片允许收缩、按钮和验证码操作可换行，HomeShell极窄宽度下导航和品牌换行，解决320物理像素、200%等效布局下的横向不可达内容。SSO保持原深色品牌和484px桌面外卡宽度，修正输入最小宽度与按钮对比。

共享Login增加准备失败后的“重新检查登录入口”按钮，复用既有prepare状态生命周期，并标注form aria-busy。没有改认证、会话、请求授权或后端合同。SSO原已有role=status/aria-live=polite/重试，Home Register原已有label/alert，Home callback原已有alert和焦点，Home nav/aside原有语义全部保留；callback产品文件未修改。真实原实现4条键盘流程已通过，未把已有正确功能当成本票新增。

System默认浏览器回归同步T19页面内持久错误提示：将错误可见定位限定#app，避免同文字全局toast产生歧义，保留无伪造数据/单次请求断言。独立严格检查发现旧evaluate返回Element联合类型，补HTMLButtonElement真实收窄，原双击只发一次请求断言保留且单独重验通过。

## 真实验证

| 验证 | 命令/环境及证据 | 结果 |
|---|---|---|
| 公开页面矩阵与键盘 | run-t21-browser.py T-21-accessibility-device-pixels --tests；实际调用 corepack pnpm exec playwright test --config playwright.accessibility.config.ts | 29/29通过，0skip，exit0：24矩阵+5键盘/AX；owned_cleanup=true |
| 全前端lint/类型 | run-t21-verify.py T-21-frontend-final frontend中的前两条 | corepack pnpm lint/typecheck均exit0；其test中断不能报通过 |
| 完整单元回归 | run-t21-verify.py T-21-unit-complete unit | corepack pnpm test exit0，602 Vitest+108 Node=710通过，0skip |
| 三App生产构建 | run-t21-verify.py T-21-build-second build | corepack pnpm build:prod exit0，Admin/Home/SSO全部成功 |
| 默认浏览器 | run-t21-default-e2e.py T-21-default-browser-final --reuse-build | 默认54项：53pass、1既有live Nacos环境skip，exit0；owned preview/config回收 |
| 真实SSO回归 | run-t21-sso-journey.py；Maven SsoHttpsSessionIntegrationTest，sso.journey.integration=true，调用playwright.sso.config.ts --grep T-07 | 12 Chrome+1 JUnit通过，0skip；真实HTTPS/Redis/MySQL，12次授权兑换被数据库断言确认 |
| 最终静态 | run-t21-verify.py T-21-static-verified static | 新旧两E2E+配置独立strict tsc、oxlint、35workspace architecture、Skill facts、diff全部exit0 |
| 最后测试类型收窄 | run-t21-default-e2e.py T-21-reset-button-verified --reuse-build --grep 'admin edits the server reset candidate' | 1/1通过，exit0，双击仍只发一个请求 |
| 治理 | T-21-governance.json | revision82 goal-plan/ticket-control/diff实际输出 |

各runner及JSON保留真实完整命令、退出码、日志与资源清理证据。所有执行串行、单worker；不使用子代理/worktree。SSO临时构建/admin/、/home/与/后已将三个原dist按逐文件哈希恢复；MySQL/Redis容器、网络、卷清单前后相同，现存服务不变。

## 可审查的视觉与可访问性证据

最终图像/测量位于 `T-21-accessibility-device-pixels-artifacts/`：Home login/register/callback及SSO authorize × 320/768/1440 × 100%/200%，共24 PNG+24 JSON。200%采用Playwright context CSS视口=物理宽度/2、DPR=2，等效桌面缩放的reflow与像素密度；每个PNG头校验输出宽度确为320/768/1440。所有scrollWidth不超CSS视口，代表桌面Home登录及320/200%注册、SSO截图已实际查看，表单必要动作无遮挡，保留两App原品牌差异。

实测文字对比：Home主按钮5.473:1，footer/注册说明7.113:1，Login状态7.578:1，Register状态4.759:1，SSO按钮5.671:1、状态6.919:1，callback错误15.272:1。阈值始终4.5；在真实disabled→ready有限动画结束后取computedStyle和最近不透明背景，未用源码色值代替渲染结果。详细采样见T-21-verification-counts.json。

五条流程只用Tab/键盘输入/Enter完成：登录入口失败重试、登录失败后成功、注册失败后成功、SSO会话网络失败与密码失败重试、callback返回登录与重新授权。焦点检查实际:focus-visible及前后outline/shadow/border/background变化；SSO错误还读取真实Chrome AXTree中的status/live=polite。原label/nav/alert作为回归保留。

边界：这是实际浏览器布局/键盘/可访问性树语义验证，**不是人工操作浏览器缩放菜单或人工读屏器听读，也不是全站WCAG认证**。29项专项通过显式HTTP fixture注入身份/故障，验证码为占位图，不证明后端或验证码可读性；真实SSO链由另外12项补足。默认Nacos真实代理场景要求NACOS_LIVE_BASE_URL，本轮未设而跳过，未计为通过；本票没有变动Nginx/Nacos，相关独立环境证据保留原责任票。专用SSO只运行受影响T07旅程，不把依赖固定端口的旧管理配置套件宣称为已验证。

## 检查点与 Skill Execution Records

T-21-input-checkpoint.json验证389条上游哈希；T-21-checkpoint.json保存9条本票路径，384个非重叠上游文件保持原哈希、5个重叠逐项登记。HEAD不变，所有修改在登记写集内。失败/中断历史保留于下方及各独立JSON，命名final的失败记录不冒充通过；最后静态通过记录为T-21-static-verified。

| Skill / phase / operation | 实际输入、操作与输出 |
|---|---|
| engineering-standards / implement / apply-scope-contract | 根据真实Home token、页面布局/对比/键盘基线定位缺项，只修改当前App样式和原prepare入口；保留既有标签/权限/会话/品牌，passed |
| engineering-standards / verify / verify-affected-contract-and-quality-gates | 29专项、53默认回归/1环境skip、12真实SSO/1JUnit、710单元、三App构建及strict/lint/架构/事实/9路径检查点；逐项记录AC、限制与资源回收，passed |

Skill入口sha与票据一致。提交/direct-parent出口依用户要求暂缓，commit AC不勾选，不标Done。

## 实施历史

Revision80：T-21开始，389条上游最新哈希全部核对；按当前品牌/共享Login token/SSO动态语义做实际浏览器诊断，保留正确label/nav，不换皮。19review/1in_progress/9ready/2blocked/0Done，全部提交暂缓。

完成标准：真实320/768/1440、200%缩放/键盘/读屏语义与渲染对比；保留品牌和原label/nav；前端质量门禁/三App构建、适用浏览器回归、checkpoint/Skill/治理全部通过。提交暂缓，不标Done。


初次视觉采样 T-21-visual-baseline 24张截图已生成并人工查看代表图；发现诊断夹具遗漏Client密码策略，导致注册落在不可用态，且CSS zoom不改变media query，不能作为浏览器200%reflow结论。该历史采样不用于验收/产品缺陷定性。夹具补合法策略；改用CDP将CSS视口减半且DPR设2，复现200%桌面缩放的布局和像素密度，明确该方法不等同人工读屏器或浏览器菜单操作。复查后才做产品修复，全部服务/Chrome已回收。


T-21-visual-baseline-reflow：合法Client policy，24张实际Chrome截图/DOM测量，owned_cleanup=true。320px、200%等效CSS viewport=160时，Home login/callback scrollWidth249、register318、SSO209均超出160；768/1440与对应2x无同类溢出。Home共享Login所需surface token为空。未改产品前先据此登记HomeShell及专用测试配置；后续对比使用实际computedStyle与nearest opaque背景，保留正常label/nav及SSO已有live region。


T-21-accessibility-red：29场景中28失败/1回调键盘通过。真实渲染确认Home主按钮文字和footer、SSO主按钮(3.322:1)对比不足、Home token缺失、放大溢出及登录初始失败无retry入口；此外测试自身有两个错误假设，不能冒充产品bug：CDP detach把DPR复原为1；焦点断言仅允许2px outline，误拒绝ElementPlus已有box-shadow与浏览器1px outline。修正为CDP由page/context生命周期关闭，以及真正键盘 :focus-visible + 前后outline/box-shadow/border/background变化（要求可见变化，不要求一种CSS实现），原目标与对比/布局断言保持；采样改为串行，下一轮原产品重验。


T-21-accessibility-red-corrected：24矩阵+登录准备失败retry为25项真实红灯；另4条原键盘登录/注册/SSO动态AX/回调返回与重新授权通过，证实已有语义和焦点不应重写。产品开始局部修复：Home定义原Login token及同品牌深色主按钮/hover；footer文字收深；HomeHeader极窄宽度换行；Login/Register卡片和验证码/按钮允许收缩换行；SSO保持原外卡484px/配色家族并收敛input min-width、按钮深蓝对比；Login已有prepare增加可键盘重试入口，复用原状态owner。SSO已有live region、所有原label/nav、Home callback保持原实现。


T-21-build-first exit0；T-21-accessibility-first 11pass/18fail，24组布局全部已无横向溢出，所有5条键盘流程通过。Home footer及注册intro在实际#f6f8f9底色下是4.467:1，继续收深为既有正文muted #475569，未降低4.5断言。部分主按钮样本是disabled→ready过渡中间色，不是稳态；采样现在等正式submit enabled和真实有限CSS动画结束，再测computed颜色，保持真实阈值。登录submit清除ElementPlus相邻按钮默认左margin，避免新增retry让全宽submit偏移。


T-21-build-second exit0；T-21-accessibility-second 29/29通过、0skip，24实际布局/对比和5键盘/AX流程全绿。查看desktop login与极窄register/SSO截图后检查PNG头发现Playwright fullPage截图按context默认DPR1导出CSS像素，虽测量时CDP DPR2正确，图片无法直接做同物理宽度放大对比。为使图像可审查，矩阵改为Playwright context本身DPR=zoom（CSS视口仍width/zoom），使用device像素截图并断言PNG宽度恰为320/768/1440；不改变任何产品或布局/颜色/键盘断言，下一轮保存最终物理像素图。


T-21-frontend-final：lint/typecheck exit0；runner在test途中退出143，未形成test结束记录。进程检查无剩余pnpm/Vitest/runner，具体终止原因未知；不能把前半段测试当全通过。检查并回收仅含已知pnpm shim的本轮t21临时目录，记录T-21-frontend-interruption.json。产品未变化，保留已通过lint/type结果，仅重跑完整test；本轮不无理由重复两项已通过门禁。


T-21-unit-complete 全部test退出0。T-21-default-browser 在54条默认套件的前5条期间退出143、无断言失败输出，原runner未写结束JSON。按启动前4173排他空闲、日志PID/实际cmdline/PGID核对，仅终止本轮1751685预览进程组，端口关闭并删除本次owned配置；记录T-21-default-browser-interruption.json。运行器新增SIGTERM/SIGINT记录/正常退出清理，子进程只按每轮唯一环境marker回收，不忽略或阻挡取消。下一轮用PTY运行，终止原因仍未知，不归咎产品或报通过。


T-21-default-browser-managed正常结束exit1：52pass/1fail/1skip。唯一失败为T19新增持久页面el-alert与全局toast同文案导致严格locator匹配两项；system-identity.spec.ts仅把错误可见断言限定到#app，保留无伪造行/一次Client请求/无未知API断言。既有NACOS_LIVE_BASE_URL专属真实Nginx/Nacos用例未设置而跳过，不能记为54/54通过；其环境在T10专属证据，非本票公开App改动。owned preview/config均已回收。


T-21-default-browser-final exit0：53pass/1既有live Nacos环境skip；所有受影响默认浏览器场景通过，临时服务/配置回收。SSO定向命令登记：复用SsoHttpsSessionIntegrationTest并启用sso.journey.integration，以playwright.sso.config.ts --grep T-07跑12个真实HTTPS、Redis/MySQL授权兑换、两App非根base/复杂state/过期恢复场景。身份和菜单是显式HTTP fixture，SSO走真实实现；其余旧SSO管理配置用例依赖固定端口运行环境，未将其计为本票通过。三App临时按/admin/、/home/、/构建，结束恢复原dist逐文件哈希；不改源码env、不部署或触碰现存服务。


T-21-sso-real真实后端JUnit1/浏览器12全通过，resources_restored=true，三App原dist哈希全部恢复。最终独立strict编译新增包含本票改过的system-identity.spec.ts，发现既有evaluate回调的Element联合类型没有click方法；未降级strict，补HTMLButtonElement运行时收窄，保留同轮双击/仅一次请求断言，重跑静态门禁和该受影响场景。首次static-final失败保留。
