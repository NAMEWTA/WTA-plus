# T-19 本地实现与验证证据

revision73，status=review。HEAD/parent_before=`76dbbe84a34624234e379661a57b232529e34ed3`；implementation_commit/result_sha/candidate 均为空。全部提交按用户指令暂缓，未暂存、提交、推送或部署，0 Done。

## 实现与 AC-019

User/Role/Menu 三个正式组件均已运行证实：旧筛选 A 晚到覆盖新 B，旧请求 finally 提前结束新查询 loading；T-19-query-red-settled 中6项真实红灯。System 现有 composables 新增 useLatestQuery，三个实际消费者各自创建局部 owner，复制查询参数/Client/分页/排序/嵌套条件并以 generation 限定结果、错误和 loading；卸载使当前请求失效。当前失败有页面内错误、loading结束且可重试，旧失败不影响当前状态。

Role/Menu 清空 Client 时立即使旧请求失效；切换 Client 在请求开始前清除旧列表与相关选择/树展开数据，B失败也不呈现A的行。保持现有过滤、排序、分页、树根/子项转换与权限按钮协议。没有扩充 HttpRequest/signal，没有宣称取消实际HTTP传输或支持未证实的无卸载会话Client切换。

User 列表与编辑使用独立 loading owner，页面仍可显示二者合并的忙状态；编辑请求结束不结束列表查询。查询不重置编辑草稿或Role权限选择，导入/凭据弹窗继续由原局部模块拥有。公共exports、manifest、后端和Axios未修改。

删除Admin无调用者的 useLoading/useFormDialog/useSearchReset 三文件；全源码rg零调用，AutoImport配置只导入库、不扫描本地hooks。保留被RoleSelect调用的useDialogState；没有建立跨web-domain共享状态框架或同义薄wrapper。

## 职责与重复实现裁决

| Owner | 当前职责/真实消费者 | 本票裁决 |
|---|---|---|
| UserPage（1209行） | 列表/部门过滤、用户基本表单、按Client的角色候选；UserCredentialDialogs、useUserImport、UserViewDrawer已分别拥有凭据/导入/查看 | 提取查询生命周期到现有System composables，编辑loading独立；保留页面组合和既有角色候选逻辑，未按行数机械拆分 |
| RolePage（1281行） | 角色列表、基本表单、菜单/按钮选择、继承/禁用推导和数据权限树 | 查询owner独立，不触碰现有权限算法与表单写入合同；该票没有证据要求重写完整权限树，保留同owner的现有组合 |
| MenuPage（741行以内） | Client筛选、根/子项和展开缓存、编辑父节点/级联删除 | 查询结果与树缓存同一次apply，Client改变清旧状态；菜单表单/删除保留独立owner |
| System composables | Post/Dept/Dict等真实Loading/Form/Search消费者；新查询primitive仅User/Role/Menu三消费者 | FormRef为本地最小结构、initialPageNum/pageSize和setTitle合同保留；现有mutation工具不偷换成latest-only语义 |
| Workflow composables | Category/Definition/SpEL等；FormInstance，Search固定pageNum=1；TableSelection还返回selectedRows | 与System返回/选项并不完全同义，保持包内owner，不为了去重反向依赖System |
| Demo composables | Test/Tree页面；Form标题必需、Search仅pageNum、Tree使用本地children合同 | 保留当前较小合同；不用额外层包装System版本 |
| Admin hooks | 只有RoleSelect引用useDialogState，其toggle/title合同仍有效；另三文件零消费者 | 删除三份死封装，保留实际宿主工具 |

1k行以上的实际文件仅User/Role，计数来自T-19-counts-and-size.json；行数不是验收目标。权限树与编辑状态继续在各组件/实例内，查询primitive不接收或共享form/permission引用。未声称修完所有System页面、角色编辑异步链或全仓Loading并发语义，实际范围为本票三个代表查询和明确死封装。

## 实际验证

| 验证 | Evidence | 命令/结果 |
|---|---|---|
| 真实组件红灯 | T-19-query-red-settled.json | 正式User/Role/Menu SFC经Vue挂载，6/6行为失败，exit1；不是源码字符串测试或复制实现 |
| 最终定向 | T-19-query-final.json | `frontend: corepack pnpm --filter @namewta/web-domain-system exec vitest run src/user/page-query.test.ts`，26/26、0skip、exit0 |
| 全前端 | T-19-frontend-final.json | `frontend: corepack pnpm lint`、`typecheck`、`test`全部exit0；538 Vitest+108 Node=646通过、0skip，System包52项通过 |
| 生产构建 | T-19-build-final.json | `frontend: corepack pnpm build:prod` exit0，Admin/Home/SSO三App完成 |
| 静态与引用 | T-19-static-final.json | architecture:check 35workspace/0违规、Skill facts、git diff --check均exit0；Admin旧三工具rg exit1表示零命中 |
| 治理 | T-19-governance.json | revision73 goal-plan、ticket-control、diff的实际结果 |

26场景覆盖三页乱序、旧错误/旧finally、当前失败/重试空结果、深查询快照、卸载、编辑草稿/权限选择不变，Role/Menu Client切换/清空及失败，User编辑完成不影响列表。测试挂载真实SFC的setup/lifecycle，使用轻量无DOM渲染器替换模板、正式domain service与受控HTTP Promise；不冒充浏览器视觉/真实HTTP/后端权限E2E。票据E2E disposition为not-required，既有App整体验收归T-30，未无关重复后端/数据库测试。

## 检查点与 Skill Execution Records

输入T-19-input-checkpoint.json核对356条最新上游哈希；最终T-19-checkpoint.json为8路径（3删除），355条非重叠上游哈希不变，共享重叠仅UserPage.vue。正式commit/direct-parent出口仍暂缓，不标Done。

| Skill / phase / operation | 实际输入、执行与输出 |
|---|---|
| engineering-standards / implement / apply-scope-contract | 以三页实际红灯落实query/编辑/卸载owner、当前错误/loading及Client失败关闭；保留Vue/web-domain边界，passed |
| namewta-fullstack-development / implement / apply-scope-contract | 回读正式domain transport和System宿主合同，快照保留Client/分页/排序，权限与树语义不重写；未扩signal或跨owner共享，passed |
| engineering-standards / verify / verify-affected-contract-and-quality-gates | 26正式组件用例、646全前端、三App构建、架构/类型/引用审计和哈希一致，passed |

Skill入口SHA未漂移，与票据frontmatter一致。下面保留实施历史，历史中的“下一步”不代表最终未完成。

# T-19 实施过程历史

revision72，356条上游hash通过；未提交。已读取System实际User/Role/Menu、四组composables、宿主状态和现有测试。

三次定向红灯复核：第一轮实际SFC在Vitest SSR转换后缺测试app的ssrContext.modules，6项装配错误，不算产品复现；补测试context后第二轮三页均复现旧A覆盖B，loading断言时点太早；第三轮使用一次setImmediate清空Promise链而非猜测微任务个数，6/6均为产品红灯，三页各覆盖列表顺序与旧finally提前终止loading。日志T-19-query-red[-mounted/-settled]全部保留。

下一步具体改变：在System已有composables中给三个真实查询消费者加入局部generation/snapshot/error/loading owner；Role/Menu清Client时立即失效，User编辑loading独立；不新增transport signal或全局状态框架，不以删除断言处理红灯。恢复入口run-t19-verify.py query-tests。

6个原行为红灯已绿色。扩展到26个实际SFC场景，首次全包typecheck暴露测试HttpClient泛型/组件内部测试视图类型不准确，已按真实泛型边界/Reflect内部访问修正，正式编译规则不变。随后加入Role/Menu Client改变立即清旧列表的失败关闭行为；其两条卸载测试在开始新Client之前抓取了旧数组引用，实际正确清空发生于卸载之前，现调整为请求开始后、卸载前抓取基线，保留同一引用不被卸载后响应修改的断言。

已确认Admin useLoading/useFormDialog/useSearchReset在整个Admin源码零消费者，且AutoImport只含库、不扫描本地hooks，移除三份死封装；useDialogState被RoleSelect使用，保留。三个web-domain的现有Form/Search/Loading仍各有真实消费者与不同选项/返回合同，不跨owner上收。System仅三个代表查询共用新useLatestQuery，原mutation loading语义保留。
