# T-28 evidence

Current candidate `459c5a3e`. The only change is the owned MySQL fixture now parses `NotifyRecipientMapper.xml`, so the real fan-out submit can bind `insertBatch`. Production wake code was not changed. AC-028 stays owned by T-30. The adjacent OIDC change was not modified.

## MySQL and Redis

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=NotifyWakeIntegrationTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Dnotify.wake.integration=true \
  -Dnotify.mysql.integration.url='jdbc:mysql://127.0.0.1:33139/namewta_notify_test_t28?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC' \
  -Dnotify.redis.integration.port=33140
```

Exit 0 after the fixture change. The first run, before that XML was loaded, failed with `Invalid bound statement (not found): NotifyRecipientMapper.insertBatch` and is not a pass. Isolated MySQL `mysql:8.4.9` (`sha256:c36050afdca850f23cef85703f84c7531a5ae155a11b5ee1c60acb09937c4084`) on `127.0.0.1:33139`, database `namewta_notify_test_t28`, loaded `10-cde-base-ddl.sql` through `50-cde-base-dml.sql`. Redis `redis:8.6.3` on `127.0.0.1:33140`. The password was supplied only through the test's existing environment variable and is not recorded. Both containers were removed after the atomic regression below.

`NotifyWakeIntegrationTest`: tests=1, failures=0, errors=0, skipped=0, 4.852s. The test printed `commitMs=347`, `firstProviderMs=456`, `pausedRedisCommitMs=270`, scenarios `slow-provider,wake-storm,lost-wake,redis-timeout,poll-recovery`. Commit finished in 347ms while the provider stayed blocked on another thread, under the 1000ms bound. One hundred extra wake publications did not create a second provider call. After both subscribers were removed, the outbox stayed `READY` until `poll` delivered it. A 1200ms Redis pause still committed in 270ms, left the outbox `READY`, and the later poll delivered exactly one attempt and one inbox recipient.

The same fixture change was re-run as `NotifyAtomicResultIntegrationTest`: tests=66, failures=0, errors=0, skipped=0, 26.10s. `wta-notify` module tests were not re-run; this commit does not change that module's production sources. Their last run is in `evidence/T-23-replan-2026-09-23.md` (117 tests, 0 skipped).

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase, operation `verify-affected-contract-and-quality-gates`, status passed. The implement-phase edit is the test fixture XML load only.
