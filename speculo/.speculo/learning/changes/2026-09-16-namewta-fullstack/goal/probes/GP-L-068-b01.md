---
artifact: learning-goal-probe
probe_id: GP-L-068-b01
lesson_id: L-068
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-068 / b01

审问对象是已写成的 Lesson `lessons/L-068-flw-definition.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **两格**：`A:FlwDefinitionController.*`、`B:IFlwDefinitionService.publish,importJson,removeDef`。OBJ-68 原文：能口述 `FlwDefinitionController` 发布/导入。档是**解释+走查**。矩阵 (a) 一行把 13 个 Java 名印齐（`list, unPublishList, getInfo, add, edit, publish, unPublish, remove, copy, importDef, exportDef, xmlString, active`），模块 `wta-workflow`，仍 `uncovered`。矩阵 (b) **没有** `IFlwDefinitionService.publish,importJson,removeDef` 独行；已印的 workflow (b) 是 `IFlwTaskService.startWorkFlow, completeTask, backProcess, urgeTask, terminationTask`（L-070）与 `TestLeaveController.submitAndFlowStart`（L-072）。矩阵 (c) `wta-modules` 业务房间 / `layered-classic` 已 covered（L-002 / L-003）；登记表 **有** `wta-workflow` classic 行。MySQL 8.4 / `dao-mapper-ownership` / `bundle-full-job` 仍 uncovered。矩阵 (d) **没有**「发布图纸 / 导入 JSON / 删定义」走廊；已 uncovered 的是「主路径 工作流请假」（L-072, L-070）与「失败路径 工作流驳回/终止」（L-070）。本批按派单只审问 **13 扇 `/workflow/definition` HTTP + 包装三闸**；口试按磁盘：一块门牌两把钥匙（6 扇进 `IFlwDefinitionService`，7 扇直通 `DefService`），发布点非申请人 BETWEEN 的办理人，导入读 JSON 并盖请求分类，删除只翻 `flow_his_task`。不要把分类七扇、实例/`WorkflowService`、任务枢纽、SpEL、请假 `submitAndFlowStart`、厨房 `createWorkflowDefinitionService` 或九工厂行并进本两格。接口另外三法 `queryList` / `unPublishList` / `exportDef` 是 (a) 列表与导出走查，**不是** (b) 三闸。

抽查锚点（2026-09-17）：`backend/wta-modules/wta-workflow/src/main/java/org/namewta/workflow/controller/FlwDefinitionController.java`；`service/IFlwDefinitionService.java`；`service/impl/FlwDefinitionServiceImpl.java`；`service/impl/FlwCommonServiceImpl.applyNodeCode`；`service/IFlwCommonService.java`；`mapper/FlwCategoryMapper.selectCategoryIdsByParentId`；`domain/vo/FlowDefinitionVo.java`；`common/ConditionalOnEnable.java`；`handler/{FlowExceptionHandler,WarmFlowDefinitionResponseAdvice}.java`；`service/impl/{CategoryNameTranslationImpl,WorkflowServiceImpl}.java`；`wta-api/.../workflow/api/WorkflowService.java`；`JsonUtils.parseObject(byte[])`；`BaseController.toAjax(boolean)`；`RepeatSubmit` 默认 5000ms；登记表 `03-backend-module-modes.md` workflow classic 行；`wta-admin/pom.xml` `bundle-full` / `bundle-core`；`application.yml` `warm-flow.enabled` / XSS `/warm-flow/save-json` / 安全排除 `/warm-flow-ui/config`；DDL `30-cde-workflow.sql` `flow_definition`/`flow_node` 与菜单 F 型九串；DML `50-cde-base-dml.sql` 两套已发布实名定义；模块 **无** `src/test`；旁证（格子归 L-073）`frontend/packages/domains/workflow/src/{index,index.test}.ts`、`web-domains/workflow/src/definition/{DefinitionPage,DesignPage}.vue`、`e2e/workflow-definition.spec.ts`。

## Q1 澄清

- objective_id: OBJ-68
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-68；chain L-068 覆盖列两格；课 `claimed_cells` 两格；矩阵 (a) 13 名仍 uncovered；(b) 无本课符号，任务/请假 (b) 挂 L-070/L-072；(c) classic 已 covered；(d) 无发布走廊，请假主路径挂 L-072；课开篇「口试名单就是矩阵这两格」+ 负空间 L-067 / L-069…L-073 / L-003
- difficulty: medium

课把宏观图纸柜台收成「一块 `/workflow/definition` 牌子、13 扇 classic 窗、两把钥匙、三道包装闸」。计划里至少有五套可数对象，派单点名的是 **一格 (a) + 一格 (b)**：

1. **OBJ-68 原文。** `course.md`：能口述 `FlwDefinitionController` 发布/导入。一句里是 **走查**，没有把 `removeDef`、13 个方法名、`WorkflowService.startWorkFlow` 或 `createWorkflowDefinitionService` 写成第三张矩阵行。课把 OBJ 扩成「13 扇 + 发布/导入/删除三闸」，那是走查，不是第二容器。
2. **chain / 课 `claimed_cells`。** 1:1 两格。`(a)` 用 `.*`，口试按磁盘 13 个公开 Java 方法。`(b)` 只收 `publish` / `importJson` / `removeDef`。不要把接口上的 `queryList` / `unPublishList` / `exportDef` 算进 (b)。
3. **矩阵两处不对齐。** (a) 一行 13 名与磁盘映射 1:1，仍 uncovered。**(b) 表里没有本课这一格。** chain 写了，课头写了，矩阵 (b) 没印。Lead 若只按「矩阵现印行」盖章，会把三闸落成幽灵格子；Lead 若凭「发布二字」把 L-070 任务副作用或 L-072 `submitAndFlowStart` 一并盖上，才会假闭合邻课。
4. **课自己的操作定义。** 门牌 `/workflow/definition`。类上 `@ConditionalOnEnable` + `@Validated` + `@RestController`，继承 `BaseController`，注入 `DefService` **和** `IFlwDefinitionService`。6 扇包装：`list`/`unPublishList`/`publish`/`remove`/`importDef`/`exportDef`。7 扇直通引擎：`getInfo`/`add`/`edit`/`unPublish`/`copy`/`xmlString`/`active`。发布闸只点「非申请人的 BETWEEN 空白 `permissionFlag`」。导入 multipart `file`+`category`，服务名 `importJson`。删除只查 `flow_his_task`。
5. **邻格 / 未入库。** L-067 分类七扇（本课只消费 `selectCategoryIdsByParentId`）。L-069 实例窗与 `wta-api` `WorkflowService`。L-070 任务枢纽五支副作用。L-071 SpEL。L-072 请假提交并拉起**已发布**定义。L-073 厨房 URL 表与 `legacyDefinitionXml`。L-003 已认 classic，本课不得再盖 `C:layered-classic`。不要发明「发布图纸」(d)。

问：本两格闭合标准是「13 扇路径/动词/权限/委托（6 包装 + 7 `DefService`）+ 发布点非申请人 BETWEEN + 导入 JSON 盖请求分类 + 删除只认历史任务本，且不把 `queryList` 算进 (b)、不升 `DefService` 为第三张 (a)、不发明 (d)、不碰厨房/九工厂」，还是 OBJ 那句必须先把矩阵补上 (b) 行才给 13 扇盖章、或必须等 GP-L-073 厨房缺 `definitionXml` 才给本柜台盖章？Lead 若凭「`FlwDefinitionController` 四字在函数表里」把 `WorkflowService.*` 或 `createWorkflowDefinitionService` 标 covered，是否假闭合了实例合同与前端工厂？Lead 若因矩阵 (b) 没印就把三闸判成未讲，是否假判？

## Q2 证据

- objective_id: OBJ-68
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 32–221；IFlwDefinitionService 18–71；Impl 51–207；FlwCommonServiceImpl 186–192；FlwCategoryMapper 38–44；FlowDefinitionVo 60–86；ConditionalOnEnable 26–28；FlowExceptionHandler 26–30；WarmFlowDefinitionResponseAdvice 24–33；CategoryNameTranslationImpl 22–27；WorkflowService 16–80；JsonUtils 96–100；BaseController 29–31；RepeatSubmit 20；R.data 86–88；modes.md 22；admin pom 149–195；application.yml 283 / 368 / 408–423；30-cde-workflow.sql 14 / 33 / 37 / 265 / 273 / 292–300；50-cde-base-dml.sql 1435–1456；domains index.ts 238 / 259–367；index.test.ts 23 / 53–64 / 87–92；DefinitionPage.vue 105–227 / 264–274 / 317–320 / 447–541 / 580–608 / 709；DesignPage.vue 1–22；e2e 112–163 / 308–319；openapi.ts 1026；模块无 src/test；后端零 definitionXml 映射
- difficulty: high

课的 13 扇 + 三闸哪些能在工作树直接复现，哪些只是测试名单比课表窄、或把厨房/引擎邻居讲成了本柜台第三扇？

**仍能复现（两格）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 类注解 `@ConditionalOnEnable @Validated @RequiredArgsConstructor @RestController @RequestMapping("/workflow/definition")`；`extends BaseController`；两字段 `DefService` + `IFlwDefinitionService` | Controller 32–40 |
| 公开 HTTP **正好 13** 个，与矩阵 13 名 1:1；**没有** `definitionXml` | Controller 49–221；后端检索零命中 |
| `GET /list` 权限 `workflow:definition:list` → `queryList`；`GET /unPublishList` 同权 → `unPublishList` | 49–66 |
| `queryList` 再 `eq(isPublish, PUBLISHED)`；`unPublishList` 再 `in(UNPUBLISHED, EXPIRED)` | Impl 72–90 |
| 共用 `buildQueryWrapper`：code/name 模糊；`category` 非空则 `selectCategoryIdsByParentId`（子孙 **+ 自己**）转字符串 `IN`；`orderByDesc(createTime)` | Impl 102–112；FlwCategoryMapper 38–44 |
| 列表 `BeanUtil.copyToList(..., FlowDefinitionVo.class)`；`categoryName` 靠 `@Translation(type = category_id_to_name)` | Impl 76 / 92；Vo 60–61；FlowConstant 29 |
| `GET /{id}` 权限 `query` → `defService.getById`，返回 `Definition` 不是 Vo | Controller 74–78 |
| `POST /` = `checkAndSave`；`PUT /` = `updateById`；两枪 `@Log` + `@RepeatSubmit` + Controller `@Transactional` | 86–108 |
| `PUT /publish/{id}` 权限 `publish`；`@Log INSERT`；有 RepeatSubmit；**Controller 无 Transactional**；进包装 `publish` | 116–122 |
| 包装 `publish`：`@Transactional`；拉 `flow_node`；空列表仍 `defService.publish`；有节点先 `applyNodeCode`；空白 `permissionFlag` 且不是申请人且 `NodeType.BETWEEN` 才记 `nodeName`；有名字 → `节点【{}】未配置办理人!` | Impl 120–138 |
| `applyNodeCode` = `getFirstBetweenNode`；空 → `流程定义缺少申请人节点，请检查流程定义配置` | FlwCommonServiceImpl 186–192 |
| `PUT /unPublish/{id}` 同权 `publish`；直通 `defService.unPublish`；Controller 有事务 + RepeatSubmit；`@Log INSERT` | Controller 130–137 |
| `DELETE /{ids}` 权限 `remove`；无 RepeatSubmit；Controller 无事务；`toAjax` → 成功 `R.ok()` 无 data | Controller 145–150；BaseController 29–31 |
| `removeDef`：只查 `flow_his_task`；有行且定义还在 → 中文 `流程定义【{}】已被使用不可被删除！`；有行但定义查不到则继续；`defService.removeDef((List<Long>) ids)`；引擎异常 → 英文 `Failed to remove flow definitions` | Impl 186–207 |
| `POST /copy/{id}` 直通 `copyDef`；`@Log INSERT` + RepeatSubmit + 事务 | Controller 158–165 |
| `POST /importDef` 权限 `import`；multipart `file`+`category`；无 RepeatSubmit；Controller 无事务；进 `importJson` | Controller 174–179 |
| `importJson`：`parseObject(bytes, DefJson)` → `setCategory(请求参数)` → `defService.importDef`；空字节返回 null 再 NPE；`catch (IOException)` 才 `文件读取失败，请检查文件内容` | Impl 147–159；JsonUtils 96–100（Jackson 3 `tools.jackson`，解析失败不是 IOException） |
| `POST /exportDef/{id}` 权限 `export`；方法 `void`；`exportJson` UTF-8；`Content-Type: application/text`；`Content-Disposition: attachment;`（无 filename） | Controller 188–193；Impl 168–178 |
| `GET /xmlString/{id}` 权限 `query`；`R.data(defService.exportJson(id))` — JSON 文本不是 XML | Controller 201–205；R 86–88 |
| `PUT /active/{id}?active=` 权限 `active`；真 `defService.active`、假 `unActive`；RepeatSubmit + 事务 + `@Log UPDATE` | Controller 214–221 |
| 接口正好 6 法：`queryList` / `unPublishList` / `publish` / `exportDef` / `importJson` / `removeDef` | IFlwDefinitionService 18–71 |
| `@ConditionalOnEnable` = `warm-flow.enabled=true`；Controller 与 `FlwDefinitionServiceImpl` 都打了；yml 默认 true；LiteFlow `enable` 跟随同一开关 | ConditionalOnEnable 26–28；Impl 51；yml 408–423 |
| `wta-workflow` 登记 classic；只进 `bundle-full`；`bundle-core` 注释「不装配工作流引擎」 | modes.md 22；pom 149–177 / 193–195 |
| 引擎 `FlowException` → `R.fail(message)` | FlowExceptionHandler 26–30 |
| 菜单 C 型 `workflow/processDefinition/index` + `workflow:definition:list`；F 型九串 query/add/edit/remove/export/import/publish/copy/active；**没有**单独 unPublish 权限；隐藏设计页 perms 写成 `workflow:leave:edit` | 30-cde-workflow.sql 265 / 273 / 292–300 |
| `is_publish` 注释 0/1/9；`node_type` 0–4；`permission_flag` 可 `@@` 分隔 | SQL 14 / 33 / 37；Vo 68–86 |
| 种子 `profile_person_verification` 已发布：START `person_apply` type 0 办理人 null；BETWEEN `person_review` type 1 有 `role:…`；END `person_finish` type 2 | 50-cde-base-dml.sql 1435–1456 |
| RepeatSubmit 默认 5000ms；list/remove/importDef/exportDef/xmlString/getInfo **没有** | RepeatSubmit 20 vs Controller 各方法 |
| 模块 **没有** `src/test` | `wta-workflow/src` 只有 `main/` |

**邻格仍能复现（不计入本两格闭合）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 厨房 `legacyDefinitionXml` → `GET /workflow/definition/definitionXml/{id}`；单测锁这条 URL；**Vue 不喊**；Controller 无窗 | domains index.ts 353–354；index.test.ts 23 / 53；DefinitionPage 零命中 |
| 厨房 `importDefinition` 带 `headers: { repeatSubmit: false }`；后端方法上没有 `@RepeatSubmit` | index.ts 358–359；Controller 174–179 |
| `deleteDefinition` 路径 `encodeURIComponent` 后再逗号拼接 | index.ts 238 / 355；index.test.ts 87–92 |
| 导出不走 freeze 方法：页 `runtime.download('/workflow/definition/exportDef/{id}', {}, '{flowCode}.json')`；e2e 锁 `POST /exportDef/d0` | DefinitionPage 709；e2e 160–163 / 317 |
| TAB「已发布」/`'0'` 打 `list`；「未发布」/`'1'` 打 `unPublishList`；`isPublish==0` 红标未发布，其它非 1 红标失效；发布钮 `isPublish !== 1` | DefinitionPage 105–107 / 149–153 / 211 / 459–471 |
| 发布确认文案含「发布后会将已发布流程定义改为失效」；包装层自己不改别人的 `isPublish` | DefinitionPage 529–541；Impl 120–138 无此 SQL |
| 导入 `FormData` `file`+`category`；`accept` json/text；文案「仅支持 json」；顶级 `ALL` 在 `before-upload` 拦下 | DefinitionPage 264–274 / 580–608 |
| 点分类 `id==='0'` 把 `category` 清空 | DefinitionPage 447–452 |
| 设计按钮核 `workflow:definition:query`；`DesignPage` iframe `runtime.designUrl`；Advice 只切 `WarmFlowController.queryDef` 补 `nodeRatio="0"` | DefinitionPage 186–205；DesignPage 1–22；Advice 24–33 |
| 安全排除 `/warm-flow-ui/config`；XSS 排除 `/warm-flow/save-json` | yml 283 / 368 |
| `WorkflowService` 在 `wta-api`，方法是删实例/终止/启动/办结，不是图纸三闸 | WorkflowService 16–80；Impl 是 `WorkflowServiceImpl` 不是 `FlwDefinitionServiceImpl` |
| 动态表单页上 Y 禁用；`add`/`edit` 仍把 body 交给引擎 | DefinitionPage 317–320；Controller 91–107 |
| e2e：未发布 TAB → `PUT /publish/d1` → 再 `GET /list`；导入 `POST /importDef` | e2e 308–319 |
| generated openapi 有 `POST /workflow/definition/exportDef/{id}` | openapi.ts 1026 |
| web-domain 权限组含 `workflow:definition:export` | web-domains/workflow/src/index.ts 75–86 |

**课写过、测试名单更窄或口试标签写偏（不是证伪本两格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 13 扇方法表 / 办理人闸 / 空文件 NPE / 占用中文 | `wta-workflow` **零** Controller/Service 单测。证人是生产源码。前端 e2e 只锁 URL 顺序，不锁 BETWEEN 闸 |
| 「厨房 13 窗对齐」 | freeze 对象 **没有** `exportDefinition`；导出走 `runtime.download`。另多一条没有后端窗的 `legacyDefinitionXml` |
| `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |
| 「短路顺序：先空白，再申请人，再 BETWEEN」 | 源码是 `&&` 短路，语义等价；没有单独的 if 阶梯 |

问：在 13 扇 URL/动词/权限/委托、两只列表抽屉（1 vs 0+9）、发布闸只点非申请人 BETWEEN、导入 JSON 盖分类、删除只翻历史本、xml 假名、空文件 NPE、无 `definitionXml` 窗、classic + 条件装配 已经能互证的前提下，本两格是否还要把「模块无单测」「厨房多遗留 URL / 少导出方法」写成开篇半句才算讲完？矩阵 (a) 13 名是否与磁盘 1:1，不必等补 (b) 行或等 L-073 才给 **(a)** 盖章？chain 已写的 (b) 是否已经在 `FlwDefinitionServiceImpl` 三法里讲完，只差矩阵补行？

## Q3 假设与反例

- objective_id: OBJ-68
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 课反例 1–14 / 误区 1–16 / 边界 1–10；Controller 13 映射；Impl publish/importJson/removeDef；applyNodeCode；种子 type 0/1/2；JsonUtils 空字节；厨房 definitionXml；WorkflowService 非本接口
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪道闸认错？

1. **「13 扇都进 `flwDefinitionService`.」** 七扇直通 `defService`。课反例 1。
2. **「`IFlwDefinitionService` 就是 `wta-api` 的 `WorkflowService`.」** 前者在模块内管图纸；后者管实例生命周期。课反例 8 / 误区。
3. **「`unPublishList` 等于 `isPublish=0`.」** 还有失效 9。课反例 2。
4. **「`xmlString` 返回 BPMN XML.」** `exportJson`。课反例 3。
5. **「导入走 `@RequestBody DefJson`.」** multipart 文件。课反例 4。
6. **「发布权限和取消发布权限不同.」** 都是 `workflow:definition:publish`。课反例 5。
7. **「删除会查 `flow_instance` / `flow_task`.」** 只查 `flow_his_task`。课反例 6。
8. **「`applyNodeCode` 是 START.」** 第一个 BETWEEN。种子 START 叫「提交申请」，第一个 BETWEEN 叫「人工复核」。课反例 7。
9. **「Controller 有 `GET /definitionXml/{id}`.」** 没有。那是厨房遗留。课反例 9。
10. **「设计器保存打 `PUT /workflow/definition`.」** `edit` 改定义头；节点图画在 Warm-Flow UI。课反例 10。
11. **「空 JSON 文件提示文件读取失败.」** 0 字节 → null → NPE。`IOException` 那句只包 `file.getBytes()`。课反例 11。
12. **「本模块有单测钉死办理人闸.」** 2026-09-17 没有 `src/test`。课反例 12。
13. **「`publish` 的 `@Log` 是 UPDATE.」** 源码 `INSERT`；unPublish、copy 同样。课反例 13。
14. **「把 `createWorkflowDefinitionService` 标 covered.」** OBJ-73。课反例 14。
15. **「空节点列表会因为缺少申请人而拒绝发布.」** 空列表跳过 NAMEWTA 闸，仍交给引擎。课边界 3。
16. **「有节点但没有 BETWEEN 会进入办理人名单.」** 先炸「缺少申请人节点」。课边界 4。
17. **「闸会解析 `role:` / `@@`.」** 只认空白。课边界 5。
18. **「服务会校验导入分类存在.」** 不会；页面拦顶级。课边界 6。
19. **「取消发布能替代删除闸.」** `unPublish` 直通引擎，不看历史本。课正例 8。
20. **「前端 `repeatSubmit: false` 等于后端 `@RepeatSubmit`.」** 导入后端没有这注解。课机制 C。
21. **「`bundle-core` 也能发定义.」** 模块不在 core。课误区 13。
22. **「关 `warm-flow.enabled` 后 13 扇变 403.」** 类不进容器，是没有这栋楼，不是没权限。课边界 1。
23. **「包装层自己把旧版打成 9.」** 那句是页面确认文案 + 引擎 `publish`；NAMEWTA 闸无此 SQL。课机制 B。
24. **「OBJ-68 包含 `FlwCategoryController`.」** OBJ-67。课误区 2。
25. **「设计页 perms `workflow:leave:edit` 是本柜台设计权.」** 种子如此；定义页设计钮核 `query`。课误区 16。

问：读完后仍说不出的，是不是「类名」，而是 **13 扇两把钥匙、已发布抽屉 vs 未发布含失效、申请人=第一个 BETWEEN 不是 START、导入是 JSON 盖分类、删除只翻历史本、xml 是假名、厨房 `definitionXml` 没有后端窗、`WorkflowService` 不是这张接口、矩阵 (b) 没印不等于三闸没讲**？

## Q4 视角

- objective_id: OBJ-68
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-68 vs L-003 / L-067 / L-069…L-073；chain 两格 vs 矩阵 (a) 一行 vs 矩阵 (b) 缺行
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这两格**？

| 视角 | 多出来的边 | 是否本课这两格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 13 名 1:1；(b) chain 有、表没有；(d) 无发布走廊；请假 (d) 挂 L-072 | **是** 闭合定义。(a) 可单独 covered。(b) 应补行或诚实记成 (a) 解释到的副作用，不要发明 (d) |
| 柜台电工 | 13 扇路径/动词/权限；6 包装 / 7 直通；publish 无 Controller 事务；xmlString 用 `R.data` | **是** (a) |
| 包装闸作者 | BETWEEN 点名；JSON 盖分类；历史本占用；空文件 NPE；英文 RuntimeException | **是** (b) 三法。`queryList`/`exportDef` **否** |
| 分类树（L-067） | `selectCategoryIdsByParentId` 含自己；导入 category 字符串 | 消费一句 **是**。七扇 CRUD **否** |
| 实例合同（L-069） | `WorkflowService` 按业务 id 启停删实例 | 负空间 **是**。合同盖章 **否** |
| 任务枢纽（L-070） | `permissionFlag` 变成用户；五支副作用 | 闸只问「写没写」**是** 边界。办理链 **否** |
| SpEL（L-071） | 表达式目录 | 不要把 `permissionFlag` 说成 SpEL **是**。目录 **否** |
| 请假（L-072） | `submitAndFlowStart` 消费已发布定义 | 菜谱上台 **是** 本课。示例流 **否** |
| 厨房/菜单（L-073） | freeze URL；`legacyDefinitionXml`；`runtime.download` 导出；iframe 设计器 | 对照假名/遗留 URL **是**。工厂盖章 **否** |
| classic 登记（L-003） | Controller → ServiceImpl → Mapper + 引擎钥匙 | 房间画像一句 **是**。不得重盖 (c) |
| 档案种子（L-034 / L-040） | 已发布定义走 profile `workflow.start` | 种子证明 START≠申请人 **是**。档案网关 **否** |
| MySQL 8.4 | 表注释 0/1/9；菜单九串 | 对照 **是** (a)。容器 **否** |
| (d) 发布走廊 | 矩阵无此行 | ASCII 三图是 (a)/(b) 文字等价物。不要发明 |

问：Lead 若凭「13 个名字在 (a) 行里」把该行标 covered，是否还要求读者能指着 **两把钥匙、两只抽屉、BETWEEN 申请人、JSON 盖分类、只翻历史本、xml 假名、无 definitionXml 窗**？这些是不是已经在本格机制链/反例里，而不需要等 L-073、也不需要先有 (d) 新行？chain 的 (b) 是否例外——矩阵缺行，但课已经把三闸讲完，Lead 应补行而不是再挖五问？

## Q5 元问题

- objective_id: OBJ-68
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 13 名仍 uncovered；(b) 无本课行；(d) 无发布走廊
- difficulty: low

建议：`defer`

原因：

- **两格与 OBJ-68 / chain / 课 claimed 对齐，主叙事站得住且与源码对齐。** L 合同章节齐全（宏观两把钥匙、直觉十句、定义表、机制链 A–E、带 caption / 文字等价物 / 图边界的三张图、正反例、变式、误区、暂停走查、来源表 S-004/S-008/S-010/S-015/S-L068-01…08）。能复现：13 扇映射、6/7 钥匙分配、已发布 vs 0+9、发布闸 BETWEEN/申请人/空列表跳过、导入 JSON 盖分类与空文件 NPE、删除只认 `flow_his_task`、xmlString 实为 JSON、无 `definitionXml` 窗、classic + `@ConditionalOnEnable`、`IFlwDefinitionService` ≠ `WorkflowService`。不必为「类名」再挖五问。
- **矩阵 (a) 该行可标 covered。** 13 名与磁盘 1:1。不要用本课盖厨房、九工厂、`WorkflowService`、任务五支或请假提交。
- **矩阵 (b) 缺行，不是课没讲。** chain/claimed 的三闸已在 `FlwDefinitionServiceImpl.publish|importJson|removeDef` 讲完。Lead 应把该符号补进 (b)（比照 L-070 已印的 `IFlwTaskService` 行）并可一并 covered；或把三闸记成 (a) 的 explained 副作用并在 chain 收窄。不要为此 `mine-more`，也不要发明发布 (d)。
- 本批可粘贴的精度项不挡住盖章：（1）模块无单测，证人是生产源码 + 前端契约/E2E；（2）厨房 freeze 没有导出方法、另锁一条没有后端窗的 `definitionXml`；（3）Jackson 3 解析失败不是那句「文件读取失败」；（4）估时 38 vs chain 35。不要把分类 CRUD、实例合同、任务枢纽、SpEL、请假、厨房方法表塞进本格。
- 不建议 `mine-more`：b01 未证明本两格还有真实矩阵空洞；再五问只会把 L-067 分类树、L-069 `WorkflowService`、L-070 办理链、L-073 厨房 URL 再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；缺的是矩阵 (b) 漏印，不是讲义容器。不建议 `split-lesson`：未满 10 问；13 扇 + 三闸是 chain 原设计的同一 OBJ-68、同一 `wta-workflow` 柜台，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (d)：三张 ASCII 图不是新走廊。不建议等 GP-L-073 才给本柜台盖章。不建议重盖 L-003 classic。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:FlwDefinitionController.* | 13 扇 URL/动词/权限/委托、6 包装 + 7 直通、两只列表抽屉、xml 假名、无 definitionXml 窗、条件装配、classic 钥匙——均与源码对齐。残留精度：无模块单测；厨房 freeze 无导出方法、另锁遗留 URL。 | 可选半句：口试方法表以 Controller 为准，不以厨房 freeze 或一个 e2e 为准。不要求重写机制链。Lead 可将矩阵该行标 covered（证据锚本 probe 与 L-068 来源表 S-L068-01…08）。 | defer（本行可 covered；精度项不挡盖章） |
| B:IFlwDefinitionService.publish,importJson,removeDef | 三闸已按磁盘讲完（BETWEEN 点名 / JSON 盖分类 / 只翻历史本；空列表跳过；空文件 NPE；占用中文 vs 引擎英文）。**矩阵 (b) 无此行**；chain/课头有。 | Lead 补矩阵 (b) 行（性状：办理人闸 / 分类章 / 历史占用）并标 covered；或把三闸收成 (a) 的 explained 并收窄 chain。不要为此再出五问。 | defer（三闸已讲；缺的是矩阵行不是课） |
| A:FlwCategoryController.* | 本课只消费子孙+自己 `IN` 与导入 category 字符串。 | 不要用本课盖分类七扇。L-067。 | defer（L-067） |
| A:FlwInstanceController.* / A:WorkflowService.* | 负空间：图纸柜台不启动、不按业务 id 删实例。 | 保持。L-069。 | defer（L-069） |
| A:FlwTaskController.* / B:IFlwTaskService.startWorkFlow,completeTask,backProcess,urgeTask,terminationTask | 办理人标识真正变成用户是任务柜台。本课闸只问写没写。 | 不要并进发布闸。L-070。 | defer（L-070） |
| A:FlwSpelController.* | `permissionFlag` 不是 SpEL。 | 保持。L-071。 | defer（L-071） |
| A:TestLeaveController.submitAndFlowStart / D:主路径 工作流请假 | 请假消费**已经发布**的定义。 | 负空间一句。不要用本课改 (d)。 | defer（L-072） |
| D:失败路径 工作流驳回/终止 | 本柜台无 backProcess / terminationTask。 | 保持挂 L-070。 | defer（L-070） |
| A:createWorkflowDefinitionService / A:createWorkflowWebDomain / 九工厂同行 | 厨房 URL、遗留 `definitionXml`、`runtime.download` 导出、iframe 设计器已作对照。 | 不要用本课盖厨房或九工厂。L-073。 | defer（L-073 / 九工厂） |
| C:layered-classic / wta-modules 业务房间 | classic + 引擎钥匙已借用。 | 保持。L-003 已 covered。本课不重盖。 | defer（L-003 已 covered） |
| C:MySQL 8.4 / dao-mapper-ownership / bundle-full-job | 表注释与菜单种子是 (a) 对照；模块只进 full。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-079 / L-085） |
| (d) 发布/导入/删除图纸走廊 | 矩阵无此行。三张 ASCII 图已写进 (a)/(b)。 | 不要为本课发明 (d)。 | defer（未入库） |
| Warm-Flow `DefService.publish` 如何把旧版打成 9 | 包装层无此 SQL；页面文案 + 引擎内部。 | 保持 deferred 引擎。不要升成第三张 (a)。 | defer（引擎内部） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本两格无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（矩阵 (b) 缺行是表漏印，课内已把三闸讲完）；合格读者说得出目的（墙上是菜谱不是在炒的菜）、主路径（13 扇两把钥匙；发布点 BETWEEN；导入 JSON 盖分类；删除翻历史本）与失败路径（非申请人中间节点空白办理人、缺申请人节点、空文件 NPE、历史占用中文、引擎失败英文/`FlowException`）；把可选半句写进原课不会超过 40 分钟；分类/实例/任务/SpEL/请假/厨房已有邻课。满 10 问前不拆。13 扇虽跨包装与引擎，仍是 chain 原设计的同一 OBJ-68、同一 `/workflow/definition` 容器，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / split 提案：否（miner 不改矩阵）；split 提案为 none；缺口表对 L-067 分类、L-069 实例/`WorkflowService`、L-070 任务与驳回 (d)、L-071 SpEL、L-072 请假主路径、L-073 厨房/九工厂、L-003 classic（已 covered）、MySQL/bundle 容器、未入库 (d)、引擎旧版失效内部显式建议 `defer`；对矩阵 (a) 行 `FlwDefinitionController` 13 名建议 Lead 标 covered（证据锚本 probe 与 L-068 来源表）；对 chain 声称的 (b) 三闸建议 Lead 补矩阵行并 covered，或收成 (a) 的 explained。本课不主张发明发布 (d)、重盖 classic、用本课标厨房/九工厂，或用 13 扇假闭合 `WorkflowService`。
