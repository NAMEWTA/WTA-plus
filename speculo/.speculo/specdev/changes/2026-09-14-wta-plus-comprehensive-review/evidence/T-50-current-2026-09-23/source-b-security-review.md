# T-50 final Source B security/contract review

Fixed Source A3 `0b33ff361820249f947a2767674356dabab59774` → Source B `50757f9b872b0b9b0131e4b2a4538bff7473f74d`, B tree `01927241641aceef0fb13ffea2a3f28a748bdaf7`. Read-only fixed-SHA review; no Maven, frontend build/test, Docker, HTTP, or repository edit was performed by this reviewer. Prior A1/A2/A3 production-security conclusions remain in `/tmp/wta-t50/source-a-security-review.md`, `source-a2-security-review.md`, and `source-a3-security-review.md`.

## Verdict at this review point

**Generated HTTP-contract/security delta: PASS. Delivery documentation: one actionable drift. Overall final acceptance: pending a clean B real rerun.** A3→B changes exactly four `frontend/packages/api-contracts` generated/snapshot files. Backend subtree object is identical at A3 and B: `507250fd94c75aa15a89506e2a4771aa3497469b`. Therefore A3 backend test and JAR/OpenAPI capture evidence may be reused **only for that byte-identical backend input**; it is incorrect to state that B itself ran A3's full/default Maven or built the JAR. Initial review occurred while B tests were running; the failed third real attempt is recorded separately below.

## Immutable source and generated output

- B's `openapi/current.json` points to new immutable revision `6dcd90b63abedf4d897c710e0b73709c7d202b18bc993b2ccc24a353d70fa1ce`. Its `source.json` is **byte-identical** to A3 owned full-JAR live `/v3/api-docs` at `/tmp/wta-t50/openapi-live/00926d62b78a715a/source.json`: 508,752 bytes, SHA-256 `2c12f7d5eeea5afc906577250a928f6d2d0e5e7bbd45a6e345dd6320bf32230c`. `provenance.json` records backend commit A3, OpenAPI 3.1.0, generator `openapi-typescript@7.13.0`, `/v3/api-docs`, 436 paths/445 schemas/77 tags and that exact raw SHA. `backendRepository="backend"` is the CLI default in `frontend/tooling/openapi/src/cli.mjs`, not an invented second source repository.
- A3 capture `/tmp/wta-t50/openapi-live/00926d62b78a715a/result.json` reports HTTP 200 JSON, exit 0, source before/after clean at A3 tree `f13b1dc`, JAR SHA-256 `eec4cc471f96a57ed94b2d8da604d56b86bcb88ee3c88a2709f78107d2876ebe`, and owned container/process/port cleanup errors `[]`. The captured `NotificationCommand` schema has `required=["priority"]`, while strategy/mode remain optional, with the A3 descriptions and three original enum values retained for historical reads. This is an actual runtime capture, not a Javadoc-only projection. The prior active snapshot had the same 436 path and 445 schema names; parsed JSON comparison shows only the `NotificationCommand` schema changed, with no missing path/schema key.
- `generated/openapi.ts:11042-11059` carries those descriptions and changes `priority?: number` to `priority: number`; `strategy?:` and `mode?:` remain optional with their enum types intact. No other contract type semantics changed; the `Skip` property-order movement in the generated file is ordering only. I found no handwritten frontend caller of generated `NotificationCommand` in fixed B; the type is exported to consumers, so frontend typecheck is still required. `git diff --check A3..B` exited 0.

## Documentation issue requiring disposition

`frontend/packages/api-contracts/README.md` currently states that the **current** snapshot comes from backend commit `7a6f75ac9238399daf7936797d07da141f0f5a03`. That matched the previous active revision's provenance, but B's current pointer and provenance now identify A3 `0b33ff3`. This is a concrete stale source-of-truth claim under engineering standard DOC-003 (public contract provenance documentation must follow boundary changes). It does not weaken the generated runtime contract or provenance hashes. The Ticket's pre-registered `frontend/packages/api-contracts/` directory write set already covers this README; B's four-file generator assignment was narrower than the Ticket write set. Lead has accepted a delivery-phase documentation-only fix: describe the current source by following `openapi/current.json` to that revision's `provenance.json`, avoiding another hard-coded commit. Preserve the exact clean B test/source records and, after the README edit, verify that all code/generated/build inputs remain byte-identical. This documentation correction alone does not require repeating the eight-class real tests and must not be represented as already present in fixed B. I did not change the repository.

## Evidence and limits

At clean A3, `/tmp/wta-t50/a3-backend-default-counts.json` reports 257 fresh classes, 1,067 total tests, 0 failures/errors, 197 explicit environment skips (870 executed passes); `a3-backend-default.json`, `a3-full-package.json`, and `a3-full-bundle.json` each report exit 0; source-before/after are clean A3 and package proof binds A3 source/tree. These tests and JAR are reusable for B solely because the backend tree is identical. A2 focused eight-class 135/0/0/0 evidence predates the A3 annotation and is not a substitute for B's last real HTTP run. B `/tmp/wta-t50/b1-openapi-{fetch,generate,check}.json` and `b1-frontend-{architecture,lint,typecheck,test,build}.json` all record exit 0; the frontend run's later `b1-source-after.json` is clean B. These are actual B frontend gate results, subject to Lead's own positive test-count/skip review. Final acceptance still needs a successful B required eight-class run (explicit priority 0/omitted primitive/enum defaults/permission/rollback) with its own clean source-before/after and owned cleanup. The target-environment legacy-mode disposition is a separate operational decision, not implied by this snapshot.

## B third real attempt: failed source boundary, bounded recovery

The actual B run `/tmp/wta-t50/runs/e5e3229eb2500be7/result.json` started clean at B SHA/tree, and its Maven command exited 0. I independently parsed the eight retained XML roots: **135 tests, 0 failures, 0 errors, 0 skips**. Nonetheless the driver returned **exit 1 / acceptance false** because `source_after_not_clean_exact_head` appears in cleanup errors and no clean `source_after` was recorded. The owned containers/ports/process group were otherwise cleaned. Do not call this a passing B eight-class acceptance, or fill in a later clean state as if it were the runner's actual source-after observation.

The interference has a bounded observed cause: B frontend `build:prod` ran from `2026-09-23T15:08:27.990121Z` until `15:09:52.776644Z`, spanning the backend runner's source-after check at about `15:09:43.665Z`. The frontend build itself exited 0; its later `/tmp/wta-t50/b1-source-after.json` and current worktree are clean B, but that later clean state does not repair the earlier failed exact-clean acceptance. After three attempts, the proposed recovery is sufficient and preserves the gate: keep the failed raw run and its XML, finish all frontend/core/static work, assert clean B SHA/tree with no concurrent repository-writing build, then rerun the same owned eight-class driver serially with unchanged zero-skip/source-before/source-after/cleanup checks. If this exact serial rerun fails again, investigate the new observed cause rather than waiving the source check. No product defect is established by this interference, and no test assertion is removed.
