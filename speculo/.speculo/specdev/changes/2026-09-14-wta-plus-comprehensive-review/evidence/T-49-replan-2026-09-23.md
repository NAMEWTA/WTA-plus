# T-49 evidence

Isolated MinIO `quay.io/minio/minio:RELEASE.2025-04-22T22-12-26Z` on `127.0.0.1:33101`. The container was removed after the run. Credentials were the test's existing integration defaults, passed as system properties and not copied here.

## Commands

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=org.namewta.test.oss.upload.OssUploadPropertiesUnitTest,org.namewta.test.oss.upload.OssUploadStorageRoutingMinioIntegrationTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Doss.minio.integration.endpoint=http://127.0.0.1:33101 \
  -Doss.minio.integration.access-key=namewta \
  -Doss.minio.integration.secret-key=<test default>
```

Exit 0. `OssUploadPropertiesUnitTest`: tests=4, failures=0, errors=0, skipped=0. `OssUploadStorageRoutingMinioIntegrationTest`: tests=1, failures=0, errors=0, skipped=0. Combined surefire: tests=5, skipped=0.

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=org.namewta.test.notify.AttachmentFreeSubmitDoesNotTouchOssTest \
  -Dsurefire.failIfNoSpecifiedTests=false
```

Exit 0. tests=1, failures=0, errors=0, skipped=0. `NotificationApplicationRuntimeService.submit` with an empty attachment list did not call the injected `OssService`.

## What the runs show

- Avatar's SINGLE policy no longer has multipart threshold or part size. A SINGLE policy with those values at 0 still validates, and a large file stays SINGLE. AUTO/MULTIPART still reject an invalid part size.
- The MinIO test uploaded a real multipart object to the private bucket, then switched the default storage key to the public route. The historical ticket stayed `private-route` and the private object was still readable at the same size. The new portal init failed closed with `STORAGE_ACCESS_POLICY_MISMATCH`. Before the switch, a wrong client, `text/html`, and an oversize file failed with `ACCESS_DENIED` or `INVALID_FILE`. No canary object was created.
- `namewta.sso` in `backend/wta-admin/src/main/resources/application.yml` still has `enabled`, `web-origin`, `web-base-path`, `cookie-name`, `cookie-secure`, `code-ttl`, and `session-ttl`. This edit does not delete those keys.
