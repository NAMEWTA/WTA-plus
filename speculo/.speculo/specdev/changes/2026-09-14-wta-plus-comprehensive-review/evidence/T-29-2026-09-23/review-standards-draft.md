# T-29 standards-axis source review — fixed 12-path candidate

**Axis result: pass for the 12 source paths, with no source must-fix identified.** This is not a T-29 acceptance verdict: the private directory migration and actual skill-facts gate are still pending with the Lead. This review is independent of any other review axis.

- Fixed base `7a1810288d3292ceeb4987f488b13353af1a1286`, fixed head `9f055ba15d9d5a828fb08cdfb0b24efa32642889`; merge base equals base. `git diff base...head` has one source commit, `9f055ba fix(docs): align private release paths and current skill facts`. All 12 and only the 12 product paths named by `/tmp/t29-extra-paths.json`/`evidence/dispatch-T-29.md` appear under `.agents/skills` in that diff. The same commit carries T29/T01 governance ledger changes, which are outside this source-path review.
- Applicable standards: root `AGENTS.md`; `.agents/skills/engineering-standards/SKILL.md` and `references/rules/documentation-and-comments.md` DOC-003, `security-and-data.md` SEC-003/005, `quality-gates.md` QUALITY-001/003; changed deploy/customization Skills' own private-report, backup, 0600 and path rules; code-review skill `source-discovery`, `fowler-smells`, `risk-review`, `reviewer-contracts`. Authoritative task scope: T29 Ticket, AC-029 and the dispatch packet.

## Findings

No must-fix finding within the fixed 12-path source diff. No unrelated rule weakening or change outside the declared product set was found.

## Checked invariants and concrete evidence

1. **Hard-rule preservation.** Each of the ten changed Markdown files is byte-for-byte its base version after only the intended substitution `temp/relase` → `temp/release`, plus the single `release-state.py` → actual `release-state.mjs` fact in `00-project-profile.md:71` or the current `SmsDeliveryQueryClientTest` test-root row in `01-module-map.md:45`. This exact-transform comparison means 0600, ignored directory, single latest report, secret non-disclosure, backup, ownership, waiver, transaction and rollback MUST paragraphs were not silently removed. Root `.gitignore:74` ignores `temp/`; the actual `release-artifacts/scripts/release-state.mjs` and SMS test file exist. The customization Skill still honors explicit `report_root` override and says the target project's private directory must be ignored (`project-customization-delivery/references/inputs.md:14`, `verification.md:49`).
2. **Checker scope and failure behavior.** `validate-skill-facts.mjs:27-48` already builds `all` exclusively from Markdown below `.agents/skills`; the new obsolete-path check applies to that complete Skill Markdown corpus instead of the former canonical subset that excluded deployment. It does not traverse or read `temp/` private reports, and its error contains only the public obsolete path literal. The existing ignore, tracked-file, output-owner and old-directory checks remain at lines 61-69. Two new independent synthetic fixtures inject the obsolete path into the deployment Skill and customization Skill and assert exit 1 plus the error (`validate-skill-facts.test.mjs:59-76`); the prior old-directory and missing-ignore tests remain. No skip/disable/exclusion was introduced. Static review of the Promise-free scanner/fixture paths finds the negative tests capable of failing the old scanner for the intended reason; the implementing agent's and Lead's actual execution records remain authoritative for test counts/exit status.
3. **Current facts and navigation.** The profile names the real `.mjs` file; the module map now declares the extant SMS test root. The ten changed Markdown files have 14 local relative inline links, all resolving in the current checkout. All remaining old-spelling hits in `.agents/skills` are checker/test literals, not Skill Markdown. The 12-path diff passes `git diff --check` at the fixed base/head.
4. **Existing 37-manual contract.** A separate safe current read-only audit at fixed head is `/tmp/wta-t29-current-audit-v2.py` → `/tmp/wta-t29-current-audit-v2.json` (both 0600). It preserves historical source/evidence inputs; its only output is in `/tmp`, and it does not read `temp/`. It returned exit 0: 37 removed manuals still absent with historical original SHA and rules/targets intact, 28 special rules retained, 147 captured Markdown links/anchors valid, 48 current module rows matching child POMs/test-root facts, 0 removed-manual references. It reports `historical_owner` and actual nearest `effective_owner` separately from `rule_destination`; exactly one known Profile BOM owner delta is explicit instead of being silently treated as identical. For the historical 855-file protection snapshot, the script permits only root `AGENTS.md` after comparing the historical blob at `70eed51...` to its protected SHA and proving every nonblank byte line unchanged; the observed later root change is blank-line-only. It does not overwrite or rebaseline old evidence. See `/tmp/wta-t29-verification-audit.md` for T29-wide historical/current boundaries.

## Required T-29 completion work, outside this source-axis verdict

- The Lead still must complete and verify the private backup, isolated restore test and no-replace same-parent migration of the old private directory, including permissions/hashes and any precise private path references, before reporting `validate-skill-facts.mjs` as passed on the real repository. The reviewer did not inspect private files or run that gate; the old directory may still make it exit 1 by design.
- The Lead must record current exact-HEAD command/cwd/exit and counts for applicable Skill/handbook/facts/release checks and the full AC-029 document/current-link inventory. Historical T29 checkpoint hashes and this static review cannot substitute for those runs, a clean direct-parent record, or remote CI results. The project profile correctly calls the workflow a local/CI candidate, not a proven required remote gate.

Read-only review only: no repository edits, tests, heavy gates, builds, services or private-data reads were performed. `/tmp` safe audit writes are separate from the repository.
