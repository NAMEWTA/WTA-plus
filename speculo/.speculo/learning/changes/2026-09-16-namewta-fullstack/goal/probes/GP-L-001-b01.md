---
artifact: learning-goal-probe
probe_id: GP-L-001-b01
lesson_id: L-001
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-001 / b01

审问对象：已写成的 `children/2026-09-14-namewta-architecture/lessons/L-001-repo-map.md` 对当前工作树 `/srv/WTA-plus`。不是学习者。本文件不含 `Q-question 问答目录` 协议字段。

声称覆盖格子（chain / 矩阵 (c)）：

- `NAMEWTA 系统 Context`（C4 人 / 机器 / 外部渠道）
- `repo-ownership frontend/backend/docs/release-artifacts/skills`

OBJ-01 可观察行为：指五个房间（`frontend/`、`backend/`、`docs/`、`release-artifacts/`、`.agents/skills/`）并说明默认交付不是 git submodule。Context 是同一课的 deep 升级，不是另开 OBJ。

## Q1 澄清

- objective_id: OBJ-01
- bloom_level: understand
- socratic_move: clarification
- expected_evidence: Lesson 开篇覆盖声明、所有权表、Context ASCII、OBJ-01 原文、矩阵两行备注
- difficulty: medium

这节课声称覆盖的范围内单元究竟是哪几个？有没有把别的格子的名字借来当本课证据？

**审问结论：** 声称单元就是上述两格，没有第三格。OBJ-01 只要求五房间 + 非 submodule；Lesson 正文把 C4 Context 画进同一课，并写明 Container 只点名、不拆内部。这与 chain「L-001 → C:context, C:repo-ownership」一致。

借名但不声称覆盖的符号（本课当门铃/柜子标签，不是把那些格子标成已讲完）：

| 借来的名字 | 源码锚点 | 真正的矩阵格子 |
| --- | --- | --- |
| `admin-web` / `home-web` / `sso-web` 包名 | `frontend/apps/*/package.json` 的 `@namewta/{admin,home,sso}-web` | Container `admin-web` / `home-web` / `sso-web`（L-004） |
| `NamewtaApplication` / `org.namewta` | `backend/wta-admin/src/main/java/org/namewta/NamewtaApplication.java` | Container `wta-admin`（L-002） |
| `10-cde-base-ddl.sql` / `50-cde-base-dml.sql` | `release-artifacts/docker/infrastructure/mysql/init/` 六份脚本 | Container `MySQL 8.4 基座`、Component `mysql-base`（L-085） |
| Redis 8.6.3 / MinIO 服务 | `docker-compose-infrastructure.yml` `services.redis` / `minio` | Container `Redis 8` / `MinIO/OSS` |
| `SsoSessionController` `/sso`、`SsoOAuthController` `/sso/oauth2` | `wta-sso/.../controller/anonymous/` | 函数轴 SSO（L-055 起） |
| `OpenApiGatewayFilter` + `NAMEWTA-HMAC-SHA256` | `OpenApiCanonicalizer.ALGORITHM`；Filter 类注释 “machine requests” | 函数轴 OpenAPI |
| `ProviderCallbackController` `POST /notify/callback/{channel}` | `@RequestMapping("/notify/callback")` + `@PostMapping("/{channel}")` + `X-Notify-Signature` | 函数轴 `ProviderCallbackController.callback` |

定义边界清楚：单一仓 = 一 Git 里同时有 `frontend/` 与 `backend/`，不是「一个可部署进程」；所有权 = 目录写入责任；C4 Context = 谁碰到系统，不是仓库树。两张图互相不可替代——Lesson 误区 5 已写。澄清槽没有把两格拧成一格，也没有漏掉 OBJ-01 的五个房间。

## Q2 证据

- objective_id: OBJ-01
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: 2026-09-16 工作树 `ls`、无 `.gitmodules`、README / DEC-004 / ARCH-001 / 控制器与 Compose 原文
- difficulty: medium

源码和磁盘上哪些函数、容器或数据流真正被这节课讲到，而不是只被点名？

**审问结论：** 所有权与 Context 边有工作树证据；函数体/失败路径没有被讲到（也不该被这课讲到）。

顶层对照（2026-09-16 `find /srv/WTA-plus -maxdepth 1`，不含 `.git` 历史）：

| 磁盘 | Lesson 是否记录 | 核对 |
| --- | --- | --- |
| `frontend/` `backend/` `docs/` `release-artifacts/` `.agents/` `scripts/` `speculo/` | 是 | 五主房间 + 相邻房间与根 README 树一致 |
| `AGENTS.md` `CLAUDE.md` `README.md` `.gitignore` | 是 | 存在 |
| `.gitmodules` | 明确「不存在」 | `ls`：No such file |
| `frontend/.git` / `backend/.git` | 「没有各自的 `.git`」 | 均不存在 |
| `.claude/` / `.codex/` | 「本仓不维护副本」 | 均不存在 |

合入 / 非 submodule 的三重权威与过期门牌：

- 根 `README.md` 开篇：「单一 monorepo」；「交付不以 git submodule 或上游 URL 为依赖」；树注释「合入，非 submodule」。
- `DEC-004`：`main` 承载产品；不以 git submodule 为默认交付。
- `engineering-standards/SKILL.md` 约束：「前后端合入本 monorepo 的 `backend/` 与 `frontend/`」。
- `ARCH-001` **现在仍写** submodule MUST，Source 还写着 `.gitmodules`。Lesson 把这标成 C-001 过期文本，裁决顺序「磁盘 → 根 README → DEC-004 / 规范入口 → 过期规则只当冲突」与 AGENTS.md / 工程规范入口一致。
- `scripts/README.md`：`ci/verify-submodules.sh` 是聚合仓遗留校验，不是克隆步骤。磁盘上脚本仍在。

Context 边（人 / 机器 / 渠道 / 柜子）对得上入口，未走进实现：

- 三个浏览器门：`frontend/apps/{admin-web,home-web,sso-web}/package.json` 均有 `name: @namewta/*-web` 与 `"build"` 脚本。根 `frontend/package.json` 的 `build:dev` 显式 filter admin-web 与 home-web。
- SSO 后端：`SsoSessionController` `@RequestMapping("/sso")`；`SsoOAuthController` `@RequestMapping("/sso/oauth2")`；`application.yml` `security.excludes` 含 `/sso/**` 与 `/sso/oauth2/**`。
- 机器门：`OpenApiCanonicalizer.ALGORITHM = "NAMEWTA-HMAC-SHA256"`；`OpenApiGatewayFilter` 是验签 Servlet 边界。
- 渠道门：`ProviderCallbackController` 类注释「验签后只更新统一投递状态」；`POST /{channel}` + `X-Notify-Signature`。
- 柜子：Compose 默认服务 `mysql:8.4.9`、`redis:8.6.3`、`minio`。`nacos` 带 `profiles: [nacos]`，与 Lesson「不断言 Nacos 一定开着」一致。自有表基座文件名与 `00-project-profile.md`「排除与冻结」一致：自有表只进 `10` / `50`；磁盘六份脚本齐全。

未讲到、也不属于这两格的机制：HMAC 规范化行、Outbox、OAuth PKCE、OSS 直传状态机、生产拓扑。Lesson 图边界已标明。数据流轴的主/失败路径不在本课声称里。

## Q3 假设与反例

- objective_id: OBJ-01
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: ARCH-001 现行文本、项目画像 L14、apps/README、enhancements 发布面、`backend/wta-extend/`、Compose `profiles`
- difficulty: hard

合格读者读完 Lesson + 源码后，对这两格仍会说不出什么？哪些假设如果反转，地图会裂？

**审问结论：** 两格的可观察行为说得出。剩下的说不出项要么是后续课，要么是 Lesson 已标未关闭冲突，要么是磁盘多出来的范围外房间。

仍说得出：

- 五个房间主人与「不拥有」列（Vue 不进 backend static、Skill 不进 fat jar、自有表不进 `20`/`30`/`40`/`60`）。
- 一次 clone 看见前后端，不需要 `git submodule update`；反驳材料三件套（README、DEC-004、无 `.gitmodules`）在源码里找得到。
- Context 三类门铃 + 三个柜子；箭头是「谁碰到系统」不是「编译成一个文件」。

假设若反转：

1. **若仍信 ARCH-001：** 读者会去建 `.gitmodules`。工作树反例已在：根无该文件，前后端非嵌套 Git。Lesson 反例 2 / 变式 D 覆盖。
2. **若把「合入」当成「frontend import Java」：** Lesson 边界与误区 6 覆盖；根 README 也写独立构建、只经 HTTP/JSON。
3. **若把三个 App 的存在当成产品发布面：** 这是 C-003，Lesson 未关闭。源码侧发布面仍写两 App：`frontend/apps/README.md`（admin-web + home-web）、`docs/namewta-enhancements.md`「当前发布面包含 admin-web 和 home-web」、`release-artifacts/README.md`「只发布 admin-web 与 home-web」。根 README 把 `sso-web` 标「已激活」。OBJ-01 不要求关闭发布面；本课把「源码/build 脚本存在」当事实是对的。
4. **若信画像「唯一可构建 App 是 admin-web」：** `.agents/skills/engineering-standards/references/project/00-project-profile.md` L14 **现在仍写**这句话（「其他未激活终端仅保留 README 占位」）。Lesson 写成「曾写」，时态不准，但冲突本身（C-002）已记录，且三个 `package.json` 均带 `build`。这不推翻 repo-ownership 格子。
5. **`ls backend/` 会看到 `wta-extend/`：** 所有权表只列 `wta-admin` / `wta-api` / `wta-common-*` / `wta-modules`。磁盘上还有 `wta-extend/`（`wta-monitor-admin`、`wta-snailai-server`、`wta-snailjob-server`）。Goal 范围把独立进程划出。合格读者可能把这当成第六个后端房间。这是所有权表的边角，不是「指不出五个根房间」。
6. **Compose 还有 `elasticsearch` `profiles: [ai]`：** Context 图没画。与 Nacos 一样是可选 profile，不是默认柜子。Lesson 对 Nacos 写了边界，对 ES 沉默。不足以让 Context 格说不出人/机器/渠道。

反例课内五条都能在源码复现。没有「读完仍指不出五个房间或画不出人/机器/渠道」的矩阵级空洞。

## Q4 视角

- objective_id: OBJ-01
- bloom_level: analyze
- socratic_move: viewpoints
- expected_evidence: C4 惯例（出站外部系统）、`release-artifacts/skills/`、四类 Compose、社交登录入口、wta-third
- difficulty: medium

换一个观察者，架构边或缺哪一条？缺的那条是否属于这两格？

**审问结论：** 缺的是别的观察者会多画的边，不是这两格的必选边。

| 视角 | 多出来的边 | 是否本课格子 |
| --- | --- | --- |
| C4 出站 | 社交 IdP（README / 增强说明的社交登录；`AuthController` social 入口属 L-011/L-022）；`wta-third` 出站 HTTP（L-060 起） | 否。本格备注是「人/机器/外部渠道」。渠道类已用短信/邮件回调建立；出站是后续数据流 |
| 发布运维 | 四类 Compose（infrastructure / backend / frontend / observability）；`release-artifacts/skills/wta-namewta-nginx-config/` 是 Nginx 配套 Skill，不是 `.agents/skills/` 工程规范根 | 否。容易把两个 `skills/` 认成同一个主人；一行就能消歧，不另开格 |
| 磁盘 `ls backend/` | `wta-extend` 独立进程房间 | 否。Goal 明确范围外 |
| 可选基础设施 | Nacos profile、ES `profiles: [ai]` | 否。Nacos 已作「不要断言开着」；ES 同构可选 |
| 数据流轴 | 登录主路径、会话丢失、OSS 直传 | 否。矩阵 (d) 不是 L-001 |

仓库树视角与 Context 视角 Lesson 已经拆成两张图，并写了类比失效处。缺边不构成「一课声称覆盖超过一个容器却讲不清」——Container 格子本就留给 L-002/L-004/L-085。

## Q5 元问题

- objective_id: OBJ-01
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: split-rules 操作定义、mine-more 前置（b01 须先证明真实矩阵缺口）、两格与源码对照
- difficulty: medium

建议处置：`defer`

原因：

- 本课声称的两格，Lesson 合同章节齐全（直觉、精确定义、机制链、带 caption / 文字等价物 / 图边界的两张 ASCII、正反例、变式、误区、来源表），工作树与入口源码支持 OBJ-01 与 Relational Context。
- `mine-more` 只允许在 b01 已证明**真实矩阵缺口**时开 b02。本批没有这种缺口。把社交 IdP、third 出站、`wta-extend`、可选 ES 再问五题，只会挖进别的 chain 节点。
- `re-dispatch-L` 适用于缺动机/机制/边界/例子。本课不缺。时态「曾写」、`wta-extend` 点名、两个 `skills/` 消歧都是可选一行补丁，不挡住把格子标 covered。
- `split-lesson` 的操作定义未触发：不是满 10 问仍不清；一课两格是 chain 原设计（Context + repo-ownership Component），不是「超过 1 个容器却讲不完」；补丁不会把 `time_budget` 顶过 40 分钟。`split_depth: 0`，本批禁止填 split 表。

残余项诚实标 defer，交给后续课或范围外，而不是为本课加问。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| NAMEWTA 系统 Context | 无覆盖缺口。人（管理操作者 / 门户用户 / SSO 认人应用）机器（OpenAPI HMAC）渠道（`POST /notify/callback/{channel}`）柜子（MySQL 8.4 / Redis 8 / MinIO/OSS）均有源码边。未画社交 IdP、third 出站、可选 ES，属后续课或可选 profile。 | 不必为覆盖改图。若补丁：图边界加一句「社交登录与 third 出站不在本课 Context；ES 与 Nacos 一样是 Compose profile」。 | defer |
| repo-ownership frontend/backend/docs/release-artifacts/skills | 无覆盖缺口。五房间在根目录存在；无 `.gitmodules`；前后端非嵌套 Git；DEC-004 / 根 README / 规范入口支持合入。边角：`backend/wta-extend/` 未点名；`frontend/packages/` 实际还有 `adapters` / `api-contracts` / `web-kit`；画像 L14 仍写唯一可构建 App；`release-artifacts/skills/` 与 `.agents/skills/` 同名不同主人。 | 可选一行：`wta-extend` 在磁盘上但是独立进程、本 Goal 范围外；packages 全名交给 L-004；C-002 改为「画像仍写」；发布配套 Skill ≠ 工程规范根。 | defer |

Miner 只填表。上表「处置建议」不是 Lead 裁决。

## split-proposal

none

（b01 未满足「10 问仍解释不清」的操作定义：未满 10 问；声称格子无 uncovered 空洞需要靠 b02 的 Q10 继续 `mine-more`；合格读者说得出五房间与 Context 门铃；补丁不突破 40 分钟。）

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：是。Lesson + 源码已覆盖声称的两格，Lead 可将 `(c) NAMEWTA 系统 Context` 与 `(c) repo-ownership frontend/backend/docs/release-artifacts/skills` 从 `uncovered` 改为 `covered`（证据锚本 probe 与 L-001 来源表）。残余出站系统 / `wta-extend` / 可选 ES / 两个 `skills/` 消歧显式 `defer`。`split-proposal` 为 none。本课不主张改 MySQL/Redis/MinIO/三 App Container 或任何 (a)(d) 格子。
