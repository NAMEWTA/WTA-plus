---
artifact: learning-goal-probe
probe_id: GP-L-045-b01
lesson_id: L-045
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-045 / b01

审问对象是已写成的 Lesson `children/2026-09-14-wta-notify/lessons/L-001-notify-notice.md`（chain 课号 **L-045**，不是子课号 L-001）与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、verdict、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain / 矩阵 (a) **两行**）：`A:NotifyNoticeController.*`、`A:NotifyNoticeUseCase.*`。派单与矩阵符号把六个 Java 名写全：`list, get, save, publish, retract, remove`。矩阵 UseCase 行原文「与 Impl 同切片」——磁盘 **没有** `NotifyNoticeUseCaseImpl`，管家是具体 `@Service` 类。OBJ-45 原文：能口述 `/notify/notice` **列表/详情/保存/发布/撤回/删除的五层链**。档是**解释+走查**。chain 覆盖列只写这两格 (a)。矩阵 (a) 两行模块都是 `wta-notify`，均仍 `uncovered`。矩阵 (b) **有**独行 `NotifyNoticeController.publish/retract`（口号「公告状态机」），仍 `uncovered`；chain **未**把 (b) 写进覆盖列。矩阵 (c) `wta-modules` 业务房间 / `layered-classic` 已 covered（L-002/L-003；L-003 曾用本链当 layered 正例）。矩阵 (d) 「主路径 通知提交」=`NotificationController.submit → Outbox → claim → 渠道`，备注 L-051/L-052，仍 uncovered；**没有**「公告发布」独行。本批按派单审问 **两格 (a)**；口试按磁盘六扇 HTTP + 管家两字段。收件箱已见/已读留给 L-046；配置账号/试发留给 L-047；收件人目录留给 L-048；监控 snapshot 留给 L-049；供应商门铃留给 L-050；统一 submit/query/retry/cancel 与 ALL 分页拉人留给 L-051；Outbox 领取留给 L-052；厨房 `createNotificationService.notices.*` 留给 L-053；菜单四键留给 L-054。不要把 L-003 的登记表格子再盖第二次章。

抽查锚点（2026-09-17）：`backend/wta-modules/wta-notify/src/main/java/org/namewta/notify/controller/admin/NotifyNoticeController.java`；`usecase/NotifyNoticeUseCase.java`（无 `usecase/impl`）；`service/{NotifyNoticeService,NotifyNoticePublisherService}.java`；`domain/policy/NoticeAudiencePolicy.java`；`dao/NotifyPersistenceDao.java`；`mapper/NotifyNoticeMapper.java` + `resources/mapper/notify/NotifyNoticeMapper.xml`；`domain/{entity/{NotifyNotice,NotifyNoticeSnapshot},bo/NotifyNoticeBo,vo/NotifyNoticeVo}.java`；`wta-api/.../notify/api/{NotificationApplicationService,NotificationCommand}.java`；测试 `usecase/NotifyNoticeUseCaseTest.java`、`service/{NotifyNoticeServiceTest,NotifyNoticePublisherTest}.java`、`domain/policy/NoticeAudiencePolicyTest.java`（**没有** `NotifyNoticeController*Test` / ArchitectureTest）；DDL `10-cde-base-ddl.sql` `notify_notice*`；DML `50-cde-base-dml.sql` 菜单七串 + 两条种子公告；`.agents/skills/engineering-standards/references/{notification.md,project/03-backend-module-modes.md}`；厨房旁证 `frontend/packages/domains/notify/src/{transport.ts,transport.test.ts,notice/index.ts}`（格子归 L-053）；`frontend/packages/api-contracts/generated/openapi.ts` `/notify/notice/**`。

## Q1 澄清

- objective_id: OBJ-45
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-45；chain L-045 覆盖列两格 (a)；子课 L-001 无 `claimed_cells` 字段、课号仍是 L-001；矩阵 (a) 六名 + UseCase 行仍 uncovered，UseCase 行写「Impl」但磁盘无 Impl；(b) 有 publish/retract 独行；(c) layered 已 covered；(d) 通知提交走廊是 L-051/L-052；课开篇六件事 + 负空间 L-046…L-054 / L-003
- difficulty: medium

课把公告柜台收成「一块门牌、六扇窗、五层楼梯、发信发生在另一只下游」。计划里至少有六套可数对象，派单点名的是 **两格 (a)**：

1. **OBJ-45 原文。** `course.md`：能口述 `/notify/notice` 列表/详情/保存/发布/撤回/删除的五层链。六个动词是口试主题，没有把 `NotifyNoticeSnapshot`、幂等键、`NotificationApplicationService.submit` 写成第三张矩阵行。子课 OBJ-01 同句，课号不要写成 L-001。
2. **chain 覆盖列。** 两格 (a)：`NotifyNoticeController.*` 与 `NotifyNoticeUseCase.*`。不写 Service/Publisher/Policy/DAO 符号，不写 (b)，不写 (d)。
3. **矩阵 (a) 两行。** Controller 行六名与磁盘公开映射 1:1。UseCase 行把「Impl」绑进备注；磁盘管家是具体类，profile/sso/third 才是接口+`usecase/impl`。Lead 若按行标 covered，**不会**把 `NotifyInboxController` 或 `NotificationController.submit` 假闭合。
4. **矩阵 (b) 已有独行。** `NotifyNoticeController.publish/retract` = 公告状态机。chain 没把它写进覆盖列，但本课机制链 4–6 与正例 3 / 反例 3 / 变式 A **就是**这台机器（已发布 `return null` 跳过 Publisher；撤回只改 lifecycle，不 `cancel`）。不要等 L-051 才承认「连点两次不二次投递」。也不要为 (b) 另开一课才给两格 (a) 盖章。
5. **课自己的操作定义。** 门牌 `/notify/notice`；GET 二 / POST 四；save 按 `noticeId==null` 在方法体内切 `add`/`edit`，其余五扇注解权限；写路径 UseCase `@DSTransactional`，读路径直接转发；Controller 唯一业务字段 `NotifyNoticeUseCase`；下游两字段 `NotifyNoticeService`（抽屉+锁）与 `NotifyNoticePublisherService`（快照+`submit`）；可编辑集合只有 `DRAFT`/`RETRACTED`。规范句「UseCase 负责目标解析」以 Java 为准：解析在 Policy + 两个 Service。
6. **邻格 / 未入库。** L-046 收件箱读的是 `NotifyMessage` 不是 `notify_notice`。L-047 MAIL/SMS 正文权威。L-048 目录搜索。L-051 `submit` 与 ALL 分页 `selectAllActiveUsers`。L-052 Outbox。L-053 厨房六枪。L-003 已用本 list 链当 layered 正例，本课不得再盖 `C:layered-classic`。(d) 通知提交走廊不是本两行。

问：本两格闭合标准是「六扇路径/动词/七串权限/save 体内切 add·edit + 管家两字段 + 写路径盖事务章 + 已发布 `null` 跳过 Publisher + 撤回不 cancel + 空 USER 不退化成 ALL，且不把 Service/XML 升成第三张 (a)」，还是 OBJ 那句必须把五层每一个类都立成矩阵行、或必须先把 (b) 拆成第二课 / 等 GP-L-051 `submit` 才给 publish 盖章？Lead 若凭「`NotifyNoticeUseCase` 四字含 Impl 备注」去找 `UseCaseImpl` 文件，是否把本行假判成未讲完？Lead 若用本课把 L-003 或 L-051 标 covered，是否假闭合了登记表与统一提交走廊？

## Q2 证据

- objective_id: OBJ-45
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 23–69；UseCase 13–34；UseCaseTest 29–55；Service 21–134；Publisher 23–66；Policy 9–39；PolicyTest 17–46；DAO 17–42；Mapper.java 12–16；Mapper.xml 4–6；Bo 13–32；Entity 15–30；Snapshot 14–25；Command 27–32；ServiceTest 16–23；PublisherTest 26–63；notification.md 31–35；登记表 layered 行；DDL 1412–1449；DML 1532 / 1547–1565 / 1621–1632；RuntimeService `selectAllActiveUsers`（邻格）；transport.ts 27–37；openapi.ts 2748–2821 / 5651–5682
- difficulty: high

课的六扇 + 管家哪些能在工作树直接复现，哪些只是测试名单比课表窄、或把统一提交讲成了另一扇门？

**仍能复现（两格 (a)）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 类注解 `@Validated @RestController @RequiredArgsConstructor @RequestMapping("/notify/notice")`；**有** `extends BaseController`；映射全是 Get/Post，无 PUT/DELETE | Controller 23–27；openapi `put?: never` / `delete?: never` |
| 公开 HTTP **正好六个**，与矩阵六名 1:1 | Controller 31–69 |
| GET 两扇：`/list`=`notify:notice:list`；`/{noticeId}`=`notify:notice:query`；无 `@Log` | 31–40 |
| POST 四扇：save 无方法级 `@SaCheckPermission`，体内 `StpUtil.checkPermission(noticeId==null ? add : edit)`；publish=`publish`；retract=`retract`；remove=`remove`。save/publish/retract=`BusinessType.UPDATE`，remove=`DELETE` | 43–69 |
| Controller 唯一业务字段 `NotifyNoticeUseCase noticeUseCase`；不 import `service` 包 | 28；import 15 |
| list 把 `PageQuery` 拆成两个整数再进管家 | Controller 33–34；UseCase 19–20 |
| 管家具体类 `@Service`，字段只有 `NotifyNoticeService` + `NotifyNoticePublisherService`；**无**接口、**无** `usecase/impl` | UseCase 13–17；全仓 grep 无 Impl |
| 读路径 `list`/`get` **无** `@DSTransactional`；写路径 `save`/`publish`/`retract`/`remove` 有 | UseCase 19–33 |
| `save`：`noticeId==null` → `add`（lifecycle=`DRAFT`，status=`1`）；否则 `update`（锁行、`requireEditable`、保留原 lifecycle/status/publishedAt/retractedAt） | UseCase 23–24；Service 56–71 |
| `publish`：`noticeService.publish` 返回 `null` 则 **不再** `publisher.publish`；非 null 才复印 | UseCase 25–29；UseCaseTest 29–46 |
| Controller `publish` 返回类型 `void` 之后 **一律** `R.ok()`（含二次连点） | Controller 54–56 |
| `retract`/`remove`/`save` 走 `toAjax(int)`（`rows>0` 才 ok） | Controller 47 / 63 / 69；BaseController 19–20 |
| Service `publish`：`findForUpdate`；已 `PUBLISHED` → `null`；否则要求可编辑，写成 `PUBLISHED`、status=`"0"`、填 `publishedAt`、`retractedAt=null` | Service 100–110；Entity 28–29 `FieldStrategy.ALWAYS` |
| Service `retract`：已 `RETRACTED` 原样返回（仍会让 UseCase 计 1）；非 `PUBLISHED` 抛「仅已发布通知允许撤回」；成功只改 lifecycle + `retractedAt`，**不改 status、不调 cancel** | Service 113–120；UseCase 30–31 |
| `requireEditable` 只认 `DRAFT`/`RETRACTED`；已发布未撤回删/改/再发都撞「仅草稿或已撤回通知允许编辑、发布或删除」 | Service 130–133 |
| `delete`：ID 必须为正；`distinct().sorted()` 后逐个锁+可编辑；已发布未撤回不能删 | Service 92–97 |
| `get` 找不到 → `ServiceException("通知不存在")`；`toVo` 渠道缺省 `IN_APP`，`recipientType` 缺省 `ALL` | Service 30–45 |
| `toEntity` 先 `NoticeAudiencePolicy.normalize`；`USER`/`USER_TYPE` 在**草稿阶段**就调 `selectNotificationUsers` / `selectUsersByUserTypeIds` | Service 74–78 |
| Policy：`ALL` 两边空；`USER` 必须有正数用户 ID 且类型空；`USER_TYPE` 相反；空指定**不会**退化成全体；渠道只 `IN_APP`/`SMS`/`MAIL`，空/未知/`PUSH` 拒绝；null type→`ALL`，null channels→`IN_APP` | Policy 15–32；PolicyTest 17–45 |
| Publisher 再 normalize：`ALL` 交空列表且类型保持 `ALL`；`USER`/`USER_TYPE` 解析后提交类型压成 `USER`；空用户在 `insertSnapshot` **之前**抛「所选范围没有可接收通知的正常用户」 | Publisher 28–40；PublisherTest 26–36 |
| 快照 `latestSnapshot` 版本 +1（无则 1）；`pathSnapshot=/notify/notice?noticeId=`；然后 `notificationService.submit` | Publisher 42–65 |
| 命令：`appId=notify`，`sceneCode=templateCode=notice-published`，`bizType=NOTICE_PUBLISHED`，`ASYNC` + `Strategy.ALL` + priority `50`，幂等键 `notice-published:{noticeId}:{version}` | Publisher 61–65；Command 27–32 |
| DAO 分页：标题 like、类型/status 精确、**不**按 lifecycle 筛；`orderByDesc createTime, noticeId`；内部 `new PageQuery(pageSize, pageNum)` | DAO 22–28 |
| 锁行 XML：`select * from notify_notice where notice_id = #{noticeId} for update` | Mapper.xml 4–6 |
| 菜单七串 list/add/edit/publish/retract/remove/query 与 DML 210060…0030–0036 一致 | DML 1532 / 1547–1551 / 1565 |
| 种子两行已是 `ALL` + `["IN_APP"]` + `lifecycle=PUBLISHED` + `status=0`，并补 snapshot；不插 Intent | DML 1621–1632 |
| 厨房六枪 URL 与 Java 门牌同形；`save` 三类目标同一枪、`publish` 独立 POST | transport.ts 27–37；transport.test.ts 5–20；旁证，格子 L-053 |

**邻格仍能复现（不计入本两行闭合）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| ALL 全体分页在 `NotificationApplicationRuntimeService.selectAllActiveUsers`，**不是** Publisher | RuntimeService 276；L-051 |
| 应用 `cancel` 在 `NotificationController` / `NotificationApplicationUseCase`，撤回链零引用 | NotificationController 55–59；NotifyNotice* 无 `cancel`；L-051 |
| 收件箱 HTTP 是 `/notify/inbox` | transport.ts 39–43；L-046 |
| 厨房工厂 `createNotificationService` | L-053 |
| layered 正例 list 链已被 L-003 用过 | GP-L-003-b01 Q2；本课只借用，不重盖 `C:layered-classic` |

**课写过、测试名单更窄或口试数字写偏（不是证伪）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「写操作加 `@DSTransactional`」 | 源码 publish 也有。UseCaseTest 49–54 循环只断言 `save`/`retract`/`remove`，**漏了 `publish`** |
| 「已是 `RETRACTED` 则原样返回（UseCase 计 1 行）」 | Service 从不返回 `null`。UseCase `retract(id)==null ? 0 : 1` 的 0 分支是死代码；`toAjax(0)` 这条失败路走不到 |
| 「撤回只改公告 lifecycle」 | 成立，且 **status 仍停在发布时的 `"0"`**。列表筛的是 status 列，不是 lifecycle。课没写成半句「撤回后 status 不拨回 `1`」 |
| 「规范：UseCase 负责目标解析」 | 课已标 Java 优先。解析在 Policy + Service/Publisher；管家只判 `notice != null` |
| 「同一事务写意图、接收者快照、Outbox」 | 公告内容快照是 `NotifyNoticeSnapshot`；Intent/Recipient/Outbox 在 `submit` 里屋（L-051/L-052）。本课 `@DSTransactional` 包住 `publisher.publish`→`submit`，但 **没有** Controller 合同测试或提交抛错回滚单测 |
| 「五层链」 | 磁盘确实 Controller→UseCase→Service/Publisher→DAO→Mapper。全仓 **没有** `NotifyNoticeController*Test` / ArchitectureTest；ServiceTest 只覆盖未知 id 发布 |
| 「Controller 只注入 UseCase」 | 成立。课没写它 **继承** `BaseController` 才有 `toAjax`；publish 不走 `toAjax` |
| PublisherTest「USER / ALL 提交」 | 断言了 recipientIds 与渠道，**没有**钉 `recipientType` 被压成 `USER`、也没有钉幂等键字符串 |

问：在六扇路径/动词/七串权限、save 体内切 add·edit、管家两字段、写路径盖章、已发布 `null` 跳过 Publisher、撤回不 cancel、空 USER 不退化、草稿阶段就打目录 已经能互证的前提下，两格 (a) 是否还要把「UseCaseTest 漏断言 publish 事务注解」「retract 的 0 分支是死代码」「撤回后 status 仍是 0」「没有 HTTP 合同测试」写成开篇半句才算柜台讲完？还是这些已经落在课的失败关闭/规范冲突段里，Lead 可以直接盖章？

## Q3 假设与反例

- objective_id: OBJ-45
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: Controller 只注入 UseCase；无 UseCaseImpl；读路径无事务注解；publish 返回 null 跳过 Publisher；retract 不 cancel；Policy 空 USER 不退化；save≠submit；status≠lifecycle；USER_TYPE 提交压成 USER；ALL 拉人在 RuntimeService；BaseController+toAjax；无 HTTP 合同测试
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪层楼梯认错？

1. **「Controller 里已经有 Service 了。」** 唯一字段是 UseCase。课误区 1。
2. **「字段名叫 noticeUseCase 但 layered 日常仍调 `INotifyNoticeService`。」** 没有这张接口。生产主链是具体 `NotifyNoticeService`。
3. **「矩阵写了 Impl，所以一定有 `NotifyNoticeUseCaseImpl`。」** 磁盘管家就是具体类。把 sso/profile 的接口+impl 套过来会找错文件。课迁移 / L-003 已对照。
4. **「UseCase.publish 自己解析用户 / 写快照。」** 解析与 `insertSnapshot` 在 Publisher。管家只判断非 null。课误区 2。
5. **「`POST /save` 就是发送。」** save 只写 `notify_notice`。没有 `publisher.publish` 就没有 `submit`。课反例 1。厨房测试三类目标都走同一枪 `save`、`publish` 独立 POST，旁证同向。
6. **「连点两次发布会交两封。」** 第二次 `Service.publish` 返回 `null`，Publisher 不跑；窗口两次都 `R.ok()`。课正例 3。
7. **「撤回等于取消短信 / 抽回 Outbox。」** 撤回只改公告行。`cancel` 是 L-051 另一扇门。课误区 3。
8. **「`recipientType=USER` 但 ID 为空会发给全体。」** Policy 抛「发送对象类型与所选目标不匹配」。课反例 2。
9. **「已发布未撤回可以 `remove`。」** `requireEditable` 拒绝。课反例 3。
10. **「status 和 lifecycle 是同义词。」** add：status=`1` + `DRAFT`；publish：status=`0` + `PUBLISHED`。列表筛 status。课误区 5。
11. **「规范怎么写 UseCase 就怎么分工。」** 目标规范化在 Policy 与两个 Service。课误区 4；冲突以工作树为准。
12. **「`USER_TYPE` 会进统一通知的 validate。」** 草稿可写 `USER_TYPE`；submit 时压成 `USER`。应用 API 不认 `USER_TYPE`（L-051）。课边界。
13. **「Publisher 对 ALL 会在本类里 `selectAllActiveUsers`。」** Publisher 交空列表 + 类型 `ALL`。分页拉人在 RuntimeService。模块事实表那句不要当成本课函数。
14. **「读列表也盖了 `@DSTransactional`。」** 本管家读路径直接转发。不要拿 `PersonAdminUseCaseImpl` 十二扇全盖章当样板。
15. **「publish 失败会 `toAjax(0)`。」** publish 是 `void` + `R.ok()`。`toAjax` 只服务 save/retract/remove。
16. **「六扇也包含 inbox / config / 厨房工厂，因为 OBJ 写了通知。」** 课负空间与 chain 邻居明确不认。把假设反转后，Lead 会用本两行假闭合 L-046/L-047/L-053 或 (d) 提交走廊。
17. **「L-003 已经走查过 list 链，本课无新格子。」** L-003 盖的是登记表房间画像。本课盖的是六扇目的与管家事务边界。不要互认为 covered-by-parent 到对方那一行。

问：读完后仍说不出的，是不是「类名」，而是 **save≠发信、已发布 `null` 跳过 Publisher、撤回不 cancel、空 USER 不退化、status≠lifecycle、管家没有 Impl、解析不在 UseCase、ALL 拉人不在 Publisher、本两行不含 inbox/submit/厨房**？

## Q4 视角

- objective_id: OBJ-45
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-45 vs L-003/L-046…L-054；notification.md 冲突句；无 HTTP 合同测试；DAO Wrapper vs 单条 XML
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:NotifyNoticeController.*`** 或 **`A:NotifyNoticeUseCase.*`**？

| 视角 | 多出来的边 | 是否本课两格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 六名 + UseCase 行（备注 Impl 与磁盘不符）；(b) 已有 publish/retract 状态机行；(d) 提交走廊是 L-051/L-052 | **是** 闭合定义。两行 (a) 可单独 covered。(b) 可用本课机制链盖章，**不要**等改 chain。不要发明「公告发布」(d) |
| 窗口柜员 | 六映射、七串权限、save 体内切 add/edit、publish 恒 `R.ok()`、继承 `BaseController`/`toAjax` | **是** 第一格 |
| 管家 / 事务 | 两字段；写四法盖章；读两法不盖；`null` 跳过 Publisher；retract 计 1 | **是** 第二格。不要发明 UseCaseImpl 行 |
| 抽屉 Service | 锁、lifecycle、status=`0`/`1`、可编辑集合、批量删排序加锁 | **是** (a) explained。不要另开 Service 行 |
| Publisher / 快照 | 再 normalize、空用户先失败、版本+1、幂等键带版本、`submit` 参数 | **是** publish 的 explained 副作用。Intent/Outbox 表结构 **否**（L-051/L-052） |
| Policy | 空指定不退化；渠道三白名单 | **是** save/publish 失败句。不要另开 Policy 行 |
| DAO/XML | Wrapper 分页排序；只有 `selectByIdForUpdate` 下沉 XML | **是** 走查。(c) dao-mapper-ownership **否**（L-083） |
| 规范作者 | 「UseCase 负责解析 / 同一事务写 Outbox」 | 冲突已标、Java 优先 **是** 边界。按规范改代码 **否** |
| 收件箱（L-046） | 用户看见的是 `NotifyMessage` | 对照一句 **是**。四扇 inbox **否** |
| 配置（L-047） | MAIL/SMS 正文在场景绑定 `notice-published` | 对照一句 **是**。账号/试发十扇 **否** |
| 收件人目录（L-048） | `/notify/recipients` 搜索 | 草稿打 `UserService` **是** 本窗校验。目录 HTTP **否** |
| 应用提交（L-051） | `NotificationController.submit/query/retry/cancel`；ALL 分页拉人 | 本窗调用 `submit` **是** 出口。四扇应用 API / `cancel` **否** |
| Outbox（L-052） | claim/wake | `@DSTransactional` 包住 submit **是** 边界。领取算法 **否** |
| 厨房（L-053） | `createNotificationService.notices` 六枪 | URL 对照 **是**。工厂格子 **否** |
| 菜单页（L-054） | `createNotifyWebDomain` 公告页 | **否** |
| layered 登记（L-003） | 房间画像 + 脚本退出码 | 本链当正例 **是** 借用。`C:layered-classic` **否**（已 covered） |
| API-005 | 查询 GET、变更 POST；本切片已无 PUT/DELETE | 盘点现状 **是**。不发动传输 Ratchet **否** |
| MySQL 8.4 | 两张公告表、种子不插 Intent | 表证据 **是** (a)。容器 **否**（L-085） |

问：Lead 若凭「六个方法名都在函数表里」把两行 (a) 标 covered，是否还要求读者能指着 **save 体内切权限、已发布 `null` 跳过 Publisher、撤回不 cancel、空 USER 不退化、管家没有 Impl、解析不在 UseCase**？这些是不是已经在本两格函数表/反例里？(b) 状态机是不是同一台机器、可以凭本课盖章而不是第二容器？(d) 提交走廊是不是明确「矩阵已有、归 L-051/L-052」？

## Q5 元问题

- objective_id: OBJ-45
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 两行、 (b) 已有 publish/retract 独行、 (d) 通知提交归 L-051/L-052；子课文件号 L-001 ≠ chain 课号 L-045
- difficulty: low

建议：`defer`

原因：

- **两格 (a) 与矩阵六名 + 管家具体类 1:1。** L 合同章节齐全（宏观地图、直觉、定义表、机制链 1–7、带 caption / 文字等价物 / 图边界的发布顺序图、正反例、变式、迁移、误区、暂停走查、来源表 S-N-01/S-N-09/S-N-10/S-N-11）。**主叙事站得住且与源码对齐**：一块门牌六扇；GET/POST；七串权限且 save 体内切 add/edit；Controller→UseCase→Service/Publisher→DAO→XML；管家两字段、无 Impl；写路径 `@DSTransactional`、读路径转发；已发布返回 `null` 从而跳过 Publisher；撤回只改 lifecycle 不 `cancel`；Policy 空目标不退化；草稿阶段就打 `UserService`。不必为「类名」再挖五问。子课文件名 L-001 不是本 probe 的 `lesson_id`。
- 本批可粘贴的精度项已经落在课的表/反例/规范冲突段或测试边界，不挡住把 **两行 (a)** 标 covered：（1）UseCaseTest 事务循环漏 `publish`（源码有注解）；（2）`retract==null→0` 是死代码；（3）撤回后 status 仍是 `"0"`；（4）无 HTTP 合同测试 / ArchitectureTest；（5）PublisherTest 未钉压成 `USER` 的 recipientType 与幂等键字面。不要把 inbox、配置、应用四扇、Outbox、厨房工厂、登记表房间塞进本两行。
- **(b) `NotifyNoticeController.publish/retract` 不要另开一课，也不要等 GP-L-051。** 矩阵口号「公告状态机」；磁盘就是 `return null` 跳过二次 submit、撤回不 cancel、撤回后再发换快照版本因而换幂等键。chain 覆盖列只写 (a)；标 (b) covered 的证据锚是本课机制链 + 本 probe + 矩阵已有该行，不是先改 chain。miner 不改 chain。
- **不要发明「公告发布」(d) 行，也不要把 (d)「主路径 通知提交」并进本两行。** ALL 分页拉人、Intent/Outbox 表、`cancel` 是 L-051/L-052。本课只保证 publish 成功时管家会调用 `submit`，失败/幂等时不会。
- 不建议 `mine-more`：b01 未证明两行还有真实矩阵空洞；再五问只会把 L-051 `submit`、L-052 claim、L-046 收件箱或 L-053 厨房再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；半句测试名单/死代码/status 残留不是第二容器。不建议 `split-lesson`：未满 10 问；六扇与管家是 chain 原设计的同一 OBJ-45、同一 `wta-notify` 容器，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议因矩阵备注写了 Impl 就去造 `UseCaseImpl` 行。不建议用本课重盖 L-003。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:NotifyNoticeController.list, get, save, publish, retract, remove | 六扇路径/动词、七串权限、save 体内切 add/edit、publish 恒 `R.ok()`、继承 `BaseController`/`toAjax`、无 PUT/DELETE——均与源码对齐。残留：无 HTTP 合同测试；课未点名 BaseController。 | 可选半句：口试「窗口返回值」要问 publish 是不是 `void`+`R.ok()`，save/retract/remove 才走 `toAjax`。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与子课来源表 S-N-01）。 | defer（本行可 covered；精度项不挡盖章） |
| A:NotifyNoticeUseCase（与 Impl 同切片） | 具体 `@Service`、两字段、读不盖章写盖章、`publish` 非 null 才 Publisher、无 `UseCaseImpl` 文件——均与源码对齐。残留：测试循环漏断言 publish 注解；retract 的 null→0 死分支。 | 可选半句：矩阵「Impl」按「同切片」读，不要去找 impl 目录；口试事务章以源码四枚写方法为准。Lead 可将本行标 `covered`。 | defer（本行可 covered；不要发明 Impl 行） |
| B:NotifyNoticeController.publish/retract | 矩阵口号「公告状态机」。磁盘：已发布 `return null` 不二次 submit；撤回只改 lifecycle；再发换 snapshot 版本/幂等键。chain 未列 (b)。 | 不要等 L-051，也不要拆第二课。课已拆「连点两次 / 撤回≠取消 / 再发新键」。Lead 可将本行标 `covered`。 | defer（本行可 covered；证据在本课，不改 chain 也能盖） |
| D:主路径 通知提交 | 矩阵挂 L-051/L-052。本课只把 `submit` 当 publish 出口。 | 不要并进六扇才能盖章，也不要发明「公告发布」第二 (d)。 | defer（L-051/L-052） |
| A:NotifyInboxController.* / A:NotifyInboxUseCase.* | 用户看见的不是 `notify_notice` 行。 | 保持。L-046。 | defer（L-046） |
| A:NotifyConfigController.* / A:NotifyConfigUseCase.* | MAIL/SMS 正文权威对照一句。 | 保持。L-047。 | defer（L-047） |
| A:NotifyRecipientController.search, byIds | 草稿打 `UserService` 不是目录 HTTP。 | 保持。L-048。 | defer（L-048） |
| A:NotificationController.submit, query, retry, cancel / A:NotificationApplicationService | 本窗调用 `submit`；`cancel` 与 ALL 拉人未认。 | 保持。L-051。 | defer（L-051） |
| B:NotifyOutboxClaimUseCase / D:fail-outbox | 本课事务包住 submit，不讲领取。 | 保持。L-052。 | defer（L-052） |
| A:createNotificationService / A:notificationDirectory | 厨房六枪 URL 只作对照。 | 保持。L-053。 | defer（L-053） |
| A:createNotifyWebDomain | 公告页未认。 | 保持。L-054。 | defer（L-054） |
| C:layered-classic | L-003 已用本 list 链当正例并 covered。 | 不要因本课再标一次。 | defer（L-003 已 covered） |
| NotifyNoticeService / Publisher / Policy / DAO | 课内 explained，不是矩阵独行。 | 不要为本课发明 (a) 行。 | defer（未入库；covered-by-parent of 两格 (a)） |
| C:MySQL 8.4 / dao-mapper-ownership | 两张表与 FOR UPDATE 是 (a) 证据，不是容器/组件闭合。 | 不要因本课标容器 covered。 | defer（(c) / L-085 / L-083） |
| API-005 变更 POST | 本切片已全 GET/POST。 | 本课盘点，不发动 Ratchet。 | defer（传输 Ratchet） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本两行无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（测试名单窄、死代码、status 残留是半句精度；(b) 已在课内）；合格读者说得出柜台目的（六扇管一封还没寄出的信）、主路径（save 只写抽屉 → publish 锁行改 lifecycle → Publisher 快照+`submit`）与失败路径（空 USER 不退化、已发布 `null` 跳过、已发布未撤回不能删、撤回不 cancel）；把可选半句写进原课不会超过 40 分钟；inbox/配置/应用提交/Outbox/厨房/登记表已有邻课。满 10 问前不拆。六扇与管家虽穿过 Service/Publisher/DAO/XML，仍是 chain 原设计的同一 OBJ-45，同一 `wta-notify` 容器，不是「超过 1 个容器却讲不完」。(a) 与 (b) 是同一台发布/撤回机器的函数目的与方法性状，不是两个 C4。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-046 收件箱、L-047 配置、L-048 目录、L-051 应用提交、L-052 Outbox、L-053 厨房、L-054 菜单、L-003 登记表、未入库 Service/Policy 行、MySQL/DAO 容器以及 API-005 显式建议 `defer`；对 `A:NotifyNoticeController.list, get, save, publish, retract, remove` 与 `A:NotifyNoticeUseCase` 建议 Lead 标 `covered`（证据锚本 probe 与子课来源表）；对 `B:NotifyNoticeController.publish/retract` 建议凭本课标 `covered`，不要等 chain 补写 (b) 或等 L-051；对矩阵「主路径 通知提交」建议保持 L-051/L-052，不要发明公告 (d)。本课不主张改 Java 窗、造 UseCaseImpl 行、把 inbox/厨房算进 (a)、或用本课重盖 layered 登记。
