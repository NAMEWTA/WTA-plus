---
artifact: learning-goal-probe
probe_id: GP-L-066-b01
lesson_id: L-066
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-066 / b01

审问对象是已写成的 Lesson `lessons/L-066-third-web-domain.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **一格**：`A:createThirdWebDomain`。OBJ-66 原文：能说明 `createThirdWebDomain`。档是**解释+走查**。矩阵 (a) **没有** Vue 页符号，也没有 `createThirdWebDomain` 独行；符号落在 **九工厂同行** `createAdminWebDomain, createSystemWebDomain, createProfileWebDomain, createNotifyWebDomain, createThirdWebDomain, createWorkflowWebDomain, createDemoWebDomain, createAiWebDomain, createProfileSelfWebDomain`（模块 `web-domains/*`，仍 uncovered，备注「manifest 工厂」）。矩阵 (a) 厨房独行 `createThirdService` **仍 uncovered**（L-065 已 written，备注空）。矩阵 (b) **没有** runtime 校验 / 页面扣扳机 / 四键注册行；third (b) 已 covered 的是 `ThirdCredential 保存`（L-062）；仍 uncovered 的是 `ThirdPartyGateway.execute`。矩阵 (c) `admin-web` / `home-web` 容器与 `frontend-direction` / `admin-web-composition` 已 covered（L-004 / L-007 / L-008）。矩阵 (d) **没有** 浏览器三方菜单流；已 uncovered 的「主路径 third 出站」原文仍是 `ThirdPartyGateway.execute → 限流/凭证/HTTP/SPI → 记录`（挂 L-064）。本批按派单只审问这一格 (a) 切片；口试按磁盘：工厂 id 是 `web-domain-third`，`domainId` `'third'`，本课认 **四键一次冻住 + 一张 `ThirdPage` 四个 `kind` 怎么喊 `runtime.service` + 凭据嵌在行上不是第五键 + 厨房封面枪页面几乎扣完、缺的是厨房没收的 `/save` 与 execute**，不把九工厂行整行闭合，不把厨房独行提前盖章，也不把 L-060…L-064 五扇 Java 或 L-033 入站 OpenAPI 算进本切片。`createThirdService` 的 URL / `encodeURIComponent` / 15 枪封面留给 L-065；厅堂插头留给 L-007；导航 host / 九工厂行警告留给 L-020；入站 OpenAPI 留给 L-033；供应商/接口/凭据/观测/网关五扇 Java 留给 L-060…L-064（子课 L-001…L-005）。

抽查锚点（2026-09-17）：`frontend/packages/web-domains/third/src/{index,index.test,runtime,pages}.ts`；`ThirdPage.vue`；包 `package.json` `exports` 与 `AGENTS.md`；`frontend/packages/domains/third/src/{index,index.test}.ts`；`apps/admin-web/src/application/services.ts` 第 9 / 91 行；`router/adminManifestRegistry.ts` 第 24 / 245–254 / 336–376 行与 `.test.ts` 第 107 行；`apps/home-web/src/router/homeManifestRegistry.ts` 第 34–39 行；`50-cde-base-dml.sql` `NAMEWTA-THIRD-MENU-DML-001`；`frontend/e2e/third-party-management.spec.ts`；`ThirdProviderController` add vs `/save`；档案楼 `requireProfileWebRuntime`；通知楼 `NotifyWebRuntime` 对照。

## Q1 澄清

- objective_id: OBJ-66
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-66；chain L-066 覆盖列只 `A:createThirdWebDomain`；L-066 `claimed_cells` 一格；矩阵 (a) 九工厂同行仍 uncovered、无 Vue 行、无本工厂独行；厨房独行仍 uncovered（L-065）；(b) 无本课符号、execute 挂 L-064；(c) 两厅堂已 covered；(d) 无浏览器三方菜单流，已 uncovered 的主路径是 execute；课开篇「只给 createThirdWebDomain 这一颗」+ 负空间 L-065 / L-007 / L-020 / L-033 / L-060…L-064 / L-044 / L-054
- difficulty: medium

课把浏览器三方这一头收成「管理端墙上的四行点菜单，怎么把同一座厨房喂进同一张页的四种门脸」。计划里至少有六套可数对象，派单点名 **一格 (a) 切片**：

1. **OBJ-66 原文。** `course.md`：能说明 `createThirdWebDomain`。一句里是 **菜单工厂**，不是 `createThirdService`、不是五扇 Java 窗、也没有把九个 `create*WebDomain` 写成口试清单。课把 OBJ 扩成「四张门脸怎么喊厨房」，那是走查，不是第二格。
2. **chain / 课 `claimed_cells`。** 一格。符号就是 `createThirdWebDomain`。磁盘上 **没有** `@namewta/web-domain-third-provider` 四个包，也 **没有** `createThirdCredentialWebDomain`。管理端工厂 id `web-domain-third`，四键一次冻住。chain **不**写 Vue 文件名，也 **不**写 `kind`。
3. **矩阵 (a) 两处不对齐。** 厨房独行仍 uncovered，本课不得当第二张厨房章。九工厂行把本课切片和 `createSystemWebDomain` 等邻居绑死。Lead 若按**现印同行**标 covered，会把 L-020 / 未写的 workflow/demo/ai 菜单假闭合。矩阵 **没有** 四张门脸符号，也没有 `ThirdPage.vue` 行。
4. **课自己的操作定义。** 本课认：工厂冻四键 `third/{provider,endpoint,invocation,statistics}/index`；`h(ThirdPage, { runtime, kind })` 把厅堂 `thirdService` 塞进**同一张**页；供应商/接口走 list/get/save/status/remove；凭据三枪嵌在行上，测试锁死 `componentKey` 不含 `credential`；观测只 list。厨房里 **没有** `getCredential` / `execute` / Controller `POST .../save`。工厂 **不** `requireThirdWebRuntime`。托盘 **没有** `hasPermission` / `dicts` / `directory` / `navigate`。
5. **凭据抽屉不是第二张 claimed 格。** OBJ 要「工厂如何消费厨房」。课把嵌套三枪收进对照走查，并写明不是第五键。不要为了盖章把 `third/credential/index` 再立一行，也不要把抽屉降成「纯邻居、本课不认调用」。
6. **邻格。** L-065 厨房 URL 与 15 枪封面。L-060…L-064 五扇 Java。L-007 厅堂插头。L-020 九工厂行警告。L-033 入站 OpenAPI。L-044 档案楼 fail-closed。L-054 通知楼「厨房有、页面无」。

问：本格闭合标准是「四键 + 一张 Vue 四个 kind + 不校验托盘 + 页面实际扣的 `runtime.service.*` + 凭据非第五键 + 厨房没收的 `/save` 与 execute，且不碰九工厂整行、不升 Vue 矩阵行、不提前盖厨房章」，还是 OBJ 那句必须把四张门脸升成矩阵行、或必须先把九工厂行拆开、或必须等 GP-L-065 才给本切片盖章？Lead 若凭「`createThirdWebDomain` 四字在函数表里」把九工厂行标 covered，是否假闭合了另外八个工厂？

## Q2 证据

- objective_id: OBJ-66
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: web-domain `index.ts` 7–21；`index.test.ts` 5–15；`runtime.ts` 1–8；`pages.ts` 1–2；`ThirdPage.vue` 7 / 11–17 / 21–30 / 70–71 / 76–103 / 128–133 / 140–147 / 150–162 / 166–189 / 191–208 / 214–263；`AGENTS.md` 1–4；包 `package.json` 6–8；domains `index.ts` 19–57；`index.test.ts` 6–22；admin `services.ts` 9 / 91；`adminManifestRegistry.ts` 24 / 245–254 / 336–376；registry 测试 107；home `homeManifestRegistry.ts` 34–39；DML 1635–1666；e2e 206–306；`ThirdProviderController` 25–26；profile `runtime.ts` 64；notify `runtime.ts` 18–28
- difficulty: high

课的四键菜单 + 页面消费链哪些能在工作树直接复现，哪些只是测试名单比课表窄、或口试把「16 支枪」说成厨房封面？

**仍能复现（本切片）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂 `createThirdWebDomain`，id `web-domain-third`，`domainId` `'third'`，消息只 `namespace: 'thirdAdmin'` / `title: '三方接口管理'` | `index.ts` 16 |
| 四键顺序：`third/{provider,endpoint,invocation,statistics}/index` → `ThirdProvider` / `ThirdEndpoint` / `ThirdInvocation` / `ThirdStatistics` | `index.ts` 11–14；`index.test.ts` 7–12 |
| `view(name, kind, runtime)` → `h(ThirdPage, { runtime, kind })`；四键外包同一张 Vue | `index.ts` 8 / 11–14；包内只有 `ThirdPage.vue` |
| `service: {} as never` 仍能冻出四键 | `index.test.ts` 6–12 |
| 无 `requireThirdWebRuntime`；对照档案楼 `requireProfileWebRuntime` | `runtime.ts` 全文 1–8；profile `runtime.ts` 64 |
| 权限四组：provider 含 `third:credential:list\|add\|remove`；endpoint **不含** credential 串；观测两组只有 `:list` | `index.ts` 17–20 |
| 测试锁死 `componentKey` 不含 `credential`，同时 provider 组含 `third:credential:add` | `index.test.ts` 13–14 |
| `ThirdWebRuntime.service` 类型来自 `ThirdService`；可选 `confirm` / `success` / `error`；**没有** `hasPermission` / `dicts` / `directory` / `navigate` | `runtime.ts` 3–8；对照 notify `runtime.ts` 18–28 |
| 包名 `@namewta/web-domain-third`；`exports` 只有 `"."` | `package.json` 6 |
| 包依赖 `domain-third` + `platform-app-runtime` + vue/element-plus；**没有** axios、**没有** `@namewta/domain-system` | `package.json` 8 |
| 页 **零** `@/application/services`；**零** `execute`；**零** `hasPermission(` | web-domain 包 grep 空 |
| `kind` 四路标题/权限前缀/`editable`/列 | `ThirdPage.vue` 128–133 |
| 查询框都写「供应商」；providers 把值传给 `listProviders`；endpoints 另有「供应商 ID」；观测两路把空串也传进去 | 11–17 / 150–157 |
| 无分页组件；`rows = response.data ?? []` | 19 / 158 |
| 供应商/接口：新增核 `:add`；编辑/启停核 `:edit`；删除核 `:remove`；凭据钮核 `third:credential:list` | 7 / 23 / 28–30 |
| 编辑先 `getProvider` / `getEndpoint`，保存一律 `saveProvider` / `saveEndpoint`；对话框底部编辑核 `:edit`、新增核 `:add`，**同一枪厨房方法** | 70–71 / 166–180 |
| 启停 `'0'`/`'1'`；失败 `error?.` 再 `load()` | 23 / 191–200 |
| 删除 `confirm?.('确认删除当前配置？')`；**没有**成功 toast，**没有** `try/catch` | 203–207 |
| `load()` 无 `catch`，`finally` 仍关 loading | 150–161 |
| 凭据抽屉：供应商行 `endpointCode` 留空；接口行填 `endpointCode`；打开先清 `secretJson` | 214–221 / 76 |
| 列表列只有类型/范围/KEK/过期；`CredentialSummary` **没有** secret 字段 | 78–81；domains `index.ts` 19–22 |
| 替换再清 `secretJson`；`enabled === '0'` 才勾开关；厨房 **没有** `getCredential` | 235–241；`ThirdService` 32 只有 list/save/delete |
| 保存凭据成功后再清 JSON；删除核 `third:credential:remove` + `confirm?.` | 243–260 |
| 观测 `editable === false`：无新增、无操作列、无启停、无凭据；只 `listInvocations` / `listStatistics` | 26 / 131–132 / 156–157 |
| 调用明细列不挂 `sanitizedRequestJson` | 131 |
| `resetForm` 含 `pathSchemaJson` / `responseSchemaJson` / `adapterCode` / `credentialId`；供应商/接口对话框 **没有**这些输入 | 140–147 vs 38–67 |
| 厅堂：`thirdService = createThirdService(domainHttp)`；runtime 塞 `service` + 三个弹窗口 | `services.ts` 91；registry 245–254 |
| `selectedManifestIds` 含 `'web-domain-third'`；`selectedDomainIds` 含 `'third'`；`thirdDomainModule` 进 domainModules | registry 345 / 365 / 375 |
| `third/provider/index` + `third` → `componentName: 'ThirdProvider'` | registry 测试 107 |
| home **不**选这份 id，也 **没有** `createThirdService` / `thirdService` | `homeManifestRegistry.ts` 34–39；home-web grep 空 |
| 四张 C 型 + 凭据三串 F 型挂在供应商页 `…0011` 下；`menu_id` 从 `…0012` 跳到 `…0014`，没有 `…0013` 凭据 C 型 | DML 1646–1666 |
| 观测两页 **没有** F 型 query | DML 1652–1666 只有 provider/endpoint/credential 按钮 |
| e2e：四页投影；凭据 JSON 值为空；全文无 `raw-secret`；`/third/*` 带管理端 `clientid`；启停打 `POST /third/provider/1/status` | e2e 227–274 |
| list-only：新增/编辑/删除/开关消失；凭据仍可开，但保存/替换/删除/JSON 框都没有；未授权 `/system/third/endpoint` 无「三方接口」标题、不追加 `GET /third/endpoint/list` | e2e 278–305 |
| 厨房 `saveProvider` 打 `POST /third/provider`（Controller **add** 口）；Controller 另有 `POST /save`（`*:edit`） | domains `index.ts` 44；Controller 25–26 |
| 厨房封面 **15** 个扁平方法；**没有** `execute` | domains `index.ts` 27–34 / 39–57；domains 包 grep 空 |
| 厨房 id 插进 URL 前 `encodeURIComponent`；测试锁 `provider%2F1` | domains `index.ts` 37；`index.test.ts` 17 |
| `thirdDomainModule.capabilities` 含 `'third-credential'`，不是 componentKey | domains `index.ts` 36 |
| `AGENTS.md` 写 credential **summary** 经 runtime，HTTP 留在 domain-third | `AGENTS.md` 3–4 |
| 页面标题写死在 `config`，不读工厂 `messages`；e2e 找 heading「三方供应商」，种子菜单名是「供应商管理」 | 页 129；DML 1647；e2e 228 |

**课写过、测试名单更窄或口试数字写偏（不是证伪本格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「`ThirdService` 封面 16 支枪」「圈 16 支枪」「厨房 16 支枪都有扳机」 | 同课另一句已写成「供应商五 + 接口五 + 凭据三 + 观测两」= **15**。L-065 封面、`export interface ThirdService`、`createThirdService` 返回值都是 15。16 是口误，不是第 16 个方法 |
| 「测试锁死四键顺序与凭据非第五键」 | 四键顺序 + `includes('credential') === false` + provider 组含 `third:credential:add`。**没有** endpoint/invocation/statistics 权限数组的全量 `toEqual`，也 **没有** 锁 `id === 'web-domain-third'` |
| 「registry 测试锁 `ThirdProvider`」 | 只锁这一键。另外三键 `ThirdEndpoint` / `ThirdInvocation` / `ThirdStatistics` 在工厂字面量里，测试没点名 |
| 口试表把动词写成 GET/POST | 厨房 `method: 'get'`/`'post'` 小写。课图表题已写「动词以 transport 为准」 |
| 课 `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |

问：在四键顺序、空 service 仍能冻、一张 Vue 四个 kind、页面不 import 厅堂单例、供应商/接口五枪、凭据三枪嵌套且无明文、观测只 list、厨房无 execute 也无 `/save`、home 不选、工厂不 fail-closed 已经能互证的前提下，(a) 是否还要把「16→15」「权限数组测试只锁 credential:add」「registry 只锁 ThirdProvider」写成开篇半句，才算本切片讲完？还是这些已经落在课的表/反例里（数字口误除外），Lead 可以把本切片视为已讲、并保持九工厂行 uncovered、厨房独行仍留给 L-065？

## Q3 假设与反例

- objective_id: OBJ-66
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无四个 third web-domain 包；无 `requireThirdWebRuntime`；Vue 不 execute / 不 getCredential / 不打 `/save`；厨房 15 枪不是 16；home 无此工厂；凭据非第五键；`runtime` 无 hasPermission；capabilities 不是菜单；OpenAPI 是另一栋楼；九工厂行不因本课整行闭合
- difficulty: high

课的隐含假设若反转，合格读者会把哪座工厂或哪只抽屉认错？

1. **「四张 Vue `import { thirdService }`。」** 包内搜不到 `@/application/services`。只有一张 `ThirdPage.vue`。课反例 1 / 误区 4 覆盖。
2. **「`createThirdWebDomain` 等于九个 web-domain 工厂都 covered。」** 矩阵 (a) 同行绑死。GP-L-020 / GP-L-044 / GP-L-054 已挖过。课反例 2 / 误区 3 覆盖。
3. **「`ThirdPage.vue` 会 `execute` 出站。」** 那张页只改配置、刷回放。execute 是 L-064；厨房 facade 没收这支枪。课反例 3 覆盖。
4. **「系统 OpenAPI 页就是三方接口页。」** OpenAPI 键是 `system/openApi/index` + `system`。真三方键是 `third/endpoint/index` + `third`。课反例 4 覆盖。
5. **「有凭据按钮就有 `third/credential/index`。」** 测试：`componentKey.includes('credential')` 为 false。种子也没有这张 C 型。课反例 5 覆盖。
6. **「替换凭据会先 get 再回显明文。」** 页把 `secretJson` 置空。厨房没有 get-by-id。`getCredential` 在 **OpenAPI** 厨房。课反例 6 覆盖。
7. **「`runtime.hasPermission`。」** 托盘没有这口。按钮走 `v-hasPermi`。对照档案楼/通知楼不要背错。课反例 7 覆盖。
8. **「`createThirdWebDomain(undefined)` / 空 service 会当场 throw。」** 今天不会在工厂里 throw。缺 `listProviders` 的失败发生在 `onMounted`。课反例 8 / 误区 10 覆盖。
9. **「观测页能选日期、翻页、重放。」** 只有一个供应商编码框和一张表。课反例 9 覆盖。
10. **「home 的厨房没有 invocation / 少两道菜。」** home **没有**这座厨房，也没有这份菜单工厂。不要套 L-039「同一厨房两份菜单」。课反例 10 覆盖。
11. **「`capabilities` 有 third-credential 所以一定有凭据菜单键。」** 名片有能力，工厂可以不挂。今天就是这样。课反例 11 覆盖。
12. **「编辑保存打 `POST /third/provider/save`。」** 页面消费的方法名是 `saveProvider`；厨房这一枪的 URL 是集合根 POST。`/save` 是 Controller 另一扇窗。课反例 12 / 误区 14 覆盖。
13. **「厨房 16 支枪。」** 磁盘 15。把 16 当真会在口试里发明一支不存在的枪。课机制 7 / 图 2 文字等价物写偏；L-065 与同课「五+五+三+二」才是权威。
14. **「页面自己拼 `/third/provider/list`。」** 厨房才拼。课误区 6 覆盖。
15. **「`service.providers.list`。」** 扁平 `listProviders`。课误区 5 覆盖。
16. **「凭据走 `createOpenApiService`。」** 走 third 的 credential HTTP。课误区 7 覆盖。
17. **「观测页也能改供应商。」** `editable === false`。课误区 8 覆盖。
18. **「`v-hasPermi` 是厨房方法 / runtime 字段。」** 是厅堂装的指令。课误区 9 覆盖。
19. **「home 也能打开三方接口管理。」** 工作树没有这份工厂接线。课误区 11 覆盖。
20. **「OBJ-66 包含 `createThirdService`。」** OBJ-65。课误区 1 覆盖。
21. **「OBJ-66 包含 `ThirdProviderController`。」** OBJ-60。课误区 2 覆盖。
22. **「前端授权。」** 藏按钮 ≠ 授权。编辑钮核 `:edit`，详情枪要 `:query`；保存钮在编辑态核 `:edit`，厨房却打 add 口。课误区 13 / 边界 2 覆盖。
23. **「接口页没有凭据 DOM，因为权限目录把 credential 串放在 provider 组。」** 页面在接口行同样画「凭据」按钮。课边界 8 覆盖。
24. **「`confirm` 缺席时删除会被拦住。」** `confirm?.` 得到 `undefined` 仍往下走。课边界 3 覆盖。
25. **「工厂注册了四键 = 所有 `/third/*` HTTP 都有独立页。」** `/save` 与 execute 没有页。课图 2 文字等价物覆盖。

问：读完后仍说不出的，是不是「工厂的名字」，而是 **厨房方法全集 ≈ 墙上已扣的扳机、缺的是厨房没收的窗、九工厂行不能因本课整行闭合、工厂不 fail-closed、凭据不是第五键、home 根本不选、15 不是 16、编辑保存不是 `/save`、execute 不在点菜单上**？

## Q4 视角

- objective_id: OBJ-66
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-66 vs L-007/L-020/L-033/L-044/L-054/L-060…L-065；九工厂行；claimed_cells 工厂名 vs 一张 Vue / 凭据抽屉；厨房独行仍 uncovered
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这一格 (a) 切片**？

| 视角 | 多出来的边 | 是否本课这一格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 九工厂同行 vs chain 工厂名；厨房独行仍 uncovered；无 Vue 行；(b) 无本课符号、execute 仍 uncovered；(d) 无浏览器三方菜单流，出站走廊挂 L-064 | **是** 闭合定义。本切片可视为已讲。九工厂行保持 uncovered。不要发明 (b)/(d)，不要把一张 Vue 升成新行才能给切片盖章，不要用本课盖厨房章 |
| 管理端菜单工厂 | 四键顺序、四组 `third:*`、不校验托盘、`h(ThirdPage, { runtime, kind })`、未选无键 | **是** 切片里的 `createThirdWebDomain` |
| 一张页四个 kind | 供应商/接口五枪；凭据三枪嵌套；观测只 list；`v-hasPermi`；`confirm?.` | **是** OBJ 对照走查，不是矩阵第二格 |
| 凭据抽屉 | 非第五键；无明文；无 get-by-id；种子 F 型挂供应商页；接口行仍画按钮 | **是** 嵌套对照。不要升格成 `third/credential/index` |
| 厨房作者（L-065） | URL 全表；`encodeURIComponent`；15 枪封面；集合根 POST 不是 `/save` | 本课借用封面形状与没收窗负空间 **是**。工厂盖章 **否**（L-065） |
| 厅堂电工（L-007） | `createThirdService(domainHttp)`；registry 注入 runtime | 插头一句 **是**。12 个 `create*` 全表 **否** |
| 导航 host（L-020） | admin 选 `web-domain-third`；home 不选；未授权路由失败关闭 | 选择集一句 **是**。15 键 / 恢复链 **否**。九工厂行 **否** 整行 |
| 入站 OpenAPI（L-033） | `system/openApi/index`；`getCredential` 在那栋楼 | 走错楼一句 **是**。HMAC 会话 **否** |
| 档案楼（L-044） | `requireProfileWebRuntime` 十一口当场 throw | fail-closed 对照 **是**。企业三页 **否** |
| 通知楼（L-054） | 四张 Vue；厨房有枪页面没扣；托盘有 directory/dicts/hasPermission | 对照「一张 Vue / 厨房枪扣完 / 托盘更瘦」**是**。通知四页 **否** |
| 供应商窗（L-060） | add 与 `/save` 两扇 | 页喊 `saveProvider`、厨房打 add URL **是** 边界。Java 五层 **否** |
| 接口窗（L-061） | 同上 | 页喊 `saveEndpoint` **是** 边界。选门 **否** |
| 凭据窗（L-062） | 列表不回显；无 get | 前端清空 JSON **是** 对齐。KEK 信封 **否** |
| 观测窗（L-063） | 近 7 天 / 最多 200 条 | 前端不画这些数字 **是** 边界。Query 窗 **否**。该 (a) 行仍 uncovered，不要用本课盖 |
| 出站大门（L-064） | `ThirdPartyGateway.execute` | 无 Vue、无厨房字段 **是** 负空间。限流/SPI/记录 **否**。(b)/(d) 保持挂 L-064 |
| MySQL 8.4 | 菜单种子四 C + 凭据 F；跳号 `…0013` | 种子键对照 **是** (a)。容器 **否** |

问：Lead 若凭「工厂名在九工厂行里」把该行标 covered，是否还要求读者能指着 **四键、一张 Vue、页面不喊 execute、工厂不校验托盘、凭据非第五键、home 不选、编辑保存不是 `/save`、15 不是 16**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要等 L-065 才给**本切片**当已讲？九工厂行是否例外——必须保持 uncovered，直到其余工厂也有课？厨房独行是否必须保持 uncovered，本课只往备注写「third 四键消费已讲」？

## Q5 元问题

- objective_id: OBJ-66
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 九工厂同行仍 uncovered、厨房独行仍 uncovered、无 Vue 行、无本课 (b)/(d)
- difficulty: low

建议：`defer`

原因：

- **`A:createThirdWebDomain` 是 chain 切片，不是矩阵独行。** L 合同章节齐全（宏观地图、定义表、机制链 1–8、带 caption / 文字等价物的两张图、正反例、变式、误区、暂停走查、来源表 S-005/S-006/S-010/S-L066-01…08）。能复现：四键顺序与 `h(ThirdPage, { runtime, kind })`、空 service 仍能冻、无 fail-closed、一张 Vue 四个 kind、供应商/接口五枪、凭据三枪嵌套且无明文、观测只 list、厨房无 execute 也无 `/save` 方法、home 不选、`v-hasPermi` 不走 runtime。不必为「工厂的名字」再挖五问。
- **Lead 不可以凭本课把九工厂同行标 `covered`。** GP-L-020 / GP-L-044 / GP-L-054 已立过同一条。本切片证据可写进该行备注（「third 管理端四键 + 一张 Vue 四个 kind + 凭据嵌套已讲」）。**厨房独行保持 uncovered**，等 L-065，不要用本课开第二张厨房章。
- 本批可粘贴的精度项不挡住切片视为已讲：（1）口试枪数以 `ThirdService` / L-065 的 **15** 为准，不是本课六处「16」；（2）权限数组测试只锁 provider 组含 `third:credential:add`；（3）registry 测试只锁 `ThirdProvider`；（4）动词小写 `'get'`/`'post'`。不要把五扇 Java、厨房 URL 全表、入站 OpenAPI、九工厂其余八个名字塞进本格。
- 不建议 `mine-more`：b01 未证明本切片还有真实矩阵空洞；再五问只会把 L-065 厨房 URL、L-060 `/save`、L-062 KEK、L-063 200 条或 L-064 execute 再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；「16→15」是半句精度，不是第二容器。不建议 `split-lesson`：未满 10 问；四门脸消费是 chain 原设计的同一 OBJ-66，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：不校验 runtime、凭据非第五键、编辑钮核 edit 却打 add 口已是 (a) 的解释到副作用。不建议把一张 Vue 升成矩阵行才给切片盖章：课已写明格子按 chain 认工厂。不建议等 GP-L-065 才给本切片当已讲。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:createThirdWebDomain | 课按磁盘讲了四键、一张 Vue 四个 kind、不校验托盘、页面实际扣的 `runtime.service.*`、凭据嵌套非第五键、厨房没收的 `/save` 与 execute、home 不选。**矩阵无此独行**；符号落在九工厂同行。残留精度：六处写「16 支枪」，磁盘与 L-065 是 15；权限测试只锁 credential:add；registry 只锁 ThirdProvider。 | 可选半句：口试枪数以 `ThirdService` 15 个扁平方法为准（五+五+三+二），不要背本课「16」。不要求重写机制链。Lead **不要**改九工厂行为 covered。可将本切片证据写入该行备注。 | defer（切片已讲；九工厂行不可因本课整行闭合） |
| 九工厂行 `create*WebDomain`（含 `createThirdWebDomain`） | 本课只用选择集 + 四键当切片证据。其余八个工厂未因本课讲完。 | 保持 uncovered。L-020 已覆盖 `createSystemWebDomain` 切片；L-039/L-044 已覆盖 profile 两份菜单；L-054 已覆盖 notify 四页；其余后续课。 | defer（九工厂行保持 uncovered） |
| A:createThirdService | 厨房独行仍 uncovered。本课只借用封面形状与没收窗负空间。 | 不要用本课盖厨房章。备注可写「third 四键消费已讲」。 | defer（L-065） |
| A:admin-web.services | 插头 `createThirdService(domainHttp)` 与 registry 接线已借用。 | 保持。L-007。 | defer（L-007） |
| A:createSystemWebDomain / B:navigation host | 选择集 + 未授权路由失败关闭已点。15 键全表未讲。 | 保持。L-020。 | defer（L-020） |
| A:createOpenApiService / system/openApi/index | 走错楼一句；`getCredential` 在那栋楼。 | 保持。L-033。 | defer（L-033） |
| A:createProfileWebDomain enterprise | fail-closed 对照已借用。 | 保持。L-044。 | defer（L-044） |
| A:createNotifyWebDomain | 「厨房有枪页面没扣 / 四张 Vue」对照已借用。 | 保持。L-054。 | defer（L-054） |
| ThirdProviderController.list, get, add, save, status, remove | add 与 `/save` 分窗旁证已借用。该行已 covered。 | 不要重盖。页喊 `saveProvider` 不是第二张 Controller 章。 | defer（L-060 已 covered） |
| ThirdEndpointController.* | 同上。已 covered。 | 保持。L-061。 | defer（L-061 已 covered） |
| ThirdCredentialController.list, add, remove | 无 get、列表不回显旁证已借用。已 covered。 | 保持。L-062。不要把嵌套抽屉升成第五键。 | defer（L-062 已 covered） |
| ThirdObservabilityController.invocation/list, statistics/list | 页只 list、不画 7 天/200 条。该行仍 uncovered。 | 不要用本课盖观测 Java。 | defer（L-063） |
| B:ThirdPartyGateway.execute / D:fail-third-http / 主路径 third 出站 | 无 Vue、无厨房字段。 | 不要并进菜单工厂。保持挂 L-064。 | defer（L-064） |
| (b) runtime 校验 / 页面扣扳机 / 四键注册 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 浏览器三方菜单流 | 矩阵无此行。ASCII 图不是新的 (d)。已有的出站走廊仍是 execute。 | 不要发明前端三方菜单数据流行。 | defer（未入库） |
| MySQL 8.4 / Redis 8 | 菜单种子是 (a) 对照，不是容器闭合。Client 头在厅堂 HTTP。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本切片无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（九工厂同行是矩阵绑行问题，课内已把切片讲完并显式禁止整行闭合；厨房独行有自己的课）；合格读者说得出工厂目的（管理端四键点菜单把厨房喂进同一张页）、主路径（供应商/接口 list-get-save-status-remove / 凭据嵌套三枪 / 观测只 list）与失败路径（工厂不校验、缺方法在 `onMounted` 才炸、`confirm?.` 缺席不拦截、`load` 无 catch、编辑钮核 edit 却打 add 口、home 不选这份 id）；把可选半句写进原课不会超过 40 分钟；后端五扇/厨房/厅堂插头/入站 OpenAPI/九工厂其余名字已有邻课。满 10 问前不拆。四门脸虽跨供应商/接口/观测/凭据抽屉，仍是 chain 原设计的同一 OBJ-66，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对九工厂整行、L-065 厨房、L-007 厅堂、L-020 导航、L-033 OpenAPI、L-044 档案楼对照、L-054 通知楼对照、L-060…L-062 已 covered 的 Java 窗、L-063 观测、L-064 出站与 (d)、未入库 (b)/(d) 以及 MySQL/Redis 容器显式建议 `defer`；对 `A:createThirdWebDomain` 建议视为已讲切片、**不要**把九工厂同行标 `covered`、**不要**用本课盖仍 uncovered 的厨房独行。本课不主张改 Java 窗、发明 Vue 矩阵行、或把凭据抽屉升成第五键。
