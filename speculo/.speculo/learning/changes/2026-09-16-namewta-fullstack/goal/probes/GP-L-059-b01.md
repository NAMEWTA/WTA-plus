---
artifact: learning-goal-probe
probe_id: GP-L-059-b01
lesson_id: L-059
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-059 / b01

审问对象是已写成的 Lesson `lessons/L-059-sso-api-catalog.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **两格 (a)**：`A:SsoClientCatalog`、`A:SsoIdentityService`。OBJ-59 原文：能说明 `SsoClientCatalog` / `SsoIdentityService` 只经 `wta-api` 读用户与 Client。档是**解释+边界**。前置 OBJ-55、OBJ-18。矩阵 (a) **一行两符号** `SsoClientCatalog, SsoIdentityService`，模块栏印 `wta-api/sso`，仍 `uncovered`，备注「只读用户与 Client」。矩阵 (b) **没有**目录/认人独行；已 covered 的 SSO (b) 是 `SsoOAuthController.authorize|token|revoke` 与 `SsoSessionController.login/logout`。矩阵 (c) `wta-api` 容器已 covered（L-002）；`wta-modules` 业务房间 / `layered-classic` 已 covered（L-002/L-003）；Redis 8 / MySQL 8.4 / `dao-mapper-ownership` 仍 uncovered。矩阵 (d) 「主路径 SSO 授权码」「失败路径 SSO PKCE」**已经** covered（L-055 + L-056 + L-057 + 对应 probe）。本批按派单只审问 **这两份 `org.namewta.sso.api` 合同窗**：谁填窗、认人厅怎样隔着 Port 伸手、店卡抄什么/不抄哈希、三法谁在何时喊、禁票两道闸。不要把矩阵该行的模块栏读成「实现住在 wta-api」，不要发明 Port/(b) 第三行，也不要把已 covered 的授权码走廊或管理窗再盖一次。发码 PKCE/精确回调留给 L-055；换票/撕票留给 L-056；Cookie/Redis 手环留给 L-057；sso-web 四枪留给 L-058；`SysClientController` / `SysSsoAppController` 十四扇留给 L-018；厅堂 `/auth/login` 留给 L-014；验证码/密码策略留给 L-012；`PasswordAuthStrategy` / `SysLoginService` 里屋留给 L-013；临时柜签发留给 L-015；路牌 `ssoAuthorizeUrl` 留给 L-011；`LoginHelper` / `StpUtil` 容器留给 L-084。`SsoClientCatalogPort` / `SsoIdentityPort` / 两份 Adapter 只作本窗机制证人，不另立矩阵行。

抽查锚点（2026-09-17）：`backend/wta-api/src/main/java/org/namewta/sso/api/{SsoClientCatalog,SsoClientView,SsoIdentityService,SsoAuthenticatedUser}.java`（分包正好四份）；`wta-api/pom.xml`；`backend/wta-modules/wta-system/src/main/java/org/namewta/system/sso/{SystemSsoClientCatalog,ClientAccessPaths,SsoClientFieldsSupport}.java`；`ISysClientService` / `SysClientServiceImpl.queryByClientId|rotateSsoSecret|fillClientRuleFields`；`ClientUserTypeAccessService.requireLoginAccess`；`wta-admin/.../web/sso/AdminSsoIdentityService.java`；`PasswordAuthStrategy.authenticate`；`SysLoginService.loginSucceeded`；`TemporaryPasswordService.consume`；`backend/wta-modules/wta-sso/src/main/java/org/namewta/sso/{adapter/gateway/{WtaApiSsoClientCatalogAdapter,WtaApiSsoIdentityAdapter,SaTokenSsoBusinessTokenAdapter},port/{SsoClientCatalogPort,SsoIdentityPort},service/{SsoAuthorizationService,SsoSessionService},support/{SsoTokenExtras,PkceS256},domain/bo/SsoTokenBo,config/{SsoProperties,SsoServiceConfiguration,SsoAutoConfiguration}}.java`；`wta-sso/pom.xml`；`wta-sso/AGENTS.md`；登记表 `03-backend-module-modes.md` layered 行；`CacheNames.SYS_CLIENT` / `PWD_ERR_CNT_KEY`；`org.namewta.system.api.UserService`；`org.namewta.system.api.model.LoginUser`；测试 `ClientAccessPathsTest`、`SsoClientFieldsSupportTest`、`SsoTokenExtrasTest`、`SsoAuthorizationServiceTest`、`SsoSessionServiceTest`（**零** `AdminSsoIdentityService*Test` / `SystemSsoClientCatalog*Test`）；种子 `50-cde-base-dml.sql` `NAMEWTA-SSO-DSL-001` 与 `client_key='home'` 回填；旁证（格子归邻课）`SsoOAuthController` / `SsoSessionController` 的 `@ConditionalOnProperty`；`PkceS256.requireS256`（L-055）；`SsoTokenBo` 无 secret（L-056）。

## Q1 澄清

- objective_id: OBJ-59
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-59 解释+边界、前置 OBJ-55/OBJ-18；chain L-059 两格 A:SsoClientCatalog A:SsoIdentityService；课 claimed_cells 两格；矩阵 (a) 一行两符号仍 uncovered、模块栏 wta-api/sso；(b) 无本课符号；(c) wta-api 已 covered；(d) 授权码/PKCE 已 covered；负空间 L-055…L-058 / L-018 / L-014 / L-013 / L-012 / L-015 / L-011 / L-084 / L-002 / L-005
- difficulty: medium

课把跨模块只读收成「认人厅隔着两扇 `wta-api` 合同窗问店卡和值班前台，自己不翻 system 抽屉」。计划里至少有四套可数对象，派单点名的是 **chain 这两格 (a)**：

1. **OBJ-59 原文。** `course.md`：能说明 `SsoClientCatalog` / `SsoIdentityService` 只经 `wta-api` 读用户与 Client。档是解释+边界，不是方法性状新行，也不是 HTTP 走查。一句里是**两份 Java 合同**，没有把 `SsoOAuthController`、`SsoSessionController`、`ssoApi.ts`、`ISysClientService`、`SysUserMapper` 或 `SsoClientCatalogPort` 写成口试清单。
2. **chain / 课 `claimed_cells`。** 两格，磁盘符号就是这两个接口。chain **不**写 View / AuthenticatedUser / Port / Adapter，**不**写 (b)/(d)。课把 View 与手环收成合同窗上的纸，把 Port/Adapter 收成「认人厅自己的门缝」。
3. **矩阵 (a) 一行两符号仍 uncovered。** 模块栏印 `wta-api/sso`。实现全仓只有 `SystemSsoClientCatalog`（`wta-system`）与 `AdminSsoIdentityService`（`wta-admin` 主机）。Lead 若把模块栏读成「实现必须住在 wta-api 包内」会假失败；若凭「L-002 已讲 wta-api 容器 / L-005 已点过名字」宣布本课无对象，会把「谁 implements、店卡不抄哈希、三法三时刻、禁票两道闸」丢掉。邻格 HTTP 已 covered，不能再拿来当本行证据。
4. **课自己的操作定义。** 本课认：`org.namewta.sso.api` 正好四份类型；目录 1 法 `findByClientId` 找不到返回 `null`；认人 3 法 `verifyPassword` / `assertClientAccess` / `buildLoginUser`；`wta-sso` POM 无 `wta-system`，生产代码对 `org.namewta.system.*` 只见 `LoginUser`（住在 `wta-api`）；店卡白名单抄写且 **不** `setSsoSecretHash`，热路径 `fillView` 先清哈希；验密两参数、可撕临时柜、不带 Client、不发 Token；发码前准入、换票组装并禁 `clientKey=sso`；`SsoTokenExtras.bind` 再禁 key 或门牌字符串 `"sso"`；换票无 `client_secret`。
5. **邻格。** L-018 管管理窗 bind/rotate，并把「邻居只读适配器」推到本课。L-055…L-057 管 `/sso/*` HTTP。L-058 管浏览器四枪。L-005 只点过 `SsoIdentityService` 这个名字。L-002 划过主机可以填 api 窗。L-013/L-015 管门厅先准入再撕临时柜——对照，不是本格。L-084 管 `LoginHelper.login` 写 Redis。

问：本格闭合标准是「两份合同 + 谁填窗 + POM 墙 + 店卡不带哈希 + 三法三时刻 + 禁票两道闸，且不重盖 L-055…L-058 / L-018、不发明 Port/(b) 行、不把模块栏当成实现落点」，还是必须先给 `verifyPassword` 立 (b)、或必须有 `AdminSsoIdentityService` 单测、或必须把 (d) 授权码走廊再盖一次才给这两格盖章？Lead 若凭「wta-api 容器已 covered」跳过 OBJ-59，是否把填窗人和哈希空槽假闭合进了 L-002/L-005？

## Q2 证据

- objective_id: OBJ-59
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: sso.api 四份；Catalog 6–15；Identity 8–34；View 26 / 122–127；AuthenticatedUser 14–22；SystemSsoClientCatalog 15 / 23–46；AdminSsoIdentity 29 / 41–88；Adapter 14–24 与 15–41；Port 8–16 与 9–35；AuthorizationService 30–31 / 73–83 / 136–148 / 150–161；SessionService 16–42；TokenExtras 23–44；TokenBo 10–26；pom 14–18 无 wta-system；AGENTS.md 7；fillView 128–132；queryByClientId 90–96；rotate 263–273 无 CacheEvict；ClientAccessPaths 33–46；AccessService 30–47；PasswordAuthStrategy 91–103；loginSucceeded 261–263；consume 55–62；SsoProperties 18；Controller ConditionalOnProperty；登记表 20；种子 445–454 / 1754–1756；UserService 无验密；零 AdminSsoIdentityService 测试；StubIdentity 172–176 比生产更严
- difficulty: high

课的两扇合同窗哪些能在工作树直接复现，哪些只是测试名单比课表窄、或口试把邻格 HTTP/管理窗说成本课格子？

**仍能复现（本切片两合同 + 填窗 + 门缝 + 三法时刻）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| `org.namewta.sso.api` 正好四份 Java；无第五份接口、无 Mapper、无 `@RequestMapping` | 该目录四文件；接口无 Spring Web 注解 |
| 目录唯一方法 `findByClientId`；找不到返回 `null` | `SsoClientCatalog` 6–15 |
| 认人正好三法；`buildLoginUser` 返回 `LoginUser` | `SsoIdentityService` 8–34 |
| 视图是可序列化 JavaBean，**有** `ssoSecretHash` 空槽；**没有** `clientSecret` | `SsoClientView` 26 / 122–127；类上无 login 明文字段 |
| 手环只有 `userId` + `username`；注释不含业务 Token | `SsoAuthenticatedUser` 6–22 |
| 全仓 `implements SsoClientCatalog` 只有 `SystemSsoClientCatalog` | 该类 15；Adapter 实现的是 `SsoClientCatalogPort` |
| 全仓 `implements SsoIdentityService` 只有 `AdminSsoIdentityService` | 该类 29；包名 `org.namewta.web.sso`（主机，不是 system） |
| 目录白名单 set：含 `redirectUris`←`ssoRedirectUriList`、`accessPath`←`ClientAccessPaths.resolve`；**没有** `setSsoSecretHash` | `SystemSsoClientCatalog` 28–46 |
| home 且白名单非空才并入 getInfo / getRouters / logout / `/profile/**`；其它 Client 原样 | `ClientAccessPaths` 33–46；单测 26–48 锁门牌 `428a8310cd442757ae699df5d894f051` |
| `queryByClientId` `@Cacheable(sys_client#30d)`；进缓存前 `fillView` **先** `setSsoSecretHash(null)` | `SysClientServiceImpl` 90–96 / 313–321；`SsoClientFieldsSupport` 128–132；`CacheNames` 42 |
| `rotateSsoSecret` **方法上无** `@CacheEvict`；`bindSsoAccess` 才 `allEntries` | `rotateSsoSecret` 261–273 vs `bindSsoAccess` 279 |
| 认人厅 Service 字段是 **Port**，不是 `wta-api` 接口 | `SsoAuthorizationService` 30–31；`SsoSessionService` 16–17；`SsoServiceConfiguration` 40–47 |
| 两份 Adapter 每方法一行委托 | `WtaApiSsoClientCatalogAdapter` 22–24；`WtaApiSsoIdentityAdapter` 23–40 |
| `wta-sso/pom.xml` 有 `wta-api`，**没有** `wta-system`；生产 `SysUserMapper` / `ISysClientService` 零命中 | pom 14–18；main 树两符号零命中 |
| 生产 `org.namewta.system.*` import 只有 `LoginUser`（`wta-api`） | 五处 import 全是 `system.api.model.LoginUser` |
| 登记表 `wta-sso` layered，「仅经 wta-api 读取用户与 Client」；`AGENTS.md` 同一句 | 登记表 20；`wta-sso/AGENTS.md` 7 |
| `verifyPassword`：验永久 → 失败才 `verify` → **立刻** `consume` → `loginSucceeded`（只删 `pwd_err_cnt:`）→ 两字段手环；**不**查 Client | `AdminSsoIdentityService` 41–55；`loginSucceeded` 261–263 |
| 门厅相反：`requireLoginAccess` 夹在 `verify` 与 `consume` 之间 | `PasswordAuthStrategy.authenticate` 91–103 |
| `assertClientAccess`：`requireClient`（空或非 `"0"` →「客户端不可用」）+ `requireLoginAccess`；**不**看 `ssoEnabled`，**不**禁 `clientKey=sso` | Identity 61–64 / 83–88；AccessService 30–47 |
| `buildLoginUser`：再 `requireClient`；`"sso".equalsIgnoreCase(clientKey)` →「禁止签发 SSO 中心 Client 业务票」；再准入；`loginService.buildLoginUser`；**不** `LoginHelper.login` | Identity 70–81；`SysLoginService` 零 `LoginHelper.login` |
| extras `CLIENT_KEY` 写入的是**门牌 `clientId`**；再禁 key **或** 门牌字符串 `"sso"` | `SsoTokenExtras` 27–40；单测 20–30 锁 admin 门牌 `e5cd7e4891bf95d1d19206ce24a7b32e` |
| 盖章在 Adapter：`LoginHelper.login(user, SsoTokenExtras.bind(client))` | `SaTokenSsoBusinessTokenAdapter` 23–25 |
| 登录只喊 `verifyPassword`；session/logout 不喊合同；authorize 先 `findByClientId`，无手环早退、有手环才 `assertClientAccess`；exchange 再目录 + `buildLoginUser`；revoke 两法都不喊 | SessionService 38–61；AuthorizationService 73–83 / 136–148 |
| 空白用户名密码不进 `verifyPassword` | SessionService 38–40 |
| `requireEnabledClient`：null/停用「客户端不可用」；`ssoEnabled != TRUE`「客户端未启用 SSO」；**不**禁 `clientKey=sso` | AuthorizationService 150–161 |
| `SsoTokenBo` 无 secret 字段；`wta-sso` 源码零 `ssoSecret` / `client_secret` | TokenBo 10–26；模块 Java 零命中 |
| `authorize` 仍 `PkceS256.requireS256`，**不**读店卡 `pkceRequired` | AuthorizationService 75–76；`PkceS256` 46–49 |
| `namewta.sso.enabled` 默认 true；关掉是 Controller/UseCase 不进容器，api 类型仍在 classpath | `SsoProperties` 18；两 Controller + 两 UseCaseImpl 的 `@ConditionalOnProperty`；`SsoAutoConfiguration` 只挂 properties |
| `UserService` 无验密 / 无 `assertClientAccess` | `wta-api/.../system/api/UserService.java` 14–157 |
| `ISysClientService` 不在 `wta-api` | 包名 `org.namewta.system.service` |
| 种子：home 门牌该串、`client_key` 回填 `'home'`；厅堂行 key=`sso`、门牌 `0ae7adbebb81e87a9735ed0fba0a1135`、`sso_enabled=0`、`sso_auth_mode=local` | DML 445–454 / 1754–1756 |
| 会话测试假 `buildLoginUser` 抛「SSO session must not issue a business token」 | `SsoSessionServiceTest` 48–50 |
| exchange 断言发出去的 `clientId` 不是 `"sso"` | `SsoAuthorizationServiceTest` 70–84 |

**测试名单比课表窄 / 不要说成本课格子：**

| 现象 | 含义 |
| --- | --- |
| **零** `AdminSsoIdentityService*Test` | 当场 consume、禁 `clientKey=sso`、`requireClient` 不看 SSO 开关——证人是源码，不是单测 |
| `SystemSsoClientCatalog` 无同名测试类；`ClientAccessPathsTest` 只锁 home 路径并入 | **不断言** `view.getSsoSecretHash()==null`；哈希空槽靠 fillView 单测 + 目录不 set |
| `SsoAuthorizationServiceTest.StubIdentity.assertClientAccess` 在 `clientId.equals("sso")` 时抛 | **比生产更严**。活实现不在这一枪禁 sso。只读 SSO 测试会把禁票提前到准入 |
| `SsoTokenExtrasTest` 手造 **同时** `clientId=sso` 且 `clientKey=sso` | 不隔离「门牌字符串闸」与「key 闸」。种子厅堂是 MD5 门牌 + key=`sso`，活样本走 **key** |
| `HomeClientLoginAccessPathTest` 走 `IAuthStrategy.buildLoginParameter` | 门厅 extras，不是目录抄写。本课证人是 `SystemSsoClientCatalog` + `SsoTokenExtras` |
| `fillView` 默认 kind/PKCE/autoConsent | 发生在 VO，目录再抄。管理窗 L-018 |
| Controller `@ConditionalOnProperty` | HTTP 开关注销是 L-055…L-057；本课只保证 api 类型不随开关消失 |
| Adapter **无** `@ConditionalOnProperty` | 关掉 SSO 后门缝 Bean 仍可能在；不是第二份目录实现 |

问：合格读者指着「POM 墙 + 目录不 set 哈希 + 三法分枪 + extras 写业务门牌」，是否已经够给矩阵这一行盖章？还是必须先补 `AdminSsoIdentityService` 单测、或把 Stub 的过严准入写进课，才算 (a) explained？

## Q3 假设与反例

- objective_id: OBJ-59
- bloom_level: analyze
- socratic_move: assumptions
- expected_evidence: 课反例 1–14 与磁盘对齐；StubIdentity 172–176 ≠ AdminSsoIdentity 61–64；TokenExtrasTest 42–45 双闸绑死；零 Identity 单测；rotate 无 CacheEvict 但本课换票不读哈希；UserService 无密码法；Port 不是第二份 wta-api
- difficulty: high

课列了十四个反例。合格读者读完后仍可能把哪一句说成本格事实？哪一句其实是邻格或测试桩？

1. **「`wta-sso` 里有一份 `SsoClientCatalog` 实现。」** 没有。只有 Port + 一行 Adapter。反例 1 成立。
2. **「`ISysClientService` 就是目录合同。」** 它在 `org.namewta.system.service`。反例 2 成立。
3. **「认人实现也在 system。」** 在 `wta-admin/.../web/sso/`。登记表甚至不把 `wta-admin` 当业务五层房间。反例 3 成立。
4. **「`findByClientId` 会带回可校验哈希。」** 热路径 VO 已清；目录不 set；sso 模块不读 getter。反例 4 成立。**单测没锁目录视图的哈希字段。**
5. **「换票 body 要 `client_secret`。」** `SsoTokenBo` 没有。反例 5 成立（协议格子在 L-056，本课只认合同窗不供 secret）。
6. **「`verifyPassword` 等于 `/auth/login`。」** 无验证码、无 Client、无 `LoginHelper.login`。反例 6 成立。
7. **「SSO 验密也是先准入再撕临时柜。」** 磁盘是当场 consume。反例 7 成立。**无 Identity 单测锁顺序。**
8. **「`assertClientAccess` 会拒绝 `clientKey=sso`。」** 活实现不会。反例 8 成立。**SSO 单测桩会拒绝 `clientId=sso`，这是课外陷阱。**
9. **「`SsoAuthenticatedUser` 就是 `LoginUser`。」** 手环两字段 vs 底单带权限。会话测试禁止登录路径组装。反例 9 成立。
10. **「`UserService` 能替代。」** 没有密码方法。反例 10 成立。
11. **「目录 `userTypeId` 就是准入。」** 认人丢开店卡，再 `queryByClientId` 拿 `SysClientVo`。反例 11 成立。
12. **「只读所以验密零 Redis。」** 错次键与临时柜会动。反例 12 成立。
13. **「Adapter 是业务规则。」** 一行委托。反例 13 成立。
14. **「种子 `clientId=sso`。」** 种子 **key** 是 `sso`，门牌是那串 MD5。反例 14 成立。extras 对门牌字符串 `"sso"` 是额外闸。
15. **课没写成反例、但口试会踩的：** 只读 `SsoAuthorizationServiceTest` 会以为准入枪已经禁 sso；只读 `SsoTokenExtrasTest` 会以为种子门牌本身是 `"sso"`。
16. **「只读等于 `queryByClientId` 缓存永远新鲜。」** rotate 不按键驱逐是 **L-018** 的管理窗事实。对本课：哈希本就不在店卡上，exchange 也不 `matches`。不要把缓存题扩成第二格。
17. **「矩阵要先有 (b) `verifyPassword` 行才算讲完。」** 矩阵无此行。副作用写进 (a) explained。
18. **「本课可以把 (d) 授权码走廊再盖一次。」** 该两行已 covered。本课 ASCII 只保证「哪一步碰到哪份 api 方法」。
19. **「关掉 `namewta.sso.enabled` 目录接口会从 classpath 消失。」** 不会。消失的是 HTTP Bean。
20. **「`SsoClientCatalogPort` 可供 notify 去读 Client。」** 那是认人厅门缝。邻居应自己 POM + Adapter，或只用 `wta-api`。

问：读完后仍说不出的，是不是「两个接口名」，而是 **谁 implements（system 填目录、admin 填认人、sso 只 Adapter）、店卡不带哈希、验密当场 consume、准入不禁 sso key、组装和 extras 才禁、POM 伸不到 Mapper、测试桩比生产更严**？这些是不是已经在本格机制链/反例里，而不需要 (b) 新行、也不需要等 Identity 单测才盖章？

## Q4 视角

- objective_id: OBJ-59
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a) 一行两符号 uncovered、模块栏 wta-api/sso；(b) 无本课符号；(c) wta-api 已 covered；(d) SSO 两行已 covered；OBJ-59 vs L-002/L-005/L-018/L-055…L-058/L-013/L-015/L-084；chain 两格 vs 实现落点跨三栋楼
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这两格 (a)**？

| 视角 | 多出来的边 | 是否本课这两格 |
| --- | --- | --- |
| 矩阵维护者 | 一行两符号仍 uncovered；模块栏 `wta-api/sso`；无 (b)；无 Port 行；(c)(d) 邻格已 covered | **是** 闭合定义。本切片可视为已讲。不要发明 (b)/Port 行，不要重盖容器或授权码走廊 |
| 合同窗 | 四份类型、1+3 方法、null vs 抛错两套语义 | **是** (a) |
| 填窗人 | system classic 填目录；admin 主机填认人；sso 不 implements | **是** OBJ 边界。不要把主机落点说成 layered 违规 |
| POM 墙 | `wta-sso` 无 `wta-system`；编译期伸不到 Mapper | **是** |
| 店卡抄写 | 白名单字段；home 并入；哈希空槽；fillView 先清 | **是**。rotate 缓存驱逐 **否**（L-018） |
| 三法时刻 | 登录验密 / 发码准入 / 换票组装 | 调用关系 **是**。HTTP 五层 **否**（L-055…L-057） |
| 禁票两道闸 | `buildLoginUser` 看 key；extras 看 key 或门牌字符串 | **是**。签发 Redis **否**（L-084） |
| 门缝 Port | Adapter 一行；Service 只见 Port | 机制证人 **是**。不要升成第二份 wta-api |
| L-002 / L-005 | api 容器、名字点过 | 对照一句 **是**。再盖容器/合同总览 **否** |
| 管理窗（L-018） | bind/rotate/十四扇；信封哈希 | 「邻居不注入 ISysClientService」**是** 边界。HTTP **否** |
| 授权门口（L-055） | 精确回调、强制 S256、无用户不写码 | 「目录锁不禁 sso key」**是**。五层 **否** |
| 换票（L-056） | PKCE verify、吃码、revoke 非 SLO | 「组装禁 sso、BO 无 secret」**是** 负空间。换票门牌 **否** |
| 域会话（L-057） | Cookie / `sso:session:` | 「登录只得手环」**是**。TTL/Secure **否** |
| sso-web（L-058） | 四支枪 | JVM 合同浏览器 import 不到 **是** 边界。四枪 **否** |
| 门厅策略（L-013 / L-015） | 先准入再 consume；图形验证码 | 对照顺序 **是**。策略枢纽 **否** |
| `UserService` | 通知查人 | 另一扇窗 **否** |
| (b) 验密副作用 / 临时柜 CAS | 矩阵无此行 | 本格 explained **是**。不要发明第二 (b) |
| (d) 主路径 SSO 授权码 | 已 covered | ASCII 时刻表 **是** 本课图。不要用本课再改 (d) |
| Redis 8 / MySQL 8.4 | 错次键 / `sys_client` 行 | 不要因本课标容器 covered |

问：Lead 若凭「两个接口名已在 L-005 出现过、wta-api 容器已 covered」宣布 OBJ-59 无事可做，是否还要求读者能指着 **谁 implements、POM 墙、店卡不 set 哈希、验密当场 consume、准入不禁 sso、组装和 extras 才禁、Stub 比生产更严**？这些是不是已经在本格机制链/反例里，而不需要 (b) 新行、也不需要把 (d) 再盖一次？模块栏 `wta-api/sso` 是否只标记合同所在分包，不要求实现搬进该 jar？

## Q5 元问题

- objective_id: OBJ-59
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 一行两符号仍 uncovered；无本课 (b)；C:wta-api 已 covered；(d) SSO 两行已 covered
- difficulty: low

建议：`defer`

原因：

- **chain 两格与课 claimed 1:1；与矩阵同一行两符号对象相同。** L 合同章节齐全（四栋楼地图、直觉十二句、定义表、机制链 1–4、带 caption / 文字等价物 / 图边界的三张图、正反例、变式、误区、暂停走查、来源表 S-004/S-008/S-010/S-015/S-L059-01…08）。**主叙事站得住且与源码对齐**：api 分包四份、目录 1 法认人 3 法、system 填目录、admin 填认人、sso 只 Adapter+Port、POM 无 wta-system、店卡不抄哈希、fillView 先清、验密当场 consume、准入不禁 sso key、组装与 extras 禁票、换票无 secret、三法三时刻。不必为「两个接口名」再挖五问。
- **Lead 不可以用本课重盖 (c) wta-api，也不可以用本课再盖 (d) 授权码走廊或 L-055…L-058 HTTP。** 那些行已经 covered。本切片证据写入矩阵 (a) 该行即可（「只读合同 / 填窗人 / 哈希空槽 / 三法三时刻 / 禁票两闸已讲」）。模块栏保持 `wta-api/sso`（合同分包），不要改成 `wta-sso` 或 `wta-admin`。
- 本批可粘贴的精度项不挡住该行视为已讲：（1）零 `AdminSsoIdentityService` 单测；（2）目录测试不断言哈希 null；（3）`StubIdentity.assertClientAccess` 比生产更严；（4）`SsoTokenExtrasTest` 把两道闸绑在同一个手造 `"sso"` 视图上；（5）课 `estimated_minutes: 38` vs chain 35。不要把 rotate 缓存、PKCE 公式、Cookie 构造、厅堂 `/auth/login` 塞进本格。可选半句不是第二容器。
- 不建议 `mine-more`：b01 未证明这两格还有真实矩阵空洞；再五问只会把 L-018 rotate、L-055 门口、L-013 夹心 consume 或 L-084 `LoginHelper` 再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；可选半句（测试桩过严、无 Identity 单测）不是缺章节。不建议 `split-lesson`：未满 10 问；两份 api 合同是 chain 原设计的同一 OBJ-59；实现跨 system/admin/sso 三栋楼是本课要讲的边界，不是「超过 1 个容器却讲不完」；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 Port/(b) 行：门缝与验密副作用是 (a) 的 explained。不建议等 Identity 单测才给本行盖章。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:SsoClientCatalog | 1 法、null 语义、system 唯一实现、白名单抄写、不 set 哈希、home 并入、Adapter 一行——均与源码对齐。残留：无同名测试类；home 并入单测不断言哈希 null。 | 可选半句：口试哈希空槽以 `findByClientId` 不 set + `fillView` 首行清槽为准，不要假装 catalog 测试锁了 getter。 | defer（本格可视为已讲） |
| A:SsoIdentityService | 三法签名、主机唯一实现、当场 consume、准入不禁 sso、组装禁 key、不发 Token——均与源码对齐。残留：零 `AdminSsoIdentityService*Test`；SSO 测试桩在准入枪拒绝 `clientId=sso`。 | 可选半句：禁票以活实现为准，不要背 Stub。单测缺口不挡 (a) explained。 | defer（本格可视为已讲） |
| 矩阵 (a) 行 `SsoClientCatalog, SsoIdentityService` | 仍 uncovered。本课交付「只读用户与 Client」的合同+填窗+边界。 | Lead 可将本行标 `covered`。证据栏 L-059 + GP-L-059-b01。模块栏保持 `wta-api/sso`。不要改成实现模块名。 | defer（行可 covered） |
| (b) verifyPassword 副作用 / consume 顺序 / 禁票性状 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。Lead 若要单独性状格，走 replan，不走 b02。 | defer（未入库） |
| A:SsoClientCatalogPort / SsoIdentityPort / Adapter | 课认门缝转交。矩阵无此符号。 | 不要升成第二份 wta-api 或第三格。 | defer（未入库对照） |
| C:wta-api / C:wta-modules / C:layered-classic | 容器与登记已 covered。本课用认人实现当邻居禁令正例。 | 不要因本课再改容器状态。 | defer（L-002 / L-003 已 covered） |
| D:主路径 SSO 授权码 / 失败路径 SSO PKCE | 已 covered。本课 ASCII 只标「哪一步碰到哪份 api」。 | 不要用本课再盖 (d)。 | defer（L-055…L-057 已 covered） |
| A:SsoOAuthController.authorize / UseCase / D:fail-pkce | 门口验 S256、无用户不写码。 | 保持邻格。本课只认目录锁与准入时刻。 | defer（L-055 已 covered） |
| A:SsoOAuthController.token,revoke / B:exchange,revoke | 换票与撕票。本课只认 `buildLoginUser` 与 extras 禁票。 | 保持。 | defer（L-056 已 covered） |
| A:SsoSessionController.login,session,logout | 手环 Cookie。本课只认登录喊 `verifyPassword`。 | 保持。 | defer（L-057 已 covered） |
| A:parseAuthorizeQuery / loginWithPassword / requestAuthorize / fetchSession | 浏览器认人厅。JVM 合同 import 不到。 | 保持。 | defer（L-058） |
| A:SysClientController / A:SysSsoAppController | bind / rotate；rotate 无 `@CacheEvict`。 | 本课只留「哈希不在店卡上，故缓存陈旧对本窗无害」。管理窗保持 L-018。 | defer（L-018 已 covered） |
| B:IAuthStrategy / SysLoginService / 临时柜签发 | 门厅夹心 consume、图形验证码。 | 对照一句即可。 | defer（L-013 / L-012 / L-015 已 covered） |
| A:IdentityAccessService.login / A:AuthController.clientContext | 厅堂 `/auth/login` 与路牌。 | 保持。 | defer（L-014 / L-011 已 covered） |
| B:LoginHelper / StpUtil | 盖章写 Redis。本窗只点名 Adapter。 | 保持。 | defer（L-084） |
| C:Redis 8 / MySQL 8.4 / dao-mapper-ownership | 错次键 / `sys_client` / 主机 Mapper。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-083 / L-084 / L-085） |
| A:UserService | 另一扇查人窗。 | 不要并进本行。 | defer（未声称） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；这两格无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（矩阵同行仍 uncovered，但课已把合同/填窗/哈希空槽/三法/禁票讲完；邻格 HTTP 与管理窗已有自己的课且多数已 covered）；合格读者说得出目的（认人厅只经 wta-api 读用户与 Client）、主路径（登录验密得手环 → 目录看店 → 准入发码 → 组装+extras 盖业务票）与失败路径（目录 null、未启用 SSO、验密失败撕柜、登录域不合格、`clientKey=sso` 禁票、换票无 secret 可验）；把可选半句写进原课不会超过 40 分钟；L-055…L-058 / L-018 / L-013 / L-084 已有邻课。满 10 问前不拆。两份合同虽跨 system/admin/sso 三栋楼，仍是 chain 原设计的同一 OBJ-59、同一 `wta-api` 合同面，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-055…L-058 HTTP、L-018 管理窗、L-002/L-003 容器、L-013/L-012/L-015 门厅、L-014/L-011 厅堂、L-084 Sa-Token、未入库 Port/(b) 行以及 MySQL/Redis 容器显式建议 `defer`；对 chain 两格建议视为已讲切片；对矩阵 (a) 同行建议 Lead 标 **covered**（证据 L-059 + 本 probe），**不要**改模块栏、**不要**发明 (b)/Port 行、**不要**用本课再盖 (c)(d) 或邻格 HTTP。本课不主张等 Identity 单测才盖章、把 Stub 过严准入当成活实现、或把 rotate 缓存题扩进本行。
