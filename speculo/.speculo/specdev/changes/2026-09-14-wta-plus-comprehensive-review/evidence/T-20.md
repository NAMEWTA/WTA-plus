# T-20 本地实现与验证证据

revision79，status=review；parent/HEAD=`76dbbe84a34624234e379661a57b232529e34ed3`。implementation_commit/result_sha/candidate均为空，用户要求全部提交暂缓。未暂存、提交、推送或部署，0 Done。

## 实现与 AC-020

12 个实际 tsconfig 显式开启 noImplicitAny、strictNullChecks、strictFunctionTypes：platform http/auth/app-runtime/permission；axios/oss-upload-browser adapters；admin/system/profile/third domains；profile web-domain；Home App。工厂使用正式结果类型提供上下文，递归与回调按实际合同标注。原根配置与其他包不整体切换，不新增 ignore/exclude/any/双cast，不升级依赖或手改生成合同。

Axios 使用锁定依赖的官方类型，删除原 factory 双重断言与两处真实链测试双重断言。拦截器返回 AxiosResponse，平台 request/post/callable 出口统一取 data；真实链证明公开返回 JSON/text/null/二进制、取消、401恢复、错误分类和敏感信息清理保持。未知 headers 仅接受合法传输类型；unknown/null/非字符串错误不再二次 TypeError；非法下载值明确失败，合法 Blob/ArrayBuffer/view 字节保持，owner 总会 close。isHandledError 只将实际已处理 TransportError 收窄，不把任意真值属性伪装成该类型。

完整 Admin 编译证实旧手写 `src/types/axios.d.ts` 顶层 ambient module 覆盖官方 Axios 模块，而非依赖类型缺失。该自造 AxiosResponse 没有消费者，已删除8行旧声明；仍编译所有原源码。相关内部 mock 同步真实 AxiosResponse 包装，原下载消息、Client登录、401与敏感信息断言全部保留。EX-001撤销，项目画像记录12个目标和存量债。

System 原 public user/menu mapper 是唯一投影边界：unknown transport 校验 ID、可选文字、数组/分页总数/树结构，去除私有字段；缺失/非法身份或结构明确拒绝，不伪造空成功。合法 null 可选姓名/children、空列表和数值 ID=0 保留。Home 复用原公开 user mapper，验证成功前不写 roles/permissions/userId，避免部分身份污染；nullable名称不会写成 undefined/null。Admin context 复用已有 password-policy parser，非法 flag/policy拒绝；用户凭据边界用实际 narrowing 取 password/正整数有效期。

Profile 两个 application service 已明确 current=null，OSS adapter 已有正式签名/取消/可空ETag合同，基线无自身strict诊断；继续复用 T-14/T-18 的校验和测试，没有另建解析框架或重复parse。

## 真实验证

| 项目 | Evidence | 命令、退出码与边界 |
|---|---|---|
| 基线诊断 | T-20-strict-baseline.json / strict-neighbors.json | 对实际包强制三个开关串行编译，保留所有诊断；不是运行时故障声明 |
| 官方依赖探针 | T-20-axios-types-probe.json | 当前锁定Axios ESM request/create官方类型独立编译exit0，临时源码已回收 |
| 严格目标 | T-20-strict-final.json | 12目标CLI strict全部exit0、零诊断；之后完整workspace typecheck以这12份正式配置重验最终源码通过 |
| 实际消费者模式 | T-20-consumer-types-actual.json | Axios/System按根legacy三个false的辅助编译与完整Admin typecheck全部exit0；不替代正式strict gate |
| 原实现红灯 | T-20-boundary-red.json | 39实际失败：HTTP2、Axios8、System24、Home5；原异常与错误状态保留日志 |
| 最终全前端 | T-20-frontend-verified.json | `frontend: corepack pnpm lint`、`typecheck`、`test`全部exit0，602 Vitest+108 Node=710通过/0skip；新增64项 |
| 生产构建 | T-20-build-final.json | `frontend: corepack pnpm build:prod` exit0，Admin/Home/SSO三App全部成功 |
| 架构/事实/差异 | T-20-static-final.json | architecture:check 35workspace/0违规、Skill facts、git diff --check全部exit0 |
| 治理 | T-20-governance.json | revision79 goal-plan、ticket-control、diff实际结果 |

受影响包最终数量：Axios46、platform-http7、domain-admin56、domain-system95、Home15；原SSO、Profile、OSS、Workflow与System页面测试随全前端执行。详细计数见 T-20-verification-counts.json。没有required skip/零测试成功；E2E按本票not-required，真实Axios链是库内传输链/受控adapter测试，不冒充真实后端HTTP或浏览器端到端验证。此前业务票的真实环境验收仍由原票保存，整体候选归T-30。

三轮完整编译失败后已执行原因复核及缩小验证，后续真实编译/测试失败也全部保留，见下方历史；没有把失败run命名中的final当作通过。唯一最终全前端通过记录为 **T-20-frontend-verified.json**。

## 检查点与剩余范围

输入 T-20-input-checkpoint.json 核对363个最新上游路径；最终 T-20-checkpoint.json 保存38路径（含删除旧axios.d.ts），351非重叠上游哈希不变、12共享重叠逐项保存。HEAD不变，所有文件在登记写集内。

根三个子开关仍false，其他包尚未整体strict化。最初诊断 Admin全App236条/System web-domain47条是**修改前基线**，包含后来清理的传递依赖，未把它们宣称为当前剩余准确数量。非目标App/包自身的旧any/空值/表单问题未在本票重写；例如Admin下载宿主的旧any仍是原scope工程债。生成OpenAPI与后端接口未改变，菜单/用户请求继续GET，权限和Client参数仍由原owner提供。

## Skill Execution Records

| Skill / phase / operation | 实际输入、操作与输出 |
|---|---|
| engineering-standards / implement / apply-scope-contract | 输入真实strict诊断与现有public mapper/HTTP合同；局部unknown narrowing、合法null保留、同owner工厂类型、三项包级strict、删除无消费者ambient覆盖；没有跨domain反向依赖/平行解析框架，passed |
| engineering-standards / verify / verify-affected-contract-and-quality-gates | 最终710测试、完整类型/lint、三App构建、35workspace架构、事实检查、38路径checkpoint与上游不变；逐项AC复核，passed |

入口SHA与票据绑定一致；引用里的旧事实按当前源码修正，硬约束不删除。提交/direct-parent出口按用户要求暂缓，不勾选commit AC、不标Done。以下仅为完整实施历史。

# T-20 实施历史

revision75，363条上游哈希核对通过，全部提交暂缓。已完成9个初始包和5个相邻包的真实noImplicitAny/strictFunctionTypes/strictNullChecks CLI诊断，详见T-20-strict-baseline.json和T-20-strict-neighbors.json。未改全局开关，不把类型诊断当运行时复现。

12个限定目标为platform http/auth/app-runtime/permission、axios/oss上传adapter、admin/system/profile/third domain、profile web-domain和Home App。platform-http与OSS adapter已0诊断；其余主要是Object.freeze未给结果上下文、回调/递归返回类型。Admin236条/System web-domain47条不纳入本次整体strict启用；后续记录剩余债，不使用exclude/ignore规避。

下一步：依当前锁定Axios类型验证EX-001是否仍需，明确nullable/非法用户、菜单、ClientContext、错误与下载边界样本，补实际类型/mapper，再启用已清理目标的三个严格开关；全前端/三App回归和哈希/Skill/治理完成前不review。


## 实施过程（revision76）

- T-20-boundary-red.json：原实现实际失败 39 项（platform HTTP 2、Axios 8、System 24、Home 5），包括假 handled 收窄、空/非字符串错误再次 TypeError、非法数据转 Blob、用户/菜单缺失 ID/形状及 Home 部分身份污染；已有正常用例保持。新增测试不复制产品解析。
- 12 个 tsconfig 显式开启三项 strict；Object.freeze 使用正式结果类型，少量回调/递归返回补注解。根配置、System web-domain/Admin 全 App 未整体开启。
- T-20-strict-first：9/12 包通过，剩余是 AxiosHeaderValue 不含 undefined（使用官方 Raw header 所需 Partial 语义）和原 const never helper 的控制流收窄（改具名 never function），没有 cast/ignore 绕过。T-20-strict-final：12/12 退出0、零诊断。
- T-20-boundary-first：五个定向包全部通过；之后补真实 Axios header/nullable 输出、下载 view 字节范围与标量错误 JSON 验证，待全前端运行。
- Admin ClientContext 复用原 password-policy parser；Home 复用唯一 System public user mapper。非法 envelope 明确失败，合法 null 可选姓名/children、空列表、Profile current=null、OSS 可空 ETag 继续由各 owner 持有。
- 官方 Axios 类型编译通过，删除 adapter/实际链测试三处双重断言。拦截器现在返回 AxiosResponse，公开 request/post/callable 统一返回 data；单测 fixture 同步真实第三方合同，外部 payload/取消/私密信息清理仍由真实 Axios 链验证。EX-001 与项目画像同步修正。

- T-20-frontend-first：lint exit0，完整 typecheck exit2，暴露非 strict 消费者 Workflow 编译源码时 unknown 与 undefined 的收窄差异；System user envelope 的 code 出口改为显式 number/undefined，声明 UserQueryResponse<unknown>。这是上下游不同 strict 配置的真实编译问题，未放宽消费者配置；test 未运行，需重验。

- T-20-frontend-second：lint exit0，typecheck exit2；Workflow 消费者已通过，最终 Admin 全App暴露旧 `src/types/axios.d.ts` 顶层 ambient module 覆盖真实 Axios 类型。rg 证实自造 AxiosResponse 无调用方，只有官方适配器使用 AxiosResponse。revision77 先登记精确路径，再删除旧声明；不是 exclude/ignore 或删测试，所有源文件仍参与原完整检查。


### 三轮完整门禁失败后的检查与重排

T-20-frontend-first/second/third 均 lint exit0/typecheck exit2/test未运行。第一轮暴露非strict消费者对unknown/undefined的控制流差异；第二轮Admin ambient axios覆盖模块阻断官方类型；第三轮恢复官方类型后，只剩header在关闭strictNullChecks时的unknown分支。共同模式是单包strict通过不能代表由App配置重新编译依赖源码时通过，不是业务行为测试被删或降低要求。

第三轮后停止直接重复全仓：检查所有新增null/unknown分支，header出口改为独立正向typeof分支及字面null/undefined分支；保持真实值与两种消费者编译模式。下一轮先执行Axios/System的strictNullChecks=false反向编译与完整Admin消费者typecheck，全部通过再恢复完整门禁。恢复条件为定向类型、完整frontend及3App build均exit0；任何后续失败保留原日志并回真实源码，不恢复ambient模块或放宽规则。

- T-20-consumer-types 第一项 exit1，混合 noImplicitAny=true/strictNullChecks=false 还暴露旧 recovery catch 回调返回 undefined 的隐式 any。直接补 `(): void`，不改错误恢复行为；后两项未运行。定向重验记录 separate，非全仓盲重跑。

- T-20-consumer-types-final：Axios 混合配置通过；System 的未修改 open-api/index.test.ts expiresAt:null fixture 触发混合配置隐式any，Admin项未运行。复核真实根/Admin配置后，定向反向编译改为同时关闭三个开关以模拟实际legacy消费者，而不是扩大产品写集修复仓内不存在的混合模式。12个正式包配置仍三项true，完整strict及workspace门禁仍为必要条件；该辅助命令不替代它们。

- T-20-consumer-types-actual 三命令均exit0；T-20-frontend-final lint/typecheck均exit0，test exit1（Admin 69pass/2fail）。失败仅Admin HTTP fixture仍假设Axios service直接返回业务data；正式实现已按官方AxiosResponse统一unwrap，因此revision78登记并更新2个测试中的mock shape，全部原断言保留。包内真实Axios链、Home与其他所有已运行包通过。最终再次完整验证以免把失败记录标绿。
