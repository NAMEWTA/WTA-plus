# T45 private UIv4 fixture guard — frozen read-only review

Reviewed `/tmp/wta-t45/ui-v4-offline-freeze.json` (SHA256 `c52320b95efd49e785b8380ec24f005f3b61758dadefaa320cbfe2babec3e298`) and runner `/tmp/wta-t45/run-minio-diagnostic-ui-v4.py` (SHA256 `afb76c7321027459d60318a78b88716637de0fcf862e49973f58b03cd74e2859`) against v3. Offline record says 10 runner tests, four unchanged server tests, `py_compile=0`, static preflight=0; `real_execution=not_run`. I did not execute tests/services or edit the repository.

**No static blocker in the new guard.** V4 preserves the v3 product/browser inputs and all positive/negative assertions. After the real invalid-policy POST has set `invalid_policy_fail_closed=true`, it obtains an authenticated GET of the UI's exact page 1/size 10. `safe_oss_config_page` keeps only status/business code, total, row count and booleans for the owned invalid/public/private IDs plus unknown-policy count; it rejects malformed/duplicate IDs, unrelated invalid policies and a page it cannot fully account for. `require_owned_bad_before` requires the exact owned bad row and both positive rows to be present, so the former R1 first-page contamination is now a **network precondition**, not a static assumption. Only then does `exact_owned_bad_delete_sql` target the generated owned ID in its reserved range, generated owned key and `access_policy='9'`; the SQL step requires exactly one affected row. A second authenticated GET must prove bad absent, positives still present, unknown-policy count zero and total/row count decreased by one before Chrome starts. The old HTTP negative evidence is retained; the product continues to reject unknown editable policy values.

The response is handled in memory; the retained `browser_fixture` object contains bounded counts/booleans, not raw rows, credentials or endpoints. Failure at any precondition leaves the original two-case Chrome gate unclaimed. Fresh owned DB and the backend's uncached mapper page query support observing the exact delete, while the still-running registry diagnostic snapshot is separate from the list response. The strict `total == len(rows) <= 10` condition deliberately fails closed if a fixture later grows beyond one page; this can require a future owned fixture/navigation adjustment, but does not silently make an off-page positive pass. The offline tests cover exact deletion, wrong page/unowned policy rejection, safe output and hash-fenced browser inputs.

This review authorizes no inference that UIv4 passed: Lead must confirm the frozen runner hash and be355 exact clean source/JAR/dist proofs, then inspect v4's actual `browser_fixture.before/after`, two Playwright cases each one attempt with zero skip/fail/flaky, and full cleanup. A passed browser run may be reused with byte-equivalent be355 product inputs; the earlier C3 and R1 browser failures remain historical.
