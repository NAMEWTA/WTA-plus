# Evidence: T-01 — 手机号写入必填与旧账号登录兼容

- Change：2026-09-19-remote-issues-phone-ai
- Ticket：<Path>{roots.state}/specdev/changes/2026-09-19-remote-issues-phone-ai/ticket/01-required-phone-write-paths.md</Path>
- Spec：<Path>{roots.state}/specdev/changes/2026-09-19-remote-issues-phone-ai/spec.md</Path>
- Goal：<Path>{roots.state}/specdev/changes/2026-09-19-remote-issues-phone-ai/goal-plan.md</Path>
- Lead / implementation owner：codex-root；workspace=current，branch=main。
- Base / implementation / candidate / result：86154daa4c67fa02ac13439c038a09caf5e6308a / pending / not-applicable / pending。
- 状态：blocked（待本地提交及基线修复方案确认）；这是本地实施记录，不是不可变提交后的正式 Done Evidence。

## 1. 实现摘要

新建和注册必填有效大陆手机号。资料编辑、个人保存和覆盖导入在实际 Service 写入前验证最终号码；null/省略保留原有效号码且不回写读取快照，显式空串/空白和原无有效号码拒绝。非空输入先标准化再进行入口原有唯一性检查，Service 使用同一标准化规则。共享更新 BO 只增加格式约束，不增加全局 NotBlank。注册动态事务与系统用户/授权持久化保持一致。

Admin/Home 增加手机号输入，domain 注册参数和请求显式传值；管理用户编辑/新增页面必填。个人资料页面复用原有必填，三个 Admin 表单统一补充 whitespace 拒绝空白。独立登录、密码重置、禁用合同不增加手机号约束。无 DDL、索引或运行库变化。

## 2. Lead Dispatch And Candidate Return

Lead direct，只有 codex-root 修改产品与 SpecDev。授权来源是用户激活 I 并要求完成整个 change；提交及受保护基线处理已发送具体异步方案，尚未得到答案。没有创建 commit 或推进外部分支。

只读 phone_facts 提供写入口/动态事务接缝；ai_facts 提供 validator 四处路由及 HTTPS 专项运行接缝。Lead 回读原始文件并实际运行本记录内命令。代理没有写文件、运行 E2E 或生成验收结果。

## 3. 修改范围与路径所有权

实际写集按 T-01 frontmatter；Map revision 5 增加既有 OSS 注册测试、明确浏览器专项配置以及原真实 PasswordAuthStrategy 测试的精确单文件归属。源码只写 T-01 手机号合同。API 生成物、SQL、锁文件、AI 组合由后续票独占，尚未修改。

受保护的原始 AGENTS/config/validator/test 删除状态保持；其基线 hash 在 <Path>{roots.state}/specdev/changes/2026-09-19-remote-issues-phone-ai/evidence/implementation-baseline.json</Path>。没有以 stash/删除/修改 clean-source 检查取得绿色。

## 4. 验收与合同映射

| AC | 验证接缝 | 本地事实 | 完成状态 |
|---|---|---|---|
| AC-001 | 双 App / domain / RegisterBody / 真实注册 Service | DTO、业务、domain 红绿通过；两端手机号浏览器 2 pass；真实 Spring MVC/HTTPS 浏览器 6 pass | local-pass |
| AC-002 | 管理新增/编辑 / 个人资料 / 实际 MySQL | 新建、原空/原有效、null/显式非法、标准化、无部分写入通过；UI 10 pass（包含双端注册） | local-pass |
| AC-003 | 真实 validator / SysUserImportListener / Service / Mapper / MySQL | 4 个失败行未写入；3 个后续合法新增/更新成功；不回显号码和密码 | local-pass |
| AC-004 | 真实 PasswordAuthStrategy.authenticate / 浏览器 Client 入口 | null/空串/空白三例成功认证，原登录域和密码断言保留；默认全量回归和 Client 浏览器 7 pass | local-pass |
| AC-005 | 入口唯一性 / 公开兼容 / 安全错误 / 事务 | 规范化重复号三入口拒绝；真实注册授权失败回滚；公开 Java 签名保持 | local-pass |

## 5. Workspace Verification

所有运行地点 current-workspace；Node 24.21.0 / Java 21 / pnpm 10.34.5；pnpm 根脚本使用任务专属 Corepack shim PATH，避免系统 pnpm 9 接管子进程。以下是 Lead 实际本地运行，尚未作为固定实现 commit 的集成证据。

### 窄切片 red → green

| 行为 | red（exit 1） | green（exit 0） |
|---|---|---|
| RegisterBody 必填 | 4 failure / 0 error | 4 pass |
| 注册 Service 直接调用缺失/非法号码 | 5 failure / 0 error（加上原开关测试共 7） | 7 pass |
| admin/registerUser 新建写入 | 2 failure / 0 error | 2 pass |
| admin/profile 最终手机号无效 | 12 failure / 0 error（共 14） | 14 pass |
| 管理 BO 格式约束 | 3 failure / 0 error（共 8） | 8 pass |
| 三个 Controller 标准化后重复号码 | 3 failure / 0 error | 3 pass |
| domain 请求传 phone | 1 failure：请求缺字段 | 1 pass |
| domain 非法 phone 不消耗验证码/不发请求 | 6 failure：错误接受请求 | 7 pass（含合法） |
| Admin/Home 手机号输入 | 两个独立 UI red：缺手机号输入 | 双端 2 pass |
| 注册动态事务反向验证 | 暂时移除外层 DSTransactional 后，授权失败留下 1 个用户，期望 0 的断言失败 | 恢复生产注解，MySQL 四场景 4 pass / 0 skip |

Maven 定向命令（cwd=backend）：

```bash
./mvnw -pl wta-admin -am test -Dtest=PhoneControllerUnitTest,PhoneValidationUnitTest,PasswordWritePathUnitTest,SysRegisterServiceRegistrationUnitTest,UserPhoneWriteServiceTest,SystemUserAvatarOssOwnerUnitTest -Dsurefire.failIfNoSpecifiedTests=false
```

exit 0：system 14 + admin 22 = 36 pass / 0 skip。未匹配的 reactor 模块不是本合同执行数。

真实 MySQL 命令（cwd=backend，任务动态分配的 loopback 端口；任务自有数据库 namewta_phone_test_i）：

```bash
./mvnw -pl wta-admin -am test -Dtest=PhoneRegistrationMySqlIntegrationTest,PasswordAuthStrategyTemporaryUnitTest -Dsurefire.failIfNoSpecifiedTests=false -Dphone.mysql.integration=true '-Dphone.mysql.integration.url=jdbc:mysql://127.0.0.1:<owned-port>/namewta_phone_test_i?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai' -Dphone.mysql.integration.password=<owned-test-password>
```

exit 0：4 个真实 MySQL 用例 + 5 个认证用例，0 skip。MySQL 镜像 mysql:8.4.9；只初始化源码 10-cde-base-ddl.sql 的 sys_user/sys_user_type/sys_user_type_rel 三表；独立连接读取提交结果。密码策略、Client 读取、无头像 OSS 是明确夹具；用户/关系 Mapper 和事务不是 mock。反向验证仅在本任务工作区临时恢复旧注解，已恢复 DSTransactional；不声称该代理 fixture 包含完整生产 Spring 自动配置。

前端（cwd=frontend，PATH 指向任务 Corepack shim）：`corepack pnpm test`、`corepack pnpm typecheck`、`corepack pnpm lint` 均 exit 0。完整 `pnpm build:prod` 在 Client 专项 webServer 链中执行成功（后续 preview 及 7 用例实际启动），包含依赖和三 App；末次表单 whitespace 修复后 phone 专项重建 Admin/Home，并重跑受影响两个包 typecheck 与六文件 Oxlint，均 exit 0。首次裸子进程 pnpm 9 报 engine 不兼容属于环境失败，修正 PATH 后通过，没有修改依赖/规则。

## 6. 双轴审查

标准轴、规范轴均 pending。需要用户确认本地 commit 后取得不可变实现 checkpoint；目前不能以工作区自查冒充固定点双轴 review。

## 7. Integration Verification

Parent before=86154daa4c67fa02ac13439c038a09caf5e6308a；method=direct-parent；candidate 不适用；implementation/result pending。E2E required，Lead 本地预验收如下：

- phone 专项：两端注册 + 管理新增/编辑 + 个人资料，分别独立验证缺失和纯空白输入及合法保存，共 10 pass / 0 skip。真实页面/domain，HTTP 为 Playwright fixture。
- registration 专项：任务自有双随机 HTTPS 端口、真实生产 App，`playwright.registration.config.ts` 10 pass / 0 skip，涵盖开关、验证码、重复提交、取消、网络/业务失败恢复。API 为 Playwright fixture。
- transport 专项：真实 Spring Auth MVC + HTTPS 双 App，1 JUnit + 6 browser pass / 0 skip；持久化/登录/Token 是既有 fixture，不冒充真实数据库或认证核心。
- Client 专项：7 pass / 0 skip，覆盖失败关闭、公开策略、个人密码、菜单恢复、通配权限/无关权限拒绝、401 单次恢复。使用默认配置的任务临时 Chrome override，因为环境只安装系统 Chrome；没有忽略用例。
- public accessibility 定向 Home registration：1 pass / 0 skip，真实 HTTPS/键盘输入和失败后重试。
- current-parent 固定提交后的集成检查及 git clean/result 关系尚未完成。

### Failure History And Lead Recovery

均为本地 implementation 阶段，正式 integration attempts=0。

- 首次 UI 默认入口被 engine 环境失败拦截，不计业务 red；专项双 App 配置和正确 pnpm 修复后执行。
- Home 必填星号影响可访问名称，输入增加明确 aria-label 后通过。
- 管理界面新增/编辑回归夹具遇 Element Plus 隐藏 checkbox/combobox 占位层，按真实用户可见 label/placeholder 修正定位；不使用强制 click 或删除断言。
- 新 MySQL 测试初次编译失败为 Converter 构造重载及 AssertJ 单参数签名，修正测试后实际四例通过；不是产品 red。

## 8. 偏差与决策

产品合同无偏差。Map 3–5 为必要相邻 fixture/专项入口收敛；LOG-009 记录本地实施授权与提交等待。T-03 需消费 RegisterBody 的必填和新增格式约束并统一正式更新当前 OpenAPI；本票不修改该生成物。

## 9. 残余风险与交付定位

本票计划内本地代码、测试、构建和 Lead E2E 已完成；不可变 checkpoint/双轴 review、正式集成仍待确认后完成。旧 AI 数据、两个 Maven 占位和六份 SQL 是 T-02/T-03/整体 Gate 的目标，本票不声称完成。尚未产生发布物、推送、部署或远程 Issue 回写。

## Skill Execution Records

四个 implement 绑定已实际执行；verify 绑定仍因不可变提交/交付门未满足而 failed，不代表测试失败。正式完成时重新核验记录。

```json
[
  {
    "id": "engineering-standards",
    "sha256": "dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9",
    "phase": "verify",
    "operation": "verify-scope-contracts-and-quality-gates",
    "status": "failed",
    "evidence": [
      "已执行本票源码边界/命名/Java-Vue-MyBatis/安全/数据/依赖检查和质量门，详见 §§3–5 及 Local Verification Closure；提交和固定点双轴审查尚未授权完成，交付门保持失败。"
    ]
  },
  {
    "id": "namewta-fullstack-development",
    "sha256": "675c053c11d8b22cd394c875f48688242d8e8328dd14e69657f5d5b12f2af68b",
    "phase": "implement",
    "operation": "implement-vertical-slice-and-verify-boundaries",
    "status": "passed",
    "evidence": [
      "实际贯穿双 App 表单→domain mapping→RegisterBody/管理 BO→Controller→既有 classic Service→Mapper；真实 MySQL 4 用例和 34 浏览器用例按明确 fixture 边界验收，见 §§1、4、5、7。"
    ]
  },
  {
    "id": "java-api-compatibility",
    "sha256": "b90f5592e75b3f757f52649a16f78e92850fee0ccac9f12619d3d7aa94bd7aca",
    "phase": "implement",
    "operation": "preserve-public-signatures-and-review-validation-evolution",
    "status": "passed",
    "evidence": [
      "RegisterBody/SysUserBo 保留字段和 public Java 签名，仅收紧新增必填及格式；共享更新省略仍允许；所有现有调用方编译、默认 893 discovered、legacy 认证三例及 null 更新 MySQL 通过；OpenAPI 当前快照未手改，由 T-03 更新。"
    ]
  },
  {
    "id": "wta-module-guide",
    "sha256": "bb57a781314abe316f06ba9538f62043f1bd053904c0832969e575c09731a7a3",
    "phase": "implement",
    "operation": "resolve-module-owner-and-public-entrypoints",
    "status": "passed",
    "evidence": [
      "已核验 System 为 classic 模块，写入规则落既有 Controller/Service 与纯 policy；调用方闭包只有注册、管理、个人资料和导入，未新增跨模块绕行；后端 full/core jar 清单验证通过。"
    ]
  },
  {
    "id": "wta-common-modules-guide",
    "sha256": "e92775ce47af41bd33c1b3293d7f8a3185fdd739b9b588519e043c654f678d98",
    "phase": "implement",
    "operation": "resolve-common-validation-and-vendor-dependency-boundaries",
    "status": "passed",
    "evidence": [
      "复用 common-core ValidationUtils/ValidationFormat 与已有 Excel listener、Mapstruct、动态数据源事务；未引入新库或平行校验器，真实 validator/import/MyBatis/dynamic transaction 已执行，见 §5。"
    ]
  }
]
```


## Local Verification Closure

- 默认 `./mvnw test` exit 0：893 discovered，772 executed/pass，121 属性门控 skip；完整跳过清单在 <Path>{roots.state}/specdev/changes/2026-09-19-remote-issues-phone-ai/evidence/T-01-test-counts.json</Path>。本票 required MySQL 与 HTTPS 已另行激活，分别 4 和 1 JUnit / 0 skip；未声称其余外部服务已验证。
- `./mvnw -Pbundle-full -DskipTests package` + `bash scripts/ci/verify-admin-bundle.sh full` 均 exit 0；`./mvnw clean package -Pbundle-core -Dmaven.test.skip=true` + `bash scripts/ci/verify-admin-bundle.sh core` 均 exit 0。测试已独立执行，打包跳过测试不计为测试通过。
- 前端全量 test exit 0：Vitest 621 + architecture Node 101 + OpenAPI Node 7；typecheck/lint exit 0。
- `corepack pnpm --filter @namewta/tooling-openapi openapi:check` exit 0，仅证明旧捕获快照与生成物一致；不证明最终后端 DTO 已重新捕获。T-03 仍必须更新当前正式合同。
- 34 个浏览器用例在 5 个专项完成：phone 10、registration 10、transport 6、client-auth 7、Home 注册键盘恢复 1；未将无匹配或 server 启动失败算通过。
- 独立静态预检发现 Admin 注册空白手机号没有错误提示，后续独立 fresh-page red 1 failure 复现；个人资料 pure-blank red 1 failure 亦复现。三个 Admin 表单增加 whitespace 必填，phone 10 全绿。最初在空串错误之后复用同一表单测试空白可能观察到旧错误，已改为独立页面参数用例，防止假绿。
- Client 401 旧夹具依赖首页隐式请求通知收件箱，当前没有该请求而失败；改为个人资料必然触发的受保护 GET 并返回 401，原单次提示/单次 logout 断言保留，7 用例全绿。不改产品认证逻辑。
- 任务 MySQL 容器 3e6110c657a0477fd0e580ce1bcd50c95bb5b0cd70c50e1342853ca60600cb7e（label phone-ai-i）已精确销毁；测试 HTTPS 服务和 4173/4174 preview 端口全部退出。未操作其他容器或运行数据库。
- 当前 4 个受保护用户改动 hash/删除状态与 implementation-baseline 完全相同；git diff --check exit 0。
- 本地可回读代码文件清单/摘要在 <Path>{roots.state}/specdev/changes/2026-09-19-remote-issues-phone-ai/evidence/T-01-local-source-fingerprints.json</Path>。这不是 implementation commit，不能代替 I 的固定点审查/集成门。

SpecDev 当前工件校验：`--stage tickets` exit 0 / 0 errors / 0 warnings。整 change 的 `--stage implement` exit 1 / 2 errors：尚未启动的 T-02/T-03 无 workspace execution record。这与整体未完成一致，不创建虚假执行记录换取绿色。最初 blocked 状态仍带 ready=true 的结构冲突已修正；T-01 ready=false。受保护 validator 的 goal-plan 路由修复仍等待原方案授权。
