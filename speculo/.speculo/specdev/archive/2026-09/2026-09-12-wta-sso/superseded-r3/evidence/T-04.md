# Evidence T-04

- Goal: FirstPartySsoProvider first slot; context `ssoEnabled`/`ssoAuthorizeUrl`/`authMode`.
- Commands:
  - `./mvnw -pl wta-admin -am test -Dtest=AuthClientContextSsoUnitTest,PasswordPolicyBoundaryUnitTest -Dsurefire.failIfNoSpecifiedTests=false` exit 0
  - `pnpm --filter @namewta/domain-admin test` exit 0
- Coverage: AC-001/014/015/023 context contract. Default `both` keeps `POST /auth/login`.
- Residual: live login-page slot order not screenshotted (no 4174).
