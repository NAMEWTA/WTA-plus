# T-07 evidence

HEAD `450c9d11549d16d9da4e809d57f19f6fb9741a01` (parent `268879cb`) closes AC-007. The product SSO callback path was already present. The journey harness had to rebuild admin-web and home-web before serving them, because the default production dist uses context `/` and API `/prod-api`, while this ticket's browser journey serves `/admin` and `/home` with `/api`. The process wait is 600 seconds so twelve serial Chrome cases can finish; Playwright still fails a stuck test on its own timeout. Tracked `.env` files were not changed. AC-007 stays in the T-30 combined run. The adjacent OIDC change was not modified.

## Command

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=SsoHttpsSessionIntegrationTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Dsso.mysql.integration.url='jdbc:mysql://127.0.0.1:33111/namewta_sso_test_t07?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai' \
  -Dsso.redis.integration.port=33112 \
  -Dnamewta.repo.root=/srv/WTA-plus \
  -Dsso.journey.integration=true
```

Exit 0. Isolated MySQL 8.4.9 database `namewta_sso_test_t07` and Redis 8.6.3 were removed after the run. `SsoHttpsSessionIntegrationTest`: tests=1, failures=0, errors=0, skipped=0. The test printed `T-07: 12 real Chrome App callback/recovery scenarios passed with Redis/MySQL SSO`. Playwright: 12 passed, 0 skipped, 18.9s.

The twelve cases are admin and home, each covering the registered return path, an opaque state round trip, an expired code with a fresh authorization, a network failure that is not replayed, a wrong verifier, and a wrong state that fails before exchange. Consumed authorization codes were 12. The UI assertion rejects the fixture business token text, and failure cases clear the callback query.

```bash
cd frontend && pnpm --filter @namewta/platform-auth exec vitest run src/sso.test.ts
```

Exit 0. `src/sso.test.ts`: tests=13, failures=0, skipped=0. Five of those cases reject an unsafe returnTo (`https://outside.example/`, `//outside.example/`, `/\\outside.example/`, `/%2f%2foutside.example/`, and a path containing a newline) before navigation and before any pending record is stored.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9` and namewta-fullstack-development `675c053c11d8b22cd394c875f48688242d8e8328dd14e69657f5d5b12f2af68b`, verify phase. No production controller, API, or SQL change. The harness stays in the existing SSO integration test, which was outside the original ticket write set; that deviation is recorded on the ticket.
