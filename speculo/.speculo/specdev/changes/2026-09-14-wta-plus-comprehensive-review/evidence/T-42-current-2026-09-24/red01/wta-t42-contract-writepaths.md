# T42 HTTP/OpenAPI 与六 SQL 预登记路径（只读）

固定产品基座 `5417c257130216e2b283ca933d9496d76c10f46a`，治理激活 revision220。仅核路径/消费者；未构建、运行服务或修改仓库。

## HTTP 与生成合同

- 真实公开入口是 `backend/wta-modules/wta-notify/src/main/java/org/namewta/notify/controller/admin/NotificationController.java:29` 的 `POST /notify/notification`，`@RequestBody NotificationCommand`，权限 `notify:notification:submit`；新增字段的 OpenAPI schema 来源是 `backend/wta-api/src/main/java/org/namewta/notify/api/NotificationCommand.java`。不必为仅新增 record 字段改 Controller 路由或权限，但应在真实 HTTP 测试验证 JSON 字段绑定与失败关闭。现 `backend/wta-admin/src/test/java/org/namewta/test/notify/NotificationModeOpenApiTest.java` 可扩 schema 断言，`.../NotifySupportedModeIntegrationTest.java` 既有 HTTP 正例 `commandJson` 故意省略附件；若字段仍允许缺省并规范化为 `[]`，这一路保持正常；若标 `required=true`，该正例和所有旧 HTTP 调用都会变成新必填合同，须显式迁移并验证，不能仅靠生成类型推断。
- 正式生成写集是 `frontend/packages/api-contracts/openapi/current.json`、**新** `frontend/packages/api-contracts/openapi/revisions/<新revision>/source.json` 与 `provenance.json`、`frontend/packages/api-contracts/generated/openapi.ts`。现 current 指向 `0c1c94bf47083a6b7a3620c9494ee23bd629321ac7d8c3d7963f8c840343a477`，旧 revision 不应覆盖。必须用新 full JAR 的完整 `/v3/api-docs` 执行 `frontend/tooling/openapi` 的 `openapi:fetch -- --source <owned-loopback-url> --backend-commit <新SHA>`、`openapi:generate`、`openapi:check`，并核实 `paths['/notify/notification'].post` 请求体引用的 `components.schemas.NotificationCommand` 中新字段类型/required 与真实 HTTP 一致。`frontend/packages/api-contracts/src/index.ts` 和生成器源码目前没有因该字段必须改动的证据。测试需留 source/JAR 同源、原文 hash、schema diff 无意外路径丢失。
- 当前生成 `NotificationCommand` 仅 `priority` 必填；`frontend/packages/domains/notify/src/transport.ts` 只有查询/重试/取消等方法，未提供 `POST /notify/notification` 提交；仓内非生成前端源码没有消费 `NotificationCommand` 或 `submit_3`。所以新增**可选、缺省空列表**字段不会迫使当前前端 domain/page 改写，前端仍需 typecheck/openapi:check。若要标必填，必须单独承认这是 HTTP 破坏性改变并更新真实外部调用/测试；前端没有当前提交适配器可作为已迁移的证据。`frontend/packages/domains/system/src/monitor/types.ts` 的 `NotifyLogVO.attachmentOssIds` 属旧监控模型，非此提交传输类型，不应借它绕过新合同。
- Java record 新参数与 HTTP optional 是两个问题：仓内 12 个 `src/main` 直接构造调用点必须按 Java 编译合同增加空列表或真实附件。精确文件：`backend/wta-admin/src/main/java/org/namewta/web/controller/{CaptchaController,AuthController}.java`；`backend/wta-modules/wta-demo/src/main/java/org/namewta/demo/controller/{MailSendController,SmsController,WebSocketController}.java`；`backend/wta-modules/wta-workflow/src/main/java/org/namewta/workflow/service/impl/FlwCommonServiceImpl.java`；`backend/wta-modules/wta-profile/wta-profile-enterprise/src/main/java/org/namewta/profile/enterprise/service/EnterpriseTransferService.java`；`backend/wta-modules/wta-profile/wta-profile-person/src/main/java/org/namewta/profile/person/service/PersonRebindNotificationService.java`；`backend/wta-modules/wta-notify/src/main/java/org/namewta/notify/service/{NotifyNoticePublisherService,NotifyTestSendService}.java`。这些文件是当前 `rg 'new NotificationCommand(' .../src/main` 的实际**文件**清单（12 处构造，10 文件）；另有对应测试构造需通过全 reactor 编译迁移。只改 Demo 的 map 无法传递 typed 附件。

## 六 SQL 初始化器与静态测试

- 如果 T42 如设计新增**一个**真实 `notify_intent_attachment` 表，`release-artifacts/docker/infrastructure/mysql/init/10-cde-base-ddl.sql` 需建表；`50-cde-base-dml.sql` 需独立 `demo-mail` 场景/模板绑定种子（不为新关系伪造业务行）。六文件根保持同一组，不新建第七 SQL。最终真实新表数须从 fresh owned MySQL 校验，按一个新增表预计从 103 变为 **104**。
- `release-artifacts/scripts/init-mysql-container.sh:10-11` 的说明与 `EXPECTED_TABLES=103` 必须按实际新表数更新，否则受保护初始化器在 :189 的计数门禁拒绝 fresh schema。三处硬编码静态断言也要同步：`release-artifacts/tests/release-integration-contract.test.mjs:74`，`release-artifacts/tests/release-config.test.mjs:357`，以及 `release-artifacts/tests/ai-retirement-contract.test.mjs:37` 的 `new Set(tables).size === 103`。`init-nacos-mysql-container.sh` 的 10 是独立 Nacos 库，不随 T42 改。
- `release-artifacts/tests/notify-baseline-contract.test.mjs:10-17` 已枚举现有 Notify 表，宜把新物理关系表纳入该断言，并增加真实 PK/源与快照关系、必要索引/唯一键的针对性断言；该文件 :137-166 还校验渠道账号与场景绑定，新增 `demo-mail` 种子应保持预置账号停用/无隐式账号绑定。`release-artifacts/tests/mysql-baseline-contract.test.mjs` 只锁六文件名/来源，`mysql-sql-contract.test.mjs` 只锁标记与测试 SQL seam，未发现随 103→104 **必须**改动的断言；继续跑它们即可。

上述新增 `frontend/packages/api-contracts/` 生成四类路径、`release-artifacts/scripts/init-mysql-container.sh` 与三处 `release-artifacts/tests/` 硬编码测试如尚未在 revision220 写集，实施前须精确补登。不要预填未知 `<新revision>`；由 live capture 算得。
