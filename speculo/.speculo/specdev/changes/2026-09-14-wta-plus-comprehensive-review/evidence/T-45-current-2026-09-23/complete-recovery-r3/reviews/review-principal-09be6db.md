# T45 recovery R3 principal parser — fixed-source incremental review

Fixed parent `f5cee5dc2e5976257765e661cd4ea40d3ff09575`, candidate `09be6db6c3bace8594f7db0fe49a221e3cc5f140` (tree `2d5459e7dc77ec4120516d97a63de17eee390be4`). The worktree was clean. The product delta contains only `AbstractOssClientImpl.java` and `OssAccessDiagnosticUnitTest.java`; the other 23 T45 product paths and C3 findings are inherited from the prior fixed reviews. I did not run tests/build/services or edit the repository.

**Static verdict: no blocker in this two-file correction.** `AbstractOssClientImpl.java:1248–1265` now calls `asText()` only after `isTextual()` for a scalar principal or a scalar AWS value. Object-valued `Principal:{"AWS":["*"]}` goes through `isObject`, one-property `AWS`, one-element array, then textual-star checks; it no longer invokes Jackson 3's throwing ObjectNode/ArrayNode scalar coercion and can yield the recognized public principal. Missing/extra fields, nested object, numeric value, or a multi-element AWS array return false and hence remain `COMPLEX_POLICY` through the existing caller. This is a private type-aware parse fix, not a new IAM grammar, global mapper change, or write authorization change. The probe's earlier `INVALID_POLICY` for a semantically recognized object principal is directly addressed.

The new unit tests at `OssAccessDiagnosticUnitTest.java:121–151` pass policy strings through the existing `diagnoseAccess` path and real `JsonUtils` parser with mocked S3 responses. They assert fixed `POLICY_READ=ALLOWED/POLICY_ALLOW` for both AWS textual `"*"` and single-element `AWS:["*"]`, and `UNKNOWN/COMPLEX_POLICY` for nested AWS object, numeric AWS and two-element array. Existing textual `Principal:"*"`, Deny precedence, timeout, anonymous request and domain URL tests are unchanged. This is a meaningful regression for the local Jackson 3.1.4 failure; it does not replace the fresh owned MinIO JUnit consumer.

At this static checkpoint, Lead's new 170-class targeted run, backend default/full, both fresh real MinIO JUnit consumers, equivalent-source frontend/dist, UIv5 and core/static were pending. The prior two-JUnit run `/tmp/wta-t45/oss-two-runs/0627f8329450e8f7/result.json` remains failed at Readiness `POLICY_READ` while AccessUrl passed. The policy-shape probe `/tmp/wta-t45/policy-probe/runs/fb6caa8cfb7564b2/result.json` is root-cause evidence, not a passing replacement for that JUnit. Final AC-045 requires the new actual results and cleanup proof.
