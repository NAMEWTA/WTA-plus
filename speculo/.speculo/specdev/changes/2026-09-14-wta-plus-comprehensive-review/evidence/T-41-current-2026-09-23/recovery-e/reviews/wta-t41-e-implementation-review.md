# T-41 E recovery candidate — independent implementation-axis review

## Fixed boundary and decision

Source E `a0dcbac8fd33a754c74c154551f7090a8878fb48`, tree `0edf8653fede967b034662afd95559283efe08e7`; compare D `d93eb1d8d6095464a9606619f4161b3dd4ef35c7...E`. **The static harness repair matches the diagnosed failures, but E has not passed required browser acceptance.** The only D→E frontend paths are `frontend/e2e/run-inbox-paged-real.py` and `frontend/e2e/test_run_inbox_paged_real.py`; production backend, frontend application and public contract source did not change. This review did not run builds, services, browser or repository tests.

## Reproduction-to-repair correspondence

- The owned diagnostic at `/tmp/wta-t41/sql-diagnostic-runs/dac0c6bfd5c2159b/result.json` proved four original comparisons fail `ERROR 1267 (HY000)` and the same-value `utf8mb4_general_ci` controls pass. E's `compared_hexsql()` at runner lines 410–412 leaves generic `hexsql()` unchanged and is called only by the A/B `owned_user_id` predicate (lines 414–421), Admin `owned_admin_client_id` predicate (lines 423–430), and `verify_seed` title `LIKE` (lines 505–509). This matches all four observed query failures from three source expressions. No schema/default/session collation or business SQL was broadened.
- `random_login_password()` at lines 125–128 creates 24 alphanumeric characters; `owned_bcrypt_hash()` enforces that exact length and `$2a$` verification at lines 131–137. `account_env` now uses it for A and B at lines 785–786, within `PasswordLoginBody.password`'s 5–30 bound (`backend/wta-api/src/main/java/org/namewta/system/api/model/PasswordLoginBody.java:27`). `random_password()` remains 40 characters for the separate root/app/Redis/MinIO secrets at lines 120–123 and 781–784. Login secrets stay in the child environment and existing redaction list, not SQL plaintext or argv.
- `mysql(..., stage=...)` at lines 272–291 requires a fixed `MYSQL_STAGES` member (lines 44–49), keeps successful tabular output, and on failure retains only stage/exit/numeric MySQL error/SQLSTATE via `OwnedSqlError.safe_details()` (lines 51–64). Timeout also becomes a bounded stage failure; raw stderr/SQL are not placed in the exception or result. The top-level exception handling (lines 1014–1021) records only those fields for this typed error. All production-runner `mysql()`/`inbox_counts()` call sites now pass an allowlisted stage, including initial lookup, seed, snapshots and failure check. Existing strict counts/ownership, role, 501/481 and B2, timestamp preservation, exact-source/JAR and owned cleanup gates remain intact.
- Offline test deltas cover 24-character BCrypt, separate 40-character infrastructure secrets, precisely three collated source predicates, absent/duplicate identities, wrong shared owner, numeric-only diagnostics with a synthetic canary, unknown error and timeout, and preserved role fixture assertions. The Lead's fixed `dispatch02-offline-handoff.json`/log records `python3 frontend/e2e/test_run_inbox_paged_real.py`, exit 0, **24 tests**. I did not rerun this repository test while Lead's package ran.

## Independent production-dist and private preview proof

I ran the read-only `/tmp/wta-t41/verify-e-prod-dist-equivalence.py` against exact clean E; result `/tmp/wta-t41/e-prod-dist-equivalence.json` reports C→D exactly `adminManifestRegistry.test.ts`, D→E exactly the two browser fixture files, and C→E exactly those three. It rehashed the entire immutable C production manifest and current dist inventory: **328/328** files match by bytes and SHA-256 (Admin 310, Home 14, SSO 4). The fixed five-case API-controlled spec hash remains unchanged. This proves C's production dist is applicable to E; it is not a fresh E frontend build.

The separate private `/tmp/wta-t41/run-inbox-api-controlled-dist-v2.py` is now pinned to full E and uses the exact C→E three-path allowlist, prior 328-artifact proof and original five case/zero retry gate. It additionally retains only exact-test numeric declaration/assertion locations for failed cases; raw messages, stacks, attachments, preview logs and reporter are still removed. Its six private synthetic tests passed. This preview runner **has not been executed**; the real 501 inbox browser runner is a different gate and remains Lead-owned.

## Actual E browser result and remaining acceptance boundary

E is a test-harness recovery, not a new implementation of AC-041. The first D browser run stopped before JVM/Vite/Chrome. Lead's E run `/tmp/wta-t41/browser-runs/969f43a52216aebb/result.json` reached the real browser: six-SQL seed A=501/unread481, B=2/unread2; A login, message-box badge/top 10, real detail GET200 and detail body were observed through `frontend/e2e/inbox-paged-real.e2e.ts:89`. Its sole Chrome case then **failed at line 90**, `topDetail.getByRole('button', { name: '关闭' }).click()`; Playwright counts expected0/unexpected1/skipped0/flaky0, exit1. Source before/after remained clean E and owned cleanup errors were empty. The strict gate correctly reported `acceptance=false`; the page-26, all-read, B-isolation and timestamp checks were not reached and must not be inferred from seed counts. The exact cause of that button-locator failure is not established by the sanitized result alone. Subsequent review should use the fixed line and current dialog markup and preserve the real UI assertion rather than suppressing it.

The separate five-case production-dist preview is ready but not yet run. A subsequent candidate (if the Playwright spec/fixture changes) needs its own fixed SHA and re-pinned preview rather than reporting E's result as a pass.
