# KEEP verify

`rg` + `wta-rename-keep-contract.test.mjs`:

- `org.dromara.sms4j` / `warm` / `easyes` / `easy-es` / `mica.mqtt` / `mica-mqtt` still present
- not rewritten to `org.namewta.sms4j` etc.
- 246 KEEP hits in `{SCRATCH}/rg-keep.txt`
