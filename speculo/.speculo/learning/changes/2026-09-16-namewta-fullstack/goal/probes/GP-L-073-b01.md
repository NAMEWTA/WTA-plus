---
artifact: learning-goal-probe
probe_id: GP-L-073-b01
lesson_id: L-073
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-073 / b01

审问对象是已写成的 Lesson `lessons/L-073-workflow-frontend.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **两格**：`A:createWorkflowDefinitionService`、`A:createWorkflowWebDomain`。OBJ-73 原文：能说明 `createWorkflowDefinitionService` 与 `createWorkflowWebDomain`。档是**解释+对照**。矩阵几何 **不是 1:1 两行**：

- 厨房是 **独行** `createWorkflowDefinitionService`（模块 `domains/workflow`，仍 uncovered，备注空）。
- 菜单工厂 **没有独行**；符号落在 **九工厂同行** `createAdminWebDomain, createSystemWebDomain, createProfileWebDomain, createNotifyWebDomain, createThirdWebDomain, createWorkflowWebDomain, createDemoWebDomain, createAiWebDomain, createProfileSelfWebDomain`（模块 `web-domains/*`，仍 uncovered，备注「manifest 工厂」）。

矩阵 (b) **没有** 十二键注册 / `segment()` / 导出走 download / XML 鬼窗独行；已印的 workflow (b) 是 `IFlwTaskService` 五支枢纽（L-070）与 `TestLeaveController.submitAndFlowStart`（L-072）。矩阵 (c) `admin-web` / `home-web` 容器与 `frontend-direction` / `admin-web-composition` 已 covered（L-004 / L-007 / L-008）。矩阵 (d) **没有**浏览器流程菜单流；已 uncovered 的是「主路径 工作流请假」（L-072, L-070）与「失败路径 工作流驳回/终止」（L-070）。本批按派单审问 **厨房独行 + 菜单工厂这一颗切片**；口试按磁盘：根 `src/index.ts` 冻住六族 **57** 扁平枪 + 嵌套 `users`、路径 `segment()`、任务才投影、无 `exportDef`；菜单 id `web-domain-workflow`、`domainId` `'workflow'`、先 `requireWorkflowWebRuntime` 再冻 **12** 键；页面经 `runtime.service` 扣扳机，导出走 `runtime.download`，设计走 iframe。不把九工厂行整行闭合，不把 L-007 插头再盖第二次章，也不把 Vue 文件名升成矩阵行。分类七扇留给 L-067；图纸 13 扇 / 发布闸留给 L-068；实例十三扇与 `WorkflowService.*` 留给 L-069；任务十六扇与五支枢纽留给 L-070；SpEL 五扇留给 L-071；请假 `submitAndFlowStart` 留给 L-072；导航 host / 九工厂行警告留给 L-020；厅堂插座板全表留给 L-007 / L-008。

抽查锚点（2026-09-17）：`frontend/packages/domains/workflow/src/{index,index.test,transport,transport.test}.ts`；`src/{category,definition,task}/index.ts`；包 `package.json` `exports` / `AGENTS.md`；`frontend/packages/web-domains/workflow/src/{index,index.test,runtime,designer,process-actions,runtime-actions,user-selection,tree}.ts` 与对应测试；`CategoryPage.vue` / `DefinitionPage.vue` / `DesignPage.vue` / `SpelPage.vue` / `TaskListPage.vue` / `InstancePage.vue` / `MyDocumentPage.vue` / `LeaveListPage.vue` / `LeaveEditPage.vue` / `ProcessActionDialog.vue` / `UserSelect.vue`；`apps/admin-web/src/application/services.ts` 第 10 / 52 行；`router/adminManifestRegistry.ts` 第 23 / 88–139 / 145–150 / 336–376 行与 `.test.ts` 第 91–97 行；`apps/home-web/src/application/services.ts`；`30-cde-workflow.sql` 菜单块；`50-cde-base-dml.sql` 第 41 行；`FlwDefinitionController` / `FlwInstanceController` / `FlwCategoryController`；`frontend/e2e/workflow-definition.spec.ts`；`domains/system/src/user/public.ts` `createUserQueryPort`；档案楼 `requireProfileWebRuntime`；通知/三方 web-domain 无 `require*WebRuntime`。

## Q1 澄清

- objective_id: OBJ-73
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-73；chain L-073 覆盖列两格；课 `claimed_cells` 两格；矩阵 (a) 厨房独行仍 uncovered、九工厂同行仍 uncovered、无 Vue 行、无本工厂独行；(b) 无本课符号、任务/请假挂 L-070/L-072；(c) 两厅堂已 covered；(d) 无浏览器流程菜单流，已 uncovered 的是请假主路径与驳回/终止；课开篇「口试名单就是矩阵 (a) 这两格」+「不要把整行九厂标成 covered」+ 负空间 L-067…L-072 / L-007 / L-008 / L-020
- difficulty: medium

课把浏览器流程这一头收成「一本印好地址的出餐单 + 大厅墙上的十二行点菜单」。计划里至少有六套可数对象，派单点名 **两格 (a)**，但矩阵把它们印成 **两种几何**：

1. **OBJ-73 原文。** `course.md`：能说明 `createWorkflowDefinitionService` 与 `createWorkflowWebDomain`。一句里是 **厨房工厂 + 菜单工厂**，不是七扇分类窗、不是发布闸、不是 `WorkflowService.*`、不是任务枢纽十六扇，也没有把九个 `create*WebDomain` 写成口试清单。课把 OBJ 扩成「六族 URL 表 + 十二键怎么喂页面 + 三处故意错位」，那是走查，不是第三张矩阵行。
2. **chain / 课 `claimed_cells`。** 两格。符号就是这两个工厂名。磁盘上 **没有** `createWorkflowService` / `createWorkflowTaskService`，也 **没有** `@namewta/domain-workflow/definition` 小厨房。管理端菜单 id `web-domain-workflow`，十二键一次冻住。chain **不**写 Vue 文件名，也 **不**写 `legacyDefinitionXml`。
3. **矩阵 (a) 两处不对齐。** 厨房独行与本课第一格 1:1，Lead 若按该行标 covered **不会**绑死九个菜单工厂。九工厂行把本课第二格和 `createDemoWebDomain` 等邻居绑死。Lead 若按**现印同行**标 covered，会把 L-020 / 未写的 demo/ai 菜单假闭合。矩阵 **没有** 十二张 Vue 符号，也没有 `TaskListPage.vue` 行。
4. **课自己的操作定义。** 厨房只认 `HttpClient`，返回 `Object.freeze` 的 `WorkflowDefinitionService`：分类 6 / 图纸 13 / SpEL 5 / 任务 16 / 实例 11 / 请假 6，外加嵌套 `users`。路径 id 走私有 `segment()`。任务五份分页 + `getTask` 才投影。封面 **没有** `exportDef` / `exportCategory` / `getInfo(businessId)`。菜单工厂先 `requireWorkflowWebRuntime`（缺托盘 throw `'WorkflowWebRuntime is required'`），再冻十二键；**不** `addRoute`，**不**拼 `/workflow/*`（导出字符串走 download 口是例外）。设计页是 iframe，地址来自宿主 `designUrl`。
5. **三处故意错位是走查，不是第三格。** 厅堂单例导出名 `workflowService` ≠ 工厂名；定义/请假导出走 `runtime.download` 不是厨房方法；`legacyDefinitionXml` 打的 `definitionXml` 路径 Controller 没有、Vue 也不喊。不要为了盖章把 `exportDef` 或 xml 预览再立一行。
6. **邻格。** L-067…L-072 六份 classic 窗。L-007 厅堂插头（工厂名 ≠ 导出名已点过，没有 57 枪 URL 表）。L-020 九工厂行警告。L-054 / L-066 对照：notify/third 工厂 2026-09-17 **不** fail-closed。L-044 对照：档案楼缺十一口当场 throw。档案旁路 `completeWorkflowTask` 不是第十三键。

问：本两格闭合标准是「厨房独行按 57 枪 URL/动词/`segment`/任务投影/无 exportDef 盖章，菜单只给 `createWorkflowWebDomain` 这一颗切片（十二键 + fail-closed 托盘 + 页面实际扣的 `runtime.service.*` / download / iframe），且不碰九工厂整行、不升 Vue 矩阵行、不提前盖六份 Java 窗」，还是 OBJ 那句必须先把九工厂行拆开、或必须等 GP-L-067…L-072 全 mined 才给厨房盖章？Lead 若凭「`createWorkflowWebDomain` 四字在函数表里」把九工厂行标 covered，是否假闭合了另外八个工厂？Lead 若因菜单没有独行就把十二键判成未讲，是否假判？

## Q2 证据

- objective_id: OBJ-73
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: domains `index.ts` 237–238 / 240–257 / 259–318 / 320–332 / 334–476；`index.test.ts` 6–75 / 77–94 / 97–203 / 205–218 / 221–229；`transport.ts` 12–36；`transport.test.ts` 5–36；`category/index.ts` 3–6；`definition/index.ts` 3–6；`task/index.ts` 3；包 `package.json` 6–13 / 21–26；web-domain `index.ts` 48–205；`index.test.ts` 8–13 / 16–54 / 57–101 / 105–126；`runtime.ts` 14–29 / 44–46；`designer.ts` 9–18；`process-actions.ts` 14–21；`process-actions.test.ts` 5–26；`runtime-actions.ts` 3–14；`runtime-actions.test.ts` 30–31；`user-selection.ts` 44；`tree.ts` 1；`CategoryPage.vue` 195–208 / 251–277；`DefinitionPage.vue` 318–320 / 456 / 503–511 / 523–568 / 597 / 613–636 / 647 / 654 / 675–709；`DesignPage.vue` 11–27；`SpelPage.vue` 281–335；`TaskListPage.vue` 4–66 / 119–204；`InstancePage.vue` 166–178 / 227–299 / 282–290；`MyDocumentPage.vue` 88–146；`LeaveListPage.vue` 78 / 107 / 116–127；`LeaveEditPage.vue` 7–49 / 91–99 / 111 / 117–160；`ProcessActionDialog.vue` 193–194 / 265–322；`UserSelect.vue` 100 / 165；admin `services.ts` 10 / 52；`adminManifestRegistry.ts` 23 / 88–139 / 145–150 / 336–376；registry 测试 91–97；home `services.ts` 1–16；DML `30-cde-workflow.sql` 269 / 273–274 / 280 / 307 / 309；`50-cde-base-dml.sql` 41；`FlwDefinitionController` 189–204；`FlwInstanceController` 75–91；`FlwCategoryController` 124–125；e2e `workflow-definition.spec.ts` 160 / 317；system `user/public.ts` 65–84；web-domain 包零 `@/application/services`；frontend 零 `createWorkflowService`
- difficulty: high

课的出餐单 + 十二键哪些能在工作树直接复现，哪些只是测试名单比课表窄、或口试把「工厂注册了十二键」说成「每支枪都有按钮」？

**仍能复现（厨房格 `createWorkflowDefinitionService`）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂在根 `src/index.ts` 第 334 行，`Object.freeze`；`transport.ts` 只有 `projectWorkflowTaskTransport` | `index.ts` 334–336；`transport.ts` 12 |
| 封面 57 扁平方法 + `readonly users`；**没有**嵌套 `definitions.list` | `index.ts` 259–318 |
| 六族方法数 6/13/5/16/11/6 与实现 1:1 | 接口 260–317 vs 工厂 338–475 |
| 内部 `request` 只是 `http.request<ApiResponse<T>>`，无 `no-store` | `index.ts` 335 |
| 路径 `segment()`：数组/标量 → `encodeURIComponent` → 逗号拼接；测试锁 `first%2Fid,second%20id,comma%2Cvalue` | `index.ts` 237–238；`index.test.ts` 85–92 |
| 分类/图纸/SpEL 24 枪 URL/动词快照（含 `definitionXml` / `xmlString` / `importDef` header） | `index.test.ts` 40–74 |
| `importDefinition` 是工厂里**唯一** `headers: { repeatSubmit: false }` | `index.ts` 358–359；包内 `headers:` 一处 |
| 任务五分页 + `getTask` 走 `projectTaskPage` / `projectTask`；`instanceId` 变字符串；缺 `rows` → `{ rows: [], total }`；缺 data 的详情原样 `{ code: 204 }` | `index.ts` 240–257 / 373–422；`index.test.ts` 205–229 |
| `currentTaskUsers` 先 `projectUserSummary` 再拆掉 `phoneNumber`；测试 JSON 不能出现 `must-not-cross` | `index.ts` 433–442；`index.test.ts` 199–202 |
| 任务/实例/请假 33 条 `method url` 快照（小写动词；`updateAssignee` PUT 路径编 userId；`terminationTask` POST） | `index.test.ts` 158–192 |
| 投影丢掉 `delFlag` / `permissionList` / button `value`；非法日期 → undefined | `transport.test.ts` 5–36 |
| `users: createUserQueryPort(http)` 嵌在工厂闭包；包依赖有 `@namewta/domain-system` | `index.ts` 337；`package.json` 23 |
| 选人三 URL 在 system 口：`GET /system/user/list`、`optionselect?userIds=`、`deptTree` | `user/public.ts` 65–84 |
| 子路径只冻资源标签，例如 `workflowTaskResource = { controller: 'FlwTaskController', basePath: '/workflow/task' }` | `task/index.ts` 3；`definition/index.ts` 3–6 |
| 名片 `id: 'workflow'`，`backendModules: ['wta-workflow']`，七项 capabilities **不是** componentKey | `index.ts` 320–331 |
| 封面/实现 **没有** `exportDef` / `exportCategory` / `getInfo` / `deleteByBusinessIds` | 接口 259–318；工厂 336–476 |
| 厅堂 `workflowService = createWorkflowDefinitionService(domainHttp)`，只吃 http | `services.ts` 10 / 52 |
| home / sso **零** 这座工厂 | home `services.ts` 1–16；sso-web grep 空 |
| frontend **零** `createWorkflowService` | 全仓 grep 空 |

**仍能复现（菜单切片 `createWorkflowWebDomain`）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂先 `requireWorkflowWebRuntime`，缺托盘 throw `'WorkflowWebRuntime is required'` | `runtime.ts` 44–46；`index.ts` 48–49 |
| id `web-domain-workflow`，`domainId` `'workflow'`；`messages` 只冻三句标题 | `index.ts` 51–61 |
| 十二键顺序与名字测试 `toEqual`：category → processDefinition → design/`WarmFlow` → spel → taskWaiting → taskFinish → taskCopyList → myDocument → allTaskWaiting → processInstance → leave → leaveEdit | `index.ts` 129–202；`index.test.ts` 18–31 |
| `service: {} as never` 仍能冻出十二键 | `index.test.ts` 8–13 / 17 |
| 任务四键外包同一张 `TaskListPage.vue`，`mode` 为 `waiting` / `finished` / `copy` / `all-waiting` | `index.ts` 154–184 |
| 权限六组 id；definition 组含 `workflow:definition:export` 与 `publish`；category/spel 组 **不含** export 串 | `index.ts` 63–127 |
| 未选 `selectedManifestIds` 则 `componentKeys()` 空；重复键 `AppRuntimeError` | `index.test.ts` 57–101 |
| `WorkflowWebRuntime.service` 类型是 `WorkflowDefinitionService`；另有 `designUrl` / `chartUrl` / `download` / `fileUpload` / `treePanel` / `dicts` / `confirm`… | `runtime.ts` 14–29 |
| 包名 `@namewta/web-domain-workflow`；依赖 `domain-workflow` + platform + vue；**没有** axios、**没有** `@namewta/domain-system` | `package.json` 6 / 21–27 |
| 十二张页 **零** `@/application/services` | web-domain 包 grep 空 |
| 分类页 `listCategories` + `handleTree`；下拉同样 list，**不**打 `categoryTree`；删一行单 id | `CategoryPage.vue` 195–208 / 277 |
| 图纸两 TAB：`listDefinitions` / `listUnpublishedDefinitions`；侧栏才 `categoryTree`；保存 add/update；发布/取消/激活/复制/删除走厨房；导入 `importDefinition(FormData)`；导出 `runtime.download('/workflow/definition/exportDef/…')` | `DefinitionPage.vue` 456 / 503–511 / 523–568 / 597 / 654 / 675–709 |
| 动态表单「是」disabled，只许 `formCustom = 'N'` | 318–320 / 647 |
| 设计：`router.push('/workflow/design/index?definitionId=&disabled=')`；种子 path 是 `design/index`；`DesignPage` 不喊厨房，`createDesignerController` + iframe；`disabled: 'true'` 当成布尔 true | `DefinitionPage.vue` 613–636；SQL 273；`designer.ts` 12；`index.test.ts` 123 |
| SpEL 五枪；页无导出按钮 | `SpelPage.vue` 281–335 |
| 任务四 mode 换枪；`all-waiting` + TAB finished → `pageAllTaskFinished`；抄送无选申请人；催办/改办理人/干预只在 `all-waiting` 且 TAB waiting，核 `workflow:task:edit` | `TaskListPage.vue` 4–66 / 119–136 |
| 点「办理」不在本页调 `completeTask`，`router.push({ path: formPath, query: { id: businessId, taskId, type } })` | 159–164 |
| 办理对话框并行 `getTask` + `getNextNodes`；提交 `completeTask(createCompletePayload)` 会 trim；干预模式强制 transfer / termination，会签再加加减签 | `ProcessActionDialog.vue` 193–194；`process-actions.ts` 14–21；测试 5–26 |
| 实例运行中/已结束两枪；我发起的键在 `workflow/task/myDocument`，枪是 `pageCurrentInstances` | `InstancePage.vue` 166–167；`MyDocumentPage.vue` 88 |
| 激活开关 `next = row.activityStatus === 1` 发生在 v-model 改过之后；失败拨回 | `InstancePage.vue` 282–290 |
| 请假列表父菜单是测试菜单 `1761400000000000005`；导出 download `/workflow/leave/export`；撤销走 `cancelProcess`，留言写死「申请人撤销流程！」 | SQL 309；`50-cde-base-dml.sql` 41；`LeaveListPage.vue` 107 / 116 |
| 请假三路：draft add/update；start 先存再 `startWorkflow`（`userList: ['1','3','4']` 写死）；direct `submitLeave` → `/submitAndFlowStart` | `LeaveEditPage.vue` 111 / 137–151 |
| `flowCode` 默认 `leave1`，下拉 leave1…leave6 页内硬编码 | 91–99 |
| 天数 `floor(ms/86400000)+1`；非法区间 `undefined` | `runtime-actions.ts` 3–8；测试 30–31 |
| 选人 `service.users.list` / `departmentTree` / `options` | `UserSelect.vue` 100 / 165；`user-selection.ts` 44 |
| 厅堂 runtime 塞 `workflowService` + `designUrl`（`/warm-flow-ui/index.html?id=&onlyDesignShow=&clientid=`）+ download + treePanel + dicts | `adminManifestRegistry.ts` 88–139 |
| `selectedManifestIds` 含 `'web-domain-workflow'`；`selectedDomainIds` 含 `'workflow'` | 365–369 |
| 档案旁路 `completeWorkflowTask` → 同一座厨房 `completeTask`，不是第十三键 | registry 145–150 |
| registry 测试锁 `workflow/category/index` → `Category`、`processDefinition` → `processDefinition`；`workflow/task/index` 无键 | 91–97 |
| 设计页种子 perms `workflow:leave:edit`；隐藏 C `visible='1'` | SQL 273–274 |
| 分类/SpEL 种子有 F 型 export；厨房与页都没有对应枪/按钮 | SQL 280 / 307 |
| e2e 定义导出是 `POST /workflow/definition/exportDef/d0` | e2e 160 / 317 |
| `xmlString` 后端窗在，方法体 `defService.exportJson`；**没有** `definitionXml` 映射 | `FlwDefinitionController` 201–204 |
| 实例 `getInfo/{businessId}` 与 `deleteByBusinessIds` 后端有，厨房无方法 | `FlwInstanceController` 75–91 |
| 分类删除 `@PathVariable Long categoryId`，逗号串绑不上 | `FlwCategoryController` 124–125 |
| 对照：notify/third 包 **无** `require*WebRuntime`；档案楼有 `requireProfileWebRuntime` | web-domains grep |

**课写过、测试名单更窄或口试数字写偏（不是证伪本两格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「测试锁死十二键顺序与名字」 | 十二键 `toEqual` 有。**没有**锁 `manifest.id === 'web-domain-workflow'`，也 **没有**锁三句 `messages` |
| 「`createWorkflowWebDomain(undefined)` throw」 | 源码 44–46 有。web-domain 测试传入 stub，**没有** `undefined` 例 |
| 「权限六组」 | 测试锁六组 **id** + `toContain` / `arrayContaining` 若干串，**不是**六组全量 `toEqual` |
| 「registry 测试锁流程键」 | 只锁 category 与 processDefinition 两键，外加 `workflow/task/index` 为 undefined。其余十键在工厂字面量里 |
| 「厨房测试锁 24 枪分类/图纸/SpEL」 | 第一例 24 请求。`users` 三 URL **不在**本包测试，在 `domain-system` `public.test.ts` |
| 口试表把动词写成 GET/POST/PUT/DELETE | 厨房 `method: 'get'`/`'post'`/`'put'`/`'delete'` 小写 |
| `DesignPage`「只 `createDesignerController`」 | `onMounted` 走 `controller.url()`；`defineExpose({ open })` 另直接喊 `runtime.designUrl` |
| 课 `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |

问：在 57 枪 URL/动词/`segment`/任务投影/无 exportDef、十二键顺序、空 service 仍能冻、缺 runtime 源码关门、页面不 import 厅堂单例、分类 list+树、图纸两抽屉+download 导出、SpEL 五枪、任务四 mode、请假三路、设计 iframe、home 不选 已经能互证的前提下，(a) 是否还要把「throw 无单测」「权限数组未全量 toEqual」「registry 只锁两键」「users 测试在邻包」写成开篇半句，才算本两格讲完？还是这些已经落在课的表/反例里，Lead 可以把 **厨房独行** 视为可 covered、把 **菜单当切片已讲** 并保持九工厂行 uncovered？

## Q3 假设与反例

- objective_id: OBJ-73
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无 `createWorkflowService`；工厂不在 `transport.ts`；子路径不是小厨房；十二页不 import 厅堂单例；`createWorkflowWebDomain(undefined)` 源码 throw；notify/third 不 throw；无 Vue 喊 XML 两枪；导出走 download；分类页不打 `categoryTree`；`myDocument` 走实例分页；请假列表挂测试菜单；设计 perms 是 `leave:edit`；home 无此工厂；九工厂行不因本课整行闭合；`WorkflowService` 不是厨房 Java 名；iframe 保存不走 `updateDefinition`
- difficulty: high

课的隐含假设若反转，合格读者会把哪座工厂或哪只抽屉认错？

1. **「十二张 Vue `import { workflowService }`。」** 包内搜不到 `@/application/services`。直接消费单例的是厅堂 registry 和档案 `completeWorkflowTask`。课反例 1 / 误区 6 覆盖。
2. **「`createWorkflowWebDomain` 等于九个 web-domain 工厂都 covered。」** 矩阵 (a) 同行绑死。GP-L-020 / GP-L-044 / GP-L-054 / GP-L-066 已挖过。课反例 2 / 误区 3 覆盖。
3. **「工厂在 `transport.ts`。」** 打开 `index.ts` 第 334 行。课反例 3 / 误区 5 覆盖。
4. **「`createWorkflowService` / `createWorkflowTaskService`。」** 磁盘没有。导出名 `workflowService` ≠ 工厂名。课反例 4 / 误区 4 覆盖。
5. **「`service.definitions.list` / `service.tasks.waiting`。」** 扁平：`listDefinitions`、`pageTaskWaiting`。课反例 5 / 误区 8 覆盖。
6. **「从 `@namewta/domain-workflow/task` 进口小厨房。」** 子路径只有资源标签。课反例 6 覆盖。
7. **「页面手写 `/workflow/task/completeTask`。」** 办理走 `runtime.service.completeTask`。导出是页里出现 `/workflow/...` 字符串的例外，且走 download。课反例 7 覆盖。
8. **「`legacyDefinitionXml` 能预览图。」** 无 Vue。路径在 Controller 上没有映射。真 JSON 窗是 `xmlString`，方法体 `exportJson`，同样无按钮。课反例 8 / 误区 10 / 11 覆盖。
9. **「分类页打 `categoryTree`。」** 管理页 `listCategories` + `handleTree`。tree 是定义/实例/我发起的侧栏。课反例 9 / 误区 9 覆盖。
10. **「删除分类可以一次传 `1,2`。」** 厨房测试会拼逗号串，后端 `Long categoryId` 绑不上。页只传当前行 id。课反例 10 覆盖。
11. **「设计页权限是 `workflow:definition:edit`。」** 种子 perms 是 `workflow:leave:edit`。行按钮核的是 `workflow:definition:query`。课反例 11 覆盖。
12. **「请假列表在工作流目录下。」** C 型 `workflow/leave/index` 的父菜单是测试菜单。工作流目录下的是隐藏 `leaveEdit`。课反例 12 覆盖。
13. **「`myDocument` 走任务分页。」** 枪是 `pageCurrentInstances`。课反例 13 / 误区 15 覆盖。
14. **「home 也能打开我的待办。」** 工作树没有这份工厂接线。课反例 14 / 误区 16 覆盖。
15. **「`WorkflowService.completeTask` 就是厨房 `completeTask`。」** 一个是 Java 接口给邻居模块；一个是浏览器 `POST /workflow/task/completeTask`。课反例 15 覆盖。
16. **「iframe 保存走 `updateDefinition`。」** 保存 JSON 的窗是 `/warm-flow/save-json`。厨房封面没有。课反例 16 / 误区 13 覆盖。
17. **「`createWorkflowWebDomain(undefined)` 像 notify 那样仍能冻出键。」** 本厂会 throw。notify/third 2026-09-17 不会。课正例 10 / 误区 19 覆盖。
18. **「`requireWorkflowWebRuntime` 会检查 57 支枪。」** 只检查 runtime 对象在不在。`service: {}` 仍能冻十二键。课边界 1 覆盖。
19. **「请假提交审批 = `submitLeave`。」** 提交审批是 add/update + `startWorkflow`；`submitLeave` 是「后端发起」。课误区 14 覆盖。
20. **「取消发布要单独的 unPublish 权限。」** 与发布共用 `workflow:definition:publish`。课误区 12 覆盖。
21. **「capabilities 决定挂哪几页。」** 决定挂页的是 manifest + 菜单种子 + `selectedManifestIds`。课误区 17 覆盖。
22. **「OBJ-73 包含 `FlwTaskController`。」** OBJ-70。课误区 2 覆盖。
23. **「工厂注册了十二键 = 所有 `/workflow/*` HTTP 都有按钮。」** XML 两枪无页；三扇导出里定义/请假走 download、分类谁都不接；按业务 id 两窗没进厨房。课图 2 文字等价物覆盖。
24. **「选人走厅堂传入的 `systemService`。」** 走工厂内 `users`。课误区 7 覆盖。
25. **「干预模式也跟 `buttonList.show`。」** 测试：后端把 transfer 标 `show: false`，干预仍给 transfer / termination / 会签加减签。课边界 6 覆盖。

问：读完后仍说不出的，是不是「两个工厂的名字」，而是 **厨房方法全集 ≠ 墙上已扣的扳机 ≠ 后端窗全集、九工厂行不能因本课整行闭合、本厂 fail-closed 只检查托盘在不在、导出走 download、XML 鬼窗无 Vue、home 根本不选、工厂名 ≠ 导出名、`WorkflowService` 不在浏览器河**？

## Q4 视角

- objective_id: OBJ-73
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-73 vs L-007/L-008/L-020/L-044/L-054/L-066/L-067…L-072；厨房独行 vs 九工厂同行；claimed_cells 两工厂名 vs 十二张 Vue / 档案旁路；chain 估时 35 vs 课 38
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这两格 (a)**？

| 视角 | 多出来的边 | 是否本课这两格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 厨房独行 1:1 可盖章；九工厂同行 vs chain 菜单工厂名；无 Vue 行；(b) 无本课符号、任务/请假挂 L-070/L-072；(d) 无浏览器流程菜单流 | **是** 闭合定义。厨房独行可视为已讲并可 covered。九工厂行保持 uncovered。不要发明 (b)/(d)，不要把十二张 Vue 升成新行才能给切片盖章 |
| 厨房作者 | 57 枪 URL/动词；`segment()`；任务投影；`users` 嵌套；无 exportDef；XML 两枪仍注册 | **是** `A:createWorkflowDefinitionService` |
| 管理端菜单工厂 | 十二键顺序、六组 `workflow:*`、先关门再冻、`h(page, { runtime, mode? })`、未选无键 | **是** 切片里的 `createWorkflowWebDomain` |
| 十二张页消费 | 分类 list+树；图纸两抽屉+download；SpEL 五枪；任务四 mode + 办理对话框；实例三份分页；请假三路；选人 `users` | **是** OBJ 对照走查，不是矩阵第三格 |
| 导出/鬼窗/按业务 id | 定义/请假 download；分类/SpEL 种子空子弹；`definitionXml` 无窗；`getInfo` / `deleteByBusinessIds` 没进厨房 | **是** 负空间对照。不要升成第二张厨房章 |
| 设计器第三条河 | iframe `/warm-flow-ui/`；保存 `/warm-flow/save-json`；种子 path `design/index` vs Vue `push('/workflow/design/index')` | 谁拼 URL **是**。引擎内部 skip **否**（vendor-engine） |
| 厅堂电工（L-007） | `createWorkflowDefinitionService(domainHttp)`；导出名 `workflowService`；registry 注入 runtime | 插头一句 **是**。12 个 `create*` 全表 **否**（已 covered） |
| 导航 host（L-020） | admin 选 `web-domain-workflow`；home 不选；重复键失败关闭 | 选择集一句 **是**。其余八厂 **否** 整行 |
| 档案楼（L-044 / L-039） | `requireProfileWebRuntime`；`completeWorkflowTask` 旁路 | fail-closed 对照 + 非第十三键 **是**。企业三页 **否** |
| 通知/三方楼（L-054 / L-066） | 不校验托盘；notify 四张 Vue；third 一张页四个 kind | 对照「本厂会关门 / 十二键多张 Vue」**是**。通知四页 / 三方四键 **否** |
| 分类窗（L-067） | 七扇；export 在 Java+菜单；删除单个 Long | 厨房六枪没收 export、逗号不是后端批量 **是** 边界。七扇盖章 **否** |
| 图纸窗（L-068） | 13 扇；发布闸；`xmlString`=`exportJson`；无 `definitionXml` | 假名/遗留 URL / download 导出 **是** 对照。发布闸 **否** |
| 实例/合同（L-069） | 十三扇；`WorkflowService.*`；按业务 id 两窗 | 厨房缺两枪、名字对照 **是**。合同窗 **否** |
| 任务枢纽（L-070） | 十六扇 HTTP + 五支副作用 + 驳回/终止 (d) | URL 对齐 **是**。窗口条与 (b)/(d) **否** |
| SpEL（L-071） | 五扇；无 `/export` | 页五枪、种子空子弹 **是**。Java 窗 **否** |
| 请假示例（L-072） | `submitAndFlowStart` 事务；(d) 主路径 | 页「后端发起」对那一枪 **是** 边界。写单并发流 **否** |
| MySQL 8.4 | 菜单十二 component；设计页 `leave:edit`；请假挂测试菜单 | 种子键对照 **是** (a)。容器 **否** |

问：Lead 若凭「两个工厂名在 claimed 列里」把厨房独行 **和** 九工厂行一起标 covered，是否还要求读者能指着 **57 枪扁平、`segment`、任务才投影、十二键顺序、本厂 fail-closed、导出走 download、XML 鬼窗无 Vue、home 不选、工厂名 ≠ 导出名**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要等 L-067…L-072 才给 **厨房独行** 当已讲？九工厂行是否例外——必须保持 uncovered，直到其余工厂也有课？

## Q5 元问题

- objective_id: OBJ-73
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课；一课超 1 个容器是 Lead 裁决不是 b01 填表）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 厨房独行仍 uncovered、九工厂同行仍 uncovered、无 Vue 行、无本课 (b)/(d)
- difficulty: low

建议：`defer`

原因：

- **厨房独行与 OBJ-73 / chain / 课第一格 1:1。** L 合同章节齐全（宏观十二键地图、直觉十四句、定义表、机制链 1–10、带 caption / 文字等价物的两张图、正反例、变式、误区、暂停走查、来源表 S-005/S-006/S-008/S-010/S-015/S-L007-01/S-L073-01…08）。能复现：冻住六族 57 枪 + `users`、`segment()` 编码、任务投影与减签去手机号、唯一 `repeatSubmit` 在导入、无 exportDef、厅堂只 admin-web 接线、home / sso 不雇。不必为「工厂的名字」再挖五问。Lead 可将 `createWorkflowDefinitionService` 标 `covered`。
- **菜单是切片，不是九工厂整行。** 十二键顺序、`requireWorkflowWebRuntime`、`h(page, { runtime, mode? })`、页面实际扣的枪、download 导出、iframe 设计、档案旁路非第十三键——已在课的表/反例里。GP-L-020 / GP-L-044 / GP-L-054 / GP-L-066 已立过同一条：本切片证据可写进九工厂行备注（「workflow 管理端十二键 + fail-closed 托盘 + 页面消费已讲」），**不要**把该行标 covered。
- 本批可粘贴的精度项不挡住厨房盖章、也不挡住菜单切片视为已讲：（1）缺 runtime throw 无单测；（2）权限数组测试未全量 `toEqual`；（3）registry 只锁两键；（4）`users` 三 URL 的合同测试在 `domain-system`；（5）动词小写；（6）`DesignPage.open` 另直接喊 `designUrl`；（7）chain 估时 35、课 38。不要把六份 Java 窗、九工厂其余八个名字、Warm-Flow 引擎内部塞进本格。
- 不建议 `mine-more`：b01 未证明本两格还有真实矩阵空洞；再五问只会把 L-067 分类七扇、L-068 发布闸、L-069 `WorkflowService`、L-070 任务窗、L-072 请假事务或九工厂其余名字再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；测试名单窄是半句精度，不是第二容器。不建议 `split-lesson`：未满 10 问；厨房+菜单是 chain 原设计的同一 OBJ-73（对照 third 拆成 L-065/L-066 是另一条链，本课不是两个 C4 讲不完）；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：`segment` / download 导出 / XML 鬼窗已是 (a) 的解释到副作用。不建议把十二张 Vue 升成矩阵行才给切片盖章。不建议等 GP-L-067…L-072 才给厨房独行当已讲。不建议用本课改 (d) 请假走廊或驳回/终止。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:createWorkflowDefinitionService | 57 枪 URL/动词、扁平 freeze、`segment()`、任务投影、`users` 嵌套、无 exportDef、XML 两枪仍注册、home 不雇——均与源码对齐。残留精度：本包单测不含 `users` 三 URL；动词小写；throw/权限全量/registry 十键不在厨房测试。 | 可选半句：口试方法表以 `index.ts` 为准，`users` 合同以 `createUserQueryPort` 为准。不要求重写机制链。Lead 可将矩阵该行标 `covered`（证据锚本 probe 与 L-073 来源表 S-L073-01 / S-L073-02）。 | defer（本行可 covered；精度项不挡盖章） |
| A:createWorkflowWebDomain | 课按磁盘讲了十二键、fail-closed 托盘、页面实际扣的 `runtime.service.*`、download 导出、iframe 设计、档案旁路非第十三键、home 不选。**矩阵无此独行**；符号落在九工厂同行。残留精度：throw 无单测；权限测试非全量 `toEqual`；registry 只锁两键。 | 可选半句：口试键表以工厂 `registrations` `toEqual` 为准。不要求重写机制链。Lead **不要**改九工厂行为 covered。可将本切片证据写入该行备注。 | defer（切片已讲；九工厂行不可因本课整行闭合） |
| 九工厂行 `create*WebDomain`（含 `createWorkflowWebDomain`） | 本课只用选择集 + 十二键当切片证据。其余八个工厂未因本课讲完。 | 保持 uncovered。L-020 已覆盖 `createSystemWebDomain` 切片；L-039/L-044 已覆盖 profile 两份菜单；L-054 已覆盖 notify 四页；L-066 已覆盖 third 四键；其余 demo/ai 后续课。 | defer（九工厂行保持 uncovered） |
| A:admin-web.services / C:admin-web-composition | 插头 `createWorkflowDefinitionService(domainHttp)` 与 registry 接线已借用。 | 保持。L-007（已 covered）。 | defer（L-007 已 covered） |
| A:home-web.services | 负空间：无工厂、无单例。 | 保持。L-008（已 covered）。 | defer（L-008 已 covered） |
| A:createSystemWebDomain / B:navigation host | 选择集 + 未选无键 + 重复键失败关闭已点。 | 保持。L-020。 | defer（L-020） |
| A:FlwCategoryController.* | 六枪 URL、没收 export、逗号删除不是后端批量。该行仍 uncovered。 | 不要用本课盖分类七扇。 | defer（L-067） |
| A:FlwDefinitionController.* / B:publish,importJson,removeDef | 遗留 `definitionXml`、`xmlString` 假名、download 导出已作对照。 | 不要用本课盖发布闸。 | defer（L-068） |
| A:FlwInstanceController.* / A:WorkflowService.* | 缺 `getInfo` / `deleteByBusinessIds`；名字对照。 | 不要用本课盖大厅/对讲机。 | defer（L-069） |
| A:FlwTaskController.* / B:IFlwTaskService 五支 / D:驳回终止 | 厨房十六扇 URL 对齐。 | URL 对照一句。窗口条与 (d) 归 L-070。 | defer（L-070） |
| A:FlwSpelController.* | 页五枪；种子 export 空子弹。 | 不要用本课盖 SpEL Java。 | defer（L-071） |
| A:TestLeaveController.* / D:主路径 工作流请假 | 页「后端发起」对 `/submitAndFlowStart`。 | 不要把请假事务并进厨房。 | defer（L-072） |
| A:createProfileWebDomain / completeWorkflowTask | fail-closed 对照 + 旁路非第十三键已借用。 | 保持。L-044 / L-039。 | defer（L-044 / L-039） |
| A:createNotifyWebDomain / A:createThirdWebDomain | 不校验托盘对照已借用。 | 保持。L-054 / L-066（切片已讲，九工厂仍 uncovered）。 | defer（L-054 / L-066） |
| (b) `segment` / download 导出 / XML 鬼窗 / 十二键注册 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 浏览器流程菜单流 | 矩阵无此行。已有的走廊仍是请假主路径与驳回/终止。 | 不要发明前端流程菜单数据流行，也不要用本课改 L-072 / L-070。 | defer（未入库） |
| Warm-Flow / DefService 内部 | iframe 第三条河只认谁拼 URL。 | 保持 `deferred(vendor-engine)`。 | defer（vendor-engine） |
| MySQL 8.4 / Redis 8 | 菜单种子是 (a) 对照，不是容器闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；厨房独行无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`；菜单切片在九工厂同行上已讲完并显式禁止整行闭合；合格读者说得出两个工厂目的（出餐单印六族 URL；墙上十二键把托盘喂进页面）、主路径（分类 list+树 / 图纸两抽屉+发布导入复制 / SpEL 五枪 / 任务四 mode+办理 / 实例三份分页 / 请假三路出门）与失败路径（缺 runtime 当场 throw、缺方法要等点击才炸、分类逗号删除后端绑不上、XML 鬼窗 404、home 不选这份 id）；把可选半句写进原课不会超过 40 分钟；后端六窗、厅堂插头、导航 host、九工厂其余名字已有邻课。满 10 问前不拆。厨房与菜单虽跨 `domains/workflow` 与 `web-domains/workflow` 两个包，仍是 chain 原设计的同一 OBJ-73，不是「超过 1 个容器却讲不完」——对照 third 的拆法是另一条链已经做成 L-065/L-066，本课不要在 b01 再拆。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / split 提案：否（miner 不改矩阵）；split 提案为 none；缺口表对九工厂整行、L-067…L-072 六份 Java、L-007/L-008 插头（已 covered）、L-020 导航、L-044/L-054/L-066 对照、未入库 (b)/(d)、厂商引擎以及 MySQL/Redis 容器显式建议 `defer`；对矩阵 (a) 行 `createWorkflowDefinitionService` 建议 Lead 标 `covered`（证据锚本 probe 与 L-073 来源表 S-L073-01…02）；对 `A:createWorkflowWebDomain` 建议视为已讲切片、**不要**把九工厂同行标 `covered`。本课不主张发明 Vue 矩阵行、用本课盖六份 Controller、把档案旁路升成第十三键、或等 GP-L-072 才给厨房独行盖章。
