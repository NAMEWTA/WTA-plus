# T-23 实施记录

## 当前结果

Revision127：T-23完成本地review。五种禁用供应商预设、真实发送消息ID、持久回执/账号命名空间及腾讯TC3/阿里ACS3鉴权状态查询已闭合；原生推送不冒充支持。11项协议/HTTP测试、41项真实MySQL/Redis/重启/并发/提交故障零skip通过；默认后端857项=740通过+117环境skip，Notify前端9项/typecheck/lint、full/core同源构建及清单通过。47路径checkpoint、9上游重叠/601非重叠不变，累计648；源码88a5a9a25f9c3d88def978ac6aac64d60522638cfa43150e0cfb3f4924a9fee0。无真实账号试发或生产容量声明；30review/1ready/0Done，下一步T-30同一候选整体验收。HEAD/空index保持，全部提交暂缓。

本票可逆实现与本地验收完成，状态review；不是Done、发布或真实供应商试发结论。用户已明确无需继续等待供应商资料，按官方协议和实际SDK完成默认停用预设与安全接入。

## 实现与边界

- 五种预设：腾讯/阿里短信、QQ/163/腾讯企业邮箱SMTP，凭据为空、默认关闭，已有十个场景未暗中绑定。用户填写账号必要配置后启用；短信仍须真实审核签名/模板，腾讯须SMS SDK AppID；SMTP须开通客户端访问并填写授权密码。操作见仓根docs/notify-providers.md。
- SMS4J实际发送结果保留阿里BizId、腾讯对应号码SerialNo；不使用RequestId，缺流水号仍保持ACCEPTED。腾讯模板按连续数字位置排序。邮箱受理不提升为送达。
- 腾讯DescribeSendRecordList/TC3、阿里QuerySendDetails/ACS3使用现有账号密钥鉴权HTTPS，固定供应商域名、禁重定向、连接/读取5秒、读取总时长检查及256KiB响应上限。发送仍走SMS4J；查询无发送能力，无新增依赖/后台线程池。阿里请求强制BizId，两个发送日核对唯一记录；腾讯最多20页/1000条，精确流水号+号码，截断/歧义/未知结果失败关闭。
- SmsReceiptQueryWorker每5秒最多领取一条；SQL只选启用原生账号下有流水号的SMS ACCEPTED，最早受理60秒后、71小时内。receipt_query_at通过CAS预约15分钟，使用数据库UTC且截断到列的秒精度；网络在领取事务外，进程退出/查询失败到期重试。停用/删除/凭据编辑期间不应用旧结果。超窗继续ACCEPTED；这不是厂商推送重试期或生产吞吐保证。
- 原生查询终态通过ProviderCallbackUseCase.confirm进入已有动态事务。保留native-sms:前缀及确定性摘要，自定义HTTP禁止占用该前缀。原生推送仍不直接接入自定义HMAC地址，用户无需另设公网推送入口。
- 自定义回执完整原文HMAC、5分钟窗口、严格字符串字段/长度与精确target。身份为渠道+账号configKey+eventId的JSON数组SHA-256，事实摘要排除每次可重签的timestamp；同事件不同事实409。多收件人共享消息号允许，关联歧义拒绝；早到未关联不消费receipt。
- notify_provider_receipt、Delivery CAS及Intent聚合在同一DSTransactional事务。已提交重复幂等、回滚可重试、状态单向；永久记录、不自动过期，逻辑删除也保留唯一键。账号标识/渠道不可变，已选短信厂商不可替换，逻辑删除永久保留命名空间。
- 自定义回调确认要求HTTP200且R.code=200；401验签失败、400格式/过期、409事实冲突、422非法/歧义、503早到/数据库或提交失败。日志只记录安全元数据，正文/签名/精确收件人均脱敏，验签原文不改写。

## 同一源码验证

当前源码指纹：`88a5a9a25f9c3d88def978ac6aac64d60522638cfa43150e0cfb3f4924a9fee0`。

| 验证 | 结果 | Evidence |
|---|---|---|
| 默认后端全reactor | 857项，740通过、117环境skip，0失败/错误，exit0 | T-23-native-v1-root.json/log/source |
| 原生查询协议与HTTP | 11/11通过，包含在上述root实际执行项 | SmsDeliveryQueryClientTest；签名独立Python参考值见T-23-query-signature-vectors.json |
| 隔离MySQL/Redis/HTTP/独立JVM/提交故障 | 41/41通过、0skip，六SQL新库导入，所有owned资源恢复 | T-23-callback-native-final.json/log/source |
| full/core清理打包与产物清单 | 四条命令exit0，JAR SHA/ZIP内容实际保存 | T-23-native-v1-bundles.json/log |
| Notify前端 | 9/9、typecheck/lint exit0，整个frontend子树与该验证快照逐项一致 | T-23-stage-v2-frontend.json与最终checkpoint |
| 基座/分层/Skill事实/T/P/ticket-control/diff | 最终实际命令和退出码单独保存 | T-23-native-final-governance.json/log（v1治理失败记录保留） |

41项实际环境测试包含既有T-22回归、自定义回执并发/重启/跨账号渠道/早到/精确收件人/SQL与CAS故障/真实HTTP/日志，以及本轮7项原生核对：两个服务实例真实事务CAS只领一次、预约后进程放弃可恢复、网络在事务外、停用/删除/邮件/缺ID/超窗排除、凭据更新丢弃旧查询结果、查询失败不重发、receipt事务回滚及真实JDBC提交前断连/提交后确认丢失。确认丢失重复执行只留一条receipt。跨实例并发在同JVM独立Service/事务；进程重启另用三个独立JVM，不宣称部署双生产节点。

11项协议测试使用官方响应结构夹具和owned loopback HTTP，不使用真实供应商账号。覆盖UTF-8/查询编码签名参考值、完整分页、截断/重复/错误号码/未知状态、跨午夜两个日期、恶意/非法响应脱敏、超窗零请求、重定向/非200/超限、完整协议客户端实际HTTP、真实Socket读取超时。117个默认skip不冒充实际执行；其中本票环境类已由独立driver执行41项，其余归T-30矩阵。

## 失败与修复记录

- 旧seenEvents真实红灯3/3失败，证明回滚抑制重试、跨账号碰撞和事实冲突未拒绝；T-23-callback-red保留。
- expanded阶段29项中1个NotAMock夹具错误，修复观测真实代理内部Service；初次前端提示断言、旧DDL消息号唯一断言、DAO重复键异常越层已分别修复，未删除测试或放宽检查器；原日志保留。
- native-first的143项不包含新增9项查询测试：漏标项目dev标签被过滤。native-tagged补齐标签后152项全部实际通过；最终又补两项真实HTTP/超时，root实际执行11项。未把首次过滤计作通过。
- 最终检查点脚本首次遇到历史originals中合法null原文，未写出结果即退出；按新文件空原文处理后成功核对47路径与上游哈希，未改变产品代码。

## 所有权、限制与下一步

T-23-checkpoint.json、T-23-owned.diff保存47条产品路径，9条上游重叠、601条非重叠上游不变；与T-03的610输入合并为648。Git忽略的3条历史生成物不计源码，未捕获扩展名的既有imports通过T-03全量快照核对未变，既有上游删除不重复归属本票。HEAD保持76dbbe84a34624234e379661a57b232529e34ed3，index为空。

没有真实账号试发、生产容量验证、提交、推送或部署。查询凭据需要厂商查询权限；不直接处理厂商无本地HMAC合同的推送。下一步T-30以648条路径更新正式输入，执行全change同一候选验收。用户要求全部提交暂缓，implementation/result/candidate仍为空。

## Skill Execution Records

| Skill / phase | 实际执行 | 结果 |
|---|---|---|
| engineering-standards / implement | 分层、HTTP原文与隐私、DSTransactional、DDL基础字段、固定端点/超时、父规范同步 | passed；实际diff、41项环境验收及分层门禁 |
| namewta-fullstack-development / implement | 账号预设表单/场景校验、Entity/Mapper/DAO/Service/UseCase、六文件基座与CAS预约 | passed；前端9项、真实MySQL、base合同 |
| wta-module-guide / implement | Notify控制面、Common发送/原生查询边界与SMTP语义、父索引更新 | passed；供应商说明/官方合同及实际生产调用链 |
| wta-common-modules-guide / implement | 复用SMS4J发送、JsonUtils与LogSanitizer；补充有界原生只读查询，无平行发送框架/新依赖 | passed；11项签名/HTTP协议及日志canary |
| engineering-standards / verify | 完整root、41项环境验收、前端同子树证据、双bundle和治理 | passed；本地ticket验收完成，整change与真实生产未冒充通过 |

## 历史决策与阶段记录

Revision120：T-23开始，610上游hash已核对。用户要求的五种禁用账号预设、必要凭据/签名校验和SMS4J消息ID保留先行；已登记common-sms、DML、通知配置页面、精确父规范与供应商说明写集。官方原生回执均不同于现有HMAC；阿里重试文档互相矛盾、腾讯仅说明再试2次，保留期不猜测。回执安全接入/身份仍在内部研究，不等待用户、不标review。29review/1in_progress/1ready/0Done，全部提交暂缓。

完成标准：五种禁用预设可安全配置启用；真实SDK成功消息ID和模板参数可追溯；receipt/状态/聚合同事务、跨实例/重启/回滚/冲突/早到和真实协议安全适配验收；前后端/DDL/双bundle门禁与实际证据完成。SMTP受理不标送达，无真实凭据时不宣称真实供应商投递通过。

Revision121：T-23三项真实MySQL红灯已复现（回滚重试、跨账号eventId、同事件事实冲突），3/3失败且隔离资源已回收。冻结自定义HMAC接入的渠道+账号configKey+eventId持久身份；业务事实摘要排除每次重签的传输timestamp，其他字段不可变；同一供应商消息多收件人必须明确target，歧义拒绝且不消费receipt。receipt无自动清理，与状态和聚合同事务。原生厂商接入仍单独研究；现有HMAC不宣称原生短信回调。新增common Skill绑定；29review/1in_progress/1ready/0Done，所有提交暂缓。

Revision122：T-23已完成30项真实数据库/HTTP/独立JVM回归及2项JDBC提交故障测试，均零skip；扩展测试首次夹具NotAMock错误已修复，失败记录保留。补齐账号命名空间生命周期：configKey和渠道创建后不可变，已选短信厂商不可替换；账号逻辑删除并永久保留唯一标识，禁止删除后重建同名账号混淆迟到回执。该变更在既有Notify/DDL写集内，随后验证真实Mapper与软删除。原生短信回执接入仍不宣称完成，29review/1in_progress/1ready/0Done，全部提交暂缓。

Revision123：全后端首次838项中1项静态基座断言失败（旧测试要求供应商消息号跨收件人唯一），110项环境测试默认跳过。实际33项MySQL/HTTP验收已证明共享消息号按target关联。先登记精确OssNotifyMigrationUnitTest写集并保存原文，再将过时断言更新为普通关联索引+持久receipt唯一约束；保留既有OSS与基础字段检查，不删测试放宽门禁。前端首次旧参数提示断言失败已按阿里名称/腾讯位置两种合同更新，9项测试/typecheck/lint通过。T-23仍in_progress，全部提交暂缓。

Revision124：带真实GlobalExceptionHandler的回调SQL失败已映射503并可重试，34/34隔离集成通过。复核新增target关联字段会经过HTTP日志，按通知规范的手机号/邮箱不得写日志硬约束，先登记common-json脱敏实现与测试两个精确写集；自定义回执正文/查询参数仅保留脱敏摘要，签名与业务原文不变。之前full/core和838默认测试通过为上一源码阶段；变更后重新运行受影响验证。全部提交暂缓。

Revision126：冻结T-23原生短信状态核对合同：腾讯DescribeSendRecordList（72小时/最多1000条）、阿里QuerySendDetails（BizId+号码+发送日），使用相同账号密钥签名HTTPS查询，复用持久receipt事务。查询失败、截断或歧义不提升状态、不重新发送；查询记录与厂商推送事件区分命名空间，SMTP保持受理语义。实施及协议/真实数据库验证待完成，旧v5证据只对应已保存阶段。29review/1in_progress/1ready/0Done，全部提交暂缓。

Revision127：T-23完成本地review。五种禁用供应商预设、真实发送消息ID、持久回执/账号命名空间及腾讯TC3/阿里ACS3鉴权状态查询已闭合；原生推送不冒充支持。11项协议/HTTP测试、41项真实MySQL/Redis/重启/并发/提交故障零skip通过；默认后端857项=740通过+117环境skip，Notify前端9项/typecheck/lint、full/core同源构建及清单通过。47路径checkpoint、9上游重叠/601非重叠不变，累计648；源码88a5a9a25f9c3d88def978ac6aac64d60522638cfa43150e0cfb3f4924a9fee0。无真实账号试发或生产容量声明；30review/1ready/0Done，下一步T-30同一候选整体验收。HEAD/空index保持，全部提交暂缓。

Revision127治理补验：首次启用ready Spec的T阶段检查报4个标题缺失；正文原已存在，按schema整理Spec章节并清除已解决的T-03/T-23未决描述。保留失败日志，不修改validator或降低ready要求。产品源码与全部测试/构建指纹不变。

Revision129：T-30真实依赖门禁发现T-23新增receipt表未同步受保护初始化器：实际126表/预期125，测试尚未开始即exit1，owned资源已恢复。回到T-23补修，先登记初始化脚本及两份发布合同测试精确写集；T-30暂停为ready，29review/1in_progress/1ready/0Done。保留旧源码88a5a9a的通过与失败证据；修正后重新冻结输入并运行受影响发布/真实服务门禁。全部提交继续暂缓。

Revision130：T-23初始化补修完成review：受保护六文件初始化实际126表，发布117项及真实MySQL/Redis/MinIO八项均零skip通过，资源恢复；仅初始化脚本和两处旧计数断言变化。T-23-checkpoint-v2共50路径，累计649路径；源码1dff1a345e1979d809bb547f3060645d86b4508f3df3aad5fd227de53ab2c504。T-30继续整体验收，30review/1in_progress/0Done；未受影响的前端/后端源树逐文件相同，旧验证证据按明确输入等价关系关联，受影响release/external已重跑。全部提交暂缓。
