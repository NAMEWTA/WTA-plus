---
artifact: learning-goal-probe
probe_id: GP-L-079-b01
lesson_id: L-079
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-079 / b01

审问对象是已写成的 Lesson `lessons/L-079-job-bundle.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **一格**：`C:bundle-full-job`。OBJ-79 原文：能说明 job 只在 `bundle-full` 进入 admin jar，`bundle-core` 排除。档是**解释+对照 pom**。矩阵 (c) **有独行** `bundle-full-job`（Component，仍 uncovered，备注空，挂 L-079）。矩阵 **没有** `bundle-full-ai` / `bundle-full-demo` / `bundle-full-workflow` / `bundle-core-profile` 独行，也没有 `snail-job.enabled`、`wta-common-job`、`wta-snailjob-server`、`createJobService` 符号。矩阵 (a) 已印的 job 工人是 L-078 的 `AlipayBillTask` / `WechatBillTask` / `SummaryBillTask` / `TestAnnoJobExecutor`（chain 四名；磁盘车间另有五份类文件）。矩阵 (c) `wta-admin` / `wta-modules` / `layered-classic` 已 covered（L-002 / L-003）。矩阵 (d) **没有**「任务被点名 / 客户端电闸」走廊。本批按派单只审问这一格 (c) 插头；口试按磁盘：admin 常驻名单**没有** `wta-job`；`bundle-full` 默认亮且第一颗额外依赖就是它；`bundle-core` 只有两份档案；`verify-admin-bundle.sh` 对 `wta-job` 做 full 必有 / core 不得有；`ProfileModuleGraphContractTest` **一句都不写** `wta-job`。不要把九个工人、YAML 电闸、extend 主任、监控 iframe、AI 注册窗、或同坐 full 插排的 ai/demo/workflow 升成本格第二张章，也不要用本课再盖已经 covered 的 `C:wta-admin`。

抽查锚点（2026-09-17）：`backend/wta-admin/pom.xml` 第 11 / 18–144 / 89–117 / 147–206 / 150–153 / 155–159 / 191–205 / 209 / 211–221 行；`scripts/ci/verify-admin-bundle.sh` 第 4–11 / 13–18 / 21–22 / 24–26 / 36–42 行；`scripts/README.md` 第 117–120 行；`ProfileModuleGraphContractTest.java` 第 11 / 29–37 行；`backend/wta-modules/pom.xml` 第 19–20 行；`backend/wta-modules/wta-job/pom.xml` 第 12–30 行；`backend/pom.xml` 第 17 / 82–116 / 435–440 / 566 行；`NamewtaApplication.java` 第 1 / 25–26 行；`SnailJobConfig.java` 第 21–24 行；`wta-common-job/.../AutoConfiguration.imports` 第 1 行；`wta-common-job/pom.xml` 第 27–34 行；`application-{local,dev,prod}.yml` 的 `snail-job.enabled: false`；`release-artifacts/scripts/release-manage.sh` 第 185–200 行；`release-artifacts/README.md` 第 65 行；`00-project-profile.md` 第 39–41 / 59 行；`01-module-map.md` 第 73 行；`03-backend-module-modes.md` 第 24 行；`wta-extend/pom.xml` 第 16 行；`SnailJobServerApplication.java` 第 1 / 12–21 行；`wta-system/pom.xml` 第 58 行 `wta-common-oss`；admin-web README 第 28 行；`frontend/packages/{domains,web-domains}/` 目录（无 `job`）；`org.namewta.job.snailjob` 九个类文件。工作树 **无** `.github/`、**无** `wta-admin/target/wta-admin.jar`、admin POM **无** enforcer、`wta-job` **零** `@RestController`、业务/组装 POM 消费 `wta-common-job` **只有** `wta-job`。

## Q1 澄清

- objective_id: OBJ-79
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-79；chain L-079 覆盖列只 `C:bundle-full-job`；课 `claimed_cells` 一格；矩阵 (c) 该 Component 仍 uncovered、无 ai/demo/workflow bundle 独行、无 enabled/server/厨房符号；(a) 工人挂 L-078；(c) `wta-admin` / 房间 / 登记表已 covered；(d) 无点名走廊；课开篇「口试名单就是矩阵 (c) 这一格」+ 负空间 L-078 / L-002 / L-080 / L-024 / L-003
- difficulty: medium

课把调度工人收成「宏观插座板上的一颗插头：默认那根插排才请它，核心店拔掉」。计划里至少有五套可数对象，派单点名的是 **一格 (c)**：

1. **OBJ-79 原文。** `course.md`：能说明 job 只在 `bundle-full` 进入 admin jar，`bundle-core` 排除。一句里是 **对照 pom 的进盒 / 排除**，不是九个花名、不是 `snail-job.enabled`、不是 `SnailAiController.registerCurrentUser`，也没有把「整根 full 插排」写成口试清单。课把头扩成三份名单 + 默认同 POM 熄灯 + 反应器≠外卖盒 + 脚本 `jar tf`，那是走查，不是第二张矩阵行。
2. **chain / 课 `claimed_cells`。** 一格：`C:bundle-full-job`。chain 估时 35；课 `estimated_minutes: 38`。前置 L-078。覆盖列 **不**写 (a) 工人，**不**写 (b)，**不**写 (d)，**不**写 `wta-common-job`。
3. **矩阵几何。** (c) 独行与派单 1:1。Lead 若按**现印这一行**标 covered，**不会**绑死 ai/demo/workflow——矩阵根本没有那些 bundle 行。Lead 若凭「课里出现了 `bundle-full` 四字」把已 covered 的 `C:wta-admin` 再盖一次，或凭「同坐一根插排」发明三张新 Component 行并标 covered，才是假闭合。Lead 若因 chain 只印 (c)、工人在 L-078、Java 契约测试不写 `wta-job` 就把本行留空洞，是假判。
4. **课自己的操作定义。** 手指必须同时点到三份名单：常驻没有 job；full 直接声明且 `activeByDefault`；core 只有 person / enterprise。口试动词是「类进不进 `wta-admin.jar` 的 `BOOT-INF/lib/wta-job-`」，不是「任务会跑」。闸 2（yml）和闸 3（extend 主任）是对照。档案两个剖面都在，用来卡住「core = 拆大厅」的误读，不是本格第二张章。
5. **邻格。** L-002 已经把插座板画在 `C:wta-admin` 上并 covered。L-078 车间九个类。L-080 同是 full-only 但有 HTTP 窗。L-024 iframe 看主任楼。L-003 登记 `wta-job` 仍是 classic，拔插头不是改模式。前端没有 `packages/domains/job` / `web-domains/job`。
6. **未入库。** 矩阵没有 `snail-job.enabled` 行，没有 `verify-admin-bundle.sh` 符号行，没有「传递 `wta-common-job`」行，没有「反应器编译工人」行。这些是本格机制，不是第二组格子。

问：本格闭合标准是「三份名单（常驻无 / full 有 / core 无）+ 同一 admin POM 里显式 core 才熄灭默认 full + `-Pprod` 熄不掉子 POM 的 full + 反应器仍列 `wta-job` 不等于 fat jar 里有 `BOOT-INF/lib/wta-job-` + 脚本 optional 数组对 job 的两支 if，且不把工人花名、yml 电闸、主任进程、iframe、AI 窗、同排 ai/demo/workflow、已 covered 的 admin 容器算进来」，还是 OBJ 那句必须先等 GP-L-078 四名工人 mined、或必须先给 `ProfileModuleGraphContractTest` 补上 `wta-job` 断言、或必须把矩阵拆出 ai/demo/workflow bundle 行才算讲完？Lead 若凭「full 插排上还有四件电器」拒绝给 `C:bundle-full-job` 盖章，是否假判？Lead 若用本课把 `C:wta-admin` 或未入库的 ai bundle 标 covered，是否假闭合？

## Q2 证据

- objective_id: OBJ-79
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: admin POM 11 / 89–117 / 147–206 / 150–159 / 191–205 / 209 / 211–221；verify-admin-bundle.sh 4–11 / 21–22 / 24–26 / 36–42；scripts/README 117–120；ProfileModuleGraphContractTest 11 / 29–37；wta-modules/pom.xml 19–20；wta-job/pom.xml 12–30；backend/pom.xml 17 / 82–116 / 435–440 / 566；NamewtaApplication 1 / 25–26；SnailJobConfig 21–24；AutoConfiguration.imports 1；yml 三份 enabled false；release-manage.sh 185–200；release README 65；00-project-profile 39–41；01-module-map 73；modes.md 24；extend pom 16；SnailJobServerApplication 1 / 12；wta-system/pom.xml 58；admin-web README 28；frontend packages 无 job；九个 snailjob 类；零 RestController；零 enforcer；零 `.github/`；零当前 `wta-admin.jar`；消费 common-job 的业务 POM 只有 wta-job
- difficulty: high

课的插头 + 三道闸哪些能在工作树直接复现，哪些只是对照、或口试把邻课/README 缩写说成本格门禁？

**仍能复现（本格 `C:bundle-full-job`）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 插座在 admin POM，不在 `wta-job/pom.xml` | admin `<profiles>` 147–206；job 模块 POM 只声明自己的两颗依赖 |
| 常驻业务插头无 job | admin 顶部 89–117：api / system / third / notify / sso；18–144 全程无 `wta-job` |
| `bundle-full` 默认亮，第一颗额外就是 job | 150–153 `activeByDefault`；155–159 artifact `wta-job`，注释「调度任务模块」 |
| full 另外五件同坐插排，但不是本格 | 161–187：ai / demo / workflow / person / enterprise |
| `bundle-core` 无 activation，只有两份档案 | 191–205；注释「显式激活后停用 bundle-full」 |
| 剖面依赖不写 version；版本锁在父 `dependencyManagement` | admin 158 无 `<version>`；父 POM 17 `<revision>6.0.0</revision>`；435–440 锁 `wta-job` |
| 环境灯不是 bundle | 父 POM 82–116：`local` / `dev` / `prod`；`dev` 104 `activeByDefault`；只写 `profiles.active`。admin POM **没有**这些 id |
| Surefire groups 绑环境灯，不绑 bundle | 父 POM 566 `<groups>${profiles.active}</groups>` |
| 产物名与 repackage 在 admin | 11 `packaging` jar；209 `finalName` artifactId；211–221 `spring-boot-maven-plugin` / `repackage` |
| 反应器永远有工人房间 | `wta-modules/pom.xml` 20 `<module>wta-job</module>` |
| 工人 jar 只依赖 json + common-job | `wta-job/pom.xml` 21–30；无 web / mybatis / `wta-api` |
| 业务侧只有 job 消费 `wta-common-job` | 全仓 POM：模块列表与 bom 锁版本之外，依赖消费点只有 `wta-job` |
| json 不会随 job 被 core 拿走 | `wta-api` / `wta-notify` / `wta-sso` / `wta-third` / 两份档案等仍声明 `wta-common-json` |
| 脚本对 job 的硬 if | `optional=(wta-job wta-ai wta-demo wta-workflow)`；full 缺则失败，core 见到则 `core bundle unexpectedly contains: wta-job` |
| 脚本 required 含 third / sso，也含传递 `wta-common-oss` | 脚本 21 行五件；`wta-system/pom.xml` 58 才是 oss 直依；admin 常驻名单没有 oss |
| README 表格缩写了 required | `scripts/README.md` 117–120 只写 system / common-notify / common-oss，漏 third / sso。口试以脚本数组为准（课已写） |
| Java 契约测试不断言 job | `fullAndCoreBundlesBothAssembleProfileWhileWorkflowRemainsFullOnly` 只认 person / enterprise / workflow；文件内零 `wta-job`；`@Tag("dev")` |
| 启动类无 `scanBasePackages` | `NamewtaApplication` 包 `org.namewta`；只有 `@SpringBootApplication` |
| 无互斥插件 | admin POM 无 enforcer；两根都显式 `-P` 时依赖集合并（Maven 加法），job 会回来 |
| 发布 full 不写 bundle 名 | `release-manage.sh` 189–200：`bundle == full` 时只要 `env_name`；非 full 拼 `env_name,bundle-core`；一律 `-DskipTests` |
| 画像 / backend README 的 core 命令用 `maven.test.skip` | `00-project-profile.md` 39–40；`backend/README.md` 55–58；与发布脚本的 skipTests **不是同一条命令** |
| 显式 `-Pbundle-full` 仍进 job | `release-artifacts/README.md` 65 |
| Skill 摘要对 job 成立 | `01-module-map.md` 73：默认 full 接入 job/…，显式 core 排除 job/…、保留 profile |
| classic 登记不随剖面变 | `03-backend-module-modes.md` 24 仍是 `wta-modules/wta-job` classic |
| 主任另一进程 | `wta-extend/pom.xml` 16；`org.namewta.snailjob.SnailJobServerApplication` 自身带 `@SpringBootApplication` |
| 前端没有 job 厨房 | `packages/domains` 与 `web-domains` 只有 admin/ai/demo/notify/profile/system/third/workflow；零 `createJobService` |
| 工人无 HTTP 窗 | `wta-job` 下零 `@RestController` |
| 本工作树还没有可 `jar tf` 的盘子 | 无 `backend/wta-admin/target/wta-admin.jar`；脚本缺产物退出码 1。课边界 4 已写：口试证据链是 POM + 脚本 + 画像命令 |

**对照，不能当本格门禁：**

| 声称 | 磁盘锚点 | 为何不是本格闭合条件 |
| --- | --- | --- |
| 闸 2 默认关 | 三份 yml `snail-job.enabled: false`；`SnailJobConfig` `@ConditionalOnProperty(..., havingValue="true")` | 配置键住在 **admin 资源**，core 包里键还在；类不在才是闸 1。矩阵无 enabled 行 |
| 自动配置登记 | `AutoConfiguration.imports` 一行 `SnailJobConfig`；common-job 再拉 `snail-job-client-starter` / `job-core` | 传递边解释「core 通常连客户端一起没有」。工具间所有权不是本格 |
| 九个车间类 | `org.namewta.job.snailjob` 九文件；其中 `TestClassJobExecutor` 走抽象类，**没有** `@JobExecutor` | L-078。本课只认类在不在 jar |
| 画像把脚本「来源」写成 workflow 文件 | `00-project-profile.md` 41 行 `.github/workflows/quality-gates.yml`；本树 **无** `.github/` | 口试仍以 `scripts/ci/verify-admin-bundle.sh` 源码为准。不要把缺失的 GHA 文件当本格未讲 |
| admin-web README 点名 `wta-job` | README 28 行「通过领域合同访问 … `wta-job`」 | 没有对应 domain 包。页面还在 ≠ 工人进了浏览器 |
| `ProfileModuleGraphContractTest` 绿 | 只护档案 + workflow | 绿不能证明 job 进了盒；红也不能证明 job 没进盒 |

问：合格读者若只背课里的「core 更瘦」，能否同时指着「档案仍在、常驻 system/third/sso 仍在、job 不在」三句？若只跑 `./mvnw test` 看到 `ProfileModuleGraphContractTest` 通过，会不会误报 OBJ-79 已有 Java 断言？若把 `scripts/README.md` 的三件 required 当成口试名单，会不会漏掉脚本里的 `wta-third` / `wta-sso`——而这漏项课**已经**写成「以脚本源码为准」，还要不要当成 L 缺机制？本工作树没有 fat jar，是否等于本格没讲完，还是课边界 4 已经把「先 clean package 再 `jar tf`」和「没有产物脚本直接失败」分开了？

## Q3 假设与反例

- objective_id: OBJ-79
- bloom_level: analyze
- socratic_move: assumptions
- expected_evidence: 课正例 1–10 / 反例 1–15 / 边界 1–10；admin 两根 profile 无第三根 `bundle-*`；父 `prod` 不在 admin POM；release-manage 双灯拼串；两根都显式则合并；yml 键可空放；iframe ≠ jar；classic 行不随剖面改；无 frontend dual-bundle
- difficulty: high

课的正例/反例是否把合法形状和违规形状分干净？合格读者读完后会不会仍抓住下面某一句？

1. **「`wta-job` 是 admin 常驻模块。」** 常驻 89–117 没有它。只说「admin POM 很长所以一定有」是否被正例 1 挡住？
2. **「`-Pbundle-core` 只是把 `snail-job.enabled` 改成 false。」** 三份 yml 两个剖面都是 false。core 改的是 classpath。反例 2 是否够硬？
3. **「反应器有 `wta-job`，fat jar 就有工人。」** 模块列表第 20 行永远在。core 的外卖盒可以没有糖。`-pl wta-admin -am -Pbundle-core` 甚至可以不编这间房；全反应器 `package` 仍会编。两种命令是否被当成同一件事？
4. **「`-Pprod` 已经 -P 了，子 POM 的 full 会灭。」** `prod` 不在 admin POM。发布 full 正是这条路，job **进**盒。反例 9 是否挡住「任意 -P 都灭 full」？
5. **「`-Pbundle-full,bundle-core` 更保险，得到纯 core。」** 无 enforcer。两根都显式亮，job 回来，core 校验会打红。反例 8 / 误区 9。
6. **「`ProfileModuleGraphContractTest` 失败说明 job 没打包。」** 测试不读这四个字。方法名自己就承认只护 workflow。
7. **「README 表格和脚本完全一样。」** 表格三件 vs 脚本五件 required。课要口试以脚本为准。读者会不会反过来用表格打脚本的第三/sso？
8. **「frontend README 写了 `wta-job`，所以浏览器有厨房。」** 2026-09-17 无 domain 包。反例 7。
9. **「把 `wta-snailjob-server` 写进 admin 依赖就算接线 job。」** 那是另一 `@SpringBootApplication`，包名仍在 `org.namewta.**` 扫描里。L-002 已禁；本课重复。读者会不会把闸 3 和闸 1 并成一句「没启用 job」？
10. **「YAML 还有 `snail-job:`，core 里客户端一定还在。」** 键可以空放着。没有 `wta-common-job` 就没有那行 AutoConfiguration。
11. **「core 把 job 的两个依赖都删了，json 也走。」** json 还有别的消费者。被捎走的是 **只** 给 job 用的 `wta-common-job`。
12. **「不写 `-P` 就是 core。」** 不写 bundle 名时 full 亮。画像默认打包命令正是这条。
13. **「本课覆盖 ai / demo / workflow 的 bundle 格。」** 矩阵没有那些行。同坐插排 ≠ 本格。
14. **「OBJ-79 要口述 `AlipayBillTask` 怎么加金额。」** 那是 L-078。本课指插头。
15. **「POM 注释写『其他 bundle-*』，所以只有名字叫 bundle 的剖面才会灭灯。」** 课机制 2 写的是 Maven 真规则：同一 POM **任一**显式剖面都会灭 `activeByDefault`。现在 admin 只有两根，所以和注释重合。若有人在 admin POM 加第三根非 bundle 剖面并 `-P` 它，full 也会灭、job 会掉。读者会不会把注释的 `bundle-*` 当成互斥插件？

读完后仍说不出的最小集合：job 不在常驻、在默认 full、不在 core；父环境灯灭不了这根组装灯；两根组装灯一起显式亮则合并；编译图不是外卖盒；脚本数的是 `BOOT-INF/lib/wta-job-` 前缀，Java 契约测试不护这颗糖。

## Q4 视角

- objective_id: OBJ-79
- bloom_level: analyze
- socratic_move: viewpoints
- expected_evidence: 矩阵 (c) 独行 vs L-002 容器；L-078 四名 (a)；L-080 HTTP；L-024 iframe；L-003 classic 行；release-manage vs 画像 skip 旗标；脚本 vs README 表格；传递 oss / 不检查档案 / 不检查 common-job；无 frontend dual-bundle
- difficulty: medium

换一个观察者，架构边缺了哪一条？缺的那条是否属于 **本课这一格 (c) 插头**？

| 视角 | 多出来的边 | 是否本课这一格 |
| --- | --- | --- |
| 矩阵维护者 | (c) 独行 `bundle-full-job` vs chain 同名；无 ai/demo/workflow bundle 行；无 enabled 行 | **是** 闭合定义。本切片可视为已讲。独行可标 covered。不要发明邻接 bundle 行，不要用本课再盖 `C:wta-admin` |
| POM 三份名单 | 常驻无 / full 第一颗 / core 两份档案 | **是** OBJ-79 的手指动作 |
| Maven 灯 | 同 POM 显式另一根才灭默认 full；父 `prod`/`dev` 是另一排；两根都显式则并集 | **是** 机制 2。不是第三根 `bundle-job` |
| 反应器 vs 盘子 | `wta-modules` 永远编译得起；`BOOT-INF/lib/wta-job-` 才是客人盘子；`-pl -am` 与全反应器不是同一瘦法 | **是** L-002 那句落到 job 上。不要另开反应器课 |
| 门禁脚本 | full 必有 job；core 不得有；required 五件；不检查档案、不检查 `wta-notify` 模块名、不检查 `wta-common-job` | **是** 本格硬证据。README 缩写是对照 |
| Java 契约测试 | 档案双剖面 + workflow full-only；`@Tag("dev")` | 邻居对照 **是**。把它升成本格断言 **否** |
| 传递客户端 | job → common-job → starter/core；core 通常连 `SnailJobConfig` 一起没有 | 对照 **是**。common 所有权 **否** |
| 传递 oss | 脚本两个剖面都要 `wta-common-oss`，来自 system | 提醒 fat jar 可含传递名 **是**。job 是 full **直接**声明，不是传递 |
| 画像 vs 发布脚本 | 画像 core 用 `maven.test.skip`；`release-manage.sh` 两剖面都 `-DskipTests`；都要求 `clean` | 两条合法命令族 **是** 边界。不要说成课自相矛盾 |
| 闸 2 / 闸 3 | yml 默认 false；extend 主任另一 JVM | 拆开三道闸 **是**。标 covered **否** |
| L-002 插座板 | 容器 `C:wta-admin` 已 covered | 本课放大 job 这一颗 **是**。再盖容器 **否** |
| L-078 车间 | 九类、八个 `@JobExecutor` 名 + 一个抽象类执行器 | 对照「类在 jar ≠ 被点名」**是**。工人 (a) **否** |
| L-080 AI | 同是 full-only，但有 `SnailAiController` | full-only 同类问题 **是**。AI 窗 **否** |
| L-024 玻璃 | 菜单「任务调度」可仍是 iframe 看主任楼 | 「页面还在」不能证明 jar 里有 job **是**。监控格 **否** |
| L-003 登记 | classic 行不随 core 消失 | 拔插头 ≠ 改模式 **是**。登记表已 covered **否** |
| 前端 dual-bundle | admin-web `services.ts` 无这根 Maven 插排 | 负空间 **是**。不要发明前端 bundle 格 |
| 迁移：core 也要跑定时 | 改 core 剖面或常驻 + 改脚本「不得包含」 | 变式，不是本格未讲完 |
| 迁移：只要客户端不要演示工人 | 不要让 admin 直接依赖 `wta-common-job` 绕过 bundle | 变式。新能力默认另登 layered |

问：Lead 若凭「Component 行在表里」把 `bundle-full-job` 标 covered，是否还要求读者能指着 **三份名单、同 POM 熄灯、父环境灯熄不掉、并集陷阱、反应器≠盘子、脚本两支 if、Java 测试不护这颗**？这些是不是已经在本格机制/反例里，而不需要等 L-078 工人课、也不需要先改 `ProfileModuleGraphContractTest`、更不需要给 ai/demo/workflow 发明矩阵行才给**本切片**当已讲？

## Q5 元问题

- objective_id: OBJ-79
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课；一课超 1 个容器是 Lead 裁决不是 b01 填表）；本课累计 5/10；`split_depth: 0`；矩阵 (c) `bundle-full-job` 仍 uncovered、无邻接 bundle 独行、工人 (a) 挂 L-078、`C:wta-admin` 已 covered
- difficulty: low

建议：`defer`

原因：

- **`C:bundle-full-job` 是 chain 切片，也是矩阵独行。** L 合同章节齐全（宏观插座板、直觉十二句、定义表、机制链 1–8、带 caption / 文字等价物的三张图、正反例、变式、误区、暂停走查、来源表 S-001 / S-007 / S-L002-01 / S-L078-06 / S-L079-01…08）。能复现：常驻无 job、full 默认第一颗、core 无 job 有档案、父环境灯不是 bundle、反应器仍有模块、脚本 optional 对 job 的两支 if、Java 测试不写 job、无 enforcer 故双 `-P` 会把 job 请回来、主任在 extend、前端无 job 包。不必为「Component 的名字」再挖五问。Lead 可将该行标 covered。
- **进盒/排除是这一格的语义，不是第二格。** 闸 2 / 闸 3、传递 `wta-common-job`、档案双剖面、脚本 required 五件 vs README 三件，已在课的机制 5–7 / 反例 2 / 边界 1。不要发明 enabled 行或 ai bundle 行。`C:wta-admin` 已由 L-002 独盖。
- 本批可粘贴的精度项不挡住切片视为已讲：（1）画像 core 用 `maven.test.skip`、发布脚本两剖面都 `skipTests`，课两条都引了；（2）工作树此刻没有 `wta-admin.jar`，课边界 4 已把「先打包再数盘子」写明；（3）课估时 38、chain 35；（4）`TestClassJobExecutor` 不是 `@JobExecutor` 注解——那是 L-078 花名精度，不是本格插头。不要把九个工人、AI 窗、iframe、已 covered 的 admin 容器塞进本格。
- 不建议 `mine-more`：b01 未证明本切片还有真实矩阵空洞；再五问只会把 L-078 工人、L-080 AI、L-002 插座板或 yml 电闸再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；Java 测试不护 job、README 缩写 required、缺失 `.github/`，课已经把口试钉在 POM + 脚本源码，不是第二容器。不建议 `split-lesson`：未满 10 问；一格 Component 是 chain 原设计的同一 OBJ-79，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (a)/(b)/(d) 行。不建议等 GP-L-078 才给本切片当已讲。不建议用本课改 `C:wta-admin` 或登记表。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| C:bundle-full-job | 课按磁盘讲了三份名单、同 POM 熄灯、父环境灯熄不掉、并集陷阱、反应器≠盘子、脚本对 `wta-job` 的两支 if、Java 契约不断言 job、无 enforcer、主任另一进程、前端无厨房。残留精度：画像 vs 发布脚本的 skip 旗标（课已并列）；当前树无 fat jar（边界 4）；课估时 38 vs chain 35；车间九类里一个不是 `@JobExecutor`（邻课花名）。 | 可选半句：口试「必须包含」以脚本 `required`/`optional` 数组为准，不拿 `scripts/README.md` 三件表、也不拿缺失的 workflow 文件当证据。不要求重写机制链。Lead **可以**改本行为 covered（证据锚本 probe 与 L-079 来源表 S-L079-01 / S-L079-02）。 | defer（切片已讲；独行可闭合） |
| C:wta-admin | 插座板总图已 covered。本课只放大 job 这一颗。 | 不要用本课再盖容器章。 | defer（L-002 已 covered） |
| C:wta-modules / C:layered-classic | 房间与 classic 行已 covered。`wta-job` 仍登记 classic。 | 不要把 core 包说成「job 改成了 layered」或「模块被注销」。 | defer（L-002 / L-003 已 covered） |
| A:AlipayBillTask / WechatBillTask / SummaryBillTask / TestAnnoJobExecutor | 九个类文件只作「扫到 Bean」对照。chain 四名仍 uncovered。 | 不要用本课盖工人章。 | defer（L-078） |
| A:SnailAiController.registerCurrentUser | 同坐 full 插排，但有 HTTP 窗。 | 不要因「都是 full-only」并进本格。 | defer（L-080） |
| A:createAiService / createAiWebDomain | 前端 AI 厨房未因本课讲完。 | 保持。 | defer（L-081） |
| 监控 iframe / L-024 | 「页面还在」不能证明 jar 含 `wta-job`。 | 玻璃看主任楼。不要改 L-024。 | defer（L-024 已 covered） |
| snail-job.enabled / SnailJobConfig / wta-common-job | 闸 2 与传递客户端已写进对照。矩阵无这些行。 | 不要为本课发明 (a)/(c) 新行。 | defer（未入库） |
| wta-snailjob-server | 主任另一进程，不在任一 admin bundle。 | 保持范围外内部；不要写进 admin 依赖。 | defer（范围外 / L-002 变式 D） |
| 未入库 C:bundle-full-ai / demo / workflow | 同坐插排。矩阵无行。 | **不要**因本课发明并标 covered。 | defer（未入库） |
| ProfileModuleGraphContractTest | 邻居测试护档案+workflow，不护 job。 | 若要 Java 也护这格，那是改测试不是改课。不要把测试绿当 OBJ-79 门禁。 | defer（产品测试洞；非本格 L 重写） |
| (b) 客户端电闸 / (d) 主任点名流 | 矩阵无此行。课已把三道闸画进 (c)。 | 不要发明点名数据流行。 | defer（未入库） |
| MySQL 8.4 / dao-mapper-ownership / Redis 8 | 基座与所有权不是插头课。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-083 / L-085） |
| 前端 job domain | 2026-09-17 无此包。README 点名是空映射。 | 保持不存在，不要编 `createJobService` 行。 | defer（源码无此符号） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本切片无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（工人/AI/容器插座板是邻课格子，课内已把 job 插头讲完并显式禁止整行假闭合）；合格读者说得出组件目的（job 只经默认 full 进 admin jar、显式 core 排除）、主路径（不写 bundle 名或 `-Pprod` / 显式 `-Pbundle-full` → 盘子里有 `BOOT-INF/lib/wta-job-`；`verify-admin-bundle.sh full` 认这颗糖）与排除路径（`-Pbundle-core` 或发布脚本 `env,bundle-core` → 盘子里不该有；脚本见到就失败；双组装灯显式并集则糖会回来）；把可选半句写进原课不会超过 40 分钟；邻课已规划。满 10 问前不拆。闸 2/闸 3 虽同页出现，仍是 chain 原设计的同一 OBJ-79 对照，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / split 提案：否（miner 不改矩阵）；split 提案为 none；缺口表对 L-002/L-003 已 covered 的容器与登记表、L-078 工人、L-080/L-081 AI、L-024 玻璃、未入库的 enabled/common-job/主任进程/ai-demo-workflow bundle 行、产品测试洞、未入库 (b)/(d)、MySQL/Redis/mapper 容器、以及不存在的前端 job 包显式建议 `defer`；对矩阵 (c) 行 `bundle-full-job` 建议 Lead 标 covered（证据锚本 probe 与 L-079 来源表 S-L079-01…02）。本课不主张发明邻接 bundle 行、用本课盖工人四名、把 `ProfileModuleGraphContractTest` 绿当成 job 门禁、或等 GP-L-078 才给本切片盖章。
