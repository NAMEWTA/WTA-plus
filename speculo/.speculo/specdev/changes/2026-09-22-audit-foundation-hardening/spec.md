---
schema_version: 3
artifact: "spec"
change: "2026-09-22-audit-foundation-hardening"
status: "ready"
ready_for_tickets: true
sources: ["USER-DECISION:2026-09-22", "<Path>docs/reviews/WTA-plus-review-64b4ea7.md</Path>"]
---

# Spec：现有基座审计整改

## 1. 问题与目标

修复已确认的现有功能错误与不必要故障耦合，减少启动配置而不增加平台功能。成功以每个原报告编号的真实行为/测试证据为准，不以文件数量或“全通过”文案为准。

## 2. 解决方案与外部行为

收件箱与实时推送分离；短信快照正确；本地写入原子，外部未知不误发；重试目标/截止/撤回执行；附件明确授权；历史分页；有界批量；OSS诊断不控制访问；迁移操作互斥；安全默认配置与薄启动入口。

## 3. 用户故事

US-001：作者部署基座时只配置真正依赖，当前用户能正常读到站内信，错误可定位而非被兜底吞掉。

## 4. 验收合同

AC-001：以下原报告编号全部具有实际修复或已证伪/不适用的证据，整体验收通过；原报告摘要保持不变。

| ID | 可观察结果 | 验证接缝 |
|---|---|---|
| S-01, S-02, N-01 | 关闭实时推送时持久收件箱仍查询，跨用户旧响应被丢弃；公共配置无真实凭据、未知跨源被拒绝。 | 默认环境构建、CorsPolicyTest、无 SSE 的真实浏览器消息盒子与 A/B 会话回归。 |
| N-02, N-03, N-04 | 短信真实逻辑快照通过下层校验；IN_APP 原子幂等且无 WAITING_RECEIPT；确定未发出的失败可重试，未知外部发送不自动重发。 | 真实 Dispatcher+渠道 fake provider、MySQL 并发收件人和回滚、Redis 幂等窗口内第一次拒绝第二次接受。 |
| N-05, N-06, N-07, N-09 | 定向重试不影响其他投递，路径主键权威；过期不调用 Provider；撤回停止未发，已发保留；邮件无业务路径可发送，附件明确授权映射。 | Controller/应用服务及实际渠道合同，截止前后与取消竞态，零/一/多附件，公告详情和撤回浏览器场景。 |
| N-08, N-10 | 超过500条历史仍可访问；未读总数来自服务器，UI区分失败与空；规模优化以计数或实际测量为证据，不凭估计引入中间件。 | 501条按用户稳定分页、未读计数、旧响应及失败状态；SQL 调用计数与有界批量基准，完整前端类型/构建/E2E。 |
| O-01, O-02, O-03, O-04 | 无MinIO不阻止核心启动；未配canary不阻止合法对象操作；策略不可读是未知；恢复与清理互斥；旧OSS列表响应不覆盖新筛选。 | 最小权限MinIO真实上传/下载、禁止桶管理权限仍可运行；真实DB恢复/清理竞态；OSS查询乱序组件/浏览器测试。 |
| D-01, D-02 | 正常启动不依赖深度JAR检查和手工解析配置；业务策略有默认值；修复证据/操作手册完整，最终单PR合并并按完成门归档。 | 原构建保护/清理安全测试保留在显式修复入口；原样完整质量门禁、真实外部服务与两次增量构建，摘要核验原报告未丢失。 |

## 5. 范围

IN：原报告16项发现与D-01/D-02建议、实现、消费者、测试、文档、归档。REUSE：现有架构与可靠性基础。OUT：SSO当前实现、外部密码实际轮换、历史强推、生产部署与重要运行数据变更。后者不是已完成操作；完成报告必须披露。

## 6. 已锁定实现约束

用户无兼容要求；不增加新消息队列、状态平台或配置真相源。保留真实授权、Outbox、最小权限及回执验证。永久知识只在A阶段按证据提升。

## 7. 数据、接口与兼容

更新全部仓内消费者，不提供静默兼容。SQL仅既有基座。原报告原样保留并验摘要。SSO边界：未来内置授权服务器可关闭，业务App对内置/外部issuer均走标准交互；本次不改其实现。

## 8. 非功能要求

安全：无真实凭据，外部未知不重发。容量：批次有界，性能声明需计数/测量。可用性：不需要的外部配置不进入核心请求或启动。运营：错误类别稳定、提示不冒充成功。

## 9. 验证策略

定向回归先验证失败，再修复；固定头执行原Quality gates、临时隔离MySQL/Redis/MinIO、fake provider与浏览器E2E。禁止删除测试/放宽规则获得通过。记录命令、环境、真实SHA、退出码、失败修复过程。

## 10. 风险、假设与未决问题

跨层合同可能暴露原有测试盲点；修复以可复现事实为准。报告N-10是规模风险而非事故，先测后改；NotificationMode已仅ASYNC，不重复制造修改。未决产品问题：无。真实密码轮换不能由GitHub执行，记录未执行并与代码验收区分。
