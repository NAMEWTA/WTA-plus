# T-48 evidence

Linux fixture only. Windows real-machine run is `user-waived/not-run`, recorded in `evidence/T-48-windows-user-waiver-2026-09-24.md`. It is not a pass and not a zero-skip test.

## Commands

```bash
bash -n scripts/start-dev.sh
bash scripts/ci/verify-dev-build-guard.sh
node --test release-artifacts/tests/local-config-safety.test.mjs
```

`bash -n` exited 0. `verify-dev-build-guard.sh` exited 0 and printed the choice-contract passed line. The node test exited 0 with tests=3, pass=3, fail=0, skipped=0, cancelled=0.

The node fixture runs `start backend` twice on a path that contains a space, then again with `SERVER_PORT` set, then the menu option 2. Recorded Maven arguments contain `spring-boot:run` and do not contain `--server.port`, ` clean `, or `install`. `repair backend` records ` clean `. The private canary is absent from stdout and stderr. An existing `SPRING_CONFIG_ADDITIONAL_LOCATION` is preserved.

`verify-dev-build-guard.sh` shows `repair backend` stops on a held build lock before the clean message and reports the owner PID. Ordinary `start backend` does not print the clean-install message. Cache deletion still rejects paths outside `frontend/` and files that are not on the cache list. The port parser does not echo the datasource password.

## Behavior

`start` does not clean, reinstall, or scan JAR sentinels, and it does not pass `--server.port`. `build`, `doctor`, and `repair` remain separate commands. `repair backend` still uses `-pl wta-admin -am ... clean install` from the reactor root. `spring-boot:run` stays `-pl wta-admin` without `-am`.

## Skill

engineering-standards was applied to the shell entry only. No backend business module, HTTP contract, or database schema changed.
