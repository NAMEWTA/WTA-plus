# T-45 Dispatch02 — 浏览器事实文案与安全定位恢复包

## 1. 三次结果与根因边界

A1固定081c75ff真实run e9853018a23b0d59因公开域名重复拼桶而匿名HEAD/GET404；d1fd57fb修复并以168定向、A2真实run6e5d5c24c38c793d证明。A2生成候选9c567ba0全前端typecheck报DefaultRow不满足完整VO；C3 d98936b68收窄输入并拒缺ID，771前端测试/类型/lint/架构及三App329构建、同SHA完整JAR通过。

C3真实UIv2 run c231806fb841027a是1pass1fail、0skip/flaky、cleanup[]，普通用户403/匿名401通过；raw report按安全合同删除，保留结果不能证明首次失败行。确定的静态夹具错误是exact文本期待“允许 / 可读”“拒绝”，页面固定文案为“观察到允许”“观察到拒绝”；如果到此断言一定不匹配，但不将该推论冒称已见最初栈。原三次结果保持于candidate-a1/a2/c3 manifests。

## 2. 独立审查与影响边界

legacy_audit核实上述文案与其他主要标题/row/button/dialog/caveat/subject/未知selector对应；意见c3-browser-retrospective-review.md已保留。产品25路径与全部合同不再修改，不改变权限、事实、路由、UI文案、timeout或后端返回迎合测试。受限MinIO诊断五只读请求、UNKNOWN与单对象范围、普通/匿名拒绝仍是硬断言。

## 3. 实质改变与派单

ops_audit仅在/tmp/wta-t45创建UI v3私有新版本，保留v2原字节：精确修正两种观察文案，preflight同时验证当前源码中的关键固定文案和组件/路径；增加仅固定case title/status/attempt、受限spec路径内行列号的安全失败定位。禁止保存原error message/DOM/URL/header/token；离线含合成秘密的负例证明不会透出。仍2case、retries0、0skip，超时、身份、HTTP和事实断言保持；正式运行前冻结所有输入hash并由Lead核对。

Lead独占治理、提交、构建、隔离服务和执行；没有产品writer或生产操作。因恢复治理提交改变exact HEAD，重新生成同源full JAR与前端生产产物后才跑v3；已通过A2后端默认/定向/受限HTTP仅在backend/release/scripts/skills树等价时明确复用，不冒称重跑默认。两真实JUnit（ReadinessMinio、AccessUrlMinio）、core/static仍实际执行，不用离线或默认环境skip代替。

## 4. 恢复入口与停止条件

本包与独立意见落盘、v3安全离线验证和hash冻结完成后，恢复批从attempts0计；前批3次不删除、不静默清零。恢复候选保持干净精确HEAD/tree，所有同源日志/manifest、真实Chrome2、两JUnit零skip、source/artifact前后同值与cleanup[]才可通过。若仍失败，先依据新的安全位置核实具体事实，不猜测、不放宽断言；同一失败无新证据或新批达到3次先再次复盘。Goal保持active，未完成/归档，未push/deploy。
