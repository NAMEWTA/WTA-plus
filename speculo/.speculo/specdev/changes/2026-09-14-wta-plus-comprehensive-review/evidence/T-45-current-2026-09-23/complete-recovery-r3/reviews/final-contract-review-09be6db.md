# T45 AC-045 final contract review — PASS on fixed 09be6db

**Verdict:** no remaining AC-045 implementation or required-verification blocker found for fixed candidate `09be6db6c3bace8594f7db0fe49a221e3cc5f140` (tree `2d5459e7dc77ec4120516d97a63de17eee390be4`), base `4c93a2fb9c7d2c1d68a0c8194197e2af1908b4d2`. The workspace was clean on this read-only review. `/tmp/wta-t45/r3-write-scope-and-ancestry.json` confirms 25 product paths, all within registered roots, and the base is an ancestor. This is an isolated current-source acceptance judgment, not a production-deployment claim. I did not edit the repository or run build/tests/services.

The unchanged 23-path implementation scope and prior source findings are covered by `/tmp/wta-t45/contract-review-8dc775b.md`, `contract-review-11c6e9e-supplement.md`, `contract-review-081c75f.md`, `contract-review-d1fd57f.md`, and `contract-review-d98936b.md`. The latest two-file parser delta is independently reviewed in `/tmp/wta-t45/review-principal-09be6db.md`: Jackson 3 object-valued AWS `[*]` Principal now follows typed checks instead of throwing and being mislabeled INVALID_POLICY; malformed or nonpublic forms remain UNKNOWN/COMPLEX_POLICY. The same-user session-generation fence, owned row ID guard, domain-bound public probe and safe fact projection remain unchanged.

**AC-045 facts and boundaries.** Current-source UIv5 real run `/tmp/wta-t45/ui-v5-runs/7d8fa892845f33d4/result.json` is `accepted=true`, browser exit 0, source clean at 09be6db, full JAR SHA256 `a0daf03adaf28c3eea2c136eef2963746b68f57fc8a4db64eca733c82f401776`, and cleanup errors empty. For each of PUBLIC_READ, PRIVATE A and PRIVATE B, the isolated MinIO count proxy observed exactly five diagnostic requests: signed object HEAD, signed policy GET, signed ACL GET, anonymous object HEAD, anonymous Range GET. No diagnostic PUT/DELETE/policy mutation is in those per-call deltas. Policy/ACL 403 remains UNKNOWN for all three. The PUBLIC_READ object's anonymous HEAD 200 and Range GET 206 produce object-scoped ALLOWED facts, **without** a false definite POLICY_MISMATCH; both PRIVATE objects' anonymous HEAD/GET 403 produce object-scoped DENIED while four bucket declaration facts stay UNKNOWN. All three summaries remain `NOT_SERVING/DIAGNOSTIC_UNVERIFIED`, rather than claiming full-bucket or anonymous-write safety. Startup MinIO call count is zero; raw backend known-secret canary hits are zero.

The same real run retains two Chromium cases, each one attempt, **2 passed / 0 failed / 0 skipped / 0 flaky**: an Admin sees public/private fact rows, source/scope caveat and bounded claims, while ordinary and anonymous identities cannot diagnose (business 403/401). The owned invalid-policy HTTP negative remained true. Before Chrome, safe first-page preflight proved seven rows, the owned invalid-policy row plus public/private rows present, and one unknown policy; after an exact owned-ID/key/policy deletion (one affected row), six rows remained, the bad row was absent, public/private remained and unknown count was zero. This closes the earlier UI fixture interference without weakening the product's strict policy mapper. The run retained only safe case/count evidence, removed private ephemeral files, and had no owned-resource cleanup errors.

The two fresh real MinIO JUnit consumers in `/tmp/wta-t45/oss-two-runs/45cde3cd5fe92d7c/result.json` have acceptance true, Maven exit 0, **one test each, zero failures/errors/skips**, correct private-properties marker, and clean 09be6db source before/after with empty cleanup errors. `OssStorageReadinessMinioIntegrationTest` now confirms real policy declaration interpretation and public/private object limits; `OssAccessUrlMinioIntegrationTest` confirms existing URL behavior. The readiness test's anonymous PUT 403 checks are **test negative controls against owned temporary buckets**, not calls from the product diagnostic or a production bucket. They do not justify saying the entire test setup made no writes; the exact diagnostic provider deltas above are the read-only evidence.

**Current-source gates and provenance.** `green04-source-and-counts.json` records 34 targeted classes / 170 tests / zero fail/error/skip at clean 09be6db. `r3-backend-default-counts.json` records 262 fresh classes / 1,139 tests: 923 executed and 216 environment skips, zero failures/errors; the skipped environment consumers are not counted as real MinIO evidence. `r3-full-package-proof.json` and `r3-full-bundle.json` record exit 0 from the clean fixed source, binding the 214 MB full Admin JAR used by UIv5. `r3core-core-package.json` and `r3core-core-bundle.json` also exit 0; `r3core-source-before/after.json` bind both to the same clean SHA. Five static checks from R1 exit 0 and are reused only through identical `.agents`, scripts, `docs/fm`, notify and frontend input trees in `r3-static-evidence-reuse.json`.

The full frontend architecture, extra architecture, OpenAPI check, lint, typecheck and test gates all exited 0 on R1 frontend bytes; `r3-frontend-evidence-reuse.json` proves those bytes are identical at 09be6db. Retained `r1-frontend-counts.json` records 663 Vitest + 108 Node = 771 tests (the extra architecture 101 are already included in Node). A new 09be6db `build:prod` and full Admin bundle check exit 0, with `r3-frontend-build-artifacts.json` recording 311 Admin, 14 Home and 4 SSO files (329 total); UIv5 binds the 311-file Admin dist manifest. Formal OpenAPI generation was captured at A2; `r3-openapi-contract-equivalence.json` compares the current 09be6db UIv5 live capture with it and proves all 438 paths and 450 components identical, only the owned loopback `servers` port differing. Thus the checked-in generated Fact/VO contract remains in sync without falsely claiming a new generation at 09be6db.

**History and practical limit.** A1 domain URL failure, A2 frontend typecheck failure, C3 stale UI text, recovery R1 row selector, UIv4 SQL collation, and the first two-JUnit POLICY_READ failure are retained as failed attempts; none was retroactively marked passing. Each has a narrowed cause and later fresh fixed-source evidence above. AC-045 deliberately describes finite policy/ACL declaration facts and one diagnostic object's HEAD/GET; it does not implement complete IAM evaluation or prove all objects, anonymous writes, future access, or a production provider's worst-case timeout. No production service or customer data was exercised by this review.
