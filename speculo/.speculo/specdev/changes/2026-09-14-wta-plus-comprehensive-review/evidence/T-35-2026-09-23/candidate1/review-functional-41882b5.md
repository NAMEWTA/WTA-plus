# T-35 固定候选功能/工程审查

**Result: request-changes.** 固定点 `8b758ea8074a63835659b9731e3a4c74c5c029c5`，候选 `41882b54a2b644f7ae661b84205cfcc9d1b72886`，tree `10a3a1709adb16d0034cc68633ae1b94c25b0a62`，direct parent 为固定点。审查输入为 `git diff 8b758ea...41882b5 -- backend` 的 25 个产品路径、该候选的 `git show <SHA>:<path>`；没有把随后 writer 的工作树修改算入此候选。规范来源：本 change `spec.md:98` AC-035、`ticket/35-sms-cross-layer-snapshot.md` 的第 4–10 节、`ADR.md` 的通知边界、原报告 N-02；遵循 `speculo/workflows/specdev/common/skills/code-review/SKILL.md` 及 `references/source-discovery.md`、`risk-review.md`、`reviewer-contracts.md` 的固定点与可证伪风险方法。本文件是功能/工程轴；不引用或借用另一审查轴的结论。未运行构建、Maven、Docker、E2E 或修改仓库。

## 必须修复

1. **High — 发送前 Redis 配额异常被记作供应商结果未知，任务错误地等待回执；可能留下已占配额。** `backend/wta-modules/wta-notify/src/main/java/org/namewta/notify/support/NotifySendPlanner.java:125-149,173-181` 在发送计划中调用生产 `RedisNotifyQuotaAdapter.tryAcquire`；该适配器 `adapter/store/RedisNotifyQuotaAdapter.java:24-35` 调 RedisUtils，Redis 断连/超时会抛 `RuntimeException`。候选 `DispatchNotificationService.java:110-129,168-176` 把包含规划、配额、请求构造和真实 `notifyClient.send` 的整段放进同一 `try`，泛型 catch 将**尚未进入供应商调用**的短信也写为 `UNKNOWN/DISPATCH_ERROR`。`NotifyDispatchResultService.java:85-101` 因 `UNKNOWN` 把 Outbox 设为 `WAITING_RECEIPT`；它永远没有可查询的供应商流水号。这违反 AC-035 的“本地错误无 WAITING_RECEIPT”及票据第 6 步的确定本地错误边界；若第二/第三级额度异常，`held` 中已经成功占用的一级额度也不会执行 `releaseHeld`。满足条件：明确在 `notifyClient.send` 前后划分异常阶段；配额不可用必须失败关闭且不能装成外部未知/等待回执，已持有额度尽力补偿并保留补偿失败的安全状态；在真实/故障注入配额 port 测试中断言 supplier=0、Delivery/Outbox 不等待回执、已占额度处理与无重复发送。不得把供应商 I/O 歧义改成可盲目重试。

2. **High — required 真实集成测试在六 SQL 新空库中确定违反唯一键，无法执行所需的五条验收。** 新增 `backend/wta-admin/src/test/java/org/namewta/test/notify/NotifySmsDispatchIntegrationTest.java:183-188` 每个 `@BeforeEach` 都插入 `('auth-captcha','SMS')` 的场景绑定。唯一六 SQL 基座 `release-artifacts/docker/infrastructure/mysql/init/50-cde-base-dml.sql:1604-1614` 已有该行，DDL `10-cde-base-ddl.sql:1625` 对 `(scene_code,channel)` 设唯一键；因此 fresh import 后五例均在真正的 submit/dispatch 断言前失败。此问题是候选测试夹具而非产品分发行为，仍阻断 Ticket 第 8/10 节 required E2E；Lead 本轮实际运行也观察到五例 setup 冲突，但本判断可由固定源码独立证明。满足条件：在本测试独占的新库中精确接管/恢复现有 `auth-captcha/SMS` seed 绑定，或使用仍保持 `NotifySceneCatalog` 必填变量合同的隔离方案；不能简单改成未知随机场景，因 catalog 对未知场景返回空必填变量，令验证码参数校验退化。五例应在隔离 MySQL/Redis 中真实达到断言，目标 Surefire XML `tests=5, errors=failure=skipped=0`，供应商 sender 唯一替身，清理只涉及自有数据/容器。

## 已核对的满足项与边界

- `NotificationApplicationRuntimeService.java:44-53,76-83` 的已有幂等重复分支先返回，再作新短信提交预检；竞争的重复键冲突也返回已存在收据。`DispatchNotificationService.validateSubmission:203-212` 调 `NotifySendPlanner.preflight:70-123`，对绑定/账号、模板码、必填及映射值在提交前检查；该路径不占 Redis 配额或发送。`NotifySendPlanner.plan:63-67` 在发送前复用同一预检，之后才进入配额。静态合同正确，除上述异常阶段问题。
- `NotifyTemplateRenderer.smsSnapshot` 只记录场景、供应商模板码和排序变量名，`DispatchNotificationService.toContent:196-200` 传非空逻辑快照，provider params 仍是独立的真实值。`NotifyDispatcher.validateRequest:119-138` 仍拒空模板快照；未放宽下层校验。短信 Adapter 用假 sender 的新增单元链验证参数/调用一次，真实集成 fixture 使用 `NotifyNotificationDao`、动态事务代理、MySQL、生产 `RedisNotifyQuotaAdapter`/`RedisNotifyIdempotencyStore`、真实 `NotifyDispatcher` 和 `SmsNotifyChannelAdapter`，仅供应商 sender 替身；其设计覆盖需要的层，但候选 1 的 setup 冲突阻止实际成立。
- `DispatchNotificationService.java:137-171` 将 Adapter 的 `PROVIDER_ERROR` 转 `UNKNOWN/PROVIDER_OUTCOME_UNKNOWN`，结果事务给 `WAITING_RECEIPT`，没有自动再次调用供应商；明确的 `NotifyValidationException` 对 SMS 转 `FAILED/SMS_LOCAL_VALIDATION_*`，配置 plan 失败也直接 `FAILED`。`NotifyDispatchResultService.configFailure:149-156` 包含本地校验/配置码；候选真实测试已有“绑定消失”和“Dispatcher INVALID_TARGET”两个负向路径。需增补配额异常负例以覆盖 finding 1。
- `NotifyDispatcher.auditRequest/auditResult:285-344` 仅向事件发布脱敏副本，原始请求仍供 Provider、幂等摘要；事件发布失败日志中的请求 ID 对 `REDACT_SENSITIVE` 为固定占位。`SmsNotifyChannelAdapter.java:42-53` 不把供应商不可信错误正文进入结果；`LogSanitizer.java:47-55,145-150` 覆盖验证码 GET 手机号与统一通知提交的目标/模板/幂等字段。`NotifyAuditSupport` 与 `NotificationApplicationRuntimeService.receipt/query`、`NotificationMonitorService`/`NotificationDeliveryView` 对敏感意图隐藏公开 providerMessageId/errorCode；原数据库原值保留供内部回执关联，FULL 旧投影仍保留。候选测试包含事件、HTTP/操作日志、管理投影与原值保留的正反例。
- `NotifyDeliveryEvent` 的公开记录类型未改签名，只改变敏感策略时的监控语义并在类型注释中声明；仓库生产消费者搜索仅见 `NotifyAutoConfiguration` 把事件交 Spring `ApplicationEventPublisher`，未见生产代码把事件请求字段重新发给供应商。`NotificationMonitorUseCase` 改用安全 `NotificationDeliveryView` 后未留下旧 `from(NotifyDelivery)` 调用；最终消费者 compile/测试由 Lead 实跑证实。

本报告不能把静态核对当作通过的构建/E2E。Lead 此轮报告 37 类 163 项单元/契约通过，但初次 selector 漏 `NotifyNoticePublisherServiceTest`；完整 selector 与新候选 5 例真实服务结果须在独立验收证据记录。候选 1 的 required 真实链未通过，应保留此报告的 request-changes，后续候选另以固定 SHA 补审，不覆盖。
