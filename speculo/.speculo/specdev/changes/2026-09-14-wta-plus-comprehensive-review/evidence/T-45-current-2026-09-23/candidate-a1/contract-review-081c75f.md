# T45 fixed 081c75f incremental contract review — static pass

Fixed `11c6e9e6f74e218b8d75bb9d41a2c5c384e51b40...081c75ff92b147bd338ca98a041182973596bca2` (tree `c2b19640b965b395b83f19faed5e0e4f08a9f493`), seven product paths. Read-only review; I did not edit the repository or run build, tests or services. This report resolves the source finding in `/tmp/wta-t45/contract-review-8dc775b.md`; the unchanged common/VO/domain production scope is inherited from that fixed review and the 11c HTTP-basis supplement.

## Source verdict

**No remaining implementation blocker found in this increment.** `SystemWebRuntime.sessionSnapshot()` is a nonsecret `{generation, identityLoaded}` port (`frontend/packages/web-domains/system/src/runtime.ts:52–54`). The Admin host obtains both fields from the existing Pinia user store (`frontend/apps/admin-web/src/router/adminManifestRegistry.ts:199–202`). `OssConfigPage.vue:329–371` captures generation at request start, requires a loaded identity before sending, checks the same generation and loaded state in success/error/finally, and watches both for immediate close/abort. The previous same-user re-login case cannot pass `current()` after `user.login()` increments `sessionGeneration`; a late old response is rejected even if user ID and permission are unchanged. Closing or unmounting still increments the local version and aborts the request. New SFC tests cover same-user generation change, late old success, loaded=false clearing and no request, plus host projection and the type port.

I initially considered `views/sso-callback.vue` changing token without incrementing this generation. Its real route is `/sso/callback`, a **top-level route outside Layout** (`frontend/apps/admin-web/src/router/index.ts:51`); navigation there unmounts the Layout's AppMain/KeepAlive and the OSS page. The page's `onUnmounted` sets `pageMounted=false` and aborts, so that path cannot retain an old diagnostic page across callback in the current single-SPA route tree. I withdrew it as a T45 blocker; no token-epoch expansion is needed for this ticket.

The 081 backend test increment closes the two evidence gaps noted in `/tmp/wta-t45/contract-review-11c6e9e-supplement.md`: same-resource, same-action Allow+Deny proves `POLICY_READ=DENIED` and `POLICY_WRITE=UNKNOWN` (`OssAccessDiagnosticUnitTest.java:243–258`), while signed HEAD, policy and ACL timeout tests assert mocked pending futures are cancelled and independent anonymous GET remains ALLOWED (`:122–134`, `:260–278`). Interrupted signed HEAD now asserts the thread flag remains set and its future is cancelled (`:410–430`). These assertions check application cancellation of `CompletableFuture`; they do not establish a strict remote SDK abort deadline, which remains a real-service verification boundary. The UI's HTTP-basis wording was generalized from “object request” to “request,” correctly covering S3 Policy/ACL response facts as well as anonymous object requests.

## Actual targeted verification, not full acceptance

I read Lead's retained `/tmp/wta-t45/green02-source-and-counts.json`: clean before/after at `081c75f`, 34 suites, 166 tests, **0 fail / 0 error / 0 skip**, exit 0. Retained fronttarget02 records show domain 12, web 17 and Admin host 81 tests pass with exit 0, and `/tmp/wta-t45/fronttarget02-source.json` records clean before/after at the same SHA. This source and test increment is therefore clear for continued candidate verification. At the initial review checkpoint, default/full reactor, restricted MinIO/HTTP/browser, formal OpenAPI capture, full frontend gates and final clean proof were still Lead-owned and pending.

Lead subsequently reported the 081 backend default reactor exit 0: 262 suites, 1,135 tests, 0 failures, 0 errors and 216 environment skips (919 executed). Those skips are not treated as the required real MinIO result. Full package was still running at this report update; restricted MinIO/HTTP/browser, formal OpenAPI and complete UI gates remain Lead-owned.
