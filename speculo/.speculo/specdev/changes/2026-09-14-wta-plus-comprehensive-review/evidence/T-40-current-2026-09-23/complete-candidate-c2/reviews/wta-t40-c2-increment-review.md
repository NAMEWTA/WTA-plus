# T40 C2 fixed increment review

**Static verdict: PASS for the C1→C2 test-only repair; C2's own default/full/browser acceptance remains pending.** Base `2c8047a2bfca47389557e4261ff5a55036e1c72e`, fixed C2 `851a6d6eb0edf9b53876bf43b56bb1747f1f7b4a`, tree `a6d6fe1e3c5c372372ceb34bae8a191d44c46d47`. The three-dot product diff is exactly one file: `backend/wta-modules/wta-notify/src/test/java/org/namewta/notify/service/runtime/DispatchNotificationServiceTest.java` (+33/−4). No production, frontend, SQL, POM or browser source changed. I used fixed Git objects and did not run tests or modify the repository.

The two previously failing `notice-published` positive fixtures now call `publishedNotice` (`DispatchNotificationServiceTest.java:453-487,524-546`). That helper sets app `notify`, scene/template `notice-published`, biz type/ID, the versioned idempotency key and `NotifyNoticeVersionFence.initial(...)` metadata (`:567-576`). This matches the production fence's identity and marker contract; it does not bypass the gate or replace it with a mock. The original positive assertions remain: the isolated scene is actually sent and accepted while the quota-limited captcha request is not, and the rendered MAIL subject/body come from the notice wrapper rather than caller snapshots. The replacement only supplies the identity facts that the old test omitted.

The new negative case (`:548-565`) starts with that valid notice identity, then deliberately removes only the `noticeVersion` metadata marker. With a live MAIL binding/account configured, dispatch must make zero Provider calls, leave no new Attempt, and persist Delivery `UNKNOWN` / `NOTICE_VERSION_OUTCOME_UNKNOWN` with Outbox `WAITING_RECEIPT`. The fixture starts from a claimed READY task with zero attempts but no durable unsent marker (`:629-649`), so this is the conservative missing-provenance branch rather than a false `CANCELLED` assertion. It is a unit fixture, not a substitute for real DB concurrency evidence.

`/tmp/wta-t40/c2-source-equivalence.json` names the correct fixed C2 head/tree and `clean:true`. Its reuse boundaries are supported by the exact one-file diff: C1 frontend source, five browser files, skill/build inputs and the 329 retained dist artifacts are unchanged; F1 real18 and A3 shared135 retain their original run coordinates and unchanged relevant production/selected-class/POM/SQL inputs. These records may be cited as input-equivalent reuse, not relabelled as C2 executions. C1's default Maven run exited 1 on the two old fixtures, so it remains a failed historical attempt; the C2 repair needs a fresh passing default result before current-candidate backend acceptance. Browser and full gates also need Lead's stated final verdict.

No blocker found in this increment. The prior C1 static implementation review remains applicable to unchanged production source; it never asserted C1's failed default run had passed.
