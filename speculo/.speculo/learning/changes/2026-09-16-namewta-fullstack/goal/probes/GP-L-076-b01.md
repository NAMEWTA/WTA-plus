---
artifact: learning-goal-probe
probe_id: GP-L-076-b01
lesson_id: L-076
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-076 / b01

审问对象是已写成的 Lesson `lessons/L-076-test-rich-text.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **一格**：`A:TestRichTextController.list,create,update,remove,assets,get`。OBJ-76 原文：能口述 `TestRichTextController` 与 **OSS 资产解析**。档是**解释+走查**。矩阵 (a) **有独行**，六名与磁盘 1:1：`list, create, update, remove, assets, get`，模块 `wta-demo`，仍 `uncovered`，备注「富文本+OSS」。磁盘详情方法就叫 **`get`**，路径 `GET /{id}`，**不是**单表那种 `getInfo`。矩阵 (a) **没有** `ITestRichTextService` / `RichTextProcessor` / `RichTextPage.vue` / `createRichTextService` 独行。厨房 `createDemoService, createRichTextService` 仍 uncovered（L-077）。九工厂行里的 `createDemoWebDomain` 仍 uncovered。矩阵 (b) **没有** 对账 / `assets` 两条走廊 / 乐观锁独行；OSS 已印的 (b) 是 `SysOssUploadController.complete / abort` 与 `createOssUploadClient.upload`，均已 covered（L-027 / L-029）。矩阵 (c) `wta-modules` / `layered-classic` 已 covered（L-002 / L-003）；登记表 classic 行就是 `wta-modules/wta-demo`，「不得作为新模块 layered 反例」。MinIO/OSS 容器已 covered（L-025…L-029）。MySQL 8.4 / `dao-mapper-ownership` / `bundle-full-job` 仍 uncovered。矩阵 (d) **没有**「富文本装订 / 领出门条」走廊；已 covered 的是「主路径 OSS 直传」（L-027，浏览器 PUT 细节 L-029）。`OssService` 落在 (a) 同行 `UserService, RoleService, DeptService, PostService, ConfigService, OssService, TaskAssigneeService`（`wta-api/system`，仍 uncovered）——本课只认厨师怎么打电话，**不**代写整行六个邻居。本批按派单只审问这一格 (a)；口试按磁盘：同一门牌 `/demo/rich-text` **正好 6** 个公开映射，写三扇全是 POST，没有上传窗，没有 Excel，没有 `@RepeatSubmit`，没有 `@DataPermission`，没有 `@SaIgnore`，没有 `@ConditionalOnEnable`。不要把 Service/Mapper 升成第二张 (a)，不要发明 (b)/(d)，也不要把 L-074 八扇、L-075 树表、L-077 厨房、L-026 卡片柜、L-027 票房算进本切片。

抽查锚点（2026-09-17）：`backend/wta-modules/wta-demo/src/main/java/org/namewta/demo/controller/TestRichTextController.java`；`service/ITestRichTextService.java`；`service/impl/TestRichTextServiceImpl.java`；`domain/{TestRichText,bo/TestRichTextBo,bo/TestRichTextVersionBo,vo/TestRichTextVo,vo/TestRichTextSummaryVo,vo/TestRichTextAssetVo}.java`；`mapper/TestRichTextMapper.java`（**无** XML）；`wta-common-richtext/.../RichTextProcessor.java` 与 `RichTextProcessorTest.java`；`wta-api/.../OssService.java`；`OssLifecycleManager.bind/unbind/reconcileReferences/snapshot/resolveAccessUrl`；`SysOssServiceImpl.objectMetadata`；`TestRichTextListAutoMapperTest.java`；`TestRichTextOssOwnerUnitTest.java`；`business-oss-owners.json` `demo-rich-text-content`；`wta-demo/docs/rich-text.md`；登记表 `03-backend-module-modes.md` classic 行；DDL `10-cde-base-ddl.sql` `test_rich_text`；菜单 `50-cde-base-dml.sql` `2100800000000000001`–`0015`；`application.yml` `richtext-*`；`SysOssUploadController` 五扇 `system:oss:upload`；`SysOssController.downloadUrl`；`wta-admin/pom.xml` `bundle-full` / `bundle-core`；`wta-demo/pom.xml`；`PageQuery.DEFAULT_PAGE_SIZE`；厨房 `domains/demo/src/rich-text.ts`；厅堂 `apps/admin-web/src/application/services.ts` `richTextAssets`；菜单工厂 `web-domains/demo` `demo-rich-text` 六串；页 `RichTextPage.vue`；`web-kit/rich-text` `canonicalizeRichText` / `queueUpload`。`controller/` 下 **零** UseCase。工作树 **零** `TestRichTextController` HTTP 合同测试，**零** `TestRichTextServiceImpl` 行为单测；有的是 MapStruct 缝与 owner 字符串 stub。

## Q1 澄清

- objective_id: OBJ-76
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-76；chain L-076 覆盖列只 A 六名；课 `claimed_cells` 一格；矩阵 (a) 六名独行仍 uncovered、无 Service/Vue/厨房行；(b) 无本课符号、直传 complete/abort 与浏览器 upload 已 covered 挂 L-027/L-029；(c) 房间与登记表已 covered、MinIO 已 covered；(d) 无富文本装订流，已 covered 的 OSS 走廊是直传；负空间 L-074/L-075/L-077/L-026/L-027/L-029；OssService 落在七名同行仍 uncovered
- difficulty: medium

课把富文本演示收成「一块 `/demo/rich-text` 牌子：六扇窗，纸上只写仓库箱号，出门条另开一扇」。计划里至少有六套可数对象，派单点名的是 **一格 (a)**：

1. **OBJ-76 原文。** `course.md`：能口述 `TestRichTextController` 与 OSS 资产解析。一句里是 **柜台类名 + 资产解析语义**，没有把六扇逐个写成 OBJ 清单，也没有写出 `createRichTextService`、`SysOssUploadController`、`OssService.*` 十一法。课把头扩成「规范化 → 校验归属/临时态/类型 → 落库 → `reconcileReferences` → `GET /assets` 发短时地址」，那是走查，不是第二张矩阵行。
2. **chain 覆盖列。** 一格：`A:TestRichTextController.list,create,update,remove,assets,get`。不写 (b)，不写 (d)，不写 `ITestRichTextService`，不写厨房。chain 估时 35；课 `estimated_minutes: 38`。
3. **课 `claimed_cells` / 矩阵对照。** 课与 chain 都只钉这一格。矩阵 (a) 那一行六个名字与磁盘 1:1；`get` 是矩阵名也是 Java 名。Lead 若按 **现印 (a) 行** 标 covered，**不会**把 L-074 八扇、L-075 树表或 L-077 两座工厂假闭合；若凭「课里出现了 OssService」把 `UserService … OssService …` 七名同行一并盖章，则会假闭合其余六个跨模块合同；若凭「assets 解析短时 URL」把 `SysOssController.downloadUrl` 或票房五扇标进 OBJ-76，则会假闭合 L-026 / L-027。
4. **课自己的操作定义。** (a) 门牌 `/demo/rich-text`，磁盘 6 映射；查用 GET，写三扇全是 POST 路径 `create` / `{id}/update` / `{id}/remove`；`assets` 与 `get` 共用 `demo:richtext:query`；写三扇都 `@Log(..., isSaveRequestData=false, isSaveResponseData=false)`；成功形状是 `R.ok(Vo)` / `R.ok()`，不是 `toAjax`，不是 `R.warn` 601。classic：Controller 只抱 `ITestRichTextService`；Impl 抱 `TestRichTextMapper` + `OssService`；无 UseCase、无 DAO、无 XML。所有权是 `createBy` + `clientPk`。库里 HTML 只许 `oss://<正整数>`；短时 https 只在 `assets`。
5. **邻格。** L-074 单表八扇（`getInfo`、Excel、`@DataPermission`、`toAjax`）。L-075 树表六扇。L-077 厨房六枪 URL + 厅堂注入的 `assets` 端口 + 权限组六串（含 `common:richtext:upload`）。L-027 票房五扇，方法上还有 `system:oss:upload`。L-029 浏览器 PUT。L-026 管理柜 `downloadUrl` 权限字 `system:oss:download`。直传 (d) 已 covered，本课从「已经有一个临时 ossId」开始。
6. **未入库。** 矩阵 **没有** `ITestRichTextService` 独行 (a)、没有 `RichTextProcessor` 符号行、没有「对账副作用」(b)、没有「装订/领条」(d)、没有 Vue 行。厨师六法、规范化工具、两条走廊是 (a) 的机制，不是第二组格子。

问：本格闭合标准是「六扇 URL/动词/权限/Log/返回形状 + 纸上只写 `oss://` + 验货三闸 + 写库后同一事务对账 + `assets` 两条走廊（有稿号看已装订，无稿号只看自己的临时箱），且不把上传票房、管理柜 download-url、厨房工厂、单表八扇、树表六扇、OssService 整行算进来」，还是 OBJ 那句必须先等 GP-L-077 的 `createRichTextService`、或必须先把 L-027 直传再走一遍、或必须把矩阵 OssService 行一起盖章才算讲完？Lead 若凭「课里出现了 OssService 四字」把 `UserService, RoleService, DeptService, PostService, ConfigService, OssService, TaskAssigneeService` 整行标 covered，是否假闭合了其余六个合同？Lead 若因缺少 Controller 单测或 chain 只印了 (a) 就把六扇判成未讲，是否假判？

## Q2 证据

- objective_id: OBJ-76
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 25–57；ITestRichTextService 13–19；ServiceImpl 31–33 / 36–44 / 47–50 / 52–65 / 67–84 / 86–95 / 97–105 / 107–119 / 121–135 / 137–145 / 147–150 / 152–158 / 160 / 164 / 165–174；Bo 16–20；VersionBo 9–10；Entity 16–29；SummaryVo 12–17；Vo 14–19；AssetVo 11–18；Mapper 12–18；PageQuery 59；Processor 19–22 / 44–79 / 100–111 / 114–129；ProcessorTest 14–38；OssService 28–29 / 55–76；OssLifecycleManager 67–87 / 90–122 / 133–148；SysOssServiceImpl 254–277；owners json 53–66；OssOwnerUnitTest 12–14；ListAutoMapperTest 36–61；modes.md 24；demo pom 26–45；admin pom 149–171 / 193–205；DDL 388–402；DML 1709–1719；yml 139–178；UploadController 25；SysOssController 68–70；kitchen rich-text.ts 52–63；services.ts 54–69；web-domain 56–59；RichTextPage 46–67；Editor 123–126；docs/rich-text.md 7–14
- difficulty: high

课的六扇与资产解析哪些能在工作树直接复现，哪些只是测试名单比课表窄、或邻格事实？

**仍能复现（本课 A）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 门牌 `/demo/rich-text`；类上 `@Validated` `@RequiredArgsConstructor`；只注入 `ITestRichTextService` | Controller 25–30 |
| 磁盘 6 个映射：list/create/update/remove/assets/get；`GET /assets` 写在 `GET /{id}` 上面 | Controller 32–57 |
| 写三扇全是 POST；都 `@Log` 且不存请求/响应体；无 `@RepeatSubmit` / `@SaIgnore` / `@ConditionalOnEnable` / `@DataPermission` / `MultipartFile` | Controller 36–49；包内 grep 空 |
| `list` perm `demo:richtext:list`；入参只有 `PageQuery`；返回 `PageResult<SummaryVo>` | Controller 32–34 |
| `create` perm `add`；`update` perm `edit`；`remove` perm `remove`；`assets` 与 `get` 共用 `demo:richtext:query` | Controller 36–57 |
| 接口正好六法，与窗 1:1；没有第七法、没有 import/export/tree/batch | ITestRichTextService 13–19 |
| classic：Impl 抱 Mapper + `OssService`；`REF_TYPE = "test_rich_text"`；无 usecase 包 | ServiceImpl 31–33；modes.md 24；demo pom 有 `wta-api` 无 `wta-system` |
| `list`：`createBy` + `clientPk` + `orderByDesc(updateTime)` → `selectVoPage`；**不**喊 `selectOwned` | ServiceImpl 36–44；Mapper 13–18 全仓只有定义 |
| 无分页参数时页大小 `Integer.MAX_VALUE`（注释「默认查全部」） | PageQuery 59 |
| SummaryVo 只有 id/标题/version/updateTime，**没有 html** | SummaryVo 12–17 |
| `get`：`owned` 找不到 →「富文本不存在或无权访问」；`view()` 手抄，HTML 仍是 `contentHtml` | ServiceImpl 47–50 / 152–158 / 164 |
| `create` `@DSTransactional`：normalize → `validateAssets(assets, null)` → insert → reconcile；insert≤0 →「保存失败」 | ServiceImpl 52–65 |
| `update`：Bo 的 version 无 `@NotNull`，服务自己查空 →「版本不能为空」；`clientPk` 从旧行抄，不靠请求改；`updateById≤0` →「请刷新后重试」且此时**尚未** reconcile；返回前 `version+1` 手写 | ServiceImpl 67–84；Bo 20 |
| `remove`：POST + `TestRichTextVersionBo.version` `@NotNull`；逻辑删后再把旧 ossIds 对成空集 | Controller 46–49；VersionBo 9–10；ServiceImpl 86–95 |
| `validateAssets` 三闸：元数据读不到 →「资源不存在」；上传者/Client 不对 →「不属于当前用户或 Client」；非临时且不在旧稿 →「新增内容只能引用临时上传资源」；kind 对不上 contentType →「类型不匹配」 | ServiceImpl 121–135 |
| `kindMatches`：image/audio/video 前缀；附件是三者都不是；`contentType==null` 失败 | ServiceImpl 137–145 |
| `assets`：带 `richTextId` 则 `owned` + 已保存 HTML `ossIds().containsAll`，失败整单抛「资源不属于当前富文本」，再 `resolve(id, true)` 跳过临时检查；不带则 `resolve(id, false)` | ServiceImpl 97–105 |
| `parseIds`：空白 → 空集合；去重；>100 或 `id<=0` 或非数字 →「OSS ID 参数无效」 | ServiceImpl 165–173 |
| `resolve`：上传者/Client 不对、非引用模式且非临时、任何 `RuntimeException` → 该条 `unavailable`（不抛） | ServiceImpl 107–119 / 174 |
| HTTP 的 `ossIds` 是**必填** `@RequestParam String`；缺参到不了厨师空集合，空白串才会 `R.ok([])` | Controller 53 vs ServiceImpl 166 |
| 规范化：1 MiB / 1 万节点 / 100 资源；媒体只许 `oss` 协议；禁 poster/srcset；同 id 两用途拒绝；外链补 `noopener noreferrer` | Processor 20–22 / 44–79 / 100–111 / 132–137 |
| 测试钉：去 onclick、收 `oss://12`、拒 Base64、拒同 id 两用途、拒 `url(javascript`、拒超出 Long 的 data-oss-id、幂等、audio+附件进 `ossIds()` | ProcessorTest 14–38 |
| Bo：`title` `@NotBlank`；`html` `@NotNull` **不是** `@NotBlank`；无 `@Size` | Bo 16–20 |
| 实体：`@TableName("test_rich_text")`；`@Version`；`@TableLogic` Java `Long`；DDL `title varchar(120)`、`del_flag char(1)` | Entity 16–29；DDL 388–399 |
| 对账合同：`refType` 真实表名；current−previous bind，previous−current unbind；零引用变回临时并写过期 | OssService 45–56；LifecycleManager 67–87 / 90–122 |
| `selectUrlByIds` `@Deprecated`；本课走 `resolveAccessUrl` | OssService 28–29 / 76；ServiceImpl 113 |
| `objectMetadata` 默认抛 Unsupported；生产覆盖；`PENDING` 删除当不可用 | OssService 69–70；SysOssServiceImpl 254–274 |
| owner 登记 encoding `canonical-html-oss-id-references`；`restoreStrategy: not supported`；合同测试只 `contains("oss://")` | owners json 53–66；OssOwnerUnitTest 12–14 |
| 列表 MapStruct 缝：Entity → SummaryVo 无 html 字段可丢；Vo 能把 `contentHtml` 映到 `html`——**不是** `get` 的 `view()` 路径 | ListAutoMapperTest 36–61 vs ServiceImpl 164 |
| `bundle-full`（默认）才依赖 `wta-demo`；`bundle-core` 没有这一行 | admin pom 149–171 / 193–205 |
| 菜单 C/F `2100800000000000001`–`0015`；query 备注「详情与资源访问」；F 型含 `common:richtext:upload`；**没有**作文种子行 | DML 1709–1719 |
| `richtext-image/audio/video/file`：PRIVATE + `required-permission: common:richtext:upload`；image 不含 svg | yml 139–178 |
| 票房五扇另要 `system:oss:upload`；管理柜出门条是 `system:oss:download` | UploadController 25；SysOssController 68–70 |
| 厨房六枪 URL 与本课 1:1；`assets` 是注入端口，不是第七个 HTTP 方法 | rich-text.ts 52–63 |
| 厅堂 `upload` 策略 `richtext-${kind}`（附件→`file`）；`resolve` 打 `/demo/rich-text/assets` | services.ts 54–67 |
| 权限组六串含 upload，仍不含 `system:oss:upload` | web-domain 56–59 |
| 页 `list({pageNum:1,pageSize:50})`；保存走 create/update；删除带 version | RichTextPage 46–67 |
| 编辑中 resolve 带 `richTextId`；失败 catch，占位仍 `oss://id` | Editor 123–126 |
| 接入文档：六扇路径；删节点不要立刻 OSS 删除 | docs/rich-text.md 7–28 |

**课写过、测试名单更窄或口试数字写偏（不是证伪本格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「null / 空白 → 空集合，后面 `R.ok([])`」 | 厨师 `parseIds` 对 null/空白成立。HTTP `ossIds` 缺省必填，缺参是框架 400，空白串才进空集合 |
| 「测试钉规范化四例」 | `RichTextProcessorTest` 四方法有。**没有** Controller/ServiceImpl 对账、owned、assets 走廊的行为单测 |
| 「owner 合同测试」 | 方法体是字符串 stub，不是 `reconcileReferences` 集成 |
| 「`view()` 手抄」 | 主路径手抄。另有 MapStruct 测试证明 Vo 也能映 html，课已标成转换缝 |
| 课 `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |

问：在六扇映射、写三扇 POST 且日志不存体、list 无 HTML、get 无权即抛且 HTML 仍是 `oss://`、create/update/remove 先规范再验货再落库再对账、assets 两条走廊、classic 只经 `OssService`、`selectOwned` 未被调用、normalize 拒绝 Base64/同 id 两用途 已经能互证的前提下，(a) 是否还要把「缺 Controller 行为单测」「HTTP 缺 `ossIds` 是 400 不是空列表」「课估时 38 vs chain 35」写成开篇半句，才算本格讲完？还是这些已经落在课的边界/反例里，Lead 可以把 **这一行** 视为可 covered？

## Q3 假设与反例

- objective_id: OBJ-76
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 写窗全 POST；Java `get` 不是 `getInfo`；SummaryVo 无 html；get 不换 https；无上传映射；无 `@DataPermission`；无权详情抛异常；带 richTextId 对照已保存 HTML；预览坏 id 常 unavailable；html `@NotNull` 允许空串；无防重；无 XML；列表不走 `selectOwned`；owner 单测是 stub；`selectUrlByIds` deprecated；demo 不得当 layered 反例；厨房 assets 端口不是第七扇；票房双钥匙
- difficulty: high

课的隐含假设若反转，合格读者会把哪一扇窗或哪只箱子认错？

1. **「六扇是标准 REST：`POST /`、`PUT /`、`DELETE /{id}`。」** 写三扇全是 POST，路径带 `create/update/remove`。课反例 1 / 误区 7 覆盖。
2. **「详情方法叫 `getInfo`。」** 磁盘是 `get`。那是单表/请假的名字。课反例 2 / 误区 6 覆盖。
3. **「列表也带正文，浏览器直接显示图。」** SummaryVo 无 html；有 html 的窗也不带签名 URL。课反例 3 / 误区 5 覆盖。
4. **「`GET /{id}` 已经把 `src` 换成 https。」** 没有。短时地址只在 `assets`。课反例 4 / 误区 4 覆盖。
5. **「上传也是本课一扇 `/demo/rich-text/upload`。」** 票房在 `/resource/oss/uploads`。本课零 `MultipartFile`。课反例 5 / 误区 3 覆盖。
6. **「`richtext-image` 只需 `common:richtext:upload`。」** 还要跨过票房方法上的 `system:oss:upload`。本课六扇注解里没有 upload 字。课反例 6 / 误区 21 覆盖。
7. **「`assets` 就是 `SysOssController.listByIds` / `downloadUrl`。」** 门牌、权限字、是否检查作文会员资格都不同。课反例 7 / 误区 12 覆盖。
8. **「新代码继续用 `OssService.selectUrlByIds`。」** 合同标 deprecated；本课走 `resolveAccessUrl`。课反例 8 / 误区 11 覆盖。
9. **「编辑器删图必须调 OSS 删除，否则会泄漏。」** 只改 HTML；真正 unbind 在下一次保存的对账。课反例 9 / 误区 10 覆盖。
10. **「带 `richTextId` 就能预览尚未保存的新 id。」** `containsAll` 对照的是**已保存** HTML。新 id 整单「资源不属于当前富文本」；编辑器 catch 掉。课反例 10 / 误区 13 覆盖。
11. **「这是 layered，Service 不许碰 Mapper。」** 登记 classic；Impl 必须能指到 `mapper` 和 `ossService`。课反例 11 / 误区 18 / 24 覆盖。
12. **「新模块就该抄这一份。」** 登记表禁止把 demo 当 layered 反例。课反例 12 / 误区 17 覆盖。
13. **「所有权靠 `@DataPermission` 滤部门。」** 本课是 `createBy` + `clientPk`。课反例 13 / 误区 8 覆盖。
14. **「无权看详情会 `R.ok(null)`。」** 抛「不存在或无权访问」。课反例 14 / 误区 9 覆盖。
15. **「OBJ-76 包含 `createRichTextService` / RichTextPage。」** 那是 OBJ-77。页只当对照扳机。课反例 15 / 误区 22 覆盖。
16. **「owner 合同测试已经把对账跑通。」** stub 只查字符串含 `oss://`。课反例 16 / 误区 20 覆盖。
17. **「列表走 `selectOwned` / XML。」** 主走廊是 `selectVoPage`；没有 XML；`selectOwned` 全仓零调用。课反例 17 / 误区 19 覆盖。
18. **「html `@NotBlank`，空正文建不了。」** 注解是 `@NotNull`。空串能过 Bean Validation，normalize 成空片段，零资源也可以建一篇只有标题的作文。课反例 18 / 误区 15 覆盖。
19. **「有防重。」** 没有 `@RepeatSubmit`。双提交可能两篇。课边界 3 / 误区 16 覆盖。
20. **「预览坏 id 会 404。」** 常是 200 + `unavailable`；稿号/会员资格失败才整单抛。课正例 11 / 误区 14 覆盖。
21. **「`query` 只开详情。」** 同一把钥匙还开 `assets`。菜单 F 型备注已经把「详情与资源访问」写在一起。课边界 4 覆盖。
22. **「`bundle-core` 还留着富文本窗。」** 不留。课正例 12 / 误区 25 覆盖。
23. **「厨房对象上有第七枪 upload。」** upload 在厅堂注入的端口上，URL 不打本课门牌。课图 2 文字等价物覆盖。
24. **「OBJ-76 是整个 wta-demo 模块。」** 只认富文本 Controller 六法。课误区 1 覆盖。

问：读完后仍说不出的，是不是「六个方法名」，而是 **库里只有 `oss://`、出门条是第六扇不是详情附赠、删节点≠删 OSS、带 `richTextId` 对照的是已保存 HTML、预览坏 id 常 200+unavailable、html 允许空串、没有防重、demo 不是 layered 模板、上传不在本课六扇、`query` 一钥开 get 和 assets、OssService 整行不能因本课闭合**？

## Q4 视角

- objective_id: OBJ-76
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-76 vs L-074/L-075/L-077/L-026/L-027/L-029/L-003；claimed 六名 vs 厨师/规范化/厨房；OssService 七名同行；chain 估时 35 vs 课 38
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这一格 (a)**？

| 视角 | 多出来的边 | 是否本课这一格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 六名独行 1:1 可盖章；无 Service/Vue 行；(b) 无本课符号；(d) 无装订走廊；OssService 落在七名同行 | **是** 闭合定义。本行可视为已讲并可 covered。OssService 同行保持 uncovered。不要发明 (b)/(d)，不要把厨房升成第二张 (a) |
| 门卫 | 六扇 URL/动词/权限/Log 不存体；无上传；`assets` 在 `get` 前 | **是** `A:TestRichTextController.*` |
| 厨师 | owned 两把钥匙；normalize/validate/reconcile；`view()` 手抄；`selectOwned` 死代码 | **是** 同一格的机制，不是第二行 |
| 规范化工具 | 1 MiB/节点/100 资源；只许 `oss://`；拒 Base64 与同 id 两用途 | **是** 写窗前置闸。工具在 `wta-common-richtext`，格子仍是本课怎么喊 |
| 仓库管家 | bind 临时变正式；unbind 零引用回临时；`resolveAccessUrl`；PENDING 元数据不可用 | 本课怎么喊 **是**。`OssService` 整行六个邻居 **否** |
| 出门条两条走廊 | 有稿号：owned + 已保存 containsAll + 跳过临时检查；无稿号：只给自己的临时箱；单条坏箱 unavailable | **是** 第六扇的语义，不是未入库 (d) |
| 单表样板（L-074） | 八扇、Excel、`getInfo`、`toAjax`、`@DataPermission` | 对照差 **是**。八扇盖章 **否** |
| 树表（L-075） | 扁列表与父子 | 对照「本课讲插图箱号」**是**。树表六扇 **否** |
| 厨房/菜单（L-077） | 六枪 URL 1:1；`assets` 端口注入；canonicalize；权限组含 upload | URL 对齐 / 草稿会员资格为何会抛 **是** 对照。两座工厂 **否** |
| 票房/直传（L-027 / L-029） | init/sign/complete；浏览器 PUT；双钥匙 | 策略名与临时 ossId **是** 起点。五扇与 PUT 进度 **否**（已 covered） |
| 管理柜（L-026） | `downloadUrl` + `system:oss:download`；列表抹 URL | 不是本课 `assets` **是** 负空间。四扇盖章 **否**（已 covered） |
| 登记表（L-003） | `wta-demo` = classic；不得当 layered 反例 | 模式一句 **是**。房间章 **否**（已 covered） |
| 装配电闸 | `bundle-full` 才进 admin；`bundle-core` 整栋实验室不进包 | 本类进不进 classpath **是**。(c) `bundle-full-job` **否**（L-079） |
| MySQL 8.4 | 表 `test_rich_text`；菜单六 F；无作文种子 | 种子/DDL 对照 **是** (a)。容器 **否**（L-085） |

问：Lead 若凭「六个 Java 名在 claimed 列里」把本行标 covered，是否还要求读者能指着 **写窗全 POST、列表无 HTML、get 仍是 `oss://`、assets 两条走廊、验货三闸、对账在写库之后、作者+Client 不是部门注解、样板≠模板**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要等 L-077 才给 **本切片** 当已讲？OssService 七名同行是否例外——必须保持 uncovered，直到其余六个合同也有课？

## Q5 元问题

- objective_id: OBJ-76
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课；一课超 1 个容器是 Lead 裁决不是 b01 填表）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 六名独行仍 uncovered、厨房两厂仍 uncovered、OssService 七名同行仍 uncovered、无本课 (b)/(d)
- difficulty: low

建议：`defer`

原因：

- **`A:TestRichTextController.list,create,update,remove,assets,get` 是 chain 切片，也是矩阵独行。** L 合同章节齐全（宏观六扇地图、直觉十二句、定义表、机制链 1–9、带 caption / 文字等价物的四张图、正反例、变式、误区、暂停走查、来源表 S-004/S-006/S-008/S-010/S-015/S-L076-01…09）。能复现：六扇映射与权限字、写三扇 POST 且日志不存体、list 无 HTML、get 无权即抛且 HTML 仍是 `oss://`、create/update/remove 先规范再验货再落库再对账、assets 两条走廊、classic 门卫不抱 Mapper、厨师只经 `OssService`、`selectOwned` 未被调用、demo 不得当 layered 反例。不必为「Controller 的名字」再挖五问。Lead 可将该行标 `covered`。
- **资产解析是第六扇的语义，不是第二格。** 有稿号看已装订、无稿号只看自己的临时箱、单条坏箱 unavailable、会员资格失败整单抛——已在课的图 3 / 反例 10 / 正例 11。不要发明 (b) 对账行或 (d) 装订走廊。直传 (d) 已由 L-027 独盖。
- 本批可粘贴的精度项不挡住切片视为已讲：（1）HTTP 缺 `ossIds` 是框架 400，空白串才进空集合；（2）工作树没有 Controller/ServiceImpl 行为单测，owner 单测是 stub；（3）课估时 38、chain 35。不要把单表八扇、树表六扇、厨房两厂、票房五扇、OssService 整行塞进本格。
- 不建议 `mine-more`：b01 未证明本切片还有真实矩阵空洞；再五问只会把 L-077 厨房、L-074 八扇或 L-027 直传再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；缺参 400 是半句精度，不是第二容器。不建议 `split-lesson`：未满 10 问；六扇是 chain 原设计的同一 OBJ-76，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b)/(d) 行。不建议等 GP-L-077 才给本切片当已讲。不建议用本课改 OssService 七名同行或 MinIO 容器。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:TestRichTextController.list,create,update,remove,assets,get | 课按磁盘讲了六扇路径/动词/权限/信封、classic 三跳、纸上只写 `oss://`、验货三闸、写库后对账、assets 两条走廊、作者+Client、无上传/无防重/无部门注解。残留精度：HTTP 缺 `ossIds` 为 400；无 Controller 行为单测；课估时 38 vs chain 35。 | 可选半句：口试「空 ossIds」以门卫必填为准——缺参 400，空白串才 `R.ok([])`。不要求重写机制链。Lead **可以**改本行为 covered（证据锚本 probe 与 L-076 来源表 S-L076-01 / S-L076-02）。 | defer（切片已讲；独行可闭合） |
| A:TestDemoController.list,page,importData,export,getInfo,add,edit,remove | 本课只借用八扇差（`getInfo` / Excel / `@DataPermission` / `toAjax`）。该行仍 uncovered。 | 不要用本课盖单表八扇。 | defer（L-074） |
| A:TestTreeController.list,export,getInfo,add,edit,remove | 树表六扇未因本课讲完。 | 保持。L-075。 | defer（L-075） |
| A:createDemoService, createRichTextService | 厨房六枪 URL 与厅堂 `assets` 端口已借用。该行仍 uncovered。 | 不要用本课盖厨房章。备注可写「富文本六扇消费已讲」。 | defer（L-077） |
| 九工厂行 `create*WebDomain`（含 `createDemoWebDomain`） | 本课只用权限组六串 + 页扣扳机当对照。其余工厂未因本课讲完。 | 保持 uncovered。不要整行闭合。 | defer（L-077 / 九工厂行保持 uncovered） |
| A:SysOssController.list, listByIds, downloadUrl, remove | download-url 负空间已点。该行已 covered。 | 保持。L-026。 | defer（L-026 已 covered） |
| A:SysOssUploadController.init, signParts, parts, complete, abort / B:complete/abort | 策略名、双钥匙、临时 ossId 已作起点。该行已 covered。 | 不要把票房五扇并进本格。 | defer（L-027 已 covered） |
| A:createOssUploadClient.upload | 浏览器 PUT 产出临时 id 已作对照。该行已 covered。 | 保持。L-029。 | defer（L-029 已 covered） |
| A:UserService, RoleService, DeptService, PostService, ConfigService, OssService, TaskAssigneeService | 本课只认厨师怎么喊 `reconcileReferences` / `snapshot` / `objectMetadata` / `resolveAccessUrl`。整行六个邻居未讲。 | **不要**因本课改该行为 covered。可在备注写「demo 富文本消费 OssService 四法已讲」。 | defer（OssService 同行保持 uncovered） |
| C:layered-classic / wta-modules | 登记表 classic 行已 covered。 | 不要用本课再盖房间章，也不要发动迁 layered。 | defer（L-003 已 covered） |
| C:MinIO/OSS | 容器已 covered。本课不重开直传河。 | 保持。 | defer（L-025…L-029 已 covered） |
| C:bundle-full-job | 本课电闸是 demo 进不进 admin jar，不是 job 装配行。 | 不要因 `bundle-full` 四字盖 L-079。 | defer（L-079） |
| (b) 对账 / assets 走廊 / 乐观锁 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 富文本装订/领条流 | 矩阵无此行。已有的 OSS 走廊仍是直传。 | 不要发明前端装订数据流行，也不要用本课改 L-027。 | defer（未入库） |
| Mapper XML / BO/VO getter | 无 XML；Bo `@NotNull html`；SummaryVo 无 html。 | 保持 covered-by-parent。 | defer（covered-by-parent） |
| 测试夹具 TestRichTextController/ServiceImpl | 工作树没有这类行为测试；owner 单测是 stub。 | 保持 deferred(test-fixture)。证据走 Controller/ServiceImpl/ProcessorTest。 | defer（deferred(test-fixture)） |
| MySQL 8.4 / Redis 8 | 表与菜单种子是 (a) 对照，不是容器闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本切片无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（单表/树表/厨房/票房是邻课格子，课内已把六扇与资产解析讲完并显式禁止整行假闭合）；合格读者说得出函数目的（插图作文本的六扇 HTTP、库里只存箱号、出门条另开窗）、主路径（自己的目录摘要 / 交新本装订 / 改本换图或拿掉图 / 撕本对空账 / 详情仍是 `oss://` / 有稿号或无稿号领条）与失败路径（规范拒绝 Base64、新 id 必须仍临时、种类不对、无权详情抛、乐观锁不对账、带稿号预览未保存 id 整单抛、预览坏箱 unavailable、关 `bundle-core` 整类不在）；把可选半句写进原课不会超过 40 分钟；邻课已规划。满 10 问前不拆。六扇虽跨读写/领条，仍是 chain 原设计的同一 OBJ-76，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / split 提案：否（miner 不改矩阵）；split 提案为 none；缺口表对 L-074/L-075 邻窗、L-077 厨房与九工厂行、L-026/L-027/L-029 已 covered 的票房/柜/直传、OssService 七名同行、L-003 已 covered 的登记表、未入库 (b)/(d)、covered-by-parent 的 XML、deferred 的测试夹具、以及 MySQL/Redis 与 job bundle 容器显式建议 `defer`；对矩阵 (a) 行 `TestRichTextController.list, create, update, remove, assets, get` 建议 Lead 标 `covered`（证据锚本 probe 与 L-076 来源表 S-L076-01…02）。本课不主张发明 Vue 矩阵行、用本课盖厨房两厂、把 `OssService` 整行闭合、或等 GP-L-077 才给本切片盖章。
