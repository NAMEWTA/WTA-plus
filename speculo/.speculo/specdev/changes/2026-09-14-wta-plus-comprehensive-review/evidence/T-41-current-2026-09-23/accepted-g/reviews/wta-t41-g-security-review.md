# T41 G final security/session review — PASS

**Fixed G:** `c21de75f030688d274dd24e9d6075eff1bdbd5a6`, tree `fea078b0dfa5917dc4375311e9aa455f843560cd`. **Compare F:** `cb8063b686fcd82d0a91064061e674a59fb6fc14`. Read-only fixed Git diff and `/tmp/wta-t41/g-source-equivalence.json`; no tests, builds, services or repo edits by this reviewer.

**Final T41 security/session-axis verdict: PASS on fixed G.** The exact-G full package and real 501 browser run passed, with evidence and cleanup detailed below. F's failed browser attempt remains failed and is not relabelled.

## Historical static checkpoint before the G browser run

At the earlier static checkpoint, the G selector increment passed source review while the exact-G package and real browser result were still pending.

`git diff F..G -- frontend backend` contains only `frontend/e2e/inbox-paged-real.e2e.ts`. The two prior whole-page positive `getByText(title)` checks for B's shared and B-only messages are replaced at :112-116 by a loop that filters B's actual rendered table rows by each title, asserts **exactly one row**, and asserts the first cell's text is exactly that title. The seed titles are distinct, while each row's title and summary text are intentionally equal; the new check removes intra-row locator ambiguity without accepting duplicate rows or a wrong first-column value. Existing two-row count (:111), A/B foreign-negative checks (:106, :117), A page-26 one-row/detail/HTTP checks, global read-all, B state preservation, zero-push assertion and ordinary browser clicks are untouched. No force click, DOM event injection, mock backend, skipped case or relaxed count was introduced. `git diff --check F..G -- frontend/e2e` exited 0.

`g-source-equivalence.json` distinguishes prior evidence from a G execution: D backend default 1071 (870 run/201 environment skips), T41 owned HTTP 4/0skip and shared Notify 135/0skip, D frontend 751/0skip, C production dist 328 rehashed, E five API-controlled Chrome 5/0skip, F core/static/OpenAPI gates all their original source points. F→G changed only this separate real-501 test source; those inputs are otherwise byte-equivalent, but their previous runs must retain their original source labels. At that historical checkpoint, G's exact real-501 runner and full-JAR evidence were not yet available, so no overall acceptance pass was given then.

## Final G acceptance evidence and security-axis disposition

The exact-G real Admin browser run is now complete: `/tmp/wta-t41/browser-runs/61b121b3b258cc3c/result.json` reports **acceptance true, exit 0**, one expected Chromium case, zero unexpected/skipped/flaky, one attempt, and the intended test file/title. Source before and after are the same clean G commit/tree above. The full admin JAR SHA256 before/after is `488f4fe2a0b99e09ccd01090ec280e8bd326efa78764f645019619899f6154e3`; `/tmp/wta-t41/g-full-package-proof.json` ties that artifact to a clean exact-G package and `g-full-package.json`/`g-full-bundle.json` each report exit 0. `g-browser-ts-lint.json` also reports exit 0.

The real owned six-SQL fixture began with A **501 total/481 unread** and B **2 total/2 unread**, one shared message, 502 synthetic messages and 503 relationships. After ordinary Chrome login, top-ten/detail, A page-26/detail, per-message read and global read-all, it records A **501 total/0 unread**, B **2 total/2 unread**, and `preexisting_read_and_seen_timestamps_preserved=true`. The spec also asserts zero push transport requests. The run's Playwright, Vite and backend process groups have no live members; no owned containers remain; both captured anonymous volumes are absent; all five loopback ports are closed; cleanup errors are empty. No password/token/raw reporter was used as retained proof.

Evidence provenance is explicit, not relabelled as G execution: D's current backend default 1071 total (870 executed/201 environment skips), owned T41 HTTP four cases and shared Notify 135 cases, and 751 frontend tests; C's 328 production-dist artifacts rehashed under unchanged executable inputs; E's separate five API-controlled Chrome cases (5/0skip, exact clean E); F's core bundle and static/OpenAPI gates under unchanged inputs; E's 24 offline runner tests on the same Python runner; G's fresh full package, browser-spec lint and exact real-501 run. `/tmp/wta-t41/g-source-equivalence.json` records the byte-equivalence boundaries. The earlier B frontend test failure and D/E/F browser failures retain their historical failed status and clean cleanup evidence; G is the first exact one-case passing 501 browser acceptance.

**Final T41 security/session-axis verdict: PASS on fixed G, with the stated source-specific verification evidence.** No remaining security-axis blocker or required scenario gap was identified in this review. This is an acceptance/review finding for the isolated current change, not a claim about a production deployment.
