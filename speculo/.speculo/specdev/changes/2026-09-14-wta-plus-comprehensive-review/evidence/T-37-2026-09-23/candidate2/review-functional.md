# T-37 C2 functional / engineering review

- Fixed full-ticket base: `38032d24335c52cafea855b19d51fb36295162ef`; candidate: `3a87bf71876d92e4afdd227de156045226e52be2` (tree `d60c20e385db3f3b7b064e9a13e0df4474a120ef`). Read-only review of `git diff e2907c41f4b1bef442c68b95a0fc09aa9c3b273a...3a87bf71876d92e4afdd227de156045226e52be2` plus the unchanged 22-path C1 scope. Product delta is exactly three paths: Redis store, Redis integration test, SMS integration test. Governing contract: AC-037, ticket/37 revision 159, notification standards. No product/governance edits or test/service runs by this reviewer.
- **Verdict: pass for the reviewed product contract and T37 real integration; affected T36 regression pending.** C1 report `/tmp/wta-t37-c1/review-functional.md` remains the historical failed candidate; this review does not relabel its 13-test failure as success. Lead has since supplied the clean exact-head T37 real integration result: `/tmp/wta-t37-c2/real-retry-counts.json`, run `e30e8562a767e12d`, SMS 10 + Redis 3, all 0 failures/errors/skips, source/tree clean before and after; all captured anonymous volumes, containers, process group and ports were absent/closed at cleanup. The T36 Atomic/Wake affected regression remains in progress and is not asserted passed here.

## C1 finding closure in the fixed C2 diff

1. The previous production CompositeCodec/NameMapper `complete(second)` failure occurred because Redisson 4.6.1's bucket Args CAS uses a different key path from the script used to mark/reclaim. C2 replaces `complete` with `ownerScript` on the same bucket codec/mapped name as `markRetryable`, using a strict expected-value CAS and positive `PSETEX` window; `release` now uses an exact-owner CAS-delete. The script never deletes/replaces a different owner, and `markRetryable` additionally checks state, digest, and request identity. New integration assertions check completed/release behavior under both StringCodec and project CompositeCodec+NameMapper, including stale owner and retained expiry. This closes the C1 defect with both static reasoning and the subsequent real positive 3/3 Redis run under the fixed source. Source: `backend/wta-common/wta-common-notify/src/main/java/org/namewta/common/notify/idempotency/RedisNotifyIdempotencyStore.java:27-44,83-129` and `backend/wta-admin/src/test/java/org/namewta/test/notify/idempotency/RedisNotifyIdempotencyStoreIntegrationTest.java:153-189` at C2.
2. SMS fixture reverses the mapping to the actual planner contract `logical name -> provider position`: `code -> 1`, `expireMinutes -> 2`. The immutable command already supplies both logical names. This removes C1's pre-submission error without weakening assertions for first rejected call, bounded Outbox backoff, second physical call, accepted completion, and stale lease no-resend; the subsequent real SMS class passed 10/10, including the exact two-physical-call case. Source: `backend/wta-admin/src/test/java/org/namewta/test/notify/NotifySmsDispatchIntegrationTest.java:480-482,498-527` and fixed `NotifySendPlanner.java:95-122`.

## Preserved full-ticket behavior and evidence limit

- Unchanged C1 paths preserve conservative status classification: only whole-batch confirmed unsent/retryable can move an owner to `RETRYABLE`; accepted, mixed, unknown, old FAILED, and transition ACK loss do not grant resend. Redis nonce/digest ownership and retained retry TTL remain. SMS4J actual single-attempt instance identity and structured Tencent single-phone rejection are unchanged. Existing Outbox budget/lease and provider-I/O-outside-transaction paths remain unchanged.
- Actual C1 real run was 13 selected tests with 2 errors and 0 skips; both errors have specific C2 changes above. Lead's C2 real T37 run passed 13/13 with 0 failures/errors/skips on clean exact `3a87bf7`, with captured anonymous volume absent and all owned resources cleared. This reviewer did not run Maven, Docker or provider calls. The 39-class regression result is owned by Lead; T36 Atomic/Wake affected regression was still running when this report was finalized and is not called passed. The original T37 runner's lowercase Docker volume diagnostic is corrected in frozen v2 `/tmp/wta-t37/run-notify-retry-integration-v2.py`, SHA `10d2134219b0cbcc78368893e6d802deea0103fb1fd478698337161ff125523c`, with 8 synthetic safety tests; exact anonymous volume names are recorded before cleanup on future failures.
