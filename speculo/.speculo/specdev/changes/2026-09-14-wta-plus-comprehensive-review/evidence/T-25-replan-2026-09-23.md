# T-25 evidence

Current HEAD `35dc7bbe` already meets AC-025. No new implementation was required. This cancels only the repeated construction. AC-025 stays owned by T-30. The adjacent OIDC change was not modified.

## MySQL department tree

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=DepartmentTreeMySqlIntegrationTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Ddepartment.mysql.integration=true \
  -Ddepartment.mysql.integration.url='jdbc:mysql://127.0.0.1:33135/namewta_dept_test_t25?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai'
```

Exit 0. Isolated MySQL `mysql:8.4.9` (`sha256:c36050afdca850f23cef85703f84c7531a5ae155a11b5ee1c60acb09937c4084`) on `127.0.0.1:33135`, database `namewta_dept_test_t25`, loaded `10-cde-base-ddl.sql` through `50-cde-base-dml.sql`. The password is the existing test default and is not copied here. The container was removed after the run.

`DepartmentTreeMySqlIntegrationTest`: tests=14, failures=0, errors=0, skipped=0, 4.234s. The service rejects a descendant parent even when the caller bypasses the dropdown, rejects a missing parent that is not the root sentinel, and rejects self, null, negative, and deleted parents without a partial write. A legal cross-root move and a detach to the root rewrite every descendant. Simultaneous opposite moves inside one tree and between roots cannot create a cycle. A descendant write failure rolls the whole subtree back. Data scope is enforced after locking, and corrupt stored ancestors cannot grant a move outside the actual tree. Concurrent delete plus child insert leaves no orphan, and a concurrent ancestor move uses the committed parent path.

## System module regression

```bash
cd backend && ./mvnw -pl wta-modules/wta-system -am test
```

Exit 0. `wta-system`: tests=33, failures=0, errors=0, skipped=0. Upstream `wta-common-redis` reported tests=2, skipped=2 in `RedisUtilsDeadlineIntegrationTest`. Those gated checks are not the department contract and are not counted as passes. The full `wta-admin` reactor was not used as this proof because its other gated suites are outside AC-025.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase, operation `verify-affected-contract-and-quality-gates`, status passed. No production source edit.
