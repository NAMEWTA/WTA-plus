# T-41 行为红灯与阶段2派单

固定源码 `8a90e404fcd899aa68482f3f52bb902521dc6aa0`，tree `392b77364be5798c7e111879391fa522c43f9ba8`；2026-09-23 UTC 15:40:53—15:41:35。Lead运行冻结v1 driver，owned MySQL8.4.9/Redis8.6.3，六SQL/103表、app-only DB用户；测试采用生产同类JWT StpLogicJwtForSimple及LoginHelper完整用户extra、真实Jetty/MVC/SaInterceptor/Controller/DAO。

精确类 NotifyInboxPagingIntegrationTest，1 tests/1 failure/0 errors/0 skipped，Maven与driver exit1、acceptance=false。A501/B2关系（共享1）及A/B HTTP200/本人隔离断言已先通过；唯一失败为 `page 26 has rows, total and unreadTotal, not the old 500-item array` 的Map形状boolean断言 Expected true/false。不是编译、环境或鉴权失败。这里验证Controller已签发会话下的身份归属，并未声称跑过生产登录端点/完整Client链；完整登录由后续真实Admin浏览器覆盖。

source before/after exact clean；PGID631930无剩余成员、两fullID容器已清理、匿名卷不存在、32844/32845端口关闭、cleanup.errors=[]。原始运行结果、脱敏Maven log及freshXML按manifest原字节保留在T-41-current-2026-09-23/red/；测试JWT不输出，保留前额外扫描无JWT形状串。红灯是施工前回归基线，不计正式候选集成尝试，不作业务通过。

## 阶段2派单

cors_audit为唯一产品writer，现授权Ticket41 revision174硬写集内完成分页/详情/POST安全日志、MPJ与内部domain/model/read投影、domain/Web/Admin会话隔离/global badge及全部消费者和测试；不改生成OpenAPI快照，由Lead固定full JAR后生成。先后端与必要前端/测试，尽早交可编译片段但避免长时间半成品验收。新的真实browser case/config在既有frontend/e2e根；ops_audit仅在/tmp准备独立driver模板，cors审定后才复制纳入产品。

Lead独占commit/build/真实服务。对所有同步生成types的构建和exact-clean实测严格串行；没有授权部署、外部Provider或生产库操作。新路径先登记。完成后两轴审查、真实501数据库/HTTP/浏览器及所命中门禁，合格固定source才能关闭。
