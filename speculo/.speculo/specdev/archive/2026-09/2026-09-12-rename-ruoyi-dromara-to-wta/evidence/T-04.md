# Evidence: T-04 — WTA-plus prep tree (W0b)

- **状态:** done
- **Tree:** `/workspace/vp-dev/WTA-plus` (local, no public remote)

## Layout map (docs aggregate → WTA-plus)

| Old (docs checkout) | WTA-plus |
|---|---|
| `ruoyi-vue-plus-namewta/` | `wta-vue-plus-namewta/` (content inlined, no submodule `.git`) |
| `plus-ui-namewta/` | `plus-ui-namewta/` |
| `docs/` `speculo/` `release-artifacts/` `scripts/` `.agents/` | same |
| `.gitmodules` | **not copied** (submodule delivery removed) |
| `.gitee/` | **not copied** |
| `node_modules/` `target/` `.git/` | excluded |

Proof: `{SCRATCH}/prep-tree.txt`. Nested legacy `.git` count 0. `.gitee` count 0.

Local orphan (no remote, not T-14): `8680fe6b7225950fbe9cdce23d6e38b7e141b189` — `git rev-list --max-parents=0 HEAD` is that single root.

NAC-06: owned rename was **not** done as default-branch commits on legacy remotes.
