# T-21 evidence

Current candidate `cfb57baa`. AC-021 stays owned by T-30. The adjacent OIDC change was not modified.

## Failure before the fix

On `15bacc95`, the same Chrome command below failed once: 28 passed, 1 failed, 0 skipped. `Home registration retains labels and alert while keyboard retry completes` stopped because the username wrapper style did not change on the retry. After a remote rejection the form is disabled and enabled again, and the username could stay focused without the Element Plus focus ring.

## Fix

`frontend/apps/home-web/src/views/RegisterPage.vue` moves focus to the registration alert after that recovery, and the input wrapper paints its ring on `:focus-within`. Login, SSO, and the callback page were not changed. Home and SSO brands stay separate: Home login still requires a non-empty `--client-surface`, and SSO keeps its own card.

## Browser

Isolated nginx `nginx:1.31.1` served the production Home and SSO bundles over HTTPS on `127.0.0.1`. The containers were removed after the passing run. Playwright used system Chrome and ignored the self-signed certificate.

```bash
cd frontend && pnpm --filter @namewta/home-web build:prod
cd frontend && A11Y_HOME_ORIGIN=https://127.0.0.1:33129 \
  A11Y_SSO_ORIGIN=https://127.0.0.1:33130 \
  A11Y_EVIDENCE_DIR=<temporary directory> \
  pnpm exec playwright test --config playwright.accessibility.config.ts
```

`build:prod` for Home exited 0 (`built in 866ms`). Playwright exited 0. 29 passed, 0 skipped, 15.8s. The 24 viewport cases are Home login, Home register, Home `/sso/callback`, and SSO `/authorize`, each at 320, 768, and 1440 CSS pixels and at 100% and 200% (device pixel ratio 2, CSS viewport halved). Each case wrote a device-pixel PNG whose width the test asserted, and a JSON measurement. Scroll width never exceeded the viewport. Lowest rendered text contrast was 5.473 on Home login, 4.759 on Home register, 7.113 on the Home callback, and 5.671 on SSO authorize. All are at least 4.5.

The other five cases are keyboard-only: Home login preparation retry with a visible focus; Home login failure then success, with the first failure in `role=alert`; Home registration labels and alert retained across the retry; SSO session failure then a failed password, with a polite live region present in the accessibility tree; Home callback alert, keyboard back, and keyboard restart.

## Other gates

```bash
cd frontend && pnpm lint
cd frontend && pnpm --filter @namewta/home-web test
```

Both exited 0. Lint: 0 warnings and 0 errors, architecture check 33 packages and 0 violations. Home tests: 7 files, 15 tests, 0 skipped. Admin and SSO production builds were not rebuilt; those apps did not change.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, implement phase for the registration focus change and verify phase for the commands above, status passed. Output is this file.
