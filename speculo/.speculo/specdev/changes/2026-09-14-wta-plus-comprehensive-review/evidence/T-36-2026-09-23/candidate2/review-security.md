# T-36 C2 安全／规格轴固定候选审查（只读）

**结论：静态增量 PASS；AC-036 最终验收待 Lead 的固定候选真实门禁。** 固定 T-36 base `5118051403de0648540646d98536d8c4f3f9f26d`、C1 `54cf715c2829ff95cf994401ff52f02668c1e171`、C2 `a2749a7dcc0bbc5c0643e07eba938446611c995a`，C2 tree `ff190a2f846c3b1545832bc05fbab744913d3bf2`。本次仅以 `git show`／三点 diff 审 C2 四个产品路径，未审写作中的工作树、未改仓库、未执行 Maven/Docker/SQL/服务。C1 已审未变范围由 `/tmp/wta-t36-c1/review-security.md` 的静态部分等价继承；**C1 三个真实失败不会因此自动视为已关闭**，须看 C2 的真实结果。

1. **提交故障后的同步清理没有扫掉合法外层上下文。** `DispatchNotificationService.java:212-244` 在 begin 和 complete 的代理调用前分别检查无 XID 且同步集合空；只有对应调用抛 `RuntimeException|Error` 后、且无活 XID，才移除本线程同步集合，并原样重抛。这针对已核对的 dynamic-datasource 4.5.0 `TransactionalTemplate` 在 commit 异常时可能留下“XID=null、旧同步非空”的局部缺陷（`/tmp/wta-t36-c1/ds-event-cleanup-review.md`）；无条件入口清理没有引入。调用前有外层活 XID，或正在执行 AFTER_COMMIT 且 XID 已空但已有合法同步时，`standaloneResultCall` 为 false，不清其集合。单元反例 `DispatchNotificationServiceTest.java:318-346` 覆盖两者。故障后依然抛原异常，既不伪造本地终态，也不推断 commit ACK。C2 同线程真实用例在 `NotifyAtomicResultIntegrationTest.java:503-541` 检查失败后的同步集合为空、下一笔独立提交有一次新 AFTER_COMMIT；这项源码断言设计合适，真实执行仍待 Lead 证明。该局部修复不保证其他 UseCase 先污染同 worker 线程后的全局恢复，应维持 C1 专项报告的边界。

2. **取消与 begin=false 的收尾仍受 DB fence 约束。** `DispatchNotificationService.java:220-225` 在未获预留时重新读 Delivery；只有已不存在或已非 PENDING 才调用 `settle(CLOSE)`，同 lease 的已预留重入仍为 PENDING，不抢先关闭。`NotifyDispatchResultService.java:177-191` 的 settle 重新按 Intent→Outbox→Delivery 锁定，并重新核对 PROCESSING、owner/token、数据库租约时间；若别的 owner 已领取或本 lease 已过期，它返回零写。C1 取消竞争原断言仍在 `NotifyAtomicResultIntegrationTest.java:678-699`：CANCELLED、Outbox DONE、零 Attempt/Message。C2 这段静态上能收口 C1 的 PROCESSING 遗留，实际竞争交错和测试结果尚未验证。

3. **既有消息的渠道快照改为语义比较，仍拒绝不同快照。** `InAppNotificationService.java:54-60` 对存储的 JSON 数组解析后与 `snapshot.channels()` 作有序列表相等比较，消除 JSON 序列化空格/编码差异的假冲突；不忽略顺序、缺失成员或其他 title/content/path/noticeType 字段。解析失败会抛错并回滚本地消息/结果事务，未降级为相等。C2 并发双用户用例在 `NotifyAtomicResultIntegrationTest.java:333-360` 建立 `[IN_APP,SMS]` 快照并核对已存列表，`:366-382` 验证历史空 noticeType 的第二收件人。C1 原来的消息主键唯一、每人一关系、同事务结果及 fence 路径无改动。

4. **列长度在独立预算前预检。** `DispatchNotificationService.java:195-210,246-248` 对 message 表的 title varchar(255)、notice_type varchar(10)、path varchar(500) 按 Unicode 码点数验证；失败进入原 `FAILED/LOCAL_DISPATCH_ERROR` 短事务终结，零预留、零 persist。对应 DDL `10-cde-base-ddl.sql:1541-1549`。真实 MySQL 用例 `NotifyAtomicResultIntegrationTest.java:253-283` 覆盖 emoji 255/256、10/11、path500，以及失败时无消息/关系、预算按既有本地终结只记一次。它解决合法上游快照列较长、写 message 时才报错并无谓消耗站内预算的问题；并未截断完整正文。C1 的 content longtext/summary 1000 码点逻辑未变。

5. **C1 安全主干等价继承。** C2 对 `NotifyDispatchResultService`、DAO、Mapper/XML、`NotifyOutboxClaimService`、`InAppCommittedPushListener` 及外部 SMS/MAIL 分类无产品改动。独立 begin 预算、同 token 固定 marker、有效租约、旧 owner、消息/关系/Delivery/Attempt/Outbox/聚合同事务、AFTER_COMMIT 仅本人推送、外部 UNKNOWN 保守分流，仍按 C1 静态审查中的精确源码前提成立。C2 四路径内未见新敏感正文输出、未新增绕过 ResultPort 的写入。恢复稿 `/tmp/wta-t36/recovery-by-id-sql-draft.md` 仍只是未经授权、未执行的指定 ID 操作稿；它需与当前 JSON 语义比较对齐后另行审阅，不是任何生产修复证据，也不覆盖 DELIVERED 缺关系分支。

**尚待 Lead 提供的固定候选证据**：当前 C2 实际 39 类/真实 `NotifyAtomicResultIntegrationTest` 的精确测试数、fail/error/skip、MySQL/Redis 资源归零、commit/tree/source-after；尤其 C1 的 AFTER_COMMIT 计数两失败、取消后 Outbox PROCESSING 一失败，及 C2 同线程 BEFORE/AFTER、双用户 JSON、长度边界用例。没有这些结果，不能勾 AC-036 或将“静态 PASS”写成整票 Done。报告不引用未出现的 E2E 通过。
