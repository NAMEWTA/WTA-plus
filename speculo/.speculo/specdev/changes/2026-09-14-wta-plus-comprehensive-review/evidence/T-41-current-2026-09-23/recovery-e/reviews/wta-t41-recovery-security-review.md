# T41 browser recovery security review — fixed D diagnosis, E pending

**Source reviewed:** fixed D `d93eb1d8d6095464a9606619f4161b3dd4ef35c7`, browser runner `frontend/e2e/run-inbox-paged-real.py` (frozen SHA256 `bd1798bba7a3ea78640e054e11f04905faf56ca38b8594196e18fc379c4c1f88`). Recovery checkpoint `f8edabc50f2df03a4b89da8f23f3c321bd277174` adds diagnosis/governance evidence, not a browser-runner product change. Read-only review; no source edits, tests, builds, Docker or services run by this reviewer. **This is not an E candidate verdict or T41 browser acceptance.**

## Deterministic D fixture failures

1. **MySQL collation 1267 before browser startup.** The D helper builds UTF-8 hex expressions using the session's default `utf8mb4_0900_ai_ci` with coercibility 2, while the owned six-SQL schema's relevant user-name, Admin Client ID and message-title columns are `utf8mb4_general_ci`. The safe isolated diagnosis in `recovery-diagnosis/result.json` records error 1267 for both user lookups, the Client lookup and title matching; the corresponding explicit `utf8mb4_general_ci` controls all succeeded. Diagnostic exit 0 means the *diagnosis* and owned cleanup succeeded, not that the D browser runner passed. The original D run `/tmp/wta-t41/browser-runs/f2ed71eeedb7b8e2/result.json` correctly remains `acceptance:false`, exit 1, before backend/browser launch; source before/after D clean and owned containers, volumes and ports cleared. Recovery should use a dedicated equality/LIKE comparison expression at those three call-site categories (A/B user, client, title), preserving the generic `hexsql` used for inserts/updates and preserving the exact owned six-SQL database. It should verify original collation and explicit comparison behavior in synthetic/offline tests before a fresh E run. Do not edit production schemas or suppress the SQL error.
2. **Browser login password length.** D runner `random_password()` returns 40 alphanumeric characters (:99-101) and assigns it to both A/B login environment values (:718-721). The actual `PasswordLoginBody.password` allows length 5–30 (`backend/wta-api/src/main/java/org/namewta/system/api/model/PasswordLoginBody.java:24-29`). This is an independently certain later login validation failure, although it did not cause the earlier SQL-stage run failure. Recovery should generate 24-character passwords only for A/B browser logins, update the owned BCrypt helper's input validation accordingly, and keep unrelated 40-character MySQL, Redis, MinIO and monitor secrets unchanged. The verifier should continue checking BCrypt `$2a$` and one-row updates in the freshly owned DB, with no plaintext in SQL/report/argv.

## Safety and remaining diagnostic bounds

The D runner uses exact clean HEAD and full-JAR/package SHA preflight (:70-75, :700-709, :733-746), generated private 0600 files (:86-88), dual-labelled full Docker IDs and loopback ports (:135-195, :758-809), owned process groups with finally cleanup (:210-241, :960-1028), and only numeric/allowlisted browser failure locations (:561-609). The raw Playwright reporter, raw process logs and browser artifacts are removed in finally. The browser run requires exactly one Chromium case, one attempt, 1 expected/0 unexpected/0 skipped/0 flaky and no trace/video/screenshot. No source fact showed a third deterministic fixture failure after the two above: the isolated SQL diagnosis reached role creation, seed and preservation stages, while fixed D `isolated_config` binds MySQL/Redis/backend/Vite to loopback, disables realtime and captcha in the owned fixture, and uses real Admin login. This does **not** predict Java/Vite/Chrome acceptance, which has not yet been achieved.

Pending E fixed candidate: inspect its exact runner/test/document delta and ensure the recovery changes stay confined to fixture comparison and login password generation, fail-closed preflight, no retained secret/raw SQL, exact owned cleanup, and no changed browser pass criteria. A clean E real browser result and source-after/cleanup proof, plus Lead's remaining core/five API-controlled cases, are required before an overall T41 decision. C's 328 production-dist hashes may be reused only under separately recorded production-input equivalence, never relabelled as E browser or API execution.
