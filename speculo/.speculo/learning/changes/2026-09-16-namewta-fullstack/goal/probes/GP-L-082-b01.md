---
artifact: learning-goal-probe
probe_id: GP-L-082-b01
lesson_id: L-082
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-082 / b01

审问对象是已写成的 Lesson `lessons/L-082-notify-dispatcher.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **两格**：`B:NotifyClient.send`、`B:NotifyDispatcher.send`。OBJ-82 原文：能口述 `NotifyClient.send` / `NotifyDispatcher` **与业务模块的同步入口**。档是**方法性状**。前置 L-051。矩阵几何 **不是两行**：矩阵 (b) **只有**同行 `NotifyClient.send / NotifyDispatcher.send`，性状原文「同步渠道入口」，仍 `uncovered`，备注空。矩阵 (a) **没有** `NotifyClient` / `NotifyDispatcher` 独行；「NotifyDispatcher 之外的 DTO」已是 `covered-by-parent`（L-051），不要再盖。矩阵 (c) `wta-common` 工具间枢纽已 covered（L-002）；MySQL 8.4 / Redis 8 / `dao-mapper-ownership` / `mysql-base` 仍 uncovered。矩阵 (d) 「主路径 通知提交」已 covered（L-051 + L-052）；本课不是第二 (d)。本批按派单只审问 **这一格 (b)**。口试按磁盘：函数式接口只有 `send`；Spring Bean **类型**是 `NotifyClient`，实现是 `NotifyDispatcher`；生产唯一扣扳机是 `DispatchNotificationService.dispatch` 第 123 行 `notifyClient.send(request)`；`IN_APP` 走 `InAppNotificationPort`，**不** send；`NotifyChannel` 内置只有 `MAIL`/`SMS`，**没有** `IN_APP`；`USER` 码 `LOGICAL_TARGET_NOT_SUPPORTED`；业务房间喊 `NotificationApplicationService.submit`。不要发明 IN_APP 渠道行，不要发明 Dispatcher HTTP 窗，不要把 `dispatch` 升成第二张 (b)，也不要用本课再盖已 covered 的 submit / claim / 主走廊 / 回调 / 公告 / 厨房。

抽查锚点（2026-09-17）：`backend/wta-common/wta-common-notify/src/main/java/org/namewta/common/notify/core/{NotifyClient,NotifyDispatcher}.java`；`config/NotifyAutoConfiguration.java`；`resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`；`pom.xml`；`model/{NotifyChannel,NotifyTarget,NotifyRequest,NotifyStatus}.java`；`spi/NotifyChannelAdapter.java`；`exception/NotifyDeliveryException.java`；`idempotency/NotifyIdempotencyProperties.java`；`backend/wta-modules/wta-notify/.../runtime/DispatchNotificationService.java` 与 `DispatchNotificationServiceTest.java`；`backend/wta-api/.../notify/api/{NotificationApplicationService,NotificationChannel}.java`；`backend/wta-admin/src/main/java/org/namewta/web/controller/CaptchaController.java`；`web/config/{NotifyContextConfiguration,RequestNotifyContextResolver}.java`；`backend/wta-admin/src/test/java/org/namewta/test/notify/core/{NotifyDispatcherUnitTest,NotifyAutoConfigurationUnitTest,NotifyChannelUnitTest}.java`；`idempotency/{NotifyIdempotencyDispatcherUnitTest,NotifyIdempotencyAutoConfigurationUnitTest}.java`；`attachment/NotifyAttachmentDispatcherUnitTest.java`；`caller/{CaptchaNotifyCallerUnitTest,NotifyCallerMigrationContractUnitTest}.java`；`wta-common-mail/.../MailNotifyChannelAdapter.java` + `MailConfig.java`；`wta-common-sms/.../SmsNotifyChannelAdapter.java` + `SmsAutoConfiguration.java`；`notification.md`「调用入口」；`01-module-map.md` notify 行。`wta-common-notify` **无** `src/test`，**零** `@RequestMapping`。生产 Java 里 `NotifyClient` 只出现在接口、Dispatcher、AutoConfiguration、以及 `DispatchNotificationService`。

## Q1 澄清

- objective_id: OBJ-82
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-82；chain L-082 覆盖列两格 B；课 `claimed_cells` 两格；矩阵 (b) **一行** `NotifyClient.send / NotifyDispatcher.send` 仍 uncovered；(a) 无本符号；(d) 主走廊已 covered（L-051/L-052）；前置 L-051；负空间 L-045/L-050/L-051/L-052/L-053/L-054/L-083…L-085
- difficulty: medium

课把同步邮筒收成「两张名字、一支枪」。计划里至少有五套可数对象，派单点名的是 **矩阵 (b) 这一行**，但 chain / 课头把它印成 **两个 claimed 符号**：

1. **OBJ-82 原文。** `course.md`：能口述 `NotifyClient.send` / `NotifyDispatcher` 与业务模块的同步入口。档是方法性状。一句里是 **接口 + 实现 + 谁允许扣扳机**，没有把 `/notify/notification` 四扇、Outbox 领取、供应商 HTTP、厨房工厂写成口试清单，也没有写出 `NotifyChannel.IN_APP`。
2. **chain / 课 `claimed_cells`。** 两格：`B:NotifyClient.send`、`B:NotifyDispatcher.send`。前置 L-051。磁盘上它们是 **同一支** `send`：接口 12 行声明，Dispatcher 70 行实现。chain **不**写 `DispatchNotificationService.dispatch`，也 **不**写 HTTP。
3. **矩阵一行对照。** (b) 符号用斜线并写，口号「同步渠道入口」。Lead 若按这一行标 covered，**不会**绑死 L-051 四枪或 L-052 领取。Lead 若按 chain 两格去矩阵里找第二行，会发明格子。矩阵 **没有** Dispatcher Controller，也没有 IN_APP 渠道行。
4. **课自己的操作定义。** Bean 口是 `NotifyClient`（`@ConditionalOnMissingBean(NotifyClient.class)` 返回 `new NotifyDispatcher(...)`）。业务同步入口停在 `NotificationApplicationService.submit`。通知楼 MAIL/SMS 才 `notifyClient.send`；`IN_APP` 走 `InAppNotificationPort.persist`。`NotifyChannel` 常量只有 `mail`/`sms`。`USER` → `LOGICAL_TARGET_NOT_SUPPORTED`。非 `ACCEPTED` → `NotifyDeliveryException`，细节在 `exception.result()`。
5. **邻格（已 covered，不要再盖）。** L-051 柜台四枪 + 写篮。L-052 短事务领取。L-050 回执不 `send`。L-045 公告 publish/retract。L-053/L-054 厨房/菜单。矩阵 (d) 主走廊原文 `submit → Outbox → claim → 渠道` 已盖在写篮+领取，**不是**本课把「渠道」再立一条 (d)。
6. **未入库。** 矩阵 **没有** `DispatchNotificationService` 独行、没有附件 SPI 行、没有 Nacos 热更新行（Goal 已 `deferred(optional-overlay)`）、没有 Dispatcher HTTP 窗。

问：本格闭合标准是「矩阵这一行 = 同一支同步 `send`：Bean 类型 `NotifyClient`、跑步的是 `NotifyDispatcher`、生产唯一扣扳机是通知楼 MAIL/SMS、业务停在 `submit`、无 HTTP、无 `NotifyChannel.IN_APP`、逻辑目标当场退回」，还是 OBJ 那句必须先等 L-051/L-052 再盖章、或必须把 chain 两格拆成矩阵两行、或必须发明 IN_APP 渠道行 / Dispatcher HTTP 窗才算讲完？Lead 若凭「两个方法名」把已 covered 的 submit/claim 再盖一次，是否假闭合了柜台与领取？Lead 若因「要等前置课」把本行继续留 uncovered，是否把教学 DAG 误当成盖章闸？

## Q2 证据

- objective_id: OBJ-82
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: NotifyClient 9–12；Dispatcher 32 / 70–115 / 117–136 / 152–153 / 223–227 / 230–241 / 283–304；AutoConfiguration 34–77；imports 1；pom 14–30；NotifyChannel 18–19；Dispatch 47 / 86–123 / 223–227 / 250–255；api Service 6–14；NotificationChannel 6；CaptchaController 54；CallerTest 71–80；DispatcherUnitTest 57–103 / 114–127 / 131–146；IdempotencyAutoConfig 34–41；AutoConfigUnitTest 23–44；DispatchTest 51–57 / 92–110 / 114–135；MigrationContract 15–40；MailAdapter 39–45；SmsAdapter 23–29；MailConfig 53–57；SmsAutoConfiguration 49–51；notification.md 5–19；module-map 31；common-notify 无 src/test、零 RequestMapping；全仓生产 NotifyClient 仅四文件
- difficulty: high

课的 (b) 同步渠道入口哪些能在工作树直接复现，哪些只是测试名单比课表窄、或口试把「业务入口」说成「注入 Dispatcher」？

**仍能复现（本课这一格 (b)）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| `@FunctionalInterface`；公开方法只有 `NotifyResult send(NotifyRequest)`；JavaDoc「统一通知同步入口」 | NotifyClient 7–12 |
| `public final class NotifyDispatcher implements NotifyClient`；JavaDoc「统一通知同步调度器」 | Dispatcher 29–32 |
| `send` 在调用线程：校验 → 去重附件 → `registry.require` → 目标校验 → 上下文 → 幂等 → 快照 → Adapter → 聚合 → complete → 事件 → `requireAccepted`；**无** `@Transactional` | Dispatcher 70–115 |
| 非 `ACCEPTED` 扔 `NotifyDeliveryException`；消息「通知未被全部目标接受」；调用方读 `result()` | Dispatcher 223–227；Exception 8–18 |
| `USER`（忽略大小写）→ `LOGICAL_TARGET_NOT_SUPPORTED`，「common-notify 只接受物理目标」；发生在 Adapter 前 | Dispatcher 235–237；单测 81–102，`attemptedTargets` 仍空 |
| 模板空白快照 → `CONTENT_SNAPSHOT_REQUIRED` | Dispatcher 130–131；单测 89–101 |
| 有附件但 SPI 未装配 → `ATTACHMENT_SNAPSHOT_NOT_CONFIGURED` | Dispatcher 152–153 |
| 普通 `RuntimeException` 折成全目标 `PROVIDER_ERROR`，再聚合后仍可能 DeliveryException | Dispatcher 105–109 / 264–270 |
| 事件失败只 warn，不改 `NotifyResult` | Dispatcher 283–291；单测 `shouldNotChangeProviderResultWhenEventPublishingFails` 131–146 |
| 重复完成：方法 `requireAccepted(原结果)`；事件另造 `SKIPPED_DUPLICATE`、空 deliveries | Dispatcher 84–86 / 294–304；幂等单测 36–51 |
| Bean 方法返回类型 `NotifyClient`，条件 `@ConditionalOnMissingBean(NotifyClient.class)`，体内 `new NotifyDispatcher(...)`，附件 SPI `getIfAvailable()` | AutoConfiguration 68–77 |
| Store 还要 `@ConditionalOnBean(RedissonClient.class)`；无 Redisson 时 Client **仍在**，有键 `begin` 才不可用 | AutoConfiguration 53–56；IdempotencyAutoConfig 34–41 |
| AutoConfiguration.imports 只登记 `NotifyAutoConfiguration` | imports 第 1 行 |
| pom 描述「渠道无关通知契约」；显式依赖 `wta-common-nacos` + `core` + `redis` | pom 14–30 |
| 模块 **无** `src/test`（`src/` 只有 `main`）；契约测试在 `wta-admin/.../test/notify/` | 目录；module-map「tests through consumers/admin」 |
| 工具间 **零** `@RequestMapping` / `@RestController` | 包检索为空 |
| `NotifyChannel` 内置只有 `MAIL="mail"`、`SMS="sms"`；允许自定义小写牌子，**没有** `IN_APP` | NotifyChannel 18–19；common-notify 零 `IN_APP` |
| `wta-api` 才有 `enum NotificationChannel { IN_APP, SMS, MAIL }` | NotificationChannel 6 |
| 生产 `NotifyClient` 字段只在 `DispatchNotificationService`；`dispatch` 第 123 行 `notifyClient.send(request)` | Dispatch 47 / 113–123 |
| `IN_APP` 分支 `persist` / `pushRealtime`，**不**碰 `notifyClient` | Dispatch 86–105 |
| MAIL/SMS：`NotifyChannel.of(channel.toLowerCase())`；`requestId`/`idempotencyKey`=`deliveryId`；邮件 `NotifyRichContent`；短信 `NotifyTemplateContent(..., "")` | Dispatch 113–121 / 223–227 |
| 缺绑定 / 账号停用 / 配额：`never().send` | DispatchTest 51–88 / 139–157 |
| 邮件正文权威在场景绑定，不是调用方 snapshot | DispatchTest 92–110 |
| 邮件 Adapter `channel()=MAIL`，支持类型只有 `EMAIL`；短信 `channel()=SMS`，只有 `PHONE`；各自 AutoConfiguration 注册，**不是** notify 模块登记 | MailAdapter 39–45；SmsAdapter 23–29；MailConfig 53–57；SmsAutoConfiguration 49–51 |
| 三份 AutoConfiguration 一起跑才能 `registry.require(MAIL/SMS)` | AutoConfigUnitTest 23–44 |
| 业务入口是 `NotificationApplicationService` 四方法；规范禁止业务依赖渠道客户端 | api 6–14；notification.md 5–19；module-guide Notify 事实 |
| `CaptchaController` 字段类型是接口；测试方法名 `emailCaptchaUsesNotifyClient...` 断言的是 `submit` | CaptchaController 54；CallerTest 71–80 |
| captcha / workflow / mailDemo / smsDemo / rebind / transfer / notice 源码含 `NotificationApplicationService`，不含 `MailBuilder` / `SmsFactory` | MigrationContract 15–40 |
| 生产模块检索：`wta-system` / 其它业务房间 **零** `NotifyClient` 引用；pom 有依赖边 ≠ 调用授权 | grep 空；system pom 64 行旁证 |
| 部分失败仍跑完全部目标，调用方接到异常 | DispatcherUnitTest 57–77 |
| 合规但未登记渠道 `feishu` → `UNKNOWN_CHANNEL` | DispatcherUnitTest 114–127 |
| `NotifyTarget.user` 工厂存在 | NotifyTarget 28–30 |
| 空 `supportedTargetTypes` 不再按 PHONE/EMAIL 滤，**仍**拒绝 USER | Adapter 16–18；Dispatcher 235–239 |
| 幂等键哈希不含明文键、不含手机号；默认窗 5 分钟；越界 `IDEMPOTENCY_WINDOW_OUT_OF_RANGE` | 幂等单测 123–141 |
| 无键且 Store 抛错仍可打供应商；有键则失败关闭 | 幂等单测 89–119 |
| 附件 `[10,10,20]` 去重保序；供应商失败保留快照对象 | Attachment 26–40 / 57–69 |
| admin 覆盖 `RequestNotifyContextResolver`（LoginHelper + MDC）；resolver 空则 `NotifyContext.empty()`，不挡发送 | NotifyContextConfiguration 16–19；Resolver 24–28；Dispatcher 244–247 |
| 生产 **无** `implements NotifyAttachmentSnapshotService` / `NotifyLogIdGenerator`（测试 Recording 除外） | 全仓检索只命中单测内部类 |

**邻格仍能复现（不计入本格闭合；用来判断要不要等 L-051/L-052）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 业务交单是 `submit`，同一事务写篮 | L-051 已 covered |
| 领取短事务之后才 `dispatch` | L-052 已 covered；Dispatch javadoc 41：Provider 在事务外 |
| 回执升级已有 Delivery，不 `send` | L-050 已 covered |
| 厨房无 `send` 枪 | L-053 已 covered |

**课写过、测试名单更窄或口试标签写偏（不是证伪本格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「唯一生产调用方」 | 生产 `NotifyClient` 引用只四文件。单测另有 mock |
| 「IN_APP 不 send」 | 源码 86–105 互证。`DispatchNotificationServiceTest` **无** IN_APP 用例 |
| 「短信工人塞空 `contentSnapshot`，真 send 会拒绝」 | Dispatch 227 第四参 `""`；DispatchTest **mock** Client，不会爆 `CONTENT_SNAPSHOT_REQUIRED` |
| 「common-notify 自己测自己」 | 无 `src/test`。证人在 admin |
| Skill `other-utils.md`「通知统一走 NotifyClient」 | 与 `notification.md` / 磁盘冲突。课已写「以规范 + 磁盘为准」 |
| 课 `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |
| 「Nacos 依赖」 | pom 有 `wta-common-nacos`；properties id `notify-idempotency`。Goal 叠加仍 deferred |

问：在函数式 `send`、Bean 口是接口、`requireAccepted`、USER/空白快照在供应商前拒绝、生产唯一 MAIL/SMS 扣扳机、IN_APP 源码不 send、业务只 `submit`、工具间无 HTTP / 无 `src/test` 已经能互证的前提下，(b) 是否还要把「IN_APP 无 dispatch 单测」「短信空快照被 mock 遮住」「Skill 摘要冲突」写成开篇半句才算讲完？还是这些已经落在课的表/反例里，Lead 可以把矩阵这一行标 covered，且 **不必**等 L-051/L-052、也 **不要**发明 IN_APP 渠道行或 HTTP 窗？

## Q3 假设与反例

- objective_id: OBJ-82
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 课反例 1–15 / 误区 1–18 / 边界 1–10；notification.md 5–19；Captcha 测试名 vs submit；Dispatch 86–105 / 227 / 250–255；NotifyChannel 18–19；NotifyTarget.user 28–30；requireAccepted 223–227；无 RequestMapping；无 src/test
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇门认错？

1. **「业务要发短信就 `@Autowired NotifyDispatcher` / `NotifyClient`。」** 规范禁止。磁盘业务走 `submit`。课反例 1 / 误区 1。
2. **「`NotificationMode.SYNC` 就是 `send`。」** SYNC 仍写 Outbox。同步邮筒在 claim 之后。课反例 2。
3. **「`NotifyDispatcher` 是 HTTP Controller。」** 工具间零扇映射。课反例 3。不要发明 HTTP 窗。
4. **「站内信也走 `send` / 存在 `NotifyChannel.IN_APP`。」** 工人 IN_APP 走 Port；common-notify 没有该常量。`NotificationChannel.IN_APP` 属于 `wta-api`。课反例 4 / 误区 5。不要发明 IN_APP 渠道行。
5. **「`NotifyTarget.user` 工厂存在所以能发给供应商。」** Dispatcher 拒，码 `LOGICAL_TARGET_NOT_SUPPORTED`。课反例 5。
6. **「`send` 返回 `SKIPPED_DUPLICATE` / `FAILED`。」** 返回路径只放行 ACCEPTED；失败扔异常；重复态在事件。课反例 6–7 / 误区 7–8。
7. **「验证码测试名证明走 Client。」** 读 `verify(notificationService).submit`。课反例 13 / 误区 9。
8. **「厨房或回调会 `send`。」** 不会。课反例 8–9。
9. **「把 `dispatch` 标 covered 就算本格。」** 矩阵符号是 `NotifyClient.send / NotifyDispatcher.send`。课反例 14 / 误区 15。
10. **「`wta-system` POM 写了 `wta-common-notify` 所以 system Service 可以 `send`。」** 依赖边不是调用授权。课反例 15。
11. **「两张 (b) 是两个方法。」** 同一支 `send`。课误区 2。不要拆矩阵第二行。
12. **「注入实现类 / 找名为 `notifyDispatcher` 的 Bean。」** 注入 `NotifyClient`。课误区 3。
13. **「`send` 异步、自己写 Outbox。」** 调用线程等到 Adapter；Outbox 在通知楼。课误区 6。
14. **「没 Redis 就没有 `NotifyClient`。」** Client 在；有键才失败关闭。课误区 12。
15. **「附件已经接到 OSS。」** 生产无 SPI 实现。课反例 12 / 误区 13。
16. **「模板无快照也能发，因为工人就是这么写的。」** Dispatcher 单测锁死拒绝；工人那条河被 mock 遮住。课误区 16 / 边界 4。
17. **「`NotifyAuditPolicy.REDACT_SENSITIVE` 会在 send 里抹手机号。」** `send` 只在拷贝 request 时原样带走该字段，不读它改发送。课误区 18。
18. **「本课覆盖 Nacos 热更新。」** pom 边可口述；Goal 叠加 deferred。课误区 17。
19. **「必须等 L-051/L-052 mined 才给本行盖章。」** 前置是教学顺序。那两格矩阵已 covered。本行证人是 Client/Dispatcher/唯一调用方，不是四枪 HTTP。
20. **「未知渠道会落到 `NotifyTarget.user` 再 send。」** `target()` 的 default 分支确实 `user(userId)`（Dispatch 254），但 IN_APP 在 if 里提前走 Port；MAIL/SMS 走 email/phone。不要把 default 说成「USER 能发」。
21. **「矩阵要先有幂等四态 / 附件 SPI 独行才算 (b) 讲完。」** 矩阵无这些行。它们是 (b) 的 explained。
22. **「Skill 摘要与磁盘冲突时应改磁盘。」** 课与规范：以磁盘 + `notification.md` 为准。

问：读完后仍说不出的，是不是「两个类名」，而是 **Bean 口是接口、同一支同步枪、业务停在 submit、唯一生产扣扳机是 MAIL/SMS、IN_APP 不进邮筒、USER 与空白模板快照在供应商前拒绝、非 ACCEPTED 走异常、无 HTTP、测试在 admin**？

## Q4 视角

- objective_id: OBJ-82
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-82 vs L-045/L-050/L-051/L-052/L-053/L-054/L-083/L-084/L-085；chain 两格 vs 矩阵一行；notification.md 5–19
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **矩阵 (b) `NotifyClient.send / NotifyDispatcher.send`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | chain 两格 vs 矩阵一行；(a) 无本符号；(d) 主走廊已 covered；(c) 容器仍 uncovered | **是** 闭合定义。按 **一行** covered。不要拆第二 (b)，不要发明 IN_APP/HTTP 行，不要等 L-051/L-052，不要再盖已 covered 邻格 |
| 接口作者 | 函数式 `send`；无 submit/query/retry | **是** `B:NotifyClient.send` |
| 调度器作者 | 校验/幂等/快照/Adapter/聚合/`requireAccepted` | **是** `B:NotifyDispatcher.send`（同一支枪） |
| 装配电工 | Bean 类型接口；Store 条件 Redisson；附件 getIfAvailable | **是** (b) 的装配半句 |
| 通知楼工人 | MAIL/SMS 填物理信封再 send；IN_APP 不进 | **是** 唯一调用方对照。`dispatch` 升格第二 (b) **否** |
| 业务房间 | `NotificationApplicationService.submit` | OBJ「与业务模块的同步入口」**是** 对照。四枪 HTTP **否**（L-051 已 covered，不要再盖） |
| 领取（L-052） | 短事务之后才允许碰 Port | 上游开关 **是** 一句。领取格子 **否**（已 covered） |
| 回调（L-050） | HMAC；不 `send`；读 `providerMessageId` | ID 从哪来 **是** 一句。门铃 **否**（已 covered） |
| 公告（L-045） | Publisher 仍 `submit` | 对照 **是**。六扇 **否**（已 covered） |
| 厨房/页（L-053/L-054） | 无 send 枪 | 负空间 **是**。工厂 **否**（已 covered） |
| 验证码（L-012） | 测试名误导 | 读 submit 断言 **是**。验证码窗 **否**（已 covered） |
| 出站网关（L-064） | `ThirdPartyGateway.execute` 是另一条河 | 对照「不是这条枪」**是**。三方格子 **否** |
| Redis 8 / 幂等桶 | 键前缀 + sha256；无 Redisson 有键失败关闭 | 本窗机制举例 **是**。容器行 **否**（L-084 也不是这条桶） |
| MySQL 8.4 / dao-mapper | Dispatcher **不写表**；工人短事务改 Delivery | 不拥有库 **是** 边界。容器/组件课 **否**（L-085/L-083） |
| LoginHelper / StpUtil | 上下文快照可能读登录人；Worker 无登录人仍可 send | 对照 **是**。会话写入 Redis **否**（L-084） |
| BaseMapperPlus / QueryBuilder | 本模块无 Mapper | **否**（L-083） |
| Nacos 叠加 | properties 实现 `NacosConfigParticipant` | 解释 pom 边 **是**。格子 **否**（deferred） |
| 附件 SPI / 厂商 Adapter 内部 | 位置已标；生产未挂实现；SMTP/SMS4J 不拆 | 边界 **是**。不要升格 claimed cell |
| 测试夹具行 | 矩阵已 deferred | 证人 **是**。不要把夹具行标 covered |

问：Lead 若凭「两个符号在 claimed 列里」把矩阵这一行标 covered，是否还要求读者能指着 **Bean 口、唯一 MAIL/SMS 扣扳机、业务停在 submit、USER 拒绝、非 ACCEPTED 走异常、IN_APP 不进邮筒**？这些是不是已经在本格机制链/反例里，而不需要等 L-051、也不需要发明 HTTP/IN_APP 行？L-051/L-052 已 covered 的柜台与领取，是不是对照边而不是本行盖章闸？

## Q5 元问题

- objective_id: OBJ-82
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (b) 本符号仍 uncovered；其余 uncovered 为 (c)/(d)/邻 (b) 基座，不是本行空洞
- difficulty: low

建议：`defer`

原因：

- **矩阵 (b) 一行与 OBJ-82 / chain 两格 / 课 claimed 1:1（两名一枪）。** L 合同章节齐全（宏观三层信、直觉十二句、定义表、机制链 1–6、带 caption / 文字等价物的邮筒图、决策表、正反例、变式、误区、暂停走查、来源表 S-007/S-015/S-L082-01…08）。**主叙事站得住且与 2026-09-17 磁盘对齐**：函数式 `send`、Bean 口 `NotifyClient`、实现 `NotifyDispatcher.requireAccepted`、生产唯一调用方 `DispatchNotificationService` 第 123 行、IN_APP 不 send、无 `NotifyChannel.IN_APP`、USER 拒绝、业务只 `submit`、无 HTTP、无 `src/test`。不必为「两个方法名」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例，不挡住把 **这一行** 标 covered：（1）IN_APP 无 dispatch 单测，证人是源码分支；（2）短信空快照被 mock 遮住；（3）Skill `other-utils` 摘要与磁盘冲突；（4）chain 估时 35、课 38；（5）生产附件 SPI 未挂。不要把 submit 四枪、claim 租约、回调验签、厨房工厂、MySQL/Redis 容器、LoginHelper 会话写入塞进本格。
- **不要等 L-051/L-052 才给本行盖章。** 前置已 mined 且那几格已 covered。本行证人不住在 Controller / ClaimUseCase。也不要用本课再盖主走廊「渠道」二字——那条 (d) 已按写篮+领取闭合。
- **不要发明 IN_APP 渠道行或 Dispatcher HTTP 窗，也不要把 chain 两格拆成矩阵两行。** 同一支 `send`。`dispatch` 是调用方，不是第二张 (b)。
- 不建议 `mine-more`：b01 未证明本行还有真实矩阵空洞；再五问只会把已 covered 的 L-051/L-052/L-050/L-053 或未讲的 L-083…L-085 再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；可选半句（mock 遮住短信快照；IN_APP 无单测；Skill 摘要）不是第二容器。不建议 `split-lesson`：未满 10 问；两名一枪是 chain 原设计的同一 OBJ-82，同一 `wta-common-notify` 容器。`split_depth: 0`，本批禁止填 split 表。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| B:NotifyClient.send / NotifyDispatcher.send（矩阵一行「同步渠道入口」） | 两名一枪、Bean 口是接口、`requireAccepted`、USER/空白快照在供应商前拒绝、生产唯一 MAIL/SMS 扣扳机、IN_APP 不进邮筒、业务停在 `submit`、无 HTTP、测试在 admin——均与源码对齐。残留精度：IN_APP 无 dispatch 单测；短信空快照被 mock 遮住；Skill 摘要冲突。 | 可选半句：口试「IN_APP 不 send」以 Dispatch 86–105 为准；短信第四参空串以 Dispatcher 契约为准，不要拿 mock 当通行证。不要求重写机制链。**若 Q2 磁盘与课一致**，Lead 可将矩阵该行标 `covered`（证据锚本 probe 与 L-082 来源表 S-L082-01…05）。不要拆成两行。 | defer（本行可 covered；精度项不挡盖章） |
| A:NotificationController.submit/retry/cancel / B:写 Outbox | 业务柜台。本课只对照「停在 submit」。 | 不要再盖。L-051 已 covered。 | defer（L-051 已 covered；不要再盖） |
| B:NotifyOutboxClaimUseCase / D:fail-outbox | 领取与失败保持待领取。本课只接到 claim 成功之后。 | 不要再盖。L-052 已 covered。 | defer（L-052 已 covered；不要再盖） |
| D:主路径 通知提交 | 矩阵已 covered（submit 写篮 + claim 领走）。本课是汇上的同步枪，不是第二 (d)。 | 不要用本课改该行，也不要另写「主路径 send」。 | defer（已 covered；不要再盖） |
| A:ProviderCallbackController.callback | 回执不 `send`。 | 不要再盖。L-050 已 covered。 | defer（L-050 已 covered；不要再盖） |
| A:NotifyNoticeController.publish/retract | 公告仍 `submit`。 | 不要再盖。L-045 已 covered。 | defer（L-045 已 covered；不要再盖） |
| A:createNotificationService / A:createNotifyWebDomain | 厨房/菜单无 send。 | 不要再盖。L-053/L-054 已 covered。 | defer（L-053/L-054 已 covered；不要再盖） |
| A:DispatchNotificationService.dispatch | 矩阵无此符号。课已写进调用方对照。 | 不要发明第二张 (b)。 | defer（未入库） |
| (b) `NotifyChannel.IN_APP` / Dispatcher HTTP 窗 | 矩阵无此行。磁盘无该常量、无该映射。 | 不要发明。 | defer（未入库；禁止立行） |
| B:LoginHelper / StpUtil | 上下文快照可能读登录人。不是会话写入 Redis。 | 保持。L-084。 | defer（L-084） |
| B:BaseMapperPlus / QueryBuilder | 本模块无 Mapper。 | 保持。L-083。 | defer（L-083） |
| D:失败路径 会话丢失 | 与 send 无关。 | 保持。L-084。 | defer（L-084） |
| D:存储点 MySQL / C:MySQL 8.4 / C:mysql-base | Dispatcher 不写业务表。 | 不要因本课标容器 covered。 | defer（L-085） |
| C:Redis 8 / C:dao-mapper-ownership | 幂等桶是本窗机制举例，不是容器课。 | 不要因本课标 Redis/DAO 所有权 covered。 | defer（(c) 容器 / L-084 / L-083） |
| wta-common-nacos 叠加 | pom 边可口述。 | 保持 `deferred(optional-overlay)`。 | defer（nacos overlay） |
| 测试夹具 `*Test.java` | 矩阵已 deferred。 | 证人不是格子。 | defer（test-fixture） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本行 (b) 无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出目的（同步把物理信封交给 Adapter）、主路径（校验 → 幂等 → Adapter → 全部 ACCEPTED 才还结果）与失败路径（USER/空白快照/未知渠道在供应商前拒绝；非 ACCEPTED 扔带着同一张结果的异常；有键无 Store 失败关闭）；缺的其余格子是 L-083…L-085 基座与未入库符号，不是本课容器；把可选半句写进原课不会超过 40 分钟；柜台、领取、回调、厨房已有邻课且已 covered。满 10 问前不拆。两名一枪是 chain 原设计的同一 OBJ-82，同一 `wta-common-notify` 容器，不是「超过 1 个容器却讲不完」。不要把 `dispatch` 或 IN_APP 拆成新课。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对已 covered 的 L-051 柜台、L-052 领取、主走廊、L-050 回调、L-045 公告、L-053/L-054 前端显式建议 `defer`（不要再盖）；对未入库 `dispatch` / IN_APP 渠道行 / Dispatcher HTTP 窗、(c) MySQL/Redis/dao-mapper/mysql-base、(d) 会话丢失与存储点 MySQL、(b) LoginHelper / BaseMapperPlus、nacos overlay 与夹具行显式建议 `defer`；对矩阵 (b) 行 `NotifyClient.send / NotifyDispatcher.send` **仅当 Q2 磁盘与课一致** 建议 Lead 标 `covered`（证据锚本 probe 与 L-082 来源表 S-L082-01…05），不要等 L-051/L-052，不要拆成两行，不要发明 IN_APP 渠道或 HTTP 窗。本课不主张用同步枪再盖主走廊，也不主张把 Skill 摘要冲突当成改磁盘的理由。
