---
artifact: learning-goal-probe
probe_id: GP-L-040-b01
lesson_id: L-040
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-040 / b01

审问对象是已写成的 Lesson `lessons/L-040-enterprise-admin.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain / `claimed_cells` / 矩阵 (a) **一行**）：`A:EnterpriseAdminController.*`。派单与矩阵符号把十二个 Java 名写全：`page, eligibleUsers, detail, reviewContext, reviewMaterial, material, decide, create, revise, assign, manageBinding, revoke`。OBJ-40 原文：能口述 `EnterpriseAdminController` **档案/审核/绑定**（三个词，**没有**把吊销写成口试名词）。档是**解释+走查**，不是方法性状。chain 覆盖列只写 `A:EnterpriseAdminController.*`。矩阵 (a) 该行模块 `wta-profile-enterprise`，仍 `uncovered`。矩阵 (b) **没有** decide/revoke/binding 独行；唯一相关 (b) 是 `EnterpriseTransferController.confirm`（L-042）。矩阵 (c) `wta-modules` 业务房间已 covered（L-002/L-003）；MySQL 8.4 / `dao-mapper-ownership` 仍 uncovered。矩阵 (d) 「主路径 profile 申请」=`PersonApplication.submit → admin decide` 已 covered（L-035+L-034），**没有**企业 submit→decide 独行。本批按派单审问 **这一行十二扇窗**；自助 `EnterpriseApplicationController`、核身回调、转移三枪、材料总库、前端工厂、个人柜台不是本行符号。

抽查锚点（2026-09-16）：`backend/wta-modules/wta-profile/wta-profile-enterprise/src/main/java/org/namewta/profile/enterprise/controller/admin/EnterpriseAdminController.java`；`controller/advice/EnterpriseAdminExceptionHandler.java`；`usecase/EnterpriseAdminUseCase.java` + `usecase/impl/EnterpriseAdminUseCaseImpl.java`；`service/{EnterpriseAdminService,IEnterpriseAdminService}.java`；`dao/EnterpriseAdminDao.java`；`mapper/EnterpriseAdminMapper.java` + `resources/mapper/enterprise/EnterpriseAdminMapper.xml`；`domain/{ProfileEnterprise,ProfileEnterpriseBinding,ProfileEnterpriseVersion}.java`；`domain/vo/{EnterpriseAdminResultVo,EnterpriseProfileAccessUrl}.java`；`domain/bo/EnterpriseAdmin{Query,Create,Decision,Binding,Assign,Revoke}Bo.java`；`port/EnterpriseApplicationPublicationPort.java`；`adapter/gateway/SpringEnterpriseWorkflowGateway.java`；测试 `EnterpriseAdminHttpContractTest`、`EnterpriseModuleArchitectureTest`、`EnterpriseBeanValidationContractTest`、`EnterpriseAdminServiceTest`、`EnterpriseAdminMySqlE2ETest`、`src/test/.../service/impl/EnterpriseAdminServiceImpl.java`；DDL `10-cde-base-ddl.sql` `profile_enterprise*` 生成列；DML `50-cde-base-dml.sql` 210050…0020–0025；`controller/self/{EnterpriseApplicationController,EnterpriseTransferController}` 与 `controller/admin/EnterpriseMaterialAdminController` 仅作负空间。

## Q1 澄清

- objective_id: OBJ-40
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-40；chain L-040 覆盖列只 `A:EnterpriseAdminController.*`；L-040 `claimed_cells` 一格；矩阵 (a) 十二名与磁盘公开方法 1:1 仍 uncovered；矩阵 (b) 无本课符号、相关行是 transfer confirm；(c) wta-modules 已 covered、MySQL uncovered；(d) 无企业申请流独行；课开篇十二扇 + 负空间 L-041…L-044 / L-034；误区 17 点名 OBJ 没写吊销
- difficulty: medium

课把管理端企业档案柜台收成「一块门牌、十二扇窗、五层楼梯」。计划里至少有五套可数对象，闭合标准要以哪一套盖章？

1. **OBJ-40 原文。** `course.md`：能口述 `EnterpriseAdminController` 档案/审核/绑定。三个词是口试主题，没有把 `eligibleUsers` / `reviewMaterial` / `revoke` / `EnterpriseAdminResultVo.version` 三把尺写成清单。对照 OBJ-34 四个词含吊销，本 OBJ **故意少一个词**。
2. **chain / 课 `claimed_cells`。** 一格：`A:EnterpriseAdminController.*`。chain 不写 UseCase/Service/XML 符号。
3. **矩阵 (a) 一行。** 十二个方法名与磁盘公开映射同名，模块 `wta-profile-enterprise`。与 Controller 1:1，**含 `revoke`**。Lead 若按行标 covered，**不会**把 `EnterpriseApplicationController.current/save/submit/probe` 或 `EnterpriseTransferController.send/confirm/unbind` 假闭合——那是另两行。
4. **课自己的操作定义。** 门牌 `/profile/enterprise/archive`；GET 六 / POST 六；五颗权限不并成「企业管理员」；写窗 `LoginHelper.getUserId()`；Controller 注入 `EnterpriseAdminUseCase`（字段名叫 `service`）；生产主链不用 `IEnterpriseAdminService`；layered 不是 classic `I*Service`；合格负责人必须先有个人档案；`revoke` 是档案生命周期关闭闸，不是另一扇门牌。
5. **邻格 / 未入库。** L-041 自助门 + 核身回调；L-042 转移（(b) `confirm`）；L-043 材料总库；L-044 前端工厂；L-034 个人柜台；(c) MySQL 8.4。矩阵 **没有** 企业 submit→decide 的 (d)。

问：本格 (a) 闭合标准是「十二扇路径/动词/权限/写窗操作者 + layered 不走 `IEnterpriseAdminService` + 档案/审核/绑定/吊销四件不是同一支笔 + 合格负责人先有个人档案」，还是 OBJ-40 三个词（因而把 `revoke` 留给转移课）就够盖章？若是前者，Lead 标本行 covered 是否必须先给 UseCase/Service/XML 或发明企业申请 (d) 单独立行，而不是把十二扇并进自助门、转移 (b)、个人柜台或 MySQL 容器？

## Q2 证据

- objective_id: OBJ-40
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 23–156；Handler 10–16；UseCase 14–78；UseCaseImpl 18–100；Service 39–210 / 215–291 / 295–542 / 656–734 / 738–764；CreateBo 12–17；QueryBo 4–10；ResultVo 4；AccessUrl 13；HttpContractTest 19–46；ArchitectureTest 74–113 / 162–172 / 237–239；BeanValidation 56–84；Mapper.xml 7–33 / 59–67 / 86–96 / 111–129 / 133–168 / 178–190 / 259–266 / 281–286；DDL 924–937 / 1110–1128；DML 1513–1518；ServiceTest 47–114；E2E 78–188；夹具 ServiceImpl 19–44；PublicationPort 24–26；ApplicationService 390–395 / 441–451；SpringEnterpriseWorkflowGateway 53–64；Gateway 12–14；IEnterpriseAdminService 35–42
- difficulty: high

课的柜台机制链哪些能在工作树直接复现，哪些只是测试名单比课表窄、或邻格事实？

**仍能复现（本格）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 类注解 `@Validated @RestController @RequiredArgsConstructor @RequestMapping("/profile/enterprise/archive")`；无 `BaseController`、无 `@ConditionalOnProperty`、无 PUT/DELETE | Controller 23–27；映射全是 Get/Post；合同测试 45–46 方法名不含 `export`/`delete` |
| 公开 HTTP **正好十二个**，与矩阵十二名 1:1 | Controller 35–156；HttpContractTest 23–35 |
| GET 六扇权限：page/detail=`query`；reviewContext/reviewMaterial=`review`；material=`material`；**eligibleUsers=`override` 不是 query** | 35–85；合同测试 23–28 |
| POST 六扇：decide/create/revise/assign/revoke=`override`；**只有** manageBinding=`manage`；六扇 `@Log` 双 false；`assign` title「管理员指定企业档案负责人」 | 90–156；合同测试 29–44 |
| 写窗一律 `LoginHelper.getUserId()`；GET 不传操作者 | 96 / 107 / 119 / 131 / 143 / 155 vs 37–85 |
| 路径 `long` 标 `@Positive`；请求体标 `@Valid`；`page` 的 QueryBo **没有** `@Valid` | Controller 37 vs 54–155；QueryBo 4–10 无约束；BeanValidation 56–74 |
| Controller 字段名 `service`，类型 `EnterpriseAdminUseCase`；源码不 import `service` 包 | 29；ArchitectureTest 107–112 |
| HTTP 方法 `reviewContext` 调 UseCase/Service **`review`** | Controller 61–65；UseCase 43–46；Service 150–156 |
| UseCaseImpl **十二个**方法都有 `@DSTransactional`（含 page/detail） | UseCaseImpl 26–99 |
| 无操作者 default 抛 `请传入 operatorId`；生产 Controller 走带 operatorId 的重载 | UseCase 14–30 vs Controller 96 等 |
| `EnterpriseAdminService` 是 `@Service` 具体类，**不** `implements IEnterpriseAdminService`；全仓只有接口自己一处引用 | Service 39–40；grep 仅 `IEnterpriseAdminService.java` |
| 生产主构造接 `EnterpriseWorkflowGateway`；兼容构造吃 `WorkflowService` + 无用 `jsonMapper` | Service 51–57 / 71–90 |
| `page`：pageNum&lt;1→1；pageSize≤0→20、上限 200；空 status → SQL `p.status != 'REVOKED'`；乱写 → `ENTERPRISE_QUERY_STATUS_INVALID`；企业名称 like、信用代码精确大写 | Service 295–307；XML 7–13 / 28–31 |
| 名册左连「还钉着」`status in ('ACTIVE','SUSPENDED')` | XML 25–26 / 44–45 / 86–90 |
| `eligibleUsers`：blank → `[]`；`searchActiveUsers(strip, 50)`，留下 `status=="0"`、**`ProfileSummary.person() != null`**、无企业有效钉子，最多 20 | Service 117–135；ServiceTest 88–101 |
| `detail` 缺行 → `ENTERPRISE_PROFILE_NOT_FOUND`；注销本仍能打开（`requireProfile` 不查 REVOKED） | Service 139–146 / 656–661；E2E 145–161 |
| 材料名牌 `materials.list(VERSION, currentVersionId)`；无 CURRENT 则 `versions.getFirst()`（XML `order by version_no desc`） | Service 141–144；XML 59–66 |
| `selectReview` 要求 `submission_seq > 0`；找不到 → `ENTERPRISE_REVIEW_CONTEXT_NOT_FOUND` | XML 111–119；Service 675–680 |
| `reviewMaterial` owner=`SUBMISSION`；`material` owner=`VERSION` 且必须有 CURRENT，否则 `ENTERPRISE_PROFILE_VERSION_NOT_FOUND` | Service 160–171；owner 写死 `ProfileType.ENTERPRISE` 768–770 |
| 出门条投影 `EnterpriseProfileAccessUrl(accessType, url, expiresAt, fileName)`，不漏 system 类型 | Service 173–175；record 13 |
| `decide` 只认大写 `APPROVED`/`REJECTED`；reason 必填 ≤500；Service 字段 `workflow==null` → `ENTERPRISE_ADMIN_WORKFLOW_UNAVAILABLE` 且 `never beginDecision` | Service 41 / 180–188 / 783–788；ServiceTest 105–113 |
| 先 `beginDecision`（WAITING→OVERRIDE_PENDING + 决定记录 PENDING）再 `workflow.terminate(Long.toString(applicationId), reason)`；抛错 → `ENTERPRISE_ADMIN_WORKFLOW_TERMINATION_FAILED` | Service 190–196 / 330–341；XML 121–129 / 133–147；ServiceTest 47–85 |
| REJECTED：`markRejected` 申请 `INVALID`，盒 `("REJECTED", 0, null, null, decisionVersion)`，不发布 | Service 197–199 / 364–371；XML 162–168；E2E 166–176 |
| APPROVED：`resumeWaiting` 拨回 WAITING → `publishApproved`（只认 WAITING）→ `snapshotImmutable` → `markApproved`（SQL 要求已是 FINISH） | Service 201–209；XML 149–160；ApplicationService 390–395 |
| 发布看见账号已有企业有效钉子 → `ENTERPRISE_ACCOUNT_ALREADY_RESPONSIBLE`；执照已有负责人 → `ENTERPRISE_RESPONSIBLE_ALREADY_BOUND`；**没有**个人那种 `rebindIntent='Y'` 拒绝 | ApplicationService 441–451 |
| 直建材料 `@Size(max = 10)`；11 条 Bean 校验先炸；先 SOURCE attach，`validateRequired(..., ALWAYS)` 尺子是法人证件类型码，再冻 VERSION | CreateBo 16；BeanValidation 82–84；Service 223–235 |
| 直建撞活信用代码唯一键 → `ENTERPRISE_ADMIN_IDENTITY_CONFLICT`；insert 档案 `previous_profile_id=null`，不写生成列 | Service 375–388；XML 178–189；实体 51–52 `FieldStrategy.NEVER`；DDL 925–937 |
| 身份键是规范化信用代码；正则打在 `legalDocumentNumber`（E2E 用 `CN_RESIDENT_ID`），不是套到信用代码上 | Service 738–763；E2E 85–86 / 196–199 |
| `revise` 锁 **ACTIVE**（`requireWritable` / XML `status='ACTIVE'`），不是「已退回申请」；版本冲突 `ENTERPRISE_PROFILE_VERSION_CONFLICT` | Service 241–255 / 422–436 / 665–671；XML 228–240；接口注释仍写「已退回」 |
| 绑定三动作 SUSPEND/RESUME/UNBIND；错动作 `ENTERPRISE_BINDING_ACTION_INVALID`；错状态 `ENTERPRISE_BINDING_STATE_CONFLICT`；尺是 `binding_version` | Service 42 / 260–267 / 471–500 |
| `assign` 先 `requireEligibleUser`（启用 + 无企业有效钉子 + **有个人档案**），两端都没有有效钉子，否则 `ENTERPRISE_BINDING_TARGET_INELIGIBLE`；新行 `binding_version=1`，不是把 UNBOUND 改回去 | Service 273–279 / 504–516 / 727–734；XML 243–248；E2E 134–141 |
| `revoke` 改 `status='REVOKED'` 且 `del_flag` 仍 `'0'`；有钉子则 UNBOUND；之后 `ENTERPRISE_PROFILE_REVOKED_READ_ONLY` | XML 281–286；Service 521–541 / 665–671；E2E 153–164 |
| 有效钉子 SQL / 生成列都是 ACTIVE **或** SUSPENDED，没有枚举值 `EFFECTIVE` | XML 86–96；DDL 1110–1128；ArchitectureTest 237–239 |
| `EnterpriseAdminResultVo.version`：decide=decisionVersion；create=1；revise/revoke=档案 version±1；manageBinding/assign=binding_version | Service 199–209 / 414 / 465–466 / 499 / 513 / 540–541 |
| Handler 只捕 `EnterpriseAdminException` → `R.fail(getMessage())`；不改 HTTP 状态码 | Handler 10–16 |
| 菜单五串 query/review/material/manage/override 与 DML 210050…0020 / 0022–0025 文案一致（0021 是 apply，自助门） | DML 1513–1518 |
| admin/self 源码不得 `@SaIgnore`；anonymous 文件名必须带 Anonymous | ArchitectureTest 162–172 |
| 五层 import 方向：Controller→usecase 禁 service；UseCase 禁 dao/mapper；Service 禁 mapper/`IService`；DAO `@Repository` 只包 Mapper | ArchitectureTest 74–113；Dao 19–23 |
| E2E 生命线：create（合格负责人）→ revise 两页 OVERRIDE→SUSPEND/RESUME/UNBIND/assign 事件 5→去掉个人档案后再 assign 失败回滚→GET 修订名+出门条→revoke 默认 page 0 / REVOKED page 1→驳回 INVALID:1:ADMIN_OVERRIDE→terminate 失败仍 WAITING:0 | E2E 78–188 |
| 夹具 `EnterpriseAdminServiceImpl` 在 `src/test`：`extends EnterpriseAdminService implements EnterpriseAdminUseCase`，`new EnterpriseAdminDao(mapper)`，无操作者重载改走 `LoginHelper` | 夹具 19–44；E2E 98–100 / 143 `new EnterpriseAdminController(service)` |

**邻格仍能复现（不计入本行闭合）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 自助门 `@RequestMapping("/profile/enterprise/application")`，权限 apply | `EnterpriseApplicationController` 26–39；L-041 |
| 核身匿名回调 `@SaIgnore` | `EnterpriseVerificationAnonymousController`；L-041 |
| 转移门 `/profile/enterprise/transfer` | `EnterpriseTransferController` 26；L-042；(b) confirm |
| 材料总库 `/profile/enterprise/materials/{ownerType}/{ownerId}` | `EnterpriseMaterialAdminController`；L-043 |
| 个人柜台同构但表/权限前缀/候选人资格不同 | L-034；DDL 个人 binding 858+ |
| 前端 `createProfileService` / enterprise 页面 | L-044 |
| system 资料页 `SysProfileController` | L-022；classic `wta-system` |

**课写过、测试名单更窄或生产缝与测试缝不完全同一句话（不是证伪）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「网关是 null → `ENTERPRISE_ADMIN_WORKFLOW_UNAVAILABLE`，一行都不写」 | **Service 字段** null（兼容构造 `legacyWorkflow(null)`）确实如此，单测 105–113。生产 `@Autowired` 主构造注入的是 `SpringEnterpriseWorkflowGateway` Bean，字段几乎不会 null。内层 `WorkflowService` 缺失时网关抛 `EnterpriseApplicationException("ENTERPRISE_WORKFLOW_UNAVAILABLE")`，被 `decide` 的 catch 包成 **`ENTERPRISE_ADMIN_WORKFLOW_TERMINATION_FAILED`**（此时 `beginDecision` 已写，靠 `@DSTransactional` 回滚）。网关接口 default `terminate` 抛的是 `UnsupportedOperationException`，同一条 catch |
| `workflow.terminate(Long.toString(applicationId), reason)` | 生产 Service 用 `Long.toString`；网关再调 `terminateInstance`。语义同，不要把申请侧 `String.valueOf` 并进管理端口试 |
| 「关键字空 → 空列表」 | Service `isBlank` → `[]`。HTTP `@RequestParam String keyword` 默认必填，**缺参数是框架 400**，不是空列表 |
| 「没有 CURRENT 时退回版本列表第一页」 | `getFirst()` 成立；XML `order by version_no desc`，第一页是**号最大**的那页，不是最早插入 |
| 「事件来源 XML 写死 `ADMIN_OVERRIDE`」 | `insertBindingEvent` 259–265 对 **create 钉子** 也写这串，即使 binding 行 `source_type='ADMIN_CREATE'` |
| `IEnterpriseAdminService.revise`「修改已退回」/ `revoke`「撤销转移挑战或档案绑定」；UseCaseImpl.revoke「撤销企业档案账号绑定」；Service.revise/revoke 同行注释 | 接口/实现注释；磁盘锁 ACTIVE / 改 REVOKED。课迁移表已点 |
| E2E「绕过 UseCase」 | MockMvc 只打 GET detail/material；create/revise/binding/revoke/decide 直接打夹具 ServiceImpl |

问：在十二扇路径/动词/五颗权限、写窗 LoginHelper、UseCase 事务、有效钉子 ACTIVE|SUSPENDED、decide 先 terminate 再发布、revoke≠UNBIND≠转移、合格负责人先有个人档案、生产不注入 `IEnterpriseAdminService`、发布端口没有 `rebindIntent` 已经能互证的前提下，(a) 是否还要把「生产缺 WorkflowService 时失败码是 TERMINATION_FAILED 不是 UNAVAILABLE」「缺 keyword 是 400」写成开篇半句，才算柜台讲完？还是这些已经落在课的失败关闭/测试夹具边界里，Lead 可以直接盖章？

## Q3 假设与反例

- objective_id: OBJ-40
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无 IEnterpriseAdminService 生产实现；Controller 字段是 UseCase；GET 也有事务；无 EFFECTIVE；POST revoke 非 DELETE 非转移；eligibleUsers 非 query；manage 只一扇；detail 名牌≠出门条；decide 只 WAITING；无 rebindIntent；合格负责人要个人档案；夹具 ServiceImpl 非主链；SysProfile / PersonAdmin 是另一栋楼
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪层楼梯认错？

1. **「这是 `SysProfileController` / 账号资料页。」** 另一栋 classic 楼。课误区 1 / 反例 12。
2. **「这是 `PersonAdminController`，换个前缀就行。」** 表、权限前缀、候选人篱笆、发布端口都不是个人柜台。课开篇包边界 / 变式对照。
3. **「layered 所以 Controller 调 Service / `IEnterpriseAdminService`。」** 注入 UseCase；接口零实现。课误区 2 / 反例 13 / 迁移表。
4. **「字段名叫 service 就是 `EnterpriseAdminService`。」** 类型是 UseCase。课开篇包边界。
5. **「UseCase 只包写操作。」** page 也 `@DSTransactional`。课误区 3。
6. **「生效绑定=状态字 EFFECTIVE。」** SQL/生成列是 ACTIVE|SUSPENDED。课误区 4。
7. **「注销=逻辑删除 / HTTP DELETE / 只解绑 / 撤销转移挑战。」** `POST .../revoke` 改 REVOKED，`del_flag` 仍 `'0'`。课误区 5 / 反例 1–2；`IEnterpriseAdminService` 注释撒谎。
8. **「detail 能看材料就能下文件。」** list 名牌 ≠ access-url。课误区 6 / 正例 7。
9. **「审核材料=材料总库窗。」** 本课 URL 写死 SUBMISSION/VERSION。课误区 7。
10. **「通过=把申请 update 成 FINISH。」** 先停工作流，再发布端口造页。课误区 8 / 机制链 5。
11. **「候选人挂 query，因为只是查询。」** 合同测试钉 override。课误区 9 / 反例 9。
12. **「`EnterpriseAdminResultVo.version` 都是档案乐观锁。」** 三把尺。课误区 10。
13. **「给 manageBinding 传 ASSIGN / decide 传 RETURN。」** 集合只有三动作 / 两决定。课反例 3 / 5。
14. **「工作流挂了驳回已经落库。」** terminate 抛错整单回滚，E2E WAITING:0。课反例 6。
15. **「给一个没有个人档案的启用账号直建/指定。」** `ENTERPRISE_BINDING_TARGET_INELIGIBLE`；候选人名单也不会出现。课反例 7 / 误区 16。
16. **「企业 `publishApproved` 也要拒 `rebindIntent`。」** 企业没有这道篱笆；转移不走 WAITING 申请桌。课类比失效 6。
17. **「测试夹具 `EnterpriseAdminServiceImpl` 就是生产分层。」** 夹具持 Mapper 且无操作者重载走 LoginHelper。课反例 11 / 误区 15。
18. **「没有工作流 Bean 时 decide 降级直接改申请。」** Service 字段 null 直接 UNAVAILABLE。课误区 14。**半句精度：**生产网关 Bean 在、内层 WorkflowService 不在，走的是 terminate 失败码 + 回滚，不是降级；申请侧码 `ENTERPRISE_WORKFLOW_*` 不要并进管理端口试。
19. **「十二扇也包含自助 current/submit / 转移三枪 / 前端工厂，因为 OBJ 写了档案。」** 课负空间明确不认。把假设反转后，Lead 会用本行假闭合 L-041/L-042/L-044。
20. **「OBJ-40 没写吊销，所以没有 revoke / revoke 是转移课。」** 矩阵十二法含 revoke；课误区 17。不要另开转移行才盖本行。
21. **「reviewContext 和 Service.review 必须同名才算覆盖。」** HTTP/矩阵名是 `reviewContext`；里屋方法是 `review`。课已写。不要另开 Service 行才盖章。
22. **「把法人证件正则套到统一社会信用代码上。」** 信用代码是身份键。课反例 14。

问：读完后仍说不出的，是不是「类名」，而是 **本行不含自助/转移/材料总库/前端、eligibleUsers 不是 query、manage 只一扇、吊销≠解绑≠转移、decide 先掐机器、盒子里的 version 要问哪把尺、合格负责人先有个人档案、生产主链不叫 `IEnterpriseAdminService`、OBJ 三个词仍要把 revoke 算进十二扇**？

## Q4 视角

- objective_id: OBJ-40
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-40 vs L-003/L-022/L-034/L-041/L-042/L-043/L-044；IEnterpriseAdminService；MySQL 生成列；API-005
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:EnterpriseAdminController.*`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 十二名 1:1 vs OBJ 三个词（少吊销）；(b) 无 decide 性状行，相关是 transfer confirm；(d) 无企业申请流 | **是** 闭合定义。十二扇行可单独 covered。不要发明企业 (d)。(b) confirm **不要**因本课假闭合 |
| 柜台作者 | 十二映射、五权限、写窗 LoginHelper、Handler 类别码 | **是** (a) 逐方法 |
| UseCase / 事务 | 十二 `@DSTransactional`；无操作者 default 抛错 | **是** 解释到副作用。不要发明 `EnterpriseAdminUseCaseImpl` 行 |
| Service 状态机 | decide 栅栏、直建/覆盖、绑定三动作、吊销、合格负责人 | **是** (a) explained。密文/工作流引擎内部 **否** |
| DAO/XML | FOR UPDATE、有效钉子集合、生成列不手写 | **是** 走查。格子仍是 Controller。(c) dao-mapper-ownership **否**（L-083） |
| 发布端口 / 网关 | publishApproved 只认 WAITING 且无 rebindIntent；terminate 适配；两套失败码 | URL/失败关闭 **是** 边界。申请 Mapper / 流程引擎 **否**（L-041 / L-070） |
| 个人档案端口 | `ProfileService.findByUserId(s)`；候选人/指定要 `person()!=null` | **是** 本行篱笆。person 实现/表 **否**（L-034） |
| 自助申请（L-041） | current/save/submit/probe 如何把申请推进 WAITING；匿名 callback | **否** |
| 转移（L-042） | send/confirm/unbind；(b) confirm 改归属 | **否**。本课只保证 revoke 不是撤挑战 |
| 材料总库（L-043） | attach/detach | **否**。本课只有两张出门条 + 直建 SOURCE attach 循环 |
| 前端（L-044） | enterprise 页面 / `profileService` | **否**。门牌对照一句即可 |
| 个人柜台（L-034） | 同构十二扇、换绑意图 | 篱笆一句 **是**。十二扇个人窗 **否**（已 covered） |
| 资料页（L-022） | 昵称/改密 | **否** |
| API-005 | 查询 GET、变更 POST；本切片已无 PUT/DELETE | 盘点现状 **是**。不发动传输 Ratchet **否** |
| MySQL 8.4 / Redis 8 | 生成列唯一键 / 会话 | 生成列语义 **是** 解释。容器 covered **否** |
| 夹具 ServiceImpl | E2E 抄近路 | **否** 主箭头。课已标抽屉家具 |

问：Lead 若凭「十二个方法名都在函数表里」把 `EnterpriseAdminController.*` 标 covered，是否还要求读者能指着 **eligibleUsers=override、manage 只一扇、吊销≠解绑≠转移、decide 先 terminate、盒子 version 三把尺、合格负责人先有个人档案、生产不走 IEnterpriseAdminService、OBJ 少写吊销仍含 revoke**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要发明企业申请 (d)？

## Q5 元问题

- objective_id: OBJ-40
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 十二扇独行、无本课 (b)、无企业 (d)
- difficulty: low

建议：`defer`

原因：

- **`A:EnterpriseAdminController.*` 与矩阵 (a) 该行 1:1。** L 合同章节齐全（宏观地图、十二窗表、精确定义/English、机制链 1–6、带 caption / 文字等价物 / 图边界的两张图、正反例、变式、迁移、误区、暂停走查、来源表 S-004/S-008/S-010/S-L003-01/S-L034-01/S-L040-01…08）。**主叙事站得住且与源码对齐**：一块门牌十二扇；GET/POST；五颗权限；写窗 LoginHelper；Controller→UseCase→Service→DAO→XML；有效钉子 ACTIVE|SUSPENDED；decide 先掐工作流再发布或 INVALID；revoke 改 REVOKED 不 del；合格负责人先有个人档案；发布端口无 `rebindIntent`；生产不注入 `IEnterpriseAdminService`。不必为「类名」再挖五问。OBJ-40 三个词少写吊销，课误区 17 已把 revoke 钉回十二扇，不是矩阵空洞。
- 本批可粘贴的精度项已经落在课的表/反例/迁移或测试夹具边界，不挡住把**本行**标 covered：（1）生产缺内层 `WorkflowService` 时失败码是 TERMINATION_FAILED + 回滚，UNAVAILABLE 是 Service 字段 null；（2）缺 `keyword` 查询参数是框架 400；（3）`insertBindingEvent` 来源写死 ADMIN_OVERRIDE 含直建钉子；（4）无 CURRENT 时 `getFirst()` 是 version_no 最大页；（5）E2E 写路径走夹具不是 UseCaseImpl；（6）申请侧 `ENTERPRISE_WORKFLOW_*` 与管理端 `ENTERPRISE_ADMIN_WORKFLOW_*` 不要并成一句。不要把自助门、转移 (b)、材料总库、前端工厂、个人柜台、MySQL 容器塞进十二扇格。
- 不建议 `mine-more`：b01 未证明本行还有真实矩阵空洞；再五问只会把自助如何推进 WAITING、转移 confirm、或工作流引擎 terminateInstance 再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；半句生产/测试缝不是第二容器。不建议 `split-lesson`：未满 10 问；十二扇是 chain 原设计的同一 OBJ-40、同一管理端柜台，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：decide 失败关闭、绑定状态机、吊销只读是 (a) 的解释到副作用（OBJ 档是解释+走查）。不建议发明企业申请 (d)：矩阵没有这条；person 那条已 covered。
- 自助 current/save/submit/probe、核身回调、转移三枪、材料 Admin/Self、enterprise 前端：课内只作负空间。这是 Lead 等邻课 mined 再标那些行，不是 miner 再问，也不是把邻行并进本行才能盖章。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:EnterpriseAdminController.page, eligibleUsers, detail, reviewContext, reviewMaterial, material, decide, create, revise, assign, manageBinding, revoke | 十二扇路径/动词/五权限、写窗 LoginHelper、layered UseCase、有效钉子 ACTIVE\|SUSPENDED、decide 先 terminate 再发布、revoke≠UNBIND≠转移、合格负责人先有个人档案、无 rebindIntent、ResultVo 三把尺、生产不走 IEnterpriseAdminService、OBJ 少写吊销仍含 revoke——均与源码对齐。残留精度：（1）生产内层 WorkflowService 缺失 → TERMINATION_FAILED + 回滚，不是字段 null 的 UNAVAILABLE；（2）缺 keyword → 框架 400；（3）binding_event 来源写死 ADMIN_OVERRIDE；（4）无 CURRENT 时第一页=version_no desc；（5）E2E 写路径走夹具；（6）申请侧/管理端两套 WORKFLOW 码。 | 可选半句：口试「没有工作流」要问是 Service 字段 null 还是网关里 `getIfAvailable()` 空；缺候选人关键字先问 HTTP 必填。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-040 来源表 S-L040-01…08）。 | defer（本行可 covered；精度项不挡盖章） |
| A:EnterpriseApplicationController.current, save, submit, probe | 自助门未讲；WAITING 从哪来未认。 | 保持。L-041。不要并进十二扇才能盖章。 | defer（L-041） |
| A:EnterpriseVerificationAnonymousController.callback | admin 无 SaIgnore；回调是另一扇。 | 保持。L-041。 | defer（L-041） |
| A:EnterpriseTransferController.send, confirm, unbind | 本课只保证 revoke 不是撤挑战。 | 保持。L-042。 | defer（L-042） |
| B:EnterpriseTransferController.confirm | 改企业归属；矩阵独行。 | 保持。L-042。不要因本课假闭合。 | defer（L-042） |
| A:EnterpriseMaterialAdminController.list, accessUrl / A:EnterpriseMaterialSelfController.attach, detach | 本课只有两张 archive 出门条 + 直建 SOURCE 循环。 | 保持。L-043。 | defer（L-043） |
| A:createProfileService / A:createProfileWebDomain enterprise | 门牌对照一句。 | 保持。L-044。 | defer（L-044） |
| A:PersonAdminController.* | 同构篱笆，表名/合格负责人不同。 | 对照一句即可。 | defer（L-034 已 covered） |
| A:SysProfileController.profile, updatePwd | 资料页已 covered。 | 对照一句即可。 | defer（L-022 已 covered） |
| IEnterpriseAdminService / 测试夹具 EnterpriseAdminServiceImpl | 矩阵无此符号。课已标抽屉家具。 | 不要为本课发明 (a)/(b) 行。 | defer（未入库） |
| (b) decide 失败关闭 / 绑定状态机 / 吊销只读 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 企业 submit → admin decide | 矩阵无此行。person 那条已 covered。 | 不要发明企业申请数据流行。 | defer（未入库） |
| C:MySQL 8.4 / dao-mapper-ownership | 生成列语义已借用。容器/所有权未认。 | 不要因本课标容器 covered。 | defer（(c) / L-085 / L-083） |
| API-005 变更 POST | 本切片已全 GET/POST。 | 本课盘点，不发动 Ratchet。 | defer（传输 Ratchet） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本行无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出柜台目的（管理端十二扇管一本企业档案）、主路径（名册/详情 GET、直建 POST、decide 先停工作流再发布或驳回）与失败路径（无网关不写、terminate 回滚、REVOKED 只读、绑定状态冲突、无个人档案不可指定）；邻课缺的是别的矩阵行不是讲义；把可选半句写进原课不会超过 40 分钟。满 10 问前不拆。十二扇虽穿过 UseCase/Service/DAO/XML 与三个端口加个人档案查找，仍是 chain 原设计的同一 OBJ-40，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-041 自助/核身、L-042 转移与 (b) confirm、L-043 材料、L-044 前端、L-034 个人柜台、L-022 资料页、未入库 IEnterpriseAdminService/(b)/企业 (d)、MySQL/DAO 容器以及 API-005 显式建议 `defer`；对 `A:EnterpriseAdminController.*` 建议 Lead 标 `covered`（证据锚本 probe 与 L-040 来源表）。本课不主张改 Java 窗、自助/转移 Controller 或任何 (b)(c)(d) 格子。
