# T-15 evidence

Current HEAD `b9c482a4` already meets AC-015. No new implementation was required. This cancels only the repeated construction. AC-015 stays owned by T-30. There is no separate browser interaction for this ticket.

## Commands

```bash
node .agents/skills/namewta-fullstack-development/scripts/validate-module-mode.mjs backend/wta-modules/wta-profile/wta-profile-person --mode layered
node .agents/skills/namewta-fullstack-development/scripts/validate-module-mode.mjs backend/wta-modules/wta-profile/wta-profile-enterprise --mode layered
```

Both exited 0. Person: 165 Java files. Enterprise: 127 Java files.

```bash
cd backend && ./mvnw -pl wta-modules/wta-profile,wta-admin -am test
```

Exit 0. Profile classes: tests=185, failures=0, errors=0, skipped=0. `wta-profile`, `wta-profile-person`, and `wta-profile-enterprise` each reported SUCCESS. The same reactor also executed the dependency suite: tests=1193, failures=0, errors=0, skipped=249. Those 249 skips are service-gated integration tests and are not counted as passes.

`ProfileMaterialPort` has no default methods. `detach`, `list`, `accessUrl`, `validateRequired`, and `snapshotImmutable` all require `MaterialOwnerKey`, and that key rejects a missing or non-positive `ownerId` in its constructor. A search of profile production Java found no `@Deprecated` bridge. The only `UnsupportedOperationException` hits are test doubles in the provider registry tests.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9` and namewta-fullstack-development `675c053c11d8b22cd394c875f48688242d8e8328dd14e69657f5d5b12f2af68b`, verify phase. No production source edit.
