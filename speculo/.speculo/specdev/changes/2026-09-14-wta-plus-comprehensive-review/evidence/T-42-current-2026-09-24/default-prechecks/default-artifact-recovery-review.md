# T42 default-gate class artifact recovery — read-only review

Fixed failed source: clean `3efc5862e1bfb86ee23e566d1eb05aa1dda24e94`, tree `55b6932cd611e4f6b7e541114be0fa51edac4044`. Reviewed `/tmp/wta-t42/default-a3-failure/source-and-failure.json`, the retained `.class`/`javap.txt`/Surefire dump, `/tmp/wta-t42/lead-retrospective-default-clean-recovery.md`, and the proposed private clean→default→full script. No build, service, process action or repository write was performed here.

**Verdict: the old default attempt failed; clean-output recovery is justified and retains the full acceptance bar.** The failed `./mvnw test` exited 1 after `common-openapi` JUnit discovery raised `ClassFormatError: Duplicate method name "create" ... LLoginUser;`. Ten fresh prefix suites held 31 tests (0 failures/errors, 2 environment skips), but discovery stopped the reactor; those counts are not a complete default pass. Full package had not run at that point. The retained class SHA-256 `1b90744e21680177a78874a93dd9f41aa81425ba8d95346d1de86f41a389167b` and mtime precede the default attempt; the class and raw failure are preserved rather than overwritten as a success record.

The saved binary is an **ECJ/JDT-style unresolved-compilation problem stub**, not a valid representation of the fixed Java source. `javap -p -s` shows `create` and `find` twice with the same JVM descriptors and a default-package `LLoginUser;` instead of the imported `org.namewta.system.api.model.LoginUser`. `javap -verbose -c` shows `Unresolved compilation problems: LoginUser cannot be resolved...` literals and affected methods whose body constructs and throws `java.lang.Error`. Fixed source has only one `create` and one `find`, imports the real LoginUser, and `git diff 5417c257..3efc5862 -- backend/wta-common/wta-common-openapi` has no changes. These facts support an invalid pre-existing build artifact. They do **not** prove which process wrote it; the observed JDT process is not attributed as the writer.

The recovery plan first retains the failed class/dump and then runs normal `./mvnw -B -ntp clean` at the same exact clean HEAD before a complete `./mvnw test`, followed by the usual clean full-package and full-bundle verification. The private `prepare-source-backend-clean-v1.py` checks exact HEAD/tree/cleanliness around each command, records exit codes/logs, retains fresh XML and a full-JAR proof; it does not exclude `common-openapi`, skip tests in the default gate, change source/compiler/profile, or relax any assertion. The later full package's `-DskipTests` is paired with, and cannot replace, the complete default test run. A successful clean alone would not establish acceptance: the new default must finish exit 0 with fresh counts/skip disclosure, then full package/bundle/source checks must pass. If the same class reappears malformed, investigate output ownership/concurrent builders rather than repeating or accepting the stale prefix.

At this review time, Lead's recovery batch session 1876 was still running. No result from that batch is claimed here. Mail27 and the separate 135-case regression success remain distinct fixed-source evidence, not a substitute for default/full gates.
