# T39 development candidate handoff (writer lock returned)

Base/test-only checkpoint `162d405642d88825f59b23bb9a61d4063ac2aaa4`; current product edits uncommitted. Governance Ticket/dispatch/Evidence dirty in same tree belongs to Lead and was not edited by this writer. `git diff --check` exit 0. No Docker, external supplier, private credential read, commit, or E2E run by writer.

## Behavior

- Submit uses ceiling scheduled second and floor expiry second; checks DB time before preparation and again immediately before first Intent insert. Duplicate receipt and retry use persisted original expiry and reject at/after deadline. External new Outbox receives internal `DEADLINE_UNSENT_READY` only on initial write; Captcha SMS/MAIL each calculate one absolute deadline, use random UUID request key per new code, and cache via new `RedisUtils.setCacheObjectUntil` single-key same-codec/NameMapper Redis TIME+SET PXAT script. Cache failure/expiry returns failure. Existing relative cache API unchanged.
- Claim captures nonpersistent pre-claim READY/PROCESSING. Dispatch copies this value only after owner/token match, and result port rechecks DB deadline with Intent→Outbox→Delivery locks and live lease. External reclaimed PROCESSING and null provenance conservatively become UNKNOWN/WAITING before route WAIT or Provider. At expiry only new marker + READY origin + zero Outbox/Delivery attempts + no provider/acceptance fact gives FAILED/NOTIFICATION_EXPIRED + DONE. Historical/no-proof external tasks become UNKNOWN/WAITING. IN_APP checks same-Intent message/recipient: unsent expires without budget/message/push, existing relation recovers DELIVERED, orphan facts terminally fail locally. Provider response after entry is completed normally even if expiry passes. Cancelled Intent stays CANCELLED.
- Rollout condition: stop old Workers before creating new marker-bearing tasks; old code could return crashed PROCESSING to READY via route WAIT, so legacy READY/0 alone is insufficient proof. No schema/Mapper modification.

## Exact selected development commands and facts

cwd `/srv/WTA-plus/backend`; runner `python3 /tmp/wta-check.py /tmp/wta-t39 <name> /srv/WTA-plus/backend ./mvnw -Pdev -pl wta-admin,wta-common/wta-common-redis -am -Dtest=CaptchaNotifyCallerUnitTest,DispatchNotificationServiceTest,NotificationDeadlineRuntimeTest,NotifyDeadlineIntegrationTest,RedisUtilsDeadlineIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false test` (v1/v2 omitted NotificationDeadlineRuntimeTest because not yet added). v1 exit 1 from two existing mock-fixture gate defaults, raw `/tmp/wta-t39/dev-selected-v1.log`; fixed by explicitly stubbing gate true. v2 exit 0; v3 exit 0; final pre-comment/docs v4 exit 0, `/tmp/wta-t39/dev-selected-v4.log`: 1 runtime + 23 dispatch + 9 Captcha = 33 actually executed, 0 failures/errors; real opt-in classes 15 MySQL and 2 Redis skipped because no owned services. These 17 skips are not acceptance. Last changes after v4 were Javadoc and markdown table placement only; `git diff --check` still exit 0.

## Test mapping

- Behavior red already recorded by Lead: Captcha existing-API SMS/MAIL expiresAt null, 4 tests / 2 fail / 0 error / 0 skip, test-only source 162d405.
- Clock <, ==, > deadline and provider-before-expiry: `DispatchNotificationServiceTest.exactDatabaseDeadlineBlocksProviderButEarlierTimeStillAllowsIt`.
- Slow submit preflight: `NotificationDeadlineRuntimeTest.slowPreparationCannotInsertAlreadyExpiredIntent`.
- Captcha shared deadline, distinct same-minute code key, post-submit >deadline, Redis failure: `CaptchaNotifyCallerUnitTest` 9 methods.
- Real MySQL: `NotifyDeadlineIntegrationTest` 15 methods cover first READY expired closure/provider zero/attempt zero, historical READY/no-proof UNKNOWN, PROCESSING reclaim UNKNOWN before expiry, future availableAt, IN_APP existing relation/empty/orphan, WAIT recheck, stale owner fence, schedule ceil/expiry floor, duplicate old expiry, provider ACCEPTED after deadline, late aggregate-trigger rollback, retry reject/zero wake, cancelled terminal. Trigger is on final Intent update after Delivery/Outbox writes, and asserts whole transaction rollback. Expected opt-in `-Dnotify.deadline.integration=true` / env `T39_MYSQL_PASSWORD` plus T36 fixture env and existing MySQL/Redis properties.
- Real Redis: `RedisUtilsDeadlineIntegrationTest` 2 methods verify production CompositeCodec+NameMapper, existing read path, exact PEXPIRETIME and old value/expiry unchanged when a late write is rejected. Expected `-Dnotify.deadline.redis.integration=true` and owned Redis port; one stable class-level Redisson context, dedicated fork required due static final RedisUtils client.
- T35 SMS fixture helper now explicitly labels its manually seeded initial READY claim so old real sender regression tests remain meaningful. T36 Atomic, T37 SMS/idempotency, T38 ManualRetry, Enterprise7 and Wake1 remain for Lead eight-class real driver. No real integration result is claimed yet.

## Scope/skills

Only authorized product/test paths: admin Captcha controller and notify test root; common RedisUtils and its exact test class; notify runtime/port/usecase/entity and notify test root; `engineering-standards/references/notification.md`, `wta-common-modules-guide/references/other-utils.md`. No wta-api signature or schema change. Applied engineering standards (lock order/fence/SQL), fullstack/module route, common module utility ownership, Java API compatibility (additive public static Redis method with original overload unchanged). Remaining: Lead fixes candidate SHA, runs real 8-class owned environment with positive class counts/zero skips, then affected full/core/module/layered checks and independent functional/security review. Historical no-expiry OTP disposition is read-only Lead Evidence; no real data migration.
