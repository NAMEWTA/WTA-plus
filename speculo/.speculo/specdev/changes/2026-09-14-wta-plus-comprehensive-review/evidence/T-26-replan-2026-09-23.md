# T-26 evidence

Current HEAD `7447af6f` already meets AC-026. No new implementation was required. This cancels only the repeated construction. AC-026 stays owned by T-30. The adjacent OIDC change was not modified.

## Real HTTP, MySQL, and Redis

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=CrudMySqlHttpIntegrationTest,CrudHttpDispatchIntegrationTest,CrudHttpMappingContractTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Dcrud.mysql.integration=true \
  -Dcrud.mysql.integration.url='jdbc:mysql://127.0.0.1:33136/namewta_crud_test_t26?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai' \
  -Dcrud.redis.integration.port=33137
```

Exit 0. Isolated MySQL `mysql:8.4.9` (`sha256:c36050afdca850f23cef85703f84c7531a5ae155a11b5ee1c60acb09937c4084`) on `127.0.0.1:33136`, database `namewta_crud_test_t26`, loaded `10-cde-base-ddl.sql` through `50-cde-base-dml.sql`. Redis `redis:8.6.3` on `127.0.0.1:33137`. The password is the existing test default and is not copied here. Both containers were removed after the run.

Combined tests=4, failures=0, errors=0, skipped=0. `CrudMySqlHttpIntegrationTest` is 1 test, 6.010s. It drives Jetty, security, validation, repeat-submit, the real Controller/Service/MyBatis path, MySQL, and Redis. For `/system/post`, `/workflow/spel`, `/demo/demo`, and `/demo/tree` it creates two rows, reads one, rejects `PUT` with 405, rejects an unauthorized `POST /update` with 403, rejects an invalid update with 500 without changing the row, then updates through `POST /update` and bulk-deletes through `POST`. `DELETE` is 405. `/demo/tree` also rejects a missing parent, a cycle, and a delete of a parent that still has a child, then accepts a legal move. `/system/dept` rejects a cycle, rewrites the grandchild ancestors after a legal move to the root, and deletes only through `POST`. `CrudHttpDispatchIntegrationTest` (1) rejects the legacy methods and keeps permission, client, and query binding. `CrudHttpMappingContractTest` (2) checks that first-party CRUD has no legacy methods, every POST has audit metadata, and Spring registers the surface without duplicate mappings.

## Contracts

```bash
cd frontend && pnpm --filter @namewta/tooling-openapi openapi:check
node docs/fm/scripts/validate.mjs
```

Both exit 0. OpenAPI contract is current: 906222 bytes, sha256 `5252f88227027d028658b378fab60ba3f26483fe8ac8d59c67c00a0654f4987a`. The FreeMarker validator checked 32 templates. Frontend architecture, typecheck, test, and production build were run on the same product sources at `04aba028` (103 files, 663 tests, 0 skipped). `7447af6f` only records that result and does not change those sources. The full default `./mvnw test` was not used as this proof because its other property-gated suites are outside AC-026 and would be skipped.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase, operation `verify-affected-contract-and-quality-gates`, status passed. No production source edit.
