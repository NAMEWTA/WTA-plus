# T45 C3 fixed-source contract review — static checkpoint; acceptance pending

Fixed base `4c93a2fb9c7d2c1d68a0c8194197e2af1908b4d2`, candidate `d98936b6881ad7c8093476a65fe9736203c60af7` (tree `b946b39ccb0bb5a242a390d9cbdc30ab37eb0323`). Read-only review of the 25 product paths in `/tmp/wta-t45/c3-write-scope-and-ancestry.json`, Ticket 45/AC-045, and the previously fixed 8dc/11c/081/d1fd source reviews. Worktree was clean. No repository edit, build, test, or service was performed by this reviewer.

**Static verdict: no remaining implementation blocker identified in C3. This is not yet overall acceptance.** The only C3 application-source delta after generated contracts is `OssConfigPage.vue:329–354` plus `OssConfigPage.test.ts:53,175–186`. `handleDiagnose` now accepts the actual table row shape `Partial<OssConfigVO>`, captures its ID once, and refuses absent/empty IDs before opening the dialog or issuing a POST. It retains the existing permission, loaded-identity, user, session-generation, local-version, close/unmount, and `AbortSignal` fences; the new test requires zero requests and no visible dialog for missing/empty ID. No cast or global type relaxation was added. Whether this resolves the original full Admin typecheck failure must be established by the C3 frontend run now in progress.

The earlier high-priority same-user re-login finding is closed in fixed 081 source: `SystemWebRuntime.sessionSnapshot` exposes only `{generation,identityLoaded}`; Admin projects those from the existing user store; the page compares generation/user/permission/version in success, error and finally and closes on session change. The d1fd URL correction is present unchanged: `AbstractOssClientImpl:1285–1298` uses a bucket-bound custom domain for anonymous object probes, retains endpoint-bucket fallback, rejects URI userinfo/query/fragment/missing host and encodes the object key once. Existing loopback tests assert exact HEAD/GET paths and invalid-domain zero requests. The A1 failed real run remains historical, not retroactively passing.

The rest of the 25-path product delta is unchanged from those reviews. `OssAccessDiagnostic` and the management VO expose immutable fixed-enum facts with subject/observation/source/scope/basis/time, without policy text, bucket/key, URL, credentials or provider errors. `OssStorageReadinessService` keeps a private entry+facts evaluation and registry revision fence; a stale result returns no facts. Policy/ACL 403 stays UNKNOWN, object HEAD/GET 403 is object-scoped DENIED, ambiguous 404/redirect/timeout/5xx remains UNKNOWN, and no anonymous write probe is introduced. Domain transport whitelists enum/time fields; the page labels provenance/scope and states the single-object limit. Existing controller POST, positive-ID, list-permission and safe-log contract is unchanged. README and System module facts describe the same bounded semantics. This implementation does not assert that PRIVATE unknown proves bucket-wide safety or anonymous write denial.

The formal OpenAPI revision in C3 points to revision `0c1c94bf47083a6b7a3620c9494ee23bd629321ac7d8c3d7963f8c840343a477`, whose provenance binds raw SHA256 `6f62122a5dcd959ee32adc2293c4b48785b1664fba748dcda59ef9ab99152a4c` to the accepted A2 HTTP capture at backend `d1fd57f`. `/tmp/wta-t45/a2-openapi-delta.json` records zero path additions/removals/changes and only new `Fact` plus changed `OssStorageDiagnosticVo` schemas. The capture retained the default SSE routes, including `/resource/message` and `/resource/message/close`. This is a generated-contract source check, not a claim that the final browser UI has passed.

Evidence available at this checkpoint: A2 restricted-MinIO/HTTP run `/tmp/wta-t45/runs/6e5d5c24c38c793d/result.json` says accepted=true, cleanup errors empty, 18 sensitive-canary categories zero hits, exactly five read-only diagnostic provider requests per config, Policy/ACL403 UNKNOWN for all three configs, PUBLIC_READ anonymous HEAD200/RangeGET206, and both PRIVATE HEAD/GET403. A2 backend default records 262 fresh suites / 1,137 tests / 0 failures / 0 errors / 216 environment skips; its full package and formal OpenAPI generation exited 0. `/tmp/wta-t45/c3-backend-evidence-reuse.json` proves backend, release-artifacts, `.agents`, and scripts trees are byte-identical between the A2 backend source and C3, so those backend-input results may be attributed as reused evidence, **not** as tests rerun at C3. `c3-write-scope-and-ancestry.json` records all 25 product paths within registered roots and the base as an ancestor.

**Still required before final verdict:** C3 complete frontend gates, a real Admin page/permissions/session browser run on a C3 full JAR, the two fresh zero-skip MinIO JUnit consumers (`OssStorageReadinessMinioIntegrationTest`, `OssAccessUrlMinioIntegrationTest`), C3 package/core/static and clean/owned-resource proof as required by Lead's acceptance plan. The previous A2 frontend typecheck failure remains a retained failed attempt until C3's actual output closes it. No live provider timeout/cancellation guarantee is inferred from the static source; the bounded per-step contract and cancellation unit assertions have been reviewed, and any stronger claim needs its own real evidence.
