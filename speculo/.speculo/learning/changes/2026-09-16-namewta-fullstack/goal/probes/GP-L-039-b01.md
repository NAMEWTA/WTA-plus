---
artifact: learning-goal-probe
probe_id: GP-L-039-b01
lesson_id: L-039
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-039 / b01

审问对象是已写成的 Lesson `lessons/L-039-profile-person-frontend.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止 `Q-question 问答目录`、行首作答栏、行首提交栏、`verdict`。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列两格：`A:createProfileService`、`A:createProfileWebDomain person/self`。OBJ-39 原文：能说明 `createProfileService` 与 web-domain **person / self / material-tag**。档是**解释+对照**。矩阵 (a) **厨房独行** `createProfileService`（模块 `domains/profile`）仍 uncovered。矩阵 (a) **九工厂同行**把 `createProfileWebDomain` 与 `createProfileSelfWebDomain` 和另外七个 `create*WebDomain` 写在同一行（仍 uncovered，备注「manifest 工厂」）；**没有** `person/self` 切片行，也没有 Vue 页符号。矩阵 (b) **没有** runtime 校验 / 隐私投影 / 权限目录行。矩阵 (c) `admin-web` / `home-web` 容器与 `frontend-direction` 已 covered（L-004 / L-007 / L-008）。矩阵 (d) **没有** 浏览器档案流。本批按派单审问这两格 (a)；口试按磁盘：厨房是一座工厂，菜单是两份 id，OBJ 点名的三块页面 owner 是对照走查，不把九工厂行整行闭合，也不把 L-044 企业三页算进本切片。`PersonAdminController` 十二扇留给 L-034；`PersonApplicationController` current/submit 留给 L-035；换绑五枪留给 L-036；材料 Admin/Self 与标签 HTTP 留给 L-037；匿名回调留给 L-038；企业 archive/transfer 页面留给 L-044；厅堂插头留给 L-007 / L-008；九工厂行的 `createSystemWebDomain` 留给 L-020；system 资料页留给 L-022；直传控件留给 L-029。

抽查锚点（2026-09-16）：`frontend/packages/domains/profile/src/{service,service.test,permissions,index,transport,transport-support,transport.test}.ts`；`person/{archive,application,rebind,materials}/{service,index}.ts`；`person/rebind/transport.ts`；`material-tags/{service,index}.ts`；`package.json` `exports`；`README.md` 第 19 行；`frontend/packages/web-domains/profile/src/{index,index.test,runtime}.ts`；`src/{person,material-tag,self,enterprise}/registration.ts`；`src/self/{runtime,registration.test}.ts`；`PersonProfilePage.vue` / `PersonProfileDetailPanel.vue` / `PersonProfileReviewPage.vue` / `PersonVerificationPage.vue` / `ProfileCenterPage.vue` / `MaterialTagPage.vue`；`person/{logic,components.test}.ts`；`material-tag/{logic,logic.test}.ts`；`apps/admin-web/src/application/services.ts` 第 53 行；`apps/home-web/src/application/services.ts` 第 16 行；`adminManifestRegistry.ts` `adminProfileWebRuntime`；`homeManifestRegistry.ts` `selectedManifestIds`；`api-contracts/generated/openapi.ts` `/profile/**`。

## Q1 澄清

- objective_id: OBJ-39
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-39；chain L-039 覆盖列 `A:createProfileService A:createProfileWebDomain person/self`；L-039 `claimed_cells` 两格；矩阵 (a) 厨房独行仍 uncovered；九工厂同行含 `createProfileWebDomain` 与 `createProfileSelfWebDomain` 仍 uncovered、无 person/self 切片行；(b) 无本课符号；(c) 两厅堂已 covered；(d) 无浏览器档案流；课开篇「格子仍按 chain 写成 person/self；不把八个邻居标成 covered」+ 负空间 L-034…L-038 / L-044 / L-020 / L-022
- difficulty: medium

课把浏览器这一头收成「同一厨房、两份菜单」。计划里至少有五套可数对象，派单点名两格 (a)：

1. **OBJ-39 原文。** `course.md`：能说明 `createProfileService` 与 web-domain person/self/material-tag。一句里厨房 + **三块**页面 owner，没有把九个 `create*WebDomain`、企业三页、或 `Person*Controller` 写成口试清单。
2. **chain / 课 `claimed_cells`。** 两格。第二格符号写成 `createProfileWebDomain person/self`。磁盘上管理端工厂是 `createProfileWebDomain`（id `web-domain-profile`，七键含企业三键），用户端工厂是旁边的 `createProfileSelfWebDomain`（id `web-domain-profile-self`，三键）。chain **不**写 Vue 文件名，也 **不**写 `createProfileSelfWebDomain` 这个函数名。
3. **矩阵 (a) 两处。** 厨房 `createProfileService` 与磁盘 1:1，可单独盖章。九工厂行把本课两份菜单工厂和 `createSystemWebDomain` 等七个邻居绑死。Lead 若按**现印同行**标 covered，会把 L-020 / L-044 / 未写的 notify/third/workflow/demo/ai 菜单假闭合。
4. **课自己的操作定义。** 厨房只认 `HttpClient`，冻住三只抽屉，不裁 App、不读 Token。管理端工厂 fail-closed 校验 host 九口 + `fileUpload` + 标签五口 + 两边 archive **十一**口（**没有** `eligibleUsers`）。self 工厂 `permissions: []`，校验只拒缺 runtime。本课认墙上的 material-tag + person 三页 + self 中心/个人认证；企业三页与企业认证表单字段留给 L-044，但管理端工厂**会一并注册**那三键。
5. **邻格。** L-007 / L-008 已认两厅堂各调一次同一厨房。L-020 已警告九工厂同行。L-034…L-037 是后端门牌。L-044 才认 `createProfileWebDomain enterprise`。

问：本两格闭合标准是「厨房独行可 covered + person/self/material-tag 切片讲完但不碰九工厂整行」，还是 OBJ-39 必须把三块 Vue owner 升成矩阵行、或必须先把九工厂行拆开才盖厨房章？若是前者，Lead 标 `createProfileService` covered 是否必须先给 `createProfileSelfWebDomain` 单独立行，而不是把七键/三键塞进 `createSystemWebDomain` 那一行？

## Q2 证据

- objective_id: OBJ-39
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: `service.ts` 55–90；`service.test.ts` 38–46 / 48–100 / 102–145 / 147–185 / 187–210；`person/archive/service.ts` 40–63；`person/application/service.ts` 11–18；`person/rebind/service.ts` 16–28；`person/materials/service.ts` 28–39；`material-tags/service.ts` 15–35；`permissions.ts` 1–22；根 `index.ts` 113–127；`package.json` 6–16；`README.md` 19；`transport.ts` 6–12；`person/rebind/transport.ts` 10–20；`transport.test.ts` 4–31；web-domain `index.ts` 16–33；`runtime.ts` 29–81；`index.test.ts` 26–73；`self/registration.ts` 8–21；`self/runtime.ts` 12–15；`self/registration.test.ts` 15–23；`person/registration.ts` 20–33；`material-tag/registration.ts` 23–29；`PersonProfilePage.vue` 153 / 173 / 197 / 215–248；`PersonProfileDetailPanel.vue` 156–223；`PersonProfileReviewPage.vue` 58–63 / 94–123；`PersonVerificationPage.vue` 75 / 96 / 106–114；`ProfileCenterPage.vue` 15 / 23；`MaterialTagPage.vue` 195 / 253–264 / 273 / 284；`person/logic.ts` 16–30；`person/components.test.ts` 7–13 / 26–32；`material-tag/logic.ts` 3–8；admin `services.ts` 53；home `services.ts` 16；`adminManifestRegistry.ts` 141–185 / 373；`homeManifestRegistry.ts` 22–39；generated `openapi.ts` `/profile/person/archive` 等
- difficulty: high

课的厨房机制链与两份菜单哪些能在工作树直接复现，哪些只是测试名单比课表窄、或邻格事实？

**仍能复现（本两格）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂 `createProfileService(http: HttpClient)`，只认 `http`，返回冻住的三只抽屉；无 `appId === 'home-web'` 分支 | `service.ts` 55–90 |
| 旧名 `person.application.probeRebind` **就是** `person.rebind.probe`（`===`）；企业 `sendTransfer === transfer.send` | `service.ts` 58–73；测试 38–46 |
| admin / home 各调一次同一工厂：`profileService = createProfileService(domainHttp)` | admin `services.ts` 53；home `services.ts` 16 |
| 九个 `exports` 子路径与 Controller 门牌对齐；**没有** `@namewta/domain-profile-person` | `package.json` 6–16 |
| 资源标签 `controller: 'PersonMaterialController'` 只是字符串；仓库无该类 | `person/materials/index.ts` 4–6 |
| archive 十二枪：GET 名册/eligible-users/详情/review-context/出门条；POST admin-create / decision / revision / assign / binding / revoke；`segment = encodeURIComponent` | `person/archive/service.ts` 38–63 |
| `'app/1'` → `/profile/person/archive/application/app%2F1/review-context`；`'tag/1'` → `/profile/material-tags/tag%2F1` | 测试 172 / 129 |
| application 三枪：GET 根、POST 根、POST `/submit` `{ expectedVersion }` | `person/application/service.ts` 14–17 |
| rebind 五枪全 POST `/profile/person/rebind/...`；probe/match 走 `http.request<unknown>` 再投影 | `person/rebind/service.ts` 19–27 |
| materials 四枪：GET list / POST attach / POST `.../detach` / GET access-url | `person/materials/service.ts` 33–38 |
| 标签五枪：GET tree（`includeDisabled` 默认 false）；其余 POST | `material-tags/service.ts` 18–34 |
| 读 GET 写 POST；测试锁死方法不是 get/post 的枪不存在；URL 不含 `export` | 测试 183–184 |
| 403 原样 `rejects.toBe(forbidden)`，厨房不包一层 | 测试 207–209 |
| probe 只许 `{ status }`；多 `profileId` / `fullName` / `documentNumber` / `phone` 扔「Profile 响应不可用」。match 只许多 `maskedPhone` | `transport.test.ts` 13–30 |
| `profilePermissions` 标签两串 + person 六串 + enterprise 六串 | `permissions.ts`；测试 188–206 |
| `profileDomainModule.capabilities` 九项（含 `person-rebind`）；名片不是 componentKey | 根 `index.ts` 113–127 |
| 管理端工厂 id `web-domain-profile`；七键顺序 materialTag → person 三页 → enterprise 三页 | `index.ts` 22 / 测试 39–47 |
| `requireProfileWebRuntime`：host 九口 + `fileUpload` + 标签五口 + archive 十一口；**没有** `eligibleUsers` / `rebind` / `materials` / `application` | `runtime.ts` 29–81 |
| `createProfileWebDomain(undefined)` 与缺 `confirm` / `service: {}` 立刻 throw | 测试 26–35 |
| 空 `selectedManifestIds` 的 fixture `componentKeys()` 是 `[]` | 测试 65–72 |
| self 工厂 id `web-domain-profile-self`；三键 center / person.application / enterprise.application；`permissions: Object.freeze([])` | `self/registration.ts` 16–20；测试 18–23 |
| `requireProfileSelfWebRuntime` 只判断 `if (!runtime)`；测试可塞 `service: {} as ProfileService` | `self/runtime.ts` 12–15；`registration.test.ts` 9 |
| 管理端个人页：`archive.page` 空状态收成 `undefined`；新建 `findUsers('PERSON')` + `materialTags.tree('PERSON'|'COMMON')` + `fileUpload` + `archive.create({ identity, bindUserId, reason, materials })`；源码禁止 `materials: []` | `PersonProfilePage.vue` 173 / 197 / 214–248；`components.test.ts` 7–13 |
| 详情：`archive.detail` / `archive.material` + `downloadMaterial`；高风险 `confirm` + 原因后 `revise` / `assign` / `manageBinding` / `revoke`。`REVOKED` 四钮全关 | `PersonProfileDetailPanel.vue` 156–223；`logic.ts` 16–30 |
| 审核：`archive.review` / `reviewMaterial`；流程钮 `completeWorkflowTask({ variables: { profileDecision: 'APPROVE'\|'REJECT' } })`；覆盖才 `decide` 且 `APPROVE`→`APPROVED`。缺 `taskId` 只 warning | `PersonProfileReviewPage.vue` 54 / 94–123 |
| 自助：`current` → 保存 `save` → 提交先 save 再 `submit(version)`。无 attach、无 rebind | `PersonVerificationPage.vue` 75 / 96 / 106–114 |
| 中心页权限写死 `'profile:person:apply'` / `'profile:enterprise:apply'`，不 import `profilePermissions` | `ProfileCenterPage.vue` 15 / 23 |
| 标签页五枪；一层 CATEGORY 可挂 CATEGORY 或 TAG；失败 `executeMaterialCommand` 不 reload | `MaterialTagPage.vue` 195 / 253–264；`logic.ts` 3–8 |
| 厅堂 `findUsers('PERSON')` → `person.archive.eligibleUsers`；`completeWorkflowTask` → `workflowService.completeTask`；`downloadMaterial` 造 `<a download>` | `adminManifestRegistry.ts` 145–182；registry 测试 161–178 |
| admin `selectedManifestIds` 含 `web-domain-profile`；home 含 `web-domain-profile-self`，登录成功送到 `` `${VITE_APP_CONTEXT_PATH}profile` `` | registry 366–377；home 22–39 |
| 全仓 Vue **零** `person.materials.*` / `person.rebind.*` 调用；只有厨房测试扣扳机 | domains `service.test.ts` 41–43 / 121–123；web-domains grep 空 |
| 生成 OpenAPI 已有 `/profile/person/archive`、`/profile/material-tags`、`/profile/person/application` 等 | `openapi.ts` 2092+ |

**课写过、测试名单更窄或邻课才钉死（不是证伪）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 口试表把动词写成 GET/POST | 厨房 `method: 'get'`/`'post'` 小写。语义对，字符串不是大写 |
| 「旧名与新名是同一只手」举 `probeRebind === probe` | 源码五个别名都挂上了；facade 测试只锁 probe/match/confirm，**没有** `submitRebind` / `unbind` 的 `toBe` |
| application 三枪 URL | 源码与 Vue 有；`service.test.ts` **没有** `current` / `save` / `submit` 请求表（只测了 `*Rebind`） |
| 覆盖决定要 `APPROVED` | **页面**映射：`decision.value === 'APPROVE' ? 'APPROVED' : 'REJECTED'`。厨房测试 `archive.decide(..., { decision: 'APPROVE' })` **原样** POST，工厂不改码 |
| 「测试禁止源码出现 `materials: []`」 | Vue `components.test.ts` 成立。厨房测试自己就 `create({ ..., materials: [] })` |
| 「OpenAPI 快照尚无 `/profile/**`」 | `README.md` 第 19 行仍写这句。`generated/openapi.ts` **已经**有路径。课已要求以生成文件为准 |
| 名册状态筛 | 空串→`undefined` 成立。下拉只有 `ACTIVE` / `REVOKED`，没有 DRAFT / PENDING_REVIEW / SUSPENDED |
| self 空 permissions | 源码 `Object.freeze([])`。`registration.test.ts` 只锁三键，不断言 `permissions` |
| 匿名回调无页面 | README 成立；生成快照有 `/profile/person/verification/providers/{providerCode}/callback`，web-domains **无** `src/anonymous/` |

问：在同一工厂两厅堂、三抽屉冻住、旧名 `===`、`app%2F1`、GET/POST 闭合、403 原样、probe 只许 status、七键顺序、admin 缺 runtime throw、self 三键空权限、页面不喊 materials/rebind、APPROVE→APPROVED 只发生在覆盖决定已经能互证的前提下，(a) 是否还要把「动词小写」「facade 测试没打 current/save/submit」「厨房 decide 夹具用 APPROVE」「README 旧句」写成开篇半句，才算两格讲完？还是这些已经落在课的表/反例里，Lead 可以直接给**厨房独行**盖章、并保持九工厂行 uncovered？

## Q3 假设与反例

- objective_id: OBJ-39
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无 `systemService.profile`；home 厨房仍有 archive；self 工厂 ≠ admin 工厂；Vue 不 attach；流程钮不打 `/decision`；`review` URL 是 `/review-context`；self 校验只拒缺 runtime；中心页字面量 apply；capabilities 有 rebind 无换绑页；九工厂行不因本课整行闭合；README 与 generated 并存
- difficulty: high

课的隐含假设若反转，合格读者会把哪座工厂或哪只抽屉认错？

1. **「home 的 `profileService` 没有 archive / rebind。」** 对象上有。差在 self 三键。课反例 1 / L-008 覆盖。
2. **「`/user/profile` 就是档案楼。」** admin 个人中心是 system 资料页（L-022）。home `/profile` 才是 self 档案中心。课反例 2 覆盖。
3. **「一份 `createProfileWebDomain` 同时给 home 用。」** home 调 `createProfileSelfWebDomain`。课开篇表覆盖。
4. **「页面自己拼 `/profile/person/archive`。」** Vue 只喊 `runtime.service.person.archive.*`。课开篇表覆盖。
5. **「新建档案会 `person.materials.attach`。」** 全仓 Vue 搜不到。直建走 `archive.create.materials` + `fileUpload`。课反例 3 覆盖。
6. **「审核通过 = `decide({ decision: 'APPROVE' })`。」** 覆盖决定要 `APPROVED`。流程变量才 `APPROVE`。课反例 4 覆盖。厨房测试夹具用 `APPROVE`，不要把测试 data 背成页面合同。
7. **「self manifest 登记了 `profilePermissions`。」** `permissions: []`。中心页自己查字面量。课反例 5 覆盖。
8. **「`createProfileWebDomain(undefined)` 会给出空菜单。」** 立刻 throw。课反例 6 覆盖。
9. **「前端资源名 `PersonMaterialController` 对应一个 Java 类。」** 只是标签。课反例 7 覆盖。
10. **「厨房会读 `Admin-Token` / 会 HMAC。」** 只收 `HttpClient`。课反例 8 覆盖。
11. **「注销是 DELETE，导出在 archive.export。」** POST `.../revoke`；测试禁 export。课反例 9 覆盖。
12. **「把九个 web-domain 工厂一行标 covered。」** GP-L-020 已警告。课反例 10 / 误区 10 覆盖。
13. **「`review` 的 URL 是 `/review`。」** 是 `/review-context`。课误区 7 覆盖。
14. **「流程审核按钮打 `/decision`。」** 默认 `completeWorkflowTask`。课误区 6 覆盖。
15. **「详情下载走 `person.materials.accessUrl`。」** 走 `person.archive.material`。课误区 5 覆盖。
16. **「self 校验和 admin 一样严。」** self 只拒缺 runtime。课误区 8 覆盖。
17. **「`capabilities` 有 person-rebind 所以一定有换绑页。」** 名片有能力，manifest 可以不挂页。课误区 11 覆盖。
18. **「中心页 import 了 `profilePermissions`。」** 没有。课误区 13 覆盖。
19. **「本课覆盖企业 archive 十二枪页面。」** L-044。课误区 14 覆盖。工厂会**一并注册**那三键，本课不认页面调用。
20. **「匿名核身也有一张 Vue。」** 没有。L-038。课误区 15 / 边界 7 覆盖。
21. **「OBJ-39 包含 `PersonAdminController`。」** OBJ-34。课误区 1 覆盖。
22. **「material-tag 目录名写错了，应改成 material-tags。」** 页面 owner 别名；HTTP 仍是复数。课误区 3 覆盖。
23. **「`PersonProfilePage` 的 doc/docx 白名单 = 材料总库白名单。」** 直建走 archive 门。课误区 12 / 边界 3 覆盖。
24. **「runtime 校验了 `eligibleUsers`。」** 找人走 `runtime.findUsers`。课机制 2 覆盖。
25. **「chain 写 `createProfileWebDomain person/self`，所以 self 工厂不在本课。」** 磁盘第二份菜单就是 `createProfileSelfWebDomain`。格子按 chain 切片命名，口试要能指两个 id。

问：读完后仍说不出的，是不是「工厂的名字」，而是 **厨房方法全集 ≠ 墙上的菜 ≠ 页面已扣的扳机、九工厂行不能因本课整行闭合、覆盖决定才改 APPROVED、self 空 permissions 不是用户没有 apply 权、rebind/materials 四枪目前只有测试**？

## Q4 视角

- objective_id: OBJ-39
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-39 vs L-007/L-008/L-020/L-022/L-029/L-034…L-038/L-044；九工厂行；claimed_cells 切片名 vs `createProfileSelfWebDomain` 函数名
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课两格 (a)**？

| 视角 | 多出来的边 | 是否本课两格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 厨房独行 vs 九工厂同行；chain 切片名 `person/self`；无 Vue 行；(b) 无投影/runtime 行；(d) 无浏览器档案流 | **是** 闭合定义。厨房行可单独 covered。九工厂行保持 uncovered。不要发明 (b)/(d)，不要把三块 Vue 升成新行才能盖厨房章 |
| 厨房作者 | 三抽屉冻住；九子路径 URL/动词；旧名 `===`；`encodeURIComponent`；probe/match 投影；403 原样 | **是** `A:createProfileService` |
| 管理端菜单工厂 | 七键顺序、fail-closed 十一口 archive、权限摊平 materialTag+person+enterprise、未选无键 | **是** 切片里的 `createProfileWebDomain`。企业三页 **调用** **否**（L-044） |
| 用户端菜单工厂 | 三键、空 permissions、瘦校验、id `web-domain-profile-self` | **是** 切片里的 self。企业认证表单字段 **否**（L-044） |
| person / material-tag 页面 | page/create/detail/material/review/decide；tree 五枪；fileUpload；APPROVE→APPROVED | **是** OBJ 对照走查，不是矩阵第三格 |
| self 个人页 | current/save/submit；中心页字面量 apply | **是** 对照。后端材料闸 **否**（L-035 / L-037） |
| 厅堂电工（L-007 / L-008） | 两厅堂各 `createProfileService(domainHttp)`；Token 在 HTTP 适配器 | 插头一句 **是**。12 个 `create*` 全表 **否** |
| 导航 host（L-020） | admin 选 `web-domain-profile`；home 选 `web-domain-profile-self` | 选择集一句 **是**。15 键 / 恢复链 **否**。九工厂行 **否** 整行 |
| 档案窗（L-034） | `/profile/person/archive` 十二扇 | URL 对齐 **是**。Java 窗 **否** |
| 自助申请（L-035） | current/submit 五层、ALWAYS 材料闸 | 三枪 URL **是**。submit 事务 **否** |
| 换绑 / 材料 / 核身（L-036 / L-037 / L-038） | 五枪 / 挂摘四枪 / `@SaIgnore` 回调 | 厨房有枪、页面未扣 / 无 Vue **是** 边界。Controller **否** |
| 企业菜单（L-044） | enterprise 三键 + self 企业认证页字段 | 工厂一并注册 **是** 负空间。页面消费 **否** |
| system 资料页（L-022） | `/user/profile`、`SysProfileController` | 走错楼一句 **是**。社交/改密 **否** |
| OSS 直传（L-029） | `runtime.fileUpload` → `ossUploadClient.upload` | 夹 `ossId` **是** 边界。adapter 窗口 **否** |
| Java 聚合行 `CompositeProfileService…` | wta-api 端口 | **否**。不要把后端聚合行并进前端工厂 |
| MySQL 8.4 / Redis 8 | 菜单种子 / 会话 | 种子键对照 **是** (a)。容器 **否** |

问：Lead 若凭「两份工厂名在函数表里」把九工厂行标 covered，是否还要求读者能指着 **厨房未裁方法、admin 七键含企业三键、home 三键、runtime 校验不含 eligibleUsers、页面不喊 attach/rebind**？这些是不是已经在本两格函数表/反例里，而不需要 (b) 新行、也不需要等 L-044 才给**厨房独行**盖章？九工厂行是否例外——必须保持 uncovered，直到其余七个工厂也有课？

## Q5 元问题

- objective_id: OBJ-39
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 厨房独行 + 九工厂同行、无 Vue 行、无本课 (b)/(d)
- difficulty: low

建议：`defer`

原因：

- **`A:createProfileService` 与矩阵厨房独行 1:1，主叙事站得住且与源码对齐。** L 合同章节齐全（宏观地图、定义表、机制链 1–8、带 caption / 文字等价物的两张图、正反例、变式、误区、暂停走查、来源表 S-005/S-006/S-010/S-L039-01…08）。能复现：只吃 `HttpClient`、三抽屉冻住、两厅堂各调一次、旧名 `===`、九子路径 GET/POST、`app%2F1`、无 export、403 原样、probe/match 失败关闭、capabilities 九项。不必为「工厂名字」再挖五问。
- **`A:createProfileWebDomain person/self` 是 chain 切片，不是矩阵独行。** 课已拆开两个 manifest id、七键/三键、两套 runtime 校验强度，并把 OBJ 点名的 person / self / material-tag 门脸走到具体 `runtime.service.*`。Lead **可以**把厨房独行标 `covered`。Lead **不可以**凭本课把九工厂同行标 `covered`（GP-L-020 已立过同一条）。企业三页调用留给 L-044；self 第三键的表单字段也留给 L-044，键名本课已登记。
- 本批可粘贴的精度项不挡住厨房盖章：（1）动词小写 `'get'`/`'post'`；（2）facade 测试未打 `current`/`save`/`submit` URL，源码与 Vue 有；（3）厨房 decide 夹具用 `APPROVE`，页面覆盖映射才 `APPROVED`；（4）README 第 19 行与 generated `/profile/**` 并存，课已点破；（5）名册筛只有 ACTIVE/REVOKED。不要把 Java 十二扇、换绑页、materials 四枪 Vue、企业 archive 页面、九工厂其余七个名字塞进本两格。
- 不建议 `mine-more`：b01 未证明厨房行还有真实矩阵空洞；再五问只会把 L-044 企业页、L-034 十二扇或 L-037 attach 再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；半句精度不是第二容器。不建议 `split-lesson`：未满 10 问；厨房与两份菜单是 chain 原设计的同一 OBJ-39，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：隐私投影、fail-closed runtime、空 permissions 是 (a) 的解释到副作用。不建议把三块 Vue 升成矩阵行才给厨房盖章：课已写明格子按 chain 认工厂/切片。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:createProfileService | 只吃 `HttpClient`、三抽屉冻住、两厅堂同工厂、旧名 `===`、九子路径 URL/动词、`encodeURIComponent`、无 export、403 原样、probe/match 投影、capabilities 九项——均与源码对齐。残留精度：动词小写；facade 测试未打 application 三枪与 `submitRebind`/`unbind` 的 `===`；厨房 decide 夹具用 `APPROVE`；README 旧句与 generated 并存。 | 可选半句：口试动词以厨房 `method` 小写为准；`current/save/submit` 以资源 `service.ts` 与 self 页为准，不要只背 facade 测试表；覆盖码映射在 Vue 不在工厂。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-039 来源表 S-L039-01…03）。 | defer（本行可 covered；精度项不挡盖章） |
| A:createProfileWebDomain person/self | 课按磁盘讲了两个 id、七键顺序、self 三键、admin fail-closed 十一口、self 只拒缺 runtime、空 permissions、person/self/material-tag 实际调用、企业三键一并注册但不认页面。**矩阵无此切片行**；符号落在九工厂同行。 | 不要并进厨房行才能盖章，也不要改九工厂行为 covered。Lead 保持该行 uncovered，直到其余七个工厂也有课（L-020 / L-044 / 后续 notify/third/workflow/demo/ai）。本切片证据可写进厨房行备注或等 L-044 后只注释「profile 两份菜单已讲」。 | defer（切片已讲；九工厂行不可因本课整行闭合） |
| A:createProfileSelfWebDomain（九工厂行内符号） | 课已锁三键 + 空 permissions + 瘦校验 + home `selectedManifestIds`。函数名不在 chain 格子字面，但磁盘就是第二份菜单。 | 保持划在 person/self 切片。不要另开矩阵行才盖厨房章。 | defer（已在本课切片内讲完） |
| A:createProfileWebDomain enterprise | 七键里后三键工厂会注册；页面 `enterprise.archive.*` 与企业认证表单未认。 | 保持。L-044。 | defer（L-044） |
| 九工厂行其余七个 `create*WebDomain` | 本课只用选择集当反例。 | 保持划出。L-020 已覆盖 `createSystemWebDomain`；其余后续课。 | defer（各 web-domain 课） |
| PersonAdminController.* / PersonApplicationController.current,submit / PersonRebindController.* / PersonMaterial* / MaterialTagController.* / PersonVerificationAnonymousController.callback | 门牌对照 / 无 Vue。 | 保持。L-034…L-038。 | defer（邻课） |
| A:admin-web.services / A:home-web.services | 插头 `createProfileService(domainHttp)` 已借用。 | 保持。L-007 / L-008。 | defer（L-007 / L-008） |
| A:createSystemWebDomain / B:navigation host | 选择集 + 两份 manifest id 已点。15 键全表未讲。 | 保持。L-020。 | defer（L-020） |
| A:SysProfile / 资料页 | `/user/profile` 走错楼一句。 | 保持。L-022。 | defer（L-022） |
| A:createOssUploadClient.upload | `runtime.fileUpload` 只当口。 | 保持。L-029。 | defer（L-029） |
| CompositeProfileService, ProfileService, PersonIdentityLookupService, ProfileMaterialPort | 后端聚合行四名字未认。 | 不要并进前端工厂才能盖章。 | defer（未认的 Java 聚合行） |
| (b) runtime 校验 / 隐私投影 / 空 permissions | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 浏览器档案流 | 矩阵无此行。ASCII 图不是新的 (d)。 | 不要发明前端申请/审核数据流行。 | defer（未入库） |
| MySQL 8.4 / Redis 8 | 菜单种子是 (a) 对照，不是容器闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；厨房独行无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（九工厂同行是矩阵绑行问题，课内已把切片讲完并显式禁止整行闭合）；合格读者说得出工厂目的（浏览器档案厨房 + 两份菜单）、主路径（admin 走 archive+标签树 / home 走 application 三枪）与失败路径（缺 runtime 关门、probe 多字段关闸、403 原样抛、缺 taskId 不发 HTTP）；把可选半句写进原课不会超过 40 分钟；后端五层/企业页面/厅堂插头/九工厂其余名字已有邻课。满 10 问前不拆。厨房包与两份 web-domain 虽跨三个 npm 包，仍是 chain 原设计的同一 OBJ-39，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-044 企业菜单、L-034…L-038 后端窗、L-007/L-008 厅堂、L-020 导航与九工厂其余名字、L-022 资料页、L-029 直传、Java 聚合行、未入库 (b)/(d) 以及 MySQL/Redis 容器显式建议 `defer`；对 `A:createProfileService` 建议 Lead 标 `covered`（证据锚本 probe 与 L-039 来源表）；对 `A:createProfileWebDomain person/self` 建议视为已讲切片、**不要**把九工厂同行标 `covered`。本课不主张改 Java 窗、发明 Vue 矩阵行、或把 enterprise 三页算进本切片。
