---
artifact: learning-goal-probe
probe_id: GP-L-055-b01
lesson_id: L-055
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-055 / b01

审问对象是已写成的 Lesson `children/2026-09-14-wta-sso/lessons/L-001-sso-authorize.md`（chain 映射为本单元 L-055）与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain 覆盖列）：`A:SsoOAuthController.authorize`、`A:SsoOAuthUseCase.authorize`、`D:fail-pkce`。OBJ-55 原文：能口述 `GET /sso/oauth2/authorize` 的 PKCE/Client/授权码链。档是**解释+走查**。child 课 OBJ-01 把同一窗收成 `SsoOAuthController.authorize` → `SsoOAuthUseCase.authorize` → `SsoOAuthUseCaseImpl` → `SsoAuthorizationService`，以及该路上的 PKCE、Client 目录、会话、授权码 DAO。矩阵 (a) **两行都是三方法绑死**：`SsoOAuthController.authorize, token, revoke` 与 `SsoOAuthUseCase.authorize, exchange, revoke`，均仍 `uncovered`。矩阵 (b) **另有**独行 `SsoOAuthController.authorize`，口号「写授权码；PKCE」，仍 `uncovered`；**没有** UseCase.authorize 独行。矩阵 (d) 「主路径 SSO 授权码」原文 `authorize PKCE → login 会话 → token 换业务令牌`，备注 L-055、L-056，仍 uncovered。矩阵 (d) 「失败路径 SSO PKCE」原文 **`code_verifier` 不匹配 → 不发令牌**，备注**只挂 L-055**，仍 uncovered。矩阵 (c) `wta-modules` 业务房间与 `sso-web` 容器已 covered（L-002/L-003/L-004）；MySQL 8.4 / Redis 8 / `dao-mapper-ownership` 仍 uncovered。child 课头 **未**列 `claimed_cells`，课号仍是 L-001。本批按派单只审问 **authorize 这一扇 GET + UseCase.authorize + 本窗 PKCE 拒绝**。token/revoke/`exchange` 留给 L-056；`POST /sso/login` 写 Cookie 留给 L-057；sso-web `requestAuthorize` 留给 L-058；`SsoClientCatalog` / `SsoIdentityService` 合同字段留给 L-059；SysClient bind/rotate 留给 L-018。不要把 `SsoAuthorizationService` 升成第二张 (a)，也不要用本课去盖 Redis 容器或「不发令牌」那句换票口号。

抽查锚点（2026-09-17）：`backend/wta-modules/wta-sso/src/main/java/org/namewta/sso/controller/anonymous/SsoOAuthController.java`；`usecase/SsoOAuthUseCase.java`；`usecase/impl/SsoOAuthUseCaseImpl.java`；`service/SsoAuthorizationService.java`；`support/PkceS256.java`；`domain/{SsoOAuthCommands,SsoAuthorizationCode,vo/SsoAuthorizeVo}.java`；`dao/{SsoAuthorizationCodeDao,SsoAuthorizationCodeDaoImpl}.java`；`mapper/SsoAuthorizationCodeMapper.java`；`resources/mapper/sso/SsoAuthorizationCodeMapper.xml`；`config/{SsoProperties,SsoServiceConfiguration}.java`；`usecase/SsoSessionUseCase.java`；`usecase/impl/SsoSessionUseCaseImpl.java`；`service/SsoSessionService.java`；`adapter/store/RedisSsoSessionStore.java`；`adapter/gateway/{WtaApiSsoClientCatalogAdapter,WtaApiSsoIdentityAdapter}.java`；`port/{SsoClientCatalogPort,SsoIdentityPort,SsoSessionPort,SsoBusinessTokenPort}.java`；`wta-api/.../sso/api/{SsoAuthenticatedUser,SsoClientView}.java`；`src/test/.../service/SsoAuthorizationServiceTest.java`；`support/PkceS256Test.java`；`service/InMemorySsoAuthorizationCodeStore.java`；登记表 `03-backend-module-modes.md` layered 行；`application.yml` `namewta.sso` 与 `security.excludes`；DDL `10-cde-base-ddl.sql` `sso_authorization_code`。**零** `SsoOAuthController*Test` / `SsoOAuthUseCase*Test`。**零** `needsLogin` 单测。旁证（格子归邻课）`SsoSessionController` / `SsoSessionCookie`（L-057）；`SsoAuthorizationService.exchange` / `PkceS256.verify`（L-056）；`frontend/apps/sso-web/src/ssoApi.ts` `requestAuthorize`（L-058）。

## Q1 澄清

- objective_id: OBJ-55
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-55 解释+走查；child course OBJ-01；chain L-055 覆盖列 A+A+D；矩阵 (a) 两行三方法绑死仍 uncovered；(b) authorize 独行「写授权码；PKCE」仍 uncovered；(d) 主路径挂 L-055/L-056；(d) fail-pkce 口号是 verifier 不匹配不发令牌、只挂 L-055；负空间 L-018/L-056/L-057/L-058/L-059
- difficulty: medium

课把认人厅收成「先核对项目门口和 PKCE 暗号，有手环才塞一次性纸条；没手环只回 JSON 请先登录」。计划里至少有四套可数对象，派单点名的是 **两格 (a) 方法 + 一条失败 (d)**：

1. **OBJ-55 原文。** 父 `course.md`：能口述 `GET /sso/oauth2/authorize` 的 PKCE/Client/授权码链。档是解释+走查，不是 OBJ-56 的方法性状。一句里点名授权入口 + PKCE + Client + 码，**没有** `POST /token`、`code_verifier`、写 Cookie、sso-web 四函数。
2. **chain 覆盖列。** 三格：`A:SsoOAuthController.authorize`、`A:SsoOAuthUseCase.authorize`、`D:fail-pkce`。不写 token/revoke，不写 (b)，不写主走廊 (d)。
3. **矩阵四行对照。** (a) Controller 与 UseCase 都把 authorize **和** token/revoke、exchange 印在同一格；Lead 若按**现印同行**标 covered，会把 L-056 假闭合。(b) 独行符号是 Controller.authorize，口号「写授权码；PKCE」——与 OBJ 走查对齐，chain 没写 (b)。(d) 主走廊挂两课，还夹了「login 会话」（L-057）。(d) fail-pkce 口号是 **verifier 不匹配 → 不发令牌**：那是 `PkceS256.verify` + `tokenPort.issue` 未调，HTTP 在 `POST /token`。
4. **课自己的操作定义。** 控制器 `@SaIgnore` + `@GetMapping("/authorize")`，读 `SsoProperties.getCookieName()`（默认 `Sso-Token`），`sessionUseCase.current`，组 `AuthorizeCommand`（user 可空），`oauthUseCase.authorize`，抄 `SsoAuthorizeVo` 后始终 `R.ok`。Impl 一行转交 `SsoAuthorizationService.authorize`。服务先 `response_type=code`、`requireEnabledClient`、`requireExactRedirect`、`PkceS256.requireChallenge` + `requireS256`、state 非空；**然后**才分叉：`user == null` → `needsLogin()` 且不 `insert`；有人 → `assertClientAccess`、`newCode`、`codeDao.insert`、`appendQuery`。HTTP 把 state/challenge/method 标成 `required = false`，服务缺一样就 `ServiceException`。本窗 **不**调 `PkceS256.verify`、**不**调 `tokenPort`、**不** `Set-Cookie`、**不** HTTP 302。
5. **本课 (d) 与矩阵口号不是同一岸。** 本窗能指到的 PKCE 失败：缺 `code_challenge`、method 不是 `S256`（含 `plain`）→ 抛错、不写码。非法 Client / 通配符回调 / 缺 state 同样不写码。`needsLogin` 是协议过关后的登录早退，仍不写码，但是 `R.ok` 不是异常。矩阵那句「不发令牌」在本方法上**观察不到**——authorize 从不发令牌。
6. **邻格。** L-056 才 `exchange` + `verify` + `consumeIfUnconsumed` + `issue`。L-057 才 `SsoSessionCookie.create`。L-058 才 `requestAuthorize` 抄查询串。L-059 才 catalog 是否带哈希。L-018 才管理端 bind/rotate。

问：本三格闭合标准是「只盖 authorize 方法 + 薄 UseCase 转交 + 授权时 PKCE/Client/state 拒绝不写码 + 未登录 JSON 早退，且不碰 (a) 三方法同行、不把 verifier 不匹配标进本 (d)」？还是 OBJ 那句必须先把 (a) 聚合行、(d) 主走廊、矩阵口号「不发令牌」一起 covered？Lead 若凭「`SsoOAuthController.authorize` 四字在函数表里」把 token/revoke 或 `code_verifier` 换票失败标 covered，是否假闭合了 L-056？

## Q2 证据

- objective_id: OBJ-55
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 31–71 / 104–115；UseCase 17；Impl 17–27；Service 66–98 / 150–187；PkceS256 17 / 46–61；Commands 16–41；VO 8–19；Properties 18–38；Configuration 24–48；Session UseCaseImpl 32–34；SessionService 51–53；Redis store 19 / 38–43；CatalogAdapter 22–24；IdentityAdapter 31–33；DaoImpl 22–24；Entity 19–86；DDL 1668–1689；yml 28–34 / 287–288；modes.md 20；ServiceTest 55–67 / 70–88；PkceS256Test 15–32；controller/usecase 包零本类测试；ssoApi.ts 50–68（邻格）
- difficulty: high

课的 (a) 授权入口、(b) 写码+PKCE、(d) 本窗拒绝哪些能在工作树直接复现，哪些只是测试名单比课表窄、或邻格事实？

**仍能复现（本课 A + 本窗 PKCE 拒绝）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 三扇门挂 `/sso/oauth2`，类上 `@SaIgnore`，缺省启用 | Controller 31–37；yml `namewta.sso.enabled: true`；excludes `/sso/**` 与 `/sso/oauth2/**` |
| 本方法是 `GET /authorize`；形参三必填 + 三可选 + request | Controller 56–62 |
| 读 Cookie 名来自 `properties.getCookieName()`，默认 `Sso-Token`；不调 `SsoSessionCookie` | Controller 104–115；Properties 28；yml 32 |
| `sessionUseCase.current(readSessionId)`；空白/无键 → `null` | Controller 63；UseCaseImpl 32–34；SessionService 51–53；Redis 38–43，前缀 `sso:session:` |
| 用户对象只有 `userId`+`username`，无业务 Token | `SsoAuthenticatedUser` 9–16 |
| 组 `AuthorizeCommand` 后只调 `oauthUseCase.authorize`；出参抄 VO，始终 `R.ok` | Controller 64–70；VO 14–19 |
| UseCase 接口三方法；本课只走 `authorize` | UseCase 17；Impl 25–27 一行 `authorizationService.authorize` |
| 授权引擎不是 `@Service`，是 `@Bean` `new`，TTL=`properties.getCodeTtl()`，钟=`Clock.systemDefaultZone()` | Configuration 24–48；Properties 33；yml `code-ttl: 5m` |
| 负向顺序：null 命令 → `response_type=code` → 目录 → 精确回调 → challenge → S256 → state | Service 67–79 |
| 空 client / 非 NORMAL / `ssoEnabled` 非 TRUE 的中文异常 | Service 150–160；CatalogAdapter 22–24 |
| 回调含 `*` 或目录无 `equals` → 禁止通配符 / 未登记 | Service 164–175 |
| `requireChallenge` 空白 → `缺少 code_challenge`；`requireS256` 空白或非 S256（trim 后大写）→ 必须为 S256 | PkceS256 46–61；METHOD=`S256` |
| `user == null` 在上述检查**之后**；`needsLogin()` = `(true, null)`；到此无 `insert` | Service 80–82；Commands 29–31 |
| 已登录：`assertClientAccess` 后填码行并 `codeDao.insert` | Service 83–97；IdentityAdapter 31–33；DaoImpl 22–24 `mapper.insert` |
| 码：32 字节 `ThreadLocalRandom` + URL Base64 无填充；challenge/state/redirect 存 trim 后原文；`consumed=false`；`expireTime=now(clock)+codeTtl`；`version=0` | Service 85–96 / 178–182；Entity 33–80 |
| `appendQuery`：已有 `?` 用 `&`，否则 `?`，拼 `code=` 与 `&state=`；**无**百分号编码 | Service 184–187 |
| 控制器成功支与登录早退都无 `Set-Cookie`、无 302、无 `@Log`（`@Log` 在 token/revoke） | Controller 56–71 vs 79 / 97 |
| `wta-sso` layered；仅经 wta-api 读用户与 Client | modes.md 20 |
| 表 `sso_authorization_code` 有 `code_challenge` / unique code / expire 索引 | DDL 1668–1688 |
| 授权时 PKCE 拒绝单测：缺 challenge、`plain`、通配符、未登记回调、缺 state | ServiceTest 55–67 |
| RFC 7636 附录 B 向量只证明 `challenge()` 公式 | PkceS256Test 15–18 |

**邻格仍能复现（不计入本三格闭合）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| `PkceS256.verify`：长度 43–128 或哈希不等 → `PKCE 校验失败`；在 `consumeIfUnconsumed` **之前** | PkceS256 69–79；Service.exchange 132–135 |
| 错 verifier 单测与成功换票写在同一方法 | ServiceTest 70–88 |
| XML `consumeIfUnconsumed` 才改 `consumed` | Mapper.xml 7–16；本课 authorize **零**调用 |
| token/revoke 不读 Cookie、不调 `sessionUseCase` | Controller 81–102 |
| 写 Cookie 是 `SsoSessionController` | L-057 |
| sso-web `requestAuthorize` 带 `credentials: 'include'` 打同一 GET，认 `loginRequired === true` | ssoApi.ts 50–67；格子 L-058 |

**课写过、测试名单更窄或口试标签写偏（不是证伪 A）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「未登录早退不写库」 | 源码 80–82 清楚；**无** `user==null` 单测，也无 Controller 绑定测试 |
| 「HTTP 可选 ≠ 服务可选」 | ServiceTest 打了缺 challenge / 缺 state；没有 Spring `required=false` 的 MVC 测试 |
| 「PKCE S256」易滑成授权时就算 SHA-256 | authorize **只** `requireChallenge`+`requireS256`，把客户端给的挑战 **原文 trim 入库**；`challenge()`/`verify()` 在换票 |
| 「纸条默认约 5 分钟」 | Bean+yml+实体 `expireTime` 有；本窗没有把钟拨过期的 authorize 测试（过期闸在 exchange） |
| `appendQuery` 不编码 | 课写了拼接；没有 `state` 含 `&` 的测试 |
| 生产环境是否已部署 SSO | 课标未决；本课不关闭 |

问：在 GET JSON 协议端点、Cookie 只读、UseCase 一行转交、检查顺序先于登录分叉、缺挑战/非 S256 抛错不写码、已登录才 insert 已经能互证的前提下，A 是否还要把「无 needsLogin 单测」「授权时不算哈希」写成开篇半句才算讲完？矩阵 (d) 那句 verifier 不匹配是否根本不在本课覆盖的 HTTP 方法上，因而不必等 GP-L-056 才能给 **authorize 方法** 盖章，也 **不能** 用本课给「不发令牌」盖章？

## Q3 假设与反例

- objective_id: OBJ-55
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: Controller 56–71 / 79–102 / 104–115；Service 67–97 / 132–135 / 164–175 / 184–187；PkceS256 46–79；Commands 29–31；课反例 1–3 / 正例 1–2 / 误区 1–6 / 边界；ServiceTest 55–88；L-002 反例 1
- difficulty: high

课的隐含假设若反转，合格读者会把哪段走廊认错？

1. **「`loginRequired` 等于 HTTP 401 或 302。」** 源码是 `R.ok` 包 VO。课误区 2。前端认布尔字段（L-058），服务器不跳。
2. **「没登录就谈不上 PKCE / Client。」** 检查在 `user == null` 之前。非法 Client 得不到「去登录」温柔出口。课误区 3。
3. **「HTTP `required = false` 表示服务可选。」** Spring 绑得进，`requireChallenge` / `requireS256` / 缺 state 仍抛。课反例 1。
4. **「`code_challenge_method=plain` 也能发码。」** `requireS256` 拒绝。ServiceTest 第二枪。
5. **「回调可以前缀匹配或带 `*`。」** `contains("*")` 直接失败；其余必须 `equals`。课反例 2。
6. **「authorize 会 Set-Cookie / 写 Redis 会话。」** 只 `readSessionId` + `current`。写手环是 L-057。课误区 1。
7. **「授权码就是业务 Token / Cookie 里是 Sa-Token。」** 码进 MySQL；Cookie 是会话标识；`tokenPort.issue` 在 exchange。课误区 5–6。
8. **「`SsoOAuthUseCaseImpl` 里有 PKCE 算法。」** 算法在 `PkceS256`，规则在 Service。Impl 只转交。课误区 4。
9. **「未登录先 insert 一枚匿名码，登录后再绑定用户。」** 源码没有这条路。图边界写明。
10. **「GET 不写库，所以这不是发码入口。」** 已登录成功支 `codeDao.insert`。课边界：不要抄成普通 CRUD 的 GET 模板。
11. **「authorize 会 `PkceS256.verify` / 核对 `code_verifier`。」** verifier 根本不是本方法形参。`verify` 在 exchange 132 行。把假设反转后，Lead 会用本课假闭合矩阵 fail-pkce 口号。
12. **「PKCE 失败也走 `needsLogin`。」** 协议失败是 `ServiceException`；`needsLogin` 只在检查过关且 `user==null`。两者都「不写码」，出口不同。
13. **「本课覆盖 `POST /token`，所以 (a) 三方法行可 covered。」** token 在同一控制器，chain 没写。L-056。
14. **「`webOrigin` 参与授权。」** 字段在 Properties 23 行，本方法不读。课图边界。
15. **「规则可以塞进控制器。」** 控制器只读 Cookie、组命令、抄 VO。课变式 C。
16. **「业务 App 直接把 `Sso-Token` 当登录态。」** 拆掉 Client 隔离。课变式 D。
17. **「矩阵要先有 UseCase.authorize 独行 / skip 哈希独行才算 (a) 讲完。」** 矩阵无这些行。它们是走查的 explained。
18. **「(a) 聚合行必须等改 chain 才处理。」** 课开篇已把 token/revoke 标成只点名。证据锚是同文件三方法 + chain 只写 authorize。miner 不改 chain。不要用本课把聚合行标 covered。
19. **「`appendQuery` 等于浏览器 302 Location。」** 返回值是 JSON 里的字符串。谁跳是 sso-web（L-058）。
20. **「授权时会验证挑战看起来像 SHA-256 输出。」** 只查非空。乱字符串只要 method=S256 也能入库；换票时 `verify` 再算。

问：读完后仍说不出的，是不是「Controller 类名」，而是 **JSON 协议不是 302、Cookie 只读、Impl 无规则、检查先于登录分叉、缺挑战/非 S256 抛错不写码、已登录才 insert、verifier 不在本窗、聚合 (a) 行盖不住 token**？

## Q4 视角

- objective_id: OBJ-55
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-55 vs L-018 / L-056 / L-057 / L-058 / L-059；chain 只 A+A+D；modes.md 20
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:SsoOAuthController.authorize` / `A:SsoOAuthUseCase.authorize` / `D:fail-pkce`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 三方法绑行 vs chain 只 authorize；(b) 独行可单独 covered；(d) 主走廊仍挂 L-055/L-056 且夹 login；(d) fail-pkce 口号是换票 | **是** 闭合定义。authorize 方法与 (b) 可单独 covered。**不要**用本课标 (a) 聚合行或主走廊。fail-pkce **口号**不是本 HTTP |
| 授权入口 | GET、Cookie 只读、组命令、`R.ok` 抄 VO | **是** (a) Controller.authorize |
| 薄用例 | Impl 唯一字段 `authorizationService`；authorize 一行 | **是** (a) UseCase.authorize。函数目的「规则不在控制器」也是 (b) 的 explained |
| 发码引擎 | 目录/白名单/PKCE/state → 分叉 → insert | **是** 走查证人。不要升 `A:SsoAuthorizationService` |
| 失败路径作者（本窗） | 缺挑战 / 非 S256 / 坏 Client / `*` / 缺 state → 异常不写码；无会话 → needsLogin 不写码 | **是** 本课能证的拒绝。与矩阵口号「不发令牌」**不是**同一句 |
| 失败路径作者（矩阵口号） | `verify` 失败、码未消费、`issue` 未调 | **否**。L-056。chain 把 D 挂在本课是计划错位 |
| 主走廊作者 | 源：authorize 写码；中：login 会话；汇：token 换票 | 写码半段 **是** 对照。整条盖章 **否**（还要 L-056，login 半段还要 L-057） |
| 换票（L-056） | `exchange` / `revoke` / `verify` / consume / `issue` | 对照一句「码绑了 challenge」**是**。验 verifier **否** |
| 域会话（L-057） | `login` 写 Cookie+Redis | 本窗只 `current` **是**。`create` **否** |
| 认人厅页（L-058） | `requestAuthorize` 抄六项查询串 | 对照谁消费 JSON **是**。页面树 **否** |
| 目录合同（L-059） | `SsoClientCatalog.findByClientId` 字段/哈希 | 本窗经 Port 取 View **是**。哈希是否拷贝 **否** |
| 管理端 Client（L-018） | bind / rotate | 目录里为何有 Client **否** |
| Redis 8 / MySQL 8.4 / dao-mapper | 会话键与码表 | 本窗机制举例 **是**。容器/组件课 **否** |
| 测试夹具行 | `*Test.java` 矩阵已 deferred | 证人 **是**。不要把夹具行标 covered |

问：Lead 若凭「authorize 四字在函数表里」把 (b) 标 covered，是否还要求读者能指着 **只读 Cookie、检查先于登录分叉、缺 PKCE 不写码、成功才 insert、返回 JSON 不是 302**？这些是不是已经在本格机制链/反例里，而不需要把 token、verifier、写 Cookie 或 sso-web 算进来？(d) 矩阵口号是不是另一条河，必须改挂 L-056 而不是用本课盖「不发令牌」？

## Q5 元问题

- objective_id: OBJ-55
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 三方法绑行、(b) authorize 独行、(d) fail-pkce 口号挂本课、(d) 主路径挂 L-055/L-056
- difficulty: low

建议：`defer`

原因：

- **Controller.authorize 与 UseCase.authorize 与 OBJ-55 / child L-001 1:1。** 合同章节齐全（宏观地图、直觉、定义表、机制链 0–10、带 caption / 文字等价物 / 图边界的登录分叉图、正反例、变式、误区、暂停走查、来源表 S-SSO-01…S-SSO-08）。**主叙事站得住且与源码对齐**：`@SaIgnore` GET JSON、Cookie 只读、Impl 一行转交、目录/精确回调/强制 S256/强制 state 先于登录分叉、`needsLogin` 不写库、已登录才 `insert`+`appendQuery`、本窗不碰 `verify`/`tokenPort`/`Set-Cookie`。不必为「Controller 类名」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例或邻格，不挡住把 **authorize 方法** 与 **(b) 独行** 标 covered：（1）授权时只验挑战存在与 method，不算 SHA-256；（2）无 `needsLogin` / Controller 单测；（3）`appendQuery` 不编码；（4）`webOrigin` 本方法不读。不要把 token/revoke、verifier、写 Cookie、sso-web 四函数、catalog 哈希塞进本三格。
- **不要把 (a) 三方法同行标 covered，也不要用本课给矩阵 fail-pkce 口号盖章。** 现印 (a) 把 authorize 和 token/revoke、exchange 绑死——本课只走查第一扇门。矩阵「失败路径 SSO PKCE」原文是 `code_verifier` 不匹配 → 不发令牌：磁盘在 `SsoAuthorizationService.exchange` 的 `PkceS256.verify`（consume 之前、`issue` 未调）。那句应等 L-056 / GP-L-056。本课能证的是授权时 PKCE/Client/state 拒绝 **不写码**，以及未登录 JSON 早退；authorize **从不发令牌**，故「不发令牌」不能当本窗观察句。
- **(d) 主路径 SSO 授权码不要独盖。** 备注已挂 L-055、L-056；原文还夹 login 会话（L-057）。本课只保证「有手环才写出绑死 Client/redirect/challenge 的码」。
- **(b) 独行不必改 chain 才盖章。** 证据锚是矩阵已有该行 + 课机制链 + ServiceTest 负向枪。miner 不改 chain。
- 不建议 `mine-more`：b01 未证明 authorize 方法还有真实矩阵空洞；再五问只会把 L-056 换票、L-057 Cookie、L-058 页面或 L-059 目录再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；可选半句（授权时不算哈希；矩阵口号是换票）不是第二容器。不建议 `split-lesson`：未满 10 问；发码与授权时 PKCE 拒绝是同一 OBJ-55，同一 `wta-sso` 容器。`split_depth: 0`，本批禁止填 split 表。不建议发明 Service (a) 行：发码规则是走查的 explained。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:SsoOAuthController.authorize | GET JSON、Cookie 只读、组命令、始终 `R.ok`、无 302/Set-Cookie——均与源码对齐。残留：无 Controller MVC 测试。 | 可选半句：口试「失败」要分 `ServiceException` 与 `needsLogin`。不要求重写机制链。Lead 可将**本方法**视为已讲；**不要**把矩阵 (a) 三方法同行标 `covered`。 | defer（本方法可 covered；聚合行不可） |
| A:SsoOAuthUseCase.authorize | Impl 一行转交 `SsoAuthorizationService.authorize`；规则不在控制器。无 UseCase 单测。 | 不要升 Service 为第二张 (a)。Lead 可将**本方法**视为已讲；**不要**把 `authorize, exchange, revoke` 同行标 covered。 | defer（本方法可 covered；聚合行不可） |
| B:SsoOAuthController.authorize（写授权码；PKCE） | 副作用：已登录才 insert 码行并拼回调串；失败/未登录不写库。PKCE 在本窗是 `requireChallenge`+`requireS256`，不是 `verify`。chain 未写 (b)。 | 不要等改 chain。Lead 可将本行标 `covered`（证据锚本 probe 与 L-001 来源表 S-SSO-01/S-SSO-03/S-SSO-05/S-SSO-07）。 | defer（本行可 covered） |
| D:fail-pkce / D:失败路径 SSO PKCE | 矩阵口号「`code_verifier` 不匹配 → 不发令牌」。磁盘在 `exchange`+`PkceS256.verify`，HTTP 是 `POST /token`。本课只证授权时缺挑战/非 S256/坏 Client/缺 state → 不写码，以及 needsLogin 不写码。 | **不要**用本课按现印口号标 covered。Lead 把该行改挂 L-056（或等 GP-L-056），或改口号为「授权时 PKCE/Client 拒绝 → 不写码」。可选半句写进 L-001：矩阵那句是换票。 | defer（口号归 L-056；本窗拒绝已写进 B） |
| D:主路径 SSO 授权码（authorize PKCE → login 会话 → token 换业务令牌） | 矩阵仍 uncovered，备注 L-055、L-056；原文还夹 L-057 会话。本课只讲写码半段。 | 不要用本课单独改该行。对照一句「有手环才写出绑死 challenge 的码」即可。 | defer（L-055/L-056/L-057；本课不够独盖） |
| A:SsoOAuthController.token, revoke / A:SsoOAuthUseCase.exchange, revoke / B:token / B:revoke | 同控制器后两扇门；`verify`+consume+`issue`；只撕提交的票。 | 保持。L-056。本课只点名。 | defer（L-056） |
| A:SsoSessionController.login, session, logout | 写/读/删 SSO Cookie 与 Redis。本课只 `current`。 | 保持。L-057。 | defer（L-057） |
| A:parseAuthorizeQuery / loginWithPassword / requestAuthorize / fetchSession | 认人厅抄查询串、认 `loginRequired`。 | 保持。L-058。 | defer（L-058） |
| A:SsoClientCatalog / A:SsoIdentityService | Port 后面的 wta-api 合同与哈希。 | 保持。L-059。本课只经 Adapter 取 View / `assertClientAccess`。 | defer（L-059） |
| A:SysClientController.bindSso, rotateSsoSecret | 管理端为何目录里有 Client。 | 保持。L-018。 | defer（L-018） |
| A:SsoAuthorizationService / PkceS256 | 矩阵无这些符号。课已写进走查。 | 不要发明 Service (a) 行。 | defer（未入库） |
| C:Redis 8 / MySQL 8.4 / mysql-base / dao-mapper-ownership | 会话键与码表是本窗机制，不是容器课。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-084 / L-085 / L-083） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；authorize 方法与 (b) 无「课没讲到」的空洞需要靠 b02 继续 `mine-more`（(a) 聚合行与 fail-pkce 口号是矩阵绑行/挂课问题，课内已把切片讲完并显式禁止整行/整句闭合）；合格读者说得出目的（JSON 授权入口发一次性码）、主路径（读 Cookie → 目录/PKCE/state → 有人 insert → 回调串）与本窗失败（缺 PKCE/坏 Client/未登录 → 不写码）；主走廊与 verifier 口号缺的是矩阵把它们挂错或挂在两课，不是讲义容器；把可选半句写进原课不会超过 40 分钟；token、会话、sso-web、catalog 已有邻课。满 10 问前不拆。发码与授权时拒绝是 chain 原设计的同一 OBJ-55，同一 `wta-sso` 容器，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-056 换票/verifier、L-057 会话 Cookie、L-058 认人厅、L-059 目录、L-018 bind、未入库 Service 行、(c) 容器与 dao-mapper 组件显式建议 `defer`；对 `A:SsoOAuthController.authorize` 与 `A:SsoOAuthUseCase.authorize` 建议视为已讲方法、**不要**把矩阵 (a) 三方法同行标 `covered`；对 `B:SsoOAuthController.authorize` 建议 Lead 标 `covered`（证据锚本 probe 与 L-001 来源表）；对矩阵「失败路径 SSO PKCE」建议保持 uncovered 并改挂 L-056（或改口号），**不要**用本课按「不发令牌」盖章；对矩阵「主路径 SSO 授权码」建议保持 uncovered 并继续挂 L-055/L-056（login 半段对照 L-057），不要用本课独盖。本课不主张发明 Service (a)、把 `verify` 升成本 (d)、或把 Redis 容器算进本三格。
