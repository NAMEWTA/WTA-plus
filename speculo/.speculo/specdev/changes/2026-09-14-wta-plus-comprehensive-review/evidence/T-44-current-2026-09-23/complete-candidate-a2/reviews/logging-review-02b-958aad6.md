# T44 02B JSON/path log-copy review — fixed source

Fixed product commit `958aad6174c880fbf8c7afbfb8f657d37c968979`, compared with A1 product `e11c1b6f3a4a4a8bdc1746044d65fe8445c0865c`. Reviewed `LogSanitizer.java` SHA-256 `678fed3d54fb849dd8e22dd255754ce209051e8477da01a6812176a02074da4c`, `LogSanitizerTest.java` SHA-256 `07f6e7c50793b53b771a844670cb7af383f23c7e69034f3cfd3680eb33efb562`, `SysLogFilterTest.java` SHA-256 `ffd8d7d530afe5cd0f34794edb82c20198c7493ec308ca301d18508a1c7a945c`. Static verdict for these three files: **no blocking finding**. This is not a claim that the separate SQL logging change or full T44 live gate has passed.

`LogSanitizer.redact` now traverses both object properties and array elements, replacing textual leaves whose HTTP(S) URL has a credential-bearing query name. This covers the actual `data.url`, `data.presignedRequest.url`, and `parts[].url` shapes from OSS signed responses; recognized `X-Amz-Credential`, `X-Amz-Signature`, and token-style query names are case-insensitive and URL-decoded once. A malformed URL with a clear credential query name is still replaced because the rule inspects text rather than depending on successful URI parsing. An ordinary public URL with `page=2` and an unrelated business `code` remain visible; the new JSON test asserts both. Jackson 3.1.4's local `ArrayNode` API has `set(int,String)`, so the array replacement call is valid. Existing `object` works on a deep copy, and `SysLogFilterTest` verifies the actual HTTP response and wrapped servlet request retain their original values while emitted events hide canaries.

`uploadToken` is now a sensitive field name. `LogSanitizer.path` masks the first nonempty segment under `/resource/oss/uploads/` and preserves real suffixes such as `/parts`, `/parts/sign`, and `/complete`. The filter's request/response event paths already pass through this method; the new web test proves both sides and separately checks a credential query parameter from `getParameterMap`. The base `/resource/oss/uploads` and `/resource/oss/config/diagnose/7` remain unchanged. `/resource/oss/uploads//arbitrary` has no token in the contract's first segment and is not an upload route; it is a nonblocking arbitrary 404 path case, not evidence that a real upload token leaks.

The fixed-source tests cover the relevant JSON shapes, ordinary-URL/business-field preservation, unchanged business response and original request path, plus credential query-name redaction. No Maven, service, MinIO or browser run was performed by this reviewer. Lead's targeted Maven result and the later exact-clean v7 MinIO log canary result must be recorded separately; v5/v6 credential-leak failures remain failed historical attempts. The independent `SqlLogInterceptor` source and test review is outside this three-file verdict.
