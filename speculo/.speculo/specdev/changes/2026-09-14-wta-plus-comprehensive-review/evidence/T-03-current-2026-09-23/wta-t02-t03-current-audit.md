# T-02 / T-03 当前代码预审（只读）

固定产品输入 `38032d24335c52cafea855b19d51fb36295162ef`；审查时 T-37 正在当前工作树施工，所有产品事实均由 `git show 38032d2:<path>`、`git grep ... 38032d2 --` 及固定两点 diff 读取，不据混合工作树判定。票据为 `ticket/02-unify-log-redaction.md`、`ticket/03-bound-request-capture.md`，AC 为该 change `spec.md:65-66`。审查遵守仓根/`backend/AGENTS.md`、engineering-standards 的 SEC-003/SEC-005、TEST-001/003/004、common 模块入口。未运行 Maven、Docker、HTTP 或任何服务；下文“存在测试”绝不表示本候选通过。

## T-02 — AC-002：**现代码有真实凭据泄漏，必须修复；不能按无需施工取消**

**高优先缺口，生产调用可达。** `backend/wta-admin/src/main/java/org/namewta/web/listener/UserLoginSuccessListener.java:45,53` 把登录事件的 `tokenValue()` 放进 `UserOnlineDTO.tokenId`。`backend/wta-modules/wta-system/src/main/java/org/namewta/system/controller/monitor/SysUserOnlineController.java:33,83-92,124-137` 接受真实 tokenId 为 `/monitor/online/{tokenId}`、`/monitor/online/myself/{tokenId}` 路径变量；`frontend/packages/domains/system/src/monitor/index.ts:121-124` 真实调用会把该值拼进 URL。`backend/wta-common/wta-common-web/src/main/java/org/namewta/common/web/logging/SysLogFilter.java:237-241,280-282,299-305` 取原始 `servletPath` 作 `path` 写入 HTTP 事件，`SysLogEventWriter.java:26,65-70` 直接序列化写专用 HTTP logger。因而执行正常在线设备移除/强退即可使真实会话 token 出现在 HTTP sink。这不是把客户端已知秘密人为塞进任意元数据的泛化。`GlobalExceptionHandler.java:55-56,77-79,87-89,105-128,137-144,158-172,255-277` 多条错误路径又原样打印 `getRequestURI()`，若此请求触发异常，错误日志也复制同一 token。无当前运行验证，但源码链足以判定 AC-002“凭据不出现在 HTTP sink/错误日志”未满足。

`LogAspect.java:88-94` 当前优先写 `HandlerMapping.BEST_MATCHING_PATTERN_ATTRIBUTE`，路径变量以路由模板进入 `OperLogEvent.operUrl`；缺映射时用 `[unmapped]`。`SysOperLogServiceImpl.java:105-115` 对操作参数/结果再次调用 `LogSanitizer`，所以本次定位**不是**已证实 OperLogEvent/数据库泄漏。三个出口应共享一个安全路径策略，以防未来改动分叉；当前最小产品写集为已声明的 `wta-common-json/.../LogSanitizer.java`、`wta-common-web/.../SysLogFilter.java`、`.../GlobalExceptionHandler.java`，必要时 `wta-common-log/.../LogAspect.java` 仅作策略统一，不应改 API/前端在线会话合同或存储 token 机制。策略须精准掩盖上述两条真实 token 路径，同时保留 `/monitor/online/list` 与普通路径、操作者、耗时、失败状态。路径日志的变更应与认证路径脱敏规则保持一致；HTTP request 事件在 MVC HandlerMapping 之前发出，不能仅依赖当时不存在的路由属性。先加当前源码红灯：真实登录 token → DTO/在线会话 → 强退/移除 URL → HTTP sink 与异常日志；普通路径/审计元数据不退化。若新测试需要在当前票据根之外加入真实在线会话/Redis测试，先由 Lead 扩写集/owner；现有 common 测试与 admin logging 测试均已在 T-02 写集内。

`SysLogFilter.java:309-312` 的 `upstreamRequestId` 及 :159-170 的请求头按字段名选择性记录，客户端若主动在普通头中放 secret，也会流入日志；但现有签发/失败流程不会自动这样做，本预审仅建议对上游 ID 格式/长度加固，不把任意用户可控元数据当成独立 AC blocker。

**现有实现/AC 对照。** `LogSanitizer.java:21-55,64-116` 能对已知凭据字段、OAuth code、回调、非法或非结构化 JSON 做安全副本；`SysLogBodySanitizer` 不输出截断原文；`LogAspect` 先脱敏再限长，保留匿名事件、登录操作者及失败类型；`SysOperLogServiceImpl` 有入库二次保护；`SsoOAuthController.java:79-80` 关闭换票响应的操作日志正文；`SysLogFilter` 在签发路径省略响应正文，业务响应保持原 token。这些是当前源码可见合同，不消除上述 path sink。签名/加解密/SSE 的兼容需本轮实测。旧日志处置与凭据轮换由独立批准记录证明，不由 T-02 自动删历史数据或据此勾选。

**测试与当前验收。** 已声明测试类：`org.namewta.common.json.utils.LogSanitizerTest`、`org.namewta.common.log.aspect.LogAspectRedactionTest`、`org.namewta.common.web.logging.SysLogFilterTest`、`SysLogFailureRedactionTest`、`SysLogEventWriterTest`、`org.namewta.test.logging.OperationLogRedactionTest`、`LogRedactionHttpMySqlIntegrationTest`。旧真实 MySQL harness 为 change `evidence/run-t02-mysql.py`，历史 `T-02.md` 记录 33/33 定向及 6 HTTP 事件/3 操作事件/3 DB 行，但旧例只走签发、普通 JSON、失败，不覆盖真实 token 路由；历史默认 reactor 曾因其他票失败。修复后需固定新 candidate、隔离 MySQL 真正运行 canary 签发/失败及 token URL 回归，验证 HTTP sink、OperLogEvent、SQL 行、错误日志均零明文且非零审计事件；运行票据 §8 的 `cd backend && ./mvnw -pl wta-common/wta-common-json,wta-common/wta-common-web,wta-common/wta-common-log,wta-modules/wta-sso,wta-modules/wta-system,wta-admin -am test`、相关 OpenAPI/加解密/SSE 回归、默认 backend test 与 full/core 打包/清单（按交付门禁）；每条记录实际 cwd/命令/exit/测试数/skip，服务资源清理和 exact-clean source。T-30 最终承接跨票综合验证，不能拿旧绿代替本票当前结果。

历史实现 commit `4cfb7eea819c142ccdbcc29004a86868b86e2551` 是固定输入祖先（`git merge-base --is-ancestor` exit 0），旧 result `6c8764c...` 为整批，不是本轮 clean exact-HEAD 出口。旧 `T-02-checkpoint.json` 14 路径在 38032d2 均仍存在，但有 10 路径内容 hash 已变化；故旧测试不能无条件迁移为当前验证。修复前建议本票继续 ready/review，不得称 Done 或 cancelled。

## T-03 — AC-003：**核心实现存在，静态未见需重复施工缺口；当前验证未完成**

`CapturedRequestBody.java:39-69` 在读流前检查声明长度；未知/低报长度最多读 `maxBytes+1` 后抛 `RequestBodyTooLargeException`，只在完整捕获后设置唯一请求属性；下游 `openStream()` 只建新游标，`copy()` 防御性复制，`prefix()` 只取有界前缀。`RepeatableFilter.java:30-49` 对 JSON/+json 先捕获再进业务，超限写实际 HTTP 413；`OpenApiGatewayFilter.java:112-162` 在机器鉴权前通过 `ReplayableOpenApiRequest` 捕获，超限映射 413，验签 `authenticate` 使用原始 body；`OpenApiAutoConfiguration.java:127-134` 把它排在普通正文/日志/XSS 前。`RepeatedlyRequestWrapper` 复用已有 owner，`SysLogFilter` 仅取前缀，`XssHttpServletRequestWrapper` 另建有界清洗视图，不回写原签名字节。普通与机器默认独立 2 MiB，`RequestBodyProperties`、`OpenApiProperties` 和 `OpenApiStartupValidator` 拒绝零/负/越界；`application.yml` 仍分别绑定 `REQUEST_BODY_MAX_SIZE`、`OPENAPI_MAX_BODY_SIZE`。日志前缀另计限。SSE/普通 multipart 由 `SysLogMediaTypePolicy` 跳过正文缓存；XSS 仅处理 JSON，未重引入异步流缓存。静态代码支持 AC-003 各不变量；不据此宣称实际 HTTP、堆/GC 或生产容量达标。

已声明测试类：`org.namewta.common.web.filter.RequestBodyLimitTest`（声明、未知长度、+json、边界、共享 owner/XSS、日志 413、取消/流），`org.namewta.common.web.config.SysLogConfigTest`（配置/顺序），`org.namewta.test.openapi.assembly.OpenApiAssemblyContextTest`（机器预算和装配），`org.namewta.test.requestbody.RequestBodyHttpIntegrationTest`（11 个 Jetty/MVC/HMAC 真 HTTP 场景：固定/chunked 边界，伪签名大正文 413/小正文 401，真实签名逐字节，取消、上传/SSE、并发与堆探针）；`BrowserHttpsTransportIntegrationTest`、`LogRedactionHttpMySqlIntegrationTest` 属相邻消费者回归。真实 HTTP 类以确定性内存 nonce/session夹具运行，不等于 Redis/生产身份全旅程。旧 `T-03.md` 称 HTTP 11/11、模块 83/83、默认 814 项中 97 外部条件 skip、512 MiB 堆探针及 full/core bundle 通过；均仅是旧输入历史证据。当前必跑票据 §8 `cd backend && ./mvnw -pl wta-common/wta-common-web,wta-common/wta-common-openapi,wta-common/wta-common-encrypt -am test`，再运行包含 admin `RequestBodyHttpIntegrationTest` 的精确选集（确保非零、0 skip）、真实定长/chunked边界/伪签名及正常签名、当前堆/GC参数和 SSE/上传取消；适用默认 test、full/core bundle/清单及源码 clean source 时点由 Lead 集成记录。未获目标部署容量，本票只证有界性，不推生产并发数。

历史实现 commit `214de538267a60fa527b1213f7dd9139622f4a2e` 是 38032d2 祖先（exit 0）。旧 checkpoint 26 文件均存在，6 文件内容 hash 变化；其中 `FilterConfig/XssFilter/XssHttpServletRequestWrapper` 的差异仅注释删除，`application.yml`、`BrowserHttpsTransportIntegrationTest`、README 有后续变更，应纳入当前消费者/配置复验。未发现当前 T-03 核心捕获路径需再次实现；在 T-02 产品缺口关闭、当前 clean candidate 的 required HTTP/回归实际通过和 Goal 历史票处置证据齐备后，可由 Lead 裁决“无需新实现、取消重复施工并由 T-30 承接 AC-003”，不能现在标 Done/cancelled。T-03 blocked_by T-02，当前预审不解除依赖。

## 写集与命令结果

T-02 原写集已包含 common-json/web/log、SSO controller、system service、admin logging test；针对真实在线会话的新增生产事实调用方 `UserLoginSuccessListener`、`SysUserOnlineController` 和前端 service只读即可，最小修复无需修改它们。若当前测试需写 `backend/wta-admin/src/test/java/org/namewta/test/monitor/...` 或其他未声明根，须先修订票据写集；不可先写后报。T-03 当前未建议新产品写集；若复验发现缺陷，再限定到已声明的 common-core/web/openapi/encrypt、admin requestbody/openapi/security/logging 测试、application.yml/README；其他路径先登记。

本次只运行 `git show`、`git grep`、`git log`、`git diff`、`git merge-base --is-ancestor` 与静态 SHA-256 对比，均为只读；有若干 `git show` 查询不存在的候选路径返回 128，不影响已定位事实。Maven/Docker/真实 HTTP/数据库命令：**未运行**，无测试计数可宣称当前通过。未读取或输出真实凭据，未修改 repo/治理。
