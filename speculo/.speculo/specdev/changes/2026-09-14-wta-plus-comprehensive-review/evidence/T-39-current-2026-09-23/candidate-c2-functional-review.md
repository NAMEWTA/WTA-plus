# T-39 C2 functional/engineering incremental review

**Fixed point:** C1 `c2ab3d63225d2d1406aa9ecc9c072fecc83da297` → C2 `6e1d7f8e9f3d67495a361634779b7f7f050e68ab`, examined with `git diff C1...C2`. Only two test files change: `EnterpriseQueuedNotificationIntegrationTest.java` and `NotifyAtomicResultIntegrationTest.java` (37 additions, four deletions). No production, schema, mapper, API, Skill, or transport source changes. The C1 fixed-base review is `/tmp/wta-t39/candidate-c1-functional-review.md`; unchanged product behavior findings carry forward. I did not run Maven, Docker, JVM, browser, or supplier I/O.

**Functional verdict: C2 eight-class real gate passed; broader gates pending.** C1's actual owned run `/tmp/wta-t39/runs/92c675af47abf448/result.json` executed 117 tests across eight exact classes, with three failures, zero errors/skips, and complete owned cleanup. It remains a failed historical C1 gate. The three failing fixtures are precisely the two paths changed here. C2's independent owned run `/tmp/wta-t39/runs/0990e739cc0c555b/result.json` completed with exit 0: eight exact classes, **118 tests, zero failures, zero errors, zero skips**. Its source before and after was clean head `6e1d7f8e9f3d67495a361634779b7f7f050e68ab`, tree `602161ee794da46728f76a140b6617b9b3e010f8`. The Maven process group has no live members, all owned containers and captured anonymous volume are absent, both owned loopback ports are closed, and cleanup has no errors. I separately parsed all eight preserved fresh XML reports and verified their SHA-256 hashes against `/tmp/wta-t39/c2-real-reports/manifest.json`; class counts match the driver result.

## C2 test changes

* `EnterpriseQueuedNotificationIntegrationTest.expiredAssociationAndTypedUnsentTerminalCannotActivateButPermitResend` now supplies a single-target `NotifyTargetResult.unsentTerminal(...)` with the actual request ID. This is the typed no-acceptance result that can close `FAILED/DONE`; it keeps the original expired-association rejection, failed confirmation, unchanged binding and subsequent new submission assertions, and adds SQL checks that Delivery is `FAILED` and Outbox is `DONE`. Removing the artificial `max_attempts=1` fixture is correct: the typed terminal result, not retry-budget exhaustion, must explain termination.
* New `genericFailedWithoutSingleTargetRemainsUnknownAndCannotActivate` retains the formerly used generic `NotifyStatus.FAILED` with an empty target list as a separate negative case. It asserts confirmation stays `QUEUED`, binding is unchanged, Delivery is `UNKNOWN`, Outbox is `WAITING_RECEIPT`, and the provider is called exactly once. This preserves T37's uncertain-result fail-closed boundary and avoids teaching the old fixture that generic failure is known unsent.
* `NotifyAtomicResultIntegrationTest.uncertainResultCommitUsesDurableFactsToPreventDuplicateInbox` still triggers BEFORE/AFTER commit faults, checks the old durable budget/message facts, clears fault injection, starts a later normal dispatch on the **same thread**, and requires one more `AFTER_COMMIT` realtime push plus exactly two recipient relations. The C2 setup now changes Intent from previously terminal `DELIVERED` to `PROCESSING` after appending the second PENDING Delivery, with an immediate SQL-backed assertion. That makes the aggregate consistent before the next dispatch. No red/green assertion or commit-fault branch was removed or weakened; the new terminal guard is no longer asked to dispatch a contradictory fixture.

## Remaining evidence and limits

The first C1 run's per-class counts were Deadline 15/0, Enterprise 7/1 failure, Wake 1/0, Common Redis 2/0, Atomic 66/2 failures, ManualRetry 13/0, SMS 10/0 and Redis idempotency 3/0, with zero skipped throughout. These numbers remain tied only to C1. C2's **fresh** counts are Deadline 15, Enterprise 8, Wake 1, Common Redis 2, Atomic 66, ManualRetry 13, SMS 10, Redis idempotency 3, each with zero failures/errors/skips. The affected default/full backend, profile transfer, common Redis, and static gates remain outside this report and must be recorded independently before final ticket closure; the live eight-class result does not imply those gates passed.
