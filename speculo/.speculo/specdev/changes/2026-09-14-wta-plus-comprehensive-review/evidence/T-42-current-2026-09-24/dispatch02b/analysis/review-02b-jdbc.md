# T42 02B JDBC fault fixture — fixed-source independent review

Fixed product/test source: `6f5ba38ba7f562adebaad8191dd2288b493baac1`. Reviewed its three test-file delta paths only; the Mail27 owned run was in progress, so this report is a **static fixture verdict**, not a test-pass or T42 acceptance claim. No repository edit, build or service was performed.

## Finding — test-evidence blocker for both BEFORE cases

`OwnedAttachmentJdbcFaults.java:173-184,201-211,251-265` marks `AckPlan.fired=true` before `terminateOwnedSession(actual)`. If the killer connection cannot execute `KILL CONNECTION` (permission, connection error or timeout), that method throws `SQLException` before the target connection's real `commit()` call. Yet `hitExactlyOnce()` treats the BEFORE plan as successful from `fired && !beforeUnexpectedlyCommitted` alone. Cases #21 and #23 at `NotifyMailAttachmentIntegrationTest.java:844-849,896-900` then also see a SQL cause and zero rolled-back facts, and can falsely report that a physical commit was attempted after a successful server-side disconnect. Record independent `killSucceeded` and `realCommitEntered` markers and require both for BEFORE `hitExactlyOnce()`, retaining the DB rollback/READY/zero-sender assertions. A kill failure should make the test fail, not prove the fault boundary. This is a fixture-proof gap, not a demonstrated production defect; retain any current run's raw verdict and rerun the fixed fixture before calling the BEFORE evidence passed.

## Other fixed-source findings

- The hook targets the live `DynamicRoutingDataSource.getDataSources()` map and CAS-replaces only its `master` entry (`OwnedAttachmentJdbcFaults.java:51-66`). The 4.5.0 local `dynamic-datasource-spring` sources JAR SHA-256 `8d0725c9538d7ef6204cd670535808072e976c117090c49bc5bbffaed4b9f40a` confirms the map is the live `ConcurrentHashMap` and the routed connection is obtained through it. `ConnectionProxy.notify(commit)` invokes `connection.commit()`, so a wrapped physical connection intercepts the actual transaction commit rather than the no-op `ConnectionProxy.commit()`. The wrapper delegates all unarmed operations, uses current-thread role plus one-shot plan, and restores the original pool entry by CAS without closing the pool (`:71-105,113-168,233-243`). If a concurrent race worker cannot stop, #25 marks the fixture poisoned and leaves the pool wrapper in place rather than restoring beneath a live transaction; the owned process/container teardown remains the boundary. This is a fail state, not a success claim.
- `OwnedAttachmentSendReservationSqlProbe.java:22-55` locates the **SET** `send_reserved=?` binding index in one update, tracks actual PreparedStatement setters, requires typed Boolean true/numeric 1 and exactly one affected row, and excludes batches and unknown SQL shapes. `OwnedAttachmentJdbcFaults.java:144-160` stamps the MAIL target only after successful execution. Thus a copy/reserve/confirm update carrying `send_reserved=false` cannot by itself fire the final-send ACK plan. #23 enters via real `dispatchPort.dispatch(lease)`, requires the prior copy to reach READY with `sendReserved=false`, preserved refs, zero sender and zero Attempt after the physical final reservation rollback (`NotifyMailAttachmentIntegrationTest.java:883-912`). If the false update were intercepted, those facts would differ. The standalone private matcher tests prepared earlier are static/offline support, not a substitute for the pending live case.
- #21/#22 call the production `notifications.submit`, not a synthetic result-port method. AFTER invokes real `commit()` before throwing `SQLRecoverableException` and records `actualCommitted`; #22 requires exactly one persisted Intent/relation/outbox/ref and the same receipt on retry (`:858-880`). #24 calls real `dispatchPort.dispatch`, requires persisted `sendReserved=true` despite thrown ACK, no captured physical sender, then cancel/reclaim/new-token dispatch with refs retained and `releaseIfSafe=false` (`:915-943`). These are specific commit-uncertainty tests; they do not claim a real network ACK was dropped.
- Incidental #25's race plan holds A at real commit after its Intent INSERT, waits until B enters its real INSERT, then releases A and requires a post-conflict `FOR UPDATE` read, one receipt and one relation (`:946-988`). #26/#27 use separate Sa-Token `LoginHelper.login` fixtures to verify persisted-actor/user and Client mismatch on idempotent reuse, with valid other-actor source controls. They exercise the service boundary, not the public HTTP credential/permission matrix; the separate owned HTTP runner must establish that.

No other static fixture blocker found. The pending Mail27 result and exact clean source/owned cleanup must be checked separately. In particular, a passing current #21/#23 would remain insufficient physical-BEFORE evidence until the marker defect above is corrected.

## Fixed-source addendum: `43e5c2be2c0bea045a902271ff32a71a4c8166bb`

The only `6f5ba38..43e5c2be` path is `OwnedAttachmentJdbcFaults.java` (8 inserted/changed lines); production code, matcher and all 27 test methods are byte-identical. The BEFORE branch now marks `beforeDisconnectAcknowledged` only after `terminateOwnedSession(actual)` returns, marks `beforeDriverCommitCalled` immediately before invoking the real `actual.commit()`, and requires both plus `fired && !beforeUnexpectedlyCommitted` in `hitExactlyOnce()`. If KILL itself throws, neither marker is set, so #21/#23 fail at their plan assertion; the original false-positive route is closed. AFTER still requires `actualCommitted` and is unchanged. This is a **static closure of the fixture finding**. The 6f5 Mail27 green run is historical and does not retroactively prove its old BEFORE injection; the 43e5 Mail27 rerun and its source/cleanup evidence were pending at this addendum's time.

## Fixed `43e5c2be` execution evidence — later verification

I independently parsed `/tmp/wta-t42/runs/a36d6d66aa5e06bd/result.json` (SHA-256 `d3e8b7e7eb0eb7328001d6193ec54195faeafa656adcb90360a7d34582a11afe`) and its retained sanitized Surefire XML. The result records `acceptance=true`, Maven/runner exit 0, exact clean source before and after `43e5c2be2c0bea045a902271ff32a71a4c8166bb` / tree `0e560210843989a1118d813e33b683506227a7a3`. The retained XML digest matches the result's `sanitized_sha256`; its suite reports **27 distinct methods, 27 tests, 0 failures, 0 errors, 0 skipped**, and the method set exactly matches the result's expected manifest. Both BEFORE methods #21 and #23 are present and passed under the strengthened `hitExactlyOnce()` assertion; the test source digest in the result matches `git show 43e5c2be:.../NotifyMailAttachmentIntegrationTest.java`. `cleanup.errors=[]`, no live Maven group/owned processes or containers, both anonymous volumes absent, and all five recorded loopback ports closed. This closes the fixture-proof blocker for this fixed candidate's owned Mail27 gate. It does not establish unrelated full-application HTTP/OpenAPI or the separate eight-class regression gate, which Lead is running separately. The old 6f5 result remains historical and unaltered.
