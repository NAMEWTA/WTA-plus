# T45 owned MinIO policy probe — Jackson 3 principal branch review

Fixed product `be35584b0d8dcfbb560dc9f338c303dafc77fbfd`; read-only inspection of source and local Jackson 3.1.4 source JAR. The fresh owned probe `/tmp/wta-t45/policy-probe/runs/fb6caa8cfb7564b2/result.json` exited 0 with clean source/cleanup and returned `POLICY_READ=UNKNOWN/INVALID_POLICY`, while signed/public object HEAD and GET were ALLOWED. Its bounded policy shape says submitted policy equals returned policy, one statement, no Condition/Not* keys, recognized AWS `[*]` principal, `GetObject` action and bucket resource. No raw policy, credentials or provider text are reproduced here.

**Concrete source defect.** `AbstractOssClientImpl.publicPrincipal:1248–1263` calls `principal.asText()` at `:1252` **before** checking `principal.isObject()`. In `jackson-databind-3.1.4-sources.jar`, `JsonNode.asText()` delegates to `asString()`; `BaseJsonNode.asString()` reports a coercion failure when `_asString()` is null, and `ObjectNode` has no scalar `_asString()` override. An object-valued `Principal: {"AWS":["*"]}` therefore throws `JsonNodeException` before the intended AWS branch. `policyFacts:1243–1245` catches the exception as `INVALID_POLICY`, exactly matching the probe. Existing common unit policy fixtures use textual `Principal:"*"`, so they did not exercise this real provider shape.

**Minimum product correction:** in `publicPrincipal`, branch on `isTextual()` first and accept only literal `"*"`; otherwise require `isObject()` and exactly one `AWS` key. Accept that AWS value only when it is textual `"*"` or a **single-element** array whose sole element is textual `"*"`. Return false for all other forms, including multi-element array, nested object, numeric and unknown principal: those should become `COMPLEX_POLICY` under the existing caller, not be treated as public or as parse failure. Avoid `asText()` on ObjectNode/ArrayNode. This is private parsing logic; no public model, API, global mapper, permission or broad IAM interpreter change is needed.

Add a focused current-Jackson/JsonUtils policy test through the existing `diagnoseAccess` seam: object-form `Principal:{"AWS":["*"]}` with Allow/GetObject/owned resource yields `POLICY_READ=ALLOWED` and write UNKNOWN; negative nonpublic/multi-element/nested/numeric principal shapes remain UNKNOWN with `COMPLEX_POLICY`. Retain existing textual-star positive and Deny precedence tests. The real `OssStorageReadinessMinioIntegrationTest` must be rerun on the fixed source; its earlier failed run is not retroactively passing, and its later anonymous PUT negative controls and policy-unchanged assertion were not reached in that attempt.
