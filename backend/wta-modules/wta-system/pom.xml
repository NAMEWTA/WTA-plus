<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>org.namewta</groupId>
        <artifactId>wta-modules</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>wta-system</artifactId>

    <description>
        system系统模块
    </description>

    <dependencies>
        <!-- 通用工具-->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-core</artifactId>
        </dependency>

        <!-- 可选 Nacos 动态配置接缝 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-nacos</artifactId>
        </dependency>

        <!-- api模块 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-api</artifactId>
        </dependency>

        <!-- 接口模块 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-doc</artifactId>
        </dependency>

        <!-- 数据库服务 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-mybatis</artifactId>
        </dependency>

        <!-- 翻译模块 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-translation</artifactId>
        </dependency>

        <!-- OSS功能模块 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-oss</artifactId>
        </dependency>

        <!-- 统一通知契约 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-notify</artifactId>
        </dependency>

        <!-- 日志记录 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-log</artifactId>
        </dependency>

        <!-- excel-->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-excel</artifactId>
        </dependency>

        <!-- SMS功能模块 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-sms</artifactId>
        </dependency>

        <!-- 安全模块 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-security</artifactId>
        </dependency>

        <!-- web服务 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-web</artifactId>
        </dependency>

        <!-- 脱敏模块 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-sensitive</artifactId>
        </dependency>

        <!-- 数据库加解密模块 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-encrypt</artifactId>
        </dependency>

        <!-- 消息推送模块 -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-push</artifactId>
        </dependency>

        <!-- OpenAPI contracts and runtime ports -->
        <dependency>
            <groupId>org.namewta</groupId>
            <artifactId>wta-common-openapi</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

</project>
