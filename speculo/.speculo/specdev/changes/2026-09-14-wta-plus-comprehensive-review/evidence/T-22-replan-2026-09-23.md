# T-22 evidence

Current HEAD `ea124ded` already meets AC-022. No new implementation was required. This cancels only the repeated construction. AC-022 stays owned by T-30. The adjacent OIDC change was not modified.

## MySQL and Redis

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=NotifyAtomicResultIntegrationTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Dnotify.atomic.integration=true \
  -Dnotify.mysql.integration.url='jdbc:mysql://127.0.0.1:33131/namewta_notify_test_t22?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC' \
  -Dnotify.redis.integration.port=33132
```

Exit 0. The MySQL password was supplied only through the test's existing environment variable and is not recorded. Isolated MySQL `mysql:8.4.9` (`sha256:c36050afdca850f23cef85703f84c7531a5ae155a11b5ee1c60acb09937c4084`) on `127.0.0.1:33131`, database `namewta_notify_test_t22`, loaded `10-cde-base-ddl.sql`, `20-cde-job.sql`, `30-cde-workflow.sql`, `40-cde-ai.sql`, and `50-cde-base-dml.sql`. Redis `redis:8.6.3` on `127.0.0.1:33132`. Both containers were removed after the run.

`NotifyAtomicResultIntegrationTest`: tests=66, failures=0, errors=0, skipped=0, 28.99s. Logged connection failures are the injected commit faults (`BEFORE` kills the session, `AFTER` loses the acknowledgement). They are not test failures.

The cases that close AC-022 include: an attempt insert failure rolls the delivery back and leaves the claim recoverable; every in-app write stage rolls back the message, recipient, and result; a zero-row finish rolls back the delivery, attempt, and aggregate; any result-write failure rolls back the other rows; an expired owner cannot renew or finish until another worker reclaims; an old worker cannot overwrite a reclaimed completed result; two concurrent claimers produce one committed result; the same lease reservation creates one physical attempt. After a rolled-back attempt, provider I/O can run again and the test records that this is not an exactly-once claim. One inbox fact and one committed attempt remain.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase, operation `verify-affected-contract-and-quality-gates`, status passed. No production source edit.
