# Evidence T-07 — three hard gates + dual-surface completion (AC-024)

- Goal: Same-session live E2E on MySQL/Redis, backend **18080**, admin **4174**, home **4175**, sso-web **4176**, `captcha.enable:false`, user **WTA**. Must include SSO 管理 create+config, Client **已接入** (not red 没有接入), three gates, login SSO as **icon**. Token-isolation-only is not pass.
- Date: 2026-09-13
- Auth: CTO-t316s1. deploy-namewta-environment: local current-workspace Origin/callback matrix `4174/sso/callback`, `4175/sso/callback`, `4176/authorize`.
- superseded-r3 Evidence and old `sso-admin-config-*.png` (breadcrumb 客户端管理) are **not** this file.

## Commands (run twice after screenshot-wait fix)

```bash
# ports 3306/6379/18080/4174/4175/4176 open; java --spring.profiles.active=dev,local
cd /workspace/vp-dev/WTA-plus
bash scripts/sso-hard-e2e.sh
```

(`scripts/sso-hard-e2e.sh` → `pnpm exec playwright test --config frontend/playwright.sso.config.ts`)

| Pass | Exit | Results | Log |
|---|---|---|---|
| 1 | 0 | 2 passed (sso-admin-config + sso-three-gates) | `/tmp/grok-goal-0577f1e6ed19/implementer/sso-r4-e2e-1.log` |
| 2 | 0 | 2 passed | `/tmp/grok-goal-0577f1e6ed19/implementer/sso-r4-e2e-2.log` |

Earlier same-session passes (pre secret-on-create / wait fix) also exit 0; named logs above are the last consistent pair.

## Screenshots (absolute)

SSO Admin create + config:

- `/workspace/vp-dev/WTA-plus/temp/team/lead/e2e/sso-r4-admin-list.png` — 系统管理 / **SSO 管理**, 创建应用, 拿配置, exact callbacks
- `/workspace/vp-dev/WTA-plus/temp/team/lead/e2e/sso-r4-admin-create.png` — 创建 SSO 应用 + 精确回调
- `/workspace/vp-dev/WTA-plus/temp/team/lead/e2e/sso-r4-admin-config.png` — 请立即保存配置: `clientId` + `sso_secret（只此一次）`

Client own-app success (not red 没有接入):

- `/workspace/vp-dev/WTA-plus/temp/team/lead/e2e/sso-r4-client-success.png` — 客户端管理 **已接入** green tag

Three gates + icon:

- `/workspace/vp-dev/WTA-plus/temp/team/lead/e2e/sso-ac001-default-provider-path.png` — first social-row **W** circle (not primary text “WTA SSO”)
- `/workspace/vp-dev/WTA-plus/temp/team/lead/e2e/sso-ac001-admin-logged-in.png` — admin after SSO
- `/workspace/vp-dev/WTA-plus/temp/team/lead/e2e/sso-ac002-reuse-no-password.png` — home **档案中心** after SSO reuse (header **退出**, no 登录/注册, no warehouse password)
- `/workspace/vp-dev/WTA-plus/temp/team/lead/e2e/sso-ac003-client-isolation.png` — same logged-in home while admin token + home `clientid` is refused (`code !== 200`); page has no toast 没有访问权限

Copies also under `evidence/sso-r4-*.png` and `evidence/sso-ac00*.png`.

## AC mapping

| AC | Result |
|---|---|
| AC-001 icon first-party path | Pass (circle W + admin logged-in) |
| AC-002 reuse no warehouse password | Pass |
| AC-003 Token extras differ; cross-client refused | Pass (`code !== 200` + isolation shot) |
| AC-024 dual-surface + three gates | Pass: SSO 管理 (a)(b)(c) + 已接入 + three gates. Not red 没有接入. Not isolation-only. |

## Residual / blockers

- **No push / no CR** until Lead+CTO accept these shots. Do not claim product PASS for push.
- Historical `sso-admin-config-list.png` (客户端管理 breadcrumb) must not be used as Round4 create evidence.
