# T-35 candidate 2 功能/工程增量审查

**Result: request-changes.** 原始固定 base `8b758ea8074a63835659b9731e3a4c74c5c029c5`；candidate 1 `41882b54a2b644f7ae661b84205cfcc9d1b72886`；本次 candidate 2 `0b14311c9652b4d765c4db8c8898d28662eb5adb`，tree `61db0cf144ec2aa1d42cdf01c9cd81aaebb4eaf8`，direct parent 为 candidate 1。输入仅 `git diff 41882b5..0b14311 -- backend` 的五个路径及 `git show 0b14311:<path>` 固定源码。规范为本 change `spec.md:98` AC-035、`ticket/35-sms-cross-layer-snapshot.md` 第 4–10 节、原报告 N-02；沿用 `/tmp/wta-t35/review-functional-41882b5.md` 对其余 20 个未变产品路径的静态通过项。未读取 writer 后续脏工作树作为此候选，未运行 Maven、Docker、服务或改 repo。本报告只提供功能/工程轴，不借用其他审查轴结论。

## 尚须修复的发现

1. **High — `notifyClient.send` 入口早于真实供应商调用，Redis 幂等 ACQUIRE 故障仍被误判为外部未知并等待不存在的回执。** `backend/wta-modules/wta-notify/src/main/java/org/namewta/notify/service/runtime/DispatchNotificationService.java:130-131` 在进入 `NotifyClient.send` 之前先把 `smsClientEntered=true`。生产 `NotifyClient` 是 `NotifyDispatcher`（`backend/wta-common/wta-common-notify/src/main/java/org/namewta/common/notify/config/NotifyAutoConfiguration.java:69-76`）；其 `NotifyDispatcher.send:73-78` 在 `adapter.send:98-100` 之前先调用 Redis 幂等 `beginIdempotency`。`NotifyIdempotencyCoordinator.begin:30-41` 的真实 Redis 异常包装成 `NotifyIdempotencyUnavailableException(phase="ACQUIRE")`，它是 `RuntimeException`，尚未调用供应商。candidate 2 的 `DispatchNotificationService.java:170-180` 因 `smsClientEntered=true` 走 `UNKNOWN/DISPATCH_ERROR`，随后 `NotifyDispatchResultService.java:86-95` 将任务放入 `WAITING_RECEIPT`，该请求没有供应商消息/回执可等。这仍违反 AC-035 的“本地错误无 WAITING_RECEIPT”以及 N-02 的执行阶段本地错误边界。满足条件：只针对能够证明供应商尚未进入的 typed `ACQUIRE` 路径做确定的本地失败或有界恢复；与 `NotifyIdempotencyUnavailableException(phase="COMPLETE")` 严格区别，后者可能已发生真实供应商 I/O，必须保守维持 UNKNOWN。补真实 Dispatcher + 失败 idempotency store（或隔离 Redis 故障）的反例：supplier 调用 0、Outbox 不进 WAITING_RECEIPT，并保留 COMPLETE/供应商 I/O 歧义不盲目重发。不得把整个 `notifyClient.send` 内的 RuntimeException 一律归本地可重试。

## candidate 1 两项阻断的关闭程度

- **发送前配额异常：代码层已修正，待当前候选实测。** `DispatchNotificationService.java:87,130,170-180` 对进入 NotifyClient 前的 SMS 异常设置 `FAILED/PREPARATION_RETRYABLE`。该 code 不在 `NotifyDispatchResultService.configFailure:151-156`，故结果事务走 `READY`，`nextAttemptAt` 指数退避、`maxAttempts` 达上限后 `DEAD_LETTER/FAILED`（`:86-100`），不会落入 `WAITING_RECEIPT`。`NotifySendPlanner.java:125-160,193-208` 在配额异常时逆序尝试释放已持有的每一级；即使释放抛异常，也继续处理其他 held key，将清理异常挂到原异常后重抛。新增 planner 单元用例覆盖第二级额度异常释放一级额度及原异常保留；新增 Dispatch 单元用例覆盖无供应商调用且任务可重试。第六条真实集成 `NotifySmsDispatchIntegrationTest.java:343-365` 使用生产 RedisQuotaAdapter、真实 DAO/结果事务和坏模板配额键验证 account 配额归零、第一次 READY/第二次恢复 ACCEPTED；但实际运行结果尚未提供。当前 finding 1 指出此修复的 `NotifyClient` 内部前置阶段仍有缺口。
- **六 SQL fixture 唯一键冲突：源码已修正，待当前候选实测。** 测试现在读取固定 seed `auth-captcha/SMS` 的 `binding_id=2100630000000000002`，要求 `account_id` 原本为空，再以带条件更新精确替换账号/模板/映射（`NotifySmsDispatchIntegrationTest.java:190-205`），不再重复 INSERT；`@AfterEach:208-239` 先删本例 intent 相关记录，恢复原 seed 五个被改字段，再按随机 account ID/config key 删除本例账号与 Redis 键。两个“绑定撤除”场景只把 seed 的 `account_id` 置空，留给 cleanup 恢复。`@Execution(SAME_THREAD)` 保证类内不并行覆盖固定 seed。使用真实 `auth-captcha` 场景保留 `NotifySceneCatalog` 的 `code/expireMinutes` 必填变量校验，未以未知随机场景放松合同。失败时断言非一行/seed 非预期会显式失败；最终由 driver 删除仅双 label 自有的隔离容器。第六条测试增量使当前验收目标成为精确 Surefire 类 `tests=6, failures=errors=skipped=0`，本静态审查不预填通过。

其余未变的 20 个产品路径——真实短信参数与非空逻辑快照、common 下层空快照负例、敏感事件副本、日志与管理投影、Provider 原值仅内部保存、旧 FULL 投影兼容及消费者调用链——沿用 candidate 1 的静态审查结论；这不替代 Lead 对本 candidate 2 的 38 类/真实六例的实际验收。第六例直接把 READY 任务设置为 PROCESSING 以演练恢复后的 dispatch，未通过正常 Outbox claim 时间门禁；有界退避/上限仍须由结果事务现有测试和源码验证，不能用该一例声称真实调度器时钟已验。
