# 当前 change 的决定

## ADR-001：缩减故障域，不堆叠兜底

Status: accepted。来源 USER-DECISION:2026-09-22 与原审计。

保留现有模块边界和数据库 Outbox；收件箱与 SSE 配置分离；IN_APP 采用本地事务及数据库唯一约束，外部 I/O 保留短事务领取/结果与未知语义。OSS 本地配置、实际对象 I/O、可选只读诊断各自负责其事实，不以诊断快照卡住全部调用。接受无法从低权限账号证明完整桶策略的限制，不自行实现完整 IAM 判定器。

这些决定在本 change 内替代原来“诊断未通过则禁止读写”和“所有渠道异常都进入 UNKNOWN”的实现合同；永久知识在 A 阶段经真实证据提升，未提前改写历史 ADR。

## ADR-002：同版基座切换与保留审计

Status: accepted。来源当前用户请求。

不保留废弃参数的静默兼容，不新增双写或兼容模块；同时更新所有仓内消费者和测试。SQL 只改既有六文件基座。原报告按字节保留；修复和已证伪/不适用项通过完成报告说明，不改写原始审计。SSO 当前实现不属于本次修复；保留可选授权服务器、内外 IdP 同流程的设计边界。
