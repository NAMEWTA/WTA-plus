# T-02 C1 functional / engineering review

- Fixed base `c16966167526f9b6ab6eb213265034b3bbe53e46`, test-only red checkpoint `8a88965363ca295122efadabf0bc97e99c6704c4`, C1 candidate `617a3693ddb2897851ab46d4131690898df6a690`, tree `a8d1181dfc950d6edf6d94a9cc1c42e10e88da00`. Inspected immutable `git diff c169661...617a369` and fixed `git show` content for the seven product paths (three production, four test), AC-002/T-02 revision 161 and dispatch packet. No Maven, Docker, HTTP or repository write by this reviewer.
- **Verdict: pass for the reviewed product behavior and seven-class real HTTP/MySQL gate; eleven affected consumer classes pending.** No confirmed product blocker in the fixed diff. The test-only red run `/tmp/wta-t02/red-behavior-assessment.json` is valid and specific: 35 tests, 1 failure, 0 error/skip; the genuinely issued and exercised token remains in HTTP audit at canary line 336. That red stopped before later error/OperLog/DB assertions and does not establish their old behavior. Lead has since supplied `/tmp/wta-t02-c1/real-http-mysql-counts.json`: clean exact `617a369` before/after, seven classes / 39 tests, 0 failures/errors/skips, both real canary methods executed, all owned container/volume/process/port cleanup clear. The 11-class consumer regression was still running when this review was updated and is not asserted passed.

## Contract and layer trace

- `backend/wta-common/wta-common-json/src/main/java/org/namewta/common/json/utils/LogSanitizer.java:20,117-151` adds `tokenId` to the existing normalized sensitive-field set and a narrow online-operation path transformer. It masks one token segment for `/monitor/online/{tokenId}` and `/monitor/online/myself/{tokenId}`, including trailing slash or matrix suffix, while preserving `/monitor/online/list`, the parent route and unrelated paths. Both methods return **log copies**; the original request path and response object are not transformed. The normalized field rule reaches nested list/get responses and leaves `userName`/ordinary `code` visible.
- `backend/wta-common/wta-common-web/src/main/java/org/namewta/common/web/logging/SysLogFilter.java:305,359` applies that shared path policy only when creating request/response events. It still uses the original path for body policy/transport and does not rewrite servlet request, returned token or HTTP status/duration. `GlobalExceptionHandler.java:48-188,258-283` uses the same policy for URI text across its logging handlers while retaining the original URI for SSE path classification and preserving the prior HTTP response/error code behavior. The context-path guard returns `[REDACTED]` when the URI/context relation is inconsistent instead of logging ambiguous raw input. This respects common-json → common-web dependency direction; no new business-module dependency is introduced.
- The new `LogRedactionHttpMySqlIntegrationTest#onlineDeviceTokenFromRealIssuerNeverReachesHttpOrErrorAudit` at candidate lines 215-405 creates valid Sa-Token sessions through `LoginHelper.login`, checks them before use, sends two GET responses containing the live token, then makes two HTTP POSTs to actual `SysUserOnlineController` methods via LogAspect proxy. It verifies forced logout, self removal, HTTP request/response event absence, two OperLogEvents, actual isolated MySQL rows, and two GlobalExceptionHandler formatted errors without either token. It also checks online user names, route templates, nonblank SQL titles, nonnegative duration, and that the real GET response still contains the token. The older class method still checks ordinary fields, failed operation status and auth token response. This test uses `T02_MYSQL_PASSWORD` directly from the child environment, random owned database guard, and only the minimal `CREATE/DROP/INDEX/SELECT/INSERT` grants.
- `LogSanitizerTest`, `SysLogFilterTest`, and `SysLogFailureRedactionTest` add narrow path, nested `tokenId`, context-path, unchanged original request/response, and ordinary-route cases. The previously existing signing/encryption/SSE/response tests are in Lead's 11-class consumer selector `/tmp/wta-t02-c1/consumer-selection.json`; they were not run by this reviewer and their result was pending at report update.

## Evidence limits and residual boundaries

- The canary GET handler serializes prepared `SysUserOnline` rows carrying **real issued tokens**. It does not call production `SysUserOnlineController.list/getInfo`, whose Redis lookup and permission annotations need a larger running app; it tests the actual `SysLogFilter` response-copy behavior at the production URLs. The POST handler calls actual controller methods through AOP and live Sa-Token, but manually supplies Spring's matched route-template attribute and does not run a full Spring MVC permission interceptor. These are test-boundary facts, not claims of Redis/ACL integration. The product patch changes no controller, session issuance, permission or response contract.
- URL masking intentionally covers valid single-segment online operation routes. Malformed multi-segment paths and arbitrary user-controlled metadata are outside revision 161's narrow behavior, not proof of universal secret scrubbing. Existing operation-log route-template handling is reused, not newly repaired here.
- Lead owns the frozen v2 runner SHA `cff4e66c3f108c71a212623fdab88f88783415f977d33803ed2bf2180024f452`. The required fresh exact seven-class run has now passed 39/39 with both canary methods present and 0 skips, proving the test's HTTP/error/OperLog/SQL assertions on its stated seams. Separately positive zero-skip 11 affected consumers remain pending. Any setup/fixture failure must be classified separately from the historical behavior red. No legacy log deletion or credential rotation occurs in this C1 product diff.
