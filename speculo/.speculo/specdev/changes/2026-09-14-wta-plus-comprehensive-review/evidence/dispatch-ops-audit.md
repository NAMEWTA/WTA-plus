# Dispatch ops_audit-20260923-01

operation=dispatch；task_kind=research；delivery_channel=native；Lead=/root；provider=gpt-6-sol/xhigh；owner=/root/ops_audit；固定代码bafd5d5（HEAD dd8179e治理更新不影响输入）。

用户明确提供三个/srv/ops目录并要求按部署替换调整。子代理只读该授权范围的Compose/env/容器/数据库连接/账号及OSS配置，不写仓库或部署，不轮换/重启/数据库写入。敏感值不输出，不source env。允许/tmp/wta-ops-audit保存0600脱敏结果。输入AGENTS、部署skill与适用refs、T-32、Goal；输出精确身份、旧值有效性布尔、共享影响面、备份/验证/恢复建议与局限。Lead独立核对，环境写入和Gate状态不委派；发现归属不明停相关调查。容器其他项目仅看公开清单，不访问其数据。T-32本地票已完成，外部Gate仍pending。
