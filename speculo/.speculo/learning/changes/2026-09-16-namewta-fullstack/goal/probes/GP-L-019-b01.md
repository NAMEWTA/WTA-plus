---
artifact: learning-goal-probe
probe_id: GP-L-019-b01
lesson_id: L-019
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-019 / b01

审问对象是已写成的 Lesson `lessons/L-019-system-domain-service.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 frontmatter 声称格子：`A:createSystemService.users,roles,menus,departments,posts,clients,userTypes,ssoApps`（OBJ-19）。矩阵 (a) **同一行**还挂着 `identity, resources`。chain 覆盖列与 claimed 八口对齐（含 `ssoApps`）。OBJ-19 原文只点前七个名字，没有 `ssoApps` / `identity` / `resources`。矩阵 (b) **没有** `createSystemService` 行。`identity` 两 URL 已由 L-014 借用；`resources` 是同包另一函数 `createSystemResourceService`（L-021 正文却写「全表仍归 L-019」）；`publicUsers` 不在矩阵行；`createOpenApiService` / `createMonitorService` / `createSystemWebDomain` 是后课。Goal：「同一资源 list/get/add/edit/remove 必须能指」；矩阵按工厂聚合一行，课内仍要逐方法解释。

抽查锚点（2026-09-16）：`frontend/packages/domains/system/src/service.ts`（`createSystemService` / `SystemService` / `CrudService` / `segment`）；`src/index.test.ts`（71 条 `it` 标题仍写 seven slices）；`src/transport.ts`；`src/user/public.ts`；`src/resource-service.ts` `createSystemResourceService`；`src/index.ts` 同包再导出 `createOpenApiService`；`frontend/packages/platform/contracts/src/index.ts` `HttpMethod`；`frontend/apps/{admin-web,home-web}/src/application/services.ts`；`frontend/apps/admin-web/src/components/RoleSelect/index.vue`；`frontend/packages/web-domains/system/src/{runtime.ts,index.ts,user/UserPage.vue,user/UserCredentialDialogs.vue,client/ClientPage.vue,sso-app/SsoAppPage.vue}`。

## Q1 澄清

- objective_id: OBJ-19
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-19；chain L-019 覆盖列；L-019 `claimed_cells`；矩阵 (a) 一行十个名字；矩阵 (b) 无本课符号；Goal「工厂聚合一行、课内逐方法」；课开篇「八口 ≠ 工厂上每一个键」
- difficulty: medium

课把厨房收成「八个抽屉」。计划里至少有四套可数对象，课没有声明以哪一套当作闭合的操作定义：

1. **OBJ-19 原文。** `course.md`：指着 `createSystemService` 的 `users/roles/menus/departments/posts/clients/userTypes` 说明前端如何映射 HTTP。七个名字。没有 `ssoApps`，没有 `identity`，没有 `resources`。
2. **chain / 课 `claimed_cells`。** 八口，把 L-018 的厨房孪生 `ssoApps` 写进本格。课自己警告：不要因为 OBJ 没写就把它推走。
3. **矩阵 (a) 一行。** `createSystemService.users, roles, menus, departments, posts, clients, ssoApps, userTypes, identity, resources`。十个名字、一个状态格。Lead 若按行标 covered，会把 L-014 已借过、本课声明不口试的 `identity`，以及另一份文件 `resource-service.ts` 的 `resources`，一并盖章。
4. **Goal 逐方法。** 八口磁盘公开方法 74 个（`users` 18、`roles` 14、`menus` 8、`departments` 7、`posts` 7、`clients` 9、`ssoApps` 5、`userTypes` 6）。课的口试口径是「按资源分组看形状 + 例外，不默写七十行」。71 条快照是 URL 对照，不是方法全集。

问：本课 (a) 闭合标准是「八口各自的前缀 / list 形状 / 写动词 / 特殊枪」，还是矩阵那一行十个符号都必须在本课标 covered？若是前者，Lead 是否必须先把 `identity` / `resources` 从该行拆走（或标 `covered-by-parent` / 改挂 L-021+），才能诚实覆盖八口？`ssoApps` 若只因为 OBJ-19 没点名就不算，chain 与 L-018 孪生对照是否作废？逐方法若等于 74 个 URL 默写，38 分钟课是否已经用「形状+例外」满足 Unistructural，还是必须再开函数全表？

## Q2 证据

- objective_id: OBJ-19
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: `service.ts` 31–32 / 34–120 / 122–287；`index.test.ts` 30–201 / 203–256；`transport.ts` 17–29；`user/public.ts` 65–84；`resource-service.ts` 240–251；admin/home `services.ts`；`UserPage.vue` 576–631 / 996–1008；`UserCredentialDialogs.vue`；`ClientPage.vue` 379–386 / 584–591；`SsoAppPage.vue` 153–190；`RoleSelect/index.vue` 104 / 178 / 248；`web-domains/system/src/index.ts` 64–67；`SysUserTypeController.changeStatus` / `listByUser` / 各 `export`
- difficulty: high

课的机制链哪些能在工作树直接复现，哪些已被磁盘削薄或证伪？

**仍能复现：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂 `createSystemService(http: HttpClient)`，只认 `HttpRequest`，返回 `Object.freeze` 的 `SystemService` | `service.ts` L122–123 / L181–287 |
| 口试八口都是 freeze 对象；另三键 `identity` / `resources` / `publicUsers` 同函数创建 | L182–191；`resources: createSystemResourceService(http)`；`publicUsers = createUserQueryPort(http)` |
| `segment`：数组或单值 → `encodeURIComponent` → 逗号连接 | L31–32；71 条里 `'client/1'` → `client%2F1` |
| 本地 `request` 把失败原样抛出，不填空表 | L123；`index.test.ts` L213–217 `rejects.toBe(error)` |
| `users` 前缀 `/system/user`；分页 `PageResult`；`get()` → `GET /system/user/`；`get(id)` 拼 id；`updateAuthRoles` 走 **params**；解锁 **GET** `/unlock/{id}`；永久重置 PUT + `isEncrypt`；资料三枪 `/profile*` | L192–252；71 条含 `get()` 与 `get(id, clientId)` |
| 候选/临时：POST、`Cache-Control: no-store`、投影丢掉 `ignored`；非正整数 `expiresInSeconds` 抛「用户凭据响应不可用」 | L212–228；`transport.ts` L17–29；`index.test.ts` L219–256 |
| `roles`：分页 CRUD；`cancelUser` **data**；`cancelUsers` / `selectUsers` **params**；`departmentTree` → `/system/role/deptTree/{id}` | L158–179 |
| `menus.list` 是数组不是 rows；`tree` / `roleTree`；`cascadeDelete`；**没有** `getRouters`（在 `identity.loadMenus`） | L253–265 vs L184 |
| `departments.list` 数组；`excludeChildren`；`delete` 单 id；`options` 拼 `?deptIds=` | L266–276 |
| `posts.options` 条件在 **params** `{ deptId, postIds }`；`departmentTree` → `/system/post/deptTree` | L277–286 |
| `clients.update` PUT 根路径；`changeStatus` body 用门牌 `clientId`；`bindSsoAccess` POST `/sso/bind`；`rotateSsoSecret` POST `/sso/rotate-secret`；`options` 包装 `list({ pageNum: 1, pageSize: 1000 })` 返回 `rows ?? []` | L125–139；`index.test.ts` L203–211 |
| `ssoApps` 五枪：list/get/add、update **POST `/system/ssoApp/update`**、`rotateSecret` POST `/rotateSecret`；无 delete / changeStatus / bind | L140–148 |
| `userTypes.options` 独立 GET `/system/userType/options` | L149–157；admin `notificationDirectory.userTypes` → `systemService.userTypes.options()`（`services.ts` L87） |
| `identity.load*` 解开 `.data`：`GET /system/user/getInfo`、`GET /system/menu/getRouters` | L182–185 |
| `HttpMethod` 五值含 `patch`；本文件八口只用 get/post/put/delete | `platform/contracts` L19；`service.ts` 无 `'patch'` |
| `CrudService` 是内部 interface；`roles`/`posts`/`clients`/`userTypes` 交差；`users`/`menus`/`departments`/`ssoApps` 手写 | L113–120 vs 各字段类型 |
| 71 条 `expect(cases).toHaveLength(71)`，从 `clients.list` 走到 `posts.departmentTree` | `index.test.ts` L30–201 |
| 71 条不含 `rotateSsoSecret`、两支凭据 POST、`clients.options` | 71 条 cases；凭据另测；`options` 另测 rows |
| admin 组合 `createSystemService(domainHttp)`；home 同工厂只喂 `identity`；home-web 零 `systemService.users` | admin `services.ts` L34 / L48；home L8 / L13 |
| `UserPage` 别名 list/get/add/update/delete/resetPassword/changeStatus/unlock/departmentTree；导入地址拼 `/system/user/importData`；导出 `runtime.download('system/user/export')` | `UserPage.vue` L576–631 / L996–1008 |
| 凭据对话框走 candidate / resetPassword / issueTemporaryPassword | `UserCredentialDialogs.vue` L139 / L159 / L176 |
| `ClientPage` 解构含 `bindSsoAccess`、**不含** `rotateSsoSecret`；导出 `system/client/export` | L379–386 / L584–591 |
| `SsoAppPage` 调 list/get/add/update/rotateSecret 五枪 | L153–190 |
| `RoleSelect` 直接 `import { systemService }`，打 `roles.list` / `roles.options` | `RoleSelect/index.vue` L104 / L178 / L248 |
| web-domain 权限含 `system:ssoApp:remove`，工厂没有 `ssoApps.delete` | `web-domains/system/src/index.ts` L67 vs `service.ts` L67–73 |
| `publicUsers` 同 URL 投影 `UserSummary`，本格不 claimed | `user/public.ts` L65–84 |
| OpenAPI / monitor 是另一座工厂 | `index.ts` 导出 `createOpenApiService`；admin `createMonitorService` from `@namewta/domain-system/monitor` |

**课写过、磁盘已削薄或证伪：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「71 条覆盖八口里大多数 URL，但不包括 `rotateSsoSecret`」——暗示缺的只是那则 `cases` | 全仓前端 **只有** `service.ts` L134–135 出现 `rotateSsoSecret` / `/sso/rotate-secret`。没有页面调用，也 **没有** 另一则 `it`。缺的不是「71 里漏了一行」，是厨房有枪、零快照、零消费者 |
| 「工厂缺口：用户 export/import、客户端 export」 | 页面还用宿主 download 打 `system/role/export`、`system/post/export`、`system/userType/export`（`RolePage.vue` L911、`PostPage.vue` L397、`UserTypePage.vue` L298）。后端另有 `SysUserTypeController.changeStatus`（PUT）与 `listByUser`（权限 `system:user:query`）；工厂 `userTypes` 类型因 `CrudService.changeStatus?` 可选而不报错，freeze 对象上 **没有** 这两枪；`UserTypePage` 也不调。缺口表写窄了 |
| 「整座服务和每个抽屉都 `Object.freeze`」 | 八口与外层确实 freeze。`resources` 的 freeze 发生在 **另一函数** `createSystemResourceService`（`resource-service.ts` L240–251），不是 `service.ts` 手写抽屉。把 `resources` 说成「同工厂一个抽屉」会掩盖它是独立导出工厂 |
| 「只有 `identity.load*` 拆 `.data`，`clients.options` 拆 `.data.rows`」 | 八口里凭据两枪也会改写 `data`（投影后仍包 `ApiResponse`）。不是第三种「拆信封」，但「只有两处动返回值」不精确 |
| 口试图把 `resources` 画成虚线盒子，留给 L-021 起 | L-021 开篇：「不宣称拆 `createSystemService.resources` 全表（**L-019**）」；L-022：「前端厨房（格子仍归 **L-019**）」。两课把厨房格推回来。矩阵一行又把 `resources` 和八口绑死。不是源码反例，是覆盖所有权环 |
| 71 条 `it` 标题 `all seven slices` | 课称八口。磁盘把 clients+ssoApps 算进同一则 cases，标题仍写 seven。课没引用这句标题，但对「八口=测试切片数」的读者是陷阱 |

问：在八口前缀、分页/数组、`ssoApps.update` POST、`clients.options` 包装、`users.get()` ≠ getInfo、params/data 例外已经能互证的前提下，(a) 是否还要把「工厂缺口」写成完整对照表（五处 export + `userTypes` 两枪 + 零测试的 `rotateSsoSecret`），才算把「有窗无抽屉」讲完？`resources` 所有权环不补，Lead 能不能标这一行 covered？

## Q3 假设与反例

- objective_id: OBJ-19
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: OBJ-19 vs claimed 八口 vs 矩阵十名；`index.test.ts` 71 vs `rotateSsoSecret` 零引用；`userTypes` 无 changeStatus/listByUser；`clients.options` 非 `/options`；`ssoApps.update` POST；`menus.tree` vs `identity.loadMenus`；`CrudService`；FE-CRUD-002 vs `method: 'put'`；`createSystemResourceService`；home 只喂 identity
- difficulty: high

课的隐含假设若反转，合格读者会把哪张纸条贴错抽屉？

1. **「矩阵一行十个名字 = 本课全覆盖。」** 课声明不口试 `identity` / `resources`。按行标 covered 会把 `createSystemResourceService`（dict/config/oss/social）和 L-014 的 identity 口一次性结案。
2. **「OBJ-19 没写 `ssoApps` = 那口不算。」** chain / claimed 含它。反例就是 `ssoApps.update` POST `/update` 对 `clients.update` PUT——课自己的对照题。
3. **「`createSystemService` 的方法都在 71 条里。」** 74 个八口方法里，`rotateSsoSecret`、candidate、temporary、`clients.options` 不在那则 cases。后三支另有测试；`rotateSsoSecret` 全仓无第二处。
4. **「测试标题 seven slices = 只有七口。」** cases 数组含 `ssoApps` 五枪。七是旧切片口头禅，不是矩阵。
5. **「`users.get()` 就是登录后的 getInfo。」** URL 是 `/system/user/`，返回仍是 `ApiResponse`；getInfo 在 `identity.loadInfo` 且已拆 `.data`。
6. **「`menus.list` / `menus.update` 管动态路由。」** 路由是 `identity.loadMenus` → `/system/menu/getRouters`。管理树是 `menus.tree` → `/treeselect`。`tree()` 无参时仍发送 `params: { clientId }`（可能是 `undefined`），不是省略 params。
7. **「所有 `options` 都打 `/optionselect`。」** `clients.options` 打 `/system/client/list`；`userTypes.options` 打 `/options`；`posts.options` 用 params 对象。
8. **「八口都是 `CrudService`。」** `users.get` id 可空；`menus`/`departments` list 不是 `PageResult`；`ssoApps` 无 delete 且 update 非 PUT。`posts`/`userTypes` 因 `changeStatus?` 可选而「交差成功」，不表示后端没有或页面能调 `userTypes.changeStatus`。
9. **「工厂没有的枪后端就没有 / 页面也没有。」** 五处 export 页面走 `runtime.download`。`system:ssoApp:remove` 在 manifest 与菜单 SQL，厨房无 delete。`userTypes.changeStatus` / `listByUser` 在 Controller，页面与工厂都无。
10. **「`clients.rotateSsoSecret` 缺是因为厨房没写。」** 厨房有；`ClientPage` 不调；SSO 页走 `ssoApps.rotateSecret`。
11. **「变更已经全部 POST，因为 FE-CRUD-002 / API-005。」** 八口存量 PUT/DELETE 与 classic 窗对齐。旁证：`resource-service.ts` 里 OSS 配置已经 POST——那是 `resources` 文件，不能拿来证明八口已迁移。
12. **「home 调用了 `createSystemService` = home 组装了八口管理页。」** 产品只把 `.identity` 喂给登录。
13. **「`publicUsers` 是 `users` 别名。」** 同 URL，投影不同；矩阵未点名。
14. **「OpenAPI / monitor / 字典也在这座工厂的口试名单里。」** 另函数 / 另 subpath / `resources` 键。

问：读完后仍说不出的，是不是「八个抽屉的名字」，而是 **矩阵十名里哪八个算本格、71 是否等于方法全集、`options` 三种信封、`userTypes` 缺哪两枪、`rotateSsoSecret` 有枪无客**？

## Q4 视角

- objective_id: OBJ-19
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a) 一行；OBJ-19 vs L-014/L-021/L-022；`createSystemResourceService`；`createSystemWebDomain`；FE-CRUD-002；`runtime.download`；home `services.ts`
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于这八口 (a)？

| 视角 | 多出来的边 | 是否本课格子 |
| --- | --- | --- |
| 矩阵维护者 | 一行十名 vs claimed 八口 vs OBJ 七名；L-021/L-022 把 kitchen 推回 L-019 | **是** 闭合定义。不拆行 / 不改挂，无法诚实标 covered |
| 厨房映射值班员 | 八口 74 法的 url/method/params|data；分页袋 vs 数组；`segment`；三种 options | **是** (a)。口试按组可以，缺口表必须写全「有窗无方法」 |
| L-014 身份 | `identity.loadInfo/loadMenus` 两 URL 已出现在 IdentityAccessService.getInfo/getMenus | **否** 再认一遍。本课对照「不要和 `users.get` / `menus.tree` 弄混」一句即可。矩阵符号建议 `covered-by-parent` 或拆行 |
| L-021+ 资源柜 | `createSystemResourceService`：`dictData` / `dictTypes` / `configs` / `oss` / `ossConfigs` / `social`；部分变更已是 POST | **否**。同包另一工厂。L-021 声称格子是三份 Controller，不是 `resources`；所有权应改挂，而不是塞进 38 分钟八口课 |
| L-022 资料页 | `users.profile*` 的旧密码 / NPE / Client 角色组 | URL 三枪 **是** `users` 抽屉。改密策略 **否**（L-022） |
| web-domain / L-020 | `createSystemWebDomain` 只吃 `runtime.service`；权限清单含 `ssoApp:remove`、user import/export | 缺口一句 **是**。导航 host / manifest 全表 **否** |
| 宿主旁路 | `runtime.download` / 拼出来的 upload URL | 工厂没有 export **是** (a) 缺口。下载实现 **否** |
| API-005 / FE-CRUD-002 | 目标合同变更 POST；八口仍 PUT/DELETE | 盘点现状 **是**。发动 Ratchet **否** |
| home 产品组合 | 同工厂只喂 identity（L-008 已 covered） | 一句对照 **可以**。不再认 `A:home-web.services` |
| RoleSelect / 厅堂组件 | App 组件可直接打 `systemService.roles.*` | 证明厨房是唯一写 URL 处 **是**。组件树 **否** |
| `publicUsers` | 通知选人投影 | **否**。矩阵未列 |
| 矩阵 (b) | 凭据投影失败抛错、options `?? []`、request 不吞错 | 无本课 (b) 行。当作 (a) 映射副作用即可，不要发明性状格 |

问：Lead 若凭「八口形状表都在课文里」把矩阵该行标 covered，`identity`/`resources` 是否仍因「同行未拆 / L-021 反向推责」而必须先改矩阵？若 (a) 只要求八口 HTTP 形状 + 完整有窗无抽屉表，本课是否已经够，只差把缺口写全、把所有权表钉在开篇？

## Q5 元问题

- objective_id: OBJ-19
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；八口同一文件 `service.ts`
- difficulty: low

建议：`re-dispatch-L`

原因：

- 八口同属 OBJ-19、同一 C4 组件 `frontend/packages/domains/system` 的一座工厂。L 合同章节齐全（直觉、定义、机制链、带 caption 的图、正反例、变式、误区、来源表）。**主叙事站得住**：工厂只认 `HttpClient`；八口前缀与分页/数组形状；`segment`；params/data 例外；`ssoApps` 对 `clients` 的动词差；三种 options；`users.get()` ≠ getInfo；`menus` 不管 `getRouters`；home 只喂 identity；FE-CRUD-002 尚未落在这八口。不必为「抽屉名字」再挖五问。
- 本批已有可粘贴的磁盘反例，属于同一课的闭合定义与缺口表精度，不是第二容器：矩阵一行十名 vs claimed 八口 vs OBJ 七名；L-021/L-022 把 `resources`/资料厨房推回本课；工厂缺口写窄（角色/岗位/登录域 export，`userTypes.changeStatus`/`listByUser`）；`rotateSsoSecret` 零测试零页面。补丁应短：开篇加一张「OBJ 七名 / 口试八口 / 矩阵十名 / 不口试三键」；「有窗无抽屉」改成完整对照（五 export + 登录域两枪 + Client 轮换有枪无客）；71 条旁注测试标题仍写 seven slices、`rotateSsoSecret` 不在任何 `it`。不要把 `createSystemResourceService` 全表、OSS 直传、导航 host、OpenAPI/monitor 塞进 38 分钟课。
- 不建议再开一批问：重复本批证据。不建议拆课：未满 10 问；八口仍是同一工厂、同一 OBJ-19（`ssoApps` 是 L-018 的厨房孪生，不是第二容器）；补丁不必然撑破 40 分钟。不建议把八口整格推迟：HTTP 形状已与源码对齐。`identity` / `resources` 若 Lead 坚持矩阵必须按行闭合，则先拆行或改挂，而不是把资源柜搬进本课。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:createSystemService.users | 18 枪形状已对齐（含 `get()` 两态、授权走 params、解锁 GET、资料三枪 URL、凭据 no-store 投影）。缺口：export/import/importTemplate 不在工厂，课已写；`profile*` 策略细节属 L-022 | 保持 URL 三枪。导入导出保持宿主旁路。不要把 SysProfile 失败句搬进来 | re-dispatch-L（保持）或 covered 八口后 |
| A:createSystemService.roles | 14 枪已对齐（含 allocated/unallocated、cancel data vs cancelAll/selectAll params、`deptTree/{id}`）。缺口：`system/role/export` 页面走 download，课的工厂缺口没点名 | 缺口表加一行角色 export | re-dispatch-L（一行） |
| A:createSystemService.menus | 8 枪已对齐；list 是数组；无 `getRouters`。`tree()` 无参仍带 `params: { clientId }` | 可选半句。不要把动态路由算进本格 | re-dispatch-L（半句）或保持 |
| A:createSystemService.departments | 7 枪已对齐；list 数组；delete 单 id；`?deptIds=`。后端无 export | 无 | — |
| A:createSystemService.posts | 7 枪已对齐；options 走 params；`/post/deptTree`。缺口：`system/post/export` 未写入工厂缺口表。后端无 changeStatus，工厂也无——这是对齐，不是缺口 | 缺口表加岗位 export | re-dispatch-L（一行） |
| A:createSystemService.clients | 9 枪形状已对齐；`options` 包装 list；bind 有页面。缺口：`rotateSsoSecret` 有实现、无测试、无页面；client export 课已写 | 改口：厨房有轮换枪，缺的是快照和 `ClientPage` 调用，不是缺方法 | re-dispatch-L |
| A:createSystemService.ssoApps | 五枪已对齐；update POST `/update`；无 delete。manifest `system:ssoApp:remove` 不是工厂方法 | 保持与 clients 对照。不要另开格子 | — |
| A:createSystemService.userTypes | 工厂六枪已对齐；`/options` 独立路径。缺口：后端 `changeStatus`、`listByUser`、`export` 三窗；页面只有 export 走 download，无停用/按用户查 | 缺口表写三窗；点明 `CrudService.changeStatus?` 可选 ≠ 实现了停用 | re-dispatch-L |
| 矩阵行上的 identity | 两 URL 课用作对照，声明不 covered。L-014 已讲 `getInfo`/`getMenus` 走这两枪 | 开篇表：本课不认。Lead 拆行或 `covered-by-parent`（L-014） | defer（L-014） |
| 矩阵行上的 resources | 课虚线推给 L-021 起；L-021/L-022 正文推回 L-019。实现是独立工厂 `createSystemResourceService`（`dictData/dictTypes/configs/oss/ossConfigs/social`） | 开篇写「另一函数，不在八口」。请 Lead 拆行并改挂 L-021/L-022/L-026/L-029；同时改那两课「格子仍归 L-019」句 | defer（L-021+） |
| publicUsers | 同 URL 投影；矩阵未列 | 保持一句反例，不认格 | 不入库 |
| createOpenApiService / createMonitorService | 课已划出 | 保持 | defer（L-033 / L-024） |
| A:createSystemWebDomain | 课只借 runtime.service 与权限 `ssoApp:remove` | 保持一句。全表 L-020 | defer（L-020） |
| API-005 / FE-CRUD-002 | 存量 PUT/DELETE 已盘点。OSS 配置 POST 在 resources 文件 | 不要用 resources 反证八口已迁移 | 不发动 |
| 矩阵 (b) createSystemService | 无行。投影抛错 / options `?? []` / 失败不吞已作为映射副作用出现 | 不要自造 (b) 格 | 不入库 |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；缺口落在同一 OBJ-19 的闭合定义与缺口表精度（十名 vs 八口、工厂缺枪写窄、`rotateSsoSecret` 零消费者）；把所有权表和完整「有窗无抽屉」补进原课不会必然超过 40 分钟；`resources`、identity、web-domain、OpenAPI、monitor 已有后续课或可拆矩阵行。满 10 问前不拆。八口仍是同一 `createSystemService` 容器，不是两个 C4。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 `identity`（L-014）、`resources`/`createSystemResourceService`（L-021+）、`createSystemWebDomain`（L-020）、`createOpenApiService`（L-033）、`createMonitorService`（L-024）显式建议 defer；对八口给出 `re-dispatch-L`（所有权表 + 完整工厂缺口）供 Lead 在拆矩阵行之后标 covered
