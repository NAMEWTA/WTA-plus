---
artifact: learning-goal-probe
probe_id: GP-L-084-b01
lesson_id: L-084
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-084 / b01

审问对象是已写成的 Lesson `lessons/L-084-satoken-redis.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / 派单覆盖 **三格**：`C:Redis 8`、`B:LoginHelper / StpUtil`、`D:fail-session-lost`。OBJ-84 原文：能说明 Sa-Token 会话写入与 Redis 的失败路径（登录成功但会话丢失算哪边）。档是**方法性状**。前置 L-013。chain 覆盖列写成 `B:LoginHelper B:StpUtil D:fail-session-lost`，**漏写**矩阵已有的 (c) `Redis 8`。矩阵几何 **不是** 把锁/限流函数格再盖第二次章：

- (c) `Redis 8` | Container | 会话/锁/限流（仍 uncovered）。口试认柜子：镜像 `redis:8.6.3`、容器 `namewta-redis`、会话抽屉。锁与限流只点名同住。
- (b) `LoginHelper / StpUtil` | 会话写入 Redis（仍 uncovered）。矩阵 **一行两符号**，不是两行。`PlusSaTokenDao` / Caffeine 是机制，不是 (a) 公开 API。
- (d) `失败路径 会话丢失` | 登录成功但 Redis/Sa-Token 读失败（仍 uncovered，备注 L-084）。口诀：`LoginHelper.login` 已经把票交出去，再读空或读挂。

矩阵 (c) `wta-common 工具间` 已 covered（L-002，备注含 satoken）。矩阵 (d) `失败路径 认证拒绝` 已 covered（L-006）。不要用本课再盖 L-011 门厅、L-013 策略/值班员、L-014 `Admin-Token`、L-023 在线窗、L-032 nonce/限流函数格、L-057 `sso:session:`、L-083 dao-mapper、L-085 MySQL 8.4 / mysql-base。本批按派单审问 **这三格**；口试按磁盘：写点是 `LoginHelper.login` 两步（`StpUtil.login` + token-session `"loginUser"`），落 Redis 的是 `PlusSaTokenDao`（`RedisUtils` + 5 秒 Caffeine），JWT 简单模式仍把档案袋放进 Redis。丢失的回执不是一种 HTTP。

抽查锚点（2026-09-17）：`backend/wta-common/wta-common-satoken/src/main/java/org/namewta/common/satoken/utils/LoginHelper.java`；`core/dao/PlusSaTokenDao.java`；`config/SaTokenConfig.java`；`core/service/SaPermissionImpl.java`；`handler/SaTokenExceptionHandler.java`；`resources/common-satoken.yml`；模块无 `src/test`；后端根 POM `satoken.version=1.45.0`；`wta-admin/.../application.yml` `sa-token`；`application-dev.yml` `spring.data.redis` / `redisson.keyPrefix`；`wta-common-security/.../SecurityConfig.java` 拦截器；`wta-common-redis/.../RedisExceptionHandler.java` / `ClusterCacheInvalidationCoordinator.java` / `KeyPrefixHandler.java` / `aspectj/RateLimiterAspect.java`；`wta-common-web/.../GlobalExceptionHandler.java`；`wta-system/.../SysMenuController.getRouters`；`wta-admin/.../web/service/SysLoginService.java`；`AuthController.login`；五只 `*AuthStrategy`；`SaTokenSsoBusinessTokenAdapter`；`UserActionListener` / `UserLoginSuccessListener`；`SysUserOnlineController.forceLogout`；`SaTokenOpenApiMachineSessionOperations`；`RedisSsoSessionStore`；`release-artifacts/docker/docker-compose-infrastructure.yml` 服务 `redis`；`docker-compose-backend.yml` `SPRING_DATA_REDIS_*`。

## Q1 澄清

- objective_id: OBJ-84
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-84；chain L-084 覆盖列 `B:LoginHelper B:StpUtil D:fail-session-lost`；课 `claimed_cells` 三格含 `C:Redis 8`；矩阵 (c) Redis 8 仍 uncovered、备注「会话/锁/限流」；(b) `LoginHelper / StpUtil` 一行；(d) 「失败路径 会话丢失」仍 uncovered；L-006 `D:fail-auth` 已 covered；`SaTokenConfig` 注入 `StpLogicJwtForSimple`；负空间 L-011 / L-013 / L-014 / L-023 / L-032 / L-057 / L-083 / L-085
- difficulty: medium

课把宏观保险柜收成「Redis 8 这只柜子 + 两只手写入 + 发过票再读失败算哪边」。计划里至少有四套可数对象，闭合标准要以哪一套盖章？

1. **OBJ-84 原文。** `course.md`：能说明 Sa-Token 会话写入与 Redis 的失败路径（登录成功但会话丢失算哪边）。档是方法性状。一句里是 **写入点 + 丢失算哪边**，没有把 `@RateLimiter` / Lock4j / OpenAPI nonce 写成口试清单。
2. **chain 覆盖列。** `B:LoginHelper B:StpUtil D:fail-session-lost`。两个 B 名在矩阵是 **同一行**。chain **没有** `C:Redis 8`。Lead 若按 chain 字面只盖 (b)+(d)、把 (c) 留成空洞，是假空洞；若按「Redis 四字」去盖 L-032 限流函数格或 L-057 手环，是假闭合。
3. **课 `claimed_cells` / 派单三格。** 与矩阵三行 1:1：容器 Redis 8、写手 LoginHelper / StpUtil、失败路径会话丢失。符号以磁盘和矩阵原文为准。
4. **口诀（操作定义）。** 若 `LoginHelper.login` **已经返回过票**（策略把 `StpUtil.getTokenValue()` 填进 `LoginVo.access_token`，HTTP 已 `R.ok`），之后 Redis / Sa-Token **读失败或读空** = `D:fail-session-lost`，**不是** L-006 `D:fail-auth`。`fail-auth` 是写柜前就失败、本次没有 token-session。写柜当下 Redis 就挂、登录 HTTP 自己炸、前端通常拿不到票：那是基础设施拒绝登录，**还没发出成功收据**，不要硬塞进丢失。
5. **JWT 简单模式仍有 Redis。** `SaTokenConfig.getStpLogicJwt` 是 `new StpLogicJwtForSimple()`。纸条是 JWT（extra 够核 `clientid` / `userId`），档案袋 `LoginUser` 仍进 token-session / Redis。不要把 Simple 口述成 Stateless。
6. **(c) 备注里的锁/限流。** NAMEWTA 把会话、锁、限流塞进同一只容器。本课深挖**会话抽屉**。OpenAPI nonce / 原子桶是 L-032 已 covered 的 (b) 函数格；`@RateLimiter` 发码口是 L-012；Lock4j 失败席是 `RedisExceptionHandler`。问：Lead 能否凭会话抽屉证据（镜像、容器名、AOF、`SPRING_DATA_REDIS_HOST: redis`、DAO 走 `RedisUtils`）把 (c) Redis 8 标 covered，**而不**重开 L-032 锁/限流行、也不把 `PlusSaTokenDao` 升成 (a)？

问：本三格闭合标准是「容器认 `redis:8.6.3` / `namewta-redis` 且只把锁限流当同住邻居、写手认 `LoginHelper.login` 两步且 JWT simple 仍落 Redis、丢失认已经发过 `access_token` 再读空/读挂/被踢，且不碰 L-011 门厅、不把 `SysLoginService.login` 发明出来、不把 `online_tokens:` 当通行证」，还是 OBJ 那句必须先把 chain 补上 `C:Redis 8` 才给容器盖章、或必须等锁/限流函数格再讲一遍才给 (c) 盖章？Lead 若因备注写了「锁/限流」而拒绝给 Redis 8 盖章，是否假判？Lead 若凭「大家都用 Redis」把 L-032 / L-057 再标一次 covered，是否假闭合？

## Q2 证据

- objective_id: OBJ-84
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: `LoginHelper.java` 36 / 55–66 / 104–109 / 205–220；`PlusSaTokenDao.java` 27–38 / 78–82 / 196–216 / 224–236；`SaTokenConfig.java` 30–49；`common-satoken.yml` 5–13；`application.yml` 261–269；`application-dev.yml` 150–167；`SecurityConfig.java` 86–114；`SaTokenExceptionHandler.java` 44–56；`SaPermissionImpl.java` 52–62；`SysMenuController.java` 48–56；`RedisExceptionHandler.java` 27–31；`GlobalExceptionHandler.java` 158–163；`SysLoginService.java` 112–125；`AuthController.java` 76–105 / 134；`PasswordAuthStrategy.java` 79–84；`UserActionListener.java` 26–36；`UserLoginSuccessListener.java` 60–64；`SysUserOnlineController.java` 89–94；`SaTokenOpenApiMachineSessionOperations.java` 56–74；`RedisSsoSessionStore.java` 19；`docker-compose-infrastructure.yml` 85–108；`docker-compose-backend.yml` 18–20；模块无 `src/test`
- difficulty: high

课的出餐单哪些能在工作树直接复现，哪些只是口试精度或测试名单为空？

**仍能复现（写手格 `B:LoginHelper / StpUtil`）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工具间五份业务 Java，无本模块单测 | `wta-common-satoken/.../satoken/` 下 config / dao / service / handler / utils；无 `src/test` |
| `LoginHelper.login` 先 `StpUtil.login(loginId, extras)` 再 `getTokenSession().set("loginUser", loginUser)` | `LoginHelper.java` 55–66；`LOGIN_USER_KEY = "loginUser"` 第 36 行 |
| extra 叠 `userId` / `userName` / `dept*` / `userType`；`clientid` 等由策略先放进 model | 58–65；`CLIENT_KEY = "clientid"` 第 42 行 |
| `getLoginUser()` **只** catch `NotLoginException` → null；会话在、无 `"loginUser"` 键 → null，不变成未登录异常 | 104–109 / 138–143 |
| 私有 `getExtra` catch **任何** `Exception` → null；`getUserType()` 直接 `StpUtil.getExtra`，不吞 | 205–210 / 218–220 |
| `SaTokenConfig` 四颗 Bean：`StpLogicJwtForSimple`、`SaPermissionImpl`、`PlusSaTokenDao`、`SaTokenExceptionHandler` | `SaTokenConfig.java` 30–57 |
| 内置 yml：`is-read-cookie: false`、`token-prefix: "Bearer"`、`is-read-body/header: true` | `common-satoken.yml` 5–13 |
| 组装 yml：`token-name: Authorization`、`is-concurrent: true`、`is-share: false` | `application.yml` 261–267 |
| 真正 set 的是 DAO：`RedisUtils.setCacheObject`；timeout 0 / `NOT_VALUE_EXPIRE` **直接 return，不写 Redis** | `PlusSaTokenDao.java` 207–216 |
| L1 Caffeine `expireAfterWrite(5, SECONDS)`，最大 1000；命名空间 `"sa-token"` | 29–38 |
| 读：`caffeine.get(key, RedisUtils::getCacheObject)`（空结果也会进 L1 最多 5 秒） | 196–198 |
| `update` / `updateObject` 先 `hasKey`；没有键就 no-op | 78–82 / 142–146 |
| 写完 `invalidate("sa-token", key)`；广播无订阅者或 2 秒收不齐 ACK → `ClusterCacheInvalidationException`（发生在 Redis **已经 set 之后**） | DAO 224–226；Coordinator 92–105 |
| 密码策略：`LoginHelper.login` 之后 `LoginVo.accessToken = StpUtil.getTokenValue()` | `PasswordAuthStrategy.java` 79–84 |
| SSO 业务票同一只柜：`SaTokenSsoBusinessTokenAdapter.issue` 也 `LoginHelper.login` | adapter 23–25 |
| 权限只读当前 token-session 快照；对不上或无 `LoginUser` → 空 `ArrayList`，不回库 | `SaPermissionImpl.java` 52–62 |

**仍能复现（容器格 `C:Redis 8`）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 镜像 `redis:8.6.3`，容器名 `namewta-redis` | `docker-compose-infrastructure.yml` 85–87 |
| 宿主机 `${NAMEWTA_BIND_HOST:-127.0.0.1}:46379` → 6379；AOF；`--requirepass` | 91–100 |
| 健康检查要 `PONG`（磁盘是 `redis-cli --no-auth-warning -a "$$REDIS_PASSWORD" ping \| grep -q PONG`） | 101–105 |
| 后端组装 `SPRING_DATA_REDIS_HOST: redis`，端口 6379，密码同一只 `REDIS_PASSWORD` | `docker-compose-backend.yml` 18–20 |
| 本机 overlay **不是**这只柜：`localhost:6379` / `wta123` | `application-dev.yml` 150–159 |
| Redisson 前缀 `keyPrefix: WTA` | `application-dev.yml` 165–167 |
| 同住邻居只点名：验证码键、`pwd_err_cnt:`、`online_tokens:`、`sso:session:`、OpenAPI nonce/限流、`RateLimiterAspect` / Lock4j | `CacheNames`；`RedisSsoSessionStore` 第 19 行 `sso:session:`；`RateLimiterAspect.java`；`RedisExceptionHandler` 只接锁失败 |

**仍能复现（失败路径 `D:fail-session-lost` 与负空间）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| `SysLoginService` **没有** `login()`，无 `LoginHelper.login` / `StpUtil.login`；唯一写路径是 `logout` finally `StpUtil.logout()` | 全文件；`logout` 112–125 |
| `AuthController.login` **自己不写**；过闸后 `IAuthStrategy.login`；全方法无 `StpUtil.login` | `AuthController.java` 76–105。`StpUtil.checkLogin` 只在 `socialCallback` 134 / `unlockSocial` 158 |
| 名牌是监听器后贴：`doLogin` 发事件 → `online_tokens:` + token | `UserActionListener` 26–28；`UserLoginSuccessListener` 60–64；`CacheNames.ONLINE_TOKEN_KEY` |
| 踢人：`DELETE /monitor/online/{tokenId}` → `StpUtil.kickoutByTokenValue`；已无效仍 `R.ok()` | `SysUserOnlineController` 89–94 |
| OpenAPI 机器会话 **不**经 `LoginHelper.login`：`stpLogic.createLoginSession`，`isWriteHeader=false` | `SaTokenOpenApiMachineSessionOperations.java` 56–74 |
| 拦截器：`checkLogin` → `getLoginUser` → 非机器请求 `getExtra("clientid").toString()` 必须对上头/参数 | `SecurityConfig.java` 98–113 |
| `checkLogin` 失败 → 401 四句（过期/顶下线/踢下线/默认状态异常） | `SaTokenExceptionHandler.java` 44–56；Hutool `HTTP_UNAUTHORIZED` |
| `checkLogin` 过、档案袋空：`getRouters` `R.fail("当前登录缺少客户端上下文")`，`R.fail(String)` 的 JSON `code=500` | `SysMenuController.java` 48–52；`R.java` 130–131 |
| 档案袋空 + `@SaCheckPermission` → 空权 → 403「没有访问权限，请联系管理员授权」 | `SaPermissionImpl` 空列表；Handler 29–34 |
| Redis **进程挂了**：`RedisExceptionHandler` **只** `LockFailureException` → 503「业务处理中…」；会话读挂 → `GlobalExceptionHandler` `RuntimeException` 席「发生未知异常…[错误编号: 8 位数字]」，`code=500` | Redis handler 27–31；Global 158–163 |
| 写失败 ≠ 丢失：`login` 无自己的 Redis try/catch，DAO 抛错则登录 HTTP 失败 | `LoginHelper.login` 55–66 无 catch |

**课写过、测试名单空或口试不要假装 SCAN（不是证伪本三格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「本课没有在运行中的 Redis 里 SCAN 过键名」 | 工作树无会话键 SCAN 测试。权威是 DAO 把 Sa-Token 的 key 交给 `RedisUtils`，`KeyPrefixHandler` 加 `WTA:` |
| 「timeout=0 时 DAO 跳过写；没有集成测试钉死『一定返回了票』」 | `writeValue` 208–210 有闸。无 HTTP 单测。口试说「DAO 这一闸在磁盘上」 |
| 「1.45.0 `createTokenValue` 没把 timeout 编进 JWT」 | 厂商对照，源码不在本仓；POM `satoken.version=1.45.0`。不要当本仓前端合同 |
| 健康检查口诀「ping 要看到 PONG」 | compose 用 `--no-auth-warning` 且 `grep -q PONG`，比课半句更窄，机制仍是 PONG |
| chain 估时 35、课 `estimated_minutes: 38` | 不是机制错误 |
| 模块零单测 | 覆盖证据靠源码互证，不靠本模块测试名 |

问：在「两步写、DAO 才落 Redis、JWT simple 仍有档案袋、值班员无 `login()`、门厅自己不 `StpUtil.login`、名牌后贴、三种丢失回执（401 映射没了 / 500 缺客户端上下文 / 500 未知异常）、503 锁席不接连接异常、容器是 `redis:8.6.3` / `namewta-redis`」已经能互证的前提下，这三格是否还要把「无 SCAN 键名」「timeout=0 无 HTTP 单测」「无 satoken 模块测试」写成开篇半句才算讲完？还是这些已经落在课的边界/来源表里，Lead 可以把 **(c) Redis 8（会话抽屉）**、**(b) LoginHelper / StpUtil**、**(d) fail-session-lost** 视为可 covered，并把锁/限流函数格保持划给 L-012 / L-032？

## Q3 假设与反例

- objective_id: OBJ-84
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 课反例 1–14 / 误区 1–20；`SysLoginService` 无 `login()`；`AuthController.login` 无 `StpUtil.login`；`StpLogicJwtForSimple`；`ONLINE_TOKEN_KEY`；`RedisSsoSessionStore` `sso:session:`；`createLoginSession` + `isWriteHeader=false`；`RedisExceptionHandler` 只锁席；`getRouters` 缺上下文；`SaPermissionImpl` 空列表；`is-read-cookie: false`；`is-concurrent: true`
- difficulty: high

课的隐含假设若反转，合格读者会把哪只抽屉或哪边失败路径认错？

1. **「白页 / JSON 失败 = 没登录成功 = `D:fail-auth`。」** 先看口袋有没有 `access_token`。有票再失败，优先本课。L-006 是写柜前三闸。课反例 1 / 误区 2 覆盖。
2. **「`SysLoginService.login` 写 Redis 会话。」** 没有这个方法。值班员写错次、发审计、finally 撕柜。课反例 2 / 误区 4 覆盖。
3. **「`AuthController.login` 自己 `StpUtil.login`。」** 方法体过闸后只调策略。`StpUtil.checkLogin` 是社交回调/解绑。课误区 3 覆盖。
4. **「简单模式不用 Redis。」** 本仓注入 Simple，DAO 是 Redis。档案袋不在 JWT 里。课反例 3 / 误区 5 覆盖。
5. **「JWT 还没过期所以会话还在。」** 有效期权威在柜和 `checkLogin`。课反例 4 / 误区 6 覆盖。
6. **「把在线名单上的名牌踢掉 = 已经退出。」** `online_tokens:` 是监听器贴纸。`getRouters` 不读这把键。只 DEL 名牌，通行证与档案袋可以还在。课反例 5 / 误区 7 覆盖。
7. **「SSO `sso:session:` 丢了 = 业务会话丢了。」** 那是 L-057 认人厅手环。业务票仍走今天这只柜；手环丢了不会自动 `StpUtil.logout`。课反例 6 / 误区 15 覆盖。
8. **「OpenAPI 机器票也是 `LoginHelper.login`。」** 磁盘是 `createLoginSession`，`isWriteHeader=false`。格子是 L-032。课反例 11 / 误区 11 覆盖。
9. **「会话丢失一律 401。」** 映射没了才走 `NotLoginException`。档案袋空是缺上下文 500 或空权 403。Redis 挂了是未知异常 500。课反例 7 / 误区 8 覆盖。
10. **「Redis 挂了走 `RedisExceptionHandler` 变 503。」** 那一席只接 `LockFailureException`。课反例 8 / 误区 9 覆盖。
11. **「`getUserId()` 空了就证明没登录。」** extra 读失败被吞成 null；`isLogin` / `checkLogin` / 档案袋是三条尺。课反例 9 / 误区 14（`getUserType` 不吞）覆盖。
12. **「Cookie 里的 Authorization 就是会话。」** `is-read-cookie: false`。票走 header / body，前缀 Bearer。课反例 12 / 误区 10 覆盖。
13. **「Caffeine 是第二只 Redis / 跨节点。」** 进程内 5 秒影子。课反例 13 / 误区 12 覆盖。
14. **「`update` 能把丢了的会话写回来。」** `hasKey` 为假则 no-op。课反例 14 / 误区 13 覆盖。
15. **「本课把锁和限流的函数格也 covered。」** 容器认柜子；OpenAPI 限流仍是 L-032，发码限流仍是 L-012。课反例 10 覆盖。
16. **「并发登录会互挤。」** 默认 `is-concurrent: true`。`BE_REPLACED` 文案在处理器里，本仓默认走不到。课边界 4 / 误区 19 覆盖。
17. **「超管不受会话丢失影响。」** 档案袋丢了 `getUserId()` 可能 null，超管判断 false。课误区 16 覆盖。
18. **「写路径广播失败 = 会话丢失。」** 方向相反：柜里可能有孤儿键、客户端没票。课边界 2 覆盖。

问：读完后仍说不出的，是不是「Redis 8 的镜像名」，而是 **已经发过票再读失败才算本课 (d)、名牌 ≠ 通行证、SSO 手环 ≠ 业务柜、机器会话不经 `LoginHelper.login`、丢失不是一种 HTTP、简单模式仍有档案袋、值班员和门厅都不写柜**？

## Q4 视角

- objective_id: OBJ-84
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (b)/(c)/(d)；OBJ-84 vs L-006/L-011/L-013/L-014/L-023/L-032/L-057；chain 漏写 Redis 8 vs `claimed_cells` 三格；课方法性状表
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这三格**？

| 视角 | 多出来的边 | 是否本课这三格 |
| --- | --- | --- |
| 矩阵维护者 | (c) Redis 8 备注含锁/限流；chain 漏写该容器；(b) 一行两符号；(d) 与已 covered 的 fail-auth 对打 | **是** 闭合定义。Lead 可盖这三行。不要重开 L-032 函数格，不要发明 DAO (a) 行 |
| 写柜的手 | `LoginHelper.login` 两步；`StpUtil` 厂商入口；DAO + 5s L1；timeout 0 跳过 | **是** `B:LoginHelper / StpUtil` |
| 容器电工 | `redis:8.6.3` / `namewta-redis` / AOF / 46379 / 组装 `HOST: redis` / 本机 overlay 不是这只柜 | **是** `C:Redis 8` 会话抽屉。锁/限流 **点名同住，否** 函数格 |
| 丢失分诊 | 有没有成功发过票；三种回执；写失败 ≠ 丢失；孤儿键方向相反 | **是** `D:fail-session-lost` |
| 门厅（L-011） | Client 红章、`@ApiEncrypt`、欢迎通知失败不推翻已写会话；Controller 零处 `LoginHelper.login` | 写点不在门厅 **是** 负空间一句。六扇 HTTP **否**（已 covered） |
| 盖章口（L-013） | 五只策略何时喊 `LoginHelper.login`；值班员无 `login()`；贴纸在监听器 | 谁喊开柜 **是** 对照。Bean 选择 / 错次 / 注册不发票 **否** |
| 前端抽屉（L-014） | `Admin-Token` / `Home-Token`；解析到 `access_token` 才 `setToken`；口袋和柜子可不同步 | 口袋纸条对照 **是** 一句。`prepareLogin` 全表 **否** |
| 监控室（L-023） | 扫描 `online_tokens:`；`kickoutByTokenValue` 是丢失的主动子类 | 名牌 ≠ 通行证、踢人回执 **是** 对照。四份 Controller **否**（已 covered） |
| 机器门（L-032） | `createLoginSession`；nonce SET NX；限流桶；Redis 错 503 | 同柜不同钥匙 **是** 点名。HMAC / nonce / 限流尺子 **否**（已 covered） |
| 认人厅（L-057） | Cookie + `sso:session:`；换业务票才 `LoginHelper.login` | 手环不是档案袋 **是** 点名。三扇 `/sso` **否**（已 covered） |
| 认证拒绝（L-006） | 缺 Client / 验证码 / 菜谱 → 不写会话 | 对打「哪边」**是**。走廊主路径 **否**（已 covered） |
| MySQL 8.4 / dao-mapper | `sys_login_info` 是审计不是柜；Mapper 所有权 | **否**。L-085 / L-083 |

问：Lead 若凭「三格名字在 claimed 列里」把 Redis 8、LoginHelper / StpUtil、fail-session-lost 一起标 covered，是否还要求读者能指着 **两步写、DAO 才 set、simple 仍有 Redis、发过票才算丢失、401/403/缺上下文/未知异常四种脸、名牌不是通行证、锁限流只是同住**？这些是不是已经在本课函数表/反例里，而不需要等 L-032 再讲一遍才给 **(c) 容器** 盖章、也不需要把 `PlusSaTokenDao` 升成新行？chain 漏写 Redis 8 是否例外——仍按矩阵 (c) 与 `claimed_cells` 盖，不要当成未教？

## Q5 元问题

- objective_id: OBJ-84
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课；邻格可 defer）；本课累计 5/10；`split_depth: 0`；矩阵 (c) Redis 8、(b) LoginHelper / StpUtil、(d) 失败路径 会话丢失仍 uncovered
- difficulty: low

建议：`defer`

原因：

- **三格与 OBJ-84 / 课 `claimed_cells` / 矩阵三行对齐。** L 合同章节齐全（宏观地图、直觉三张收据、定义表、机制链 1–6、带 caption / 文字等价物的图、正反例、变式、误区、暂停走查、来源表 S-002 / S-L006-01 / S-L013-01 / S-L014-01 / S-L023-01 / S-L032-01 / S-L084-01…11）。能复现：`LoginHelper.login` 两步、`PlusSaTokenDao` + 5 秒 Caffeine、`StpLogicJwtForSimple` 仍落 Redis、容器 `redis:8.6.3` / `namewta-redis`、值班员无 `login()`、门厅自己不写、名牌后贴、读路径三种主回执。不必为「格子的名字」再挖五问。Q2 与磁盘互证时，Lead 可将这三行标 `covered`。
- **(c) 是会话抽屉盖容器，不是重开锁/限流。** 备注「会话/锁/限流」里的后两项只点名同住。L-032 已 covered nonce/限流函数格；L-012 已 covered 发码限流。不要因为备注有「锁/限流」而把 Redis 8 留成空洞，也不要用本课再盖那些 (b) 行。chain 漏写 `C:Redis 8` 不是未教——`claimed_cells` 与矩阵 (c) 已印。
- 本批可粘贴的精度项不挡住盖章：（1）无 SCAN 键名；（2）timeout=0 无 HTTP 单测；（3）satoken 模块零测试；（4）健康检查命令比课半句更窄；（5）JWT `createTokenValue` 不传 timeout 是厂商 1.45.0 对照；（6）chain 估时 35、课 38。不要把五只策略、前端钥匙全表、在线四窗、OpenAPI HMAC、SSO 三扇、MySQL 基座塞进本格。
- 不建议 `mine-more`：b01 未证明这三格还有真实矩阵空洞。再五问只会把 L-011 门厅、L-013 策略、L-014 抽屉、L-023 踢人窗、L-032 机器会话或 L-057 手环再问一遍。`mine-more` 只在 Redis 8 或 LoginHelper 行仍未教到时才合法——本批不是。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；同一 OBJ 没有「课写了 `SysLoginService.login`、磁盘没有」这类事实空洞。不建议 `split-lesson`：未满 10 问；三格同属 OBJ-84（柜子 + 手 + 丢失算哪边），不是两个 C4 讲不完；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 `PlusSaTokenDao` (a) 行。不建议用本课改已 covered 的 `D:fail-auth` 或 `wta-common 工具间`。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| C:Redis 8 | 镜像/容器名/AOF/46379/组装 HOST/本机 overlay 对照均与源码对齐。残留精度：健康检查命令多 `--no-auth-warning`；无 SCAN 键名。锁/限流只点名同住，不是本行函数表。 | 可选半句：口试以 compose 服务 `redis` 为准。Lead 可将该行标 `covered`（证据锚本 probe 与 L-084 来源表 S-L084-07）。备注可写成「会话抽屉已讲；锁/限流同住，函数格仍 L-012/L-032」。不要重开 L-032。 | defer（本行可 covered；不要因备注有锁/限流而假判未讲） |
| B:LoginHelper / StpUtil | 两步写、JWT simple 仍落 Redis、DAO + 5s L1、timeout 0 跳过、读 catch 不对称、权限不回库——均与源码对齐。残留精度：模块零测试；timeout=0 HTTP 未钉；厂商 `createTokenValue` 不在本仓。矩阵一行两符号。 | 可选半句：口试以 `LoginHelper.login` 55–66 与 `PlusSaTokenDao.writeValue` 为准。不要求重写机制链。Lead 可将该行标 `covered`（S-L084-01 / S-L084-02 / S-L084-03）。不要把 DAO 升成 (a)。 | defer（本行可 covered） |
| D:fail-session-lost | 操作定义（已发票再读失败）与三种主回执、写失败≠丢失、孤儿键方向相反，均与源码对齐。踢人是主动子类，函数格仍 L-023。 | 不要改 L-006 `D:fail-auth`。Lead 可将该行标 `covered`（S-L084-04 / S-L084-06 / S-L084-08 / S-L084-11）。 | defer（本行可 covered） |
| D:fail-auth / 主路径 登录后查询 | 对打「哪边」已点。两格已 covered。 | 保持。L-006。 | defer（L-006 已 covered） |
| A:AuthController.login 等门厅 | 自己不写柜已作负空间。 | 不要用本课再盖六扇。 | defer（L-011 已 covered） |
| B:IAuthStrategy / SysLoginService | 谁喊开柜、值班员无 `login()`、贴纸在监听器已作对照。 | 不要重拆五只 Bean。 | defer（L-013 已 covered） |
| A:IdentityAccessService / B:session-store | 口袋 `Admin-Token` 与柜子可不同步已点。 | 全表保持 L-014。 | defer（L-014 已 covered） |
| A:SysUserOnlineController | 名牌扫描与 `kickoutByTokenValue` 已作对照。 | 不要用本课再盖四窗。 | defer（L-023 已 covered） |
| B:SaTokenOpenApiMachineSessionOperations / nonce / 限流 | 机器会话不经 `LoginHelper.login`、同柜不同钥匙已点名。 | 不要重开已 covered 的 (b) 三行。 | defer（L-032 已 covered） |
| A:SsoSessionController / `sso:session:` | 手环 ≠ 业务柜已点名。 | 不要用本课盖三扇 `/sso`。 | defer（L-057 已 covered） |
| C:wta-common 工具间 | 备注已含 satoken，行已 covered。 | 不要再盖一次。 | defer（L-002 已 covered） |
| C:MySQL 8.4 / mysql-base | 审计表不是会话柜。 | 保持划出。 | defer（L-085） |
| C:dao-mapper-ownership / B:BaseMapperPlus | 无关本柜。 | 保持划出。 | defer（L-083） |
| PlusSaTokenDao 作为 (a) | 课已声明 DAO 是机制。矩阵无此符号。 | 不要发明公开 API 行。 | defer（未入库） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；三格无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出柜子（`redis:8.6.3` / `namewta-redis`）、主路径（策略喊 `LoginHelper.login` → `StpUtil.login` + token-session `"loginUser"` → `PlusSaTokenDao` / RedisUtils）与失败路径（已经把 `access_token` 给出去，再读空、被踢、或 Redis 进程挂了；回执可能是 401 / 403 / 缺上下文 / 未知异常）；把可选半句写进原课不会超过 40 分钟；门厅、策略、前端抽屉、在线窗、机器会话、SSO 手环、MySQL/DAO 已有邻课。满 10 问前不拆。容器 + 写手 + 丢失算哪边仍是 chain 原设计的同一 OBJ-84，不是两个 C4 讲不完。不要把 `PlusSaTokenDao` 或锁/限流拆成新课。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / split 提案：否（miner 不改矩阵）；split 提案为 none；缺口表对 L-011 门厅、L-013 策略/值班员、L-014 前端抽屉、L-023 在线窗、L-032 机器会话/nonce/限流、L-057 SSO 手环、L-006 fail-auth（已 covered）、L-002 工具间（已 covered）、L-083 dao-mapper、L-085 MySQL、未入库 DAO (a) 行显式建议 `defer`；对矩阵 (c) `Redis 8`、(b) `LoginHelper / StpUtil`、(d) `失败路径 会话丢失` 建议 Lead 标 `covered`（证据锚本 probe 与 L-084 来源表 S-L084-01…11），条件是不把锁/限流函数格再盖第二次章、不把 `online_tokens:` 或 `sso:session:` 当成通行证。本课不主张发明 `PlusSaTokenDao` 矩阵行、用本课改 `D:fail-auth`、或因 chain 漏写 `C:Redis 8` 而把容器留成空洞。
