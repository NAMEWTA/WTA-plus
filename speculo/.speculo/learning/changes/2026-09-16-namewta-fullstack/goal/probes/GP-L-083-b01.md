---
artifact: learning-goal-probe
probe_id: GP-L-083-b01
lesson_id: L-083
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-083 / b01

审问对象是已写成的 Lesson `lessons/L-083-mybatis-base.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **两格**：`C:dao-mapper-ownership`、`B:BaseMapperPlus / QueryBuilder`。chain 原文把 (b) 写成两个 token `B:BaseMapperPlus B:QueryBuilder`，矩阵 **印成一行**。OBJ-83 原文：能说明 `BaseEntity` / `BaseMapperPlus` / QueryBuilder 的所有权：DAO 或 classic ServiceImpl 才持 Mapper。档是**解释+边界**。前置 L-003。矩阵几何 **不是 1:1 三个类名**：

- (c) 独行 `dao-mapper-ownership`（Component，仍 uncovered，备注 L-083）。
- (b) 独行 `BaseMapperPlus / QueryBuilder`（口号「不纯查询；DAO 或 classic ServiceImpl 才持有」，仍 uncovered，备注空）。
- **没有** `BaseEntity` 独行。五枚填充章是同一把钥匙上的**标签贴纸**，不是第三张矩阵行。
- **没有** `IService` / `ServiceImpl` 继承行。DEC-009：生产代码不继承 MP `IService`/`ServiceImpl`。不要发明。

矩阵 (c) `wta-modules` / `layered-classic` / `wta-common 工具间` 已 covered（L-002 / L-003）。`MySQL 8.4 基座` / `mysql-base` 仍 uncovered（L-085）。`Redis 8` / `LoginHelper / StpUtil` 仍 uncovered（L-084）。(b) `NotifyClient.send / NotifyDispatcher.send` 仍 uncovered（L-082）。(d) 「失败路径 会话丢失」挂 L-084，**不是**本课走廊。本批按派单审问 **(c) 所有权 + (b) 两件不纯查询家具**；口试按磁盘：钥匙在**层**不在文件名；`*Dao*.java` **零** `import QueryBuilder` 仍算所有权成立；裸 `BaseMapper` 仍只能被 DAO 持有；`TestBatchController` 是 L-003 反例不是模板。不要把 `BaseEntity` 升成第三格，不要发明 IService 行，也不要提前盖 MySQL / Redis / NotifyDispatcher。

抽查锚点（2026-09-17）：`backend/wta-common/wta-common-mybatis/src/main/java/org/namewta/common/mybatis/core/{domain/BaseEntity,mapper/BaseMapperPlus,mapper/LambdaCrudChainWrapper,query/QueryBuilder,query/LambdaQueryBuilder,query/LambdaJoinQueryBuilder,page/PageQuery}.java`；`handler/InjectionMetaObjectHandler.java`；`aspect/DataPermissionPointcut.java`；`config/MybatisPlusConfig.java`；`resources/common-mybatis.yml`；`META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`；`wta-admin/.../application.yml` `mapperPackage` / `enableLogicDelete`；`03-backend-module-modes.md`；`02-decisions-and-exceptions.md` DEC-009；`TestDemo.java` / `TestDemoServiceImpl.java` / `TestDemoMapper.java` / `TestBatchController.java` / `TestEncryptController.java` / `EsCrudController.java`；`SysOperLogServiceImpl.java`；`SysUserMapper.java`；`SysOssConfigMapper.java`；`SystemOpenApiCredentialService.java`；`OssStorageReadinessService.java`；`PersonApplicationDao.java` / `PersonApplicationMapper.java` / `PersonModuleArchitectureTest.java`；`NotifyNotificationDao.java` 与 notify/third 各 Mapper；`SsoAuthorizationCodeDaoImpl.java`；`CompleteAutoPassComponent.java`；`WorkflowHistoryOssOwner.java`。全仓 `*Dao*.java` **零** `QueryBuilder`；全仓 **零** `com.baomidou.mybatisplus.extension.service.IService` / `ServiceImpl`。

## Q1 澄清

- objective_id: OBJ-83
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-83；chain L-083 覆盖列 `B:BaseMapperPlus B:QueryBuilder C:dao-mapper-ownership`；课 `claimed_cells` 两格；矩阵 (c) `dao-mapper-ownership` 仍 uncovered；(b) `BaseMapperPlus / QueryBuilder` 仍 uncovered、无 BaseEntity 行、无 IService 行；(c) 房间/登记表/工具间已 covered；(d) 无本课走廊；课开篇「口试名单就是矩阵 (c) 这一格加上 (b) 这一行」+「BaseEntity 不是第三张矩阵行」+ 负空间 L-082 / L-084 / L-085 / L-016 / L-003 / L-074
- difficulty: medium

课把仓库钥匙收成「两把合法口袋 + 三件家具，家具里只有两件进矩阵」。计划里至少有五套可数对象，派单点名 **两格 (c)+(b)**，但 OBJ 写了 **三个类名**：

1. **OBJ-83 原文。** `course.md`：能说明 `BaseEntity` / `BaseMapperPlus` / QueryBuilder 的所有权：DAO 或 classic ServiceImpl 才持 Mapper。一句里是 **所有权规则 + 两件不纯查询家具 + 一张贴纸**，不是 `10-cde-base-ddl.sql`，不是 Redis 写会话，不是 `NotifyDispatcher.send`，也没有把 MP `IService` 写成口试清单。
2. **chain / 课 `claimed_cells`。** 两格。符号是 `C:dao-mapper-ownership` 与 `B:BaseMapperPlus` / `B:QueryBuilder`。矩阵把后两个印成 **一行**。磁盘上 **没有** 第三张 `BaseEntity` 行，也 **没有** `IService` 行。
3. **矩阵两处对齐。** (c) 与本课第一格 1:1。 (b) 口号就是课的那句「不纯查询；DAO 或 classic ServiceImpl 才持有」。Lead 若按这两行标 covered **不会**绑死 L-085 基座、L-084 会话、L-082 同步入口。Lead 若按 OBJ 三个类名拆成三行，会发明库存没有的格子。
4. **课自己的操作定义。** 所有权看**层**：layered 只有 `@Repository` DAO 许持 Mapper / Wrapper / QueryBuilder / `PageQuery`；classic CRUD 房间的 Service（常见 `*ServiceImpl`，也可以不叫 Impl）许持。UseCase、layered Service、Controller **不得** `import` 本模块 `mapper.`。`BaseEntity` 只给 `createDept/createBy/createTime/updateBy/updateTime`；`version` / `delFlag` 写在子类。
5. **文件名不是测试。** classic 的 `SystemOpenApiCredentialService`、`OssStorageReadinessService` 不叫 Impl，仍持 Mapper。layered 的任何 Service **都不许**，哪怕改名叫 Impl。`SsoAuthorizationCodeDaoImpl` 仍是 DAO。job / ai 登记 classic 但没有 Mapper 链，本课没有第三把钥匙。
6. **邻格。** L-003 登记表（已 covered）。L-016 角色 `data_scope` 怎么变成 SQL。L-074 demo 八扇 HTTP。L-082 同步入口。L-084 填充器只**读** LoginUser。L-085 六份 SQL 与 `@DS`。

问：本两格闭合标准是「(c) 钥匙在层不在文件名（layered 只有 DAO；classic 的 Service/Impl 才许；门卫/管家/新房子厨师不许）+ (b) Plus 能写能删能转 VO、QueryBuilder 是每次新实例的工厂且 `lambdaJoin` 链能自己开火，且 BaseEntity 五枚章当贴纸不升第三行、不发明 IService 行、不提前盖 MySQL/Redis/NotifyDispatcher」，还是 OBJ 那三个类名必须先拆成三张矩阵行、或必须等全仓 DAO 改用 QueryBuilder 才给 (b) 盖章？Lead 若凭「BaseEntity 四字在 OBJ 里」发明第三行，是否假行？Lead 若因 `*Dao*.java` 零 `import QueryBuilder` 就把 (b) 判成未讲，是否假判？Lead 能否凭本课把 (c) 与 (b) **两行一起**标 covered？

## Q2 证据

- objective_id: OBJ-83
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: BaseEntity 25–50；BaseMapperPlus 37 / 105–125 / 143–154 / 206–223；LambdaCrudChainWrapper 836–837；QueryBuilder 10–50；LambdaJoinQueryBuilder 922–945；InjectionMetaObjectHandler 29 / 37–70 / 80–97；DataPermissionPointcut 26–28；MybatisPlusConfig 35 / 46–54 / 141；common-mybatis.yml 24–31；AutoConfiguration.imports 1；application.yml mapperPackage / enableLogicDelete；modes.md 9–10 / 18–26 / 28；DEC-009 41–44；TestDemo 19 / 59–66；TestDemoServiceImpl 30–32 / 41–43 / 90–97；TestDemoMapper 24 / 33–94（无 selectById 贴纸）；TestBatchController 27–30 / 48；TestEncryptController 27–28；EsCrudController 24；SysOperLogServiceImpl 77–88；SysUserMapper 66–84；SysOssConfigMapper 19；SystemOpenApiCredentialService 29–34；OssStorageReadinessService 31–34；PersonApplicationDao 27–30；PersonApplicationMapper 23；PersonModuleArchitectureTest 92–129 / 143；NotifyNotificationDao 14–24；notify 五张裸 BaseMapper + 配置侧 Plus；third 五张裸 BaseMapper；SsoAuthorizationCodeDaoImpl 12–16 / 31；CompleteAutoPassComponent 37–40 / 90–92；全仓 *Dao*.java 零 QueryBuilder；controller/usecase 零 QueryBuilder；layered Service 零 mapper import；全仓零 MP IService/ServiceImpl
- difficulty: high

课的所有权 + 两件家具哪些能在工作树直接复现，哪些只是生产调用点比课表窄、或口试把「谁许持有」说成「今天谁 import 了 QueryBuilder」？

**仍能复现（(c) `dao-mapper-ownership`）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 登记表：layered DAO 是业务持久化唯一入口；classic ServiceImpl 可持 Mapper；不得扩大越层 | `03-backend-module-modes.md` 9–10 / 42 |
| layered 正例：`PersonApplicationDao` `@Repository`，字段 `PersonApplicationMapper mapper` | Dao 27–30 |
| person 架构测试：Service 不得 `import ...mapper.` / `IService` / `BaseMapper` / `QueryWrapper`；UseCase 不得 dao/mapper；DAO 必须 mapper + `@Repository`；Controller 只 import usecase | `PersonModuleArchitectureTest` 92–129 |
| Mapper 第二泛型写成同一实体，不是 Vo；禁止 `@Select` 与 `domain.vo` | `PersonApplicationMapper` 23；测试 143–145 |
| notify DAO 类注释「业务服务不得直接依赖 MyBatis Mapper」；持七个 Mapper；现用 `new LambdaQueryWrapper<>()` | `NotifyNotificationDao` 14–24 / 28 |
| DAO 可叫 `*DaoImpl`：`SsoAuthorizationCodeDaoImpl` 仍 `@Repository`，字段 Mapper，Wrapper 在 DAO | 12–16 / 31 |
| classic 正例：`TestDemoServiceImpl` 字段 `TestDemoMapper demoMapper`；`queryById` → `selectVoById`；`buildQueryWrapper` 用 `Wrappers.lambdaQuery()` | ServiceImpl 32 / 41–43 / 90–97 |
| classic 不带 Impl 仍持 Mapper：`SystemOpenApiCredentialService` 字段 `SysOpenApiCredentialMapper mapper`；`OssStorageReadinessService` 持两枚 Mapper | Credential 29–34；Readiness 31–34 |
| 门卫抱 Mapper 不是模板：`TestBatchController` 注释「为了便于测试 直接引入mapper」，`insertBatch` 从 HTTP 打进仓库 | 27–30 / 48 |
| 同款越层：`TestEncryptController` 字段 `TestDemoEncryptMapper mapper` | 27–28 |
| ES 演示抱 `DocumentMapper`，矩阵 deferred，不是本课 MyBatis 钥匙 | `EsCrudController` 24 |
| layered Service / UseCase **零** `import ...mapper.`；Controller 的 mapper import 只出现在 demo 三扇窗 | 2026-09-17 grep |
| 全仓生产 **零** `extends ServiceImpl` / `implements` MP `IService`。classic 的 `ITestDemoService` 是项目接口，不是 MP | grep 空；`ITestDemoService` 18 |
| DEC-009：DAO/Service 都不继承 MP `IService`；新模块默认 layered | `02-decisions-and-exceptions.md` 41–44 |
| job / ai 登记 classic、无 Mapper 链；本课不发明钥匙 | modes.md 24–26；与 L-003 / L-078 / L-080 对照 |

**仍能复现（(b) `BaseMapperPlus / QueryBuilder` + 贴纸 `BaseEntity`）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| `BaseMapperPlus<T,V> extends BaseMapper<T>` | Plus 37 |
| `lambda()` 每次新 `LambdaCrudChainWrapper`，先 `mapperProxy()` 从容器取代理 | 105–125 |
| `insertBatch` → `Db.saveBatch`；`updateBatchById` → `Db.updateBatchById` | 143–154 |
| 链上 `delete()` 能删 | Chain 836–837 |
| `selectVoById` = `selectById` + `MapstructUtils.convert`；SQL 仍是实体行 | Plus 218–223 |
| `QueryBuilder` 私有构造；`lambda` / `lambdaJoin` / `lambdaJoin(alias, class)` | QueryBuilder 10–50 |
| `lambdaJoin(...).list/page/count` **能自己开火** | JoinBuilder 922–945 |
| classic 正例：`SysOperLogServiceImpl.buildQueryWrapper` 用 `QueryBuilder.lambda` + `likeIfText` / `eqIfPresent` | 77–88 |
| Mapper default 仍在 SQL 面：`SysUserMapper.selectUserExportList` `lambdaJoin("u", SysUser.class)`，`@DataColumn` 用 `d.dept_id` / `u.create_by` | 66–84 |
| 生产 join 调用点更爱 `.build()` + `selectJoinList`，Javadoc 才演示 `.list(Vo.class)` | UserMapper 71–84 vs JoinBuilder 195–201 |
| 2026-09-17 `*Dao*.java` **零** `import QueryBuilder`；`controller/` `usecase/` 也为零；QueryBuilder 出现在 system/workflow 的 `service/impl`、若干 Mapper default、LiteFlow、`WorkflowHistoryOssOwner` | 全仓 29 处 import，0 处 DAO |
| notify 运行时五张仍裸 `BaseMapper`：Intent / Recipient / Delivery / Attempt / Outbox。配置/公告侧是 Plus | 各 Mapper 接口 |
| third **五张**都裸 `BaseMapper`：Provider / Endpoint / Credential / Invocation / Statistic | 各 Mapper 接口 |
| 裸奔仍只能被 DAO 持有；所有权不看第二基类名字 | NotificationDao 字段表 |
| `BaseEntity` 五字段，fill INSERT 或 INSERT_UPDATE；**无** `@Version` / `@TableLogic` | BaseEntity 25–50 |
| `TestDemo extends BaseEntity`，自己声明 `@Version Long version`、`@TableLogic Long delFlag` | 19 / 59–66 |
| `NotifyIntent extends BaseEntity` 但 `version` 只是 `Integer` 字段、无 `@Version` | 16 / 35 |
| 填充器 `DEFAULT_USER_ID = -1L`；插入时 `createBy` 为空才填；更新时 **总是**覆盖 `updateTime` | Handler 29 / 46–58 / 83–90 |
| 拦截器链：数据权限 → 分页 → 乐观锁。注释写了全表阻断，Bean **没有** `BlockAttackInnerInterceptor` | Config 46–54 / 141 |
| `@MapperScan("${mybatis-plus.mapperPackage}")`；admin YAML `org.namewta.**.mapper`；yml `idType=ASSIGN_ID`，逻辑删除 1/0 | Config 35；application.yml 295；common-mybatis.yml 24–28 |
| 数据权限不对继承生效；`TestDemoMapper` 给 `selectVoPage` / `selectVoList` / `selectByIds` / `updateById` 贴了，**没** override `selectById` | Pointcut 26–28；Mapper 33–94 |
| LiteFlow 存量泄漏：`CompleteAutoPassComponent` 注入 `FlowTaskMapper` 并 `QueryBuilder.lambda(FlowTask.class)` | 40 / 90–92 |
| 短注解 SQL 窄例外：`SysOssConfigMapper` `@Select ... for update` | 19 |
| `PageQuery` 默认 `pageSize=Integer.MAX_VALUE` | PageQuery 59 |

**课写过、测试名单更窄或口试数字写偏（不是证伪本两格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「`lambdaJoin.list()` 自己开火」 | 类上 922 有。生产 Mapper 多 `.build()` + `selectJoinList`。能力仍在，调用点更窄 |
| 「全仓搜索 dao/usecase/controller 为零」 | 2026-09-17 仍为零。另有 `WorkflowHistoryOssOwner`（classic `@Service` 不带 Impl）也 import QueryBuilder |
| 「notify 运行时几张表还停在裸 BaseMapper」 | 正好五张。公告/配置 Mapper 已是 Plus，不要口试成「notify 全裸」 |
| 「没有一张全仓测试扫 Controller 不 import Mapper」 | person 测试锁 Controller 只 import usecase、不 import service，**没**写「不得 mapper」。所以 `TestBatchController` 能活 |
| 课 `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |
| `wta-admin` 的 `SysLoginService` / `AdminSsoIdentityService` 也持 `SysUserMapper` | 登记表写明 admin 不按业务五层套。不是第三把合法钥匙，也不是本格空洞 |

问：在层/文件名对照、Plus 写删转 VO、QueryBuilder 工厂 + join 自执行能力、DAO 零 QueryBuilder 仍用 Wrapper、裸 BaseMapper 仍被 DAO 持有、TestBatch 越层、零 MP IService 已经能互证的前提下，(c)(b) 是否还要把「生产 join 更爱 build+selectJoinList」「TestEncrypt 同款越层」「admin 组装服务也抱 Mapper」写成开篇半句，才算本两格讲完？还是这些已经落在课的表/反例/边界里，Lead 可以把 **(c) `dao-mapper-ownership`** 与 **(b) `BaseMapperPlus / QueryBuilder`** 视为可 covered？

## Q3 假设与反例

- objective_id: OBJ-83
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: DAO 零 QueryBuilder；NotifyNotificationDao 用 LambdaQueryWrapper；third/notify 裸 BaseMapper 仍 DAO 持有；SystemOpenApiCredentialService 无 Impl 后缀；SsoAuthorizationCodeDaoImpl 仍是 DAO；layered Service 零 mapper；TestBatchController / TestEncryptController；CompleteAutoPassComponent；EsCrudController；零 MP IService；L-003 同一模块禁止混模式；modes.md 未登记默认 layered
- difficulty: high

课的隐含假设若反转，合格读者会把哪把钥匙或哪件家具认错？

1. **「`*Dao*.java` 零 QueryBuilder，所以所有权没遵守 / (b) 没讲。」** 遵守的是谁许持有。notify/sso/third DAO 现用 `LambdaQueryWrapper` 仍算 DAO 持有。课误区 5 / 正例 5 覆盖。
2. **「只有文件名带 `ServiceImpl` 的类才许抱 Mapper。」** 看层。classic 不带 Impl 的 `@Service` 可以；layered 带 Impl 也不可以。课开篇对照表 / 误区 4 覆盖。
3. **「DAO 必须叫 `XxxDao`，叫 `DaoImpl` 就是 classic。」** `SsoAuthorizationCodeDaoImpl` 仍是 `@Repository` DAO。课开篇对照表覆盖。
4. **「同一模块里，这个方法 layered、那个方法 classic。」** L-003 禁止混用。混用后「ServiceImpl 持 Mapper」无法判定合法还是越层。本课不改登记表。
5. **「`NotifyIntentMapper extends BaseMapper` 所以 notify 不是 layered。」** layered 看调用链和 DAO，不看第二基类。课误区 21 / 边界 1 覆盖。
6. **「third 表上没登记，可以 classic 持 Mapper。」** 未登记默认 layered。third 磁盘已经是 DAO 持有。课误区 18 覆盖。
7. **「`BaseMapperPlus` 等于只读。」** 能 `insertBatch` / `lambda().delete()`。课误区 2 覆盖。
8. **「我没注入 Mapper、只 new 了 QueryBuilder.lambdaJoin().list()，所以没越层。」** 口袋里是持久化枪。课边界 4 覆盖。生产调用点虽更爱 `build()`，能力仍在类上。
9. **「门卫抱 Mapper 是 classic 存量允许。」** 允许的是 ServiceImpl。`TestBatchController` / `TestEncryptController` 是棘轮拧松。课反例 1 / L-003。
10. **「新模块 `extends ServiceImpl`。」** 全仓搜不到。DEC-009 禁止。课反例 4。不要把 classic 的项目接口 `ITestDemoService` 认成 MP `IService`。
11. **「`BaseEntity` 含 version / delFlag。」** 不含。看 `TestDemo` 子类。课误区 7。
12. **「数据权限贴纸贴在接口上，`selectVoById` 也有。」** Pointcut 不对继承生效。`selectVoById` 走没贴的 `selectById`。课反例 5。
13. **「LiteFlow 节点持 QueryBuilder 是新模块样板。」** 存量泄漏。课边界 3。
14. **「`EsCrudController` 证明 Controller 可以抱 Mapper。」** 那是 ES 演示，矩阵 deferred。课类比失效 5。
15. **「job/ai 没有 Mapper 所以本课失败。」** 它们没有这把钥匙。课误区 17。
16. **「把 QueryBuilder 放进 Spring 单例字段，记得 `clear()` 就行。」** 每个操作新实例。课反例 3。
17. **「admin 的 `SysLoginService` 也持 Mapper，所以还有第三把钥匙。」** 登记表：`wta-admin` 不按业务五层套。不要发明第三格。
18. **「本课覆盖 `@DS` / 六份 SQL / LoginHelper 写 Redis / NotifyDispatcher。」** L-085 / L-084 / L-082。课误区 19–20 / 开篇负空间。

问：读完后仍说不出的，是不是「DAO 才碰 Mapper」这句口号，而是 **谁许持有 ≠ 今天谁 import 了 QueryBuilder、层 ≠ 文件名、裸 BaseMapper 仍有所有权、同一模块禁止混模式、TestBatch 不是模板、BaseEntity 不是第三行、零 MP IService 不是漏讲**？

## Q4 视角

- objective_id: OBJ-83
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (b)/(c)/(d)；OBJ-83 vs L-003/L-016/L-074/L-082/L-084/L-085；chain 两 token vs 矩阵一行 (b)；BaseEntity 贴纸 vs 未入库第三行；DEC-009 vs 未入库 IService 行
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这两格**？

| 视角 | 多出来的边 | 是否本课这两格 |
| --- | --- | --- |
| 矩阵维护者 | (c) 所有权独行 1:1 可盖章；(b) 一行绑住 Plus+QueryBuilder；无 BaseEntity 行、无 IService 行 | **是** 闭合定义。两行可视为已讲并可 covered。不要发明第三行 |
| 登记表（L-003） | layered 三行 + third 磁盘五层未入表；classic 五行；同一模块一种模式；job/ai 无 Mapper | 钥匙在哪一层 **是**。登记表本身 **否**（已 covered） |
| 仓库管理员 / classic 厨师 | DAO 或 classic Service 才持 Mapper/Wrapper/QueryBuilder | **是** `C:dao-mapper-ownership` |
| 叉车与购物单 | Plus 能写能删能转 VO；QueryBuilder 每次新实例；join 能自开火 | **是** `B:BaseMapperPlus / QueryBuilder` |
| 标签贴纸 | 五枚章；子类才有 version/delFlag；没登录 `-1L`；更新时间总覆盖 | **是** OBJ 走查，不是矩阵第三格 |
| 数据权限电工（L-016） | 贴纸贴在真正执行的 Mapper 方法；`mapperProxy`；不沿继承 | 贴纸位置 + 代理 **是**。角色 `data_scope` 怎么拼 SQL **否** |
| demo 八扇（L-074） | ServiceImpl 字段是 Mapper；`selectVoById` 没贴权限 | 持有关系一句 **是**。八扇 HTTP **否** |
| 通知同步入口（L-082） | `NotifyClient.send` / `NotifyDispatcher.send` | 对照「不是 Mapper」**是** 负空间。同步入口 **否** |
| 会话河（L-084） | 填充器读 `LoginHelper.getLoginUser()` | 读 LoginUser **是** 对照。写 Redis / 会话丢失 **否** |
| MySQL 基座（L-085） | 10/50 是自有表唯一基座；`@DS` 所有权 | yml 1/0、ASSIGN_ID 对照 **是**。(c) 容器 / `@DS` **否** |
| 组装大厅 `wta-admin` | `SysLoginService` 持 `SysUserMapper` | 不是业务五层、不是第三把钥匙 **是** 边界。登录窗 **否**（L-011 / L-013） |
| 厂商 WarmFlow Mapper | `FlowTaskMapper` 来自 `org.dromara.warm.flow.orm.mapper` | 不是这台 Plus **是** 对照。引擎内部 skip **否** |

问：Lead 若凭「两个 claimed 符号在列里」把 (c) 和 (b) 一起标 covered，是否还要求读者能指着 **层 ≠ 文件名、Plus 不纯查询、QueryBuilder 谁许持有、DAO 今天不用 QueryBuilder 仍合格、裸 BaseMapper 仍有所有权、TestBatch 不是模板、BaseEntity 不当第三行**？这些是不是已经在本格函数表/反例里，而不需要等 DAO 全仓改名、也不需要发明 IService 行、也不需要等 L-085 才给本两格当已讲？Lead 若把 `BaseEntity` 或 MP `IService` 当成第三格，是否发明了矩阵行？

## Q5 元问题

- objective_id: OBJ-83
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课；一课超 1 个容器是 Lead 裁决不是 b01 填表）；本课累计 5/10；`split_depth: 0`；U9 已有 L-082…L-085 四课；矩阵 (c) `dao-mapper-ownership` 仍 uncovered、(b) `BaseMapperPlus / QueryBuilder` 仍 uncovered、无 BaseEntity 行、无 IService 行
- difficulty: low

建议：`defer`

原因：

- **(c) 与 OBJ-83 / chain / 课第一格 1:1。** L 合同章节齐全（宏观钥匙地图、直觉、定义表、机制链 1–10、带 caption / 文字等价物的图、正反例、变式、误区、暂停走查、来源表 S-008/S-007/S-L003-01/S-L083-01…09）。能复现：layered DAO 唯一持有、classic Service/Impl 可持有、门卫越层不是模板、零 MP IService、层 ≠ 文件名。不必为「DAO 才碰 Mapper」再挖五问。Lead 可将 `C:dao-mapper-ownership` 标 `covered`。
- **(b) 与矩阵独行 1:1。** Plus 能写能删能转 VO、`lambda()` 走代理、QueryBuilder 私有构造 + 两入口、join 链类上能自开火、每次新实例、DAO 今日零 import 仍用 Wrapper——已在课的表/反例里。chain 两个 B token 不要拆成两行。Lead 可将 `B:BaseMapperPlus / QueryBuilder` 标 `covered`。
- **BaseEntity 是贴纸。** 五字段、无 version/delFlag、`-1L`、更新覆盖时间已走查。不要升成第三格才给前两格盖章。
- 本批可粘贴的精度项不挡住盖章：（1）生产 join 调用点更爱 `.build()` + `selectJoinList`，`.list()` 主要在 Javadoc；（2）`TestEncryptController` 与 TestBatch 同款越层，课只点名后者；（3）`wta-admin` 组装服务持 `SysUserMapper` 不是第三把钥匙；（4）notify 配置 Mapper 已是 Plus，裸奔的是运行时五张；（5）person 架构测试没扫 Controller 的 mapper import；（6）chain 估时 35、课 38。不要把 MySQL 基座、Redis 会话、NotifyDispatcher、角色 `data_scope` SQL 塞进本格。
- 不建议 `mine-more`：b01 未证明本两格还有真实矩阵空洞；再五问只会把 L-085 `@DS`、L-084 Redis、L-082 dispatcher 或 L-016 拼 SQL 再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；DAO 未改用 QueryBuilder 是棘轮，课已写明。不建议 `split-lesson`：未满 10 问；U9 已有四课且 cap 15，但拆课条件未触发（不是两个 C4 讲不完）；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 BaseEntity 行或 IService 行。不建议等全仓 DAO 改 QueryBuilder 才给 (b) 当已讲。不建议用本课改 (d) 会话丢失或 MySQL 10/50。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| C:dao-mapper-ownership | 层 ≠ 文件名、layered DAO 唯一持有、classic Service/Impl 可持、门卫越层不是模板、零 MP IService——均与源码对齐。残留精度：TestEncrypt 同款越层课未点名；person 测试未扫 Controller mapper import；admin 组装服务持 Mapper 需一句「不是第三把钥匙」。 | 可选半句：口试以层为准，不以 `*Impl` 后缀为准；demo 入口抱 Mapper 一律当反例。不要求重写机制链。Lead 可将矩阵该行标 `covered`（证据锚本 probe 与 L-083 来源表 S-L083-04…08）。 | defer（本行可 covered；精度项不挡盖章） |
| B:BaseMapperPlus / QueryBuilder | Plus 写删转 VO、`lambda/mapperProxy`、QueryBuilder 工厂、join 自执行能力、DAO 零 import 仍用 Wrapper、裸 BaseMapper 仍 DAO 持有——均与源码对齐。残留精度：生产 join 更爱 `build+selectJoinList`。 | 可选半句：口试「能自己开火」以 `LambdaJoinQueryBuilder.list/page/count` 为准，不以今日调用点计次。Lead 可将矩阵该行标 `covered`（证据锚本 probe 与 S-L083-02 / S-L083-03）。不要把 chain 两个 token 拆成两行。 | defer（本行可 covered；精度项不挡盖章） |
| BaseEntity | 课已走查五枚章与 `-1L`。矩阵无此符号。 | 不要发明第三格。口试仍要会指字段，因为它是 OBJ 贴纸。 | defer（未入库；保持贴纸不是格子） |
| IService / ServiceImpl 继承 | 全仓生产零 MP 继承。矩阵无此行。DEC-009 已写进课。 | 不要发明继承行。classic 的项目接口 `ITestDemoService` 不是 MP `IService`。 | defer（未入库） |
| C:layered-classic | 同一模块一种模式、登记两列、job/ai 无 Mapper。 | 保持。L-003（已 covered）。 | defer（L-003 已 covered） |
| B:ISysPermissionService / `@DataPermission` SQL | 贴纸位置与代理已点。角色 `data_scope` 怎么变成 SQL 未讲。 | 保持。L-016。 | defer（L-016） |
| A:TestDemoController.* | ServiceImpl 持 Mapper、`selectVoById` 没贴权限已借用。 | 不要用本课再盖八扇 HTTP。 | defer（L-074） |
| B:NotifyClient.send / NotifyDispatcher.send | 同步入口不是 Mapper。该行仍 uncovered。 | 不要用本课盖 dispatcher。 | defer（L-082） |
| B:LoginHelper / StpUtil；(d) 会话丢失 | 填充器只读 LoginUser。 | 不要用本课盖写 Redis。 | defer（L-084） |
| C:MySQL 8.4 基座 / mysql-base；(d) 业务表只进 10/50 | yml 1/0、ASSIGN_ID 是 (b)/(c) 对照，不是容器闭合。 | 不要因本课标容器 covered。 | defer（L-085） |
| C:Redis 8 | 填充器读会话，不写。 | 不要因本课标容器 covered。 | defer（L-084） |
| WarmFlow `FlowTaskMapper` / LiteFlow | 存量泄漏已标。不是这台 Plus。 | 保持棘轮；新模块不许抄。 | defer（vendor-engine / 存量泄漏，不另开格） |
| EsCrudController / DocumentMapper | ES 演示。 | 保持 `deferred(capability-demo-not-product-room)`。 | defer（capability-demo） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；两格无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`；(c) 与 (b) 已讲完并显式禁止把 BaseEntity / IService 升成新行；合格读者说得出两把钥匙（layered DAO；classic Service/Impl）、两件不纯查询家具（Plus 能写能删；QueryBuilder 每次新实例且 join 能开火）与失败路径（门卫抱 Mapper、UseCase 组 Wrapper、缓存可变购物单、新模块继承 MP IService、把 DAO 没用 QueryBuilder 当成违规）；把可选半句写进原课不会超过 40 分钟；登记表、数据权限拼 SQL、demo 八扇、dispatcher、Redis、MySQL 10/50 已有邻课。满 10 问前不拆。U9 现有 L-082…L-085 四课，拆课会把格子拿走，但 10 问条件未满足，本批禁止填 split 表。不要把 BaseEntity 或 IService 拆成新课。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / split 提案：否（miner 不改矩阵）；split 提案为 none；缺口表对 L-003 登记表（已 covered）、L-016 拼 SQL、L-074 八扇、L-082 dispatcher、L-084 Redis/会话、L-085 MySQL/`@DS`、未入库 BaseEntity 行、未入库 IService 行、WarmFlow 泄漏、ES 演示显式建议 `defer`；对矩阵 (c) 行 `dao-mapper-ownership` 与 (b) 行 `BaseMapperPlus / QueryBuilder` 建议 Lead 标 `covered`（证据锚本 probe 与 L-083 来源表 S-L083-01…08）。本课不主张发明 BaseEntity 矩阵行、发明 IService 继承行、因 DAO 今日零 QueryBuilder 拒盖 (b)、用本课盖 NotifyDispatcher / LoginHelper / 10-cde-base，或等邻课 mined 才给本两格盖章。
