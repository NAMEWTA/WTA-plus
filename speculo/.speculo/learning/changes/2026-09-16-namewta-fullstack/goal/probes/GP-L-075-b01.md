---
artifact: learning-goal-probe
probe_id: GP-L-075-b01
lesson_id: L-075
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-075 / b01

审问对象是已写成的 Lesson `lessons/L-075-test-tree.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **一格**：`A:TestTreeController.list,export,getInfo,add,edit,remove`。OBJ-75 原文：能口述 `TestTreeController` 树表。档是**解释+走查**。矩阵 (a) **有独行**，六名与磁盘 1:1：`list, export, getInfo, add, edit, remove`，模块 `wta-demo`，仍 uncovered，备注「树表」。矩阵 (a) **没有** `ITestTreeService` / `queryList` / `buildTree` / `TreePage.vue` 行。厨房 `createDemoService, createRichTextService` 与九工厂行里的 `createDemoWebDomain` 仍 uncovered（L-077）。矩阵 (b) **没有** GET 导出 / 空 TODO 删除 / 类上数据权限 / wrapper 丢掉 parentId 独行；demo 已印的 (a) 邻居是 `TestDemoController` 八扇（L-074）与 `TestRichTextController` 六扇（L-076），均 uncovered。矩阵 (c) `wta-modules` / `layered-classic` 已 covered（L-002 / L-003）；登记表 classic 行就是 `wta-modules/wta-demo`。MySQL 8.4 / `dao-mapper-ownership` 仍 uncovered。矩阵 (d) **没有** 树表 CRUD 走廊；已有数据流都不挂 `/demo/tree`。本批按派单只审问这一格 (a) 切片；口试按磁盘：同一门牌 `/demo/tree` **正好 6** 个公开映射，classic 三跳，门卫只抱 `ITestTreeService`，`list` 扁 `List` 且 wrapper 不读 `parentId`，`export` 是 **GET** 流、厨房与树页没收，`getInfo` 无行仍 `R.ok(null)` 且**走类上数据权限**，`add`/`edit` 注解组 + 默认 5000ms 防重，`remove` 空 TODO 后逻辑删（可见行才动，不整票作废）。不要把 Service/Mapper/Vue 升成第二张 (a)，不要发明 (b)，也不要把 L-074 八扇、L-076 富文本、L-077 厨房五枪算进本切片。

抽查锚点（2026-09-17）：`TestTreeController.java`；`ITestTreeService.java`；`TestTreeServiceImpl.java`；`TestTreeMapper.java` 与空 XML；`TestTree` / `TestTreeBo` / `TestTreeVo`；对照 `TestDemoController` / `TestDemoMapper` / `TestDemoServiceImpl`；`FlwCategoryController.export` / `categoryTree`；`SysDeptController.excludeChild`；`RepeatSubmit`；`BaseController.toAjax`；`DataPermission` / `DataPermissionPointcut` / `PlusDataPermissionInterceptor` / `PlusDataPermissionHandler`；`BaseMapperPlus.selectVoById` / `selectVoList`；登记表 `03-backend-module-modes.md`；`wta-admin/pom.xml` `bundle-full` / `bundle-core`；DDL `test_tree`；DML 菜单 1761400000000001506–1511 与十三行种子；厨房 `domains/demo/src/{index.ts,index.test.ts,test-tree/index.ts}`；web-domain `index.ts` / `index.test.ts` / `composables.ts` / `TreePage.vue`；`adapters/axios-browser` `downloadWithAxios`；OpenAPI `/demo/tree**`。工作树 **零** `TestTree*Test`。

## Q1 澄清

- objective_id: OBJ-75
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-75；chain L-075 覆盖列只 `A:TestTreeController.list,export,getInfo,add,edit,remove`；L-075 `claimed_cells` 一格；矩阵 (a) 六名独行仍 uncovered、无 Service/Vue 行；厨房与九工厂行仍 uncovered（L-077）；(b) 无本课符号、单表/富文本挂 L-074/L-076；(c) 房间与登记表已 covered；(d) 无树表 CRUD 流；课开篇六扇 + 负空间 L-074 / L-076 / L-077 / L-003 / L-017 / L-067
- difficulty: medium

课把树表抽屉收成「一块牌子、六扇窗、classic 三跳、扁列表 + 浏览器胶水」。计划里至少有四套可数对象，派单点名 **一格 (a) 切片**：

1. **OBJ-75 原文。** `course.md`：能口述 `TestTreeController` 树表。一句里是 **走查**，不是 `TestDemoController` 八扇、不是 `TestRichTextController.assets`、也没有把厨房五枪写成第二张矩阵行。课把 OBJ 扩成 classic 抽屉 + 扁列表拼树 + GET 导出，那是走查，不是第二格。
2. **chain / 课 `claimed_cells`。** 一格。符号就是六个方法名。矩阵把它们印成独行，与磁盘公开映射 1:1。chain **不**写 Service、不写 Vue、不写 (b)。
3. **矩阵 (a) 对齐。** 本课有独行，Lead 按现印六名标 covered **不会**把单表八扇或厨房假闭合。不要把 `ITestTreeService` 或 `queryList` 另立一行才给本切片盖章。厨房独行与九工厂行仍 uncovered，本课不得当第二张前端章。
4. **课自己的操作定义。** 门牌 `/demo/tree`；GET 三 / POST 一 / PUT 一 / DELETE 一；无类上 `@SaIgnore`、无 `@ConditionalOnEnable`。六扇都贴权限字。门卫只注入 `ITestTreeService`，不抱 Mapper。`list` 扁 `List` 不分页，wrapper 不读 `parentId`；`export` 同一 `queryList` 改 Excel 流、返回 `void`、动词是 **GET**；`getInfo` 无行仍 `R.ok(null)` 且走类上数据权限；`add`/`edit` `@Validated(Add/EditGroup)` + `@RepeatSubmit` 默认 5000ms；`remove` 空 TODO 后逻辑删。没有 `/page`、`importData`、`treeSelect`、`excludeChild`、`categoryTree`。
5. **导出与树不是第二张 claimed 格。** OBJ 要「能口述六扇」。课把「Java+菜单有 export、树页没扳机、厅堂 download 固定 POST」和「管理页 `list`+`buildTree`、假根 id=0」收进对照走查。不要为了盖章把 `TreePage.vue` 再立一行，也不要把无页导出降成「本课不认这扇窗」。
6. **邻格。** L-074 单表八扇、POST 导出、方法级数据权限、删前点名。L-076 富文本与 OSS 资产。L-077 厨房五枪、权限组四串、`buildTree`。L-003 已认 classic，本课不得再盖 `C:layered-classic`。L-017 部门 `excludeChild`。L-067 分类 `categoryTree` / POST 导出 / 601。能力展示 Controller 保持 `deferred(capability-demo-not-product-room)`。

问：本格闭合标准是「六扇路径/动词/权限/信封 + classic 三跳门卫不抱 Mapper + list 扁且 wrapper 丢掉 parentId / export GET 无页扳机 / getInfo 可空且走类上数据权限 / add·edit 注解组+5 秒防重 / remove 空 TODO 部分删除仍可能 ok，且不升 Service/Vue 矩阵行、不发明 (b)、不提前盖 L-077」？还是 OBJ 那句必须把六个方法拆成六行、或必须等 GP-L-077 厨房缺枪才给本切片盖章？Lead 若凭「demo 房间还没挖完」把这一行留空洞，是否假判成未讲完？Lead 若用本课把厨房或单表八扇标 covered，是否假闭合了 L-077 / L-074？

## Q2 证据

- objective_id: OBJ-75
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 32–106；ITestTreeService 15–55；ServiceImpl 24–29 / 38–39 / 48–52 / 61–67 / 77–84 / 94–97 / 105–107 / 117–121；Mapper 15–19；XML 5–7；Entity 21–63；Bo 22–55；Vo 21–61；RepeatSubmit 20；BaseController 19–30；DataPermission 11 / 28；Pointcut 27–33；Interceptor 55–66 / 83–94；Handler 111–113；BaseMapperPlus 206–223 / 343–360；modes.md 24；pom 148–171 / 192–205；DDL 1709–1724；DML 164–169 / 372–384；TestDemoController 90 / 117–122；TestDemoMapper 24 / 33–94；TestDemoServiceImpl 148–155；FlwCategoryController 61 / 144–145；SysDeptController 57；厨房 index.ts 60–67 / 84–95 / 104–139；index.test.ts 14–48；test-tree/index.ts 3；web-domain index.ts 40–74；index.test.ts 32–43；composables 95–114；TreePage 11–38 / 166–180 / 197–214 / 236–280；axios-browser 261–263；OpenAPI 5931–5942 / 14308–14336；零 TestTree*Test
- difficulty: high

课的六扇 + classic 抽屉哪些能在工作树直接复现，哪些只是测试名单比课表窄、或口试把邻课门卫说成「demo 都一样」？

**仍能复现（本切片）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 类上 `@Validated` `@RequiredArgsConstructor` `@RestController` `@RequestMapping("/demo/tree")`，继承 `BaseController`；无 `@ConditionalOnEnable`、无 `@SaIgnore` | Controller 32–36 |
| 构造只注入 `ITestTreeService`；无 Mapper / `LoginHelper` / `PageQuery` | Controller 38 |
| 六扇：`GET /list`、`GET /export`、`GET /{id}`、`POST /`、`PUT /`、`DELETE /{ids}`；无 `/page`、无 `importData` | Controller 43–106 |
| `list` 权限 `demo:tree:list`，无 `@Log`，入参 `@Validated(QueryGroup.class)`，回 `R.ok(List)`，不是 `PageResult` | 43–47 |
| `export` 权限 export、`@Log EXPORT`、**GET**、`void`、同一 `queryList` → `ExcelBuilder` 表名「测试树表」 | 53–58 |
| Excel 只出父id / 部门id / 用户id / 树节点名 / 创建时间；`id` 无 `@ExcelProperty`；无 `children`、无 `version`、无 `@Translation` | Vo 21–61 |
| `getInfo` 权限 query、路径 `@NotNull Long`、`R.ok(queryById)`，无行不 fail | 66–70；ServiceImpl 38–39；BaseMapperPlus 218–223 |
| `add` 权限 add、`@Log INSERT`、`@RepeatSubmit`、`@Validated(AddGroup.class)`；**不是** `ValidatorUtils` | 76–81；对照单表 117–122 |
| `edit` 权限 edit、`@Log UPDATE`、`@RepeatSubmit`、EditGroup | 87–92 |
| `remove` 权限 remove、`@Log DELETE`、**无** `@RepeatSubmit`；路径 `Long[]` `@NotEmpty`；门卫写死 `true` | 100–105 |
| `@RepeatSubmit` 默认间隔 5000ms；单表 add 才是 2 秒 | RepeatSubmit 20；TestDemoController 117 |
| `toAjax(boolean)` true→ok，false→fail；无 `R.warn` 601 | BaseController 29–30 |
| classic：Impl 唯一字段 `treeMapper`；注释 `// @DS("slave")` 未启用 | ServiceImpl 24–29 / 48 |
| 接口五法；`list` 与 `export` 共用 `queryList`，不要把 `queryList` 背成第七扇 HTTP | ITestTreeService 22–55 |
| `buildQueryWrapper`：`eq` deptId/userId，`like` treeName，**不读** parentId，再 `orderByAsc(id)` | 61–67 |
| `validEntityBeforeSave` 与删除 `isValid` 都是空 TODO；`isValid` true/false 结果相同 | 105–107 / 117–121 |
| `insertByBo` 成功回填 `bo.setId`；INSERT 不被拦截器改写 | 77–84；Interceptor 83–94 只处理 UPDATE/DELETE |
| `updateByBo` → `updateById`；类上数据权限给 UPDATE 加 AND | 94–97；Handler 111 |
| `deleteByIds` 逻辑删；`toAjax(rows>0)`：看得见的撕掉，看不见的静默跳过 | 121；对照单表 150–152 点名抛「您没有删除权限!」 |
| Mapper **类上** `@DataPermission(dept_id + user_id)`；XML 空 `<mapper>` | Mapper 15–19；XML 5–7 |
| 切点：方法注解优先，否则类型/代理接口上的类注解；故 `selectById` / `selectList` / `updateById` / `deleteByIds` 都进拦截器 | Pointcut 27–33 |
| 单表对照：`TestDemoMapper` **无**类上注解，五个方法 override 才贴；`getInfo` 走无注解 `selectById` | TestDemoMapper 24 / 33–94；课反例 9 |
| `selectVoList` = `selectList` + 转 Vo；不执行 XML | BaseMapperPlus 355–360 |
| 实体 `@TableName("test_tree")`、`parentId`、`@Version`、`@TableLogic` Java `Long`；Bo/Vo **没有** version；Bo `parentId` 无校验、无 QueryGroup 约束 | Entity 21–63；Bo 31–55；Vo 31 |
| 登记表 classic：`wta-modules/wta-demo`，不得当 layered 反例 | modes.md 24 |
| `bundle-full` 默认激活才依赖 `wta-demo`；`bundle-core` **没有**这一行 | pom 148–171 / 192–205 |
| DDL `parent_id` 默认 0、`version int`、`del_flag int`；表里没有 `id=0` | DDL 1709–1724 |
| 种子十三行两片林：`…0102` 与 `…0108`；菜单 C+F 含 export `…1511`，无 import | DML 164–169 / 372–384 |
| 分类导出是 **POST**；分类才有 `categoryTree`；部门才有 `excludeChild` | FlwCategoryController 61 / 144；SysDeptController 57 |
| 厨房五枪 list/get/add/update/delete；**没有** exportTree / pageTree | index.ts 84–95 / 135–139 |
| `TreeVO` 才有 `children`；资源标签 `controller:'TestTreeController'` `basePath:'/demo/tree'` | index.ts 60–67；test-tree/index.ts 3 |
| 测试锁 `{ parentId: 0 }` 仍打 `/demo/tree/list`；`getTree('tree/8')` → `tree%2F8`；删除逗号串 `tree%2F8,tree%209` | index.test.ts 31–35 / 43–47 |
| web-domain `demo-tree` 四串 **不含** query / export；`toEqual` 锁死 | index.ts 52–53；index.test.ts 37 |
| 树页工具栏只有新增+展开，无导出钮、无 `runtime.download`；搜索只绑 `treeName` | TreePage 11–38 |
| `getList` → `listTree(queryParams)` → `buildTree(id, parentId)`；下拉再打一次不带查询的 `listTree()`，浏览器造 `{ id: 0, treeName: '顶级节点' }` | 197–214；composables 95–114 |
| 工具栏新增 `parentId=0`；行内新增 `parentId=row.id`；删一行单 id；toast「操作成功」 | 236–243 / 268 / 276–279 |
| `handleUpdate` 先 `form.parentId = row.id`，再 `getTree` + `Object.assign` | 247–254 |
| 页面 `rules.parentId` required；Bo 无 `@NotNull` | TreePage 176；Bo 37 |
| 厅堂 `downloadWithAxios` 固定 `client.post`；抄单表 `runtime.download('demo/demo/export')` 打不中 GET 窗 | axios-browser 261–263；对照 DemoPage 278 |
| OpenAPI `/demo/tree/export` 是 **get**；`TestTreeVo` 无 children、无 version | OpenAPI 5931–5942 / 14308–14336 |
| 无 Java `TestTree*Test` | 工作树搜空 |

**课写过、测试名单更窄或口试把邻课门卫说偏（不是证伪本格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「测试锁死厨房五枪」 | 锁 URL + segment 编码 + `{ parentId: 0 }` 请求形状。**没有** `exportTree` 不存在的 `not.toContain`，也 **没有** 锁「wrapper 丢掉 parentId」 |
| web-domain「权限组四串不含 query/export」 | `toEqual` 已锁四串全量，比分类课只锁 query 更硬。缺席仍是数组字面量，不是单独的 `not.toContain('demo:tree:export')` |
| 「`handleUpdate` 先写自己再覆盖」 | Vue 247–254。本格借用当扳机证据，格子仍是 Java 六法 |
| 课 `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |
| 「页面上是树」 | REST 仍是扁 `List`；`children` 是 `buildTree` 写进对象的。前端 `TreeVO` 有该字段，OpenAPI 没有 |

问：在六扇映射、classic 三跳、GET 导出、list 扁且 wrapper 不读 parentId、getInfo 走类上数据权限、空 TODO 删除、厨房五枪没收 export、树页无导出钮、假根不在表、OpenAPI GET 已经能互证的前提下，(a) 是否还要把「厨房测试没写 not.toContain exportTree」「课估时 38 vs chain 35」「Vue 先写 parentId=自己」写成开篇半句，才算本切片讲完？还是这些已经落在课的表/反例里，Lead 可以把本切片视为已讲、并保持厨房/九工厂行 uncovered？

## Q3 假设与反例

- objective_id: OBJ-75
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无第七扇 HTTP page/import/treeSelect/categoryTree；无 POST /export；list 非 PageResult、Vo 无 children；wrapper 不 eq parentId；厨房无 exportTree；getInfo 有类上数据权限；删除空 TODO 不点名；无 R.warn 601；无 ValidatorUtils；add/edit 都是 5 秒；Bo/Vo 无 version；无 TestTreeUseCase；门卫不注入 Mapper；OBJ-75 不含厨房
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪道闸认错？

1. **「CRUD 四个字 / 单表八扇也在树上。」** 磁盘六扇。没有 `/page`、没有 `importData`。课反例 4 / 误区 2 覆盖。
2. **「`list` 回的是树 / OpenAPI 有 children。」** 扁 `List`；`children` 是浏览器写的。课反例 2 / 17 覆盖。
3. **「导出是 POST，和单表/分类一样。」** 树表是 GET。单表与分类才是 POST。课反例 3 / 误区 5 覆盖。
4. **「页面能导出、厨房有 exportTree。」** 2026-09-17 都不能。菜单 F 有 export 字。课反例 4 / 5 / 误区 6 / 7 覆盖。
5. **「传 `parentId=0` 只返回根。」** wrapper 丢掉 parentId。厨房测试那一行仍是整表可见林。课反例 6 / 误区 13 覆盖。
6. **「这是 layered，Service 不许碰 Mapper。」** 登记 classic。课反例 7 / 误区 14 覆盖。
7. **「新模块就该抄这一份。」** 登记表禁止当 layered 反例。课反例 8 / 误区 14 覆盖。
8. **「`getInfo` 像单表一样不滤部门。」** 树表 Mapper 类上有注解；详情 SELECT 会滤。课反例 9 / 误区 8 覆盖。
9. **「`deleteWithValidByIds` 会点名，越权抛『您没有删除权限!』。」** 那是单表。本课 TODO 为空。课反例 10 / 误区 9 覆盖。
10. **「有孩子会 601 / 不能给自己当爸爸 / 父必须存在。」** 分类课才有。本课这些闸都不存在。课反例 11 / 12 / 误区 15 / 16 覆盖。
11. **「`validEntityBeforeSave` 会查唯一名。」** 空 TODO。课反例 12 / 误区 10 覆盖。
12. **「QueryGroup 会挡住空查询。」** Bo 上没有 QueryGroup 约束。课反例 13 覆盖。
13. **「OBJ-75 包含 `createDemoService` / `TreePage`。」** OBJ-77。课反例 14 / 误区 1 / 20 覆盖。
14. **「`add` 用了 `ValidatorUtils`。」** 树表是 `@Validated(AddGroup.class)`。课反例 15 / 误区 12 覆盖。
15. **「厅堂 download 抄过来就能导出。」** `downloadWithAxios` 固定 POST。课反例 16 / 误区 6 覆盖。
16. **「add 和 edit 防重不一样长。」** 都是 5 秒。2 秒是单表 add。课误区 11 / 边界 5 覆盖。
17. **「`TestTreeVo` 有 version，抢改会失败。」** HTTP 进出不带版本。课误区 21 / 边界 13 覆盖。
18. **「假根 id=0 在 test_tree 里。」** 不在。DDL 默认只是父号。课误区 23 / 边界 1 覆盖。
19. **「`bundle-core` 还留着树窗 / 有运行时电闸。」** 关的是装配。课正例 10 / 边界 10 / 误区 19 覆盖。
20. **「前端授权。」** `v-hasPermi` 藏按钮；REST 仍是 `@SaCheckPermission`；query/export 连权限组都没收。课边界 6 / 误区 17 覆盖。
21. **「XML / WITH RECURSIVE 拼树。」** 空 XML；Java wrapper。课反例 1 / 误区 3 覆盖。
22. **「删爸爸会级联撕孩子。」** 孩子升格为根。课正例 6 / 边界 3 覆盖。

问：读完后仍说不出的，是不是「Controller 的名字」，而是 **六扇合同 ≈ 磁盘映射、缺的是厨房没收的 export 与 GET≠POST、list 扁 / 树在浏览器、wrapper 丢掉 parentId、类上数据权限让详情也滤、空 TODO 删除不点名、九工厂/厨房行不能因本课闭合、样板 ≠ 模板**？

## Q4 视角

- objective_id: OBJ-75
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-75 vs L-003/L-017/L-067/L-074/L-076/L-077；claimed_cells 六扇 vs 厨房五枪 / 一张树页；厨房独行仍 uncovered
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这一格 (a) 切片**？

| 视角 | 多出来的边 | 是否本课这一格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 六名独行 vs chain 六名；无 Service/Vue 行；(b) 无树表独行；(d) 无树表 CRUD 流 | **是** 闭合定义。本切片可视为已讲。独行可标 covered。不要发明 (b)/(d)，不要把一张 Vue 升成新行才能给切片盖章，不要用本课盖厨房章 |
| 树表门卫 | 六扇路径/动词/六串权限、GET 导出、`RepeatSubmit` 只罩 add/edit、删 `Long[]`+`true` | **是** 切片里的 `TestTreeController.list,export,getInfo,add,edit,remove` |
| classic 厨师 | wrapper 丢掉 parentId、两个空 TODO、Impl 抱 Mapper、空 XML、类上数据权限 | **是** 走查后果，不是矩阵第二格 |
| 导出窗 | Java+菜单+OpenAPI GET 有；厨房无枪；树页无钮；download 固定 POST | **是** 对照。不要把「没按钮」说成「没这扇窗」 |
| 扁列表→树 | 管理页 list+`buildTree`；下拉假根 id=0；like/权限/逻辑删会让孩子升根 | **是** 对照。不要升格成前端格子 |
| 厨房作者（L-077） | 五枪 URL；`encodeId`；`{ parentId: 0 }`；权限组漏 query/export | 本课借用封面形状与没收窗负空间 **是**。工厂盖章 **否**（L-077） |
| 单表窗（L-074） | 八扇；POST 导出；方法级权限；删前点名；详情不滤；add 2 秒 + `ValidatorUtils` | 对照一句 **是**。八扇盖章 **否** |
| 富文本（L-076） | OSS 资产口；Vo 有 HTML | 负空间 **是**。资产窗 **否** |
| 分类窗（L-067） | POST 导出；`categoryTree`；601；父必须存在；不能给自己当爸爸 | 对照「本课把闸拆掉」**是**。分类七扇 **否** |
| 部门树（L-017） | `excludeChild`、ancestors、缓存 | 对照 **是**。部门房间 **否** |
| 登记表（L-003） | classic 允许 Impl 抱 Mapper；不得扩大越层；不得当 layered 反例 | 不改登记 **是**。房间画像 **否**（已 covered） |
| MySQL 8.4 | 表 `test_tree` + 两片林 + 菜单 F 型 export | 种子对照 **是** (a)。容器 **否** |
| 能力展示柜 | Redis/MQTT/MCP/Excel… Controller | 保持 deferred。模式不标它们 covered |

问：Lead 若凭「六个方法名在独行里」把该行标 covered，是否还要求读者能指着 **六扇合同、classic 三跳、GET 导出、list 扁、wrapper 丢掉 parentId、类上数据权限、空 TODO 删除、厨房没收 export**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要等 L-077 才给**本切片**当已讲？厨房独行是否必须保持 uncovered，本课只往备注写「树表六扇消费已讲」？

## Q5 元问题

- objective_id: OBJ-75
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；U8 已 15 节满员；矩阵 (a) 六名独行仍 uncovered、厨房独行仍 uncovered、无本课 (b)/(d)
- difficulty: low

建议：`defer`

原因：

- **`A:TestTreeController.list,export,getInfo,add,edit,remove` 是 chain 切片，也是矩阵独行。** L 合同章节齐全（宏观地图、定义表、机制链 1–8、带 caption / 文字等价物的三张图、正反例、变式、误区、暂停走查、来源表 S-004/S-008/S-010/S-L075-01…09）。能复现：六扇映射与权限字、`list` 扁且 wrapper 不读 parentId、`export` 是 GET 流、`getInfo` 可空且走类上数据权限、`add`/`edit` 注解组 + 5 秒防重、`remove` 空 TODO 后逻辑删、classic 门卫不抱 Mapper、XML 空壳、厨房五枪没收 export、树页无导出钮、假根不在表、OpenAPI GET。不必为「Controller 的名字」再挖五问。
- **Lead 可以把本课这一行标 `covered`。** 六名与磁盘 1:1，不会绑死单表八扇或厨房。**厨房独行与九工厂行保持 uncovered**，等 L-077，不要用本课开第二张前端章。不要发明树表 (b) 行：GET 导出 / 空 TODO / 类上数据权限 / wrapper 丢掉 parentId 已是 (a) 的解释到副作用。
- 本批可粘贴的精度项不挡住切片视为已讲：（1）厨房测试锁 `{ parentId: 0 }` 的请求形状，没有 `not.toContain` exportTree，也没有锁「wrapper 丢掉 parentId」；（2）课估时 38、chain 35；（3）`handleUpdate` 先写 `parentId=自己` 是 Vue 扳机，格子仍是 Java 六法；（4）无 `TestTree*Test`，证据走 Controller/SQL/厨房 URL。不要把单表八扇、富文本 OSS、厨房 URL 全表塞进本格。
- 不建议 `mine-more`：b01 未证明本切片还有真实矩阵空洞；再五问只会把 L-074 八扇、L-076 资产或 L-077 厨房再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；测试名单窄与估时差是半句精度，不是第二容器。不建议 `split-lesson`：未满 10 问；六扇是 chain 原设计的同一 OBJ-75，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。U8 已 15 节满员，即使以后拆也不得塞进本 unit。不建议发明 (b) 行。不建议等 GP-L-074 / GP-L-077 才给本切片当已讲。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:TestTreeController.list, export, getInfo, add, edit, remove | 课按磁盘讲了六扇路径/动词/权限/信封、classic 三跳、list 扁且 wrapper 不读 parentId、export GET 无页扳机、getInfo 可空且走类上数据权限、add/edit 注解组+5 秒防重、remove 空 TODO 部分删除仍可能 ok、厨房没收 export、假根不在表。残留精度：厨房测试不锁 export 缺席与 wrapper 忽略 parentId；课估时 38 vs chain 35；无 Java 测试类。 | 可选半句：口试「传 parentId=0」以 wrapper 为准——请求形状不是过滤器。不要求重写机制链。Lead **可以**改本行为 covered。 | defer（切片已讲；独行可闭合） |
| A:createDemoService, createRichTextService | 厨房独行仍 uncovered。本课只借用五枪 URL 与没收窗负空间。 | 不要用本课盖厨房章。备注可写「树表六扇消费已讲」。 | defer（L-077） |
| A:createDemoWebDomain（九工厂行） | 本课只用树表四串权限 + 无 query/export 当切片证据。其余工厂未因本课讲完。 | 保持 uncovered。 | defer（L-077 / 九工厂行保持 uncovered） |
| A:TestDemoController.list, page, importData, export, getInfo, add, edit, remove | POST 导出、方法级权限、删前点名、2 秒防重、`ValidatorUtils` 已借用。该行仍 uncovered。 | 不要用本课盖单表八扇。 | defer（L-074） |
| A:TestRichTextController.list, create, update, remove, assets, get | Vo 无附件对照已借用。 | 保持。L-076。 | defer（L-076） |
| C:layered-classic / wta-modules | 登记表 classic 行已 covered。 | 不要用本课再盖房间章，也不要发动迁 layered。 | defer（L-003 已 covered） |
| A:SysDeptController.excludeChild | 部门才有 exclude。 | 对照一句。部门房间已 covered（L-017）。 | defer（L-017 已 covered） |
| A:FlwCategoryController.* | POST 导出 / `categoryTree` / 601 已借用。该行已 covered（L-067）。 | 不要把分类闸搬进树表。 | defer（L-067 已 covered） |
| (b) GET 导出 / 空 TODO 删除 / 类上数据权限 / wrapper 丢掉 parentId | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 树表 CRUD 流 | 矩阵无此行。 | 不要发明前端树表数据流行。 | defer（未入库） |
| Mapper XML / BO/VO getter | 空 XML 与校验组已走查。 | 保持 covered-by-parent。 | defer（covered-by-parent） |
| 测试夹具 TestTree*Test | 工作树没有这类测试。 | 保持 deferred(test-fixture)。证据走 Controller/SQL/厨房。 | defer（deferred(test-fixture)） |
| demo Redis/MQTT/MCP/Excel… Controller | 同房间玩具柜。 | 保持 deferred(capability-demo-not-product-room)。 | defer（deferred） |
| MySQL 8.4 / dao-mapper-ownership | 种子表是 (a) 对照，不是容器/所有权闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-083 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本切片无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（厨房/单表/富文本是邻课格子，课内已把六扇讲完并显式禁止整行假闭合）；合格读者说得出函数目的（家谱练习卡、六扇 HTTP）、主路径（扁列表 / GET Excel 流 / 单条详情 / 注解组新增改名 / 空 TODO 后逻辑删）与失败路径（越权更新 0 行 `R.fail`、部分删除仍 ok、无行详情仍 200、5 秒防重、关 bundle 整栋不进包、抄 POST download 打不中 GET、传 parentId 不是查子树）；把可选半句写进原课不会超过 40 分钟；邻课已规划。满 10 问前不拆。六扇虽跨读写/导出/浏览器胶水，仍是 chain 原设计的同一 OBJ-75，不是「超过 1 个容器却讲不完」。U8 已 15 节满员，本 unit 不得再追加 split 子节点。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对厨房/九工厂、L-074/L-076 邻窗、L-003 已 covered 的登记表、未入库 (b)/(d)、covered-by-parent 的 XML、deferred 的测试夹具与能力展示柜、以及 MySQL/DAO 容器显式建议 `defer`；对 `A:TestTreeController.list,export,getInfo,add,edit,remove` 建议视为已讲切片、**可以**把矩阵六名独行标 `covered`、**不要**用本课盖仍 uncovered 的厨房独行与九工厂行。本课不主张改 Java 窗、发明 Vue 矩阵行、把 `queryList` 升成第七扇 HTTP、或等 GP-L-077 才给本切片当已讲。
