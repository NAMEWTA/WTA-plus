# T-42 Dispatch05 fixed-source runtime review

**Technical verdict: PASS for T-42 candidate `d95b464e46ec73d7a913809f4c84332a7f2eeffb`**, subject to Lead's final manifest and governance validator. This does not complete other tickets, the entire change, deployment, push, or archive. Historical failed attempts and old-source green runs remain at their own coordinates.

Source and build: d95/tree `39f18706226fed11ed19b5cdc1f822d60492350c` is clean before and after the targeted/default/full, Mail, Notify, HTTP, core and static records. Targeted 4 classes/10 tests passed with zero fail/error/skip. Default Maven had 265 fresh suites, 1,184 total, 940 executed passes and 244 environment skips, zero fail/error; those skips are not counted as passes. Full package and full bundle exited 0; the new full JAR private copy independently hashes and sizes exactly as `dispatch05-full-package-proof.json` and its artifact manifest report. Core package and bundle also exited 0 with same clean d95 before/after and a separately recorded core artifact. The two d95 targeted/default JDT wrapper records captured zero editor processes, so they prove no paused process remained in those invocations, not a nonzero pause event.

Owned real services: Mail run `57cd7d5353524ca5` accepted exactly one fresh suite and the source-bound 27 method names, 27/0/0/0. Its retained XML hash matches the run's sanitized SHA; the run reports seven generated-value redactions. Notify run `74c5b32f424f893f` accepted eight exact fresh suites, each method-count map matching the d95 source-bound manifest, 135/0/0/0. Both results have same clean d95 before/after and no cleanup errors, residual owned containers, anonymous volumes, loopback ports or Maven/owned process members. Their physically isolated MySQL/Redis/MinIO fixtures and fake final Mail sender establish the exercised contracts; they do not claim SMTP provider delivery or production capacity.

Full-app HTTP/OpenAPI: run `65515ae9c04c0b30` accepted with the same clean d95 and new full JAR unchanged before/after, complete owned cleanup, actual login/upload/submit, 19-digit >2^53 quoted source ID with exact actor/Client and one attachment relation/reference, omission/empty relation zero, and nine rejected authorization/invalid-string cases with zero DB/OSS writes. No-token and ordinary logged-in user produced application codes 401/403; numeric JSON was accepted with exact source/actor/Client/ref evidence and remains outside the documented `string[]` schema. Four intents were scheduled in the future, zero outboxes due, and zero enabled external MAIL/SMS accounts; this is not an outgoing-mail test. Live raw OpenAPI response was 438 paths/450 schemas with no loss. Its own raw SHA matches retained `source.json`; independent parsed comparison to formal revision `95e5ae4b…` found paths, components and all other fields except ephemeral `servers` equal. Thus formal generation did not need new bytes and the prior frontend gates remain valid under exact input equivalence, not as d95 reruns.

Frontend and static: `frontend`, `.agents`, `scripts` and `release-artifacts` Git trees are byte-identical between original frontend source 73c28 and d95; no changed path outside `backend/` or `speculo/` occurred in that range. The 73c28 frontend record remains 663 Vitest + 108 Node = 771 passes, plus 311/14/4 = 329 three-app build files, with its original command/source coordinates. At d95, skill facts, fullstack facts, Notify layered (113 Java files), FM, handbooks, and terminal release contracts all exited 0. Release contracts report 128 passes, zero fail/skip plus Compose checks; static source before/after remained clean d95. The previous static04 eight-error failure is historical and is not relabeled as a pass.

The latest independent contract review (`dispatch05-independent-contract-review.md`) provides the separate implementation axis. Lead's `dispatch05-lead-acceptance.json` records technical acceptance true, the exact three real run IDs and no runtime cleanup errors while expressly leaving governance closure/validator open. This runtime review is based on finished sanitized records and fresh XML; it did not run a build/service or edit the repository. The final retained manifest must hash the current review, actual terminal records and newest independent-review bytes before governance closes T-42.
