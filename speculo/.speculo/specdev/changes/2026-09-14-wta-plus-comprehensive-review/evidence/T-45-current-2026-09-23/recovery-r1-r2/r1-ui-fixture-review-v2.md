# T45 recovery R1 UI row-count failure — read-only fixture review

Fixed product `be35584b0d8dcfbb560dc9f338c303dafc77fbfd` is product-byte-equivalent to C3. The retained R1 run `/tmp/wta-t45/ui-v3-runs/c8ce84f90b58bf05/result.json` says `accepted=false`, browser 1 pass / 1 fail / 0 skip / 0 flaky, and `cleanup_errors=[]`. Its safe structured location identifies the positive case at private spec `ui-v3/oss-diagnostic-real.spec.ts:46:21`, `expect(row).toHaveCount(1)`. It does **not** retain the actual match count (0 versus more than 1) or its cause.

There is a concrete potential fixture-contamination chain. Runner v3 inserts a new owned `sys_oss_config` row `id_bad` with `access_policy='9'` at `run-minio-diagnostic-ui-v3.py:1070–1078`, uses that row for a real negative POST at `:1197–1204`, and leaves it in the same owned database before Chrome starts at `:1344+`. The normal Admin page calls `listOssConfig` with page 1/size 10 (`OssConfigPage.vue:409–415,489–495`). The domain list projection maps **every returned row** via `projectOssConfig` (`resource-service.ts:282–291`), whose `projectAccessPolicy` accepts only physical `'0'` and `'2'` (`:85–89`). A returned `'9'` throws `ResourceContractError` before the page assigns any rows. Existing `resource-service.test.ts:230–238` explicitly requires an unknown physical policy `'1'` to reject the list; accepting `'9'` in an editable UI is outside T45's contract. This would explain a failed unique-row assertion **if** the bad row was included in the first page; the retained result lacks both list-response inventory and actual locator count, so neither first-page inclusion nor a count of zero is an observed network/browser fact.

The narrow recovery is to preserve the completed invalid-policy HTTP negative result (`diagnostic.invalid_policy_fail_closed=true` in R1), then remove only the runner-owned `id_bad` row **before** opening Admin Chrome. Use an exact owned-ID + owned-key + `access_policy='9'` predicate in the fresh isolated DB and require affected row count 1; no broad delete, production cleanup, product enum relaxation, or deletion of the original negative assertion. The backend `queryPageList` does not cache a page, so a subsequent GET can observe the corrected owned state. This is a fixture boundary, not evidence that the UI should edit an invalid policy.

Add a bounded authenticated list preflight using the same page 1/size 10 query as the UI. Before deletion, record only HTTP/business status, total, row count, and booleans for whether `id_bad`, owned PUBLIC, and owned PRIVATE IDs appear; this will distinguish the hypothesized first-page contamination from a different cause. After the exact delete, repeat the list preflight and require a successful response, `id_bad_present=false`, and both owned positive IDs present in the returned page. If the positives are outside page 1, adjust the **owned fixture/query** or explicit UI navigation with equivalent assertions rather than silently relaxing `toHaveCount(1)`. Retain only these safe counts/booleans and source/input hashes; do not store raw response, credentials, endpoints or provider text. The fresh Chrome positive/negative two-case gate remains required. R1's failure is historical and must not be marked passed by this proposed recovery.
