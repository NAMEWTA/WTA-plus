# T-14 本地验证证据

状态：review（revision 64）。用户要求本 change 全部暂不提交；implementation commit、result SHA 均为空，0 Done。HEAD 保持 `76dbbe84a34624234e379661a57b232529e34ed3`，未暂存、推送或部署。

## 实现与合同

- Home 注入窄 `uploadMaterial` 端口，复用浏览器 OSS adapter 和实际 `general` 私有策略；保存草稿取得 WORKING owner 后，执行上传、OSS complete、Profile attach，全部登记完成后才允许提交。
- `SelfMaterials` 管理进度、取消、晚到响应、登记失败与重试。登记响应丢失时先查询实际引用，避免重复上传/挂载；替换先登记新件再移除旧引用。预览通过 Profile owner 获取临时 URL；失败可重试并清理旧提示。移除只解除业务引用。
- 个人身份证双面、企业营业执照/法人身份证明、非法人经办授权书均由新增 GET requirements 读取现有数据库规则；提交仍根据已保存申请独立校验。未修改后端必填校验或既有 current 状态集合。草稿/退回可编辑，WAITING 只读。个人性别值与后端 MALE/FEMALE/UNKNOWN 合同一致。
- Home Client 默认角色补 person/enterprise material 与 system:oss:upload 功能权限，不授予 OSS 管理查询、下载或物理删除权限。真实基座 DDL 补 SysOss/Mapper 已使用的 `delete_state`，默认 ACTIVE；没有运行在线迁移。
- 实际 HTTP 发现专用异常被通用 RuntimeException advice 抢先处理。Profile 三个专用 handler 与 SaToken handler 显式 `@Order(0)`，保留业务错误及 401/403；先有独立红灯测试，权限与提交规则未放宽。

## AC 验证

| AC-014 | 实际证据与结论 |
|---|---|
| 个人双面材料提交 | 真实 Home Chrome 填表、保存、缺标签聚焦；直接 API 缺材料被拒；上传双面、刷新恢复、签名私有图像实际解码、提交为 WAITING，刷新只读。passed |
| 企业必填与条件材料 | 法人经办两份、非法人经办三份分别实际提交；缺授权书定位对应标签。passed |
| 失败/取消/过期/归属 | 直传网络失败与取消不登记，草稿保留后重试；真实 Redis 票据过期拒绝 complete；丢登记响应先查询恢复；替换/移除保留物理文件；预览网络失败恢复；其他账号无法读/挂/删当前 owner，也不能向自己的草稿挂他人 OSS；缺权限返回 403。passed |
| 真实数据与回滚 | 六份实际基座 SQL 初始化 MySQL；真实 DAO/Mapper/XML、动态数据源事务、Redis 票据、OSS metadata/lifecycle、MinIO。提交快照与引用落库、对象内容逐个读取；在提交记录和两份不可变材料插入后注入工作流失败，独立 HTTP 查询证明 DRAFT、0 submission、0 snapshot、原 2 OSS 引用，重试变为 WAITING/1/2/4。passed |

## 实际命令与结果

完整 cwd、命令、退出码、耗时及日志路径由下列 JSON 记录；均串行执行。门禁日志无凭据输出，失败历史保留。

| 验证 | 记录 | 结果 |
|---|---|---|
| 全前端 lint/typecheck/test | `T-14-frontend-final.json` | 3 命令 exit 0；574 tests = 466 Vitest + 108 Node，架构门禁 0 baseline violation |
| 最后预览提示修复的受影响包 | `T-14-focused-final.json` | domain-profile/web-domain-profile/Home typecheck/test/lint exit 0，58 tests = 12 + 36 + 10 |
| Profile 与依赖构建测试 | `T-14-backend-final.json` | `./mvnw -B -ntp -pl wta-modules/wta-profile,wta-modules/wta-profile/wta-profile-person,wta-modules/wta-profile/wta-profile-enterprise -am package` exit 0，214/214，无跳过；显式选择实现子模块 |
| 异常优先级红绿 | `T-14-advice-red.json` / `T-14-advice-green.json`；`T-14-auth-advice-red.json` / `T-14-auth-advice-green.json` | 业务码 7 中 1 fail→7/7；鉴权 9 中 2 fail→9/9，未修改断言 |
| 三 App 生产构建 | `T-14-build-final.json` | `corepack pnpm build:prod` exit 0；最终 Admin/Home 另以 `T-14-regression-build-final.json` 正确测试配置重建 |
| 真实 Profile 浏览器 | `T-14-browser-final-clean-preview.json` | 1 Java 集成/8 Chrome 全通过、0 skip；MySQL/Redis/MinIO 容器和卷库存完全恢复 |
| 默认浏览器回归 | `T-14-default-e2e-correct-config.json` | `corepack pnpm test:e2e --config <owned config> --workers=1` exit 0，51 pass/1 skip；仅独立真实 Nacos 环境未配置，未计通过 |
| 会话/路由回归 | `T-14-lifecycle-final.json` | 20/20，含真实 HTTPS EventSource 关闭；owned 服务/证书清理 |
| 静态与分层 | `T-14-static-final.json` | E2E strict tsc、oxlint、person/enterprise layered 检查、Skill facts、diff check，具体结果以记录为准 |
| 治理 | `T-14-governance.json` | goal-plan、ticket-control、diff 均 exit 0；0 errors/181 shared-path warnings，由单 writer 串行合同裁决 |

正确测试构建参数为 `VITE_APP_MESSAGE_ENABLED=true`、`VITE_APP_NACOS_ADMIN=/nacos/`、`VITE_APP_MONITOR_ADMIN=/applications`、base `/`、API `/prod-api`。前一默认回归的 4 fail 来自 runner 使用未消费的变量名，已修正 runner 并重建验证，未放宽产品或断言。完整整合失败、根因和逐次实质改变见 `T-14-integration-history.md`。最终预览提示修改已补定向单测、Home 构建及真实 8 Chrome 重跑。

## 检查点与边界

`T-14-input-checkpoint.json` 输入 255 条上游最新哈希。`T-14-checkpoint.json` 记录 33 条当前变更路径；249 条非重叠上游哈希保持不变，6 条共享路径更新已复核（Home 组合与依赖、Playwright 主配置、锁文件和基座 DDL）。原 profile-management 测试未修改，已在默认回归执行。暂存区为空，HEAD 不变。

真实测试使用隔离 loopback HTTP Jetty、实际构建 Home、Chrome、六文件 MySQL、Redis、私有 MinIO。登录发行和菜单载荷为 fixture，角色权限来自实际 Home 基座数据；供应商/verification attempt/workflow 为显式替身，不能据此宣称外部供应商或真实审批引擎已验收。工作流失败注入用于验证真实 Profile/OSS 数据库事务回滚。正式审批、最终同一发布候选及整体测试仍归责任票/T-30。

默认 Nacos live 场景未执行；T-14 自身 required E2E 无跳过。构建现有大 chunk 和静动态 import 警告保留，未改阈值。现有在线数据库未迁移，不推送/部署。后续 T-15 修改 Profile 共享实现时必须回读本检查点和行为证据。

## Skill Execution Records

| Skill / phase / operation | 输入、执行与输出 |
|---|---|
| engineering-standards / implement / apply-scope-contract | 实际分层、Java/Vue、API、权限、事务和 DDL 规则用于 requirements 查询、self 状态机、最小权限与基座缺列修复；214 后端、574 前端、真实事务验证支持，passed |
| namewta-fullstack-development / implement / apply-scope-contract | Home runtime→domain/material HTTP→UseCase/Service/DAO/XML 的实际纵向切片与菜单 SQL，未复制管理页面/规则，真实两端验收 passed |
| wta-module-guide / implement / apply-scope-contract | 回读 current/owner/材料目录/OSS 事实并同步 Profile 父级索引；复用现有策略、生命周期与权限，passed |
| wta-common-modules-guide / implement / verify-authentication-advice-order | 回读 SaToken 现有专用 handler/配置及 Global fallback；独立 401/403 红绿与实际缺权限 403，passed |
| engineering-standards / verify / verify-affected-contract-and-quality-gates | 上述命令、红绿、8 Chrome/真实数据库、51 默认+明确 skip、20 会话及哈希复核；不将 fixture 边界或 commit hold 写成 Done，passed |

入口哈希以票据 frontmatter 为准；本次未变更任何 Skill 入口正文，不激活部署 Skill。
