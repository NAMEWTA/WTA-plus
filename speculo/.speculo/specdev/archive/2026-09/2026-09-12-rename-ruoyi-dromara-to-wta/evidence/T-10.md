# Evidence: T-10 — Static/Build + KEEP 回归

- **Change:** `2026-09-12-rename-ruoyi-dromara-to-wta`
- **Ticket:** `ticket/10-build-keep-verify.md`
- **Shipped tree:** `/workspace/vp-dev/WTA-plus`
- **状态:** done

## 1. 实现摘要

- KEEP 四件套坐标/import 仍在；未改写成 `org.namewta.sms4j` 等。
- 根 POM `groupId=org.namewta` `artifactId=wta-vue-plus`；模块 `wta-admin`。
- `./mvnw -pl wta-admin -am -DskipTests package` 连续两次 BUILD SUCCESS。
- `pnpm architecture:check` exit 0。
- 合同测试 `release-artifacts/tests/wta-rename-keep-contract.test.mjs` 3/3 pass。

## 4. 验收

| Contract | 结果 | Evidence |
|---|---|---|
| AC-PACKAGE | pass | mvn logs; contract test |
| AC-KEEP | pass | `{SCRATCH}/rg-keep.txt` (246 hits); contract test |
| AC-PREFIX | pass | `wta-admin` exists; no `ruoyi-admin` |
| AC-NAME | pass | titles WTA-Plus; `@namewta` kept |

## 5. Commands

| Command | cwd | exit |
|---|---|---|
| `node --test tests/wta-rename-keep-contract.test.mjs` | WTA-plus/release-artifacts | 0 — `{SCRATCH}/keep-owned-contract.log` |
| `./mvnw -pl wta-admin -am -DskipTests package` | WTA-plus/wta-vue-plus-namewta | 0 — `{SCRATCH}/mvn-wta-admin-1.log` |
| same, second run | same | 0 — `{SCRATCH}/mvn-wta-admin-2.log` |
| `pnpm architecture:check` | WTA-plus/plus-ui-namewta | 0 — `{SCRATCH}/pnpm-architecture-check.log` |
