# T-41 browser recovery diagnosis — implementation axis, read only

## Fixed inputs and verdict

- Product/blocked checkpoint: clean `d58fc3d1fdc7e0fd885b3fd217e76395d0ff5347`, tree `96d25291826f59d9f9e5edf46c91f7239e83be29`; browser runner SHA-256 `bd1798bba7a3ea78640e054e11f04905faf56ca38b8594196e18fc379c4c1f88`.
- Diagnostic: `/tmp/wta-t41/sql-diagnostic-runs/dac0c6bfd5c2159b/result.json`, `diagnostic_success=true` but `acceptance=false`. It started one dual-labelled MySQL 8.4.9 with fresh six-SQL schema; source before/after equals the fixed checkpoint. Initializer exit 0, 103 tables, zero outbox/external deliveries/enabled external accounts, one private OSS config. Its container, exact anonymous volume, process group and loopback port are absent after cleanup; `cleanup.errors=[]`.
- **Cause confirmed for the SQL fixture failure.** `sys_user.user_name`, `sys_client.client_id` and `notify_message.title` are `utf8mb4_general_ci`; `CONVERT(0x… USING utf8mb4)` in the runner evaluates as `utf8mb4_0900_ai_ci` with coercibility 2. Four original comparisons fail `ERROR 1267 (HY000)`; each same-value explicit `COLLATE utf8mb4_general_ci` control succeeds. The first failed real browser run stopped in this SQL region before JVM, Vite or Chrome, so it did not exercise the page contract. Its original result did not retain stderr, hence the precise code is proven by the later isolated reproduction rather than recovered from that first run.

## Narrow repair proposal for the sole product writer

1. Leave the generic `hexsql()` at `frontend/e2e/run-inbox-paged-real.py:366` unchanged for value inserts/updates. Add a narrowly named helper for **comparison RHS only**, e.g. `hexsql_general_ci_comparison(value) = hexsql(value) + ' COLLATE utf8mb4_general_ci'`. This pins the seed schema's actual collation without changing database defaults, session collation, schema or business code.
2. Use that helper in exactly three source expressions: `one_user` at lines 841–847 (called once for A and once for B), Admin `client_rows` at lines 848–852, and `verify_seed`'s title `LIKE` at lines 443–444. These correspond exactly to the four proven `1267/HY000` failures. Do not blanket-replace `hexsql` in `seed_plan`, role INSERT, BCrypt UPDATE or arbitrary queries: the diagnostic already ran both password updates, role creation, 502 message/503 relation insertion and the 501/2 count/ownership checks successfully. It also verified 20 pre-read timestamps, the oldest seen timestamp and both B rows' baseline.
3. Retain the original row-count/uniqueness, role/menu, 501/481 and 2/2, title/ownership and read/seen preservation gates. A comparison control succeeding is diagnostic evidence; final acceptance still requires the full clean-source browser run and its existing real HTTP, DB and browser assertions.

## Separate deterministic login blocker

`account_env` at lines 718–719 calls `random_password()`, which generates **40** alphanumeric characters at lines 99–102. `owned_bcrypt_hash()` at lines 104–111 hard-requires 40. The real password-login DTO at `backend/wta-api/src/main/java/org/namewta/system/api/model/PasswordLoginBody.java:27` enforces 5–30 characters. Thus after the SQL repair the fresh test accounts would authenticate with a rejected 40-character request. This was **not** the first real run's observed failure: no login request occurred.

Generate two independent 24-character alphanumeric **login** secrets, allow exactly that length in the login BCrypt helper, and retain the existing 40-character DB-root/app/Redis/MinIO infrastructure secrets. Do not change the production DTO or its validation. Store login secrets only in the existing private child-process environment; never in argv, SQL plaintext, result, raw reporter, trace or HAR. The isolated SQL diagnostic intentionally used 40-character synthetic values only to prove the database UPDATE, and is not a login validation test.

## Failure visibility and regression expectations

The product runner's `mysql()` at lines 244–249 currently calls `docker()`, whose exception drops stderr and reports only `exec -i`. Add a bounded failure classifier at that boundary or a narrowly staged adapter: allowlisted stage identifier, exit code, MySQL `ERROR` number and five-character SQLSTATE only. Retain raw stderr and SQL in memory only; never persist them or include them in an exception string, even after the existing redaction step. A successful query must still return the original tabular output unchanged. An unrecognized error must remain a failure with `number/state=null`, not be treated as a successful control. No automatic alternate-collation retry should hide a new mismatch in the final gate; the final runner should issue the explicitly collated query directly.

Extend `frontend/e2e/test_run_inbox_paged_real.py` offline tests to prove: (a) only the A/B lookup, Admin Client lookup and title LIKE use the explicit comparison collation; existing inserts, role and password UPDATE do not; (b) two login passwords are 24 alphanumeric characters while four infrastructure secrets remain 40; (c) BCrypt `$2a$` checks still pass and plaintext is absent from SQL; (d) synthetic `ERROR 1267 (HY000)` with a canary in stderr yields only stage/exit/number/state in persisted output, and unknown stderr produces no raw spill; (e) absent, duplicate and wrong-owner rows still fail. Compile/preflight of the repaired browser runner and the exact offline test selector must pass before re-freezing its SHA. A later owned full browser attempt is still required; the isolated SQL diagnostic is not a substitute.

## Separate private five-case production-dist preview runner

I read the entire `/tmp/wta-t41/run-inbox-api-controlled-dist.py` (331 lines). Its present immutable v1 is tied to D `d93eb1d8…` at line 31, C production build `eff81f64…` at line 32, manifest SHA at line 33, and exact five-case test SHA at line 34. It checks all 328 production-dist artifact hashes and inventory, production no-push mode, exact five Chrome titles and no retry/skip; it uses one owned loopback Vite preview, separate process groups, deletes raw reporter/log/artifacts, verifies port and source/dist after. It has **not** been run as part of this review.

After the recovery E candidate is fixed, preserve v1 and make a separate `/tmp/wta-t41/run-inbox-api-controlled-dist-v2.py` with distinct run root. Pin E's full SHA and current exact five-case test hash. Replace the current C→D exact `registry.test.ts` delta check at lines 62–66 with an **exact reviewed C→E frontend changed-path list**, limited to test/browser-runner/support files actually changed; continue refusing any production source delta or differing 328-byte/hash artifact manifest. Keep existing five-case identity, production `.env.production` checks, one worker/zero retries, trace/video/screenshot off, PGID/port cleanup and raw deletion. Optionally retain only validated failing case title/status plus numeric declaration/assertion file-line-column that resolve to the exact fixed test file; never copy Playwright error messages, stacks, requests or attachments. Run this v2 only after E is frozen and its dist equivalence is independently confirmed; it is separate from the real 501/DB/login browser gate.

No repository files, build outputs, services or frozen runners were modified or executed for this review.
