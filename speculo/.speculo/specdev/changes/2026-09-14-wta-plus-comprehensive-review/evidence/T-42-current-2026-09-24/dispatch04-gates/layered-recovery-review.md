# T-42 layered gate failure — minimal source-conformant repair proposal

Fixed inspected source: `73c28e751f6276e45b0488ab5d8eacbf9f0c77fb`. Actual required gate `/tmp/wta-t42/static04-notify-layered.{json,log}` exited 1 with eight errors. This file is a **read-only proposal**, not a patch, test result, approval or waiver. No repository source, checker, service or build was changed here. Lead must preregister any additional production/test/manifest paths before delegating to the sole writer.

The eight errors have three roots: two adapters reach directly into DAO and a runtime Service (`NotifyAttachmentReleaseJob:5-6`, `NotifyAttachmentSnapshotAdapter:9-11`); session adapter depends on an actor Port misplaced in runtime Service (`SessionNotifyAttachmentActorAdapter:4`, actor Port itself); snapshot transaction class is neither named `*Service` nor located in `usecase/` for its four `@DSTransactional` operations (`NotifyAttachmentSnapshotTransactions:21,26,48,67,81`). The gate's `forbiddenByLayer.adapter` explicitly forbids `.dao.`, `.service.`, `.usecase.` imports. Its `usecase` rules forbid DAO/Mapper/System API but allow Service and `@DSTransactional`; its `service` rules allow DAO/System API but reject service-to-service wiring and direct transactions. Renaming files alone or hiding imports with fully qualified names would not satisfy the actual architecture.

Minimal conformant shape:

1. Move the unchanged `NotifyAttachmentActorPort` interface from `service/runtime/` to `port/`; update the runtime service, session adapter and `NotifyAttachmentRuntimeRedTest` imports. This preserves the trusted session actor and Worker no-session behavior.
2. Add one `port/NotifyAttachmentSnapshotPort.java` with typed relation reads, bounded release-candidate read, and reserve/confirm/uncertain/release operations. The adapter-facing `Prepared` may retain the already public `OssService.NotificationCopyReservation` with the relation; `confirm` can accept that `Prepared` plus copied byte length/SHA, so the UseCase implementation need not import `org.namewta.system.api`. Avoid common-notify/Spring/DAO imports in the Port. Return domain relation or small ID pairs; never expose credentials.
3. Rename `service/runtime/NotifyAttachmentSnapshotTransactions.java` to `NotifyAttachmentSnapshotService.java`. Keep current DAO/System authorization, lock order, owner/ref writes, token CAS, `COPY_UNKNOWN`, `send_reserved` and terminal-unsent release conditions **byte-equivalent in behavior**. Remove all four `@DSTransactional` annotations from this Service. It can provide the read methods used by the port without granting adapters DAO access.
4. Add `usecase/NotifyAttachmentSnapshotUseCase.java` implementing the new Port, delegating to the Service. Put `@DSTransactional` on `reserve`, `confirm`, `uncertain` and `releaseIfSafe` as separate public Spring-proxied methods; keep the release-candidate and relation reads as read methods. The adapter calls the Port, never the UseCase concrete class. Its external `OssService.copyNotificationSnapshot` remains between `reserve` and `confirm` Port calls, **outside** any DB transaction; failure still makes a separate `uncertain` call. `NotifyAttachmentReleaseJob` obtains candidate IDs and invokes `releaseIfSafe` solely through the Port, preserving cursor size 100 and its failure handling.
5. Update `NotifyAttachmentSnapshotAdapter` to depend only on the new Port plus its existing `OssService` and common snapshot SPI. Update `NotifyAttachmentReleaseJob` to Port only; no DAO/Service import remains. The existing System `OssService` nested reservation/result DTO can be converted in the Service to avoid forbidden UseCase imports. Update the single integration test injection of the old transaction class to the new Port or UseCase interface, preserving every assertion. Update `business-oss-owners.json` snapshot owner/caller class to the renamed actual Service, because its caller scan observes that Service's `reserveNotificationSnapshot` and `releaseNotificationReferences` calls; **do not** add an allowlist exception or delete either persistent carrier.

Exact known product/test/manifest edges requiring advance write-set check: old/new actor-port paths; old/new snapshot Service paths; new Port and UseCase paths; both adapters; `NotificationApplicationRuntimeService` actor import; `NotifyAttachmentRuntimeRedTest`; `NotifyMailAttachmentIntegrationTest`; `backend/wta-admin/src/test/resources/oss/business-oss-owners.json`. Search only these names at fixed source for any additional compile consumers before editing. A rename moves paths and may require registration of both removed and added paths.

Acceptance of a future fixed revision requires the unchanged layered checker to exit 0; targeted architecture/owner tests and exact-source default/full/core; fresh owned Mail27 on the new Spring proxy and transaction boundaries, particularly reservation rollback/ACK unknown, uncertain-copy owner retention and late-sender release; fresh Notify135 because dispatch/worker uses this path; full-JAR HTTP quoted ID/permissions/OpenAPI and source/cleanup fence for the new JAR. Previously green `73c28e` results remain historical input evidence, not automatic proof of a changed runtime/transaction graph. Generated frontend contract bytes need not be altered if raw schema is unchanged, but check provenance/equivalence and frontend outputs at the final source.
