# T-05 evidence

Current HEAD `671bfc86` already meets AC-005. No new implementation was required. This cancels only the repeated construction. AC-005 stays owned by T-30. `RepeatSubmitAspect` does not touch a Mapper or the notification idempotency store; T-37 remains the notify retry contract.

## Command

```bash
cd backend && ./mvnw -pl wta-common/wta-common-redis -am test \
  -Dtest=RepeatSubmitRedisIntegrationTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Drepeat.redis.integration.port=33102
```

Exit 0. Isolated Redis `redis:8.6.3` on `127.0.0.1:33102`, removed after the run. `RepeatSubmitRedisIntegrationTest`: tests=7, failures=0, errors=0, skipped=0.

The seven cases drove the shipped aspect and Redisson codec: an expired owner cannot delete the next owner's key, a compare-delete cannot remove another value, an unsuccessful result releases only its own lease and can retry, success and non-R results keep the original window, business exceptions and errors leave no thread-local residue, an invalid interval never acquires or calls the business method, and a release failure preserves the business failure.
