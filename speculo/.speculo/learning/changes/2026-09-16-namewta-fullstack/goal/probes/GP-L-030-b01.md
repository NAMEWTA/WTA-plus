---
artifact: learning-goal-probe
probe_id: GP-L-030-b01
lesson_id: L-030
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-030 / b01

审问对象是已写成的 Lesson `lessons/L-030-openapi-catalog.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain / `claimed_cells` / 矩阵 (a) **一行**）：`A:SysOpenApiCatalogController.selfInterfaces,selfInterface,userInterfaces,userInterface`。OBJ-30 原文只点名「自有/他人接口目录」，没有把四个 Java 方法写成口试名单。课开篇收成「正好四个」公开 HTTP 方法：零 POST、零凭据、零 HMAC。矩阵 (b) **没有** `SystemOpenApiCatalogService.list/detail`、`OpenApiOperationRegistry`、`OpenApiAuthorizationResolver`、`OpenApiAuthorizationMatcher` 行；HMAC / 机器会话 / nonce 是 (b) 另三行，留给 L-032；凭据 create/reset 留给 L-031。矩阵 (c) `wta-common` 工具间（含 openapi）已 covered（L-002）；MySQL / Redis 仍 uncovered。矩阵 (d) **没有** 目录预览流、也没有机器 HMAC 调用流。`SysOpenApiCredentialController` 留给 L-031；`createOpenApiService` / `openApi.vue` 留给 L-033；L-005 那条河是管理端 `generated/openapi.ts`，不是机器货架。Goal 边界禁止把 demo 能力展示 Controller 扩进 in-scope：`OpenApiDemoController.echo` 只当注册表夹具，不是本格。

抽查锚点（2026-09-16）：`backend/wta-modules/wta-system/.../controller/system/openapi/SysOpenApiCatalogController.java`；`openapi/catalog/SystemOpenApiCatalogService.java`；`openapi/authorization/SystemOpenApiAuthorizationResolver.java`；`mapper/SysOpenApiAuthorizationMapper.java` + `mapper/system/SysOpenApiAuthorizationMapper.xml`；`controller/system/openapi/SysOpenApiCredentialController.java`（共用门牌）；`wta-common-openapi`：`OpenApiOperationRegistry` / `OpenApiAuthorizationMatcher` / `OpenApiAutoConfiguration` / `OpenApiGatewayFilter` / `OpenApi.java` / `OpenApiCatalogItem` / `OpenApiHeaders` / `OpenApiCanonicalizer`；`wta-demo/.../OpenApiDemoController.java`；`LoginHelper.isSuperAdmin`；`SystemConstants.SUPER_ADMIN_USER_ID`；`GlobalExceptionHandler.handleServiceException`；`application.yml` `openapi.enabled`；`03-backend-module-modes.md` classic 行；`10-cde-base-ddl.sql` `sys_open_api_credential`（无 catalog 表）；`50-cde-base-dml.sql` `system:openApi:list|query|add|edit|remove|self`；`SystemOpenApiCatalogTest`；`SystemOpenApiAuthorizationResolverTest`；`OpenApiOperationRegistryTest`；`frontend/packages/domains/system/src/open-api/{service.ts,transport.ts,index.test.ts}`；`frontend/packages/api-contracts/generated/openapi.ts` `/system/openApi/self/interfaces*` `/users/{userId}/interfaces*`。

## Q1 澄清

- objective_id: OBJ-30
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-30；chain L-030 覆盖列四方法；L-030 `claimed_cells`；矩阵 (a) 该行与磁盘四扇同名；(b) 无 catalog Service/Registry/Matcher 行，HMAC 三行是 L-032；(c) MySQL/Redis uncovered；(d) 无目录流；课开篇「正好四个」+ 负空间 L-031/L-032/L-033
- difficulty: medium

课把「宏观货架」收成 **一个 (a) 格子、一个 OBJ**。计划里至少有五套可数对象，闭合标准要以哪一套盖章？

1. **OBJ-30 原文。** `course.md`：能口述 `SysOpenApiCatalogController` 自有/他人接口目录。点名的是**一份类 + 两扇概念窗**，没有写 `selfInterface` / `userInterface` 两个详情名，也没有点名 24 hex、合法 Client、三句业务文案。
2. **chain / 课 `claimed_cells` / 矩阵 (a) 一行。** 四个方法与磁盘公开映射同名：`selfInterfaces, selfInterface, userInterfaces, userInterface`。默认档 Unistructural + explained。状态仍 `uncovered`。
3. **课自造的里屋。** `SystemOpenApiCatalogService.list/detail`、`OpenApiOperationRegistry.afterPropertiesSet`、`SystemOpenApiAuthorizationResolver.resolve`、`OpenApiAuthorizationMatcher.matches` 写成机制主链。矩阵 (b) 无这些符号；HMAC / 机器会话 / nonce 是别人的 (b) 行。
4. **邻居多出来的字。** 同一前缀下凭据十三条窗（L-031）；网关签名河（L-032）；前端工厂 `currentUser.listInterfaces` / `targetUser.listInterfaces`（L-033）；管理端快照 `generated/openapi.ts`（L-005）；`OpenApiDemoController.echo`（demo 模块，默认 `bundle-full` 才进 classpath）。
5. **菜单种子里多出来的字。** DML 有 `system:openApi:add|edit|remove`，挂在凭据写窗，本 Controller 四扇用不到。

问：本课 (a) 闭合标准是「矩阵这一行的四个 GET：路径、动词、三颗权限、他人窗再叠主键闸、目标人从会话还是路径取、以及每扇碰不碰凭据表 / 写不写会话」，还是 OBJ-30 那句「自有/他人」就够盖章？若 Lead 只凭「类还在 `controller/system/openapi`」标 covered，两个详情还算不算本格？若必须看见 `interfaceId` SHA-256 函数体 / `legalClientJoin` SQL / 网关 `matcher.matches` 才算性状闭合，是否已经把这一行 (a) 扩成一张不存在的 (b)，或把 `wta-common-openapi` / demo Controller / HMAC 数据流标 covered？

## Q2 证据

- objective_id: OBJ-30
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: `SysOpenApiCatalogController` 21–61；`SystemOpenApiCatalogService` 30–90；`SystemOpenApiAuthorizationResolver` 32–109；`SysOpenApiAuthorizationMapper.xml` 5–89；`OpenApiOperationRegistry` 41–148；`OpenApiAuthorizationMatcher` 15–51；`OpenApiAutoConfiguration` 35–73；`OpenApiGatewayFilter` 137–141；`OpenApiDemoController` 15–28；`LoginHelper` 228–238；`SystemConstants.SUPER_ADMIN_USER_ID`；`GlobalExceptionHandler` 64–70；`application.yml` 51–53；`SystemOpenApiCatalogTest` 36–95；`SystemOpenApiAuthorizationResolverTest` 49–77；`OpenApiOperationRegistryTest` 57–74 / 97；DDL 无 catalog 表；DML OpenAPI 菜单；`service.ts` 95–100 / 128–134；`index.test.ts` 129–130 / 174–175；`openapi.ts` 4540–4620
- difficulty: high

课的机制链哪些能在工作树直接复现，哪些被磁盘削薄或说满？

**仍能复现（本格）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 一份类、四扇窗、根路径 `/system/openApi`；**不**继承 `BaseController`；只注入 `SystemOpenApiCatalogService`；类注释 *Credential-independent* | Controller 18–27；无 `@PostMapping` / `@Log` / `@RepeatSubmit` / `@Validated` |
| 与凭据 Controller **共用门牌**，方法路径不打架 | CredentialController `@RequestMapping("/system/openApi")`；生成 OpenAPI 同时有 `/self/interfaces*` 与 `/self/credential*` |
| `wta-system` classic；无 UseCase；磁盘 `Controller → CatalogService → Registry + Resolver + Matcher` | `03-backend-module-modes.md` classic 行；模块内零 `usecase/openapi`；无 `ISysOpenApiCatalogService` |
| GET 自有两扇权限 `system:openApi:self`；目标人 `LoginHelper.getUserId()` | Controller 29–38 |
| GET 他人列表 `system:openApi:list` + `requireSuperAdmin()`；他人详情 `system:openApi:query` + 同一闸；目标人 = 路径 `userId` | Controller 41–54 |
| `requireSuperAdmin()`：`LoginHelper.isSuperAdmin()` → `userId == 1761100000000000001L`；文案 `OpenAPI target catalog is unavailable` | Controller 56–60；`LoginHelper` 228–238；`SUPER_ADMIN_USER_ID`；单测 *RechecksAdminServerSide* 对非超管只打 `userInterfaces(9L)` |
| 四扇最后都进 `list(targetUserId)` / `detail(targetUserId, interfaceId)` | Service 30–45 |
| 列表：先 `resolve`，再 `registry.all()`，`matcher` 不过则省略，不过则不标灰 | Service 30–36；单测只回 `allowed` 不含 `denied` |
| 详情：快照失败时还没 `find`；号不存在或 matcher 不过 → **同一句** `OpenAPI interface is unavailable` | Service 38–44；单测 `denied` 抛这句 |
| `openapi.enabled=true` 才进容器；yml 默认 `${OPENAPI_ENABLED:true}`；AutoConfiguration 类注释 *Default-off*，以 yml + `havingValue=true` 为准 | Controller/Service/AutoConfiguration 均 `@ConditionalOnProperty`；yml 51–53；AutoConfiguration 35–40 |
| 授权快照：人 `status='0'` 未删；`countLegalClients==0` 或人空 → `OpenAPI authorization snapshot is unavailable`；`userId==null` 同样 | Resolver 32–39 / 107–109；Mapper `selectActiveUser` + `countLegalClients`；单测 *failsClosedWhenUserOrLegalClientIsMissing* |
| 合法 Client：启用 Client × 启用用户类型 × 该人 `sys_user_type_rel.status='0'` | XML `legalClientJoin` 5–17 |
| 角色 = 合法 Client 上启用角色，且默认角色 **或** `sys_user_role` 显式；权限再经 `sys_role_menu` → `sys_menu.perms`（菜单 `status='0'` 且 `client_id` 对得上；空 perms 丢掉） | XML `selectActiveRoles` / `selectActivePermissions` |
| 合成 `LoginUser`：`userType="openapi"`；`clientPk`/`clientKey`=null；超管目标人再贴 `superadmin` 与 `*:*:*`；`posts` 空；`dataScopeRoleMap` 目录 matcher **不读** | Resolver 55–75；单测 *without Client fallback* / *preservesSuperAdminSemantics* |
| 货架来自启动扫描：只收**方法级** `@OpenApi`（注解 `@Target(METHOD)`）；`interfaceId = hex(SHA-256(method + "\n" + path)[0..12])` = 24 个 `0-9a-f`；排序 path 再 method；`all()` 是 `List.copyOf` | Registry 46–75 / 141–148；单测 `[0-9a-f]{24}` 与 `add` → `UnsupportedOperationException` |
| 撞车 / 缺 path / 缺 method / `SaIgnore` / `SaCheckOr` / 非空 login type → 该 handler 计入 `rejectedMappings`，**不**停机 | Registry 58–67 / 103–138；registry 单测 `rejectedMappings()==2`（含 `@SaIgnore` 与 schema 失败 mapping） |
| 演示回显：`GET /demo/openapi/echo`，`@OpenApi("OpenAPI 回显演示")`，无 Sa 注解；`interfaceId=2766c4b3fe40d2486d3ce0dc`（`GET\n/demo/openapi/echo` 的 SHA-256 前 24 hex） | `OpenApiDemoController` 15–28；公式与磁盘一致 |
| curl 示例：主机 `https://api.example.com`；头 `X-OpenAPI-Version: v1`、`<APP_KEY>`、`<UNIX_SECONDS>`、`<BASE64URL_128_BIT_NONCE>`、`<NAMEWTA_V1_SIGNATURE>`；有 requestSchema 才加 JSON body。单测 curl **含**占位符、**不含** `secret` | Service 53–68；CatalogTest 51–53 |
| 四扇对 MySQL **只读**授权投影；不读 `sys_open_api_credential`；不写 Redis nonce；不建机器会话 | Catalog 服务零凭据类型；DDL 只有凭据表 519+；Gateway 才写 nonce/会话（L-032） |
| 前端运输：`currentUser.listInterfaces/getInterface` → self GET；`targetUser.*` → `/users/{id}/interfaces*`；`interfaceId` `encodeURIComponent`（`order/query` → `order%2Fquery`） | `service.ts` 95–100 / 128–134；`index.test.ts` 129–130 / 174–175 |
| `groupOpenApiCatalog` 按 path 第一段分组，是 domain 投影 | `transport.ts` 183–194 |
| 菜单种子含 `self\|list\|query`；`add/edit/remove` 是凭据写窗 | DML OpenAPI 菜单段 |
| API-005：四扇全 GET，因此零 `@Log` | Controller 全文 |

**课写过、磁盘已削薄或说满（不是证伪四扇窗）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「关掉开关，这两颗 Bean 不进容器，四扇窗 404」 | Controller + CatalogService + `OpenApiAutoConfiguration`（Registry/Matcher/Gateway）缺席。**`SystemOpenApiAuthorizationResolver` 没有** `@ConditionalOnProperty`，作为 `@Service` 仍在。HTTP 故事不变：没有 Controller 就是 404，不是空列表 |
| 「空数组表示快照成功但 matcher 全滤掉（演示回显仍会在）」 | 默认 `bundle-full`（`activeByDefault=true`）才把 `wta-demo` 打进 `wta-admin`。瘦 profile 下快照成功也可以是真·空数组。口试不要保证「只要能拍快照就一定看见 echo」 |
| 「单测断言示例含 `<APP_KEY>` / `<NAMEWTA_V1_SIGNATURE>`」 | CatalogTest **只断言 curl**。Java 示例用变量名 `appKey` / `namewtaV1Signature`，**没有**尖括号占位符 |
| 「目录和网关各持同一类」匹配器 | Spring 里 Matcher 是 AutoConfiguration 的**单例 Bean**，CatalogService 与 GatewayFilter 注入的是同一颗，不只是同一份源码 |
| 「图上 resolve 旁写同一 SPI」 | `OpenApiAuthorizationResolver` 确是 SPI：目录每次现查；网关在建机器会话时经 `OpenApiMachineSessionBridge` 再 resolve。请求线程里网关用的是会话里的 `LoginUser`，不是再打一遍 catalog SQL |
| 「他人详情失败关闭」单测 | 非超管只打了 `userInterfaces(9L)`。`userInterface` 走同一私有方法，磁盘没有第二条实现，但 HTTP 映射没有第二条失败用例 |
| 「业务异常 / unavailable」 | 三句都是无 code 的 `ServiceException` → `handleServiceException` **无** `@ResponseStatus` → HTTP **仍 200**，JSON `code=HttpStatus.ERROR`（500），`msg` 才是那三句。不要说成 HTTP 404/403/500 |
| 「馆长看自己的自有货架，通常几乎是全注册表」 | 超管 `*:*:*` 是 `resolve` **成功之后**才贴。超管若停用或 `countLegalClients==0`，自有窗同样 snapshot unavailable，不会先贴全能证再滤 |

问：在四扇窗路径/权限/目标人、他人窗主键闸、快照失败不给空货架、详情两因一句、零 POST 零凭据、开关关掉是 404 已经能互证的前提下，(a) 是否还要把 **「演示回显一定在空货架上」「Java 示例也含 `<APP_KEY>`」「unavailable 是 HTTP 4xx」** 写成开篇半句，才算把四扇窗讲完？还是这些已经（或应当）落在反例里，Lead 可以直接盖章？

## Q3 假设与反例

- objective_id: OBJ-30
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 四映射全表；三句文案；无 catalog 表；self 现查库；他人过滤键是路径 userId；query≠list；无 @OpenApi 在目录方法上；无 UseCase；ServiceException 信封；demo 在 bundle-full
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪只柜子认错？

1. **「目录存在 `sys_open_api_catalog` 表。」** 没有。10 号脚本只有 `sys_open_api_credential`。课反例 1 覆盖。
2. **「超管看他人 = 把自己的 `*:*:*` 货架复制过去。」** 过滤键是路径 `userId`。课反例 2 / 正例 B 覆盖。
3. **「有 `system:openApi:list` 就能看同事。」** 还差主键闸；文案是 target catalog unavailable，不是 Sa-Token 权限不足。课反例 3 覆盖。
4. **「他人详情权限也是 list。」** 是 `system:openApi:query`。课误区 13 覆盖。
5. **「自有详情 404 = 书号写错，403 = 没权。」** 服务端合成一句 unavailable。课反例 4 覆盖。HTTP 信封其实是 200 + JSON 500，课没写成反例。
6. **「self 读当前浏览器 Client 登录包。」** 每次 `resolve(target)`，跨合法 Client 求并。课反例 10 覆盖。
7. **「这四扇也会出现在机器货架上。」** 目录方法没有 `@OpenApi`。课反例 6 覆盖。
8. **「关掉开关返回空数组。」** Bean 消失 → 404。课反例 7 覆盖。
9. **「空数组 = OpenAPI 关掉了。」** 空数组是快照成功后 matcher 全滤；默认装配下 echo 通常还在。课反例 7 半覆盖；瘦 profile 无 demo 时可以真空。
10. **「`interfaceId` 等于 Java 方法名 / `@OpenApi` 文案。」** 24 hex。课反例 8 覆盖。
11. **「这是 layered UseCase。」** 登记表 classic。课反例 9 覆盖。
12. **「curl 示例能直接打生产。」** example.com + 占位符。课反例 5 覆盖。
13. **「货架 200 就能 HMAC 打通。」** 还缺凭据、签名、时钟、nonce、限流。课误区 12 覆盖。
14. **「`createOpenApiService` 是本课矩阵 A。」** 工厂格子是 L-033。课负空间覆盖。
15. **「把 L-021 的 refreshCache 打到 OpenAPI 就能刷新货架。」** 注册表只在启动扫描。课误区 14 覆盖。
16. **「OBJ-30 只有自有/他人两扇，详情不算。」** 矩阵与 chain 点名四个方法。课开篇「正好四个」。
17. **「演示回显 Controller 也是本格。」** Goal 边界不把 demo 展示 Controller 扩进 in-scope；它只证明「空规则 ⇒ 能拍快照就会上架」。
18. **「超管看自己，即使没有合法 Client 也是全货架。」** `*:*:*` 贴在 resolve 成功之后。课正例/五问提到合法 Client，开篇「通常」易听成无条件。

问：读完后仍说不出的，是不是「四个方法名」，而是 **自有 vs 他人以谁盖章、他人窗叠两把锁、快照失败 ≠ 空列表、详情两因一句、关掉是 404、200 货架 ≠ 能 HMAC、unavailable 信封是 JSON 500 不是 HTTP 4xx**？

## Q4 视角

- objective_id: OBJ-30
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-30 vs L-005/L-031/L-032/L-033；API-005；DML 菜单；OpenAPI 生成物；bundle-full demo
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:SysOpenApiCatalogController.selfInterfaces, selfInterface, userInterfaces, userInterface`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 四方法独行 = 磁盘四映射；OBJ 写自有/他人、矩阵写四窗；课已拆「两概念 × 列表/详情」；(b) 无 catalog Service 行；HMAC 三行是 L-032；(c) MySQL/Redis uncovered；(d) 无目录流 | **是** 闭合定义。四窗可单独 covered。不要发明 (b)，不要把 common-openapi 容器再标一次，不要把 HMAC 调用流算进来 |
| 目录柜台（本 Controller） | 四扇：路径/动词/三权限/他人主键闸/目标人来源；无 POST、无 `@Log` | **是** (a) 逐方法 |
| 货架服务 | `list`/`detail` 只按目标人快照过滤；详情同一句 unavailable；占位示例 | **是** 四窗的 explained 副作用。不要另开 Service 行 |
| 启动注册表（common-openapi） | 方法级 `@OpenApi` → 24 hex；拒绝计数；目录方法不上架 | 作为「货架上从哪来」**是**。Registry 全表 / 网关装配 **否**（L-032） |
| 授权投影 SQL | 合法 Client + 角色并集 + 超管再贴 `*:*:*` | 作为四窗读哪几张表 **是**。用户/角色/菜单 CRUD **否**（L-015…L-018） |
| 共用尺子 | 目录预览与网关放行同一 Matcher 类/Bean | 预览过滤 **是**。HMAC/nonce/限流/机器会话 **否**（L-032） |
| 凭据柜台（L-031） | 同一门牌 `/system/openApi`；`add/edit/remove` | 负空间 **是**。凭据十三条窗 **否** |
| 浏览器工厂（L-033） | self/users GET + `encodeURIComponent`；`groupOpenApiCatalog` | 运输合同邻居证据 **是**。`createOpenApiService` 格子 **否** |
| 管理端快照（L-005） | `generated/openapi.ts` 有四条路径 | 邻居证据 **是**。不要把复印件说成立法原件 |
| 演示回显 | `2766c4b3fe40d2486d3ce0dc` | 注册表夹具 **是**。不要把 `OpenApiDemoController` 扩进矩阵 |
| 菜单种子 | `self\|list\|query` 在；`add/edit/remove` 给凭据 | 口试按注解 **是**。不发动改 DML **否** |
| API-005 | 四扇 GET 只读；变更才 POST+`@Log` | 与规范同向 **是**。不发动传输 Ratchet **否** |
| MySQL 8.4 / L-085 | 无目录表；只读用户/Client/角色/菜单 | 本课用到的表名 **是**。基座容器 **否** |
| 全局异常口袋 | 三句 `ServiceException` → HTTP 200 + JSON `code=500` | 失败句 **是**。不要发明业务码合同 |

问：Lead 若凭「四个方法名都在函数表里」把这一行 (a) 标 covered，是否还要求读者能指着 **自有=会话目标人、他人=路径目标人+超管主键、快照失败不给空货架、详情两因一句、关掉 404、200 不等于能 HMAC**？这些是不是已经在函数表/反例里，而不需要 (b) 新行、也不需要把 Registry/网关/工厂标 covered？

## Q5 元问题

- objective_id: OBJ-30
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；单一 C4 容器 `wta-system` 的一份 Controller
- difficulty: low

建议：`defer`

原因：

- **`A:SysOpenApiCatalogController.selfInterfaces,selfInterface,userInterfaces,userInterface` 与矩阵 (a) 该行 1:1。** L 合同章节齐全（直觉、定义、机制链 1–6、带 caption / 文字等价物 / 图边界的两张 ASCII、正反例、变式、误区、来源表）。**主叙事站得住且与源码对齐**：四扇 GET 与磁盘同名；classic 无 UseCase；自有 `self`、他人 `list`/`query` + 主键闸；目标人过滤；快照现查库且不读凭据；详情两因一句；注册表启动扫描、目录方法不上架；开关关掉是缺 Bean 不是空数组；curl 占位符无 secret。不必为「窗口名字」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例，或只需半句、不挡住把**本行**标 covered：（1）unavailable / snapshot / target catalog 三句是 HTTP 200 + JSON `code=500`，不是 HTTP 4xx；（2）Java 示例是 `appKey` 变量，尖括号占位符在 curl；（3）演示回显依赖默认 `bundle-full` 的 `wta-demo`，不是无条件空货架补丁；（4）Resolver Bean 不随开关卸掉，但没有 Controller 仍是 404；（5）非超管单测只打了他人列表。不要把凭据生命周期、HMAC 网关、`createOpenApiService`、demo Controller 塞进 38 分钟课。
- 不建议 `mine-more`：b01 未证明本行还有真实矩阵空洞；再五问只会把 Registry 扫描、网关验签或凭据窗再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；信封与 echo/classpath 半句是可选补丁，不是第二容器。不建议 `split-lesson`：未满 10 问；四方法是 chain 原设计的同一 OBJ-30、同一份 Controller，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：list/detail/resolve/matcher 是 (a) 的解释到副作用。不建议把 HMAC (b)、`createOpenApiService`、MySQL 容器或未入库的机器调用 (d) 标 covered。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:SysOpenApiCatalogController.selfInterfaces,selfInterface,userInterfaces,userInterface | 四扇窗路径/动词/三权限、他人主键闸、目标人来源、快照失败不给空列表、详情两因一句、零 POST 零凭据、关掉是 404、classic 无 UseCase——均与源码对齐。残留精度：（1）三句业务异常是 HTTP 200 + JSON `code=500`；（2）`<APP_KEY>` 只在 curl；（3）echo 依赖默认 `bundle-full`；（4）Resolver 不随开关卸；（5）非超管只测了 `userInterfaces`。 | 可选半句：信封说「`R.fail` + HTTP 仍 200」；Java 示例不要背成尖括号；echo 不是瘦包装的验收。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-030 来源表 S-L030-01…08）。 | defer（本行可 covered；精度项不挡盖章） |
| A:SysOpenApiCredentialController.* / B:SysOpenApiCredentialController.create/reset | 本课只借用同一门牌与「不读凭据表」。 | 保持负空间。 | defer（L-031） |
| B:OpenApiSigner / OpenApiCanonicalizer；SaTokenOpenApiMachineSessionOperations；RedissonOpenApiNonceStore / RateLimiter | 本课只借用同一 Matcher 与「200 ≠ 能 HMAC」。 | 保持。 | defer（L-032） |
| A:createOpenApiService | self/users GET 与 `encodeURIComponent` 已作邻居证据。 | 保持。 | defer（L-033） |
| public-contracts HTTP/SQL/OpenAPI | 管理端快照复印件已划。 | 不要把 `openapi.ts` 当成机器货架立法原件。 | defer（已 covered by L-005） |
| OpenApiDemoController.echo | 注册表夹具，证明空规则会上架。 | 不要扩进矩阵（Goal 边界：demo 展示 Controller 不进 in-scope）。 | defer（范围外） |
| MySQL 8.4 / mysql-base | 无目录表、只读授权投影已点名。 | 保持。 | defer（L-085） |
| Redis 8 | 目录窗不写 nonce / 机器会话。 | 不要因本课标 covered。 | defer（(c) 容器 / L-032） |
| (d) 机器 HMAC 调用流 | 矩阵无此行。课 ASCII 把网关画在图外。 | 不要把本课图 1 当成 (d) 新行。 | defer（未入库；L-032 若要流再开） |
| (b) SystemOpenApiCatalogService.list/detail / Registry / Resolver / Matcher | 矩阵无此行。课已把副作用写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本行无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出四扇窗目的、主路径（self 按会话目标人滤货架 / 超管按路径目标人滤货架）与失败路径（开关 404、他人非超管、快照 unavailable、详情 unavailable）；把可选半句写进原课不会超过 40 分钟；凭据/网关/前端工厂已有后续课。满 10 问前不拆。四个方法仍是同一 `wta-system` 容器的一份 Controller。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-031 凭据、L-032 网关 HMAC、L-033 工厂、L-005 复印件、demo Controller、L-085 基座、Redis 容器、未入库 (b)/(d) 显式建议 `defer`；对本课一行 (a) 建议 Lead 标 `covered`（证据锚本 probe 与 L-030 来源表）。本课不主张改 MySQL 容器、HMAC 数据流、凭据窗或任何 (b)(c)(d) 格子。
