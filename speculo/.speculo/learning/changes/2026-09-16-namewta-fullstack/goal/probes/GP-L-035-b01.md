---
artifact: learning-goal-probe
probe_id: GP-L-035-b01
lesson_id: L-035
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-035 / b01

审问对象是已写成的 Lesson `lessons/L-035-person-application.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain / `claimed_cells` / 矩阵 (a) **一行**）：`A:PersonApplicationController.current, submit`。OBJ-35 原文：能口述 `PersonApplicationController` current/submit。档是**解释+走查**。chain 覆盖列只写 (a)。矩阵 (a) 符号就是 `PersonApplicationController.current, submit`，模块 `wta-profile-person`，仍 `uncovered`。矩阵 (b) **没有** submit 回滚 / 乐观锁 / 生成列行。矩阵 (c) `wta-modules` 业务房间已 covered（L-002/L-003）；MySQL 8.4 基座仍 uncovered。矩阵 (d) **有** 主路径 profile 申请：原文 `PersonApplication.submit → admin decide`，备注 `L-035, L-034`，仍 `uncovered`。本课 `claimed_cells` **未**列出 (d)；派单仍要求本批同时审问 **(a) current/submit** 与 **(d) 申请主路径的 submit 半段**。`PersonAdminController.*` / `decide` 留给 L-034；换绑五枪留给 L-036；材料 attach/detach 留给 L-037；核身匿名回调留给 L-038；`createProfileService` / person web-domain 留给 L-039；企业 `current,save,submit,probe` 留给 L-041。同门 `save` 是课内必须口述的邻居，不是矩阵第四个符号。

抽查锚点（2026-09-16）：`backend/wta-modules/wta-profile/wta-profile-person/src/main/java/org/namewta/profile/person/controller/self/{PersonApplicationController,PersonApplicationExceptionHandler}.java`；`usecase/{PersonApplicationUseCase,impl/PersonApplicationUseCaseImpl}.java`；`service/{PersonApplicationService,IPersonApplicationService}.java`；`dao/PersonApplicationDao.java`；`mapper/PersonApplicationMapper.java`；`src/main/resources/mapper/person/PersonApplicationMapper.xml`；`listener/PersonApplicationProcessListener.java`；`adapter/gateway/SpringPersonWorkflowGateway.java`；`domain/{ProfilePersonApplication,application/{PersonApplication,PersonIdentityFields},bo/{PersonApplicationSaveBo,PersonApplicationSubmitBo},vo/PersonApplicationVo,exception/PersonApplicationException}.java`；`controller/admin/PersonAdminController.java` + `service/PersonAdminService.java`（(d) 汇，不认本行）；`src/test/.../controller/self/PersonApplicationHttpContractTest.java`；`architecture/PersonModuleArchitectureTest.java`；`service/impl/{PersonApplicationServiceTest,PersonApplicationServicePersistenceTest,PersonApplicationMySqlE2ETest,PersonApplicationServiceImpl}.java`；`release-artifacts/docker/infrastructure/mysql/init/{10-cde-base-ddl.sql,50-cde-base-dml.sql}`；`.agents/skills/engineering-standards/references/project/03-backend-module-modes.md` layered 行；`frontend/packages/domains/profile/src/person/application/{service,index}.ts`（旁证，格子归 L-039）；`frontend/packages/api-contracts/generated/openapi.ts` `/profile/person/application`。

## Q1 澄清

- objective_id: OBJ-35
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-35；chain L-035 覆盖列只 `A:PersonApplicationController.current,submit`；L-035 `claimed_cells` 一格；矩阵 (a) 该行仍 uncovered；(b) 无本课符号；(c) profile 房间已 covered、MySQL 8.4 未 covered；(d) 「PersonApplication.submit → admin decide」挂 L-035+L-034 仍 uncovered；课开篇「认矩阵 (a) 一格」+ 负空间 L-034/L-036/L-037/L-038/L-039；课明确 submit ≠ 档案诞生
- difficulty: medium

课把自助窗收成「两枪名字 + 同门铅笔 + 分层五层」。计划里至少有五套可数对象，派单点名的是 **(a) 与 (d)**：

1. **OBJ-35 原文。** `course.md`：能口述 `PersonApplicationController` current/submit。档是解释+走查。一句里两枪，没有把 `save`、`handleProcess`、`PersonAdminController.decide`、材料 attach、核身回调写成口试清单。
2. **chain 覆盖列。** 一格：`A:PersonApplicationController.current,submit`。不写 (d)，不写 save。
3. **课 `claimed_cells` / 矩阵 (a) 一行。** 符号就是 `PersonApplicationController.current, submit`，模块 `wta-profile-person`。与磁盘两映射 1:1。Lead 若按行标 covered，**不会**把同门 `save` 或管理端十二扇假闭合。
4. **课自己的操作定义。** GET 根 = `current`（无 body，空申请 `R.ok(null)`）；POST `/submit` = `submit`（body 只有 `expectedVersion`）；POST 根 = 同门 `save`。权限一把 `profile:person:apply`。主人只来自 `LoginHelper.getUserId()`。分层 `Controller/Listener → UseCase(@DSTransactional) → Service → DAO → Mapper XML`。submit 出口是 `WAITING` + 工作流门铃，**不是** `profile_person`。
5. **矩阵 (d) 主路径。** 原文 `PersonApplication.submit → admin decide`，备注 `L-035, L-034`。课把汇写成「Listener 收到 `FINISH` 且非 `REJECT` 之后 `publishApproved`，归 L-034」。磁盘管理端 `decide` 是另一条：`WAITING` → `OVERRIDE_PENDING` → `workflow.terminate` → 拨回 `WAITING` → `publishApproved`。E2E 生命线用 `handleProcessEvent("finish")` 当短路，不打 admin HTTP。
6. **邻格。** L-034 已认管理端十二扇与 decide 先掐流程再发布。L-036 换绑。L-037 ALWAYS 材料窗。L-038 匿名回调。L-039 厨房三枪 URL。L-041 企业多一扇 probe。L-003 已认 `wta-profile` = layered。

问：本格 (a) 闭合标准是「两枪 URL/动词/权限 + 空申请 200 空 data + submit 先冻快照再按门铃且失败回滚 + 字段名 `service` 实际是 UseCase」，还是 OBJ 那句必须把同门 `save` 升成第二格、或必须把 (d) 整条走廊并进本行才能盖章？(d) 的汇是矩阵原文的 **admin decide**，还是课里的 **Listener FINISH**？Lead 若只凭本课标 (d) covered，是否假闭合了 L-034 的 terminate+publish？

## Q2 证据

- objective_id: OBJ-35
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 28–62；Advice 9–16；UseCase 12–42；UseCaseImpl 19–53；Service 55–58 / 104–160 / 166–215 / 279–331 / 359–364 / 720–734 / 782–788；Mapper XML 113–129 / 160–220；HttpContractTest 23–57；ArchitectureTest 30–48 / 92–136；ServiceTest 91–105 / 122–163 / 166–240；PersistenceTest 43–52；E2E 86–113 / 160–208 / 326–374；Listener 16–33；Gateway 27–49；VO/BO/SubmitBo；DDL 754–791；DML apply/flowCode/manual；frontend service.ts 11–18；openapi.ts 2256–2293；Admin decide 164–194 / Mapper 108–141；R.fail 130 + HttpStatus.ERROR=500
- difficulty: high

课的 current/submit 与 (d) submit 半段哪些能在工作树直接复现，哪些只是测试名单比课表窄、或把 (d) 的汇讲成了另一扇门？

**仍能复现（本格 (a) + (d) 的 submit 半段）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 门牌 `/profile/person/application`；三映射无 PUT/DELETE/`{id}` | Controller 28；openapi.ts 2256–2293 `put?: never` / `delete?: never`；合同测试 25 |
| 字段名叫 `service`，类型是 `PersonApplicationUseCase` | Controller 31 |
| `current` GET 根、无 `@Log`；`save` POST 根 `@Log` UPDATE 双 false；`submit` POST `/submit` `@Log` OTHER 双 false | Controller 36–62；合同测试 34–39 / 45–50 |
| 三方法权限都是 `profile:person:apply`；`self` 无 `@SaIgnore` | Controller 每方法；合同测试 31–33；ArchitectureTest 44–46 |
| 主人只来自 `LoginHelper.getUserId()`；SaveBo 组件名不含 `userId`/`applicantUserId`；SubmitBo 只有 `@PositiveOrZero int expectedVersion` | Controller 39 / 50 / 61；SaveBo 11–20；SubmitBo 8；合同测试 26–28 |
| Controller 不把 SubmitBo 整份下传，只拆 int | Controller 61 |
| 生产 Controller 只 import usecase，不 import service | ArchitectureTest 126–130；Controller import 第 8 行 |
| UseCase 无参 default `@Deprecated`，调用抛「请传入 userId」；带 userId 的 default 回落到无参 | UseCase 12–37 |
| 生产实现覆盖 `current/save/submit/handleProcess`，**每一枪** `@DSTransactional`；Service 方法本身无此注解 | UseCaseImpl 24–53 |
| 生产 Service 类名就是 `PersonApplicationService`，实现 `PersonApplicationPublicationPort`，**不** `implements IPersonApplicationService` | Service 55；接口 `IPersonApplicationService.java` 存在但生产类声明不含它 |
| 生产 service 包 **没有** `*ServiceImpl`；测试适配器在 `src/test/.../PersonApplicationServiceImpl`，注释写明兼容 | ArchitectureTest 98–102；测试类 20–22 |
| `current`：`requireUserId` → `selectOpenByUserId`（**无** `for update`）→ `Vo.from` 或 `null` | Service 104–107 / 240–242；XML 113–120 |
| 开着的 SQL 状态：`DRAFT,BACK,CANCEL,WAITING` 且 `del_flag='0'`；无 `LIMIT 1` | XML 119 |
| 空申请是成功：Controller `R.ok(voOrNull)`，不是 404 | Controller 39；Service `orElse(null)` |
| VO 有身份展示字段 + `snapshotVersion`/`version`；**没有** `identityKey`/`applicantUserId`/`rebindIntent`/`targetProfileId` | Vo 10–24 vs `PersonIdentityFields.identityKey` |
| 可编辑 = `DRAFT/BACK/CANCEL`；`PersonApplication.editable()` 与 `EDITABLE_STATUSES` 同组；`WAITING` 不可 save/submit | domain 24–26；Service 58 / 117–118 / 137–138 |
| `save`：WAITING → `PERSON_APPLICATION_READ_ONLY`；normalize 拼 `TYPE:NUMBER`（strip + `Locale.ROOT` 大写）；INSERT 写死 `DRAFT`、`submission_seq=0`、`rebind_intent='N'`、`version=0`；`updateDraft` 把 BACK/CANCEL 拉回 DRAFT 且 `version+1` | Service 112–128 / 279–300；XML 160–183；IdentityFields 22–31 |
| 账号已有有效绑定则 `targetProfileId` 盯原档案；单测换证件号仍 9201 | Service 122–125；ServiceTest 91–105 |
| `submit` 顺序：锁开着的行 → 本人且可编辑 → 版本 → `validateComplete` → 供应商 → `requireSubmissionAllowed` → ALWAYS 材料 → `insertSubmission` → `snapshotImmutable(WORKING→SUBMISSION)` → `markWaiting` → `startAttempt` → `workflow.start` | Service 134–160；ServiceTest 122–145 `submitsImmutableFieldAndMaterialSnapshotBeforeWorkflow` |
| 无开着的行 → `PERSON_APPLICATION_NOT_FOUND`（`lockOpenByUserId` → `requireApplication`） | Service 246–247 / 667–669 |
| `WAITING` 再 submit → `PERSON_APPLICATION_NOT_EDITABLE` | Service 137–138 |
| 版本不对 / `markWaiting` 条件不中 → `PERSON_APPLICATION_VERSION_CONFLICT` | Service 140–141；XML 213–219 `submission_seq = #{submissionSeq} - 1` 且可编辑且旧 version |
| 身份被别人有效绑定 → `PERSON_REBIND_CONFIRMATION_REQUIRED`，不改 binding | Service 323–326；PersistenceTest 43–52 |
| 指纹 `SHA-256(identityKey + "\n" + snapshotVersion)`；供应商异常收成 `PERSON_PROVIDER_UNAVAILABLE` | Service 799–800 / 782–788 |
| 网关：无 `WorkflowService` 或无 `profile.person.flowCode` → `PERSON_WORKFLOW_UNAVAILABLE`；`startCompleteTask` 失败 → `PERSON_WORKFLOW_START_FAILED`；变量 `profileType=PERSON`、`snapshotVersion`、`submissionId`；`businessId` 是申请主键字符串 | Gateway 27–49 |
| `workflow.start` 抛错时 **never** `publishApproved` | ServiceTest 148–163 |
| Advice `R.fail(exception.getMessage())`；`R.fail(String)` 的 JSON `code=500`（`HttpStatus.ERROR`）；HTTP 状态由 MockMvc `isOk()` | Advice 13–16；R.java 130；HttpStatus 82；E2E 196–197 |
| 生成列 `open_user_id` / `open_identity_key` 只在进行中四态取值；两 unique key；实体 `FieldStrategy.NEVER` | DDL 759–791；实体 27–34 |
| 配置种子 `profile.person.provider.default=manual`、`profile.person.flowCode=profile_person_verification`；权限串 `profile:person:apply` | DML 534 / 1427 / 1429 / 1508 |
| 登记表 `wta-modules/wta-profile` = layered | `03-backend-module-modes.md` 18 |
| 前端厨房三枪 URL/动词与本窗对齐；`controller: 'PersonApplicationController'` | `service.ts` 14–17；`index.ts` 4–7 |
| submit 成功当时 `profile_person` 仍 0 行；SUBMISSION 材料 2 行 | E2E 93–100 |

**(d) 汇与邻格仍能复现（不计入本行 (a) 闭合；用来判断走廊能不能单独盖章）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| E2E 在 submit 之后另调 `handleProcessEvent("finish")` 才出现 `profile_person` + ACTIVE 绑定 + VERSION 材料 | E2E `finish()` 326–337 / 102–113 |
| Listener 把 `ProcessEvent` 转成 `PersonApplicationProcessCommand`，`@Order(HIGHEST_PRECEDENCE)`，不是这三扇 HTTP 的同步返回 | Listener 16–33 |
| `handleProcess`：flowCode 不对或 businessId 非正数 → return；必须仍是 `WAITING` 且 seq 对得上；`FINISH`+`REJECT` → `INVALID` 不发布；`FINISH` 非拒绝 → `publishApproved` + 材料 SUBMISSION→VERSION；`BACK/CANCEL/INVALID/TERMINATION` 只改状态 | Service 166–200 |
| **矩阵原文的汇**是 admin `decide`：锁 `WAITING` → `OVERRIDE_PENDING` → `workflow.terminate` → 驳回 `INVALID` / 通过 `resumeWaiting` 再 `publishApproved` | AdminService 164–194；AdminMapper 108–141；L-034 已写这条链 |
| `OVERRIDE_PENDING` **不在** `selectOpenByUserId` 的四态里；decide 进行中 `current()` 会变成 null | XML 119 vs AdminMapper `markOverridePending` 119–125 |
| 换绑 Listener 同样 `HIGHEST_PRECEDENCE`；普通申请 INSERT `rebind_intent='N'`；`publishApproved` 看见 `Y` 会炸 | Rebind Listener 29；XML 169；Service 376–378 |

**课写过、测试名单更窄或把 (d) 汇讲偏（不是证伪 (a)）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「连只读的 `current` 也盖了 `@DSTransactional`」 | UseCaseImpl 24–27 **有**；合同测试只断言 `save`/`submit`/`handleProcess`（51–57），**没有** `current(long)` |
| 「E2E 工作流失败回滚是 UseCase 事务章的铁证」 | E2E fixture 371–374 把 **测试适配器** `PersonApplicationServiceImpl` 直接塞进 Controller，**不经过** `UseCaseImpl`。失败后显式 `session.rollback()`（198 行）。HTTP `isOk` + `$.msg=PERSON_WORKFLOW_UNAVAILABLE` + 回滚后 DRAFT/0 submission 仍成立，但证人是 JDBC 会话回滚，不是 Spring `@DSTransactional` |
| 「档案/绑定是 `handleProcess` 收到 `FINISH` 之后」 | 这是引擎回写门 + E2E 短路。矩阵 (d) 写的是 **admin decide**。生产 decide **terminate** 流程，不靠 FINISH 事件来 publish |
| 口试动词写成 GET/POST | 厨房 `method: 'get'`/`'post'` 小写（axios 合同）。语义对 |
| `selectOpenByUserId` 无 `LIMIT 1` | XML 事实；课边界 4 已写。无单测打脏数据多行 |
| Service 构造有未使用的 `Object jsonMapper` | Service 78；真正序列化 `JsonUtils.toJsonString`。课边界 3 已写 |

问：在两枪 URL/权限/空申请 200、submit 先冻材料再开流程、工作流失败不 publish、生成列占坑、分层不越层已经能互证的前提下，(a) 是否还要把「合同测试没钉 current 的事务注解」「E2E 绕开 UseCaseImpl」写成开篇半句才算讲完？(d) 是否必须等 GP-L-034 把 terminate+publish 钉死，才能把整行从 submit 半段补成矩阵原文的汇？

## Q3 假设与反例

- objective_id: OBJ-35
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 空申请非 404；WAITING 不可再 submit；body 无 userId；工作流失败不留 submission；非换绑窗；字段名 service≠classic；Advice HTTP 仍 200；Listener 非 submit 同步返回；VO 无 identityKey；材料窗是 L-037；E2E finish≠admin decide；OVERRIDE_PENDING 对 current 不可见
- difficulty: high

课的隐含假设若反转，合格读者会把哪把枪或哪条走廊认错？

1. **「`current` 没有申请就 404。」** 磁盘 `orElse(null)` + `R.ok`。课反例 1 覆盖。
2. **「`WAITING` 再 POST `/submit` 幂等返回同一 VO。」** `PERSON_APPLICATION_NOT_EDITABLE`。课反例 2 覆盖。
3. **「body 里塞 `userId` 能替别人交。」** SaveBo 无此字段；WHERE 还带 `applicant_user_id`。课反例 3 覆盖。
4. **「工作流 Bean 缺失时 submission 留下当审计。」** 与事务边界相反。课反例 4 覆盖。证人精度见 Q2：E2E 靠 `session.rollback()`。
5. **「这扇窗的 submit 就是换绑。」** 身份被占停在 `PERSON_REBIND_CONFIRMATION_REQUIRED`。课反例 5 / L-036。
6. **「字段名 `service` = classic `Controller → Service → Mapper`。」** 架构测试会拆掉 Controller import service。课反例 6 覆盖。
7. **「`R.fail` 是 HTTP 500 页面。」** E2E `status().isOk()`，JSON `code=500`。课误区 6 / 边界 2。
8. **「`current` 是 `GET /application/{id}`。」** 没有 id。办结后 SQL 看不见，变 null。课误区 1。
9. **「`WAITING` 排除出 current。」** SQL 包含它。课误区 2 / 图 3。
10. **「submit body 还要再交姓名证件。」** SubmitBo 只有版本号。课误区 3。
11. **「UseCase 是空转发，可以删。」** 事务注解除了它没处盖。课误区 4。测试适配器不是生产盖章位置（见 Q2）。
12. **「`PersonApplicationServiceImpl` 是生产分层。」** 只在 test。课误区 5。
13. **「材料校验是 L-035 的 HTTP。」** submit 喊端口，挂材料是 L-037。课误区 7。
14. **「VO 带 `identityKey`。」** 没有。课误区 8。
15. **「Listener 画进 submit 时序的同步返回。」** 浏览器拿到的是 WAITING。课误区 9。
16. **「本课覆盖管理员 `decide`，所以 (d) 整行可 covered。」** 课自己写「decide 是另一张桌子」。矩阵备注挂两课。把假设反转后，Lead 会用本行假闭合 L-034。
17. **「E2E `finish` 就是矩阵里的 admin decide。」** `finish()` 造 `ProcessEvent` 直接打 `handleProcessEvent`。生产 decide 先 `OVERRIDE_PENDING` 再 `terminate`。课把两条门收成一句「流程回写 / 管理员决定」，没有把「E2E 短路 ≠ decide HTTP」写成反例。
18. **「企业那扇也只有 current/submit。」** L-041 格子含 `save` 与 `probe`。个人没有 probe。课变式已写。
19. **「save 也在矩阵 (a) 这一格，因为没有草稿就没有提交。」** 课要求口述邻居，格子名仍是 current/submit。升格会发明第四个符号。
20. **「进行中四态之外，decide 过程中 current 仍看得到本子。」** `OVERRIDE_PENDING` 不在 GET SQL 里。这是 (d) 走廊中段，课未写。属 L-034 状态，不是 (a) 的证伪。

问：读完后仍说不出的，是不是「两枪的名字」，而是 **本行不含 save/decide、空申请是 200 空 data、submit ≠ 档案、E2E finish 不是 admin decide、字段名 service 不是 classic**？

## Q4 视角

- objective_id: OBJ-35
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-35 vs L-003/L-034/L-036/L-037/L-038/L-039/L-041；claimed_cells 无 (d)；Admin decide 链；生成列；UseCase 事务
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:PersonApplicationController.current, submit`** 或本课能独立闭合的 (d)？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 两枪独行 vs 课内必须口述 save；(d) 挂两课；`claimed_cells` 无 (d)；(b) 无 submit 回滚行 | **是** 闭合定义。(a) 可单独 covered。save 不要升格。(d) 本课只交 submit 半段 |
| 自助窗作者 | 三映射、权限、Log 不存包体、LoginHelper、Advice 类别码 | **是** (a)。decide HTTP **否** |
| 分层电工 | Controller↛Service；Service↛Mapper；DAO `@Repository`；XML 语句 id 对齐；禁止注解 SQL | **是** 本窗五层。整个 profile 房间画像 **否**（L-003 已 covered） |
| 提交规则书 | 锁→完整校验→绑定闸→ALWAYS→冻快照→WAITING→核身→门铃；失败类别码 | **是** (a) 主链。材料挂载 HTTP **否**（L-037） |
| (d) 走廊作者 | 源：POST `/submit`；中：WAITING + 流程实例；汇：档案/绑定 | submit 半段 **是**。汇若按矩阵原文 = admin decide，**否**（L-034）。汇若按 E2E = Listener FINISH，是测试短路，**不要**拿它盖矩阵行 |
| 管理端柜员（L-034） | `OVERRIDE_PENDING`、terminate、resume、`publishApproved` | 对照一句「submit ≠ 档案」**是**。十二扇全表 **否** |
| 换绑窗（L-036） | 五枪；本窗停在 `PERSON_REBIND_CONFIRMATION_REQUIRED` | 失败闸 **是** 边界。confirm 改绑定 **否**（且 (b) 行在换绑） |
| 材料窗（L-037） | attach/detach WORKING | submit 的 `validateRequired` **是** 闸。HTTP **否** |
| 核身邮筒（L-038） | 匿名 callback | submit 里 `startAttempt` **是** 门铃。回调窗 **否** |
| 前端厨房（L-039） | `createPersonApplicationService` 三枪 | URL 对齐旁证 **是**。工厂格子 **否** |
| 企业对照（L-041） | 多 `save` 入格、多 probe | 「个人没有 probe」**是** 对照。企业四枪 **否** |
| MySQL 8.4 容器 | 生成列当裁判 | 本窗 unique 意图 **是** 机制。容器课 **否**（L-085） |
| dao-mapper-ownership | DAO 唯一持 Mapper | 本窗举例 **是**。组件行 **否**（L-083） |
| (b) submit 回滚/乐观锁 | 矩阵无此行 | 课已写进 (a)。不要发明 (b) |

问：Lead 若凭「两枪都在函数表里」把 `PersonApplicationController.current, submit` 标 covered，是否还要求读者能指着 **空申请 200、submit 先冻后按铃、失败不 publish、生成列占坑、分层不越层、submit ≠ 档案**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要把 admin decide 算进 (a)？(d) 整行是否明确写成「等 GP-L-034，或接受半段+汇分家」？

## Q5 元问题

- objective_id: OBJ-35
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 两枪独行、无 save 行、无 (b)；矩阵 (d) 挂 L-035+L-034
- difficulty: low

建议：`defer`

原因：

- **`A:PersonApplicationController.current, submit` 与矩阵 (a) 该行 1:1。** L 合同章节齐全（宏观地图、直觉十二句、定义表、机制链 1–7、带 caption / 文字等价物 / 图边界的三张图、正反例、变式、误区、暂停走查、来源表 S-004/S-008/S-010/S-L035-01…10）。**主叙事站得住且与源码对齐**：三扇 HTTP、权限一把、LoginHelper、空申请 `R.ok(null)`、UseCase 盖事务章、submit 先冻材料再 `workflow.start`、失败类别码、生成列占坑、submit 当时没有 `profile_person`。不必为「两枪名字」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例或邻格，不挡住把 **(a)** 标 covered：（1）合同测试未钉 `current(long)` 的 `@DSTransactional`（源码有）；（2）E2E 工作流失败走测试适配器 + `session.rollback()`，不是 `UseCaseImpl` 的 Spring 事务证人；（3）厨房动词小写；（4）`jsonMapper` 未使用。不要把 save、decide、材料窗、换绑、核身回调、厨房工厂塞进 (a)。
- **(d) 主路径 profile 申请不能由本课单独 covered。** 矩阵原文汇是 `admin decide`。本课只把源讲完：POST `/submit` → `WAITING` + 门铃。汇在 L-034 的 terminate+publish。课把 E2E `handleProcessEvent("finish")` 与 Listener FINISH 写成档案诞生门，和矩阵用词不完全同构；这是 Lead 盖 (d) 时要选的操作定义，不是 (a) 的空洞。GP-L-034 尚未落盘。
- 不建议 `mine-more`：b01 未证明 (a) 还有真实矩阵空洞；再五问只会把 save 邻居、L-037 材料、L-036 换绑、L-034 decide 或 L-039 厨房再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；可选半句（E2E 短路 ≠ decide HTTP；E2E 回滚证人是 JDBC 会话）不是第二容器。不建议 `split-lesson`：未满 10 问；(a) 与 (d) 半段是 chain 原设计的同一 OBJ-35，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：乐观锁、回滚、生成列是 (a) 的解释到副作用。不建议把 `save` 升成矩阵第二格才盖章：课已写明格子只认 current/submit。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:PersonApplicationController.current, submit | 门牌三映射、权限/Log、LoginHelper、空申请 200 空 data、分层五层、submit 先冻快照再按门铃、失败类别码、生成列占坑、submit ≠ 档案——均与源码对齐。残留精度：（1）合同测试未钉 current 的 `@DSTransactional`；（2）E2E 失败回滚绕开 UseCaseImpl；（3）厨房动词小写。 | 可选半句：口试事务章以 `PersonApplicationUseCaseImpl` 为准；E2E 工作流失败的回滚证人是测试会话。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-035 来源表 S-L035-01…10）。 | defer（本行可 covered；精度项不挡盖章） |
| D:主路径 profile 申请（PersonApplication.submit → admin decide） | 本课交清源：submit → WAITING + `workflow.start`，当时无 `profile_person`。汇按矩阵原文是 L-034 `decide`（`OVERRIDE_PENDING` → terminate → resume → `publishApproved`）。课/E2E 常用 Listener `FINISH` 当短路。`claimed_cells` 未列本行。 | 不要用本课单独标 covered。Lead：等 GP-L-034 后按两课拼接盖章；操作定义写明「汇 = admin decide HTTP，不是 E2E finish」。可选在本课边界加半句，不升格、不发明第二课。 | defer（整行等 L-034 mine；本课不 mine-more） |
| 同门 save | 课已讲完且源码可复现。矩阵 (a)/(b) **无此符号**。 | 不要并进 current/submit 才能盖章。本批不把 save 当本行缺口。 | defer（未入库邻居） |
| A:PersonAdminController.* / decide | 本课只保证 submit 不发布档案。terminate+OVERRIDE_PENDING 未认格。 | 保持。L-034。 | defer（L-034） |
| A:PersonRebindController.* / B:confirm | 本课只保证身份被占停在 `PERSON_REBIND_CONFIRMATION_REQUIRED`。 | 保持。L-036。 | defer（L-036） |
| A:PersonMaterialSelfController.attach, detach | submit 的 ALWAYS 闸已借用。挂材料 HTTP 未讲。 | 保持。L-037。 | defer（L-037） |
| A:PersonVerificationAnonymousController.callback | submit 才 `startAttempt`。回调窗未讲。 | 保持。L-038。 | defer（L-038） |
| A:createProfileService / createProfileWebDomain person/self | 三枪 URL 旁证已借用。 | 保持。L-039。 | defer（L-039） |
| A:EnterpriseApplicationController.current, save, submit, probe | 个人没有 probe 的对照。 | 保持。L-041。 | defer（L-041） |
| (b) submit 回滚 / 乐观锁 / 生成列 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| C:MySQL 8.4 / mysql-base | 生成列是本窗机制，不是容器课。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |
| C:dao-mapper-ownership | 本窗 DAO 持 Mapper 是举例。 | 保持。L-083。 | defer（L-083） |
| 失败路径 profile 换绑 | 矩阵另一行，挂 L-036。 | 本课只点名确认码。 | defer（L-036） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本行 (a) 无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出两枪目的（看我的进行中本子 / 交不可变复印件并按门铃）、主路径（save 邻居 → submit → WAITING）与失败路径（版本冲突、不可编辑、身份需换绑确认、工作流失败不留快照）；(d) 缺的是与 L-034 的盖章时序和「admin decide vs Listener FINISH」一词，不是讲义容器；把可选半句写进原课不会超过 40 分钟；管理端、换绑、材料、核身、厨房、企业 probe 已有邻课。满 10 问前不拆。两枪与 (d) 半段虽跨自助窗与管理端柜台，仍是 chain 原设计的同一 OBJ-35 加一条联合 (d)，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-034 decide、L-036 换绑、L-037 材料、L-038 核身、L-039 厨房、L-041 企业、未入库 save/(b)、MySQL 容器与 dao-mapper 组件显式建议 `defer`；对 `A:PersonApplicationController.current, submit` 建议 Lead 标 `covered`（证据锚本 probe 与 L-035 来源表）；对 `D:主路径 profile 申请` 建议 Lead 等 GP-L-034 或按「源=submit 半段、汇=admin decide」拼接后再盖章。本课不主张改 save 入格、发明 (b)、或把 Listener FINISH 写成矩阵 (d) 的汇。
