---
artifact: learning-goal-probe
probe_id: GP-L-081-b01
lesson_id: L-081
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-081 / b01

审问对象是已写成的 Lesson `lessons/L-081-ai-frontend.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **两格**：`A:createAiService`、`A:createAiWebDomain`。OBJ-81 原文：能说明 `createAiService` 与 `createAiWebDomain`。档是**解释+对照**。矩阵几何 **不是 1:1 两行**：

- 厨房是 **独行** `createAiService`（模块 `domains/ai`，仍 uncovered，备注空）。
- 菜单工厂 **没有独行**；符号落在 **九工厂同行** `createAdminWebDomain, createSystemWebDomain, createProfileWebDomain, createNotifyWebDomain, createThirdWebDomain, createWorkflowWebDomain, createDemoWebDomain, createAiWebDomain, createProfileSelfWebDomain`（模块 `web-domains/*`，仍 uncovered，备注「manifest 工厂」）。

矩阵 (b) **没有** 一枪登记 / 四口 fail-closed / 探路 / 15 秒 / 权限空数组独行；已印的 ai (a) 是 `SnailAiController.registerCurrentUser`（L-080，仍 uncovered）。矩阵 (c) `admin-web` / `home-web` 容器与 `frontend-direction` / `admin-web-composition` 已 covered（L-004 / L-007 / L-008）。矩阵 (d) **没有**浏览器 AI 会话流；已 covered 的走廊是登录后查询与 OSS 直传（L-006 / L-027 / L-029）。本批按派单审问 **厨房独行 + 菜单工厂这一颗切片**；口试按磁盘：根 `src/index.ts` 冻住 **1** 扁平枪 `registerCurrentSnailUser`，`POST /snail-ai/user/register`，无 body，有 `data` 才 `projectAiUserTransport` 并丢掉 `avatarUrl`，失败原样抛；菜单 id `web-domain-ai`、`domainId` `'ai'`、先 `requireAiWebRuntime` **核四口** 再冻 **1** 键 `ai/chat/index`、`permissions: []`；页面经 `createAiChatSession` 扣 `runtime.service`，聊天文档走宿主 `probeFrame` + iframe，**不**进 freeze 对象。不把九工厂行整行闭合，不把 L-007 插头再盖第二次章，也不把 Vue 文件名或 `createAiChatSession` 升成矩阵行。Java 登记窗留给 L-080；运维玻璃 `monitor/snailai/index` 留给 L-024；导航 host / 九工厂行警告留给 L-020；厅堂插座板全表留给 L-007 / L-008。厂商引擎内部保持 `deferred(vendor-engine)`。

抽查锚点（2026-09-17）：`frontend/packages/domains/ai/src/{index,index.test,transport,transport.test}.ts`；`src/snail-ai/index.ts`；包 `package.json` `exports` / `AGENTS.md` / `README.md`；`frontend/packages/web-domains/ai/src/{index,index.test,runtime,chatSession,chatSession.test,pages}.ts`；`snail-ai/AiChatPage.vue`；`apps/admin-web/src/application/services.ts` 第 3 / 90 行；`application/session.ts` 第 3 行；`router/adminManifestRegistry.ts` 第 59–76 / 308–334 / 343 / 358 / 365–376 行与 `.test.ts` 第 109–126 / 182–261 行；`apps/home-web/src/application/services.ts`；`homeManifestRegistry.ts` 第 34–39 行；`50-cde-base-dml.sql` 第 42 / 69 行；`SnailAiController.java`；`application-dev.yml` `snail-ai.enabled` / `open-api.enabled`；`application.yml` `security.excludes`；`frontend/e2e/ai-domain.spec.ts`；`api-contracts` `OpenApiUserVO`；档案楼 `requireProfileWebRuntime`；demo / workflow 只核对象在不在；通知/三方 web-domain 无 `require*WebRuntime`。

## Q1 澄清

- objective_id: OBJ-81
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-81；chain L-081 覆盖列两格；课 `claimed_cells` 两格；矩阵 (a) 厨房独行仍 uncovered、九工厂同行仍 uncovered、无 Vue 行、无本工厂独行、无 `createAiChatSession` 行；(b) 无本课符号、Java 窗挂 L-080；(c) 两厅堂已 covered；(d) 无浏览器 AI 会话流，已 covered 的是登录后查询与 OSS 直传；课开篇「口试名单就是矩阵 (a) 这两格」+「不要把整行九厂标成 covered」+ 负空间 L-080 / L-024 / L-007 / L-008 / L-020
- difficulty: medium

课把浏览器 AI 这一头收成「门房手里唯一一张登记牌 + 大厅墙上的一扇同域玻璃」。计划里至少有六套可数对象，派单点名 **两格 (a)**，但矩阵把它们印成 **两种几何**：

1. **OBJ-81 原文。** `course.md`：能说明 `createAiService` 与 `createAiWebDomain`。一句里是 **厨房工厂 + 菜单工厂**，不是 Java `registerCurrentUser`、不是监控四块屏、不是 `monitor/snailai/index`，也没有把九个 `create*WebDomain` 写成口试清单。课把 OBJ 扩成「一枪 URL + 一键怎么喂页面 + 四处故意错位」，那是走查，不是第三张矩阵行。
2. **chain / 课 `claimed_cells`。** 两格。符号就是这两个工厂名。磁盘上 **没有** `createAiChatService` / `createSnailChatService`，也 **没有** `@namewta/domain-ai/snail-ai` 小厨房。管理端菜单 id `web-domain-ai`，一键一次冻住。chain **不**写 Vue 文件名，也 **不**写 `createAiChatSession`。
3. **矩阵 (a) 两处不对齐。** 厨房独行与本课第一格 1:1，Lead 若按该行标 covered **不会**绑死九个菜单工厂。九工厂行把本课第二格和 `createDemoWebDomain` 等邻居绑死。Lead 若按**现印同行**标 covered，会把 L-020 / 已切片的 notify/third/workflow/demo 菜单假闭合。矩阵 **没有** `AiChatPage.vue` 符号，也没有会话工厂行。
4. **课自己的操作定义。** 厨房只认 `HttpClient`，返回 `Object.freeze` 的 `AiService`：封面就一支枪 `registerCurrentSnailUser`。无路径 `encodeId`。有 `response.data` 才投影。封面 **没有** `chat` / `stream` / `send` / `registerCurrentUser`。菜单工厂先 `requireAiWebRuntime`（缺托盘 throw `'AiWebRuntime is required'`，缺四口函数也 throw），再冻一键；**不** `addRoute`，**不**拼 `/snail-ai/user/register`。聊天地址是会话拼的 `/snail-chat/`，要先 `probeFrame` 成功才写进 iframe。
5. **四处故意错位是走查，不是第三格。** 厅堂单例导出名 `aiService` ≠ 工厂名；Java 方法名 `registerCurrentUser` ≠ 厨房方法名。厨房只打 `POST /snail-ai/user/register`；iframe 打 `{base}/snail-chat/?openId=&trustedCredential=`。`monitor/snailai/index` 是 `admin-external-monitor`，权限字 `monitor:snailai:list`，不是本厂第二键。`permissions: []` 且种子 perms 空串，不等于匿名可进。不要为了盖章把 `createAiChatSession` 或控制台键再立一行。
6. **邻格。** L-080 classic 登记窗。L-024 运维四屏 + 外部 iframe。L-007 厅堂插头（工厂名 ≠ 导出名已点过，没有这一枪 URL 表）。L-020 九工厂行警告。L-054 / L-066 对照：notify/third 工厂 2026-09-17 **不** fail-closed。L-073 / L-077 对照：流程/演示厂会关门，但只核对象在不在，`service: {} as never` 仍能冻出键。L-044 对照：档案楼缺十一口当场 throw。

问：本两格闭合标准是「厨房独行按 1 枪 URL/动词/无 body/有 data 才投影并丢掉 `avatarUrl`/失败原样抛/无 chat·snail-chat 盖章，菜单只给 `createAiWebDomain` 这一颗切片（一键 + 四口 fail-closed + 页面实际扣的 `runtime.service.registerCurrentSnailUser` / 宿主探路 / iframe），且不碰九工厂整行、不升 Vue 矩阵行、不把 `createAiChatSession` 升成第三格、不提前盖 Java 窗」，还是 OBJ 那句必须先把九工厂行拆开、或必须等 GP-L-080 mined 才给厨房盖章？Lead 若凭「`createAiWebDomain` 四字在函数表里」把九工厂行标 covered，是否假闭合了另外八个工厂？Lead 若因菜单没有独行就把一键+四口判成未讲，是否假判？

## Q2 证据

- objective_id: OBJ-81
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: domains `index.ts` 20–22 / 24–28 / 30–43；`index.test.ts` 6–12 / 14–34 / 36–41；`transport.ts` 6–13；`transport.test.ts` 4–8；`snail-ai/index.ts` 2–4；包 `package.json` 6–8 / 16–20；openapi `OpenApiUserVO` 9450–9456；web-domain `index.ts` 8–15 / 17–38；`index.test.ts` 15–25 / 27–46 / 48–56；`runtime.ts` 3–8 / 10–24；`chatSession.ts` 17–45 / 79–177；`chatSession.test.ts` 18–98 / 101–121 / 123–155 / 157–182 / 184–199 / 201–234 / 236–251；`AiChatPage.vue` 1–34；`pages.ts` 1；admin `services.ts` 3 / 90；`session.ts` 3；`adminManifestRegistry.ts` 59–76 / 308–334 / 343 / 358 / 365–376；registry 测试 109–126 / 182–261；home `services.ts` 1–16；`homeManifestRegistry.ts` 34–39；DML 42 / 69；`SnailAiController` 31–46；`application-dev.yml` 32–66；`application.yml` 284–285；e2e `ai-domain.spec.ts` 39–72 / 122–185；web-domain 包零 `@/application/services`；frontend 零 `createAiChatService`；sso-web / home 零这两座工厂
- difficulty: high

课的出餐单 + 一键哪些能在工作树直接复现，哪些只是测试名单比课表窄、或口试把「工厂注册了一键」说成「所有 `/snail-*` HTTP 都有按钮」？

**仍能复现（厨房格 `createAiService`）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂在根 `src/index.ts` 第 30 行，`Object.freeze`；`transport.ts` 只有 `projectAiUserTransport` | `index.ts` 30–31；`transport.ts` 6 |
| 封面 1 扁平方法；**没有**嵌套 `users.register` / `chat.send` | `index.ts` 20–22 / 31–42 |
| 内部这一枪自己写 `http.request`，无 `no-store`、无 `isEncrypt`、无 body | `index.ts` 33–37 |
| 请求快照 `{ url: '/snail-ai/user/register', method: 'post', headers: { repeatSubmit: false } }` | `index.test.ts` 28–30 |
| `method` 小写 `'post'` | `index.ts` 35；测试 29 |
| 有 `response.data` 才展开并把 `data` 换成投影；没有 data 把响应原样当 `AiApiResponse<SnailOpenApiUser>` | `index.ts` 38–40 |
| 投影：`openId ?? ''`；`externalId` / `nickname` / `created` 原样；**不拷** `avatarUrl` | `transport.ts` 7–12 |
| generated `OpenApiUserVO` **有** `avatarUrl`；投影函数键表没有它 | openapi 9450–9456 vs `transport.ts` 7–12 |
| 失败 `rejects.toBe(failure)`；不 log、不捏假用户 | `index.test.ts` 36–41 |
| 子路径再 export 同一工厂 + 资源标签 `controller: 'SnailAiController'`，`basePath: '/snail-ai'`；标签**不**发 HTTP | `snail-ai/index.ts` 2–4 |
| 名片 `id: 'ai'`，`backendModules: ['wta-ai']`，`capabilities: ['embedded-chat']` **不是** componentKey | `index.ts` 24–28；测试 7–11 |
| 包 `exports` 只有 `"."` / `./snail-ai` | `package.json` 6–8 |
| 包依赖有 `api-contracts`，登记 URL 仍是字符串字面量，不是 `keyof paths` | `package.json` 17；`index.ts` 34 |
| 厅堂 `aiService = createAiService(domainHttp)`，只吃 http | `services.ts` 3 / 90 |
| home / sso **零** 这座工厂，也零 `aiService` | home `services.ts` 1–16；sso-web grep 空 |
| frontend **零** `createAiChatService` / `createSnailChatService` | 全仓 grep 空 |
| Authorization / `clientid` **不是**厨房写的；E2E 锁登记带 authorization，`clientid` 等于 `e5cd7e4891bf95d1d19206ce24a7b32e` | e2e 3 / 67–71 / 140–145 |

**仍能复现（菜单切片 `createAiWebDomain`）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂先 `requireAiWebRuntime`，缺托盘 throw `'AiWebRuntime is required'` | `runtime.ts` 10–11；`index.ts` 18；测试 49 |
| 四口：`baseUrl` / `probeFrame` / `service.registerCurrentSnailUser` / `trustedCredential` 都必须是函数 | `runtime.ts` 12–23 |
| 缺 `baseUrl` / `probeFrame` 函数 throw 对应 `'… is required'`；测试锁了前三句 | 测试 48–55 |
| 缺 `service` 或 `registerCurrentSnailUser` 不是函数 throw `'AiWebRuntime.service is required'`；缺 `trustedCredential` 函数 throw `'AiWebRuntime.trustedCredential is required'` | `runtime.ts` 18–22 |
| id `web-domain-ai`，`domainId` `'ai'`；`messages` 冻 `chatTitle: 'Snail AI'` / `retry: '重新加载'` | `index.ts` 20–27；测试 18 |
| 一键测试 `toMatchObject`：`id: 'embedded-chat'`，`componentKey: 'ai/chat/index'`，`componentName: 'AiChatPage'` | `index.ts` 29–36；测试 19–21 |
| `permissions` 等于 `[]`（测试原文 intentionally empty） | `index.ts` 28；测试 22 |
| 选中时 `componentKeys()` 是 `['ai/chat/index']`；空清单是 `[]` | 测试 44–45 |
| `loadAiChatPage` 再核四口，动态 import Vue，外包 `h(page, { runtime })`；页面 props 只有 `runtime` | `index.ts` 8–15；`AiChatPage.vue` 8 |
| 包 `exports` 有 `"."` / `./pages` / `./snail-ai`；`pages.ts` 再 export 默认组件，**不**注入 runtime | `package.json` 6–9；`pages.ts` 1 |
| 包依赖 domain-ai + platform + vue + element-plus；**没有** axios | `package.json` 17–21 |
| 聊天页 **零** `@/application/services` | web-domain 包 grep 空 |
| 会话 `load()`：脏底座 / 缺票 **不扣**登记枪；登记失败换成「加载 AI 聊天失败，请稍后重试」；空 `openId` 换成「获取 AI 用户身份失败」 | `chatSession.ts` 84–110；测试 18–121 |
| 同域底座拒 `https://attacker.example/prod-api`、`//attacker.example/prod-api`、`/prod-api?target=other`、`javascript:alert(1)` | `chatSession.ts` 25–38；测试 58–81 |
| 探路成功才写 `frameUrl`；路径 `/prod-api/snail-chat/`，query 有 `openId` 与 `trustedCredential` | `chatSession.ts` 41–45 / 137–159；测试 123–155 |
| 探路失败 / 15s 超时：`frameUrl` 空，文案「AI 聊天连接中断，请重新加载」；挂住的 probe 被 abort 后允许立刻重试 | `chatSession.ts` 22 / 121–153；测试 157–234 |
| 过期登记 `stale-user` 盖不住 `current-user` | 测试 236–251 |
| `dispose()` 清三空并 abort 探路 | `chatSession.ts` 170–175；测试 153–154 |
| Vue 标题 / 按钮 / 空态写死在模板；iframe `allow="clipboard-read; clipboard-write"`；不读 manifest `messages` | `AiChatPage.vue` 22–32 |
| 厅堂 `adminAiWebRuntime`：`baseUrl` ← `VITE_APP_BASE_API`；`probeFrame` ← `fetch(..., { credentials: 'same-origin', method: 'GET', redirect: 'error', signal })`，必须 ok 且 Content-Type 是 `text/html` 或 `application/xhtml+xml`；`service` ← `aiService`；`trustedCredential` ← `getToken() ?? null` | `adminManifestRegistry.ts` 59–75；`session.ts` 3 |
| 探路 JSON / 503 throw `'AI chat probe failed'`；未读且未锁的 body 会 `cancel()` | registry 测试 182–242 |
| `selectedManifestIds` 含 `'web-domain-ai'`；`selectedDomainIds` 含 `'ai'` | 365–371 / 343 |
| registry 测试锁 `ai/chat/index` → `AiChatPage`，`ai/model/index` → undefined，`monitor/snailai/index` → `SnailAi` | 测试 109–126 |
| home 清单是 `web-domain-admin` + `web-domain-profile-self`，**不**选 ai | `homeManifestRegistry.ts` 34–39 |
| 一级 C 型「AI会话」`ai/chat/index`，`perms` 空串；「系统监控 / AI控制台」`monitor/snailai/index`，`perms=monitor:snailai:list` | DML 42 / 69 |
| 运维键编进 `admin-external-monitor`，`domainId: 'system'`，同一张 `views/monitor/external/index.vue`，`target: 'snail-ai'`，URL 来自 `VITE_APP_SNAILAI_ADMIN` | registry 308–334；`external/index.vue` 18 |
| E2E：`getInfo.permissions` 等于 `[]` 仍加载 iframe；探路 abort 时整页 HTML **不含** token 字面值；点「重新加载」会再登记、再探路 | e2e 39–46 / 122–146 / 148–185 |
| Java 方法名 `registerCurrentUser`，无 `@SaCheckPermission`，无 request body；条件注解 `snail-ai.open-api` | `SnailAiController` 31–46 |
| `snail-ai.enabled` 是 **false**；`open-api.enabled` 是 **true** | `application-dev.yml` 34 / 66 |
| `security.excludes` 含 `/snail-chat/**` 与 `/api/snail/chat/**`；厨房方法表没有这两条 | `application.yml` 284–285 vs `index.ts` 20–22 |
| 对照：notify/third 工厂 **无** `require*WebRuntime`；demo/workflow 只核对象在不在；档案楼核十一口 | notify/third `index.ts` 直接 freeze；demo `runtime.ts` 10–12；workflow `runtime.ts` 44–46；profile `runtime.ts` 64–79 |

**课写过、测试名单更窄或口试数字写偏（不是证伪本两格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「测试锁了前三句（required / baseUrl / probeFrame）。没有断言 service / trustedCredential 那两句」 | `index.test.ts` 48–55 确实只锁三句。源码 18–22 仍有后两句 |
| 「`transport.test.ts` 用的夹具没有 `avatarUrl`，所以投影前后相等过了」 | `transport.test.ts` 6–7 只有 `openId` / `nickname` / `created`。丢掉头像以函数键表为准 |
| 「缺 data 分支按源码口试」 | `index.ts` 38–40 有。厨房测试只锁有 data 的 happy path 与 reject |
| 「测试锁了 stale-user 不能盖住 current-user」 | `chatSession.test.ts` 236–251 有。`trustedCredential` 抛错分支源码 94–102 有，会话测试只锁 `null` |
| 「registry 测试锁流程键」 | 锁了 `ai/chat/index` / `ai/model/index` / `monitor/snailai/index` 三行，外加 probe HTML/JSON/503 |
| 口试表把动词写成 POST | 厨房 `method: 'post'` 小写 |
| README / AGENTS 写「流式交互、下载和导航通过宿主端口取得」 | `runtime.ts` 3–8 只有四口。以工作树为准 |
| 课 `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |

问：在 1 枪 URL/动词/无 body/有 data 才投影/丢掉 `avatarUrl`/失败原样抛、一键 `ai/chat/index`、空 permissions、缺 runtime 源码与单测都关门、四口源码关门、页面不 import 厅堂单例、脏底座不登记、探路成功才有 `frameUrl`、失败 HTML 不含 token、home 不选、控制台不是本厂 已经能互证的前提下，(a) 是否还要把「后两口 throw 无单测」「transport 测试不锁丢掉的 `avatarUrl`」「缺 data 分支无测」写成开篇半句，才算本两格讲完？还是这些已经落在课的表/反例里，Lead 可以把 **厨房独行** 视为可 covered、把 **菜单当切片已讲** 并保持九工厂行 uncovered？

## Q3 假设与反例

- objective_id: OBJ-81
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无 `createAiChatService`；工厂不在 `transport.ts`；子路径不是小厨房；聊天页不 import 厅堂单例；`createAiWebDomain(undefined)` 源码与测试都 throw；notify/third 不 throw；demo/workflow `service: {}` 仍能冻键、本厂不能；无 Vue 喊 `/snail-ai/user/register`；iframe 不是厨房第二枪；`monitor/snailai/index` 不是本厂第二键；`ai/model/index` undefined；投影丢掉 `avatarUrl`；权限中性不是匿名；home 无此工厂；九工厂行不因本课整行闭合；Java 名不是厨房名；README 流式口不在 runtime
- difficulty: high

课的隐含假设若反转，合格读者会把哪座工厂或哪只抽屉认错？

1. **「这张 Vue `import { aiService }`。」** 包内搜不到 `@/application/services`。直接消费单例的是厅堂 registry。课反例 1 / 误区 6 覆盖。
2. **「`createAiWebDomain` 等于九个 web-domain 工厂都 covered。」** 矩阵 (a) 同行绑死。GP-L-020 / GP-L-044 / GP-L-054 / GP-L-066 / GP-L-073 / GP-L-077 已挖过。课反例 2 / 误区 3 覆盖。
3. **「工厂在 `transport.ts`。」** 打开 `index.ts` 第 30 行。课反例 3 / 误区 5 覆盖。
4. **「厨房方法叫 `registerCurrentUser` / 厅堂导出 `createAiChatService`。」** Java 名是 `registerCurrentUser`。厨房是 `registerCurrentSnailUser`。导出名是 `aiService`。课反例 4 / 误区 4 / 7 覆盖。
5. **「`service.users.register` / `service.chat.send`。」** 扁平一枪，没有抽屉，没有 send。课反例 5 / 误区 8 覆盖。
6. **「从 `@namewta/domain-ai/snail-ai` 进口小厨房。」** 子路径再 export 同一工厂，外加资源标签。课反例 6 覆盖。
7. **「页面手写 `/snail-ai/user/register`。」** 登记走 `runtime.service.registerCurrentSnailUser`。`/snail-chat/` 是会话拼的。课反例 7 覆盖。
8. **「iframe `/snail-chat/` 是厨房第二枪。」** freeze 对象上搜不到 `snail-chat`。探路是宿主 `fetch`。课反例 8 / 误区 9 覆盖。
9. **「`ai/chat/index` 和 `monitor/snailai/index` 是同一厂的两键。」** 前者 `web-domain-ai` / `domainId: 'ai'`。后者 `admin-external-monitor` / `domainId: 'system'`。课反例 9 / 误区 10 覆盖。
10. **「`ai/model/index` 已经注册。」** registry 锁成 undefined。课反例 / 误区 11 覆盖。
11. **「投影会留下 `avatarUrl`。」** 丢掉。课反例 15 / 误区 12 覆盖。
12. **「README 写了流式端口所以 runtime 有 download/navigate。」** 四口以 `runtime.ts` 为准。课反例 13 / 误区 13 覆盖。
13. **「`createAiWebDomain({ service: {} })` 能冻出键，缺方法等点击才爆。」** 本厂在工厂门就要求 `registerCurrentSnailUser` 是函数。那是 demo / workflow 的故事。课反例 14 / 误区 14 / 15 覆盖。
14. **「`createAiWebDomain(undefined)` 像 notify 那样仍能冻出键。」** 本厂会 throw。notify/third 2026-09-17 不会。课正例 3 / 误区 14 覆盖。
15. **「权限中性等于匿名可进。」** 没票停在「登录凭证不存在」；登记仍要 Bearer。课反例 16 / 误区 16 覆盖。
16. **「home 也能打开 AI 会话。」** 工作树没有这份工厂接线。课反例 11 / 误区 17 覆盖。
17. **「OBJ-81 包含 `SnailAiController`。」** 那是 OBJ-80。课反例 10 / 误区 2 覆盖。
18. **「`requireAiWebRuntime` 只检查对象在不在。」** 核四口。课边界 1 / 误区 14 覆盖。
19. **「capabilities 决定挂哪几页。」** 决定挂页的是 manifest + 菜单种子 + `selectedManifestIds`。课误区 18 覆盖。
20. **「厨房自己塞了 Authorization。」** 厅堂拦截器塞。课误区 20 覆盖。
21. **「登记 body 带 nickname。」** 不带。后端 LoginHelper。课误区 21 覆盖。
22. **「`snail-ai.enabled: false` 所以这页是死的。」** 浏览器打的窗看 `snail-ai.open-api.enabled`。课边界 8 / 误区 22 覆盖。
23. **「`messages` 已做 i18n。」** 模板写死。课边界 6 / 误区 23 覆盖。
24. **「工厂注册了一键 = 所有 `/snail-*` HTTP 都有按钮。」** 聊天文档走会话；控制台走监控厂；模型页不存在。课图 2 文字等价物覆盖。
25. **「把 `createAiChatSession` 标进矩阵 (a)。」** 机制，不是 claimed cell。课边界 10 / 误区 24 覆盖。

问：读完后仍说不出的，是不是「两个工厂的名字」，而是 **厨房方法全集 ≠ 墙上已扣的扳机 ≠ 后端窗全集、九工厂行不能因本课整行闭合、本厂 fail-closed 核四口（比 demo/workflow 更严）、iframe 不是第二枪、控制台是监控厂、home 根本不选、工厂名 ≠ 导出名 ≠ Java 名、权限空数组仍要登录票**？

## Q4 视角

- objective_id: OBJ-81
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-81 vs L-007/L-008/L-020/L-024/L-044/L-054/L-066/L-073/L-077/L-080；厨房独行 vs 九工厂同行；claimed_cells 两工厂名 vs 一张 Vue / 会话状态机；chain 估时 35 vs 课 38
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这两格 (a)**？

| 视角 | 多出来的边 | 是否本课这两格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 厨房独行 1:1 可盖章；九工厂同行 vs chain 菜单工厂名；无 Vue 行、无会话工厂行；(b) 无本课符号、Java 窗挂 L-080；(d) 无浏览器 AI 会话流 | **是** 闭合定义。厨房独行可视为已讲并可 covered。九工厂行保持 uncovered。不要发明 (b)/(d)，不要把 `AiChatPage` / `createAiChatSession` 升成新行才能给切片盖章 |
| 厨房作者 | 1 枪 URL/动词；无 body；`repeatSubmit: false`；有 data 才投影；丢掉 `avatarUrl`；失败原样抛 | **是** `A:createAiService` |
| 管理端菜单工厂 | 一键、空 permissions、先核四口再冻、`h(page, { runtime })`、未选无键 | **是** 切片里的 `createAiWebDomain` |
| 会话 / 一页消费 | 先底座再票再登记再探路；15s；过期 attempt；票不进失败 HTML | **是** OBJ 对照走查，不是矩阵第三格 |
| 第三条河 / 鬼窗 | `/snail-chat/` 走宿主 fetch + iframe；控制台走监控厂；`ai/model/index` undefined；excludes 不是厨房第二枪 | **是** 负空间对照。不要升成第二张厨房章 |
| 厅堂电工（L-007） | `createAiService(domainHttp)`；导出名 `aiService`；registry 注入 runtime | 插头一句 **是**。12 个 `create*` 全表 **否**（已 covered） |
| 导航 host（L-020） | admin 选 `web-domain-ai`；home 不选；`ai/model/index` 失败关闭 | 选择集一句 **是**。其余八厂 **否** 整行 |
| 监控室（L-024） | `monitor/snailai/index` / `VITE_APP_SNAILAI_ADMIN` / `externalIntent` | 两扇都叫 AI、钥匙不是一把 **是** 对照。四块屏 **否**（已 covered） |
| 档案楼（L-044） | `requireProfileWebRuntime` 核十一口 | fail-closed 对照 **是**。企业三页 **否** |
| 通知/三方楼（L-054 / L-066） | 不校验托盘；notify 四张 Vue；third 一张页四个 kind | 对照「本厂会关门 / 一键一张 Vue」**是**。通知四页 / 三方四键 **否** |
| 流程/演示楼（L-073 / L-077） | 也会关门；只核对象在不在；`service: {}` 仍能冻键；设计器 iframe 来自宿主 `designUrl` | 对照四口更严、本课 iframe 由会话拼 URL 还多一步 HTML 探路 **是**。57 枪 / 15 枪 **否** |
| 登记窗（L-080） | `registerCurrentUser`；LoginHelper；OpenApiUserClient；`status==1`；无 `@SaCheckPermission` | URL 对齐、无 body、Java 名对照 **是** 边界。后窗盖章 **否** |
| 厂商引擎 | snail-ai-server `context-path: /snail-ai`；gRPC；控制台前缀 | iframe 前缀是宿主 baseUrl、路径是 `/snail-chat/` **是** 对照。引擎内部 skip **否**（vendor-engine） |
| MySQL 8.4 | 菜单两 component；AI会话空 perms；AI控制台有 `monitor:snailai:list` | 种子键对照 **是** (a)。容器 **否** |

问：Lead 若凭「两个工厂名在 claimed 列里」把厨房独行 **和** 九工厂行一起标 covered，是否还要求读者能指着 **1 枪扁平、无 body、有 data 才投影、丢掉 `avatarUrl`、一键、四口 fail-closed、探路成功才贴玻璃、控制台不是本厂、home 不选、工厂名 ≠ 导出名 ≠ Java 名**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要等 L-080 才给 **厨房独行** 当已讲？九工厂行是否例外——必须保持 uncovered，直到其余工厂也有课？Lead 若把 `createAiChatSession` 当成第三格，是否发明了矩阵行？

## Q5 元问题

- objective_id: OBJ-81
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课；一课超 1 个容器是 Lead 裁决不是 b01 填表）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 厨房独行仍 uncovered、九工厂同行仍 uncovered、无 Vue 行、无本课 (b)/(d)
- difficulty: low

建议：`defer`

原因：

- **厨房独行与 OBJ-81 / chain / 课第一格 1:1。** L 合同章节齐全（宏观一键地图、直觉十四句、定义表、机制链 1–6、带 caption / 文字等价物的两张图、正反例、变式、误区、暂停走查、来源表 S-005/S-006/S-008/S-010/S-L007-01/S-L024-01/S-L081-01…08）。能复现：冻住 1 枪 POST `/snail-ai/user/register`、无 body、`repeatSubmit: false`、有 data 才投影并丢掉 `avatarUrl`、失败原样抛、厅堂只 admin-web 接线、home / sso 不雇。不必为「工厂的名字」再挖五问。Lead 可将 `createAiService` 标 `covered`。
- **菜单是切片，不是九工厂整行。** 一键、`requireAiWebRuntime` 四口、`h(page, { runtime })`、页面实际扣的枪、探路成功才贴 `/snail-chat/`、失败 HTML 不含 token、控制台非第二键、`ai/model/index` undefined——已在课的表/反例里。GP-L-020 / GP-L-044 / GP-L-054 / GP-L-066 / GP-L-073 / GP-L-077 已立过同一条：本切片证据可写进九工厂行备注（「ai 管理端一键 + 四口 fail-closed + 会话探路已讲」），**不要**把该行标 covered。
- 本批可粘贴的精度项不挡住厨房盖章、也不挡住菜单切片视为已讲：（1）service / trustedCredential 两句 throw 无单测；（2）`transport.test.ts` 不锁丢掉的 `avatarUrl` / 缺 `openId`；（3）厨房缺 data 分支无测；（4）会话测试不锁 `trustedCredential` 抛错；（5）web-domain 测试不锁 `messages` 两句；（6）动词小写；（7）README / AGENTS 流式口过期；（8）chain 估时 35、课 38。不要把 Java 窗、九工厂其余八个名字、监控四屏、厂商引擎内部塞进本格。
- 不建议 `mine-more`：b01 未证明本两格还有真实矩阵空洞；再五问只会把 L-080 LoginHelper / OpenApiUserClient、L-024 运维玻璃、L-007 十二厂插头或九工厂其余名字再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；测试名单窄是半句精度，不是第二容器。不建议 `split-lesson`：未满 10 问；厨房+菜单是 chain 原设计的同一 OBJ-81（对照 third 拆成 L-065/L-066 是另一条链，本课不是两个 C4 讲不完）；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：`repeatSubmit` / 探路 / 15 秒 / 权限空数组已是 (a) 的解释到副作用。不建议把 `AiChatPage` 或 `createAiChatSession` 升成矩阵行才给切片盖章。不建议等 GP-L-080 才给厨房独行当已讲。不建议用本课改 (d) 登录走廊。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:createAiService | 1 枪 URL/动词、扁平 freeze、无 body、有 data 才投影、丢掉 `avatarUrl`、失败原样抛、home 不雇——均与源码对齐。残留精度：缺 data 分支无测；投影丢掉 `avatarUrl` 的反例不在 `transport.test.ts`；动词小写。 | 可选半句：口试方法表以 `index.ts` 为准，丢掉的字段以投影函数键为准。不要求重写机制链。Lead 可将矩阵该行标 `covered`（证据锚本 probe 与 L-081 来源表 S-L081-01 / S-L081-02）。 | defer（本行可 covered；精度项不挡盖章） |
| A:createAiWebDomain | 课按磁盘讲了一键、四口 fail-closed、页面实际扣的 `runtime.service.registerCurrentSnailUser`、宿主探路、iframe `/snail-chat/`、控制台非第二键、home 不选。**矩阵无此独行**；符号落在九工厂同行。残留精度：后两口 throw 无单测；`messages` 未 `toEqual`；README 流式口过期。 | 可选半句：口试键表以工厂 `registrations` 为准，四口以 `runtime.ts` 为准。不要求重写机制链。Lead **不要**改九工厂行为 covered。可将本切片证据写入该行备注。 | defer（切片已讲；九工厂行不可因本课整行闭合） |
| 九工厂行 `create*WebDomain`（含 `createAiWebDomain`） | 本课只用选择集 + 一键当切片证据。其余八个工厂未因本课讲完。 | 保持 uncovered。L-020 已覆盖 `createSystemWebDomain` 切片；L-039/L-044 已覆盖 profile 两份菜单；L-054 已覆盖 notify 四页；L-066 已覆盖 third 四键；L-073 已覆盖 workflow 十二键；L-077 已覆盖 demo 三键。 | defer（九工厂行保持 uncovered） |
| A:admin-web.services / C:admin-web-composition | 插头 `createAiService(domainHttp)` 与 registry 接线已借用。 | 保持。L-007（已 covered）。 | defer（L-007 已 covered） |
| A:home-web.services | 负空间：无工厂、无单例。 | 保持。L-008（已 covered）。 | defer（L-008 已 covered） |
| A:createSystemWebDomain / B:navigation host | 选择集 + 未选无键 + `ai/model/index` 失败关闭已点。 | 保持。L-020。 | defer（L-020） |
| A:createMonitorService / monitor/snailai | 运维玻璃不是本厂第二键已作对照。该厨房行已 covered。 | 不要用本课再盖四块屏。 | defer（L-024 已 covered） |
| A:SnailAiController.registerCurrentUser | 厨房对准同一条 POST；Java 名 / 无 Sa 权限字 / 无 body / open-api 电闸已作对照。该行仍 uncovered。 | 不要用本课盖 LoginHelper / OpenApiUserClient / `status==1`。 | defer（L-080） |
| createAiChatSession / AiChatPage.vue | 课已走查会话梯子与一页消费。矩阵无此符号。 | 不要发明第三格。口试仍要会走，因为它是这一键怎么喊厨房的唯一路径。 | defer（未入库；保持机制不是格子） |
| (b) `repeatSubmit` / 探路 / 15 秒 / 权限空数组 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 浏览器 AI 会话流 | 矩阵无此行。已有的走廊仍是登录后查询与 OSS 直传。 | 不要发明前端 AI 菜单数据流行，也不要用本课改 L-006 / L-027。 | defer（未入库） |
| WarmFlow/SnailJob/SnailAI 厂商引擎内部 | iframe 第三条河只认谁拼 URL、探路认 HTML。 | 保持 `deferred(vendor-engine)`。 | defer（vendor-engine） |
| wta-extend / snailai-server | 独立进程 `context-path: /snail-ai` 不是本课 iframe 前缀。 | 保持 `deferred(separate-process)`。 | defer（separate-process） |
| MySQL 8.4 / Redis 8 | 菜单种子是 (a) 对照，不是容器闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；厨房独行无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`；菜单切片在九工厂同行上已讲完并显式禁止整行闭合；合格读者说得出两个工厂目的（出餐单印一枪登记 URL；墙上这一键把四口托盘喂进页面）、主路径（会话先核同域底座和票，再喊 `registerCurrentSnailUser` 换 `openId`，再让宿主 `probeFrame` 确认 `/snail-chat/` 是 HTML，成功才把带票 URL 写进 iframe）与失败路径（缺 runtime 当场 throw、脏底座/缺票不扣登记枪、登记失败换成稳定中文、探路失败票不进 DOM、home 不选这份 id）；把可选半句写进原课不会超过 40 分钟；Java 窗、厅堂插头、导航 host、监控室、九工厂其余名字已有邻课。满 10 问前不拆。厨房与菜单虽跨 `domains/ai` 与 `web-domains/ai` 两个包，仍是 chain 原设计的同一 OBJ-81，不是「超过 1 个容器却讲不完」——对照 third 的拆法是另一条链已经做成 L-065/L-066，本课不要在 b01 再拆。不要把 `createAiChatSession` 拆成新课。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / split 提案：否（miner 不改矩阵）；split 提案为 none；缺口表对九工厂整行、L-080 Java 窗、L-007/L-008 插头（已 covered）、L-020 导航、L-024 监控（已 covered）、未入库 (b)/(d)、会话工厂、厂商引擎、独立 snailai-server 以及 MySQL/Redis 容器显式建议 `defer`；对矩阵 (a) 行 `createAiService` 建议 Lead 标 `covered`（证据锚本 probe 与 L-081 来源表 S-L081-01…02）；对 `A:createAiWebDomain` 建议视为已讲切片、**不要**把九工厂同行标 `covered`。本课不主张发明 Vue 矩阵行、用本课盖 `SnailAiController`、把 `monitor/snailai/index` 升成本厂第二键、把 `createAiChatSession` 升成第三格、或等 GP-L-080 才给厨房独行盖章。
