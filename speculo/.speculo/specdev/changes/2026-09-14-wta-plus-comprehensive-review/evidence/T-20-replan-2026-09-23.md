# T-20 evidence

Current HEAD `04aba028` already meets AC-020. No new implementation was required. This cancels only the repeated construction. AC-020 stays owned by T-30. The adjacent OIDC change was not modified. E2E is not-required; the business journeys stay with their original tickets.

## Strict scope

Twelve package configs explicitly set `noImplicitAny`, `strictNullChecks`, and `strictFunctionTypes` to true:

- `frontend/packages/platform/http`
- `frontend/packages/platform/auth`
- `frontend/packages/platform/app-runtime`
- `frontend/packages/platform/permission`
- `frontend/packages/adapters/axios-browser`
- `frontend/packages/adapters/oss-upload-browser`
- `frontend/packages/domains/admin`
- `frontend/packages/domains/system`
- `frontend/packages/domains/profile`
- `frontend/packages/domains/third`
- `frontend/packages/web-domains/profile`
- `frontend/apps/home-web`

Those twelve sources contain no `@ts-ignore`, `@ts-expect-error`, or `as unknown as`. `frontend/packages/domains/system/src/transport.test.ts` rejects malformed credential transport with `用户凭据响应不可用` and keeps nullable optional user fields. `frontend/packages/domains/system/src/user/public.test.ts` and `menu/public.test.ts` accept nullable collections and empty pages. This is not a whole-repository strict conversion.

## Remaining strict debt

`frontend/tsconfig.json` sets `"strict": true` and then turns off `noImplicitAny`, `strictFunctionTypes`, and `strictNullChecks`. These configs extend that root and do not turn the three flags back on: `apps/admin-web`, `apps/sso-web`, `packages/adapters/storage-browser`, `packages/api-contracts`, `packages/domains/demo`, `packages/domains/notify`, `packages/domains/workflow`, `packages/platform/contracts`, `packages/platform/validation`, `packages/web-domains/admin`, `packages/web-domains/demo`, `packages/web-domains/notify`, `packages/web-domains/system`, `packages/web-domains/third`, `packages/web-domains/workflow`, `packages/web-kit/file-upload`, `packages/web-kit/permission`, and `packages/web-kit/rich-text`. `packages/domains/demo/src/index.ts` still has one `as unknown as` outside the strict set. None of these were silenced by a new ignore or exclusion in this pass.

## Gates

Commands from `frontend`. The shell `pnpm` resolved to `pnpm@10.34.5`. Each exit 0.

```bash
pnpm architecture:check
pnpm lint
pnpm typecheck
pnpm test
pnpm build:prod
```

Architecture check: 33 workspace packages, 0 reviewed baseline violations. Lint: 0 warnings and 0 errors. Typecheck: `vue-tsc --noEmit` completed, including `admin-web`, `home-web`, and `sso-web`. `pnpm test`: 103 files, 663 tests, 0 skipped. The only skip text is `skipped 0`. `build:prod` built admin, home, and sso.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase, operation `verify-affected-contract-and-quality-gates`, status passed. No production source edit.
