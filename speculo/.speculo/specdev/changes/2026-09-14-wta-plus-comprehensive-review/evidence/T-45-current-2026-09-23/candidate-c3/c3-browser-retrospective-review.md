# T45 C3 real-browser failure — read-only fixture retrospective

Fixed product `d98936b6881ad7c8093476a65fe9736203c60af7`; reviewed the original private UI spec `/tmp/wta-t45/ui/oss-diagnostic-real.spec.ts` (SHA256 `2abe0d9cd65c47fb1bd2d6b069910334283564e44aca7331cdd0b86ce262f0af`) and runner v2 (SHA256 `f81527dd19042cd6844f95427f2879f1072480a0566bfb3d46bd7e32f437ae10`). No repository, service or test was changed or run by this reviewer. The frozen C3 static implementation review remains unchanged.

The retained run `/tmp/wta-t45/ui-runs/c231806fb841027a/result.json` has `accepted=false`, browser counts **1 expected / 1 unexpected / 0 skipped / 0 flaky**, browser exit 1, fixed reason `Chrome exact two-case counts differ`, source clean at C3, and `cleanup_errors=[]`. Its safe `browser_authorization` summary records ordinary 403 and anonymous 401. The runner deletes raw Playwright output during cleanup and does not retain a safe per-case error location, so the first failing assertion line **cannot be established from this run**.

There is nevertheless a deterministic fixture/source mismatch if the privileged case reaches its object-observation assertion: original spec `:44,66,87–88` requires an exact observation cell of `允许 / 可读` for PUBLIC_READ or `拒绝` for PRIVATE, while fixed `OssConfigPage.vue:300–302` renders `ALLOWED` as `观察到允许` and `DENIED` as `观察到拒绝`. The test's exact-cell selector cannot match those UI strings. This is a private test expectation error; changing the product's bounded-fact wording is unnecessary. It does **not** prove that the failed run first stopped at line 66, since its raw location was not retained.

Other key selectors were checked only against fixed source, not declared browser-passed: page heading `OSS 配置` is an `<h3>` at `OssConfigPage.vue:43`; `配置key` is a table cell at `:84`; the row's `只读诊断` button has that aria label at `:120–128`; the dialog title is `OSS 访问诊断` at `:226`; the caveat contains `单对象读取不证明全桶安全` at `:227`; subject labels, `未知`, and per-fact rows correspond to `:232–246,295–317`. The spec's POST path and six-fact shape match the A2 real HTTP response contract. The retained ordinary/anonymous authorization codes independently support only that negative case, not the positive UI assertions. Row presence, dialog accessibility, timing, close behavior, and each positive assertion still require a fresh Chrome run.

For the private v3 recovery, keep all existing HTTP/permission/unknown/caveat/close checks and exact two-case, one-attempt, zero-skip/fail gate. Correct only the two object-observation expected labels to the fixed UI mapping. Before starting owned services, compare the spec's expected observation strings and key selectors with the fixed product source and reject stale literals; pin and record spec/config/runner hashes. On reporter failure, safely retain only allowlisted case title, project, status/attempt count, and an integer file line/column **if** provided in a structured error location belonging to the private spec. Do not copy raw error message, stack, DOM snapshot, URL, response body, token, screenshot or trace into the public result. Record `location_unavailable` when the structured location is absent. Perform a synthetic offline parser check with a secret-like error message to prove the safe summary omits it, while continuing to delete raw reporter/log artifacts in `finally`. Preserve the original v2 result and its failure verdict verbatim; a later v3 pass cannot retroactively validate C3's failed run.
