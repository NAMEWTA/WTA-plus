<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.namewta.notify.mapper.NotifyNoticeMapper">
    <select id="selectByIdForUpdate" resultType="org.namewta.notify.domain.entity.NotifyNotice">
        select * from notify_notice where notice_id = #{noticeId} for update
    </select>
</mapper>
