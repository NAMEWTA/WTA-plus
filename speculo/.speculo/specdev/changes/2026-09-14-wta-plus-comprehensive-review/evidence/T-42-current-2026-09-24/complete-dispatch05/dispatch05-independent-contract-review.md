# T-42 Dispatch05 fixed-source independent review

**Final independent implementation-contract verdict: PASS at fixed d95b464e, subject to Lead’s final manifest and governance disposition.** Exact-source Mail27, Notify135, HTTP, core and static results are recorded in the later sections below.

The next paragraph is the **historical initial static checkpoint**, written before those gates completed; its pending status is not the final verdict.

**Static verdict: no new product-source blocker found in this delta; T-42 acceptance remains pending fresh exact-source gates.** Fixed input is `cf356a65..d95b464e46ec73d7a913809f4c84332a7f2eeffb`; `git diff --check` exited 0. This review reads the committed 13 raw paths (11 rename-aware paths), not the mutable writer worktree. I did not edit the repository or run builds/services. Historical layered failure, candidate failures and successful old-input runs remain unchanged.

The old checker found eight actual violations. The new `NotifyAttachmentActorPort` lives under `port/` with the same `Actor(userId,clientPk)` contract. Its session adapter and runtime service import the moved Port, and the red test uses that same type. Both adapters no longer import DAO or runtime Service. `NotifyAttachmentSnapshotAdapter` obtains ordered relations and uses `reserve`/`confirm`/`uncertain` through the new `NotifyAttachmentSnapshotPort`; `NotifyAttachmentReleaseJob` gets candidates and invokes `releaseIfSafe` through that Port. No direct adapter→concrete UseCase import or hidden fully qualified DAO access was introduced. The owner manifest registers the renamed **actual** `NotifyAttachmentSnapshotService` caller for the snapshot OSS carrier and keeps the source carrier, lifecycle descriptions and no-allowlist rule. A fixed-source grep found no remaining import/reference to the removed `Transactions` or old actor Port package.

The four original short operations now enter `NotifyAttachmentSnapshotUseCase` as distinct public `@DSTransactional` methods (`reserve`, `confirm`, `uncertain`, `releaseIfSafe`), each delegating to a transaction-annotation-free `NotifyAttachmentSnapshotService`. The UseCase implements the injected Port, so each adapter call crosses the Spring proxy rather than a `this` self-call. The full-context Mail test now injects the Port and asserts it is an AOP proxy; this is a wiring check, while its real DB rollback/ACK cases must still be executed on this fixed source to prove the new proxy actually commits/rolls back at the intended boundaries. The two read methods delegate Port→UseCase→Service→DAO, and release candidates remain a bounded 100-row cursor scan; final eligibility still rechecks under the existing Intent→Outbox→Delivery→relation locks in `releaseIfSafe`.

The `Prepared` type moved to the Port but still carries the same relation entity and the **same** `OssService.NotificationCopyReservation` object. In the adapter, `copyNotificationSnapshot(prepared.reservation())` remains between `reserve` and `confirm` calls, outside both transactions. `confirm` receives the same `Prepared`, copied byte count and SHA-256 and reconstructs exactly `new OssService.NotificationCopyResult(copiedSize,copiedSha256)` before calling the same System confirmation. The Service retains token equality, relation `COPYING`, target OSS ID equality, owner lock, System target confirmation and one-row relation READY update. `uncertain` still uses the same token CAS and preserves owner/ref on unknown outcome. Materialization remains a deferred adapter resource callback outside the DB transaction. No S3 GET/PUT was moved into Service/UseCase; the Service's System reserve/confirm/release calls are the existing short DB coordination APIs, whose actual behavior is covered by the pending fresh real Mail run.

The 73c working-tree layered command `/tmp/wta-t42/dispatch05-layered-working.{json,log}` exited 0 and reports 113 Notify Java files, versus the preceding eight errors. Because that command ran **before** the d95 commit, it is supportive static evidence, not an immutable exact-commit acceptance result. Lead is running fixed-source gates now. Required remaining checks are the unchanged layered validator at clean d95; the targeted owner/fixture tests; real Mail27 with proxy/rollback/ACK, partial copy, late sender and release assertions; Notify135 worker regression; default/full/core; rebuilt full-JAR HTTP/string ID/permission and live schema; final source/cleanup, scope and governance evidence. Earlier c980 HTTP/JAR and 73c frontend results cannot by themselves validate a changed backend transaction/DI graph. Frontend generated-contract bytes are unchanged by Dispatch05 and may be reused only with an explicit final input-equivalence proof.

## Later exact-source Mail27 and backend evidence at d95

I independently parsed `/tmp/wta-t42/runs/57cd7d5353524ca5/result.json` and its retained sanitized Surefire XML. The XML SHA-256 matches the result's sanitized digest; it contains **27 unique methods matching the expected manifest, 27 tests/0 failures/0 errors/0 skips**. The run records `acceptance=true`, Maven exit 0, fresh 104-table six-SQL owned MySQL/Redis/MinIO, source before=after clean `d95b464e`/tree `39f18706226fed11ed19b5cdc1f822d60492350c`, private-property marker true, and cleanup errors `[]` with process/container/volumes/ports gone. The retained test-source SHA matches `git show d95b464e:.../NotifyMailAttachmentIntegrationTest.java`; the JDBC fault helpers are byte-unchanged from the previously reviewed 43e5 version.

The test's `@BeforeAll prepareOwnedApplication` asserts `AopUtils.isAopProxy(snapshotTransactions)` on the injected `NotifyAttachmentSnapshotPort`; all 27 methods ran, so this proxy assertion executed successfully. The XML explicitly includes both submit BEFORE/AFTER and both final MAIL send-reservation BEFORE/AFTER methods. Their unchanged source requires `plan.hitExactlyOnce()` and DB facts/zero sender/owner refs. The strengthened helper's BEFORE condition requires **both** successful owned-session disconnect acknowledgement and entry into real driver commit; AFTER requires actual commit. The MAIL BEFORE case verifies that false-copy updates and READY confirmation committed but final true reservation rolled back, `send_reserved=false`, zero sender/Attempt and two owned references. MAIL AFTER requires `send_reserved=true`, zero sender, cancel/expired-lease reclaim under a new token and `releaseIfSafe=false`. Thus the fresh result exercises the moved Spring proxy boundary and the non-forged physical commit fault markers, not merely a count of 27. The controlled physical sender remains a substitute for actual SMTP supplier delivery.

At the same fixed d95 source, `/tmp/wta-t42/dispatch05-targeted-source-and-counts.json` has four relevant suites/10 tests with 0 fail/error/skip; `/tmp/wta-t42/dispatch05-backend-default.json` exits 0 and its fresh counts file reports 265 suites/1,184 tests, 0 fail/error, 244 environment skips (940 executed). Full clean package and full bundle command records both exit 0. These results narrow the pending list; they do **not** stand in for the running Notify135, new full-JAR HTTP, core, exact clean static or final source/cleanup gates. Previous accepted 73c evidence remains historical and is not relabeled as having run on d95.

## Later exact-source Notify135, full-JAR HTTP and frontend-input evidence at d95

I independently read `/tmp/wta-t42/regression-runs/74c5b32f424f893f/result.json` and parsed all eight retained Surefire XML files. Their SHA-256 values match the sanitized digests in the result; after normalizing JUnit parameter suffixes, the per-method execution counts match the run's method manifest exactly. Total: **8 fresh suites, 135 tests, 0 failures, 0 errors, 0 skips**. Maven exited 0 and `acceptance=true`. The run used its owned fresh 104-table six-SQL MySQL and Redis; source before/after are the same clean d95 commit/tree, and the owned process group, containers, anonymous volume and two ports were removed with `cleanup.errors=[]`. This is a fresh d95 Notify regression, distinct from the earlier Mail27 run and the default reactor's 244 environment skips.

I independently read `/tmp/wta-t42/openapi-http-live/65515ae9c04c0b30/result.json`. The real full-JAR run reports `acceptance=true`, exit 0, clean d95/tree before and after, and the same JAR SHA-256 `13616132d12c5b14b63612d9e153d5d309a107c6841fccec21780f0d99602c56` before and after. It used owned 104-table MySQL/Redis/MinIO and cleaned its backend process group, three containers, two volumes and five loopback ports without errors. Live `/v3/api-docs` returned HTTP 200 with 438 paths/450 schemas and zero missing paths, operations or schemas; `attachmentOssIds` remains optional with string items, `priority` remains required and both existing SSE routes are present. A real uploaded >2^53 OSS ID sent as a quoted decimal string produced the exact source relation. Omitted and empty attachments produced zero relations; invalid decimal strings, missing source, no token and an ordinary user without permission each left zero DB/S3 writes with the expected business rejection. The extra numeric JSON token was accepted and tied to the exact source once; this is schema-outside binding behavior, not a proven numeric-rejection contract. The run had no enabled external account or due outbox, so its HTTP checks do not claim physical SMTP delivery; that is covered by the separate controlled Mail27 integration scenarios and their stated sender limitation.

`/tmp/wta-t42/dispatch05-openapi-parity.json` compares that exact live d95 capture to the already formal generated contract: paths, components and all content except ephemeral loopback `servers` are equal. Thus the c980 formal generated files can remain unchanged; I do not relabel them as newly generated at d95. The separate `/tmp/wta-t42/dispatch05-frontend-reuse-review.md` verifies that `frontend`, `.agents`, `scripts` and `release-artifacts` trees at d95 are byte-identical to the 73c28 frontend-gate input. Its 663 Vitest + 108 Node = 771 passing tests, three-app 329 build files and associated architecture/lint/typecheck/OpenAPI/build exits of 0 are **73c28 results reused by exact input equivalence**, not d95 reruns. This equality alone does not prove the backend refactor; the fresh d95 Mail27, Notify135 and full-JAR HTTP runs above address its changed runtime behavior.

**Current acceptance boundary:** the product-source review finds no remaining blocker in the d95 delta, and the fresh Mail27, Notify135, default/full-package and full-JAR HTTP checks above pass. The exact d95 core and static gates are still pending as of this addendum, as is Lead's final scope/governance disposition. Do not mark the whole T-42 ticket complete on this intermediate report.

## Exact-source core/static completion and final independent contract disposition

I read `/tmp/wta-t42/core05-core-package.json`, `core05-core-bundle.json`, `core05-core-artifact.json` and source before/after. At the same clean d95/tree, the core package and core bundle verifier each exited 0. The core JAR is `f1b40816e971b7b03c045ed189652f10012c90c8903e434f6c726820ad47f276`, distinct from the **full** JAR used by live HTTP; I have not interchanged those artifacts. The package log digest and bundled library inventory are retained in the artifact record. The fixed d95 static records `static05-{skill-facts,fullstack-facts,notify-layered,fm,handbooks,release-contracts}.json` each report exit 0, including the previously blocking Notify layered checker on 113 Java files. `static05-source-before.json` and `static05-source-after.json` both show the same clean d95/tree. This closes the prior eight-error architecture gate at the immutable candidate rather than relying on the earlier worktree-only check. `/tmp/wta-t42/dispatch05-scope-and-ancestry.json` records 86 changed non-governance paths within 69 pre-registered roots, no outside-scope paths, and a valid base ancestor.

**Independent implementation-contract verdict at d95: PASS within the evidence and limits above.** No remaining AC-042 source, authorization, transaction, public-contract or applicable static-gate blocker was found. The passed current-source runtime evidence comprises Mail27, Notify135, default reactor (940 executed/244 environment skips), full/core builds and full-JAR HTTP/OpenAPI. The frontend results remain explicitly reused from 73c28 by equal input trees. This is not a claim of physical SMTP provider delivery, production deployment, untested environment-skipped cases, or completion of the wider change. Final T-42 closure remains Lead-owned and contingent on final manifest/provenance and governance being fixed without contradicting these source/result coordinates.

I also read `/tmp/wta-t42/T-42-final.md` before Lead copies it. The substantive AC/limits and source provenance match the evidence reviewed here. One wording correction is needed in §6: its phrase “三个真实runner各自clean source前后、JAR/工具hash、fresh报告” groups a JAR hash with the Maven Mail and Notify runners, which do not each have their own JAR execution. Say that all three retain exact source and their respective command/tool/fresh reports, while the **HTTP runner specifically** binds the full-JAR hash before/after. The §5 distinction between full and core JARs is correct. The draft refers to a final `complete-dispatch05` manifest that was not present when I read it; Lead should insert that actual final coordinate before copying, without backdating it.
