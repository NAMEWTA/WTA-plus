---
artifact: learning-goal-probe
probe_id: GP-L-065-b01
lesson_id: L-065
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-065 / b01

审问对象是已写成的 Lesson `lessons/L-065-third-domain-frontend.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **一格**：`A:createThirdService`。OBJ-65 原文：能说明 `createThirdService`。档是**解释+对照**。矩阵 (a) 符号就是 `createThirdService`，模块 `domains/third`，仍 `uncovered`。矩阵 (a) **没有** Vue 页符号，也没有 `createThirdWebDomain` 独行；菜单工厂落在 **九工厂同行** `createAdminWebDomain, createSystemWebDomain, createProfileWebDomain, createNotifyWebDomain, createThirdWebDomain, createWorkflowWebDomain, createDemoWebDomain, createAiWebDomain, createProfileSelfWebDomain`（模块 `web-domains/*`，仍 uncovered，备注「manifest 工厂」）。矩阵 (b) **没有**「保存焊 add 窗」/ 路径编码 / 扁平十五枪独行；已有 `ThirdCredential 保存`（加密；列表不回显）已 covered（L-062 + GP-L-062-b01）；`ThirdPartyGateway.execute`（出站 HTTP、限流、记 invocation）仍 uncovered。矩阵 (c) `admin-web` / `home-web` / `sso-web` 容器与 `frontend-direction` / `admin-web-composition` 已 covered（L-004 / L-007 / L-008）。矩阵 (d) **没有**浏览器三方配置流；已有「主路径 third 出站」与「失败路径 third HTTP」仍 uncovered，备注 **L-064**。本批按派单只审问 **厨房工厂这一格**；口试按磁盘：根 `src/index.ts` 冻住 15 个扁平方法、四族前缀、`save*` 永远 POST 新增窗、路径 `id()` 编码、无 `execute` / 无 `/save` 方法 / 无嵌套抽屉，不把九工厂行整行闭合，不把 L-007 插头再盖第二次章，也不把四 `kind` 升成矩阵行。`ThirdProviderController` 六扇留给 L-060；Endpoint 六扇留给 L-061；凭据加密与列表不回显留给 L-062；invocation/statistics 查询留给 L-063；`ThirdPartyGateway.execute` 与 (d) 出站走廊留给 L-064；`createThirdWebDomain` 四键剧本留给 L-066；厅堂插座板全表留给 L-007 / L-008；通知厨房对照留给 L-053；OpenAPI 投影对照留给 L-033。

抽查锚点（2026-09-17）：`frontend/packages/domains/third/src/{index,index.test}.ts`；包 `package.json` `exports` / `dependencies`；`AGENTS.md`；`frontend/apps/admin-web/src/application/services.ts` 第 9、18–20、91 行；`apps/admin-web/src/router/adminManifestRegistry.ts` 第 10、245–254、336–376 行；`frontend/packages/web-domains/third/src/{index,index.test,runtime,ThirdPage}.ts|.vue`；`frontend/apps/home-web/src/application/services.ts`；`frontend/apps/sso-web` 零命中；`ThirdProviderController.java` / `ThirdEndpointController.java` / `ThirdCredentialController.java` / `ThirdObservabilityController.java`；`frontend/packages/api-contracts/generated/openapi.ts` `/third/*`；`50-cde-base-dml.sql` `NAMEWTA-THIRD-MENU-DML-001`。

## Q1 澄清

- objective_id: OBJ-65
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-65；chain L-065 覆盖列只 `A:createThirdService`；课 `claimed_cells` 一格；矩阵 (a) 该行仍 uncovered、模块栏印 `domains/third`；九工厂同行含 `createThirdWebDomain` 仍 uncovered；(b) 无本课符号，execute 挂 L-064，凭据保存已 covered；(c) 三厅堂已 covered；(d) 无浏览器配置流，已有出站走廊挂 L-064；课开篇「口试名单就是矩阵 (a) 这一格」+ 负空间 L-060…L-064 / L-066 / L-007 / L-008 / L-053 / L-033
- difficulty: medium

课把浏览器这一头收成「四本表格打印机」：一座扁平厨房打供应商 / 接口 / 凭据 / 观测。计划里至少有五套可数对象，派单点名的是 **矩阵 (a) 那一行**：

1. **OBJ-65 原文。** `course.md`：能说明 `createThirdService`。一句里只有厨房工厂，没有把 `createThirdWebDomain`、`ThirdPage` 四 `kind`、`ThirdPartyGateway.execute` 或 Provider 六扇写成口试清单。
2. **chain / 课 `claimed_cells` / 矩阵。** 三者 1:1：`A:createThirdService`。chain 不写 Vue、不写 execute、不写 (b)。矩阵模块栏印 `domains/third`。Lead 若按行标 covered，**不会**把九个 `create*WebDomain` 假闭合——那些在另一行，点名的是 `createThirdWebDomain` 不是本工厂。Lead 若凭「工厂四字在函数表里」把九工厂或 (d) 出站走廊一并盖章，才会假闭合。
3. **课自己的操作定义。** 工厂只认 `HttpClient`，返回 `Object.freeze` 的导出接口 `ThirdService`。15 个扁平方法，不是 `service.providers.list`。读 GET、写 POST；厨房源码搜不到 `'delete'` / `'put'` / `execute` / `gateway`。`saveProvider` / `saveEndpoint` / `saveCredential` 永远 POST 到 `/third/provider|endpoint|credential`，**不**打 `/save`。路径 id 走私有 `id()` = `encodeURIComponent(String(value))`；list 的 query **不**走 `id()`。内部 `call` 只是 `http.request` 标成 `ApiResponse<T>`，无投影闸、无 `no-store`。
4. **十五对十七。** generated `openapi.ts` 的 `/third/*` 共 17 条 path。厨房方法 15。差的就是 `POST /third/provider/save` 与 `POST /third/endpoint/save`。凭据族三对三（后端本来无 get / status / `/save`）。观测两对两（控制器门牌 `/third`，URL **不**出现 `observability`）。
5. **邻格。** L-007 已认插座板上「`createThirdService(domainHttp)` 一行」，**没有** 15 枪 URL 表。L-066 才认四键怎么扣扳机。L-060…L-063 是后端四份管理窗。L-064 才是出站网关。L-053 对照：通知厨房嵌套五块、另有厅堂 directory、还有备用枪。L-033 对照：OpenAPI 有 fail-closed 投影与 `NO_STORE`。九工厂行把本课菜单工厂和另外八个 `create*WebDomain` 绑死。

问：本行闭合标准是「扁平 15 枪 URL/动词/body 形状 + 保存键焊在 add 窗 + 路径 id 编码 + 无 execute/无嵌套抽屉/无 `/save` 方法 + 测试只锁 status/remove 与 `%2F`，且不碰九工厂整行、不重盖 L-007 插头、不升 Vue 矩阵行、不把 (d) 出站走廊并进本窗」，还是 OBJ 那句必须先把 `createThirdWebDomain` 标 covered、或必须等 GP-L-064 `execute` 才给厨房盖章？Lead 若凭「`createThirdService` 四字在函数表里」把九工厂行或 `ThirdPartyGateway.execute` 再盖一次，是否假闭合了另外八个菜单工厂和整条出站走廊？

## Q2 证据

- objective_id: OBJ-65
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: index.ts 4 / 19–23 / 27–58；index.test.ts 6–22；package.json 6–8；AGENTS.md 1–6；admin services.ts 9 / 18–20 / 91；adminManifestRegistry.ts 10 / 245–254 / 336–376；runtime.ts 1–8；web-domain index.ts 9–21；index.test.ts 4–15；ThirdPage.vue 6–7 / 70–71 / 128–132 / 150–180 / 191–206 / 214–247；home-web services.ts 1–16；sso-web 零命中；ProviderController 20–28；EndpointController 20–28；CredentialController 17–41；ObservabilityController 14–28；openapi.ts 1134–1287 / 3924–4026；DML 1646–1666；web-domain Vue 零 `@/application/services`
- difficulty: high

课的四本表格哪些能在工作树直接复现，哪些只是测试名单比课表窄、或邻格事实？

**仍能复现（本行厨房）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂签名只有 `http: HttpClient`；内部 `call` 即 `http.request` 断言成 `ApiResponse<T>`；无投影、无 `no-store`、无 `execute`、无 `gateway`、无 `'delete'` / `'put'`、无 `/save` 字符串 | index.ts 39–58；对该目录三词检索为空 |
| 返回 `Object.freeze`；导出接口名 `ThirdService`（不是只写 `ReturnType`） | index.ts 27–34 / 41 |
| 15 个扁平方法：Provider 五、Endpoint 五、Credential 三、Observability 二。没有 `service.providers.list` | index.ts 42–57 |
| `listProviders` GET `/third/provider/list`，`params: { keyword }`（即使 `undefined` 也出现） | index.ts 42 |
| `getProvider` GET `/third/provider/${id()}` | index.ts 43 |
| `saveProvider` POST `/third/provider`，**不是** `/third/provider/save` | index.ts 44 |
| `changeProviderStatus` POST `.../status`，`params: { status }`，无 `data` | index.ts 45 |
| `deleteProvider` POST `.../remove` | index.ts 46 |
| Endpoint 五枪同模，前缀 `/third/endpoint`；list 多 `params: { providerId, keyword }` | index.ts 47–51 |
| `saveEndpoint` POST `/third/endpoint`，不打 `/save` | index.ts 49 |
| `listCredentials` GET `/third/credential/list`，`params: { providerCode, endpointCode }`；`providerCode` 类型必填 | index.ts 32 / 52 |
| `saveCredential` POST `/third/credential`；`deleteCredential` POST `.../remove` | index.ts 53–54 |
| `listInvocations` GET `/third/invocation/list`；`listStatistics` GET `/third/statistics/list`（**statistics** 复数）；两枪都是 GET | index.ts 56–57 |
| 路径 `id()` = `encodeURIComponent(String(value))`；只插 get/status/remove 三段，不编码 query | index.ts 37 / 43–54 |
| 名片 `thirdDomainModule`：`id: 'third'`，`backendModules: ['wta-third']`，`capabilities` 四项含 `third-credential` | index.ts 36 |
| `CredentialSummary` 九字段无 `secretJson`，`enabled: string`；`CredentialForm` 才有 `secretJson: string` 与 `enabled: boolean` | index.ts 19–23 |
| `Statistic` **没有** statisticId | index.ts 25 |
| `exports` 只有 `"."`；依赖表只有 `platform-app-runtime` 与 `platform-contracts`，**没有** `api-contracts`、没有 Vue | package.json 6–8 |
| 包「does not import Vue」；权限形状 `third:<resource>:<action>` 写在 AGENTS，厨房源码不出现这些串 | AGENTS.md 3–6；index.ts 零命中 `third:` |
| 厅堂 `import { createThirdService } from '@namewta/domain-third'`；`thirdService = createThirdService(domainHttp)`；`domainHttp` 懒转发 `adminHttp.request` | services.ts 9 / 18–20 / 91 |
| 无 `createThirdDirectory`；runtime 只有 `service` / `confirm` / `success` / `error`，没有 notify 那种并列 `directory` | registry 245–254；runtime.ts 3–7 |
| `composeAppRuntime` 登记 `thirdDomainModule`；`selectedManifestIds` 含 `'web-domain-third'` | registry 345 / 375 |
| home-web `services.ts` **无** `createThirdService` / `thirdService`；`frontend/apps/sso-web` 零命中 | home-web 1–16；apps 检索只 admin-web |
| 厨房单测一个 `it`：`changeProviderStatus('provider/1','1')` → `/third/provider/provider%2F1/status` + `params.status`；`deleteProvider` 同编码 `.../remove`；数字 `2`/`3` 不改写；断言对象**没有** `data` | index.test.ts 10–22 |
| generated `/third/*` **17** 条，含厨房未接的 `/third/provider/save` 与 `/third/endpoint/save`；无 gateway / execute path | openapi.ts 1134–1287 / 3924–4026 |
| Provider 六扇：add=`POST /` 权限 `*:add`；save=`POST /save` 权限 `*:edit`；status 用 `@RequestParam` | ProviderController 25–27 |
| Endpoint 同形六扇 | EndpointController 25–27 |
| Credential 三扇：POST `/` 仍是 `*:add`；无 get、无 `/save`、无 status | CredentialController 21–40 |
| 观测 `@RequestMapping("/third")` + 两 GET；`providerCode` `required=false`；无 POST | ObservabilityController 14–28 |
| 菜单四张 C 型页 `third/{provider,endpoint,invocation,statistics}/index`；凭据三串 F 型挂在**供应商页**下，没有 `third/credential/index` | DML 1646–1666 |

**邻格仍能复现（不计入本行闭合）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| `createThirdWebDomain` 四键顺序；`componentKey.includes('credential') === false`；`third-provider` 权限组含 `third:credential:add`；工厂可塞 `service: {} as never` 仍冻出四键 | web-domain index.ts 9–21；index.test.ts 5–14 |
| 编辑保存 UI 核 `*:edit`，`save()` 仍喊 `saveProvider` / `saveEndpoint` | ThirdPage.vue 70–71 / 175–180 |
| 供应商行 `listCredentials(code)`（空串收成 `undefined`）；接口行传 `endpointCode`；`replaceCredential` 清空 `secretJson`，`enabled === '0'` 还原布尔 | ThirdPage.vue 214–241 |
| 观测列不展示 `sanitizedRequestJson` / `sanitizedResponseJson`；类型里有这两字段 | ThirdPage.vue 131；index.ts 24 |
| 四张 web-domain 页 **零** import `@/application/services` | web-domains/third 检索为空 |

**课写过、测试名单更窄或口试标签写偏（不是证伪本行）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 15 枪方法表 | `index.test.ts` 只锁 status/remove 五枪与 `%2F`。list / get / save / 观测 **无**厨房单测 |
| 「保存打新增窗」 | 证人是 `index.ts` 第 44、49、53 行。现有测试抓不住「误打 `/save`」 |
| `Object.freeze` | 浅冻顶层键。扁平对象没有内层抽屉可改；无单测锁「不能再加键」 |
| 「页面空着也会传 `''` 打观测」 | 证人是 `ThirdPage.vue` `load()`（L-066 对照）。厨房类型要求 `string`，不会因为空白短路 |
| 「`params` 里的 `undefined`」 | 工厂总是写上键。axios 是否省略 undefined query 是司机的事，厨房单测没碰到 list |

问：在 15 枪 URL/动词、无 execute、无 `/save` 方法、无嵌套抽屉、路径 `%2F`、status 走 query、凭据摘要无秘密、观测 URL 无 `observability`、home 不雇、名片有 credential 但菜单无凭据键 已经能互证的前提下，本行是否还要把「厨房单测未锁 save」「编辑 UI 核 edit、卡车开 add」写成开篇半句才算讲完？矩阵模块栏印 `domains/third` 是否与磁盘所有权 1:1，不必等拆出 Vue 行或等 L-064 才给厨房盖章？

## Q3 假设与反例

- objective_id: OBJ-65
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 课反例 1–12 / 误区 1–20 / 边界 1–10；index.ts 39–58；index.test.ts 6–22；package.json 6–8；services.ts 91；home-web 零命中；openapi.ts 1185 / 1253；ProviderController 25–26；ObservabilityController 14–18；web-domain index.test.ts 7–14；ThirdPage.vue 70–71 / 175–180
- difficulty: high

课的隐含假设若反转，合格读者会把哪段走廊认错？

1. **「`createThirdService` 在 `transport.ts`，像 notify。」** 磁盘只有根 `index.ts`。课误区 1。
2. **「从 `@namewta/domain-third/observability` 进口小厨房。」** `exports` 只有 `"."`。课反例 4 / 误区。
3. **「`service.providers.list` / `service.observability.invocations`。」** 扁平：`listProviders` / `listInvocations`。课反例 3。
4. **「`saveProvider` 打 `POST /third/provider/save`。」** 第 44 行是 `'/third/provider'`。`/save` 只活在 Java 与 `openapi.ts`。课反例 1 / 误区 4。
5. **「编辑走 PUT；删除用 HTTP DELETE；status 放 JSON body。」** 全是 POST；status 在 `params`；测试对象无 `data`。课反例 6–7。
6. **「`thirdService.execute` / `thirdService.gateway` / `reloadCache`。」** freeze 对象没有这些键。出站在 L-064。课反例 2。
7. **「看见 OpenAPI 有 `/save` 厨房就有。」** 标签不是方法表。方法表以 freeze 对象为准。课误区 20。
8. **「页面自己拼 `/third/endpoint/list`。」** 违反 App → web-domain → domain → platform。URL 只许出现在 `domains/third/src/index.ts`。课反例 5。
9. **「凭据有 get-by-id，编辑再拉一次明文。」** 没有这枪。`CredentialSummary` 无 `secretJson`。替换必须重填。课反例 8。
10. **「厨房会加密 `secretJson`。」** 只运输。加密在 L-062。课误区 10。
11. **「`capabilities` 有 `third-credential` 所以有凭据页。」** 名片不是 `componentKey`。四键测试锁死 credential 键为 false。课误区 13 / 边界 7。
12. **「观测门牌是 `/third/observability`；`listStatistics` 的 path 是 `/third/statistic/list`。」** 控制器 `/third` + `/invocation|statistics/list`。课反例 11–12。
13. **「`listInvocations` 不填供应商就不发车。」** 会发，`providerCode` 可能是空串。课误区 18 / 边界 4。
14. **「`id()` 证明生产主键可以含 `/`。」** 后端 path 是 `Long`。测试只锁编码。课误区 17。
15. **「厨房 15 枪里有墙上没挂的备用枪，像 notify 的 snapshot/seen。」** 相反：十五支都有 Vue 扣扳机；缺口在后端多出来的 `/save`。课边界 1。
16. **「编辑保存核 `*:edit` 所以卡车开向 `/save`。」** UI 闸和 HTTP 窗不是同一把锁。`save()` 仍喊 `saveProvider`。只有 add、没有 edit 的人可能看见按钮却 403。课正例 3 / 边界 2。
17. **「home-web 对称一份厨房。」** 产品差在 App 插座板。课反例 9 / 误区 12。
18. **「本课覆盖四 `kind` 的按钮剧本。」** 那是 L-066。本课认 URL 与谁注入谁。课误区 15 / 反例 10。
19. **「L-007 已经 covered 插座板，所以本行方法表也 covered。」** L-007 只认组合点。15 枪与两扇没接的窗是本课。
20. **「九工厂行可以凭本课标 covered。」** `createThirdWebDomain` 是 L-066，且与另外八个工厂印在同一行。
21. **「矩阵要先有 (b) 缺 `/save` 行 / (d) 浏览器配置流才算讲完。」** 矩阵无这些行。它们是 (a) 的 explained。已有 (d) 是 Java 出站走廊（L-064）。
22. **「启用状态 `'0'` 和凭据摘要 `enabled: '0'` 是同一个字段。」** 前者是 Provider/Endpoint 的 `status`；后者是 Vo 上的 `delFlag` 别名。课误区 19。

问：读完后仍说不出的，是不是「工厂类名」，而是 **扁平十五枪、保存焊 add 窗、无 execute、无 `/save` 方法、路径编码只管 id、status 走 query、摘要无秘密、观测 URL 没有 observability 这个词、测试只锁五枪、home 不雇、名片不是菜单**？

## Q4 视角

- objective_id: OBJ-65
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-65 vs L-007 / L-008 / L-033 / L-053 / L-054 / L-060…L-064 / L-066；九工厂行；chain 一格 vs 课一格 vs 矩阵一行
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:createThirdService`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | 一行一符号与 chain/claimed 1:1；无 Vue 行；九工厂含 `createThirdWebDomain` | **是** 闭合定义。本行可单独 covered。不要把 Vue / 九工厂 / execute 并进本行 |
| 厨房作者 | 15 枪 URL/动词/params vs data；浅 freeze；字面量 URL；`id()` | **是** (a) 工厂。`/save` 两窗与 `execute` **否** |
| 厅堂电工（L-007） | `domainHttp` 懒电线、`thirdService` 单例 | 插头一句 **是**。12 个 `create*` 全表 **否**。本课才认 URL 表 |
| 菜单页作者（L-066） | 四键 + 一张 `ThirdPage` 按 `kind` 扣 15 枪；凭据嵌套；工厂不校验 runtime | 谁喊哪一枪 **是** 对照。按钮剧本 **否** |
| Provider 窗（L-060） | 六扇；add 与 `/save` 门口分权；管家 `useCase.save` 合并 | URL 对照 / 厨房改正仍打 add **是** 本窗边界。六扇盖章 **否** |
| Endpoint 窗（L-061） | 同形六扇 | 同上 **是** 对照。装门 **否** |
| Credential 窗（L-062） | 三扇对齐；加密；列表不回显 | Summary 无 secret、厨房三枪打本前缀 **是** 旁证。加密适配器 **否**（已 covered） |
| 观测窗（L-063） | 两 GET；空白不过滤；7 天/200 条 | URL/动词 **是**。截断合同 **否** |
| 出站网关（L-064） | `execute` → Adapter → SPI → 记录 | 厨房无此枪 **是** 负空间。走廊与失败分类 **否** |
| 通知厨房（L-053） | 嵌套五块、directory、备用枪 | 形状对照 **是**。22 枪 **否**（已 covered） |
| OpenAPI 厨房（L-033） | 投影闸、`NO_STORE`、两扇柜台 | 三方无投影 **是** 对照。钥匙柜 **否**（已 covered） |
| 九工厂行 / L-066 | `createThirdWebDomain` | 注入 `{ service }` **是** 对照。四键工厂盖章 **否** |
| home / sso | 不雇这座厨房 | 负空间 **是**。不要发明 home 三方行 |
| (b) 缺 `/save` / `%2F` / 浅 freeze | 矩阵无此行 | 本格 explained **是**。不要发明第二 (b) |
| (d) 浏览器三方配置流 | 矩阵无此行；已有 (d) 是 Java 出站走廊 | ASCII 四本表格 **是** 本课图。不要发明浏览器 (d)，也不要用本课改 L-064 主路径 |

问：Lead 若凭「工厂在函数表里」把矩阵该行标 covered，是否还要求读者能指着 **扁平十五枪、保存焊 add、无 execute、测试只锁五枪、home 不雇、名片不是菜单**？这些是不是已经在本格机制链/反例里，而不需要 (b) 新行、也不需要把 `ThirdPage` 或九工厂算进来？

## Q5 元问题

- objective_id: OBJ-65
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 一行一符号、无 Vue 行、无本课 (b)；九工厂同行仍 uncovered；(d) 出站走廊仍挂 L-064
- difficulty: low

建议：`defer`

原因：

- **矩阵 (a) 该行与 OBJ-65 / chain / 课 claimed 1:1。** L 合同章节齐全（宏观四本表格、直觉十二句、定义表、机制链 1–5、带 caption / 文字等价物 / 图边界的两张图、正反例、变式、误区、暂停走查、来源表 S-005/S-006/S-010/S-L007-01/S-L065-01…08）。**主叙事站得住且与源码对齐**：工厂只认 `HttpClient`、冻住扁平 15 枪、无 execute/无 `/save` 方法/无嵌套抽屉；保存键焊在 add 窗；路径 id 编码；status 走 query；凭据摘要无秘密；观测两枪 GET 且 URL 无 `observability`；根 barrel、无 `transport.ts`；厅堂只 admin-web 接线；home / sso 不雇。不必为「工厂名字」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例或邻格，不挡住把 **本行** 标 covered：（1）厨房单测未锁 list/get/save/观测；（2）编辑保存 UI 核 `*:edit`、卡车开 `*:add`（L-066 对照，课已写进正例 3）；（3）`params` 对象含 `undefined` 键，省略是司机的事；（4）`listInvocations` 类型必填 vs 后端可选；（5）`createThirdWebDomain` 不 fail-closed 缺方法（L-066）。不要把四 `kind`、九工厂、Java 六扇、加密适配器、出站 `execute` 塞进本行。
- **不要等拆矩阵行或等 GP-L-064 才盖章。** 模块栏印 `domains/third` 与磁盘所有权 1:1。chain/课/矩阵都只认厨房。`createThirdWebDomain` 保持邻居。已有 (d) 继续挂 L-064。
- 不建议 `mine-more`：b01 未证明本行还有真实矩阵空洞；再五问只会把 L-066 按钮剧本、L-060 `/save` 分权、L-064 `execute` 或 L-053 嵌套抽屉再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；可选半句（测试名单窄、UI 闸≠HTTP 窗、`undefined` query）不是第二容器。不建议 `split-lesson`：未满 10 问；一座工厂是 chain 原设计的同一 OBJ-65，同一 `domains/third` 容器，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b)/(d) 行：缺 `/save` 与四本表格图是 (a) 的 explained。不建议把 Vue 升成矩阵第二格才盖章：课已写明格子按 (a) 这一行；四键剧本是 L-066。不建议用本课标九工厂行、重盖 L-007 插头、或用 15 枪假闭合 `execute`。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:createThirdService | 15 枪 URL/动词/params、扁平 freeze、无 execute/无 `/save` 方法/无嵌套抽屉、路径 `%2F`、status 走 query、摘要无秘密、观测无 `observability` 词、根 barrel、home 不雇——均与源码对齐。残留精度：厨房单测只锁 status/remove 五枪；`params` 含 `undefined` 键。 | 可选半句：口试方法表以 `index.ts` 为准，不以一个 `it` 为准。不要求重写机制链。Lead 可将矩阵该行标 `covered`（证据锚本 probe 与 L-065 来源表 S-L065-01…08）。 | defer（本行可 covered；精度项不挡盖章） |
| A:createThirdWebDomain / 九工厂同行 | 注入 `{ service }` 已点。四键顺序、凭据嵌套、页面扣扳机未当本格闭合。九工厂把八个邻居绑死。 | 不要用本课标该行 covered。L-066。 | defer（L-066 / 九工厂） |
| ThirdPage 四 `kind` | 课已讲谁喊哪一枪、编辑仍 `saveProvider`、凭据对话框、观测不展示脱敏 JSON。矩阵无 Vue 符号。 | 不要并进本行才能盖章。按钮剧本 L-066。 | defer（未入库对照；L-066） |
| A:admin-web.services / C:admin-web-composition | 插头 `createThirdService(domainHttp)` 已借用。 | 保持。全表 L-007（已 covered）。本课不重盖。 | defer（L-007 已 covered） |
| A:home-web.services | 负空间：无工厂、无单例。 | 保持。L-008（已 covered）。 | defer（L-008 已 covered） |
| A:ThirdProviderController.* / A:ThirdProviderUseCase | 六扇；厨房缺 `/save`。 | URL 对照一句。L-060。 | defer（L-060） |
| A:ThirdEndpointController.* / A:ThirdEndpointUseCase | 六扇；厨房缺 `/save`。 | 保持。L-061。 | defer（L-061） |
| A:ThirdCredentialController.list,add,remove / B:ThirdCredential 保存 | 三枪对齐；Summary 无 secret。加密/不回显已 covered。 | URL 旁证一句即可。不要重盖 (b)。 | defer（L-062 已 covered） |
| A:ThirdObservabilityController.invocationList,statisticsList | 两 GET；空白不过滤；7 天/200 条是后窗。 | 厨房只保证 URL/动词。L-063。 | defer（L-063） |
| B:ThirdPartyGateway.execute / D:主路径 third 出站 / D:失败路径 third HTTP | 厨房无 execute 枪。走廊仍 uncovered。 | 负空间一句。不要用本课改 (d)。 | defer（L-064） |
| A:createNotificationService / A:createOpenApiService | 嵌套抽屉 / 投影闸对照。 | 保持邻居。已 covered。 | defer（L-053 / L-033 已 covered） |
| (b) 保存焊 add / `%2F` / 浅 freeze | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 浏览器三方配置流 | 矩阵无此行。已有 (d) 是 Java 出站走廊。 | 不要发明浏览器 (d)，也不要用本课改 L-064 主路径。 | defer（未入库） |
| C:MySQL 8.4 / Redis 8 | 菜单种子是 (a) 对照，不是容器闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本行无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出目的（一座扁平厨房把四族打到 `/third/provider|endpoint|credential` 与 `/third/invocation|statistics/list`）、主路径（读 GET、写 POST、保存焊 add 窗、路径 id 编码）与失败路径（误打 `/save` 的方法不存在、只有 edit 没有 add 会在 add 窗 403、空白观测仍发车、斜杠 id 只保证 `%2F` 不保证进 `Long`）；四键剧本缺的是邻课不是讲义容器；把可选半句写进原课不会超过 40 分钟；后端四窗、出站网关、菜单工厂、厅堂插头已有邻课。满 10 问前不拆。厨房虽对照四份 Controller，仍是 chain 原设计的同一 OBJ-65、同一 `domains/third` 容器，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / split 提案：否（miner 不改矩阵）；split 提案为 none；缺口表对 L-066 九工厂、L-007/L-008 插头（已 covered）、L-060…L-063 后端窗、L-064 出站/execute 与 (d)、L-053/L-033 对照（已 covered）、未入库 (b)/(d)/Vue 符号以及 MySQL/Redis 容器显式建议 `defer`；对矩阵 (a) 行 `createThirdService` 建议 Lead 标 `covered`（证据锚本 probe 与 L-065 来源表）。本课不主张发明 `createThirdDirectory`、把 `ThirdPage` 升格、重盖 L-007、用本课标九工厂行、或用 15 枪假闭合 `execute`。
