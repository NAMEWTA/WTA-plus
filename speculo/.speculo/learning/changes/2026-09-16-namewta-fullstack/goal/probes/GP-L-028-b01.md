---
artifact: learning-goal-probe
probe_id: GP-L-028-b01
lesson_id: L-028
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-028 / b01

审问对象是已写成的 Lesson `lessons/L-028-oss-migration.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain / `claimed_cells` / 矩阵 (a) **一行**）：`A:SysOssMigrationController.batch, items, dryRun, start, retry, rollback, cleanup`。OBJ-28 原文只点名五步：`dry-run/start/retry/rollback/cleanup`。课开篇把 batch/items 收成同课两扇观察窗，并在误区 1 写明「矩阵与 chain 点名七个方法」。矩阵 (b) **没有** `process` / `claim` / `compareAndSetService` 行。矩阵 (c) MinIO/OSS、MySQL 8.4 仍 uncovered。矩阵 (d) 只有 OSS **直传** 主/失败路径（L-027/L-029），**没有** 迁移数据流行。`SysOssConfigController` 留给 L-025；`SysOssController` 列表/下载/删除留给 L-026；直传 `init/signParts/parts/complete/abort` 留给 L-027；浏览器 `createOssUploadClient` 留给 L-029。

抽查锚点（2026-09-16）：`backend/wta-modules/wta-system/.../controller/system/SysOssMigrationController.java`；`oss/migration/{OssStorageMigrationService,OssMigrationContracts,OssMigrationError,OssMigrationStage,OssMigrationStatus,OssMigrationException,MybatisOssMigrationStore,DefaultOssMigrationObjectStore,DefaultOssMigrationAccessVerifier,OssStorageMigrationProperties,OssMigrationRequiredConfigContributor,SysOssMigrationBatch,SysOssMigrationItem,mapper/SysOssMigrationItemMapper}.java`；`mapper/system/SysOssMapper.xml` `compareAndSetService`；`03-backend-module-modes.md` classic 行；`10-cde-base-ddl.sql` 约 546–601 行；`50-cde-base-dml.sql` 文件管理段（`system:oss*` / `system:ossConfig*`，无 `ossMigration`）；`OssMigrationHttpContractUnitTest`；`OssReleaseContractionUnitTest`；`OssStorageMigrationServiceUnitTest`；`OssStorageMigrationIntegrationTest`；`GlobalExceptionHandler.handleRuntimeException`；`RepeatSubmit.interval` default 5000；`AccessPolicy`；`frontend/packages/api-contracts/generated/openapi.ts` `/resource/oss/migrations*`；`createOssUploadClient` 仅在 `adapter-oss-upload-browser`。

## Q1 澄清

- objective_id: OBJ-28
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-28；chain L-028 覆盖列七方法；L-028 `claimed_cells`；矩阵 (a) `SysOssMigrationController.batch, items, dryRun, start, retry, rollback, cleanup` 独行；(b) 无 process/CAS 行；(c) MinIO/OSS uncovered；(d) 无迁移流；课开篇「正好七个」+ 误区 1
- difficulty: medium

课把搬家柜台收成「宏观五步与两扇观察窗」。计划里至少有四套可数对象，闭合标准要以哪一套盖章？

1. **OBJ-28 原文。** `course.md`：能口述 `SysOssMigrationController` dry-run/start/retry/rollback/cleanup。点名的是**五步命令**，没有写 batch/items，也没有把 `process` 写成口试清单。
2. **chain / 课 `claimed_cells` / 矩阵 (a) 一行。** 七个方法与磁盘公开映射同名：`batch, items, dryRun, start, retry, rollback, cleanup`。默认档 Unistructural + explained。状态仍 `uncovered`。
3. **课自造的里屋。** `OssStorageMigrationService.process`、`claim`、`compareAndSetService`、`verifyPublic`、`refreshBatch` 写成机制主链。矩阵 (b) 无这些符号；(c) MinIO/OSS 备注仍是「直传对象」；(d) 无 PRIVATE→PUBLIC_READ 迁移流。
4. **菜单 / OpenAPI / 前端多出来的字。** DML 有 `system:oss:*` / `system:ossConfig:*`，没有 `system:ossMigration:*`。生成 OpenAPI 有七条路径与权限描述，仓库没有 `createOssMigrationService` 或迁移页。

问：本课 (a) 闭合标准是「矩阵这一行的七个 HTTP 方法：路径、动词、四颗权限、POST+`@Log`+`@RepeatSubmit`、GET 两扇只读、以及每扇碰不碰 MySQL/Bucket」，还是 OBJ-28 那句五步就够盖章？若 Lead 只凭「类还在 `controller/system`」标 covered，batch/items 还算不算本格？若必须看见 `process()` 函数体 / `claim` SQL 才算性状闭合，是否已经把这一行 (a) 扩成一张不存在的 (b) 和 MinIO 容器？

## Q2 证据

- objective_id: OBJ-28
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: `SysOssMigrationController` 18–81；`OssMigrationHttpContractUnitTest` 19–48；`OssReleaseContractionUnitTest` 40–57；`OssStorageMigrationService` dryRun/start/process/retry/rollback/cleanup/refreshBatch/validateRequest/requireRoute；`OssMigrationError` 3–18（**14** 个）；`SysOssMapper.xml` 41–48；`SysOssMigrationItemMapper.claim` 13–21；DDL 546–601；DML 无 `ossMigration`；`GlobalExceptionHandler` 158–163；`OssStorageMigrationServiceUnitTest`；integration `assertMigrated` / rollback 双桶
- difficulty: high

课的机制链哪些能在工作树直接复现，哪些被磁盘削薄或说满？

**仍能复现（本格）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 一份类、七扇窗、根路径 `/resource/oss/migrations`；**不**继承 `BaseController`；只注入 `OssStorageMigrationService` | Controller 18–24；HTTP 测试 `containsExactlyInAnyOrderEntriesOf` 七对；无 PUT/DELETE |
| `wta-system` classic；无 UseCase；磁盘 `Controller → OssStorageMigrationService → Store/ObjectStore/AccessVerifier` | `03-backend-module-modes.md` classic 行；模块内零 `usecase/oss` |
| GET `batch` `/{batchId}`、`items` `/{batchId}/items`：权限 `system:ossMigration:list`；无 `@Log`、无 `@RepeatSubmit` | Controller 26–36 |
| POST 五步都有 `@RepeatSubmit`（默认 5000ms）、`@Log` 且 `isSaveRequestData=false` | Controller 38–80；`RepeatSubmit.interval` default 5000；收缩测试钉 POST + 不存请求体 |
| 四颗权限：`execute` 管 dryRun/start/retry；`rollback` 单独；`cleanup` 单独；删源不是 execute | 38 / 46 / 54 / 63 / 72 行注解 |
| `MigrationRequest` 只有 `ossIds` + `targetConfigKey`，没有来源配置字段 | Contracts 15–20 |
| `CleanupRequest` 单个 `boolean approved`，没有默认 true | Contracts 22–23 |
| `BatchView` 不含 `dryRun` / `errorMessage`；`ItemView` 含 stage / lastErrorStage / cleanupEligibleTime | Contracts 35–44 |
| `dryRun` 零批次、零明细、零 copy、零 delete；`ready` 是 `allMatch(ready)` | Service 46–89；单元测试 `dryRunHasNoDatabaseOrProviderMutation` |
| `validateRequest`：空/目标空/id≤0/重复 id（distinct 后长度对不上）/超过 `maxBatchSize` → 整单 `INVALID_REQUEST`，没有逐条报告 | Service 285–296 |
| 目标 `requireRoute(..., PUBLIC_READ)` 失败也是整单抛错（`STORAGE_NOT_SERVING` / `ACCESS_POLICY_MISMATCH`） | Service 48 / 298–306 |
| 逐条：缺对象、`deleteState != ACTIVE`、来源不是 PRIVATE/SERVING、来源=目标、源 head 不到、目标已有但内容冲突 → `PreflightItem.ready=false`，reason 是枚举名 | Service 52–86；PENDING 删除测试 reason=`INVALID_STATE` |
| `start` 先再 `dryRun`；`ready==false` → `INVALID_REQUEST`「迁移预检未通过」，不建批次；全绿才 `createBatch` + 同步 `process` 每件；返回 `Long` 批次号 | Service 91–107 |
| HTTP `dryRun()` 不调 `createBatch`。实体 `dryRun` 字段默认 `"N"`；DDL 列默认 `'Y'`。`BatchView` 看不见这列 | 实体 20；DDL 550；Service `dryRun` 无 insert |
| `createBatch` 状态 `RUNNING`，记下 `startedTime`；`createItem` 用 `oss.getFileName()` 当 `objectKey`，状态 PENDING + 阶段 PREFLIGHT | `MybatisOssMigrationStore` 37–60 |
| 无 `@Transactional`、无 SnailJob；`process` 在当前 HTTP 线程 | Service 全文零 `@Transactional`；start 循环直接 `process(item)` |
| `claim` SQL：`status='RUNNING'` 且 `version+1`，条件 version 匹配且 `status != 'RUNNING'` 且 `del_flag='0'`；抢不到锁 **return**，不抛 | ItemMapper 13–21；Service 195–197 |
| 先把阶段写成 `COPIED` 再 `transferAndVerify`；源不在 `OBJECT_NOT_FOUND`；目标已在且不匹配 `TARGET_CONFLICT`（不覆盖）；相同内容跳过物理 copy | ObjectStore 36–54；访问失败 retry 测试 `physicalCopies` 仍 1、`copyCalls` 为 2 |
| `matches`：先 size；再 checksum 任一算法相同；再可信 ETag（非空且不含 `-`）；再不行且 size≤上限则两侧 SHA-256；超限且无可信校验 → `CONTENT_MISMATCH` | ObjectStore 93–109 |
| `CONTENT_MISMATCH` 即使当时 stage 停在 COPIED，也记 `CONTENT_VERIFIED`；`sys_oss.service` 仍是来源 | Service 251–253；单元测试 `recordsContentVerificationFailureAtItsExactStage` |
| CAS：`update sys_oss set service=#{targetService} where oss_id=? and service=#{expectedService} and delete_state='ACTIVE'`。已经是目标则幂等；第三份配置 `SERVICE_DRIFT`，不改第三份 | `SysOssMapper.xml` 41–48；Service 215–223；漂移测试卡片停在 `"other"` |
| `verifyPublic`：`accessType=="PUBLIC"` 且 `expiresAt==null`，再匿名 `Range: bytes=0-0`，只要 200 或 206；跟随重定向、5s 超时 | `DefaultOssMigrationAccessVerifier` 23–37 |
| 访问失败：尝试 CAS 目标→来源；卡片没回到来源 → `SERVICE_DRIFT`；否则 `ACCESS_VERIFICATION_FAILED`；`errorMessage=error.name()`，测试不含 `secret`/`http` | Service 227–238 / 260–264；单元测试 79–87 |
| 成功停在 `CLEANUP_ELIGIBLE`，`cleanupEligibleTime = now + cleanupDelay`（代码默认 24h，启动校验 1min–30d）；批次这时也是 `CLEANUP_ELIGIBLE`，不是 `COMPLETED` | Service 242–247 / 267–278；单元测试 70–75 |
| `retry` 只重做 `FAILED` 且 `lastErrorStage != COMPLETED`；清理失败要再 `cleanup(..., true)`，retry 后 `copyCalls` 仍为 1 | Service 121–125；`cleanupFailureRequiresAnotherApprovedCleanupWithoutRecopying` |
| `rollback`：`terminal()`（COMPLETED/ROLLED_BACK）跳过；源不在 → 这件 FAILED + 阶段 ROLLED_BACK + `OBJECT_NOT_FOUND`；若 service 已是目标则 CAS 回来源；全程不 delete；integration：两桶对象都在，私有匿名 403 | Service 130–153；integration 137–144 |
| `cleanup`：`approved==false` → `CLEANUP_NOT_APPROVED`；混进别的状态 → `INVALID_STATE`；窗口未到 → `CLEANUP_WINDOW_OPEN`；删的是**来源** key；删失败这件 `fail(..., COMPLETED, CLEANUP_FAILED)` 再抛出，后面的件这轮不再删 | Service 155–188 |
| `refreshBatch`：`failed>0` → 批次 FAILED（哪怕 successCount>0）；全部 ROLLED_BACK → ROLLED_BACK；否则全部 `terminal()` → COMPLETED；否则 CLEANUP_ELIGIBLE。`successCount` 把可清理和已完成算在一起 | Service 267–283 |
| 活动配置：未 COMPLETED/ROLLED_BACK 的 source+target 对 readiness 声明 `OSS_MIGRATION` | ItemMapper 23–35；`OssMigrationRequiredConfigContributor`；单元测试 `containsOnlyKeys("private","public")` |
| 批次不存在：`batch` / `items` / `retry` / `rollback` / `cleanup` 都 `BATCH_NOT_FOUND` | Service 308–313 |
| `OssMigrationException` 只是 `RuntimeException`；仓库无按枚举映射的 Handler；`handleRuntimeException` 返回 `R.fail("发生未知异常，请联系管理员 [错误编号: …]")` | Exception 3；Handler 158–163；全仓 Handler 无 `OssMigrationError` |
| 菜单种子无 `system:ossMigration:*`；OpenAPI 七条路径有权限描述；前端无迁移工厂 | DML 65 / 142–149；openapi.ts 1807–1875 / 5107–5124；`createOssUploadClient` 只在 adapter |
| 策略枚举只有 `PRIVATE` / `PUBLIC_READ` | `AccessPolicy`；收缩测试 `containsExactly` |
| 属性默认：`maxBatchSize=100`（1–1000）、`maxVerifyBytes=64MiB`（1MiB–5GiB）、`cleanupDelay=24h`（1min–30d） | Properties 18–33 |
| integration（有隔离 MySQL+双 Bucket 才跑）：cleanup 后源 head 失败、目标仍在；`sys_oss_ref` 行数仍 1；rollback 后两桶都在 | Integration 125–145 / 155–168；无参数则 `Assumptions.assumeTrue` skip |

**课写过、磁盘已削薄或说满（不是证伪七扇窗）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「`OssMigrationError` 15 个名字」 | 枚举正好 **14** 个：`INVALID_REQUEST, BATCH_NOT_FOUND, OBJECT_NOT_FOUND, STORAGE_NOT_SERVING, ACCESS_POLICY_MISMATCH, TARGET_CONFLICT, COPY_FAILED, CONTENT_MISMATCH, SERVICE_DRIFT, ACCESS_VERIFICATION_FAILED, CLEANUP_NOT_APPROVED, CLEANUP_WINDOW_OPEN, INVALID_STATE, CLEANUP_FAILED`。词汇表 / S-L028-03 写 15 是数错 |
| 「请求直接 500 口袋」 | `handleRuntimeException` **没有** `@ResponseStatus`。HTTP 状态仍是 200，JSON `code` 才是 `HttpStatus.ERROR`（500），文案是未知异常+8 位编号。不要说成「没有 R 外壳」或「HTTP 500」 |
| 「RUNNING 是 claim 的占位」 | claim **SQL** 把库里标 `RUNNING`。随后 `saveItem` 是 `updateById`，全局 `updateStrategy=NOT_NULL`，内存对象仍是 createItem 的 `PENDING`，会把 RUNNING 写回 PENDING，阶段则是 COPIED。单元测试 `MemoryStore.claim` 改的是**同一对象**，所以测试里一直看见 RUNNING。长耗时 `start` 期间另一请求 `GET items`，生产路径更可能看到 `PENDING+COPIED`，不是 RUNNING |
| 「Clock 默认 UTC」管窗口 | `cleanupEligibleTime` / `completedTime` / 窗口比较走注入 `Clock`。`MybatisOssMigrationStore.createBatch` 的 `startedTime` 用 `Instant.now()`，不走这只 Clock |
| 「缺字段也是 false」 | JSON `{}` 的 primitive `boolean` 确是 false。**缺整个 body** 走 `HttpMessageNotReadableException`，进不到 `CLEANUP_NOT_APPROVED` |
| start / retry 件失败 | `process` 吞掉 `StageFailure` / `OssMigrationException` / `RuntimeException`，**不**往 HTTP 抛。`start` 仍 `R.ok(batchId)`，`retry` 仍 `R.ok()`；失败在明细与 `refreshBatch`。整单抛错只发生在校验/预检未过/cleanup 未批准/窗口/删源异常/`BATCH_NOT_FOUND` |
| integration 匿名探测 | 生产 verifier 是 Range GET 200/206。integration 夹具是裸 GET、只要 200 |

问：在七扇窗路径/权限/`@Log`、dry-run 零写入、start 先预检再同步搬、成功停在可清理、retry 不修清理失败、rollback 零 delete、cleanup 要批准+窗口、无 list-all、无菜单种子已经能互证的前提下，(a) 是否还要把 **「15 个错误名」「HTTP 500」「GET items 一定看见 RUNNING」** 写成开篇半句，才算把七扇窗讲完？还是这些已经（或应当）落在反例里，Lead 可以直接盖章？

## Q3 假设与反例

- objective_id: OBJ-28
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 七映射全表；`MigrationRequest` 无 source；dryRun 零 insert；start 同步；CLEANUP_ELIGIBLE≠COMPLETED；rollback 不删目标；cleanup 权限不是 execute；DML 无 ossMigration；无 UseCase；无专用 Handler；error 枚举 14；process 不抛
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪只柜子认错？

1. **「还有 `GET /resource/oss/migrations` 列表。」** 没有。必须带 `{batchId}`。课反例 1 覆盖。
2. **「`/resource/oss/migrations` 挂在 `SysOssController`。」** 另一份类、另一块牌子。课误区 2 覆盖。
3. **「dry-run 会插一条 `dry_run='Y'` 的批次。」** HTTP 预检零 INSERT。课反例 2 / 误区 3 覆盖。DDL 默认 `'Y'` 不是这条路径。
4. **「start 异步投递 Job / 直接打成 COMPLETED。」** `process` 在请求线程；成功件是 `CLEANUP_ELIGIBLE`。课反例 3 / 误区 4–5 覆盖。
5. **「rollback 会把橱窗副本删掉。」** integration 与单元测试 `deleteCalls=0`，目标仍在。课反例 4 覆盖。
6. **「cleanup 是 execute 权限。」** 注解 `system:ossMigration:cleanup`。rollback 也不是 execute。课反例 5 覆盖。
7. **「请求体带 sourceConfigKey。」** 只有 `ossIds` + `targetConfigKey`。课反例 6 覆盖。
8. **「对象键是 `originalName` 或 URL。」** `createItem` 用 `fileName`。课反例 7 覆盖。
9. **「可以 PRIVATE→PRIVATE 或 PUBLIC→PRIVATE。」** `requireRoute` 钉死目标 PUBLIC_READ、来源 PRIVATE。课反例 8 覆盖。
10. **「菜单有文件管理，所以超管页已有迁移按钮。」** DML 没有这些 perms。课反例 9 / 误区 11 覆盖。
11. **「这是 layered，UseCase 在 `usecase/oss`。」** 登记表 classic。课反例 10 / 误区 13 覆盖。
12. **「`retry` 能修好 cleanup 失败。」** `lastErrorStage==COMPLETED` 被跳过。课反例 11 覆盖。
13. **「失败时 HTTP 返回 `{ error: CONTENT_MISMATCH }`。」** 没有这种 Handler。逐条预检失败才把枚举名放进 `PreflightItem.reason`。课反例 12 / 误区 14 覆盖。
14. **「目标已存在就覆盖。」** `TARGET_CONFLICT`。相同内容才跳过 copy。课反例 13 覆盖。
15. **「`SysOssController.remove` 等于本课 cleanup。」** 对象管理删除是 OBJ-26。课反例 14 覆盖。
16. **「`batch` 视图能看见 `dryRun` 字段。」** `BatchView` 没有。课反例 15 覆盖。
17. **「`createOssUploadClient` 会调 start。」** 直传 Ticket 是 OBJ-29。课误区 12 覆盖。
18. **「successCount>0 表示批次 COMPLETED。」** 有 FAILED 则批次 FAILED；全绿未清理则 CLEANUP_ELIGIBLE。课误区 15 覆盖。
19. **「OBJ-28 只有五步，batch/items 不算。」** 矩阵与 chain 点名七个。课误区 1 覆盖。
20. **「start 里只要有一件失败，HTTP 就进未知异常口袋。」** `process` 吞异常；`start` 仍返回 batchId。课正例 6 实际跑了这条，正文没有写成反例。
21. **「整单抛错的 HTTP 状态是 500。」** JSON `code=500`，HTTP 200。课写「500 口袋」易听成状态码。
22. **「枚举有 15 个名字。」** 14。课词汇表写错。
23. **「GET items 在复制期间一定看到 RUNNING。」** claim SQL 是 RUNNING；随后 `updateById` 可能写回 PENDING。课把 RUNNING 当占位，没写生产可见性。

问：读完后仍说不出的，是不是「七个方法名」，而是 **五步 vs 七窗以谁盖章、start 件失败仍 200+batchId、CLEANUP_ELIGIBLE 源还在、rollback 留两份字节、删源不是 execute、整单抛错是 JSON 500 不是枚举名**？

## Q4 视角

- objective_id: OBJ-28
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-28 vs L-025/L-026/L-027/L-029；API-005；DML 菜单；OpenAPI；MinIO 容器
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **`A:SysOssMigrationController.batch, items, dryRun, start, retry, rollback, cleanup`**？

| 视角 | 多出来的边 | 是否本课本格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 七方法独行 = 磁盘七映射；OBJ 写五步、矩阵写七窗；课已拆「动手五 + 观察二」；(b) 无 process 行；(c) MinIO/OSS / MySQL 仍 uncovered；(d) 无迁移流 | **是** 闭合定义。七窗可单独 covered。不要发明 (b)，不要把 MinIO 容器标 covered，不要把直传 (d) 算进来 |
| 搬家柜台（本 Controller） | 七扇：路径/动词/四权限/五条 POST+`@Log`+不存请求体/两 GET 只读；无 list-all、无 PUT/DELETE | **是** (a) 逐方法 |
| 编排服务 | dry-run 零写入；start 再预检+同步 process；retry/rollback/cleanup 三把扳手；`refreshBatch` 口径 | **是** 七窗的 explained 副作用。不要另开 Service 行 |
| 目录指针 | CAS `sys_oss.service` + `delete_state='ACTIVE'` | 作为 start/retry/rollback 的写点 **是**。`ISysOssService` 全表 **否**（L-026） |
| 双 Bucket | copy+matches；rollback 留目标；cleanup 只删来源 | 作为五步碰哪只桶 **是**。MinIO 容器 / Policy 怎么配 **否**（运维手册 + (c)） |
| 配置柜（L-025） | 目标必须 PUBLIC_READ SERVING；来源 PRIVATE SERVING；活动迁移占用 readiness | 预检 `requireRoute` **是**。配置 CRUD / 切换副作用 **否** |
| 对象清单（L-026） | `fileName`、`deleteState`、`SysOssController.remove` ≠ cleanup | objectKey / ACTIVE 闸 **是**。list/downloadUrl/remove 全表 **否** |
| 直传 Ticket（L-027） | 另一块牌子 `/resource/oss` 上传窗 | **否**。负空间一句即可 |
| 浏览器工厂（L-029） | `createOssUploadClient`；OpenAPI 有七条路径无工厂 | 负空间 **是**。直传客户端全表 **否** |
| 菜单种子 | 文件管理有 oss/ossConfig，无 ossMigration | 口试按注解不按菜单 **是**。不发动补种子 **否** |
| API-005 | 五条变更窗 POST+`@Log`；GET 只读；`isSaveRequestData=false` | 与规范同向 **是**。不发动传输 Ratchet **否** |
| MySQL 8.4 / L-085 | 两张迁移表、唯一键、`dry_run` 列默认与实体不一致 | 本课用到的表名/键 **是**。基座容器 **否** |
| 全局异常口袋 | 整单 `OssMigrationException` → 未知异常编号 | 失败句 **是**。不要发明业务码合同 |

问：Lead 若凭「七个方法名都在函数表里」把这一行 (a) 标 covered，是否还要求读者能指着 **dry-run 零写入、start 同步且件失败仍返回 batchId、成功停在可清理、rollback 不删字节、cleanup 要第二张批准、整单抛错进未知异常口袋**？这些是不是已经在函数表/反例里，而不需要 (b) 新行、也不需要把 MinIO/OSS 标 covered？

## Q5 元问题

- objective_id: OBJ-28
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；单一 C4 容器 `wta-system` 的一份 Controller
- difficulty: low

建议：`defer`

原因：

- **`A:SysOssMigrationController.batch, items, dryRun, start, retry, rollback, cleanup` 与矩阵 (a) 该行 1:1。** L 合同章节齐全（直觉、定义、机制链 1–7、带 caption / 文字等价物 / 图边界的两张 ASCII、正反例、变式、误区、来源表）。**主叙事站得住且与源码对齐**：七扇窗与磁盘同名；classic 无 UseCase；四颗权限；dry-run 零写入；start 先预检再同步 process；复制先于 CAS；访问失败拨回；成功停在 `CLEANUP_ELIGIBLE`；retry 跳过清理失败；rollback 只拨指针、零 delete；cleanup 要 `approved`+窗口才删来源；无 list-all；菜单无 `system:ossMigration:*`；OpenAPI 有路径无前端工厂；整单抛错无专用 Handler。不必为「窗口名字」再挖五问。
- 本批可粘贴的精度项已经落在课的表/反例/正例 6，或只需半句、不挡住把**本行**标 covered：（1）`OssMigrationError` 是 **14** 不是 15；（2）未知异常口袋是 HTTP 200 + JSON `code=500`，不是 HTTP 500、也不是枚举名；（3）`start`/`retry` 件失败不抛，仍 `R.ok(batchId)` / `R.ok()`；（4）claim SQL 的 RUNNING 可能被随后 `updateById` 写回 PENDING，口试不要保证 `GET items` 看见 RUNNING；（5）`createBatch.startedTime` 用 `Instant.now()` 不是注入 Clock；（6）cleanup 缺的是字段 `approved`，缺整个 body 是另一只校验口袋。不要把直传 Ticket、对象 CRUD、配置切换、`createOssUploadClient`、MinIO 容器塞进 38 分钟课。
- 不建议 `mine-more`：b01 未证明本行还有真实矩阵空洞；再五问只会把 `process` 内部、MinIO Policy 或 L-029 工厂再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；半句数错与信封精度可选补丁，不是第二容器。不建议 `split-lesson`：未满 10 问；七方法是 chain 原设计的同一 OBJ-28、同一份 Controller，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：claim/CAS/verifyPublic 是 (a) 的解释到副作用。不建议把 MinIO/OSS 或 (d) 直传流标 covered。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:SysOssMigrationController.batch, items, dryRun, start, retry, rollback, cleanup | 七扇窗路径/动词/四权限、dry-run 零写入、start 再预检+同步搬、成功停在 CLEANUP_ELIGIBLE、retry 不修清理失败、rollback 零 delete、cleanup 批准+窗口、无 list-all、无菜单种子、classic 无 UseCase、整单抛错进未知异常口袋——均与源码对齐。残留精度：（1）错误枚举 14 不是 15；（2）口袋是 JSON code 500 / HTTP 200；（3）start/retry 件失败仍 200；（4）RUNNING 在生产 `updateById` 后可能看不见；（5）`startedTime` 不走 Clock；（6）cleanup 缺 body ≠ 缺 approved。 | 可选半句：枚举数 14；信封说「`R.fail` + 错误编号，HTTP 仍 200」；start 返回 batchId 不等于件全绿。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-028 来源表 S-L028-01…08）。 | defer（本行可 covered；精度项不挡盖章） |
| A:SysOssConfigController.* / B:ISysOssConfigService.updateOssConfigStatus | 本课只借用 SERVING + PUBLIC_READ/PRIVATE 闸。 | 保持负空间。 | defer（L-025） |
| A:SysOssController.list, listByIds, downloadUrl, remove / B:ISysOssService | objectKey=`fileName`、`deleteState=ACTIVE`、remove≠cleanup 已划。 | 保持。 | defer（L-026） |
| A:SysOssUploadController.init, signParts, parts, complete, abort | 另一块牌子。 | 保持。 | defer（L-027） |
| A:createOssUploadClient.upload / A:useDirectOssUpload | OpenAPI 有七条路径、无迁移工厂已划。 | 保持。 | defer（L-029） |
| MinIO/OSS 容器 | 双 Bucket copy/匿名 GET 只作为五步碰哪只桶。 | 不要因本课标 covered。 | defer（(c) 容器 / 运维手册） |
| MySQL 8.4 / mysql-base | 两张迁移表、唯一键、`dry_run` 列默认已点名。 | 保持。 | defer（L-085） |
| (d) 主路径 OSS 直传 / 失败 abort | 不是迁移流。 | 不要把本课 ASCII 当成 (d) 新行。 | defer（L-027 / L-029） |
| (b) process / claim / CAS / verifyPublic | 矩阵无此行。课已把副作用写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| 菜单 `system:ossMigration:*` | 种子不存在是负空间事实，不是漏讲的 HTTP。 | 口试按注解。不发动补 DML。 | defer（未入库 / 产品缺口） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本行无 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出七扇窗目的、主路径（dry-run→start→CLEANUP_ELIGIBLE）与失败路径（预检红灯、ACCESS_VERIFIED 拨回、cleanup 未批准/窗口、整单未知异常口袋）；把可选半句写进原课不会超过 40 分钟；配置/对象/直传/前端工厂/MinIO 容器已有后续课或范围外 defer。满 10 问前不拆。七个方法仍是同一 `wta-system` 容器的一份 Controller。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-025 配置、L-026 对象、L-027 直传、L-029 工厂、MinIO/OSS 容器、L-085 基座、未入库 (b)/(d) 以及菜单种子显式建议 `defer`；对本课一行 (a) 建议 Lead 标 `covered`（证据锚本 probe 与 L-028 来源表）。本课不主张改 MinIO 容器、MySQL 基座、直传数据流或任何 (b)(c)(d) 格子。
