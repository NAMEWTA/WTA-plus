# T35 current-candidate acceptance plan

Pending implementation handback. This document is a plan, not evidence of passing checks.

1. Confirm the sole writer has released all product paths. Review the exact diff against 8b758ea8074a63835659b9731e3a4c74c5c029c5 and the revision152 20-entry write set; inspect new tests and changes to public semantics. Preserve all failed development runs, especially the initial missing-idempotency-store fixture and the controlled empty-snapshot regression under the corrected fixture.
2. Finish product and governance preflight, then create a nonempty implementation commit using existing authorization. Record exact full HEAD, tree and clean all-untracked status before acceptance. Never create an empty implementation commit or label development test runs as clean acceptance.
3. Run affected Notify module tests, all common Dispatcher/idempotency/attachment/caller unit tests, SMS provider regression and LogSanitizer/real HTTP-operation sink tests with explicit class selectors. The wta-admin reactor compiles current cross-module consumers. Collect fresh exact Surefire XML, command, timestamps, positive counts and zero skips for the selected tests; document unrelated conditional integrations as excluded, not executed.
4. Run the owned MySQL/Redis driver against the fixed HEAD, with one fresh fork per selected class. The selected NotifySmsDispatchIntegrationTest must have positive executed tests and zero skips. Verify production quota, real request submission/result transactions, safe snapshot, preflight zero writes/quota/provider, send-time local FAILED/DONE, provider I/O ambiguity UNKNOWN/WAITING_RECEIPT, duplicate submission, REDACT query/monitor/receipt safety, FULL preservation and internal provider-ID retention. Only the physical supplier is replaced. Driver must confirm cleanup and unchanged clean HEAD; never use production services.
5. Run applicable static gates: engineering skill facts, backend Notify layered architecture, agent handbook/links where new paths affect inventory, and SpecDev implement validation. OpenAPI signatures are unchanged; confirm generated contract unchanged and existing callers compile. No frontend product changes are expected.
6. Obtain two independent fixed-source reviews, functional/transactional and security/contract. Implementation-time reviews remain separate. Correct any blocker and repeat only affected gates with a new immutable candidate if necessary; integration attempt limit is 3.
7. Record clean after-acceptance identity, exact commands/results, actual Skill executions, independent review verdicts and owned-resource cleanup. Then write canonical and dated T35 evidence, update ticket/map/plan/status to done with immutable result SHA, and commit governance. T36 receives the new clean parent only after this exit.
