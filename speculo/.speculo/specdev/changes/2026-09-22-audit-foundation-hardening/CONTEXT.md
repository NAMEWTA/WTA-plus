# 本 change 领域语义

持久收件箱是按登录用户读取的数据库事实；实时消息仅是刷新提示。通知意图 QUEUED 表示提交，不表示供应商接受；ACCEPTED 不表示送达。外部 UNKNOWN 表示无法断定副作用，不能自动重发；IN_APP 幂等数据库写入不产生等待外部回执的状态。

存储诊断为运维证据，不等同于对象授权或当前可用性。不可读取桶策略是未知，不是已验证私有。恢复私有指针与源对象清理必须共享对象级互斥。生产默认不接纳未知跨源凭据请求。
