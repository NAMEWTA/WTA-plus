# Evidence T-04 — sso-web password + Set-Cookie session

- Goal: AC-016 / AC-020. sso-web accepts only warehouse username/password; backend `POST /sso/login` Set-Cookie `Sso-Token` HttpOnly; no Mask/GitHub; no JS storage as SSO session.
- Date: 2026-09-13

## Commands (run twice)

```bash
export JAVA_HOME=/home/box/deps/jdk/jdk-21.0.12.1+1
cd /workspace/vp-dev/WTA-plus/backend
./mvnw -pl wta-modules/wta-sso -am test -Dtest=SsoSessionServiceTest,SsoSessionCookieTest -Dsurefire.failIfNoSpecifiedTests=false
cd ../frontend
pnpm --filter @namewta/sso-web test
```

| Pass | Exit | Results | Log |
|---|---|---|---|
| 1 BE | 0 | Tests run: 2 | `/tmp/grok-goal-0577f1e6ed19/implementer/sso-r4-t04-unit-1.log` |
| 2 BE | 0 | Tests run: 2 | `/tmp/grok-goal-0577f1e6ed19/implementer/sso-r4-t04-unit-2.log` |
| 1 FE | 0 | sso-web 3 passed | `/tmp/grok-goal-0577f1e6ed19/implementer/sso-r4-t04-fe-1.log` |
| 2 FE | 0 | sso-web 3 passed | `/tmp/grok-goal-0577f1e6ed19/implementer/sso-r4-t04-fe-2.log` |

## Shipped code driven

- `SsoSessionService.login` verifies warehouse password and stores SSO session; `buildLoginUser` is not called (no business token).
- `SsoSessionCookie.create` → name `Sso-Token`, HttpOnly, path `/`, SameSite Lax.
- `SsoSessionController.login` writes that cookie via Set-Cookie; `@Log` excludes password.
- `ssoLoginMethods === ['password']`; `loginWithPassword` POSTs `/sso/login` with `credentials: 'include'`; authorize query has no `sso_secret`.

## Residual

Live cookie in DevTools is T-07. No push / no CR.
