# T50 final verification sequence

A1 9bb2e28 real135/1failure retained. A2 1dfdcbc real135/0/0/0, default870 executed/197skip, full bundle and real HTTP capture passed, but raw schema required mismatch prevented final closure. A3 0b33ff3 adds only explicit existing-priority required metadata and schema assertion, unchanged dispatch/runtime/callers.

A3 default/full are being rebuilt due to this public-contract delta. Capture its immutable full JAR before core clean; inspect real raw priority.required and ALL/ASYNC/0 descriptions. Generate snapshots through formal fetch/generate/check only. Final B contains only generated frontend contract changes on A3; verify backend tree equivalence explicitly before attributing A3 default/full results. Final B must run eight required real classes, frontend gates, core bundle/static gates, OpenAPI check, and final reviews. Source A3 is a build/capture checkpoint, not an additional falsely claimed eight-class acceptance. Formal eight-class runs are A1, A2, then B; if the third fails, review root cause and recovery explicitly before any more attempt.

No release/deployment/production queue operation/archive is authorized by this verification sequence. The user-confirmed security owner revocation disposition is already preserved.
