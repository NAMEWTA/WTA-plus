# T-34 specification review — pass (source review)

Fixed base `6fcbfeb50747b67bcaf4bf9af1d964cc7d760c04`; fixed head `24909df0e49208535097f03d174c0592ea6836d3`; merge base equals base. Reviewed the three-point `git diff base...head -- frontend` and commits `d3549fb5`, `a94f7707`, `24909df0`. This is an isolated specification axis; no standards-axis findings were read. I ran no tests/build/E2E/services and changed no repository files.

Sources: this change's `spec.md:97` (AC-034), `ticket/34-inbox-without-realtime.md:34-35,68-85,97-105,123-125,139-143`, `ADR.md:1-24` (full pagination owned by T-41), and `tickets-map.md:77,152,217-221` (T-34 dispatch and advance write-set declaration). Fixed commit log contains no Issue/PR reference; no remote source used.

The prior medium finding S34-1 is **closed in this fixed head**. `frontend/apps/admin-web/src/layout/components/Navbar.vue:132-134` now calls `initMessageBox(true)` when the user opens the box. Existing `frontend/apps/admin-web/src/utils/push.ts:146-150,122-129` marks an in-flight same-token request dirty, suppresses its older result, and issues one follow-up GET after it settles; passive simultaneous calls remain coalesced and no polling is added. `frontend/e2e/inbox-without-realtime.spec.ts:150-165` controls the first response as an empty old snapshot and requires the notice from the second request to become visible, with exactly two inbox calls and no push request. The same test file's failure/retry scenario now allows that deliberate follow-up GET to fail before the explicit retry succeeds (`:41-45,70-91`). `ticket/34...md:20-24` and `tickets-map.md:221` declare Navbar as the narrow additional write set before the product change.

The full diff also separates REST inbox loading from the realtime flag (`push.ts:76-79,132-150`), guards old identity successes/failures by token/request id (`:102-129`), clears when token is absent (`:133-139`), and renders distinct loading/error/empty states plus one summary body (`notice/index.vue:14-39`). Its read/detail callbacks check identity and unmount (`:80-87,113-149`). The two OSS generic comments are within the explicit typecheck prerequisite and do not implement AC-047 behavior. I found no remaining source-level AC-034 behavior or write-set violation in this fixed diff.

**Evidence still required outside this source review:** the Lead's pending green controlled-browser result and a real-Admin run in an environment with backend realtime disabled. `frontend/e2e/inbox-real.e2e.ts:9-47` asserts a real inbox response and zero push requests, but source code alone cannot prove that service setting or test execution. This pass is not a claim that required E2E or quality gates passed.
