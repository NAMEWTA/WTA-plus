# T-41 private real-browser runner review

Review input fixed 2026-09-23: `/tmp/wta-t41/browser-prep/run-inbox-paged-real.py` SHA-256 `b63eedf6ddaa7e9d32dccde751482029a0d7b9afc936da6e1d97b89584df6cf5`; `test_run_inbox_paged_real.py` SHA-256 `4892233f0621601613242dd76fbdc0fb71fef8a043ff7acda6e355e766581aa8`; `README.md` SHA-256 `38dfc6e0c9337a46ad42763f312cc8a9e84915428e0f151df442c1e6986a063e`. Scope is private driver safety only; the product Playwright spec/config and final T-41 source do not yet exist. I did not run Docker, Maven, browser, Java, or the offline tests. Ops' retained `offline-safety-v6.log` records 18 tests, 0 failures/errors, and py_compile exit 0 was reported by ops.

Verdict: **pass for handoff to the sole product writer, with final spec/config and owned run still required**. Two deterministic draft defects were found before freeze and are now closed in this exact hash:

- The six-SQL baseline deletes legacy role `1761300000000000004` and test1's membership (`50-cde-base-dml.sql:429-431`, `520-528`). The final runner instead creates one random role in the freshly owned schema, binds it to test1, restricts it to the Admin Client and data scope 5, grants the four existing inbox/menu/seen/read IDs, and checks effective role/menu/client joins (`run-inbox-paged-real.py:496-531`, `796-808`). Passwords are independently generated, BCrypt `$2a$` hashed, and written only to these two fresh DB users (`:99-120`, `:802-804`). Actual HTTP login remains for the later Playwright case to prove.
- The first draft checked only A/B unread counts and could pass a timestamp overwrite. Final seed creates 501 A relations (481 unread), two B relations, 502 messages and 503 relations, with a B-only ID interleaved at the page-26 edge (`:371-421`). A's 20 pre-read timestamps and oldest off-page seen timestamp are one day older than creation. Final before/after snapshot compares all of them and both B rows' seen/read values exactly (`:470-493`, `:809-812`, `:890-895`); count check additionally requires A 501/0 unread and B 2/2 unchanged (`:424-467`). This is discriminating even if the UI action happens in the same second.

Safety and provenance checked:

- Launch requires `--execute`, exact clean HEAD, expected full JAR SHA and package proof (`:643-658`). Proof binds a successful full clean package command, same HEAD/tree, JAR hash/size/mtime and hashed successful build log; full bundle ZIP membership and `verify-admin-bundle.sh full` are checked before any service (`:321-363`, `:683-695`). Source/JAR are checked again after cleanup (`:958-970`). A future proof must actually be generated at the final clean source; historical T-50 proof cannot satisfy this equality.
- All containers use pinned local images and random loopback port publishing, exact owner/run labels and full 64-digit IDs (`:40-43`, `:130-198`, `:706-757`). The initializer receives only the captured owned MySQL ID and six current SQL files, creates a DB-scoped application user, and proves an empty external-send baseline (`:759-788`). Cleanup verifies process groups, both-label container discovery, captured anonymous volumes, all registered ports, and source/JAR identity; any error makes acceptance false (`:906-974`). No deployed service or `/srv/ops` credential is referenced.
- The `dev` profile overlay points JDBC, Redis, MinIO DB configuration, SSO/CORS, Vite proxy and known active integrations to this loopback run; it disables boot-admin, Snail, Nacos, MCP client, Easy-ES, captcha and realtime (`:562-605`, `:822-865`). `cookie-secure=false` is restricted to the explicitly launched `dev` profile by SSO startup validation. This is an owned-fixture configuration review, not a general network-deny guarantee for every optional code path.
- Secret values are generated in memory, passed to Docker via 0600 env/config files, to Java via a 0600 overlay path, and to Playwright only in child environment. They are absent from these process argv (`:663-680`, `:706-767`, `:822-879`). The retained Playwright JSON is a whitelist of source reporter hash, exact counts and case identity; raw reporter, raw process logs, browser artifacts and private env/config files are removed in `finally` (`:878-938`). Result/seed files are private. The final spec itself must not log tokens or request bodies.
- Reporter parsing requires exactly one case from `frontend/e2e/inbox-paged-real.e2e.ts`, exact title `T-41 real Admin inbox page 26 and global read-all`, Chromium, one passed attempt, and stats expected=1/unexpected=skipped=flaky=0 (`:534-561`, `:608-625`, `:873-898`). The final four-line increment at `:881-885` retains the four raw counts in the private result before applying the same strict parser, so a skipped/failed case stays rejected while its count is visible. The final spec/config must be inspected directly: the current regex guard for trace/video/screenshot and storageState/HAR is only an additional static check, not a complete parser or a substitute for source review.

Outstanding acceptance boundary: `frontend/e2e/inbox-paged-real.e2e.ts` and `playwright.inbox-paged-real.config.ts` are absent at this review. Thus `--preflight` is expected to return 1 and no browser proof exists. The writer must freeze the real UI selectors, A/B separate sessions, page-26 HTTP/visible row, global read-all, owner-specific negative HTTP+DB checks, no push requests, and recorder-off config before Lead builds and executes the exact clean candidate. The runner's SQL checks cannot substitute for those UI/HTTP assertions. No production configuration or service was changed by this review.
