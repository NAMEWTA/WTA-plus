# T40 frontend F4 fixed-source security / implementation review

**Verdict: bounded static PASS; no F4 frontend blocker found.** Browser and real-service acceptance remain separate Lead-owned evidence. This report examines fixed commit `d4000a859ead9394b189364952321a094f16030e` only. Its parent `461b0a43e77e649138066a5bab6f0e3c6217217a` has byte-identical `frontend/`; the T40 frontend implementation began at `eb8cefe0`. Current mutable E2E files were not read.

## Source conclusions

- `frontend/packages/domains/notify/src/inbox/path.ts:3-29` accepts only one positive signed-64 decimal ID, rejects arrays, leading zeros and unsafe JS numbers, and maps a historical `/notify/notice?noticeId=...` path only through the current owned `messageId`. A new inbox link must contain that same owned ID; matching current query is suppressed to avoid self-navigation. There is no management-page fallback. `path.test.ts:4-23` covers these boundaries. The transport (`transport.ts:23-43,81-97`) applies the same ID check to wire rows and detail requests.
- The Admin host route port (`adminManifestRegistry.ts:274-334`) snapshots an exact `/notify/inbox` route, sends same-page fullPath query changes synchronously, and cancels a lazy router import's watcher on unsubscribe. Its session epoch advances on token, generation, user ID or identityLoaded changes; a generation-only logout transition suspends the old identity. Fixed tests (`adminManifestRegistry.test.ts:80-134`) cover lazy cancellation and this pending-logout transition.
- `InboxPage.vue:116-136,149-169,215-340` binds list/detail/read mutations to a page-active session epoch plus request generations. A route update closes the previous detail before fetching; malformed IDs never reach the detail service; successful content arrives only from the owned detail API. Deactivation, unmount, page changes and session changes invalidate late responses. Error and retry paths have terminal loading states. Its SFC tests (`InboxPage.test.ts:124-309`) cover the relevant old-success, old-error, KeepAlive, query-update, unauthorized/not-found and recovery cases. Read controls are permission-gated; backend authorization remains authoritative.
- The topbar (`layout/components/notice/index.vue:95-116,142-211`) clears selected detail synchronously on session/notice identity changes and gates late detail/readAll responses before changing state. It derives business navigation only from the detail response path plus the current owned item ID; the corresponding tests (`index.test.ts:68-152`) exercise late success, generation-only logout, read failure and historical/new links. The user-store logout path (`store/modules/user.ts:63-99`) eventually clears notices; a generation-only pending remote logout immediately clears the detail. No F4-specific cross-identity content writeback was found.
- `NoticePage.vue:373-397` tells the operator that retraction stops only same-version work that has not started sending; accepted, unknown and delivered outcomes cannot be recalled. The action continues to use the existing retract API and refreshes on success.

## Verification provenance and limits

- Retained `frontend-f4-target-test.json/log` reports exit 0 with domain notify 7, web-domain notify 25 and Admin 80 tests passed, zero reported skips; `frontend-f4-host-test.json/log` separately reports the 6 host-registry tests passed; three-package typecheck exited 0. These are the records under `evidence/T-40-current-2026-09-23/frontend-f4/`.
- `frontend-f4-source.json` records parent `461b0a43...` with `clean:false`. Git diff from that parent to fixed F4 is empty under `frontend/`, so the committed frontend implementation and tests are identical. The record is **not** an exact-clean F4 workspace capture; it cannot establish that unrelated dirty/untracked files were absent during that test invocation. It also does not establish a browser run or a real-service deep-link journey.
- Browser acceptance still needs to exercise a current notification link, an older owned message whose stored path points to the retired management page, a foreign/nonexistent ID with the same failure shape, same-page query changes, and session switch with a pending detail. This is a validation limit, not a source-code blocker in the fixed F4 diff.

No repository file, test, build or service was changed or run by this review.
