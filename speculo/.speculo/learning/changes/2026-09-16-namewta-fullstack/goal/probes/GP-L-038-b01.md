---
artifact: learning-goal-probe
probe_id: GP-L-038-b01
lesson_id: L-038
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-038 / b01

审问对象是已写成的 Lesson `lessons/L-038-person-verification.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止 `Q-question 问答目录`、learner 协议字段、`verdict`。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain / `claimed_cells` / 矩阵 (a) **一行**）：`A:PersonVerificationAnonymousController.callback`。OBJ-38 原文：能口述 `PersonVerificationAnonymousController` 供应商回调。档是**解释+走查**。chain 覆盖列只写这一格。矩阵 (a) 符号就是 `PersonVerificationAnonymousController.callback`，模块 `wta-profile-person`，仍 `uncovered`，与磁盘唯一 `@PostMapping` 1:1。矩阵 (b) **没有**本课符号；通知格 `ProviderCallbackController.callback`（性状「幂等回调」）是另一模块。矩阵 (c) `wta-modules` 业务房间已 covered（L-002/L-003）；MySQL 8.4 基座仍 uncovered。矩阵 (d) **没有**核身回邮筒流；「主路径 profile 申请」=`PersonApplication.submit → admin decide` 已由 L-035+L-034 covered。本课 `claimed_cells` **未**列 (d)。`startAttempt` 无本课 HTTP。企业对称窗留给 L-041。厨房 / `PersonVerificationPage` 留给 L-039。本批按派单审问 **这一扇匿名 POST**。

抽查锚点（2026-09-16）：`backend/wta-modules/wta-profile/wta-profile-person/src/main/java/org/namewta/profile/person/controller/anonymous/{PersonVerificationAnonymousController,PersonVerificationCallbackExceptionHandler}.java`；`usecase/PersonVerificationUseCase.java` 与 `usecase/impl/PersonVerificationUseCaseImpl.java`；`service/PersonVerificationAttemptService.java`；`port/verification/PersonVerificationService.java`；`port/provider/{PersonVerificationProvider,PersonVerificationProviderRegistryPort}.java`；`adapter/provider/{PersonVerificationProviderRegistry,PersonManualVerificationProvider}.java`；`adapter/codec/PersonVerificationEvidenceCodec.java`；`adapter/time/SystemPersonVerificationTimeSource.java`；`support/PersonVerificationTimeSource.java`；`config/PersonVerificationProviderProperties.java`；`dao/PersonVerificationAttemptDao.java`；`mapper/PersonVerificationAttemptMapper.java`；`src/main/resources/mapper/person/PersonVerificationAttemptMapper.xml`；`domain/verification/{PersonProviderCallbackEnvelope,PersonVerificationCallbackOutcome,PersonVerificationFailureCategory,PersonVerificationAttempt,PersonApplicationVerificationState,PersonVerifiedCallback,PersonProviderStartResult,PersonProviderAttemptStatus}.java`；`src/test/.../controller/anonymous/PersonVerificationAnonymousControllerTest.java`；`src/test/.../service/impl/{PersonVerificationCallbackContractTest,PersonVerificationMySqlE2ETest,PersonVerificationProviderRegistryTest,PersonVerificationAttemptCoordinatorTest,PersonDeterministicTestProvider}.java`；`architecture/PersonModuleArchitectureTest.java`；`service/PersonApplicationService.java`（仅 `startVerificationAttempt` 边界）；`controller/self/PersonApplicationExceptionHandler.java`（对照）；企业 `EnterpriseVerificationAnonymousController.java`（仅门牌）；通知 `ProviderCallbackController.java`（仅对照）；`10-cde-base-ddl.sql` `profile_verification_attempt` / `profile_operation_audit`；`50-cde-base-dml.sql` 默认店与启用键；`frontend/packages/api-contracts/generated/openapi.ts` vs `openapi/current.json`；`frontend/packages/web-domains/profile/src/self/PersonVerificationPage.vue`；登记表 `03-backend-module-modes.md` layered 行。

## Q1 澄清

- objective_id: OBJ-38
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-38；chain L-038 覆盖列只 `A:PersonVerificationAnonymousController.callback`；L-038 `claimed_cells` 一格；矩阵 (a) 该行仍 uncovered；(b) 无本课符号；(c) profile 房间已 covered、MySQL 8.4 未 covered；(d) 无核身回调流、申请主路径已 covered；课开篇「一块门牌、一扇窗」+ 负空间 L-034/L-035/L-039/L-041/OBJ-50
- difficulty: medium

课把核身河收成「供应商投进匿名回邮筒」。计划里至少有五套可数对象，派单点名的是 **(a) 这一扇**：

1. **OBJ-38 原文。** `course.md`：能口述 `PersonVerificationAnonymousController` 供应商回调。点名的是类 + 回调，**没有**把 `startAttempt`、企业窗、申请页 `PersonVerification`、通知 `ProviderCallbackController` 写成口试清单。
2. **chain / 课 `claimed_cells`。** 一格：`A:PersonVerificationAnonymousController.callback`。不写 (b)/(d)，不写 UseCase/Service 符号。
3. **矩阵 (a) 一行。** 符号与磁盘唯一公开映射 1:1：`POST /profile/person/verification/providers/{providerCode}/callback`。Lead 若按行标 covered，**不会**把 `startAttempt`、企业 callback、通知 callback 假闭合。
4. **课自己的操作定义。** 调用方是供应商机器。方法 `@SaIgnore`。Controller 只折四字段信封 + 墙上钟 + `R.ok`。UseCase 只盖 `@DSTransactional`。成功 data 只可能 `ACCEPTED` / `IDEMPOTENT` / `LATE_IGNORED`。complete 只改 `profile_verification_attempt`，申请仍 `WAITING`。默认店 `manual` 拒收。
5. **邻格。** `startAttempt` 在 submit 事务里（L-035）。档案发布 / decide 在 L-034。厨房与 `PersonVerificationPage` 在 L-039。企业匿名窗在 L-041。通知邮筒在 OBJ-50。L-003 已认 `wta-profile` = layered。

问：本格 (a) 闭合标准是「一扇 POST + `@SaIgnore` + 信封/墙上钟 + 三种 200 口吻 + 假签/过期 401 其余拒收 400 + complete 只动尝试行且不发布」，还是 OBJ 那句必须把 `startAttempt` 升成第二格、或必须发明一条 (d) 核身流 / 把通知幂等 (b) 并进本行才盖章？Lead 标 covered 是否只要这一扇磁盘映射，而不是把企业窗、申请页或 `createProfileService` 算进来？

## Q2 证据

- objective_id: OBJ-38
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 26–72；Handler 11–28；UseCase 11–18；UseCaseImpl 13–32；Service 35–52 / 58–76 / 82–125 / 129–137 / 168–180 / 233–239；Registry 15–61；Manual 12–30；Properties 11–26；TimeSource 9–14；SystemTimeSource 8–16；Envelope 4–8；Outcome 4–7；FailureCategory 4–19；Attempt 23–59；ApplicationState 13–26；StartResult 16–20；VerifiedCallback 7–22；Provider 10–27；RegistryPort 8–11；Codec 6–35；XML 7–20 / 46–56 / 58–72 / 74–84；Dao 19–63；DDL 1189–1214 / 1241–1266；DML 1429–1431；ArchitectureTest 30–48 / 92–136；ControllerTest 36–83；ContractTest 36–109；E2E 58–111；RegistryTest 29–54；CoordinatorTest 文件内 `PersonVerificationAttemptServiceTest` 34–71；DeterministicTestProvider 22–70；ApplicationService 157–159 / 772–788；ApplicationAdvice 9–16；Enterprise 26–61；Notify callback 15–40；Page.vue 7 / 75；openapi.ts 1972–1985 / 9655–9667；`openapi/current.json` 修订指针；R.fail 153–155 + `HttpStatus.ERROR=500`；登记表 layered 行
- difficulty: high

课的机制链哪些能在工作树直接复现，哪些只是图/来源表比磁盘窄？

**仍能复现（本格 (a)）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 类上 `@RequestMapping("/profile/person/verification/providers")`；唯一 `@PostMapping("/{providerCode}/callback")`；无 GET/DELETE/start 窗 | Controller 26–48 |
| 方法 `@SaIgnore`；`@Log` 标题「个人认证供应商回调」、`BusinessType.OTHER`、不记请求体/响应体；返回 `R<PersonVerificationCallbackOutcome>` | Controller 45–49；ControllerTest 55–57 反射钉 `SaIgnore` |
| Controller 只持 UseCase + `PersonVerificationTimeSource`；方法体四步：校验 `CallbackRequest` → 折 `PersonProviderCallbackEnvelope` → `useCase.callback(..., timeSource.now())` → `R.ok(outcome)`；路径 `providerCode` 无 `@Pattern` | Controller 30–31 / 49–60 / 66–71 |
| 信封：`providerRequestId` `@NotBlank` `@Size(max=128)`；`timestampEpochSecond` `@Positive`；`payload` `@NotBlank` `@Size(max=1048576)`；`signature` `@NotBlank` `@Size(max=1024)` | Controller 66–71 |
| 生产钟 `SystemPersonVerificationTimeSource.now()` = `Instant.now()`；HTTP 测把钟钉死 | SystemTimeSource 8–16；ControllerTest 50 / `() -> NOW` |
| UseCase 接口只有 `callback`；实现 `@DSTransactional` 转 `PersonVerificationService.handleCallback`；Controller 无事务注解 | UseCase 16–18；UseCaseImpl 17 / 27–31 |
| 端口 `PersonVerificationService` 才有 `startAttempt` + `handleCallback`；生产实现 `PersonVerificationAttemptService` | Service 端口 18–25；AttemptService 29 |
| 生产三参 `@Autowired` 不收测试 Recorder；四参把第四个标成 `ignoredAuditRecorder` | AttemptService 35–52 |
| `handleCallback`：`requireEnabled` → `authenticate`；失败 `recordSecurityAudit(null, category)` 再抛；成功才 `lockByProviderRequest` | AttemptService 86–98 |
| 申请 `providerCode` ≠ 路径店 → `PROVIDER_MISMATCH`；申请终态 → 审计 `LATE_CALLBACK` 返回 `LATE_IGNORED`；非 PENDING 且 `sameCallback` → `IDEMPOTENT`；否则 `CONFLICTING_CALLBACK`；仍 PENDING → `complete` → `ACCEPTED` | AttemptService 101–124 |
| `sameCallback` 比 digest / status / normalized JSON / evidence JSON / errorCode | Attempt 54–59 |
| 终态集合 `FINISH` `INVALID` `TERMINATION`；`WAITING` 不是终态 | ApplicationState 13–26 |
| `completeAttempt` WHERE `profile_type='PERSON'` 且 `status='PENDING'`；更新行数 ≠ 1 → `PROVIDER_FAILURE`（pending fence） | XML 58–72；AttemptService 171–179 |
| `lockByProviderRequest` `FOR UPDATE` 且 `profile_type='PERSON'`；`lockApplication` join 当前 `submission_seq` 也 `FOR UPDATE` | XML 7–19 / 46–56 |
| 审计：`LATE_CALLBACK` 的 `result=IGNORED`，其余 `FAILED`；`operator_user_id=0`；`capability='profile:person:providerCallback'`；`operation_type='PROVIDER_CALLBACK'` | AttemptService 235；XML 74–83 |
| 证据 codec `schemaVersion=1` + digest + providerEvidenceJson；旧明文 decode 失败整段当证据 | Codec 7–34 |
| 注册表编码正则 `[a-z0-9][a-z0-9_-]{0,63}`；找不到 Bean → `UNKNOWN_PROVIDER`；不在 enabled 集合 → `DISABLED_PROVIDER`；构造期同名 Bean → `DUPLICATE_PROVIDER`；无 fallback | Registry 15 / 27–50；RegistryTest 29–43 |
| Spring 属性 `profile.person.verification.enabled-providers` 默认 `{manual}` | Properties 11–14 |
| `manual.authenticate` 永远 `UNSUPPORTED_CALLBACK`；`start` 回 `pending()`（单号 null） | Manual 16–29；StartResult 24–26 |
| 测试店 `test-provider`：HMAC-SHA256、规范串 `id.timestamp.payload`、`MessageDigest.isEqual`、`|skew|>300` → `EXPIRED_CALLBACK`、坏 hex → `INVALID_SIGNATURE`、通过后**无论 payload 都是 `SUCCEEDED`**，证据 `{"provider":"test-provider"}` 无 signature | DeterministicTestProvider 22–69 |
| Handler 只罩本类；`INVALID_SIGNATURE`/`EXPIRED_CALLBACK` → HTTP 401；其余 `PersonVerificationException` → 400；msg 固定英文 `Person verification callback rejected`；类别在 `data.category` | Handler 11–23；ControllerTest 79–83；E2E 93–96 |
| HTTP 成功：ACCEPTED 再 IDEMPOTENT 仍 200；冲突 400 `CONFLICTING_CALLBACK` | ControllerTest 64–73；E2E 81–96 |
| E2E：尝试 `SUCCEEDED`、申请仍 `WAITING`、尝试行数 1、冲突审计 1、存库证据不含 `signature` | E2E 99–111 |
| 合同四测：假签/过期审计且 `completeCount=0`；ACCEPTED→IDEMPOTENT→冲突且 `completeCount=1`；终态 `LATE_IGNORED` 零 complete；店名改 `other-provider` → `PROVIDER_MISMATCH` 零 complete | ContractTest 36–109 |
| 架构：anonymous 文件名含 `Anonymous` 且源码含 `@SaIgnore`；admin/self **禁止** `@SaIgnore`；Controller 只 import usecase、禁止 import `service.`；Service 禁止 import Mapper | ArchitectureTest 30–48 / 103–130 |
| `wta-profile` 登记 layered | `03-backend-module-modes.md` layered 行 |
| submit 才 `attempts.startAttempt`；失败译成 `PERSON_PROVIDER_UNAVAILABLE`，不是本课 HTTP 码 | ApplicationService 157–159 / 782–788 |
| 申请窗 Advice：HTTP 仍 200、`R.fail(msg)` 类别写在 msg | ApplicationAdvice 9–16 |
| 企业另一门牌 `/profile/enterprise/verification/providers` | Enterprise 26–48 |
| 通知邮筒 `POST /notify/callback/{channel}`、类上 `@SaIgnore`、共享 `notify.callback-secret`、失败 `R.fail(message)` | Notify 15–40 |
| 前端页文案「提交后进入审核」，`current()` 走申请厨房，不是本课 POST | Page.vue 7 / 75 |
| 生成 OpenAPI 有该 POST，data 枚举三态；`openapi/current.json` 是修订指针，正文搜不到路径 | openapi.ts 1972–1985 / 9655–9667；current.json 1–3 |
| 尝试表两唯一键：`(profile_type, application_id, attempt_no)` 与 `(provider_code, provider_request_id)`；证据列 DDL 注释写「明文」 | DDL 1200 / 1211–1212 |
| 显式重试沿用申请上钉死的店；过期提交零 insertAttempt | CoordinatorTest 34–70 |
| 失败分类十五个枚举 | FailureCategory 4–19 |
| 成功结局三个枚举 | Outcome 4–7 |
| 适配器 javadoc：authenticate 不得 publish / 改绑定 | Provider 10–15 |

**课写过、图或来源表比磁盘窄（不是证伪）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「失败 HTTP 401/400，类别在 `data.category`」 | Handler 用 `ResponseEntity.status(401/400).body(R.fail(msg, data))`。`R.fail` 把 JSON **`code` 写成 `HttpStatus.ERROR`（500）**。合同/E2E 钉的是 HTTP 行与 `$.data.category`，没钉 JSON `code`。口试若说「信封 code 也是 401」会错 |
| 配置表只写 Spring `profile.person.verification.enabled-providers` | 注册表**只**读这份 Properties。DML 另有系统配置 `profile.person.provider.enabled`（1431，备注「逗号分隔稳定 providerCode」），**Java 零引用**。不要把种子键背成注册表 |
| 图 2：`APPLICATION_NOT_FOUND` 列在「startAttempt 才抛、本课窗不走」 | `handleCallback` 在锁到尝试行之后仍调 `lockApplication`（99–100 / 129–134）。申请/当前提交找不到会抛这个码 → Handler 400。更常见在 start，但**不是**回调禁区。且这条 throw **不**走 `auditedFailure`，零审计 |
| 「`PersonVerificationAttempt` compact：PENDING 的 start 结果才允许 `completedAt==null`」 | 正/空完成时刻约束在 `PersonProviderStartResult` 16–20。`PersonVerificationAttempt` compact 只要求 `applicationId/submissionId/attemptNo` 为正（23–26）。`PersonVerifiedCallback` 反而 **强制** `completedAt` 非空（18–21） |
| 来源表 S-L038-06 写 `PersonVerificationAttemptServiceTest.java` | 磁盘文件名是 `PersonVerificationAttemptCoordinatorTest.java`；**类名**才是 `PersonVerificationAttemptServiceTest` |
| RegistryPort javadoc「适配器位于 service.impl」 | 生产实现是 `adapter.provider.PersonVerificationProviderRegistry`。课正文层图画对了；端口注释是旧句 |
| 「`openapi/current.json` 搜不到本课路径」 | 成立，因为它只有 85 字节修订指针（GP-L-005 已钉）。不要把「快照缺 path」说成生成文件也没有 |

问：在一扇 POST、`@SaIgnore`、信封/墙上钟、先验店再验签再锁单、三种 200、401/400 分家、E2E 不发布、manual 拒收已经能互证的前提下，(a) 是否还要把「JSON `code=500`」「DML `provider.enabled` 不是注册表」「回调也能 `APPLICATION_NOT_FOUND` 且该分支无审计」写成开篇半句，才算这一行讲完？还是这些已经落在课的表/反例或邻格里，Lead 按现印符号就可以盖章？

## Q3 假设与反例

- objective_id: OBJ-38
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 课反例 1–15 / 误区 1–14；Controller 无 start 映射；Manual 拒收；Handler 401 vs 申请窗 200；E2E WithoutPublishing；ArchitectureTest anonymous vs admin/self；Page.vue 申请厨房；Enterprise 另一前缀；Notify 共享 secret；Properties vs DML 启用键；R.fail JSON code=500
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪只邮筒认错？

1. **「OBJ-38 还有 `startAttempt` HTTP。」** UseCase 接口只有 callback。课误区 1 / 反例 12 覆盖。
2. **「前端 `PersonVerification` 页就是这扇 POST。」** 页走 `/profile/person/application`。课反例 6 / 误区 2 覆盖。
3. **「回调成功 = 申请 FINISH / 插入 `profile_person`。」** E2E 申请仍 `WAITING`。课反例 1 / 误区 3 覆盖。
4. **「这扇窗也要 `profile:person:apply`。」** 方法 `@SaIgnore`。课反例 2 覆盖。
5. **「失败像申请窗：HTTP 200，`msg=INVALID_SIGNATURE`。」** Handler 改状态码，msg 英文固定句，类别在 `data.category`。课反例 3 / 误区 5 覆盖。401 在这里是假签/过期，不是「没登录」。
6. **「`manual` 会按 HMAC 收 `approved`。」** `UNSUPPORTED_CALLBACK` → 400。课反例 4 覆盖。
7. **「未知店落到 manual。」** `UNKNOWN_PROVIDER`，无 fallback。课反例 5 覆盖。
8. **「Controller 自己算 HMAC，密钥在 `application.yml` / `notify.callback-secret`。」** Controller 不算。课反例 8 / 变式 E 覆盖。
9. **「第二次不同 payload 覆盖成 FAILED。」** 测试店两次都是 SUCCEEDED 意图；冲突拒绝写入。课反例 9 覆盖。
10. **「`LATE_IGNORED` 是 400。」** 它是 `R.ok` 三态之一。课反例 10 覆盖。
11. **「审计 operator 是当前登录用户。」** `operator_user_id=0`。课反例 11 覆盖。
12. **「生产装配了 `PersonVerificationSecurityAuditRecorder`。」** 只在 test。课反例 14 覆盖。
13. **「企业可打 person 路径改企业尝试。」** XML 写死 `PERSON`；企业另一 Controller。课反例 7 / 变式 G 覆盖。
14. **「OpenAPI current.json 就是权威。」** 生成文件有路径，指针文件没有。课反例 15 覆盖。
15. **「这是 classic，Controller 碰 Mapper。」** 登记表 layered。课误区 13 覆盖。
16. **「IDEMPOTENT 会再 complete。」** 非 PENDING 且 sameCallback 直接 return。课误区 6 覆盖。
17. **「终态申请抛 `APPLICATION_TERMINAL`。」** 那是 start。回调是 `LATE_IGNORED`。课误区 8 覆盖。
18. **「回调会 start 工作流。」** 门铃在 submit。课误区 10 覆盖。
19. **「300 秒窗是 Controller / UseCase 硬编码。」** 只在测试店。课类比失效 2 覆盖。
20. **「JSON `code` 随 HTTP 变成 401/400。」** 课没写。磁盘是 HTTP 行变、JSON code 仍 500。精度项，不是新窗。
21. **「DML `profile.person.provider.enabled` 就是注册表。」** 课配置表写的是 Spring `enabled-providers`。种子键无人读。精度项。
22. **「回调路径绝对不出现 `APPLICATION_NOT_FOUND`。」** 图 2 把该码划到 start。磁盘 lockApplication 共用。精度项。

问：读完后仍说不出的，是不是「类名」，而是 **供应商打这一扇、先验店再验签、三种 200 与 401/400 分家、盖章≠毕业、manual 不收邮筒、申请页不是回调窗**？JSON code=500 与未读的 DML 启用键，是不是半句精度而不是第二容器？

## Q4 视角

- objective_id: OBJ-38
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-38 vs L-034/L-035/L-039/L-041/OBJ-50；chain 只 (a)；无本课 (b)；无核身 (d)；MySQL 8.4；API-005 存量 POST
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这一格 (a)**？

| 视角 | 多出来的边 | 是否本课这一格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 一行与磁盘 1:1 仍 uncovered；(b) 无本课符号；(c) MySQL 8.4 uncovered；(d) 无核身流 | **是** 闭合定义。按现印标 covered。不要发明 (b)/(d)，不要把通知幂等 (b) 假闭合进本行 |
| 匿名窗（本类） | 一块门牌一扇 POST、`@SaIgnore`、不记体、信封四字段、墙上钟、`R.ok` | **是** (a) |
| Handler | 只罩本类；401 vs 400；英文 msg；`data.category` | **是** 本窗失败合同。不要另开 Advice 行 |
| UseCase 事务章 | 只有 callback；`@DSTransactional` 罩验签审计与 complete | **是** 解释到副作用。不要把 UseCase 升成第二格 (a) |
| 注册表 / 适配器 | 无 fallback；manual 拒收；测试店 HMAC/300 秒；不得 publish | **是** 本窗先验店再验签。生产不存在 `test-provider` |
| 锁与栅栏 | `(providerCode, providerRequestId)` FOR UPDATE；PENDING WHERE；codec 无 signature | **是** callback 的 SQL 证据。Mapper XML 行已 covered-by-parent |
| startAttempt | submit 门铃；`PERSON_PROVIDER_UNAVAILABLE`；STALE / APPLICATION_TERMINAL | 负空间一句 **是**。调用点 **否**（L-035） |
| 档案发布（L-034） | decide / Listener 才让 WAITING 毕业 | 对照「盖章≠毕业」**是**。十二扇档案窗 **否** |
| 厨房 / 申请页（L-039） | `PersonVerificationPage` 走 application current/save/submit | URL 对照 **是**。`createProfileService` **否** |
| 企业匿名窗（L-041） | `/profile/enterprise/verification/providers/...` | 对称一句 **是**。企业 callback 行 **否** |
| 通知邮筒（OBJ-50） | 共享 secret、HMAC 原文、投递单向升级 | 两只邮筒对照 **是**。`ProviderCallbackController.callback` 及其 (b) **否** |
| API-005 | 本课已是 POST，无 DELETE | 盘点现状 **是**。不发动传输 Ratchet **否** |
| MySQL 8.4 | 尝试表/审计表、两唯一键 | 表证据 **是** (a)。容器 **否**（L-085） |
| 测试夹具 Recorder / 测试店 | 四参忽略；HMAC 故事只在 test | 误区「不得回流生产」**是**。夹具行已 deferred(test-fixture) **否** 另开 |

问：Lead 若凭「方法名在函数表里」把这一行标 covered，是否还要求读者能指着 **`@SaIgnore`、三种 200、假签 401、complete 不改申请、manual 拒收**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要把企业窗或通知邮筒算进来？

## Q5 元问题

- objective_id: OBJ-38
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 一行、无本课 (b)、无核身 (d)
- difficulty: low

建议：`defer`

原因：

- **这一格 (a) 与磁盘公开映射 1:1，主叙事站得住。** L 合同章节齐全（宏观地图、直觉十二句、定义表、机制链 1–7、带 caption / 文字等价物 / 图边界的两张图、正反例、变式、误区、暂停走查、来源表 S-004/S-008/S-010/S-L038-01…08）。能复现：一块门牌一扇 POST、方法 `@SaIgnore`、信封四字段、墙上钟、UseCase 事务章、先验店再验签再锁单、三种 200、假签/过期 401 其余 400、complete 只改尝试行、申请仍 WAITING、manual 拒收、无 fallback、layered 五层 + Registry/Provider 两根电线。不必为「方法名」再挖五问。
- 本批可粘贴的精度项不挡住把 **本行**标 covered：（1）失败时 HTTP 行 401/400，JSON `code` 仍 500；（2）DML `profile.person.provider.enabled` 无人读，注册表读 Spring `enabled-providers`；（3）回调共用 `lockApplication`，偶发 `APPLICATION_NOT_FOUND` 且该 throw 无审计；（4）来源表文件名写成 ServiceTest，磁盘是 CoordinatorTest。不要把 `startAttempt` HTTP、企业窗、申请页、通知邮筒、MySQL 容器塞进这一格。
- 不建议 `mine-more`：b01 未证明本行还有真实矩阵空洞；再五问只会把 L-035 门铃、L-034 发布、L-039 厨房、L-041 企业窗或 OBJ-50 通知邮筒再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；半句精度（JSON code、未读 DML 键、图 2 把 `APPLICATION_NOT_FOUND` 画窄）不是第二容器。不建议 `split-lesson`：未满 10 问；一扇窗是 chain 原设计的同一 OBJ-38，不是两个 C4；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：幂等/迟到忽略/PENDING 栅栏是 (a) 的 explained。不建议发明核身 (d) 流：矩阵没有这条走廊。不建议改 `course.md` OBJ 才盖章：方法名已在矩阵/chain/`claimed_cells`。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:PersonVerificationAnonymousController.callback | 一扇 POST、`@SaIgnore`、信封/墙上钟、三种 200、401/400 分家、先验店再验签、PENDING 栅栏、E2E 不发布、manual 拒收、无 fallback——均与源码对齐。残留：JSON `code=500`；DML 启用键未读；回调偶发未审计的 `APPLICATION_NOT_FOUND`；来源表文件名。 | 可选半句：口试以 HTTP 行与 `data.category` 为准，不要把 JSON code 说成 401；注册表只认 Spring `enabled-providers`。不要求重写机制链。Lead 可将本行标 `covered`（证据锚本 probe 与 L-038 来源表 S-L038-01…08）。 | defer（本行可 covered；精度项不挡盖章） |
| A:PersonApplicationController.current, submit | submit 才 `startAttempt`。本课只作负空间。 | 保持。L-035 已 covered。 | defer（邻课已闭合） |
| A:PersonAdminController.* | 发布/decide 未认。盖章≠毕业已对照。 | 保持。L-034 已 covered。 | defer（邻课已闭合） |
| A:createProfileService / A:createProfileWebDomain person/self | 申请页不是回调窗。厨房未认。 | 保持。L-039。 | defer（L-039） |
| A:EnterpriseVerificationAnonymousController.callback | 对称门牌 `/profile/enterprise/...`。 | 保持。L-041。 | defer（L-041） |
| A:ProviderCallbackController.callback / B:ProviderCallbackController.callback | 通知另一只邮筒；共享 secret；矩阵另有 (b) 幂等回调。 | 不要并进本行。OBJ-50。 | defer（通知课） |
| (b) 本课 callback 幂等/迟到忽略 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 核身回调流 | 矩阵无此行。ASCII 图不是新的 (d)。申请主路径已 covered。 | 不要发明个人核身数据流行。 | defer（未入库） |
| MySQL 8.4 / Redis 8 / dao-mapper-ownership | 尝试表是 (a) 证据，不是容器闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085 / L-083） |
| Mapper XML 同资源 SQL | 本窗 lock/complete/audit 已作证据。 | 保持 covered-by-parent。 | defer（父课覆盖） |
| 测试夹具 Recorder / 测试店 | HMAC 故事与四参构造只在 test。 | 保持 deferred(test-fixture)。 | defer（夹具） |
| API-005 变更 POST | 本课已是 POST，无 DELETE。 | 本课盘点，不发动 Ratchet。 | defer（传输 Ratchet） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本行无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`；合格读者说得出这一扇的目的（供应商投已签名回执）、主路径（验签 → 锁 PENDING → ACCEPTED，申请仍 WAITING）与失败路径（假签 401、manual/冲突 400、终态 LATE_IGNORED）；把可选半句写进原课不会超过 40 分钟；门铃/发布/厨房/企业窗/通知邮筒已有邻课。满 10 问前不拆。一扇窗不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-035 门铃、L-034 发布、L-039 厨房、L-041 企业窗、通知邮筒、未入库 (b)/(d)、MySQL/DAO 容器以及 API-005 显式建议 `defer`；对 `A:PersonVerificationAnonymousController.callback` 建议 Lead 标 `covered`（证据锚本 probe 与 L-038 来源表）。本课不主张改 Java 窗、企业对称窗、申请 Controller、通知 callback 或任何 (b)(c)(d) 格子。
