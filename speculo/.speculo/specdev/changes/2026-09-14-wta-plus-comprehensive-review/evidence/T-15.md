# T-15 本地实现与验证证据

revision66，status=review；全部提交按用户指令暂缓。HEAD/parent_before=`76dbbe84a34624234e379661a57b232529e34ed3`，implementation_commit/result_sha/candidate 均为空，未暂存、推送或部署，不标 Done。

## 实现与验收

- 25 个缺少 userId/operatorId 的旧 UseCase 重载删除，正式显式身份方法为必需实现。每一项由实际 JavaCompiler 编译正向和负向片段验证：显式身份成功、缺身份失败，25/25 通过。
- 三处 handleProcess、两处 accessUrlView 与两个 WorkflowGateway 的 terminate/持久版本查询删除默认失败/空值实现，正式能力保持。真正的派生默认方法 persistedSnapshotVersion(event)、wta-api/ProfileService.findByUserId 保留，wta-api 无修改。
- 七份无调用的重复 I* 接口移除，Service/测试改用正式 UseCase/Publication/Material port。逐方法替代、调用入口、测试和逐条构造签名见 T-15-migration-map.md；实施前原始签名与 SHA 见 T-15-inventory.json。
- 空 JSON 参数、具体 Provider/CodeGenerator/WorkflowService 构造桥与重复 ProcessEvent 转换移除；真实正式 port、Clock 和配置/通知/事件依赖保留。测试通过生产 Listener 或正式 domain command 进入原业务逻辑。
- 最后复核发现的两个 ignoredAuditRecorder 构造及两个无人调用的测试辅助类也已删除；11 处测试构造调用迁移。生产 Service → DAO 的审计实现及原回调审计断言保留并通过。
- EX-002 兼容构造例外撤销，原 Spring 注入硬约束保留。没有删除安全校验、事务或工作流能力，没有删除测试用例/原断言或扩大默认兜底。

## 实际验证

所有命令串行执行，JSON 包含 cwd、完整命令、退出码、计数和日志。

| 验证 | 记录 | 实际结果 |
|---|---|---|
| 最终 Profile 干净构建 | T-15-profile-audit-final.json | `backend: ./mvnw -B -ntp -pl wta-modules/wta-profile,wta-modules/wta-profile/wta-profile-person,wta-modules/wta-profile/wta-profile-enterprise -am clean package` exit 0，239/239，0 skip，含 25 编译合同和原行为测试 |
| 最终主要消费者 | T-15-consumers-audit-final.json | `backend: ./mvnw -B -ntp -pl wta-modules/wta-profile,wta-admin -am test` exit 0，710 总数中 679 pass、31 环境门控 skip，0 failure/error；不把 skip 计为通过 |
| 真实共享材料回归 | T-15-material-browser-final.json | 1 Java + 8 Chrome，0 skip、exit 0；实际 MySQL/Redis/MinIO/HTTP、权限/owner、材料预览/提交和事务回滚；隔离资源库存恢复。发生在最后两个审计占位构造删除之前，后续改动仅审计服务构造及测试装配，已以上两项重新验证 |
| 最终分层/事实/差异 | T-15-static-audit-final.json | person/enterprise layered、Skill facts、git diff --check，4 命令均 exit 0 |
| 引用与构建审计 | T-15-legacy-audit.json | backend Java/XML 对七个旧接口、handleProcessEvent/legacyWorkflow 零引用；Profile 无 Deprecated/空 Object 占位和测试审计桥；测试 ServiceImpl 无隐式 LoginHelper 身份。3 次 rg exit 1 表示无命中，非执行失败；两份 clean JAR 不含七个旧接口类 |
| 治理 | T-15-governance.json | 独立记录 revision66 的 goal-plan、ticket-control 和 diff 实际结果 |

31 个 skip 的类、方法和精确原因已保存 T-15-legacy-audit.json，并有不可变消费者日志的分套件汇总；包括隔离 Redis/MySQL/browser 等参数门控场景。本票自身 25 编译合同及 Profile 239 项没有跳过。E2E disposition 为无独立新交互；额外复用了 T-14 的真实材料链，不冒充外部供应商或完整审批引擎验收，完整同候选验收仍归 T-30。T-15 没有前端产品变更，未机械重复前端全量门禁。

## 失败与恢复记录

首次编译合同将两模块合同放在 enterprise 测试，13 条正向 classpath 失败，企业 12 条真实旧入口负向失败；拆回各自模块后，个人 13 条均因旧调用仍可编译而按预期红灯（T-15-identity-red-modules.json），之后迁移为绿色。不得将最初 classpath 错误宣称行为红灯。

迁移后 compile 首轮漏 ProfileMaterialPort 显式 import；第二轮三处 RecordingWorkflow 依赖旧 default，改为超出夹具场景时显式 AssertionError；第三轮 test-compile 通过。共同模式是删除过渡合同暴露编译依赖，不是放宽规则的理由。每次都有具体代码修复，恢复入口为 run-t15-verify.py；后续两次完整 clean package 与最终消费者均通过。最后审计从较窄 JSON 占位搜索扩展到所有 ignored Object 并补齐漏掉的审计构造，之后重跑 239/679 验证，未以不完整清单结票。

## 检查点与边界

输入 T-15-input-checkpoint.json 的 282 路径已验证。最终 T-15-checkpoint.json 63 路径（9 删除），与上游重叠的 2 路径为 ProfileMaterialService 和个人 ProfileMaterialUseCase；280 条非重叠上游 SHA 保持不变。工作树可逆，HEAD 未变。无远程写入或运行数据迁移，T-03/T-23 未决仍留原票。所有提交继续暂缓，后续票按最新哈希读入。

## Skill Execution Records

| Skill / phase / operation | 实际输入、执行与输出 |
|---|---|
| engineering-standards / implement / apply-scope-contract | 将显式身份、分层与 Spring 端口注入硬约束落实到正式必需合同，保留安全/事务能力；EX-002 撤销但硬约束不删，passed |
| namewta-fullstack-development / implement / apply-scope-contract | Controller/Listener → UseCase → Service → DAO 的真实调用核对，测试迁移保持 HTTP 身份与领域端口边界，239 Profile/679 消费者及真实材料回归，passed |
| wta-module-guide / implement / apply-scope-contract | 按当前 Profile 公开 API/发布/材料/工作流端口逐项映射；不机械删除 I/default，wta-api 有意义派生行为保留，passed |
| engineering-standards / verify / verify-affected-contract-and-quality-gates | 编译正负例、clean JAR、原行为/消费者、真实材料链与最终分层/facts/diff、哈希审计，真实命令与 skip 边界如上，passed |

入口 SHA 以票据 frontmatter 为准，本票未修改 Skill 入口。方法级删除映射、原行为回归和编译拒绝覆盖 AC-015；正式 commit/direct-parent 出口仍未满足，不标 Done。

证据写入恢复：生成方法表时路径变量复用曾把 Markdown 写入最后一份 codec 源码；进入下一票前的哈希检查立即拦截。已将表移至正确 evidence，并将源码逐字节恢复到已测试 SHA，全部343条合并哈希重新一致。未更新哈希掩盖偏差；详见 T-15-evidence-write-recovery.json。
