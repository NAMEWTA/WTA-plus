---
artifact: learning-goal-probe
probe_id: GP-L-069-b01
lesson_id: L-069
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-069 / b01

审问对象是已写成的 Lesson `lessons/L-069-flw-instance.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **两格 (a)**：`A:FlwInstanceController.*`、`A:WorkflowService.*`。OBJ-69 原文：能口述 `FlwInstanceController` 与 `WorkflowService` 跨模块合同。档是**解释+走查**。矩阵 (a) 两行均仍 `uncovered`：Controller 行把十三法写成一串（其中 `pageByCurrent` 是 HTTP 路径，磁盘方法名是 `selectCurrentInstanceList`）；第二行模块栏印 `wta-api/workflow`，十个名字（`completeTask` 只出现一次），备注「跨模块合同」。矩阵 (b) **没有** `terminateInstance` / `cancelProcessApply` / `invalid` / `deleteInstance` 独行；已有的流程 (b) 是 `IFlwTaskService.startWorkFlow, completeTask, backProcess, urgeTask, terminationTask`（仍 uncovered，挂 L-070）。矩阵 (c) `wta-api` / `wta-modules` / `layered-classic` 已 covered（L-002/L-003）；MySQL 8.4 / Redis 8 / `dao-mapper-ownership` 仍 uncovered。矩阵 (d) 「主路径 工作流请假」「失败路径 工作流驳回/终止」仍 uncovered，备注 L-072/L-070，**不是**本课覆盖列。本批按派单只审问这两扇窗：大厅十三 HTTP + 对讲机十一声明、谁填窗、四件关火、变量两支笔、找不到的三种回报、邻居何时喊。不要把模块栏读成「实现住在 wta-api」，不要把 `IFlwInstanceService` 升成第三张 (a)，也不要把启动/办理 HTTP 或请假整条标进本两行。分类窗留给 L-067；定义发布/导入留给 L-068；任务枢纽 HTTP + (b) 留给 L-070；SpEL 留给 L-071；请假 `submitAndFlowStart` 留给 L-072；浏览器工厂 `createWorkflowDefinitionService` / `createWorkflowWebDomain` 留给 L-073；档案网关失败码精度留给 L-034/L-040。Warm-Flow 引擎内部已 `deferred(vendor-engine)`。

抽查锚点（2026-09-17）：`backend/wta-modules/wta-workflow/src/main/java/org/namewta/workflow/controller/FlwInstanceController.java`；`service/IFlwInstanceService.java`；`service/impl/{FlwInstanceServiceImpl,WorkflowServiceImpl,TestLeaveServiceImpl,FlwTaskServiceImpl}.java`；`domain/bo/{FlowCancelBo,FlowInvalidBo,FlowVariableBo,CompleteTaskBo}.java`；`liteflow/instance/{InstanceDeleteLoad,Exists,Event,Execute}Component.java`；`domain/context/InstanceDeleteContext.java`；`resources/liteflow/instance-chain.el.xml`；`handler/FlowProcessEventHandler.java`；`common/ConditionalOnEnable.java`；`common/enums/{MessageTypeEnum,TaskStatusEnum}.java`；`backend/wta-api/src/main/java/org/namewta/workflow/api/WorkflowService.java` + `domain/` 六份 + `event/` 三份；`wta-common-core/.../enums/BusinessStatusEnum.java`；`wta-admin/src/test/java/org/namewta/test/profile/contract/WorkflowTerminationContractTest.java`；`wta-profile-person/.../SpringPersonWorkflowGateway.java` 与企业镜像；登记表 `03-backend-module-modes.md` workflow 行；`application.yml` `warm-flow` / `liteflow.enable`；菜单 `30-cde-workflow.sql`；厨房 `frontend/packages/domains/workflow/src/index.ts` instance 段；厅堂 `apps/admin-web/src/application/services.ts`；**零** `FlwInstanceController*Test` / `createWorkflowService`。

## Q1 澄清

- objective_id: OBJ-69
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-69 解释+走查、前置 OBJ-68；chain L-069 覆盖列两格 A:FlwInstanceController.* A:WorkflowService.*；课 claimed_cells 两格；矩阵 (a) 两行仍 uncovered；(b) 无本课符号、任务副作用挂 L-070；(c) wta-api/房间已 covered；(d) 请假走廊仍 uncovered；负空间 L-067/L-068/L-070…L-073/L-034/L-040
- difficulty: medium

课把实例这一头收成「管理员隔着大厅十三扇看锅，邻居隔着对讲机十一法点菜，classic 房间填窗」。计划里至少有四套可数对象，派单点名的是 **chain 这两格 (a)**：

1. **OBJ-69 原文。** `course.md`：能口述 `FlwInstanceController` 与 `WorkflowService` 跨模块合同。一句里是 **HTTP 大厅 + Java 合同**，不是 `FlwTaskController`、不是请假 `submitAndFlowStart`、也不是 `createWorkflowDefinitionService`。档是解释+走查，不是给终止另立 (b)。
2. **chain / 课 `claimed_cells`。** 两格。符号就是 Controller 星号与 `WorkflowService` 星号。chain **不**写 `IFlwInstanceService`、**不**写 LiteFlow 链名、**不**写 (b)/(d)、**不**写厨房方法。课把里屋服务和删除链收成「两扇窗怎样会合」。
3. **矩阵 (a) 两行仍 uncovered。** Controller 行十三名与磁盘公开方法 1:1（口试要把 `pageByCurrent` 读成 `selectCurrentInstanceList`）。第二行十个名字对应十一份声明（两个 `completeTask`）。模块栏印 `wta-api/workflow`；全仓 `implements WorkflowService` **只有** `WorkflowServiceImpl`（住在 `wta-workflow`）。Lead 若把模块栏读成「实现必须住在 api 包」会假失败；若凭「L-002 已讲 wta-api 容器 / L-005 已点过名字」宣布本课无对象，会把十三扇动词、四件关火、终止幂等丢掉。
4. **课自己的操作定义。** 本课认：门牌 `/workflow/instance`；十三扇、九颗 `workflow:instance:*`；`active` 直连 `InsService`；`getInfo` 路径 `Long`、`flowHisTaskList` 路径 `String`；`invalid` 的操作日志类型是 INSERT；对讲机零 HTTP、十一声明、唯一填窗人；删/变量/查状态走实例服务，启动/办理转交任务服务，终止走引擎 `TaskService`；运行中桶含 `cancel`；作废章 `invalid`、终止章 `termination`、撤销仍留运行中板；`warm-flow.enabled` 同时关大厅 Bean 与填窗人。
5. **启动/办理不是第三张 (a)。** 对讲机 `startWorkFlow` / `completeTask` / `startCompleteTask` 是本窗转交，任务 HTTP 与 `IFlwTaskService` 的锁/LiteFlow 是 L-070。口试要说得出「转交给谁」，不要把 `/workflow/task/startWorkFlow` 标进本行。
6. **邻格。** L-067 分类。L-068 定义发布/导入。L-070 任务枢纽与 (b)。L-071 SpEL。L-072 请假整条。L-073 厨房与菜单工厂。L-034/L-040 档案网关失败码（缺 Bean vs terminate 抛错）。L-003 classic 登记「经公开 Workflow API 接入」。L-002 邻居只经 `wta-api`。

问：本两格 (a) 闭合标准是「十三扇路径/动词/九权限 + 十一声明零 HTTP + 唯一填窗人 + 四件关火落在哪块板 + 变量两支笔 + 找不到三种回报 + 邻居喊 startCompleteTask/terminate/读变量/deleteInstance，且不把 `IFlwInstanceService` 升成第三行、不盖 L-070 任务窗、不发明 terminate (b)、不把请假 (d) 整行标 covered」，还是 OBJ 那句必须先把 `IFlwTaskService` (b) 或请假走廊标 covered、或必须把接口行拆成 Impl 与 `IFlwInstanceService` 两行，才给这两扇盖章？Lead 若凭「`WorkflowService` 四字在 L-005 出现过」跳过 OBJ-69，是否把十三扇和四件关火假闭合进了容器课？Lead 若用本课把 (d) 请假路径标 covered，是否假闭合了任务办理与 `submitAndFlowStart`？

## Q2 证据

- objective_id: OBJ-69
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 33–211；WorkflowService 16–109；api 包 1+6+3=10 份；Impl 40–219；FlwInstanceServiceImpl 88–463；BusinessStatusEnum 27–57 / 126–137 / 164–177 / 206–215；BO 三 record 的 AddGroup；Delete 链 xml 9–20 与四组件；InstanceDeleteContext 47–57；EventHandler 81–87；ConditionalOnEnable 27；yml 408–423；合同测试 31–104；Gateway 27–99；TestLeave 160–201 / 262–269；TaskServiceImpl 117–137；厨房 index.ts 301–311 / 447–468；admin services.ts 10 / 52；登记表 22；菜单 266–289；零 Controller 单测；零 createWorkflowService；home-web 无此工厂
- difficulty: high

课的两扇窗哪些能在工作树直接复现，哪些只是矩阵把 HTTP 路径写成方法名、或口试把「类上事务」说成磁盘注解位置？

**仍能复现（两格 (a) + 会合到里屋，不含任务 HTTP 盖章）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 一份类、根路径 `/workflow/instance`；`@ConditionalOnEnable` `@Validated` `@RestController`；继承 `BaseController`；注入 `InsService` + `IFlwInstanceService` | Controller 33–41 |
| 公开方法正好十三；无启动、无办理 | Controller 50–211；类内无 `startWorkFlow` / `completeTask` |
| 三张列表：`GET /pageByRunning` `/pageByFinish` 权限 `list`；`GET /pageByCurrent` 权限 `currentList` | 50–67 / 156–159 |
| 运行中 `.in(flowStatus, runningStatus())`；已结束用 `finishStatus()`；我发起的不按桶、只 `eq createBy = 当前用户` | ServiceImpl 88–91 / 103–105 / 289–291 |
| `GET /getInfo/{businessId}` 路径类型 `Long`；找不到抛 `NOT_FOUNT_INSTANCE`；定义缺失也抛 | Controller 75–78；ServiceImpl 117–126 |
| `GET /flowHisTaskList/{businessId}` 路径类型 `String`；返回 `Map.of("list", 运行中在前历史在后, "instanceId", id)` | Controller 168–171；ServiceImpl 303–350 |
| `GET /instanceVariable/{instanceId}` 与 api 同名法；形状 `{variableList, variable}` | Controller 180–183；ServiceImpl 373–382 |
| 写窗：`PUT /updateVariable` `PUT /active/{id}` `PUT /cancelProcessApply` 有 `@Log` UPDATE + `@RepeatSubmit`；`POST /invalid` `@Log` **INSERT** + `@RepeatSubmit`；三条 DELETE 有 `@Log` DELETE、**无** `@RepeatSubmit` | Controller 87–211 |
| `active` **不**走 `flwInstanceService`：`true → insService.active` / `false → unActive` | Controller 145–146 |
| `cancelProcessApply` 参数**没有** `@Validated`；`FlowCancelBo.businessId` 的 `@NotBlank` 挂在 `AddGroup` | Controller 130；`FlowCancelBo` 14–16 |
| `invalid` / `updateVariable` 参数有 `@Validated`（默认组）；`FlowInvalidBo.id` / `FlowVariableBo` 三字段约束也在 `AddGroup` | Controller 196 / 210；两 record |
| 九颗权限与种子 1:1：`list` `query` `remove` `cancel` `active` `currentList` `variableQuery` `variable` `invalid` | Controller 各 `@SaCheckPermission`；SQL 266–289 |
| `org.namewta.workflow.api` 正好十份：接口 1 + `domain/` 6 + `event/` 3；无 `@RequestMapping`、无 Mapper | 该目录十文件；`WorkflowService` 无 Web 注解 |
| 十一份声明、十个名字；两个 `completeTask` | 接口 24–108 |
| 全仓 `implements WorkflowService` 只有 `WorkflowServiceImpl`；`@ConditionalOnEnable` `@Service`；注入实例服务 / 任务服务 / 引擎 `TaskService` | Impl 40–47；全树仅这一处 implements |
| `deleteInstance` → `deleteByBusinessIds`（`List<String>`）；大厅对应窗路径是 `List<Long>` 再 `Convert.toStr` | Impl 56–58；Controller 90–91 |
| `terminateInstance`：`@Lock4j(keys="'workflow:terminate:' + #businessId")` + `@DSTransactional`；空白 `strip` 后 `IllegalArgumentException`；无实例或结束桶 → `NO_ACTIVE_INSTANCE`（结束桶仍带回 id）；不在运行桶 → `Unsupported workflow status for termination`；引擎 null → `did not confirm`；`ignore(true)` 章 `termination` | Impl 61–88；Result record 9–14 |
| 合同测试锁：待审 → `TERMINATED` + 引擎参数；缺/结束桶 never 喊引擎；空白抛；锁键与 `@DSTransactional` 在方法上 | `WorkflowTerminationContractTest` 31–104 |
| `getBusinessStatus` / `getBusinessStatusByTaskId` 找不到 **空串**；`getInstanceIdByBusinessId` 找不到 **null** | Impl 104–153 |
| `selectByTaskId`：运行任务没有再历史任务 | ServiceImpl 430–440 |
| `setVariable`：实例 null **不写不抛**；走 `taskService.mergeVariable` | ServiceImpl 415–420 |
| `updateVariable`：缺实例抛；map 无该 key 打 error 日志 `return false`；value 类型 `String` | ServiceImpl 393–405；`FlowVariableBo` 20 |
| `startWorkFlow` / `completeTask(DTO)`：`BeanUtil.toBean` 后交给 `IFlwTaskService` | Impl 162–177 |
| 短重载 `completeTask(taskId, message)` **写死** `variables.ignore=true`（`CompleteTaskBo.getVariables()` 会先 new Map） | Impl 187–193；BO 76–82 |
| `startCompleteTask`：**方法上** `@Transactional`；先 `startWorkFlow` 再 `completeTask`；`messageType` 写死 `["1"]`（`SYSTEM_MESSAGE`）；**不**自动放 ignore；变量来自 `StartProcessDTO.getVariables()`（丢 null） | Impl 203–218；`MessageTypeEnum` 23；DTO 54–60 |
| 任务服务侧：启动 `@Lock4j(flowCode+businessId)` + `startProcessChain`；办理 `@DSTransactional` + `@Lock4j(taskId)`（格子归 L-070，本课只认转交） | `FlwTaskServiceImpl` 117–137 |
| `runningStatus` = draft/waiting/back/**cancel**；`finishStatus` = finish/invalid/termination | `BusinessStatusEnum` 126–137 |
| `checkCancelStatus` 拒已退回/已撤销/已完成/已作废/已终止/空；`checkInvalidStatus` **允许**退回、撤销、草稿，拒完成/作废/终止/空 | 164–177 / 206–215 |
| 撤销：非超管必须 `createBy`；`taskService.revoke`；flow/his 都写 `cancel`；`ignore(true)` | ServiceImpl 257–278 |
| 作废：有实例才 `checkInvalidStatus`；**无论是否找到**都 `terminationByInsId`；章 `invalid` / `TaskStatusEnum.INVALID` | ServiceImpl 451–462 |
| 删除三入口同一条 `deleteInstanceChain`：load → IF exists → event+execute，否则 noop | xml 9–20；ServiceImpl 215–247 |
| load：有业务号 `in businessId`，否则 `selectByIds`；空则 warn 且 `result=false` | Load 35–47 |
| event：**先**超管或发起人，**再**门铃；定义缺失 warn、跳过事件、仍继续删 | Event 38–57 |
| execute：先删抄送（历史状态 copy）；`history=false` → `insService.remove`；`history=true` → 清任务用户/运行任务/历史 OSS/历史任务再 `removeByIds`，结果写死 `true` | Execute 42–56 |
| 门铃事件只有 `flowCode` + `businessId` | `ProcessDeleteEvent` 22–27；Handler 81–87 |
| 请假删：先删业务行再 `deleteInstance`；监听器行已空则 return | TestLeave 199–201 / 262–269 |
| 请假提交：变量自带 `ignore=true`；默认 `flowCode=leave1`；喊 `startCompleteTask` | TestLeave 160–170 |
| 档案网关：缺 Bean 或缺 flowCode → `*_WORKFLOW_UNAVAILABLE`；`startCompleteTask`（变量 `profileType`/`snapshotVersion`/`submissionId`，**不**放 ignore）；`terminateInstance` **丢掉**返回值，异常包 `*_TERMINATE_FAILED`；读 `variableList` 里 `snapshotVersion` 正整数，找不到抛 `*_SNAPSHOT_UNAVAILABLE` | Person Gateway 27–99；企业镜像同一形状 |
| 开关：`warm-flow.enabled=true` 才装本课这些 Bean；LiteFlow `enable` 跟同一键；`ui` 是另一键 | `ConditionalOnEnable` 27；yml 408–423 |
| 登记表 `wta-workflow` classic，「通过公开 Workflow API 接入，不改内部层次」 | modes.md 22 |
| 厨房工厂名 `createWorkflowDefinitionService`；厅堂 `workflowService = createWorkflowDefinitionService(domainHttp)`；**没有** `createWorkflowService` | domains `index.ts` 334；admin `services.ts` 10 / 52；frontend 零命中 |
| 厨房有三张列表、轨迹、撤销、变量读写、按实例删、历史删、作废、激活；**没有** `getInfo`、**没有** `deleteByBusinessIds` | domains `index.ts` 301–311 / 447–468 |
| home-web **不**建这份厨房 | home-web 零 `createWorkflowDefinitionService` |
| 测试树 **零** `FlwInstanceController*Test` | 全仓无此单测 |
| `wta-profile` POM **无** `wta-workflow`；Gateway `implements PersonWorkflowGateway`，不是 `WorkflowService` | profile 模块 pom 无该依赖；Gateway 15 |

**课写过、测试名单更窄或口试把注解贴错位置（不是证伪本两格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「`startCompleteTask` 类上 `@Transactional`」 | 注解在 **方法** 203 行。类上只有 `@ConditionalOnEnable` `@RequiredArgsConstructor` `@Service`。同一事务仍成立，贴的是位置 |
| 「合同测试锁死引擎失败不能报成功」 | 测试把引擎 **抛** `IllegalStateException` 当成失败。磁盘另有 `terminationByInsId` 返回 **null** → `ServiceException("…did not confirm")`。null 回报路径课写了，测试没点 |
| 「矩阵 Controller 行十三法」 | 行内写 `pageByCurrent`，磁盘方法是 `selectCurrentInstanceList`。其余十二名与 Java 方法 1:1 |
| 课 `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |

问：在十三扇动词/权限、`active` 直连引擎、`getInfo` Long vs 轨迹 String、`invalid` 日志 INSERT、十一声明零 HTTP、唯一填窗人、终止锁+幂等、短重载 ignore、`startCompleteTask` 不自动 ignore、运行中桶含 cancel、作废仍喊引擎、删除门铃、厨房缺两扇、档案只对讲已经能互证的前提下，(a) 是否还要把「类上→方法上事务」「合同测试没锁 null 回报」「矩阵 `pageByCurrent`」写成开篇半句，才算两扇讲完？还是这些已经落在课的表/反例里（注解位置除外），Lead 可以把这两格 (a) 视为已讲，并保持 `IFlwTaskService` (b)、请假 (d)、厨房行 uncovered？

## Q3 假设与反例

- objective_id: OBJ-69
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无 `/workflow/service`；IFlwInstanceService 不在 api；profile 不 implements WorkflowService；四件关火三枚章两块板；terminate 幂等 vs getInfo 抛 vs invalid 仍喊引擎；setVariable 静默 vs updateVariable 只改已有 key；pageByRunning 不是待办；active 不是 flowStatus；startCompleteTask 不自动 ignore；工厂名不是 createWorkflowService；厨房缺 getInfo/按业务删除；无 Controller 单测不阻止 (a) 走查
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪枚章认错？

1. **「`WorkflowService` 就是 `/workflow/instance`。」** 接口没有映射。启动甚至不在这十三扇。课反例 1 覆盖。
2. **「`IFlwInstanceService` 在 `wta-api`。」** 包名 `org.namewta.workflow.service`。邻居编译期伸不到。课反例 2 / 误区 5 覆盖。
3. **「`wta-profile` 里有一份 `implements WorkflowService`。」** 只有 Gateway 拿 `ObjectProvider<WorkflowService>`。课反例 3 / 误区 7 覆盖。
4. **「终止 = 作废 = 撤销 = 删除。」** 三枚章、两套入口、删除才碎纸+门铃。课反例 4 / 图 3 覆盖。
5. **「`terminateInstance` 找不到就抛，跟 `getInfo` 一样。」** 返回 `NO_ACTIVE_INSTANCE`。课反例 5 覆盖。
6. **「`setVariable` 和 `updateVariable` 一样。」** 一支合并静默、一支只改已有 key 且缺实例就抛。课反例 6 覆盖。
7. **「`pageByRunning` 等于待我审批。」** 待办在任务楼 `pageByTaskWait`。运行中板含草稿和已撤销。课反例 7 / 误区 9 覆盖。
8. **「`cancel` 进已结束板。」** 名单在 `runningStatus`。课反例 8 / 误区 8 覆盖。
9. **「大厅 `active` 走 `IFlwInstanceService` / 会把 `flowStatus` 改成 invalid。」** 走 `InsService`；挂起是 `activityStatus`。课反例 9 覆盖。
10. **「`startCompleteTask` 自动 ignore。」** 磁盘不写。请假变量里自己放；档案网关没放。课反例 10 覆盖。
11. **「浏览器工厂叫 `createWorkflowService`。」** 厅堂是 `createWorkflowDefinitionService`。课反例 11 / 误区 3 覆盖。
12. **「前端有十三枪所以后端十三扇都映射了。」** 缺 `getInfo`、缺 `deleteByBusinessIds`。课反例 12 / 误区 16 覆盖。
13. **「删除历史只是列表换桶。」** `history=true` 清任务、历史、OSS。课反例 13 / 误区 10 覆盖。
14. **「`getBusinessStatus` 空就是抛错。」** 空串。课反例 14 覆盖。
15. **「`invalid` 的 `@Log` 是 UPDATE / 会插入业务单据。」** 磁盘 `BusinessType.INSERT`，只是操作日志枚举。课反例 15 / 误区 17 覆盖。
16. **「对讲机 `completeTask` 就是大厅某扇窗。」** 大厅没有办理窗。课反例 16 / 误区 1 覆盖。
17. **「关掉 `warm-flow.enabled` 只关设计器 UI。」** `ui` 是另一键。`enabled` 关大厅和对讲机实现。课误区 18 覆盖。
18. **「已撤销的单不能被邻居终止。」** `cancel` 在运行桶，`terminateInstance` 仍可把它打成 `termination` 进结束板。课边界覆盖。
19. **「`processInvalid` 找不到实例会像终止一样幂等返回。」** 仍喊 `terminationByInsId`。课边界覆盖。
20. **「`FlowCancelBo` 空业务 id 会被框架 400。」** 约束组是 `AddGroup`，入口没对上默认组。课边界覆盖。
21. **「OBJ-69 包含 `FlwTaskController` / 请假 Controller / 前端工厂。」** OBJ-70 / OBJ-72 / OBJ-73。课误区 1–3 覆盖。
22. **「填窗人住在 `wta-api`。」** 住在 `wta-workflow`。课误区 6 覆盖。
23. **「浏览器能 import `org.namewta.workflow.api`。」** 不能。课误区 15 覆盖。
24. **「`instanceVariable` 直接返回引擎 map。」** 包了 `variableList` + 原串。课误区 11 覆盖。
25. **「没有 Controller 单测所以 (a) 不能盖章。」** 走查证据在 Controller/服务/合同测试/网关。矩阵测试夹具行本就 deferred。不要把缺 HTTP 单测写成第三容器。

问：读完后仍说不出的，是不是「两个类名」，而是 **大厅管跑着的单、对讲机给邻居点菜、启动/办理只转交不在十三扇、四件关火不是同义词、找不到有抛/空串/幂等三种回报、九工厂式地把任务窗或请假走廊并进本行会假闭合**？

## Q4 视角

- objective_id: OBJ-69
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-69 vs L-002/L-003/L-005/L-034/L-040/L-067/L-068/L-070…L-073；claimed 两格 vs IFlwInstanceService / LiteFlow / 厨房；IFlwTaskService (b) 仍 uncovered
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这两格 (a) 切片**？

| 视角 | 多出来的边 | 是否本课这两格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 两行仍 uncovered；Controller 行 `pageByCurrent` vs `selectCurrentInstanceList`；WorkflowService 行十名=十一声明；(b) 无 terminate 独行、任务副作用挂 L-070；(d) 请假走廊挂 L-072/L-070 | **是** 闭合定义。两格 (a) 可视为已讲。不要发明 terminate (b)，不要用本课盖任务 (b) 或请假 (d)，不要给 `IFlwInstanceService` 新开一行 |
| 大厅管理员 | 十三扇动词/九权限、三张列表桶、`active` 直连引擎、撤销留运行中、作废按实例 id | **是** `A:FlwInstanceController.*` |
| 对讲机调用方 | 十一法、零 HTTP、`List<String>` 业务号、终止幂等加锁、短重载 ignore、`startCompleteTask` 站内信 | **是** `A:WorkflowService.*` |
| 填窗人 | 唯一 `implements`；删/变量/状态 → 实例服务；启动/办理 → 任务服务；终止 → 引擎 | **是** 落点走查，不是第三张 (a) |
| 里屋实例服务 | 列表 wrapper、撤销校验、轨迹 map、变量两支笔、作废仍喊引擎 | 本课认会合 **是**。单独盖章 **否** |
| 删除链电工 | 三入口、权限、门铃、运行 remove vs 历史清扫 | 本课认「同一条河」**是**。不要升成 LiteFlow 矩阵行（vendor/编排已有 defer 规则） |
| 任务枢纽（L-070） | `/workflow/task/*`；`startProcessChain`；办理锁 taskId；驳回/催办/终止任务 | 转交一句 **是**。HTTP 与 (b) **否** |
| 请假示例（L-072） | `submitAndFlowStart` 自带 ignore；先删行再 `deleteInstance`；门铃可能再删 | 活样本 **是**。七扇 Controller **否** |
| 浏览器厨房（L-073） | 工厂名；缺 `getInfo` / `deleteByBusinessIds` | 负空间对照 **是**。厨房行 / 九工厂行 **否** |
| 档案网关（L-034/L-040） | 三法时刻；缺 Bean vs terminate 失败码；丢掉 `WorkflowTerminationResult` | 谁在何时喊 **是**。十二扇管理窗 / 失败码精度 **否** |
| 分类/定义/SpEL（L-067/L-068/L-071） | 另一组门牌 | 负空间 **是**。那些 (a) 行 **否** |
| 容器课（L-002/L-003/L-005） | wta-api 容器、classic 登记、接口名字点过 | 空间关系一句 **是**。十三扇与十一法 **否** |
| MySQL 8.4 | 菜单九颗在 `30-cde-workflow.sql` 不是 `50-cde-base-dml.sql` | 种子对照 **是** (a)。容器 **否** |

问：Lead 若凭「`WorkflowService` 出现在 wta-api 容器课」把本行标 covered，是否还要求读者能指着 **十三扇、十一法、四件关火、变量两支笔、三种找不到、邻居只对讲**？这些是不是已经在本两格函数表/反例里，而不需要 (b) 新行、也不需要等 L-070 才给**本两扇**当已讲？`IFlwTaskService` (b) 与请假 (d) 是否例外——必须保持 uncovered？厨房行是否必须保持 uncovered，本课只往备注写「大厅十三扇 + 对讲机十一法已讲、工厂缺 getInfo/按业务删除」？

## Q5 元问题

- objective_id: OBJ-69
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 两行仍 uncovered、(b) 任务副作用仍 uncovered、(d) 请假走廊仍 uncovered、无本课 terminate (b)
- difficulty: low

建议：`defer`

原因：

- **两格 (a) 是 chain 原设计的同一 OBJ-69，不是两个 C4。** L 合同章节齐全（宏观地图、定义表、机制链 1–4、带 caption / 文字等价物的四张图、正反例、变式、误区、暂停走查、来源表 S-004/S-008/S-015/S-L069-01…08）。能复现：十三扇路径/动词/九权限、`active` 直连引擎、`getInfo` Long vs 轨迹 String、`invalid` 日志 INSERT、十一声明零 HTTP、唯一填窗人、终止锁+结束桶幂等、短重载 ignore、`startCompleteTask` 不自动 ignore、运行中桶含 cancel、作废与终止不同章、删除三入口同一条河、厨房缺两扇、档案/请假只对讲。不必为「两个类名」再挖五问。
- **Lead 可以把 `A:FlwInstanceController.*` 与 `A:WorkflowService.*` 标 `covered`。** 不要用本课改 `IFlwTaskService` (b)、`FlwTaskController`、`TestLeaveController`、`createWorkflowDefinitionService` / 九工厂行，也不要把 (d) 请假走廊整行标 covered。`IFlwInstanceService` **不要**新开矩阵行；备注可写「大厅/对讲机会合点已随 L-069 走查」。
- 本批可粘贴的精度项不挡住两格视为已讲：（1）`startCompleteTask` 的 `@Transactional` 在**方法**上不是类上；（2）合同测试锁的是引擎抛错，不是返回 null 的 `did not confirm`；（3）矩阵 Controller 行把 `selectCurrentInstanceList` 写成路径名 `pageByCurrent`。不要把任务 LiteFlow、请假七扇、厨房 URL 全表、档案失败码精度塞进本两格。
- 不建议 `mine-more`：b01 未证明本两格还有真实矩阵空洞；再五问只会把 L-070 启动链、L-072 请假提交、L-073 工厂或 L-034 失败码再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；「类上→方法上」是半句精度，不是第二容器。不建议 `split-lesson`：未满 10 问；大厅+对讲机是 chain 原设计的同一 OBJ-69；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：终止幂等、撤销留运行中、变量两支笔已是 (a) 的解释到副作用。不建议等 GP-L-070 才给本两扇当已讲。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:FlwInstanceController.* | 课按磁盘讲了十三扇路径/动词/九权限、`active` 直连引擎、三张列表桶、`getInfo` Long vs 轨迹 String、撤销留运行中、作废章 invalid、三条删除会合 LiteFlow、厨房缺 getInfo/按业务删除。矩阵行把 `selectCurrentInstanceList` 印成 `pageByCurrent`。无 HTTP 单测。 | 可选半句：口试方法名以 Java 为准（`selectCurrentInstanceList`），`pageByCurrent` 是路径。不要求重写机制链。Lead 可将本行标 covered（证据锚本 probe 与 L-069 来源表 S-L069-01…08）。 | defer（本行可 covered；精度项不挡盖章） |
| A:WorkflowService.* | 课按磁盘讲了十一声明零 HTTP、唯一填窗人、转交对象、终止锁+幂等、短重载 ignore、`startCompleteTask` 站内信且不自动 ignore、三种找不到回报、邻居三法+请假删除。残留精度：课写「类上 `@Transactional`」，磁盘在方法 203 行；合同测试未锁引擎 null 回报。 | 可选半句：事务注解在 `startCompleteTask` 方法上；引擎 `terminationByInsId` 返回 null 与抛错是两条失败，不要只背测试里的 throw。Lead 可将本行标 covered。不要把模块栏读成实现住在 api 包。 | defer（本行可 covered；精度项不挡盖章） |
| IFlwInstanceService / FlwInstanceServiceImpl | 本课只认会合。矩阵无此行。 | 不要新开 (a)。备注可写「已随两扇窗走查」。 | defer（未入库；covered-by-parent 即可） |
| B:IFlwTaskService.startWorkFlow, completeTask, backProcess, urgeTask, terminationTask | 本课只点转交与锁/链名字。该行仍 uncovered。 | 不要用本课盖任务副作用。 | defer（L-070） |
| A:FlwTaskController.* | 启动/办理 HTTP 不在十三扇。 | 保持。L-070。 | defer（L-070） |
| A:TestLeaveController.list, export, get, add, submitAndFlowStart, edit, remove | 活样本 `startCompleteTask` / `deleteInstance` / 门铃空回。 | 不要用本课盖七扇。 | defer（L-072） |
| D:主路径 工作流请假 / 失败路径 工作流驳回/终止 | 走廊挂 L-072/L-070。本课图边界不画任务办理。 | 不要用两扇窗整行闭合 (d)。 | defer（L-072 / L-070） |
| A:createWorkflowDefinitionService / 九工厂行 `createWorkflowWebDomain` | 工厂名对照 + 缺两扇枪。厨房行 / 九工厂行仍 uncovered。 | 不要用本课盖厨房或整行工厂。备注可写「大厅十三扇已讲、工厂缺 getInfo 与按业务删除」。 | defer（L-073；九工厂行保持 uncovered） |
| A:FlwCategoryController.* / A:FlwDefinitionController.* / A:FlwSpelController.* | 负空间已点。 | 保持邻课。 | defer（L-067 / L-068 / L-071） |
| PersonAdmin / EnterpriseAdmin 十二扇与网关失败码 | 谁喊三法已借用。缺 Bean vs TERMINATE_FAILED 精度在邻课。 | 不要重盖 L-034/L-040。 | defer（L-034 / L-040 已 covered） |
| (b) terminateInstance / cancelProcessApply / invalid / deleteInstance | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| WarmFlow/SnailJob 厂商引擎内部 | `InsService.active` / `terminationByInsId` / `revoke` 只作会合点。 | 保持 deferred(vendor-engine)。 | defer（vendor） |
| MySQL 8.4 / Redis 8 / dao-mapper-ownership | 菜单种子与终止锁是 (a) 对照，不是容器闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-083 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本两格无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（任务 (b) 与请假 (d) 有自己的课；`IFlwInstanceService` 本就不是矩阵行）；合格读者说得出两扇窗目的（大厅管跑着的单、对讲机给邻居点菜）、主路径（列表/详情/轨迹/变量、档案 startCompleteTask、管理端 `/invalid`、邻居 terminate、删除门铃）与失败路径（撤销权限与状态闸、终止空白/幂等/非运行桶、作废仍喊引擎、变量缺 key 返回 false、setVariable 静默、开关关掉 Bean 消失）；把可选半句写进原课不会超过 40 分钟；任务枢纽/请假/厨房/分类定义已有邻课。满 10 问前不拆。大厅与对讲机虽跨 HTTP 与 Java 合同，仍是 chain 原设计的同一 OBJ-69，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对两格 (a) 建议视为已讲并可 covered；对 L-070 任务窗与 (b)、L-072 请假与 (d)、L-073 厨房/九工厂、L-067/L-068/L-071、L-034/L-040、未入库 terminate (b)、vendor 引擎以及 MySQL/Redis 容器显式建议 `defer`。本课不主张给 `IFlwInstanceService` 新开行、发明 (b)/(d)，或把启动/办理 HTTP 标进十三扇。
