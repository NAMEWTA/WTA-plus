---
artifact: learning-goal-probe
probe_id: GP-L-022-b01
lesson_id: L-022
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-022 / b01

审问对象是已写成的 Lesson `lessons/L-022-sys-social-profile.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课声称格子（chain / `claimed_cells` / 矩阵 (a) 两行）：`A:SysSocialController.list`、`A:SysProfileController.profile,updatePwd`。OBJ-22：口述两份 Controller（账号资料页，**不是** profile 模块）。矩阵 (a) Profile 行**没有** `updateProfile`；矩阵 (b) **没有** 资料页/社交写库行。`createSystemService.users.profile*` / `resources.social.list` 全表留给 L-019；`IdentityAccessService.social*` 前端写手留给 L-014；门厅 `socialCallback` / `socialBinding` 留给 L-011；`SocialAuthStrategy` 发通行证留给 L-013；菜谱字段表留给 L-012；人事 `resetPwd` / 临时纸条留给 L-015；在线设备留给 L-023；OpenAPI Tab 留给 L-030 / L-031；OSS 直传留给 L-026 / L-029；Person/Enterprise 档案留给 L-034 起。同文件 `AuthController.unlockSocial`：L-011 / GP-L-011 明确**不认、留给本课**；本课开篇却写「格子已在 L-011」。

抽查锚点（2026-09-16）：`backend/wta-modules/wta-system/.../controller/system/{SysSocial,SysProfile}Controller.java`；`service/{ISysSocialService,ISysUserService}.java`；`service/impl/{SysSocial,SysUser}ServiceImpl.java`；`mapper/SysSocialMapper.java` + 空 `SysSocialMapper.xml`；`mapper/SysUserMapper.java` `update`/`updateById`；`domain/{SysSocial,bo/SysUserProfileBo,vo/{SysSocialVo,ProfileUserVo,SysUserVo}}.java`；`wta-admin/.../AuthController.java` binding/callback/unlock；`SysLoginService.socialRegister`；`SocialAuthStrategy.login`；`PasswordWritePathUnitTest.profileChangeRejectsWeakValueBeforeHashingOrWriting`；`PasswordCandidateContractUnitTest.candidateAuditNeverPersistsRequestOrResponseBodies`；`CacheNames.SYS_NICKNAME`；`03-backend-module-modes.md`；`10-cde-base-ddl.sql` `sys_social` / `sys_user`；`frontend/packages/domains/system/src/{service.ts,resource-service.ts,resource-types.ts}`；`domains/admin/src/index.ts` `social.bindingUrl/unlock`；`apps/admin-web/src/views/system/user/profile/{index,userInfo,userAvatar,resetPwd,thirdParty}.vue`；`apps/home-web` `/profile` + `createProfileService`。

## Q1 澄清

- objective_id: OBJ-22
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: L-022 `claimed_cells`；OBJ-22 原文；chain `A:SysSocialController.list A:SysProfileController.profile,updatePwd`；矩阵 (a) 两行；(b) 无对应行；磁盘四扇 HTTP vs 矩阵三名；L-011 第七扇 `unlockSocial`
- difficulty: medium

课把「镜子 + 贴纸本」收成 **两个 (a) 格子、一个 OBJ**。计划里至少有五套可数对象，课没有声明以哪一套当作闭合的操作定义：

1. **OBJ-22 原文。** `course.md`：能口述 `SysSocialController` 与 `SysProfileController`（账号资料页，不是 profile 模块）。点名的是**两份类 + 一座对照楼**，没有把 `updateProfile` / `unlockSocial` 写成口试清单。
2. **Chain / Lesson `claimed_cells`。** Social 一扇 `list`；Profile 两扇 `profile,updatePwd`。与矩阵 (a) 行名对齐。
3. **磁盘公开映射。** Social 1 + Profile 3 = 四扇：`GET /system/social/list`、`GET /system/user/profile`、`PUT /system/user/profile`、`PUT /system/user/profile/updatePwd`。课自己把第三扇标「矩阵漏写、口试按磁盘」。
4. **矩阵 (a) 两行。** 默认档 Unistructural + explained：每一公开方法的目的都要在函数表里出现。Profile 行没写 `updateProfile`；Social 行只有 `list`。状态仍 `uncovered`。
5. **矩阵 (b)。** 无 `updatePwd` / `updateProfile` / `queryListByUserId` / `unlockSocial` 行。课却把 ignore 写库、不踢票、昵称缓存、物理删、Vo 含令牌写成机制主链。这些是 (a) 的「解释到副作用」，还是未入库的枢纽性状？
6. **门厅第七扇。** L-011：`DELETE /auth/unlock/{socialId}` **不认格子、留给 L-022**。本课：「格子已在 L-011」「点到为止」。两课互推，矩阵 `AuthController` 行名也没有 `unlockSocial`。

问：本课格子的操作定义是「OBJ 点名的两份类还在 + 不是 wta-profile」，还是「chain 三扇每一扇的 HTTP 动词、登录闸、写库与失败句」？若 Lead 只凭「两份 Controller 在 system 房间」把两行标 covered，磁盘上的 `updateProfile`、以及 L-011 交过来却未被认领的 `unlockSocial`，还算不算本格？(b) 若必须看见 `resetUserPwd` / `deleteById` 函数体才算性状闭合，是否已经把 OBJ-22 扩成一张不存在的 Service 行？

## Q2 证据

- objective_id: OBJ-22
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: 两份 Controller 全文；`SysUserServiceImpl` select/updateProfile/resetUserPwd/roleGroup/postGroup；`SysSocialServiceImpl` list/insert/delete；`AuthController` 三扇社交；`socialRegister` 闸序；`SysSocialVo` 令牌字段；`ProfileUserVo` 无 createTime/password；单测方法名；前端 `users.profile*` / `resources.social.list` / `identityAccessService.social`
- difficulty: high

课的机制链哪些能在工作树直接复现，哪些被磁盘削薄或与邻课打架？

**仍能复现：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 两份类在 `wta-system` `/system/social` 与 `/system/user/profile`，**不在** `wta-profile`；无 UseCase/DAO | 两份 Controller；模块内零 `UseCase`；`03-backend-module-modes.md`：`wta-system` classic、`wta-profile` layered |
| 都 `extends BaseController`；无 `@SaIgnore`、无 `@SaCheckPermission`；要登录、不要菜单权限串 | 两份类注解；全仓无 `system:social` / `system:user:profile` 权限串 |
| Social 只有 `GET /list` → `queryListByUserId(LoginHelper.getUserId())`；无分页、无 query | `SysSocialController` 35–38 行；`SysSocialServiceImpl` 60–61 行 `lambda().eq(userId).voList()` |
| Profile 三扇：GET `/`、PUT `/`、PUT `/updatePwd`；后两扇 `@RepeatSubmit` 默认 5000ms；GET 不带 | `SysProfileController` 44–107 行；`RepeatSubmit.interval` default 5000 |
| `updatePwd`：`@ApiEncrypt`；`@Log` 请求/响应都不落；旧锁错 / 新旧相同 → `R.fail` **不**调菜谱、**不**写库；弱密码 `validateOrThrow` 后 `resetUserPwd` never | Controller 87–107 行；`PasswordWritePathUnitTest.profileChangeRejectsWeakValueBeforeHashingOrWriting`；`PasswordCandidateContractUnitTest` 钉四个 false |
| 写库两扇包 `DataPermissionHelper.ignore`；GET `profile` **不**包。`SysUserMapper.update` / `updateById` 带 `@DataPermission(dept_id + create_by)`；`selectVoById` 无此注解 | Controller 46 / 74 / 102 行；Mapper 146–163 行 |
| `profile()` 空用户下一行 `user.getUserId()` **NPE**；对比 `SysUserController.getInfo` ignore 后 `R.fail("没有权限访问用户数据!")` | Profile 46–47 行；`SysUserController` 131–134 行 |
| 角色组走 `selectRolesByUserId(..., resolveLoginClientId())`；岗位组 `selectPostsByUserId` **不**按 Client | `SysUserServiceImpl` 273–293 行 |
| `ProfileUserVo` 无 password、无 `createTime`；`SysUserVo` 有两者，password `@JsonIgnore`，邮箱/手机 `@Sensitive(perms=system:user:edit)` | `ProfileUserVo` 全文；`SysUserVo` 54–84 / 109 行；admin `index.vue` 59 行读 `state.user.createTime` |
| `updateProfile` 强制 `setUserId(当前登录者)`；手机/邮箱非空才查唯一；`setIfPresent` 是 `value != null`（空串会写） | Controller 65–73 行；`LambdaCrudChainWrapper.setIfPresent` 569–571 行 |
| 基本资料提交不带 avatar；头像页只 `{ avatar: ossId }` + `policy: 'avatar'` | `userInfo.vue` 72–78 行；`userAvatar.vue` 143–144 行 |
| `updateUserProfile` `@CacheEvict(SYS_NICKNAME, key="#user.userId")` + `@DSTransactional`；`resetUserPwd` 只 set password，无 Evict、无事务、无 kickout | ServiceImpl 498–534 行；`CacheNames.SYS_NICKNAME = "sys_nickname#30d"` |
| 三条改密走廊门牌不同：资料页要旧锁；`resetPwd` 要 `system:user:resetPwd` + `checkUserAllowed`；临时纸条 POST `/temporaryPassword` | 本课 Controller vs `SysUserController` 273–283 行 |
| `ISysSocialService` 还有 insert/update/delete/`selectByAuthId`；本课 Controller **一扇都不映射** | 接口 19–50 行 vs Controller 只 `list` |
| XML 空壳；实体无 `delFlag` / `@TableLogic`；DDL 有 `del_flag`；`deleteById` 按工作树是物理删 | `SysSocialMapper.xml` 只有 namespace；`SysSocial` 18–136 行；DDL 36 行 |
| `SysSocialVo` 含 `accessToken` / `refreshToken` / `idToken` 等，无 `@JsonIgnore` | `SysSocialVo` 47–107 行 |
| `unlockSocial`：类上 `@SaIgnore`，方法内 `StpUtil.checkLogin()` 后 `deleteWithValidById(socialId)`，**不**比对 `userId` | `AuthController` 52 / 155–161 行；`SysSocialServiceImpl` 116–117 行 |
| 前端：`users.profile/updateProfile/updatePassword`；社交 list 在 `resources`（`createSystemResourceService`），不在 `users`；解绑 `identityAccessService.social.unlock` → `DELETE /auth/unlock/{id}`；绑定 `bindingUrl` | `service.ts` 186 / 233–241 行；`resource-service.ts` 248–250 行；`domains/admin/src/index.ts` 373–383 行 |
| home-web `/profile` 组装 `createProfileService`，不是本课 URL；admin 个人中心 hidden 路由 `/user/profile` | `home-web/src/application/services.ts`；`admin-web/src/router/index.ts` 83–95 行 |
| 用户头像列 bigint OSS id；社交 avatar varchar URL | DDL `sys_user.avatar` 71 行；`sys_social.avatar` 17 行；`ProfileUserVo.avatar` Long vs `SysSocialVo.avatar` String |

**课写过、磁盘已削薄或与邻课打架：**

| 课的句子 | 磁盘 |
| --- | --- |
| 门厅侧门「格子已在 L-011」 | L-011 开篇：**第七扇不认、留给 L-022**。GP-L-011 缺口表同句。矩阵 `AuthController` 行名到 `socialBinding` 为止。本课把解绑机制讲完了，却把格子推回去 |
| `socialRegister`：「`selectByAuthId` 非空就抛『已经被绑定』；本人同 `source` 已有行则 `updateByBo`」 | 两闸**有顺序**：`authId`（source+uuid）非空**一律抛**，含「这是我自己的同一条 GitHub」。`updateByBo` 只发生在 **新 authId** 且当前用户该 `source` 已有行（换一个第三方账号覆盖原行）。同一账号再 callback **走不到** update，也刷新不了令牌 |
| `updatePwd`「`@Log` 请求/响应都不落」当密钥边界 | **成立**（单测钉死）。对比 `updateProfile` 的 `@Log` 只有 title + UPDATE，`isSaveRequestData` **默认 true**，昵称/手机/邮箱可以进 oper log。课没写这组对照 |
| 「`profile()` 空用户 NPE」 | **`updatePwd` 同样**：`selectUserById` 后立刻 `user.getPassword()`。两扇读路径都没有 getInfo 那种 `R.fail` |
| 「前端 `SocialAuthVO` 类型只有 `id/source/avatar/userName`」 | 自有字段是这四个，但 `extends BaseEntity`，**含 `createTime`**。表格列 `createTime` 在类型上说得通；令牌字段确实不在类型上。口诀「类型只有四列所以 JSON 没有令牌」会把人带偏 |
| 前端改密「`headers.isEncrypt = true`」 | 实际还有 `repeatSubmit: false`（厨房跳过客户端防重）。服务端方法上仍有 `@RepeatSubmit`。双层不要说成一层 |
| `updateUserProfile`「驱逐昵称缓存」 | `@CacheEvict` **无 condition**：`rows==0` 仍驱逐。失败句「联系管理员」不是零缓存。课把 Evict 只写成成功副作用 |
| 「`lambda().updateCount()` 最终走进带注解的 `update`」 | `updateCount()` 调 `crudMapper.update(wrapper)` 单参。两参 `update(entity, wrapper)` 才有 `@DataPermission`。ignore 包在 Controller 上是事实；AOP 是否命中两参方法是 L-083 里屋，本课当作已证略满 |

问：在四扇 HTTP 形状、classic 无 UseCase、自己改锁要旧钥匙、list 只读、解绑不在本课 Controller 已经能互证的前提下，(a) 是否还要把 **unlock 的归属句改口、`socialRegister` 写成两道有序闸、以及 Profile PUT 日志默认可存 PII**，才算「看/贴/撕 + 改锁」讲完？

## Q3 假设与反例

- objective_id: OBJ-22
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: `updateProfile` 漏行；`unlockSocial` 无 userId；`socialRegister` authId vs source；`ProfileUserVo` 无 createTime；`@SaCheckPermission` 缺失；home `/profile`；`resetPwd` vs `updatePwd`；Vo 令牌字段
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪栋楼认错？

1. **「`SysProfileController` 在 `wta-profile` / 要 `ProfileUseCase`。」** 磁盘在 `wta-system`，classic，零 UseCase。课反例 1 / 9 覆盖。
2. **「矩阵没写 `updateProfile` = 没有这扇窗。」** 同门牌不同动词的 PUT。前端基本资料 + 头像都打它。课反例 3 覆盖，但 claimed_cells / 矩阵行名仍喂这口。
3. **「资料页改密就是 `PUT /system/user/resetPwd`。」** 那扇不要旧密码，要 `system:user:resetPwd`。课反例 2 覆盖。
4. **「`GET /system/social/list` 能解绑 / 能 bind。」** list 只读。绑定 `GET /auth/binding/{source}` + `POST /auth/social/callback`；解绑 `DELETE /auth/unlock/{id}`。课反例 4 / 14 覆盖。
5. **「两份 Controller 都要 `system:user:edit`。」** 方法上没有 `@SaCheckPermission`。课反例 5 覆盖。
6. **「`profile()` 也 ignore，和 `getInfo` 一样空用户 `R.fail`。」** GET 不 ignore；空用户 NPE。课反例 6 覆盖。`updatePwd` 同一枪 NPE 未写成反例。
7. **「改密会踢掉所有设备 / 作废临时密码 / 清错次 / 驱逐昵称。」** 前三件都不做。昵称缓存只在 `updateUserProfile`。课反例 7 / 误区 11 覆盖踢票；0 行仍 Evict 未写成反例。
8. **「list 走分页；`SysSocial` 逻辑删因为 DDL 有 `del_flag`。」** `voList()` 全量；实体无 `@TableLogic`。课反例 8 覆盖分页；边界覆盖物理删。
9. **「home-web `/profile` 打本课 URL。」** home 是 `createProfileService`。课反例 10 覆盖。
10. **「社交 list 的 avatar 和用户头像都是 OSS id。」** 用户头像 bigint；社交 avatar 是第三方 URL。课反例 11 覆盖。
11. **「解绑会先确认贴纸属于我。」** 只认主键。课反例 13 覆盖。
12. **「同一 GitHub 再点一次 callback = 更新令牌。」** `authId` 已在就抛「已经被绑定」，**包括自己**。能 update 的是「同平台换了一个第三方账号」。课把两闸写成并列，读者会把占用抛错说成 upsert。
13. **「前端类型没有的字段 JSON 里就没有。」** `SocialAuthVO` 没有令牌字段，运行时 Vo 有。`UserProfileInfoVO.user` 类型是带 `createTime` 的 `UserVO`，运行时 `ProfileUserVo` **没有**该字段——左侧「创建日期」会一直是 `-`。课对令牌方向成立，对 createTime 方向只写了后半句。
14. **「L-011 已经认领 unlock，本课不用闭合。」** 邻课明确不认。把假设反转后，第七扇变成两课都不认的公开 DELETE。

问：读完后仍说不出的，是不是「两份类的名字」，而是 **`updateProfile` 算不算本格、unlock 算 list 的负空间还是孤儿窗、同一 authId 再绑会抛而不是 update、GET 镜子没有创建日期**？

## Q4 视角

- objective_id: OBJ-22
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)；OBJ-22 vs L-011/L-014/L-015/L-019/L-034；`AuthController.unlockSocial`；`createSystemService.users/resources`；`createProfileService`；API-005 PUT 存量
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于这两格？

| 视角 | 多出来的边 | 是否本课格子 |
| --- | --- | --- |
| 矩阵维护者 | (a) 三名 vs 磁盘四扇；`updateProfile` 口试必指但行名没有；(b) 无行；unlock 两课互推、矩阵 Auth 行也无此名 | **是** 闭合定义。不要求发明 (b) 就能标 (a) covered；unlock 要一句归属，不要 silently 标 Auth 行 covered |
| 资料页作者 | GET 未脱敏镜子、PUT 改发型、PUT 旧钥匙换锁；无权限串；ignore 只包写 | **是** (a) 三扇目的 + 失败句。`PlusDataPermissionHandler` SQL **否**（L-083） |
| 贴纸本作者 | list 按 userId；Vo 含令牌；解绑按主键物理删 | list 合同 **是** (a)。DELETE 实现 **是** list 的负空间（口试），不是第二容器 |
| 门厅（L-011） | 类上 `@SaIgnore`；callback 要登录、不发新票；binding 路条 | 三扇 HTTP 名字 **否**（已认六扇）。unlock 行为 **是** 本课应改口认领的负空间，不要重讲六扇 |
| 社交登录（L-013） | `grantType=social` → `selectByAuthId` 空则「还没有绑定」 | **否**。变式 F 一句即可 |
| 前端厨房（L-019） | `users.profile*` 长在 users 上；`resources.social.list`；`updatePassword` 的 encrypt + `repeatSubmit: false` | URL 对照 **可以一句**。八抽屉 / identity / 其余 resources **否** |
| 身份厨房（L-014） | `social.bindingUrl/unlock`；`socialLogin` 不看 `prepared` 也 `setToken` | URL 一句 **可以**。setToken 写手 **否** |
| 人事科（L-015） | `resetPwd` / candidate / temporaryPassword | 对照表 **是**。三份用户 Controller 全表 **否** |
| 档案楼（L-034） | home `/profile`、`createProfileService`、Person/Enterprise 五层 | 对照物 **是** OBJ-22 括号。申请/审核 **否** |
| 在线设备 / OpenAPI Tab | `monitorService.online.current`；OpenAPI 凭据页 | **否**。课已划 L-023 / L-030 |
| OSS 直传 | `ossUploadClient.upload(..., policy: 'avatar')` | 变式 B 一句 **可以**。init/sign/complete **否**（L-027 / L-029） |
| 菜谱（L-012） | `validateOrThrow` 字段表 | 闸的位置 **是** `updatePwd`。规则字段 **否** |
| API-005 | 存量 edit/改密用 PUT | **否**。本课盘点现状，不发动传输 Ratchet |
| Redis 会话 | 改密不 `kickout`；旧票仍有效 | 负空间一句 **是** (a) `updatePwd`。键形 **否**（L-084） |

问：Lead 若凭「两份类在 system、list/profile/updatePwd 都在函数表里」把两行 (a) 标 covered，是否还要求读者能指着 **磁盘第三扇 PUT、解绑不在 SysSocialController 且不按 userId、同一 authId 再绑会抛**？这些是不是已经（或应当）在函数表/反例里，而不需要 (b) 新行？

## Q5 元问题

- objective_id: OBJ-22
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 两行、无 (b)；L-011 把 unlock 交给本课
- difficulty: low

建议：`re-dispatch-L`

原因：

- 两格同属 OBJ-22、同一容器 `wta-system`。L 合同章节齐全（直觉、定义、机制链 1–6、带 caption / 文字等价物 / 图边界的三张 ASCII、正反例、变式、误区、来源表）。**主叙事站得住**：四扇 HTTP 与源码对齐；classic 无 UseCase；Social 只有 list；Profile GET 未脱敏、空用户 NPE；PUT 改资料 ignore + 唯一性 + 昵称缓存；PUT 改密旧锁 → 不能相同 → 菜谱 → 哈希 → ignore 写列、不踢票、日志不存体；贴纸写入在 `/auth/*`；Vo 含令牌；home `/profile` 是档案楼。不必为「窗口名字」再挖五问。
- 本批已有可粘贴的邻课/磁盘反例，属于**同一课的闭合定义与机制精度**，不是第二容器：① L-011 / GP-L-011 把 `unlockSocial` 交给本课，本课写「格子已在 L-011」——两课互推，矩阵无此名；② `socialRegister` 先按 authId 占用（含自己）抛，update 只覆盖「新 uuid + 同人同 source」，不是并列 upsert；③ `updateProfile` `@Log` 默认存请求体（与 `updatePwd` 四个 false 对照）；④ 矩阵/claimed 无 `updateProfile` 但磁盘/口试有。补丁应短：开篇加一张「点名窗 / 磁盘第三扇 / unlock 负空间不进 Auth 行」表；解绑改口「本课口试负空间，L-011 不认格，矩阵不要静默标 Auth 行」；门厅闸写成两行顺序；Profile PUT 日志加半句。不要把 L-019 工厂全表、L-013 发通行证、L-034 档案、L-083 数据权限 SQL、OSS 直传协议塞进 38 分钟课。
- 不建议再开一批问：重复本批证据。`mine-more` 只允许在 b01 已证明**真实矩阵缺口且五问不够写清**时开 b02——本批缺口是同一 OBJ 的归属句和闸序，一行补丁可写进原课。不建议拆课：未满 10 问；两格是 chain 原设计（Social 窗 + Profile 窗），不是两个 C4 容器；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议把 (a) 整格推迟：四扇在磁盘上，不是空符号。不建议发明 (b) 行：ignore / 不踢票 / 物理删是 (a) 的解释到副作用。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:SysSocialController.list | 一扇 GET、按当前 userId、无分页、无权限串、Vo 含令牌、XML 空、classic 持有 Mapper，均与源码对齐。残留：(1) 课把 unlock 推回 L-011，邻课不认；(2) `socialRegister` 两闸顺序被写成并列 upsert；(3) `SocialAuthVO` 继承 `BaseEntity`（有 `createTime`），不是「类型只有四列」。 | 开篇写清：本格 HTTP 只有 list；解绑/绑定是负空间口试，**不**把 Auth 第七扇写进本课 `claimed_cells`，也**不**说格子已在 L-011。闸序改两行。类型句改成「声明字段无令牌，运行时 JSON 有；createTime 在 BaseEntity 上」。 | re-dispatch-L（定义 + 一行闸序） |
| A:SysProfileController.profile, updatePwd | GET 未脱敏、不 ignore、空用户 NPE、角色组跟 Client、岗位组不跟、`ProfileUserVo` 丢掉 createTime/password；`updatePwd` 旧锁/菜谱/ignore/`@ApiEncrypt`/日志不存体/不踢票——均能复现。残留：(1) 磁盘第三扇 `updateProfile` 不在行名/claimed；(2) `updatePwd` 空用户同样 NPE；(3) `updateProfile` `@Log` 默认存请求 PII；(4) `rows==0` 仍驱逐 `SYS_NICKNAME`；(5) 前端 `updatePassword` 另带 `repeatSubmit: false`。 | 开篇表：点名窗 = 矩阵两名；口试仍要能指 PUT `/`。GET/改密失败句并写 NPE。改资料行补「日志默认可存请求体」和「0 行仍 Evict」。前端防重双层半句即可，不要扩成厨房课。Lead 可把 `updateProfile` 写入矩阵行名，或备注「口试必指、不另开格」。 | re-dispatch-L（定义 + 边界）；可选 Lead 改矩阵行名 |
| SysProfileController.updateProfile（未进 claimed_cells） | 课已讲覆盖 userId、唯一性、ignore、setIfPresent、头像对账、两枪不互清空。矩阵/claimed 没点名；磁盘是公开 PUT。 | 不要另开格子。与 L-017 add/edit 同处理：点名窗 vs 必指第三扇。 | re-dispatch-L（定义）或 Lead 写入 (a) 行 |
| AuthController.unlockSocial（矩阵无此名） | 行为已讲：要登录、按主键物理删、不核对 userId。L-011 交给本课；本课推回。 | 本课只留负空间口试。**不要**为第七扇改 OBJ-22 或新课。Lead 若要单独符号，改 Auth 行名或标 covered-by-parent of list，走矩阵不走 b02。 | defer（未入库符号）；本课改口 re-dispatch-L |
| A:createSystemService.users / resources | 课借用 `users.profile*` 与 `resources.social.list` URL，未覆盖工厂全表。 | 保持一句。全表 L-019。 | defer（L-019） |
| A:IdentityAccessService.social* | 课点 binding/unlock URL；不讲 `socialLogin` `setToken`。 | 保持。L-014 已标 social* 非其口试。 | defer（L-014） |
| A:AuthController.socialCallback, socialBinding | 课当侧门。六扇仍归 L-011。 | 保持。不要重讲 `@SaIgnore` 六扇。 | defer（L-011） |
| B:IAuthStrategy / SocialAuthStrategy | 未绑定不能 social grant。 | 变式 F 保持。发通行证 L-013。 | defer（L-013） |
| B:PasswordPolicyService | 闸的位置已讲；字段表未讲。 | 保持。 | defer（L-012） |
| A:SysUserController.resetPwd / candidate / issue | 对照表已有。 | 保持。 | defer（L-015） |
| A:createProfileService / Person* | 对照楼已画。 | 保持。 | defer（L-034） |
| A:SysUserOnlineController / OpenAPI / OssUpload | 个人中心 Tab 已划出。 | 保持。 | defer（L-023 / L-030 / L-027） |
| B:LoginHelper / StpUtil Redis；dao-mapper `@DataPermission` SQL | 点到不踢票 / ignore。键形与 handler 未讲。 | 保持。 | defer（L-084 / L-083） |
| (b) updatePwd / updateProfile 写库性状 | 矩阵无此行。课已把副作用写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；缺口落在同一 OBJ-22 的闭合定义与闸序精度（unlock 归属、`socialRegister` 顺序、`updateProfile` 日志/漏行），不是第二容器或讲不完的函数簇；把一张开篇表和两处反例写进原课不会超过 40 分钟；前端工厂、门厅六扇、档案模块、直传、在线设备已有后续课。满 10 问前不拆。两份 Controller 仍是同一 `wta-system` 容器。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / `split-proposal`：否（miner 不改矩阵）；`split-proposal` 为 none；缺口表对 L-019 / L-014 / L-011 六扇 / L-013 / L-012 / L-015 / L-034 / L-023 / L-030 / L-027 / L-084 / L-083 以及未入库的 (b) 行与 `unlockSocial` 符号显式建议 `defer`；对本课两行 (a) 与 `updateProfile` 漏行建议 `re-dispatch-L`。Lead 可在补丁后将矩阵 (a) 两行标 `covered`（证据锚本 probe 与 L-022 来源表），或先改 Profile 行名加上 `updateProfile` 再标。本课不主张改 OAuth、档案模块、前端工厂全表或任何 (b)(c)(d) 格子。
