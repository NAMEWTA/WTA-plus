# Evidence T-01

- Goal: OpsFlow Client SSO fields; wildcard redirect rejected; confidential hash stored; plaintext only once.
- Commands:
  - `./mvnw -pl wta-modules/wta-system,wta-admin -am test -Dtest=SsoRedirectUrisTest,SsoClientFieldsSupportTest,SysClientServiceSsoUnitTest -Dsurefire.failIfNoSpecifiedTests=false`
  - exit 0 (captured in `/tmp/grok-goal-64584c24c117/implementer/sso-unit-1.log` and `sso-unit-2.log`)
- Coverage: AC-017/018/019 via `SsoClientFieldsSupport` + `SysClientServiceImpl.insertByBo` negative (`*`) and confidential hash.
- Residual: live management UI not exercised (no MySQL in this environment).
