---
artifact: learning-goal-probe
probe_id: GP-L-080-b01
lesson_id: L-080
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-080 / b01

审问对象是已写成的 Lesson `lessons/L-080-snail-ai.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **一格**：`A:SnailAiController.registerCurrentUser`。OBJ-80 原文：能口述 `SnailAiController.registerCurrentUser`。档是**解释+走查**。矩阵 (a) **有独行**，符号与磁盘 1:1：`SnailAiController.registerCurrentUser`，模块 `wta-ai`，仍 uncovered，备注空。矩阵 (a) **没有** `ensureOpenApiUser` HTTP 行、没有 `OpenApiUserClient.register` 独行、没有 `createAiService` 混进本行。厨房 `createAiService` 仍 uncovered（L-081）。九工厂行里的 `createAiWebDomain` 仍 uncovered。矩阵 (b) **没有** 换工牌 / 两套信封 / `open-api`≠`openapi` 独行；已印的 (b) 不挂本课符号。矩阵 (c) `wta-modules` / `layered-classic` / `wta-admin` 已 covered（L-002 / L-003）；`bundle-full-job` 仍 uncovered（L-079）。矩阵 (d) **没有** 注册换 `openId` 走廊；已 covered 的是登录后查询与 OSS 直传等，不要发明一条 snail 注册流。本批按派单只审问这一格 (a) 切片；口试按磁盘：门牌 `/snail-ai` **正好 1** 个公开映射 `POST /snail-ai/user/register`，无 body、无权限字、须登录，身份只来自 `LoginHelper`，对面信封 `Result.status==1` 才 `R.ok`（`code=200`），失败抛厂商 `SnailAiException` 落到未知运行时席。不要把厨房一枪、监控玻璃、nginx `/snail-ai/`、或对面楼升成第二张 (a)，也不要把四把钥匙发明成 (b)/(c) 新行。

抽查锚点（2026-09-17）：`SnailAiController.java`；`wta-ai/pom.xml` 与模块目录（仅这一份业务 Java）；`SnailAiConfig.java`；`wta-common-ai/pom.xml` 与 `AutoConfiguration.imports`；`wta-admin/pom.xml` `bundle-full` / `bundle-core`；`application-{local,dev,prod}.yml` 的 `snail-ai` 段；`application.yml` `security.excludes`；`SecurityConfig`；`LoginHelper`；`SaTokenExceptionHandler`；`GlobalExceptionHandler`；`R` / `HttpStatus`；登记表 `03-backend-module-modes.md` 第 25 行；模块地图 `wta-ai` / `wta-common-ai` / `wta-snailai-server`；生成合同 `openapi.ts` `"/snail-ai/user/register"` / `OpenApiUserVO` / `ROpenApiUserVO`；厨房对照 `domains/ai/src/{index,index.test,transport,snail-ai/index}.ts`；菜单工厂对照 `web-domains/ai/src/index.ts`；厅堂 `services.ts` / `adminManifestRegistry.ts`；DML 第 42 / 69 行；nginx `location /snail-ai/`；`wta-snailai-server` `application.yml` context-path；父 POM `${snailai.version}=1.1.1`。工作树 **零** `SnailAiControllerTest`（`wta-ai` 无 `src/test`、无 `src/main/resources`）。厂商 starter **源码不在本仓**。

## Q1 澄清

- objective_id: OBJ-80
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-80；chain L-080 覆盖列只 `A:SnailAiController.registerCurrentUser`；课 `claimed_cells` 一格；矩阵 (a) 该独行仍 uncovered、无 Service/Vue/厨房行；(b) 无本课符号；(c) 房间与登记表已 covered、`bundle-full-job` 仍 uncovered；(d) 无换牌走廊；课开篇「正好 1 个」公开映射 + 负空间 L-081 / L-024 / L-078 / L-002 / L-003 / L-030…L-033
- difficulty: medium

课把这一头收成「宏观一扇注册窗：酒店房卡换对面工牌」。计划里至少有四套可数对象，派单点名 **一格 (a) 切片**：

1. **OBJ-80 原文。** `course.md`：能口述 `SnailAiController.registerCurrentUser`。一句里是 **走查这一扇 HTTP**，不是厨房 `registerCurrentSnailUser`，不是菜单 `ai/chat/index`，不是监控 `monitor/snailai/index`，也没有把 `OpenApiUserClient.register` 写成第二张矩阵行。课把 OBJ 扩成换工牌链 + 四把钥匙 + 两套信封，那是走查，不是第二格。
2. **chain / 课 `claimed_cells`。** 一格。符号就是这一个方法名。矩阵把它印成独行，与磁盘公开映射 1:1。chain **不**写 Service、不写 Vue、不写 (b)。`ensureOpenApiUser` **没有**自己的 HTTP。
3. **矩阵 (a) 对齐。** 本课有独行，Lead 按现印方法名标 covered **不会**把厨房或九工厂假闭合。不要把 `createAiService` 或 `AiChatPage.vue` 另立一行才给本切片盖章。厨房行与九工厂行仍 uncovered，本课不得当第二张前端章。
4. **课自己的操作定义。** 门牌 `/snail-ai`；公开映射正好 1：`POST /user/register`。类上 `@ConditionalOnProperty(prefix = "snail-ai.open-api", name = "enabled", havingValue = "true")`。无 `@SaCheckPermission`、无 `@SaIgnore`、无 `@Log`、无 `@RepeatSubmit`、无 `@DataPermission`。方法无入参。身份 `LoginHelper.getUserId()` / `getLoginUser()` → `externalId` / `nickname`。成功 `R.ok(vo)`，`R.code` 走 `HttpStatus.SUCCESS = 200`；厂商成功数字是常量 `SNAIL_AI_SUCCESS = 1`。
5. **钥匙与信封不是第二张 claimed 格。** OBJ 要「能口述」这一扇。课把 jar / 窗 / 电话 / Agent 四把钥匙、以及 `Result`→`R` 的翻译收进对照走查。不要为了盖章把 `SnailAiConfig` 或 `createAiService` 再立一行，也不要把「yml 写了 `open-api.enabled: true`」说成电话 Bean 一定在。
6. **邻格。** L-081 厨房一枪 + `createAiWebDomain` 一键。L-024 外置玻璃 `snail-ai`（权限 `monitor:snailai:list`）。L-078 job 同样瘦、同样 classic，但 **零扇 HTTP**。L-002 已认 `bundle-full` 才插 `wta-ai`。L-003 已认 classic 行不是一种家具。L-030…L-033 是 NAMEWTA 机器 OpenAPI，楼不一样。对面 JVM `wta-snailai-server` 保持 deferred。

问：本格闭合标准是「一扇路径/动词/无权限字/无 body + 登录门卫 + `LoginHelper` 填表 + `status==1` 才 `R.ok` + 失败不回控制器中文 + 四把钥匙分开说（连字符是对照），且不升厨房/Vue 矩阵行、不发明 (b)/(d)、不提前盖 L-081」？还是 OBJ 那句必须把 Client/Config 拆成两行、或必须等 GP-L-081 厨房缺枪才给本切片盖章？Lead 若凭「ai 房间还没挖完」把这一行留空洞，是否假判成未讲完？Lead 若用本课把厨房或九工厂标 covered，是否假闭合了 L-081？

## Q2 证据

- objective_id: OBJ-80
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: Controller 3 / 26–32 / 37–38 / 43–46 / 51–72；wta-ai 目录仅 Controller + pom + AGENTS；pom 18–45；SnailAiConfig 11–15；common-ai pom 27–42；imports 仅 `SnailAiConfig`；admin pom 147–205；backend pom 47；local yml 49–50；dev yml 32–76；prod yml 35–77；application.yml 272–287；SecurityConfig 86–126；LoginHelper 104–109 / 150–152 / 205–209；SaTokenExceptionHandler 44–55；GlobalExceptionHandler 64–65 / 97–98 / 136–138 / 158–163；R 12 / 75–76 / 130–131；HttpStatus 12 / 82；modes.md 25；module-map 21 / 50 / 53；openapi.ts 1736–1755 / 18453–18480 / 9450–9469；厨房 index.ts 20–42；transport.ts 6–12；index.test.ts 14–30；资源标签 4；web-domain 17–37；adminManifestRegistry 18 / 73–76 / 311 / 371；DML 42 / 69；nginx-lb-http 106–107；snailai-server yml 1–4；零 `SnailAi*Test`；零 `userClient.getUser`；厂商 starter 源码不在本仓
- difficulty: high

课的一扇窗 + 换工牌链哪些能在工作树直接复现，哪些只是口试把邻课窗说成本格第二扇、或把「HTTP 500」说成磁盘上的 `@ResponseStatus`？

**仍能复现（本切片）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 类上 `@Slf4j` `@Validated` `@RestController` `@RequestMapping("/snail-ai")` `@RequiredArgsConstructor` `@ConditionalOnProperty(prefix = "snail-ai.open-api", name = "enabled", havingValue = "true")`，继承 `BaseController` | Controller 26–32 |
| 构造只注入 `OpenApiUserClient`；不抱 Mapper / Service | Controller 38 |
| 公开映射正好 1：`POST /user/register` → `registerCurrentUser` | Controller 43–46；模块 Java 树只有这一份 |
| **无** `@SaCheckPermission` / `@SaIgnore` / `@Log` / `@RepeatSubmit` / `@DataPermission` | 类文件 grep 空 |
| 方法无入参；生成合同 `requestBody?: never`，另有 401 槽 | Controller 44；openapi 18453–18480 |
| 成功返回 `R<OpenApiUserVO>`，走 `R.ok(ensureOpenApiUser())` | Controller 44–46；R 75–76 |
| 常量 `SNAIL_AI_SUCCESS = 1`；`R` 成功码 `HttpStatus.SUCCESS = 200` | Controller 37；HttpStatus 12 |
| 空会话：`loginUser == null \|\| userId == null` → `SnailAiException("当前登录用户为空")` | Controller 52–56 |
| `externalId = String.valueOf(userId)`；`nickname = loginUser.getNickname()`；**不**设 `avatarUrl` | Controller 58–60 |
| 只调 `userClient.register`；空 Result / `status != 1` / 空 data 各抛一次 | Controller 61–71 |
| 厂商异常包名是 `execption`（少一个 e） | Controller 3 |
| `LoginHelper.getLoginUser()` 吞 `NotLoginException` 变 null；`getUserId()` 经 `getExtra`，异常也变 null | LoginHelper 104–109 / 150–152 / 205–209 |
| `/snail-ai/**` **不在** `security.excludes`；排除的是 `/snail-chat/**` 与 `/api/snail/chat/**` | application.yml 274–287 |
| 未排除路径先 `StpUtil.checkLogin()`，再核 `clientid`（已验证 openapi 机器身份跳过） | SecurityConfig 93–115 |
| 未登录主路：`SaTokenExceptionHandler` 对 `NotLoginException` 回 `R.fail(HTTP_UNAUTHORIZED, …)`，方法体到不了 | SaTokenExceptionHandler 44–55 |
| `GlobalExceptionHandler` 有 `ServiceException` / `BaseException` 专席，**没有** `SnailAiException` | GlobalExceptionHandler 64–65 / 97–98；全文件无该符号 |
| 未知运行时席文案「发生未知异常，请联系管理员 [错误编号: " + 8 位数字 + "]」 | GlobalExceptionHandler 158–163；R.fail 走 `ERROR = 500` |
| local：`snail-ai.enabled: false`，**未写** `open-api` 段 | local yml 49–50 |
| dev/prod：`enabled: false`，`open-api.enabled: true`，`web-port: 8900`，`prefix: snail-ai`，种子 `app-id: 1` / `token: SAI_566a6bfbc26e4998b4841cc927d50c5d`；**未写** `openapi.enabled` | dev 32–76；prod 35–77 |
| `SnailAiConfig` 听 `snail-ai.enabled=true` 才 `@EnableSnailAiAgent` + `@EnableSnailAiOpenApi` | SnailAiConfig 11–15 |
| common-ai 的 Boot imports **只**登记 `SnailAiConfig` | `AutoConfiguration.imports` 第 1 行 |
| `wta-common-ai` 运行时依赖只被 `wta-ai` 拉（BOM 只锁版本） | wta-ai pom 31–34；BOM 193；grep 无第三份 runtime 依赖 |
| 五份模块依赖：core / api / common-ai / satoken / web；无 Mapper 起步 | wta-ai pom 18–45 |
| `bundle-full`（默认）插入 `wta-ai`；`bundle-core` **不插** | admin pom 147–205 |
| 登记 classic：`wta-modules/wta-ai`，「第三方 starter 适配遵守模块边界」 | modes.md 25 |
| 模块地图：`wta-ai` 入口是 package surface、测试根 none；对面楼是 `wta-extend/wta-snailai-server` | module-map 50 / 53 |
| 父 POM `${snailai.version}=1.1.1`；common-ai 拉三份厂商 starter | backend pom 47；common-ai pom 27–42 |
| VO 字段 `openId` / `externalId` / `nickname` / `avatarUrl` / `created`；厨房投影丢掉 `avatarUrl` | openapi 9450–9456；transport.ts 6–12 |
| 厨房只打 `{ url: '/snail-ai/user/register', method: 'post', headers: { repeatSubmit: false } }`，无 body | index.ts 32–37；index.test.ts 28–30 |
| 资源标签 `controller: 'SnailAiController'`，`basePath: '/snail-ai'`，**不**发 HTTP | snail-ai/index.ts 4 |
| 菜单「AI会话」`ai/chat/index` 权限空串；「AI控制台」`monitor/snailai/index` 权限 `monitor:snailai:list` | DML 42 / 69 |
| 厅堂外置键 `admin-monitor-snailai` → 组件名 `SnailAi`、target `snail-ai`；管理端另选 `'web-domain-ai'` | adminManifestRegistry 311 / 371 |
| nginx `location /snail-ai/` → `snailai_server/snail-ai/`；对面进程 context-path `/snail-ai`、端口 8900 | nginx-lb-http 106–107；snailai-server yml 1–4 |
| 工作树 **零** `src/test`、**零** `src/main/resources`、**零** service/mapper | `find wta-ai` 只有 Controller + pom + AGENTS |

**课写过、磁盘更窄或口试容易写偏（不是证伪本格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「`@ExceptionHandler(RuntimeException.class)` + `@ResponseStatus(500)` → HTTP 500」 | `RuntimeException` 席 **没有** `@ResponseStatus`（158–163）。`@ResponseStatus(INTERNAL_SERVER_ERROR)` 贴在 **`IOException`** 席（136–138）。失败回执的 **信封码** 是 `R.fail` → `ERROR = 500`；HTTP 状态未在本席改写。文案仍是带 8 位编号的未知异常，控制器中文仍看不到 |
| 「厂商客户端听 `snail-ai.openapi.enabled`」 | 本仓能指：Controller 听 `open-api`；yml 只写带连字符的段；`SnailAiConfig` 听 `snail-ai.enabled`。厂商 `SnailAiOpenApiAutoConfiguration` **源码不在工作树**，本课也写了未实跑 `NamewtaApplication` |
| 「classic 脚本缺 resources 退出码 1」 | 本模块确无 `src/main/resources`。脚本行为已由 L-003 钉；本课不要再盖登记表 |
| 口试把「失败 HTTP 500」与「`R.code` 500」说成同一根针 | 匿名主路连方法体都进不去；进了方法再抛，走未知运行时席的是 **信封码 500 + 通用文案** |
| 课 `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |
| 「厨房会按铃」 | 厨房测试锁这一枪 URL。工厂盖章是 L-081，不是本格缺证据 |

问：在 一扇 POST、无权限字、无 body、`LoginHelper` 填 `externalId`/`nickname`、三处空/失败 throw、`status==1` 才 `R.ok`、excludes 不含 `/snail-ai/**`、local 关窗、dev/prod 只开带连字符的钥匙、`bundle-core` 整间房不进包、厨房对照无 body  已经能互证的前提下，(a) 是否还要把「RuntimeException 席没有 `@ResponseStatus`」「厂商 AutoConfiguration 源码不在仓」写成开篇半句，才算本格讲完？还是这些已经落在课的失败席/边界 1 里，Lead 可以把 **这一独行** 视为可 covered，并把厨房/九工厂保持 uncovered？

## Q3 假设与反例

- objective_id: OBJ-80
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无第二扇 GET/chat；无 `@SaCheckPermission`；无 `@SaIgnore`；无 request body；`R` 成功 200 不是 1；无 ServiceImpl/Mapper；窗钥匙是 `open-api` 不是 `snail-ai.enabled`；yml `open-api` ≠ 厂商 `openapi` 词；监控 iframe 不打本窗；nginx `/snail-ai/` 进对面楼；失败不回控制器中文；不 return `Result`；零调用 `getUser`；`@Validated` 无入参可校验；RuntimeException 席无 `@ResponseStatus`；厨房/九工厂不是本格
- difficulty: high

课的隐含假设若反转，合格读者会把哪扇窗或哪把钥匙认错？

1. **「`wta-ai` 有一套 AI CRUD / chat HTTP。」** 公开映射只有注册。课反例 1 / 误区 1 / 14 覆盖。
2. **「这就是 Snail AI 服务器。」** 服务器是 `wta-extend/wta-snailai-server`。课反例 2 / 误区 3 覆盖。
3. **「`snail-ai.enabled: false` 所以这扇窗一定不在。」** 窗听 `open-api.enabled`。课反例 3 / 误区 8 覆盖。
4. **「yml 已经启用 OpenAPI Client。」** 注释写在 `open-api` 段下。课反例 4 / 误区 9 / 边界 1 覆盖。
5. **「失败时浏览器显示『当前登录用户为空』。」** 默认未知运行时席换成带编号的通用句。课反例 5 / 误区 6 / 7 覆盖。
6. **「`status=1` 就是 HTTP 200，可以不检查 data。」** 三条都查：null Result、status、null data。课反例 6 覆盖。
7. **「classic 必须有 ServiceImpl / Mapper。」** 登记的是模块模式。课反例 7 / 8 / 误区 2 覆盖。
8. **「监控 AI 控制台会调用 `registerCurrentUser`。」** iframe 对面楼；权限字都不是同一套。课反例 9 / 误区 10 覆盖。
9. **「nginx `/snail-ai/` 会进这个 Controller。」** LB 指到 snailai-server:8900。本窗挂在 admin 的 `/{env}-api` 后面。课反例 10 / 误区 3 覆盖。
10. **「可以在 body 里帮同事注册。」** 合同无 body。谁登录换谁的牌。课反例 11 覆盖。
11. **「本课覆盖 `createAiService`。」** OBJ-81 / L-081。课反例 12 / 误区 11 覆盖。
12. **「`OpenApiUserClient.getUser` 也是本窗。」** 接口可能有；本课 Java **零调用**。课反例 13 覆盖。
13. **「`@Validated` 会校验昵称非空。」** 方法无入参，Request 是方法内 new。课反例 14 覆盖。
14. **「厂商 `Result` 会原样给前端。」** 返回类型是 `R<OpenApiUserVO>`。课反例 15 / 误区 5 覆盖。
15. **「没权限字 = 匿名可调。」** 拦截器先 `checkLogin`。课误区 7 / 15 覆盖。
16. **「`ensureOpenApiUser` 也是一扇 HTTP。」** 它是 private 帮手。课误区 13 覆盖。
17. **「失败席带 `@ResponseStatus(500)`，所以浏览器看到的是 HTTP 500。」** 磁盘把 500 状态注解放在 `IOException` 席；本窗抛出的是 `RuntimeException` 子类，回的是 `R.code=500` 的通用信封。课把两席写在一起——这是本批要钉的精度，不是第二格。
18. **「OBJ-80 包含系统 OpenAPI 签名。」** 那是 L-030…L-033。课定义表末行覆盖。

问：读完后仍说不出的，是不是「Controller 的名字」，而是 **一扇合同 ≈ 磁盘映射、身份不从 JSON 来、1 与 200 不要对调、失败看不到 throw 中文、四把钥匙差一个连字符、厨房/玻璃/LB 不是本窗、九工厂/厨房行不能因本课闭合**？

## Q4 视角

- objective_id: OBJ-80
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-80 vs L-002/L-003/L-024/L-030/L-078/L-079/L-081；claimed_cells 一扇 vs 厨房一枪 / 一键菜单；厨房行与九工厂行仍 uncovered
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这一格 (a) 切片**？

| 视角 | 多出来的边 | 是否本课这一格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 独行 vs chain 一方法；无 Service/Vue 行；(b) 无本课独行；(d) 无换牌走廊 | **是** 闭合定义。本切片可视为已讲。独行可标 covered。不要发明 (b)/(d)，不要把厨房升成新行才能给切片盖章，不要用本课盖厨房章 |
| 注册门卫 | `POST /snail-ai/user/register`、无权限字、无 body、须登录、`LoginHelper` 填表、`status==1` 才 `R.ok` | **是** 切片里的 `registerCurrentUser` |
| 信封翻译官 | 厂商 `Result.status` 1/0 vs 我们 `R.code` 200/500；失败 throw 不是 `R.fail(message)` | **是** 走查后果，不是矩阵第二格 |
| 钥匙保管员 | jar=`bundle-full`；窗=`open-api.enabled`；电话前缀课称为 `openapi`；Agent=`snail-ai.enabled` | **是** 对照。不要另开 (c) 行。厂商 AutoConfiguration 源码不在仓，保持课已写的「未实跑」 |
| 失败席 | 无 `SnailAiException` 专席；通用文案带 8 位编号；`@ResponseStatus(500)` 不在 RuntimeException 席 | **是** 对照精度。不要发明 fail-snail (b) 行 |
| 厨房作者（L-081） | 一枪 URL、无 body、`repeatSubmit: false`、投影丢 `avatarUrl` | 本课借用封面形状 **是**。工厂盖章 **否**（L-081） |
| 菜单工厂（九工厂行） | `createAiWebDomain` 一键 `ai/chat/index`；厅堂选 `'web-domain-ai'` | 负空间 **是**。整行闭合 **否** |
| 监控玻璃（L-024） | `monitor/snailai/index` + `externalIntent('snail-ai')` | 分流对照 **是**。厨房 `createMonitorService` **否**（已 covered） |
| job 入口（L-078） | 同样 classic 瘦模块，零扇 HTTP | 有窗/无窗对照 **是**。执行器格子 **否** |
| job bundle（L-079） | `bundle-full` 同时插 job 与 ai | 装配对照 **是**。`C:bundle-full-job` **否** |
| 登记表（L-003） | classic 允许没有 Mapper 链；禁止抄新模块 | 不改登记 **是**。房间画像 **否**（已 covered） |
| 装配（L-002） | full 才进 admin；core 整间房不进包；extend 另有 `SnailAiServerApplication` | 电闸对照 **是** (a)。容器 **否**（已 covered） |
| 系统 OpenAPI（L-030…L-033） | 机器调 NAMEWTA 自己；头不是 `Snail-Ai-App-Id` | 先说哪栋楼 **是**。签名协议 **否**（已 covered） |
| LB / 对面楼 | nginx `/snail-ai/` 与 context-path `/snail-ai` | 三条河对照 **是**。厂商引擎内部 **否**（deferred） |
| MySQL 8.4 | 菜单两行种子，本窗无库表 | 种子对照 **是** (a)。容器 **否** |

问：Lead 若凭「一个方法名在独行里」把该行标 covered，是否还要求读者能指着 **一扇合同、登录门卫、`LoginHelper` 填表、1≠200、失败通用信封、四把钥匙、厨房/玻璃/LB 分流**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要等 L-081 才给**本切片**当已讲？厨房行是否必须保持 uncovered，本课只往备注写「注册窗消费已讲」？

## Q5 元问题

- objective_id: OBJ-80
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 独行仍 uncovered、厨房行仍 uncovered、九工厂行仍 uncovered、无本课 (b)/(d)；U8 已 15 节不得再塞 split
- difficulty: low

建议：`defer`

原因：

- **`A:SnailAiController.registerCurrentUser` 是 chain 切片，也是矩阵独行。** L 合同章节齐全（宏观一扇窗地图、直觉十二句、定义表、机制链 1–6、带 caption / 文字等价物的三张图、正反例、变式、误区、暂停走查、来源表 S-004/S-007/S-008/S-010/S-L002-01/S-L003-01/S-L080-01…09）。能复现：唯一 `POST /snail-ai/user/register`、无权限字、无 body、`LoginHelper` → `externalId`/`nickname`、只喊 `register`、三处失败 throw、`SNAIL_AI_SUCCESS=1` 对 `R.code=200`、excludes 不含本窗、local 无 `open-api` 段、dev/prod 只开带连字符的钥匙、`bundle-core` 不进包、classic 无 Mapper、厨房对照无 body。不必为「Controller 的名字」再挖五问。
- **Lead 可以把本课这一行标 `covered`。** 方法名与磁盘 1:1，不会绑死厨房或九工厂。**厨房行与九工厂行保持 uncovered**，等 L-081，不要用本课开第二张前端章。不要发明换牌 (b) 行：两套信封 / 四把钥匙 / 失败通用文案已是 (a) 的解释到副作用。
- 本批可粘贴的精度项不挡住切片视为已讲：（1）未知运行时席 **没有** `@ResponseStatus`，500 先落在 `R.code`；课把 IOException 席的状态注解写到了本窗失败路上；（2）厂商 `openapi` 前缀源码不在仓，课已写未实跑启动；（3）课估时 38、chain 35。不要把厨房 URL 全表、iframe 玻璃、job 执行器、系统 OpenAPI 签名塞进本格。
- 不建议 `mine-more`：b01 未证明本切片还有真实矩阵空洞；再五问只会把 L-081 厨房、L-024 玻璃或 L-079 bundle 再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；HTTP 状态注脚是半句精度，不是第二容器。不建议 `split-lesson`：未满 10 问；一扇窗是 chain 原设计的同一 OBJ-80，不是两个 C4；补丁不突破 40 分钟；U8 已满 15，当前 unit 禁止再塞子节点。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b)/(d) 行。不建议等 GP-L-081 才给本切片当已讲。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:SnailAiController.registerCurrentUser | 课按磁盘讲了一扇路径/动词/无权限字/无 body、登录门卫、`LoginHelper` 填表、`status==1` 才 `R.ok`、失败不回 throw 中文、四把钥匙、厨房/玻璃/LB 分流、classic 无 Mapper。残留精度：RuntimeException 席无 `@ResponseStatus`（500 是 `R.code`）；厂商 AutoConfiguration 源码不在仓；课估时 38 vs chain 35。 | 可选半句：失败回执以未知运行时席的信封码 500 + 通用文案为准，不要把 IOException 席的状态注解安到本窗。不要求重写机制链。Lead **可以**改本行为 covered。 | defer（切片已讲；独行可闭合） |
| A:createAiService | 厨房行仍 uncovered。本课只借用一枪 URL、无 body、`repeatSubmit: false`、投影丢 `avatarUrl`。 | 不要用本课盖厨房章。备注可写「注册窗消费已讲」。 | defer（L-081） |
| A:createAiWebDomain（九工厂行） | 本课只用 `ai/chat/index` 会按铃、监控键不按铃当切片证据。其余工厂未因本课讲完。 | 保持 uncovered。 | defer（L-081 / 九工厂行保持 uncovered） |
| A:createMonitorService / monitor snailai 玻璃 | 外置键 `snail-ai` 对照已借用。该行已 covered。 | 不要用本课再盖四块屏。 | defer（L-024 已 covered） |
| A:AlipayBillTask… / job 入口 | 零扇 HTTP 对照已借用。 | 保持。L-078。 | defer（L-078） |
| C:bundle-full-job | full 同时插 ai 与 job 是装配对照。 | 不要用本课盖 job bundle。 | defer（L-079） |
| C:layered-classic / wta-modules | 登记表 classic 行已 covered。 | 不要用本课再盖房间章，也不要发动迁 layered。 | defer（L-003 已 covered） |
| C:wta-admin / bundle-full 插 ai | 类在不在 jar 已作钥匙 A。容器已 covered。 | 不要因本课再盖 L-002。 | defer（L-002 已 covered） |
| SysOpenApi* / 机器 OpenAPI | 楼不一样的对照已点名。 | 保持。L-030…L-033 已 covered。 | defer（已 covered） |
| (b) 两套信封 / 四把钥匙 / 失败通用文案 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 注册换 openId 流 | 矩阵无此行。 | 不要发明 snail 注册数据流行。 | defer（未入库） |
| wta-snailai-server 厂商引擎 | 幂等 `appId+externalId` 只作对照。 | 保持 deferred。 | defer（厂商内部 deferred） |
| 测试夹具 SnailAi*Test | 工作树没有这类测试。 | 保持 deferred(test-fixture)。证据走 Controller/配置/生成合同/厨房对照。 | defer（deferred(test-fixture)） |
| MySQL 8.4 / Redis 8 | 菜单种子是 (a) 对照，不是容器闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；本切片无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`（厨房/菜单/玻璃/job 是邻课格子，课内已把一扇窗讲完并显式禁止整行假闭合）；合格读者说得出函数目的（宏观一扇注册窗、当前用户换 `openId`）、主路径（登录会话 → `LoginHelper` 填 `externalId`/`nickname` → `OpenApiUserClient.register` → `status==1` 且 data 非空 → `R.ok`）与失败路径（匿名在拦截器停、进方法后空用户/空 Result/status≠1/空 data 抛厂商异常并落到未知运行时席、local 缺窗钥匙、core 包类不在 jar）；把可选半句写进原课不会超过 40 分钟；邻课已规划。满 10 问前不拆。U8 已 15 节，当前 unit 不得再追加 split 子节点。一扇窗虽跨钥匙/信封/对面楼对照，仍是 chain 原设计的同一 OBJ-80，不是「超过 1 个容器却讲不完」。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / split 提案：否（miner 不改矩阵）；split 提案为 none；缺口表对厨房/九工厂、L-024 玻璃（已 covered）、L-078 / L-079 邻窗、L-002 / L-003 已 covered 的装配与登记表、L-030…L-033 机器 OpenAPI、未入库的 (b)/(d)、deferred 的厂商引擎、deferred 的测试夹具、以及 MySQL/Redis 容器显式建议 `defer`；对 `A:SnailAiController.registerCurrentUser` 建议视为已讲切片、**可以**把矩阵该独行标 `covered`、**不要**用本课盖仍 uncovered 的厨房行与九工厂行。本课不主张改 Java 窗、发明 Vue 矩阵行、或把 `ensureOpenApiUser` 升成第二扇 HTTP。
