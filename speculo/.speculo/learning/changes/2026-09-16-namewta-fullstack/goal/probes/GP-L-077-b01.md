---
artifact: learning-goal-probe
probe_id: GP-L-077-b01
lesson_id: L-077
audience: mine
batch: b01
question_budget_used: 5
split_depth: 0
status: closed
---

# Probe GP-L-077 / b01

审问对象是已写成的 Lesson `lessons/L-077-demo-frontend.md` 与当前工作树 `/srv/WTA-plus`，不是学习者。禁止问答目录协议、行首应答字段、行首提交字段、判定字段、掌握标记。`b01` 承载 Q1–Q5。miner 不得写 `lessons/`。

本课 `claimed_cells` / chain 覆盖列 **三格**：`A:createDemoService`、`A:createRichTextService`、`A:createDemoWebDomain`。OBJ-77 原文：能说明 `createDemoService` / `createRichTextService` 与 demo web-domain。档是**解释+对照**。矩阵几何 **不是 1:1 三行**：

- 两座厨房落在 **同行** `createDemoService, createRichTextService`（模块 `domains/demo`，仍 uncovered，备注空）。
- 菜单工厂 **没有独行**；符号落在 **九工厂同行** `createAdminWebDomain, createSystemWebDomain, createProfileWebDomain, createNotifyWebDomain, createThirdWebDomain, createWorkflowWebDomain, createDemoWebDomain, createAiWebDomain, createProfileSelfWebDomain`（模块 `web-domains/*`，仍 uncovered，备注「manifest 工厂」）。

矩阵 (b) **没有** 三键注册 / `encodeId` / 导出走 download / PUT 对 POST 独行；已印的 demo 相关 (a) 是 `TestDemoController.*`（L-074）、`TestTreeController.*`（L-075）、`TestRichTextController.*`（L-076），均仍 uncovered。矩阵 (c) `admin-web` / `home-web` 容器与 `frontend-direction` / `admin-web-composition` 已 covered（L-004 / L-007 / L-008）。矩阵 (d) **没有**浏览器演示菜单流；已 covered 的走廊是登录后查询与 OSS 直传（L-006 / L-027 / L-029）。本批按派单审问 **厨房同行两座工厂 + 菜单工厂这一颗切片**；口试按磁盘：根 `src/index.ts` 冻住表/树 **10** 扁平枪 + 嵌套 `richText`，`rich-text.ts` 冻 **5** 枪 + 原样 `assets` 口，路径 `encodeId` / `encodeIds`，只给单表投影、丢掉 `version`，缺第二参 upload throw；菜单 id `web-domain-demo`、`domainId` `'demo'`、先 `requireDemoWebRuntime` 再冻 **3** 键；页面经 `runtime.service` 扣扳机，单表导出走 `runtime.download`，贴图走厅堂资产口。不把九工厂行整行闭合，不把 L-007 插头再盖第二次章，也不把 Vue 文件名升成矩阵行。单表八扇留给 L-074；树表六扇留给 L-075；富文本窗与 OSS 解析留给 L-076；直传三层留给 L-029；导航 host / 九工厂行警告留给 L-020；厅堂插座板全表留给 L-007 / L-008。加密/批量那些 Controller 保持 `deferred(capability-demo-not-product-room)`。

抽查锚点（2026-09-17）：`frontend/packages/domains/demo/src/{index,index.test,rich-text,transport,transport.test}.ts`；`src/{test-demo,test-tree}/index.ts`；包 `package.json` `exports` / `AGENTS.md` / `README.md`；`frontend/packages/web-domains/demo/src/{index,index.test,runtime,composables}.ts`；`test-demo/DemoPage.vue` / `test-tree/TreePage.vue` / `test-rich-text/RichTextPage.vue`；`apps/admin-web/src/application/services.ts` 第 4 / 54–69 行；`application/http.ts` 第 93–107 行；`router/adminManifestRegistry.ts` 第 19 / 78–85 / 355 / 365–368 行与 `.test.ts` 第 87 行；`apps/home-web/src/application/services.ts`；`homeManifestRegistry.ts` 第 34–39 行；`50-cde-base-dml.sql` 第 158–169 / 1702–1719 行；`30-cde-workflow.sql` 第 309 行；`TestDemoController` / `TestTreeController` / `TestRichTextController`；`adapters/axios-browser` `downloadWithAxios` 第 261–268 行；`application.yml` `richtext-image|audio|video|file`；`frontend/e2e/demo-manifest.spec.ts`；档案楼 `requireProfileWebRuntime`；通知/三方 web-domain 无 `require*WebRuntime`。

## Q1 澄清

- objective_id: OBJ-77
- bloom_level: analyze
- socratic_move: clarification
- expected_evidence: course.md OBJ-77；chain L-077 覆盖列三格；课 `claimed_cells` 三格；矩阵 (a) 厨房同行仍 uncovered、九工厂同行仍 uncovered、无 Vue 行、无本工厂独行、无 `createRichTextService` 独行；(b) 无本课符号、三份 Controller 挂 L-074…L-076；(c) 两厅堂已 covered；(d) 无浏览器演示菜单流，已 covered 的是登录后查询与 OSS 直传；课开篇「口试名单就是矩阵 (a) 这三格」+「不要把整行九厂标成 covered」+ 负空间 L-074…L-076 / L-007 / L-008 / L-020 / L-029
- difficulty: medium

课把浏览器演示这一头收成「两本出餐单钉在一起 + 大厅墙上的三行点菜单」。计划里至少有六套可数对象，派单点名 **三格 (a)**，但矩阵把它们印成 **两种几何**：

1. **OBJ-77 原文。** `course.md`：能说明 `createDemoService` / `createRichTextService` 与 demo web-domain。一句里是 **两座厨房工厂 + 菜单工厂**，不是单表八扇、不是树表六扇、不是富文本 `/assets` 后窗，也没有把九个 `create*WebDomain` 写成口试清单。课把 OBJ 扩成「15 枪 URL + 三键怎么喂页面 + 四处故意错位」，那是走查，不是第四张矩阵行。
2. **chain / 课 `claimed_cells`。** 三格。符号就是这两个厨房名加菜单工厂名。磁盘上 **没有** `createDemoTableService` / `createTreeService` / `createEncryptDemoService`，也 **没有** `@namewta/domain-demo/rich-text` 小厨房。管理端菜单 id `web-domain-demo`，三键一次冻住。chain **不**写 Vue 文件名，也 **不**写 `buildTree`。
3. **矩阵 (a) 两处不对齐。** 厨房同行把本课前两格绑死：Lead 若按该行标 covered **会**同时盖 `createDemoService` 与 `createRichTextService`——本课两格都讲了，这是 1:1，不是假闭合。九工厂行把本课第三格和 `createAiWebDomain` 等邻居绑死。Lead 若按**现印同行**标 covered，会把 L-020 / 未写的 ai 菜单假闭合。矩阵 **没有** 三张 Vue 符号，也没有 `RichTextPage.vue` 行，也没有 `createRichTextService` 独行。
4. **课自己的操作定义。** 表/树厨房只认 `HttpClient`，第二参可选 `RichTextService`，返回 `Object.freeze` 的 `DemoService`：单表 5 / 树表 5，外加嵌套 `richText`。路径 id 走私有 `encodeId` / `encodeIds`。只给单表 list/get 投影。封面 **没有** `pageDemo` / `importDemo` / `exportDemo` / `exportTree`。富文本厨房认 `HttpClient` **和** `RichTextAssetsPort`，五枪写路径全 POST，`assets` 原样挂上。菜单工厂先 `requireDemoWebRuntime`（缺托盘 throw `'DemoWebRuntime is required'`），再冻三键；**不** `addRoute`，**不**拼 `/demo/demo/list`。导出字符串走宿主 `download` 口。贴图走厅堂 `richTextAssets`，不进十五枪。
5. **四处故意错位是走查，不是第四格。** 厅堂单例导出名 `demoService` ≠ 两座工厂名；`createRichTextService` **不单独导出**。单表导出走 `runtime.download('demo/demo/export', …)`，freeze 对象上搜不到 `exportDemo`。树表后端有 `GET /export`、种子有 F，厨房没有枪、权限组没收、页没有按钮。`GET /page` 与 `POST /importData` 后端窗在，厨房方法表没有。不要为了盖章把 `exportDemo` 或 `/assets` 再立一行。
6. **邻格。** L-074…L-076 三份 classic 窗。L-007 厅堂插头（先 `richTextAssets` 再两座工厂套在一起，导出名已点过，没有 15 枪 URL 表）。L-020 九工厂行警告。L-029 OSS 直传。L-054 / L-066 对照：notify/third 工厂 2026-09-17 **不** fail-closed。L-044 对照：档案楼缺十一口当场 throw。L-073 对照：流程厂会关门、十二键、导出走 download。请假列表示例挂同一棵测试菜单，键在流程工厂。

问：本三格闭合标准是「厨房同行按表/树 10 枪 URL/动词/`encodeIds`/单表投影/无 page·import·export、富文本 5 枪 POST + 原样 assets 口、缺省 upload throw 盖章，菜单只给 `createDemoWebDomain` 这一颗切片（三键 + fail-closed 托盘 + 页面实际扣的 `runtime.service.*` / download / 厅堂资产口），且不碰九工厂整行、不升 Vue 矩阵行、不提前盖三份 Java 窗」，还是 OBJ 那句必须先把厨房同行拆成两行、或必须把九工厂行拆开、或必须等 GP-L-074…L-076 全 mined 才给厨房盖章？Lead 若凭「`createDemoWebDomain` 四字在函数表里」把九工厂行标 covered，是否假闭合了另外八个工厂？Lead 若因富文本没有独行就把五枪+assets 口判成未讲，是否假判？

## Q2 证据

- objective_id: OBJ-77
- bloom_level: analyze
- socratic_move: evidence
- expected_evidence: domains `index.ts` 84–96 / 98–102 / 104–107 / 109–141；`index.test.ts` 6–12 / 14–49 / 51–65；`rich-text.ts` 41–48 / 50 / 52–64；`transport.ts` 1–15；`transport.test.ts` 4–8；`test-demo/index.ts` 3；`test-tree/index.ts` 3；包 `package.json` 6–9 / 17–21；web-domain `index.ts` 20–83；`index.test.ts` 5–22 / 24–54；`runtime.ts` 3–13；`composables.ts` 95–115；`DemoPage.vue` 32–57 / 152 / 162–169 / 181–187 / 205–211 / 241–247 / 250–259 / 267–283；`TreePage.vue` 34–38 / 197–214 / 236–244 / 247–255 / 258–268 / 276–281；`RichTextPage.vue` 8 / 27–31 / 42–71；admin `services.ts` 4 / 54–69；`http.ts` 93–107；`adminManifestRegistry.ts` 19 / 78–85 / 340 / 355 / 365–368；registry 测试 87；home `services.ts` 1–16；`homeManifestRegistry.ts` 34–39；DML 158–169 / 1702–1719；workflow SQL 309；`TestDemoController` 54–148；`TestTreeController` 44–107；`TestRichTextController` 32–57；`downloadWithAxios` 261–268；yml 139–178；e2e `demo-manifest.spec.ts` 27–36；openapi `TestDemoVo` 含 `version`；web-domain 包零 `@/application/services`；frontend 零 `createDemoTableService` / `richTextService` 导出；sso-web / home 零这两座工厂
- difficulty: high

课的出餐单 + 三键哪些能在工作树直接复现，哪些只是测试名单比课表窄、或口试把「工厂注册了三键」说成「每扇 `/demo/*` 窗都有按钮」？

**仍能复现（厨房格 `createDemoService`）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂在根 `src/index.ts` 第 109 行，`Object.freeze`；`transport.ts` 只有 `projectDemoTransport` | `index.ts` 109–111；`transport.ts` 6 |
| 封面 10 扁平方法 + `readonly richText`；**没有**嵌套 `table.list` | `index.ts` 84–96 |
| 内部每一枪自己写 `http.request`，无 `no-store` | `index.ts` 112–139 |
| 路径 `encodeId` / `encodeIds`：标量 `encodeURIComponent(String(id))`；数组先编码再逗号拼 | `index.ts` 104–107 |
| 表/树 10 枪 URL/动词快照（小写；删两行拼 `%2F` / 空格 / 逗号） | `index.test.ts` 37–48 |
| `listDemo` 有 `rows` 数组才 `map(projectDemoTransport)`；否则原样当 `PageResult<DemoVO>` | `index.ts` 112–122 |
| `getDemo` 有 `data` 就投影，没有 data 就原样 | `index.ts` 123–131 |
| 投影输出 **没有** `version` / 审计字段；缺 deptId → `''`，缺 orderNum → `0` | `transport.ts` 6–14；`index.test.ts` 59–64 |
| OpenAPI `TestDemoVo` **有** `version` / `createTime`…；投影函数不拷这些键 | openapi 14643–14697 vs `transport.ts` 7–14 |
| 树表 **不**投影；`listTree` 类型是 `ApiResponse<TreeVO[]>`，不是分页信封 | `index.ts` 93 / 135 |
| 写动词：增 `'post'`，改 `'put'`，删 `'delete'` | `index.ts` 132–139 |
| 封面/实现 **没有** `pageDemo` / `importDemo` / `exportDemo` / `exportTree` | 接口 84–96；工厂 110–140；包内 grep 空 |
| 缺第二参仍造 `createRichTextService`，upload throw `'Rich-text assets port is not configured'`，resolve `[]` | `index.ts` 111 |
| 子路径只冻资源标签，例如 `demoTestDemoResource = { controller: 'TestDemoController', basePath: '/demo/demo' }` | `test-demo/index.ts` 3；`test-tree/index.ts` 3 |
| 名片 `id: 'demo'`，`backendModules: ['wta-demo']`，三项 capabilities **不是** componentKey | `index.ts` 98–102；测试 7–11 |
| 包 `exports` 只有 `"."` / `./test-demo` / `./test-tree`；**没有** `./rich-text` | `package.json` 6–9 |
| 包依赖 **没有** OSS adapter | `package.json` 17–21 |
| 厅堂 `demoService = createDemoService(domainHttp, createRichTextService(domainHttp, richTextAssets))`，**不**单独导出富文本工厂 | `services.ts` 54–69 |
| home / sso **零** 这两座工厂，也零 `demoService` | home `services.ts` 1–16；sso-web grep 空 |
| frontend **零** `createDemoTableService` / `createTreeService` | 全仓 grep 空 |

**仍能复现（厨房格 `createRichTextService`）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂在 `src/rich-text.ts` 第 52 行，`Object.freeze`；根 `export *` 再导出 | `rich-text.ts` 52–53；`index.ts` 5 |
| 封面 5 方法 + `assets`；`assets` **原样**出现，不是再包一层 | `rich-text.ts` 41–48 / 63 |
| 五枪：`GET /list`、`GET /{id}`、`POST /create`、`POST /{id}/update`、`POST /{id}/remove` | `rich-text.ts` 54–61 |
| 自己的 `encodeId` 只吃 `string` | `rich-text.ts` 50 |
| `update` body 必须带 `version: number`；`remove` body `{ version }` | `rich-text.ts` 45–46 / 58–61 |
| `GET /demo/rich-text/assets` **不在**这五支枪里 | `rich-text.ts` 54–63 |
| 厅堂 `richTextAssets` 未导出：upload `policy = richtext-${kind==='attachment'?'file':kind}` 再 `ossUploadClient.upload`；resolve 自己 `GET /demo/rich-text/assets`，`ossIds.join(',')` | `services.ts` 54–67 |
| yml 四份 policy `richtext-image|audio|video|file`，**没有** `richtext-attachment` | `application.yml` 139–178 |
| 后端 `/assets` 与 `GET /{id}` 都要 `demo:richtext:query` | `TestRichTextController` 51–57 |

**仍能复现（菜单切片 `createDemoWebDomain`）：**

| 声称 | 磁盘锚点 |
| --- | --- |
| 工厂先 `requireDemoWebRuntime`，缺托盘 throw `'DemoWebRuntime is required'` | `runtime.ts` 10–12；`index.ts` 29–30；测试 51–53 |
| id `web-domain-demo`，`domainId` `'demo'`；`messages` 只冻两句 `tableTitle` / `treeTitle` | `index.ts` 32–38；测试 28–31 |
| 三键顺序与名字测试 `toEqual`：`demo/demo/index` / `demo/tree/index` / `demo/rich-text/index` | `index.ts` 62–81；测试 40–44 |
| 权限三组全量 `toEqual`：table 含 export 不含 query/import；tree 不含 query/export；rich-text 含 query 与 `common:richtext:upload` | `index.ts` 40–60；测试 32–39 |
| 托盘四口：`service` / `confirm` / `success` / `download`；**没有** `error` / `dicts` / `treePanel` | `runtime.ts` 3–8 |
| 三张 Vue **零** `@/application/services` | web-domain 包 grep 空 |
| 单表：list/get/add/update/delete + `runtime.download('demo/demo/export', query, \`demo_${timestamp}.xlsx\`)`；成功文案永远「修改成功」 | `DemoPage.vue` 205–211 / 241–247 / 250–259 / 276–283 |
| 表单初始没有 version；规则有 `id` 必填，模板没有 id 表单项 | `DemoPage.vue` 162–169 / 181–187 vs 111–126 |
| 树表：`listTree` + `buildTree`；下拉再打一枪并塞 `{ id: 0, treeName: '顶级节点' }`；保存「操作成功」；删当前行单 id | `TreePage.vue` 197–214 / 236–244 / 258–268 / 276–281；`composables.ts` 95–115 |
| 树表页 **没有**导出按钮，也 **没有** `runtime.download` | `TreePage.vue` 34–38 工具栏 |
| 富文本五枪；list 写死 `pageSize: 50`；update 的 version 来自 `rows.find(...)?.version ?? 0`，不是 `get()`；**没有** `v-hasPermi`；确认走 `ElMessageBox` 不是 `runtime.confirm` | `RichTextPage.vue` 8 / 27 / 44–67 |
| 页顶 `AbortController` 卸载 abort；list/get/create **没有**把 `signal` 传进厨房 | `RichTextPage.vue` 42 / 44–62 / 70–71 |
| 厅堂 `demoRuntime` 塞 `demoService` + confirm/success/download | `adminManifestRegistry.ts` 78–85 |
| `selectedManifestIds` 含 `'web-domain-demo'`；`selectedDomainIds` 含 `'demo'` | 365–368 / 340 |
| registry 测试锁 `demo/demo/index` → `Demo` | 测试 87 |
| home 清单是 `web-domain-admin` + `web-domain-profile-self`，**不**选 demo | `homeManifestRegistry.ts` 34–39 |
| 三张 C 型挂测试菜单 `1761400000000000005`；单表无 import F；树表有 `demo:tree:export`；富文本块 `NAMEWTA-RICHTEXT-DML-001` 含 upload F | DML 158–169 / 1709–1719 |
| 请假列表示例同一父菜单，键是 `workflow/leave/index` | workflow SQL 309 |
| 单表后端 `/page` `/importData` `/export` 在；树导出是 **GET**；download 管 `client.post` | `TestDemoController` 63–90；`TestTreeController` 55；`downloadWithAxios` 263 |
| 对照：notify/third 工厂 **无** `require*WebRuntime`；档案楼核十一口 | notify/third `index.ts` 直接 freeze；profile `runtime.ts` 64–79 |

**课写过、测试名单更窄或口试数字写偏（不是证伪本三格）：**

| 课的句子 | 磁盘 |
| --- | --- |
| 「测试锁死三键顺序与名字」 | 三键 `toEqual` 有。registry 测试 **只**锁 `demo/demo/index`，不锁 tree / rich-text |
| 「厨房测试锁表/树 10 枪」 | 第一例 10 请求。**没有**断言 `/demo/rich-text/*` |
| 「缺第二参 upload throw」 | 源码 111 行有。`createDemoService(client)` 测试没喊 `assets.upload` |
| 「投影丢掉 version」 | 实现不拷该键。`transport.test.ts` 只测完整对象恒等，**没有**带 `version` 的反例 |
| 「`listDemo` 缺 rows 原样返回」 | 源码 118–120 有。测试没锁这条分支 |
| 口试表把动词写成 GET/POST/PUT/DELETE | 厨房 `method: 'get'`/`'post'`/`'put'`/`'delete'` 小写 |
| 「e2e 演示菜单」 | `demo-manifest.spec.ts` 只铺 demo + tree 两键，**没有** rich-text |
| 包 `AGENTS.md` / README 写两页、README 还写 Client | 工厂、测试、种子都是三页；home 不雇这座厅。以工作树为准 |
| 课 `estimated_minutes: 38` | chain 该行估时 35。不是机制错误 |

问：在 10+5 枪 URL/动词/`encodeIds`/单表投影/无 page·import·export、富文本 POST 五枪+厅堂 assets、三键顺序、缺 runtime 源码与单测都关门、页面不 import 厅堂单例、单表 download 导出、树表浏览器拼树、富文本 version 从列表取、home 不选 已经能互证的前提下，(a) 是否还要把「富文本 URL 无单测」「缺省 throw 无单测」「transport 测试不锁丢掉的 version」「registry 只锁一键」写成开篇半句，才算本三格讲完？还是这些已经落在课的表/反例里，Lead 可以把 **厨房同行** 视为可 covered、把 **菜单当切片已讲** 并保持九工厂行 uncovered？

## Q3 假设与反例

- objective_id: OBJ-77
- bloom_level: evaluate
- socratic_move: assumptions
- expected_evidence: 无 `createDemoTableService`；工厂不在 `transport.ts`；子路径不是小厨房；三页不 import 厅堂单例；`createDemoWebDomain(undefined)` 源码与测试都 throw；notify/third 不 throw；无 Vue 喊 `/page` `/importData`；单表导出走 download；树表导出谁都不接；富文本 update 不是 PUT；`GET /assets` 不是厨房第六枪；富文本页无 `v-hasPermi`；home 无此工厂；九工厂行不因本课整行闭合；请假示例不是第四键；加密玩具不在三键
- difficulty: high

课的隐含假设若反转，合格读者会把哪座工厂或哪只抽屉认错？

1. **「三张 Vue `import { demoService }`。」** 包内搜不到 `@/application/services`。直接消费单例的是厅堂 registry。课反例 1 / 误区 6 覆盖。
2. **「`createDemoWebDomain` 等于九个 web-domain 工厂都 covered。」** 矩阵 (a) 同行绑死。GP-L-020 / GP-L-044 / GP-L-054 / GP-L-066 / GP-L-073 已挖过。课反例 2 / 误区 3 覆盖。
3. **「工厂在 `transport.ts`。」** 打开 `index.ts` 第 109 行。富文本在 `rich-text.ts`。课反例 3 / 误区 5 覆盖。
4. **「厅堂导出了 `richTextService`。」** L-007 表写明不单独导出。页面喊 `runtime.service.richText.*`。课反例 4 / 误区 4 覆盖。
5. **「`service.table.list` / `service.richText.documents.list`。」** 扁平：`listDemo`、`listTree`；只有 `richText` 这一只嵌套口。课反例 5 / 误区 7 覆盖。
6. **「从 `@namewta/domain-demo/test-demo` 进口小厨房。」** 子路径只再 export 类型 + 资源标签。课反例 6 覆盖。
7. **「页面手写 `/demo/demo/list`。」** 列表走 `runtime.service.listDemo`。导出是单表页里出现 `demo/demo/export` 字符串的例外，且走 download。课反例 7 覆盖。
8. **「树表导出按钮走 download，和单表一样。」** 树表页没有导出按钮。后端还是 GET，不是 POST。课反例 8 / 误区 10 覆盖。
9. **「`listTree` 是分页信封。」** 类型是 `TreeVO[]`。课反例 9 / 误区 9 覆盖。
10. **「富文本 `update` 是 PUT `/demo/rich-text`。」** 是 `POST /demo/rich-text/{id}/update`。课反例 10 / 误区 13 覆盖。
11. **「`GET /demo/rich-text/assets` 是 `createRichTextService` 的第六枪。」** 厨房方法表没有。厅堂 `richTextAssets.resolve` 才打。课反例 11 / 误区 14 覆盖。
12. **「富文本页核了 `demo:richtext:*`。」** 模板没有 `v-hasPermi`。课反例 12 / 误区 15 覆盖。
13. **「单表编辑会带 version。」** `DemoVO` 没有这个字段；投影丢掉 generated `version`。课反例 13 / 误区 12 覆盖。
14. **「home 也能打开测试单表。」** 工作树没有这份工厂接线。课反例 14 / 误区 17 覆盖。
15. **「OBJ-77 包含 `TestDemoController`。」** 那是 OBJ-74。课反例 15 / 误区 2 覆盖。
16. **「请假列表示例是 `createDemoWebDomain` 第四键。」** 键在 `web-domain-workflow`，父菜单碰巧也是测试菜单。课反例 16 / 误区 23 覆盖。
17. **「`createDemoWebDomain(undefined)` 像 notify 那样仍能冻出键。」** 本厂会 throw。notify/third 2026-09-17 不会。课正例 7 / 误区 20 覆盖。
18. **「`requireDemoWebRuntime` 会检查 10 支枪。」** 只检查 runtime 对象在不在。`service: {}` 仍能冻三键。课边界 1 覆盖。
19. **「三族写动词都是 POST。」** 表/树改删是 PUT/DELETE。课误区 8 覆盖。
20. **「`GET /page` 就是 `listDemo`。」** `listDemo` 打 `/list`。`/page` 没进厨房。课误区 11 覆盖。
21. **「新增成功文案是『新增成功』。」** 单表写死「修改成功」。课正例 2 / 误区 16 覆盖。
22. **「capabilities 决定挂哪几页。」** 决定挂页的是 manifest + 菜单种子 + `selectedManifestIds`。课误区 18 覆盖。
23. **「加密演示是第四键 / `AGENTS.md` 写两页所以只有两键。」** 本工厂没有 encrypt 键；磁盘三键。课误区 21 / 22 / 反例 18 覆盖。
24. **「工厂注册了三键 = 所有 `/demo/*` HTTP 都有按钮。」** page/import 没进厨房；单表导出走 download；树表导出谁都不接；资产解析是厅堂口；加密/批量不在三键。课图 2 文字等价物覆盖。

问：读完后仍说不出的，是不是「三个工厂的名字」，而是 **厨房方法全集 ≠ 墙上已扣的扳机 ≠ 后端窗全集、九工厂行不能因本课整行闭合、本厂 fail-closed 只检查托盘在不在、导出走 download、树表导出空子弹、assets 不是厨房第六枪、home 根本不选、工厂名 ≠ 导出名、两座厨房只导出一个单例**？

## Q4 视角

- objective_id: OBJ-77
- bloom_level: evaluate
- socratic_move: viewpoints
- expected_evidence: 矩阵 (a)/(b)/(c)/(d)；OBJ-77 vs L-007/L-008/L-020/L-029/L-044/L-054/L-066/L-073/L-074…L-076；厨房同行 vs 九工厂同行；claimed_cells 三工厂名 vs 三张 Vue；chain 估时 35 vs 课 38
- difficulty: medium

换一个观察者，架构边或数据流边缺了哪一条？缺的那条是否属于 **本课这三格 (a)**？

| 视角 | 多出来的边 | 是否本课这三格 |
| --- | --- | --- |
| 矩阵维护者 | (a) 厨房同行两名可一起盖章；九工厂同行 vs chain 菜单工厂名；无 Vue 行、无富文本独行；(b) 无本课符号、三份 Controller 挂 L-074…L-076；(d) 无浏览器演示菜单流 | **是** 闭合定义。厨房同行可视为已讲并可 covered。九工厂行保持 uncovered。不要发明 (b)/(d)，不要把三张 Vue 升成新行才能给切片盖章 |
| 表/树厨房作者 | 10 枪 URL/动词；`encodeIds`；只给单表投影；无 page/import/export | **是** `A:createDemoService` |
| 富文本厨房作者 | 5 枪 POST/GET；`assets` 原样挂上；缺省口关闭 | **是** `A:createRichTextService`。矩阵无独行，与上一格同行 |
| 管理端菜单工厂 | 三键顺序、三组 `demo:*`、先关门再冻、`h(page, { runtime })`、未选无键 | **是** 切片里的 `createDemoWebDomain` |
| 三张页消费 | 单表五枪+download；树表五枪+`buildTree`；富文本五枪+资产口、无 hasPermi | **是** OBJ 对照走查，不是矩阵第四格 |
| 导出/鬼窗/资产 | 单表 download；树表种子空子弹；`/page` `/importData` 没进厨房；`/assets` 在厅堂 | **是** 负空间对照。不要升成第二张厨房章 |
| 厅堂电工（L-007） | 先 `richTextAssets` 再两座工厂套在一起；导出名 `demoService`；registry 注入 runtime | 插头一句 **是**。12 个 `create*` 全表 **否**（已 covered） |
| 导航 host（L-020） | admin 选 `web-domain-demo`；home 不选；重复键失败关闭 | 选择集一句 **是**。其余八厂 **否** 整行 |
| OSS 直传（L-029） | 上传 policy `richtext-*` 转 `ossUploadClient` | 交叉线 / policy 字符串 **是**。分片 abort **否** |
| 档案楼（L-044） | `requireProfileWebRuntime` 核十一口 | fail-closed 对照 **是**。企业三页 **否** |
| 通知/三方楼（L-054 / L-066） | 不校验托盘；notify 四张 Vue；third 一张页四个 kind | 对照「本厂会关门 / 三键三张 Vue」**是**。通知四页 / 三方四键 **否** |
| 流程楼（L-073） | 也会关门；十二键；导出走 download | 对照抽屉形状 / 动词棘轮 **是**。57 枪 **否** |
| 单表窗（L-074） | 八扇；`/page` XML；import 监听；export POST；`@Version` | 厨房没收 page/import、投影丢锁、download 对准 POST **是** 边界。八扇盖章 **否** |
| 树表窗（L-075） | 六扇；export GET | 厨房没收 export、页无按钮、浏览器拼树 **是** 对照。六扇 **否** |
| 富文本窗（L-076） | 六扇；OSS 解析；version 冲突 | 五枪 URL 对齐、resolve 在厅堂、页从列表取锁 **是** 边界。解析后窗 **否** |
| 加密/批量玩具柜 | 矩阵 deferred | 本工厂无键 **是** 负空间。不要揭 deferred |
| MySQL 8.4 | 菜单三 component；树 export F；无 import F | 种子键对照 **是** (a)。容器 **否** |

问：Lead 若凭「三个工厂名在 claimed 列里」把厨房同行 **和** 九工厂行一起标 covered，是否还要求读者能指着 **10+5 枪扁平、`encodeIds`、只给单表投影、三键顺序、本厂 fail-closed、导出走 download、树表导出空子弹、assets 在厅堂、home 不选、工厂名 ≠ 导出名**？这些是不是已经在本格函数表/反例里，而不需要 (b) 新行、也不需要等 L-074…L-076 才给 **厨房同行** 当已讲？九工厂行是否例外——必须保持 uncovered，直到其余工厂也有课？Lead 若因矩阵把两座厨房印在同一行，就把 `createRichTextService` 判成「没有格子」，是否假判？

## Q5 元问题

- objective_id: OBJ-77
- bloom_level: evaluate
- socratic_move: question-the-question
- expected_evidence: 本批 Q1–Q4；split-rules（同一 OBJ 缺机制/边界/例子 → 重写该课；满 10 问仍不清才拆课；一课超 1 个容器是 Lead 裁决不是 b01 填表）；本课累计 5/10；`split_depth: 0`；矩阵 (a) 厨房同行仍 uncovered、九工厂同行仍 uncovered、无 Vue 行、无本课 (b)/(d)
- difficulty: low

建议：`defer`

原因：

- **厨房同行与 OBJ-77 / chain / 课前两格 1:1。** L 合同章节齐全（宏观三键地图、直觉十四句、定义表、机制链 1–8、带 caption / 文字等价物的两张图、正反例、变式、误区、暂停走查、来源表 S-005/S-006/S-008/S-010/S-L007-01/S-L029-01/S-L074-01/S-L077-01…08）。能复现：冻住表/树 10 枪 + 嵌套 `richText`、`encodeIds` 编码、只给单表投影并丢掉 version、无 page/import/export、富文本五枪 POST、`assets` 原样挂上、缺省口关闭、厅堂只 admin-web 接线且不单独导出富文本工厂、home / sso 不雇。不必为「工厂的名字」再挖五问。Lead 可将 `createDemoService, createRichTextService` 该行标 `covered`。
- **菜单是切片，不是九工厂整行。** 三键顺序、`requireDemoWebRuntime`、`h(page, { runtime })`、页面实际扣的枪、download 导出、厅堂资产口、请假示例非第四键——已在课的表/反例里。GP-L-020 / GP-L-044 / GP-L-054 / GP-L-066 / GP-L-073 已立过同一条：本切片证据可写进九工厂行备注（「demo 管理端三键 + fail-closed 托盘 + 页面消费已讲」），**不要**把该行标 covered。
- 本批可粘贴的精度项不挡住厨房盖章、也不挡住菜单切片视为已讲：（1）富文本五枪 URL 无本包单测；（2）缺省 assets throw 测试没喊 upload；（3）`transport.test.ts` 不锁丢掉的 version；（4）`listDemo` 缺 rows 分支无测；（5）registry 只锁 `demo/demo/index`；（6）e2e 无 rich-text 键；（7）包 AGENTS/README 仍写两页；（8）动词小写；（9）chain 估时 35、课 38。不要把三份 Java 窗、九工厂其余八个名字、OSS 分片、加密玩具柜塞进本格。
- 不建议 `mine-more`：b01 未证明本三格还有真实矩阵空洞；再五问只会把 L-074 八扇、L-075 树 GET 导出、L-076 OSS 解析、L-029 直传或九工厂其余名字再问一遍。不建议 `re-dispatch-L` 作为盖章前提：不缺动机/机制/边界/例子；测试名单窄是半句精度，不是第二容器。不建议 `split-lesson`：未满 10 问；两座厨房+菜单是 chain 原设计的同一 OBJ-77（对照 third 拆成 L-065/L-066 是另一条链，本课不是两个 C4 讲不完）；补丁不突破 40 分钟。`split_depth: 0`，本批禁止填 split 表。不建议发明 (b) 行：`encodeId` / download 导出 / PUT 对 POST 已是 (a) 的解释到副作用。不建议把三张 Vue 升成矩阵行才给切片盖章。不建议等 GP-L-074…L-076 才给厨房同行当已讲。不建议用本课改 (d) 登录走廊或 OSS 直传。不建议因矩阵把两座厨房印在同一行就要求拆出行。

## 缺口清单

| 格子 | 缺口 | 建议补丁 | 处置建议 |
| --- | --- | --- | --- |
| A:createDemoService | 10 枪 URL/动词、扁平 freeze、`encodeIds`、只给单表投影、无 page/import/export、home 不雇——均与源码对齐。残留精度：缺 rows 分支无测；动词小写；投影丢掉 version 的反例不在 `transport.test.ts`。 | 可选半句：口试方法表以 `index.ts` 为准，丢掉的字段以投影函数键为准。不要求重写机制链。Lead 可将矩阵该行（与下一格同行）标 `covered`（证据锚本 probe 与 L-077 来源表 S-L077-01 / S-L077-02）。 | defer（本行可 covered；精度项不挡盖章） |
| A:createRichTextService | 课按磁盘讲了五枪 POST/GET、`assets` 原样挂上、缺省口关闭、厅堂不单独导出。**矩阵无此独行**；符号与 `createDemoService` 同行。残留精度：本包单测不含 `/demo/rich-text/*`；缺省 throw 测试没喊 upload。 | 可选半句：口试五枪以 `rich-text.ts` 为准，assets 解析以厅堂 `services.ts` 为准。不要求重写机制链。Lead **不要**为了这一格再拆矩阵行；与上一格一起 covered。 | defer（同行已讲；不要发明独行） |
| A:createDemoWebDomain | 课按磁盘讲了三键、fail-closed 托盘、页面实际扣的 `runtime.service.*`、download 导出、厅堂资产口、请假示例非第四键、home 不选。**矩阵无此独行**；符号落在九工厂同行。残留精度：registry 只锁一键；e2e 无 rich-text；AGENTS/README 过期。 | 可选半句：口试键表以工厂 `registrations` `toEqual` 为准。不要求重写机制链。Lead **不要**改九工厂行为 covered。可将本切片证据写入该行备注。 | defer（切片已讲；九工厂行不可因本课整行闭合） |
| 九工厂行 `create*WebDomain`（含 `createDemoWebDomain`） | 本课只用选择集 + 三键当切片证据。其余八个工厂未因本课讲完。 | 保持 uncovered。L-020 已覆盖 `createSystemWebDomain` 切片；L-039/L-044 已覆盖 profile 两份菜单；L-054 已覆盖 notify 四页；L-066 已覆盖 third 四键；L-073 已覆盖 workflow 十二键；其余 ai 后续课。 | defer（九工厂行保持 uncovered） |
| A:admin-web.services / C:admin-web-composition | 插头先 `richTextAssets` 再两座工厂套在一起，与 registry 接线已借用。 | 保持。L-007（已 covered）。 | defer（L-007 已 covered） |
| A:home-web.services | 负空间：无工厂、无单例。 | 保持。L-008（已 covered）。 | defer（L-008 已 covered） |
| A:createSystemWebDomain / B:navigation host | 选择集 + 未选无键 + 重复键失败关闭已点。 | 保持。L-020。 | defer（L-020） |
| A:createOssUploadClient.upload | 上传 policy 转直传已作交叉线。 | 不要用本课盖分片 abort。 | defer（L-029 已 covered） |
| A:TestDemoController.* | 厨房没收 `/page` `/importData`；download 对准 POST export；投影丢 `@Version`。该行仍 uncovered。 | 不要用本课盖八扇。 | defer（L-074） |
| A:TestTreeController.* | 厨房没收 GET export；页无按钮；浏览器 `buildTree`。 | 不要用本课盖六扇。 | defer（L-075） |
| A:TestRichTextController.* | 五枪 URL 对齐；`/assets` 在厅堂；页从列表取 version。 | 不要用本课盖解析后窗。 | defer（L-076） |
| A:createWorkflowDefinitionService / A:createWorkflowWebDomain | 请假示例挂测试菜单、键在流程工厂已作对照。 | 保持。L-073（厨房已 covered；菜单切片已讲）。 | defer（L-073） |
| A:createNotifyWebDomain / A:createThirdWebDomain / A:createProfileWebDomain | 不校验托盘 / 十一口对照已借用。 | 保持。L-054 / L-066 / L-044（切片已讲，九工厂仍 uncovered）。 | defer（L-054 / L-066 / L-044） |
| (b) `encodeId` / download 导出 / PUT 对 POST / 三键注册 | 矩阵无此行。课已写进 (a)。 | 不要为本课发明 (b) 行。 | defer（未入库） |
| (d) 浏览器演示菜单流 | 矩阵无此行。已有的走廊仍是登录后查询与 OSS 直传。 | 不要发明前端演示菜单数据流行，也不要用本课改 L-006 / L-027。 | defer（未入库） |
| demo Redis/MQTT/加密/批量那些 Controller | 本工厂无键。 | 保持 `deferred(capability-demo-not-product-room)`。 | defer（capability-demo） |
| MySQL 8.4 / Redis 8 | 菜单种子是 (a) 对照，不是容器闭合。 | 不要因本课标容器 covered。 | defer（(c) 容器 / L-085） |

Miner 只填表，不选择最终处置。

## split-proposal

none

未触发：本课问数 5/10；厨房同行无「课没讲到」的 uncovered 空洞需要靠 b02 继续 `mine-more`；菜单切片在九工厂同行上已讲完并显式禁止整行闭合；合格读者说得出三个工厂目的（出餐单印表/树 10 枪并夹住富文本五枪；墙上三键把托盘喂进页面）、主路径（单表 list/get/add/update/delete + download 导出 / 树表 list+浏览器拼树 / 富文本五枪 + 厅堂资产口）与失败路径（缺 runtime 当场 throw、缺方法要等点击才炸、缺第二参 upload throw、投影丢锁、树表导出谁都不接、home 不选这份 id）；把可选半句写进原课不会超过 40 分钟；后端三窗、厅堂插头、导航 host、OSS 直传、九工厂其余名字已有邻课。满 10 问前不拆。两座厨房与菜单虽跨 `domains/demo` 与 `web-domains/demo` 两个包，仍是 chain 原设计的同一 OBJ-77，不是「超过 1 个容器却讲不完」——对照 third 的拆法是另一条链已经做成 L-065/L-066，本课不要在 b01 再拆。矩阵把两座厨房印在同一行也不是拆课理由。

| 原 lesson_id | 建议新课标题 | 从原课拿走的格子 | 为什么 10 问不够 |
| --- | --- | --- | --- |
| — | — | — | — |

## 关闭

- 状态：`closed`
- `question_budget_used`：5
- 是否改变了至少一个矩阵格子，或显式 `defer` / split 提案：否（miner 不改矩阵）；split 提案为 none；缺口表对九工厂整行、L-074…L-076 三份 Java、L-007/L-008 插头（已 covered）、L-020 导航、L-029 OSS、L-044/L-054/L-066/L-073 对照、未入库 (b)/(d)、能力展示玩具柜以及 MySQL/Redis 容器显式建议 `defer`；对矩阵 (a) 行 `createDemoService, createRichTextService` 建议 Lead 标 `covered`（证据锚本 probe 与 L-077 来源表 S-L077-01…03）；对 `A:createDemoWebDomain` 建议视为已讲切片、**不要**把九工厂同行标 `covered`。本课不主张发明 Vue 矩阵行、用本课盖三份 Controller、把请假示例升成第四键、把 `/assets` 升成厨房第六枪、或等 GP-L-076 才给厨房同行盖章。
