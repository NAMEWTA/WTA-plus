# Baseline

SOURCE-BASELINE SHAs reconfirmed (T-01). Prep tree copied from those HEADs (plus documented dirty `application-local.yml`, which was **dropped** from the tree to publish).

No re-baseline required: product HEADs did not move.
