# T-37 C1 安全／规格轴正式源码审查

**固定输入**：base `38032d24335c52cafea855b19d51fb36295162ef` → implementation `e2907c41f4b1bef442c68b95a0fc09aa9c3b273a`，tree `3f09371a2fea57ceb19fb000629e7c0b84494c7a`。只用 `git show e2907c41:<path>` 与 `git diff 38032d2...e2907c41` 读取产品，未将后续工作树混入。范围是 AC-037、ticket37/dispatch-T-37 revision159 及适用项目安全/通知规范；没有审 T38 人工恢复或 T42 附件完整交付。本审查未运行 Maven、Docker、Redis/MySQL 或供应商调用。

**更新后结论：REQUEST CHANGES；C1 未通过。** 下文原始静态审查在 Lead 真实运行前曾给出 PASS，这个历史时点保留，不能再用作 C1 总体结论。Lead 的 `/tmp/wta-t37/runs/b0d200e1eaf3766e/result.json` 与同次 Maven 计数显示 13 tests、0 failures、2 errors、0 skipped、退出码 1；这既揭示了实际产品缺陷，也使另一条 SMS 端到端场景尚未验证。后续工作树由唯一 writer 修正后，须固定新 SHA 重审并重跑。此更新是收到运行证据后的复核，不冒称初始静态审查已发现。

### C1 运行后新增的阻断证据

1. **高优先级：生产 NameMapper 下完成态 CAS 打错键。** 固定候选 `backend/wta-common/wta-common-notify/src/main/java/org/namewta/common/notify/idempotency/RedisNotifyIdempotencyStore.java:74-83,100-104` 的 `complete` 与 `release` 使用 `RBucket.compareAndSet(CompareAndSetArgs)` / `compareAndDelete(CompareAndDeleteArgs)`。本地 Redisson 4.6.1 source `org/redisson/RedissonObject.java:171-185` 表明 `getName()` 是 NameMapper *unmap* 后的逻辑键、`getRawName()` 才是物理键；`org/redisson/RedissonBucket.java:468-490,520-558` 的这两种 args 实现直接以 `getName()` 调 `commandExecutor.evalWriteAsync`，并传给 KEYS，没有像 `org/redisson/RedissonScript.java:239-254` 那样 map 路由键及 KEYS。固定测试 `backend/wta-admin/src/test/java/org/namewta/test/notify/idempotency/RedisNotifyIdempotencyStoreIntegrationTest.java:102-114,150-158` 使用与生产一致的 CompositeCodec + KeyPrefixHandler；第 156 行新 owner `complete(second, accepted(...))` 实际抛出“Redis 通知幂等占位已失效”，而 StringCodec/无 NameMapper 变体完整通过。该错误会让真实完成态未落入 Redis，导致已接受响应的幂等回放失效并影响 UNKNOWN/等待状态。此前旧 owner 拒绝和 `release(first)` 后仍是 InProgress 的断言虽通过，但在打错键时可虚绿；须有新 owner `complete` 和 `release` 的正例覆盖双 codec。`markRetryable` 与再领取的单键脚本走 `RedissonScript.eval` 显式映射路径，20 并发只获一 owner 和 PTTL 断言已到达；不能由这些局部成功推断 complete/release 安全。旧四字段/损坏值断言位于第 160-177 行，在 Composite 用例第 156 行失败后尚未执行。
2. **SMS 真实链路尚未到 provider。** 固定测试 `backend/wta-admin/src/test/java/org/namewta/test/notify/NotifySmsDispatchIntegrationTest.java:498` 的 `exactSingleAttemptTencentRateLimitReclaimsAndCallsTheSelectedBlendTwice` 在 `DispatchNotificationService.validateSubmission:266` 抛 `ServiceException: 短信模板缺少必填变量映射 code`。这是该隔离 fixture 的模板变量映射缺口；在修复夹具并重跑前，两次物理调用、一次确认为未发送后重试、后续重复零调用等 AC-037 真实组合断言均未到达，不能宣称通过。不能据此单独推断生产 SMS 发送实现错误。

额外 driver 卷不存在的检查由 ops 处理，不作为产品 finding；这里仅引用清洗后的计数和测试栈，不读取 private/env/raw 秘密。

## AC-037 与关键不变量

1. **明确未受理才二次物理调用。** `NotifyDispatcher.java:99-133,268-310` 对 adapter 结果先验证每个目标与请求一致，再聚合；只有非空且**所有** `NotifyTargetResult.status=UNSENT_RETRYABLE` 才让当前 `Acquired` owner CAS 成 `RETRYABLE`，随后返回非接受结果。部分已接受、部分 UNKNOWN、旧 FAILED 和 provider 抛异常均走完成态，不释放整批。`DispatchNotificationService.java:269-297` 仅单目标 typed retryable 映射到固定 `PROVIDER_UNSENT_RETRYABLE`；`NotifyDispatchResultService.java:151-164,219-225` 以现有 Outbox 预算/退避决定 READY 或 DEAD_LETTER，终结未受理固定码 DONE，其余 UNKNOWN 进入 WAITING_RECEIPT。当前主生产 path `DispatchNotificationService.java:105-116` 为一笔 delivery 构造一个目标并设置稳定 idempotencyKey；测试 `NotifySmsDispatchIntegrationTest.exactSingleAttemptTencentRateLimitReclaimsAndCallsTheSelectedBlendTwice` 具备两次 claim、两次 blend 调用和最终重复零调用断言，但真实执行结果由 Lead 核定。
2. **已接受及不确定响应不盲发。** `SmsNotifyChannelAdapter.java:33-55` 对成功仅标 ACCEPTED；传输/解析抛异常标 OUTCOME_UNKNOWN，普通非成功旧 FAILED 不授予重试。`MailNotifyChannelAdapter.java:69-84` 在 `sender.send` 后异常标 OUTCOME_UNKNOWN。`NotifyDispatcher.java:106-111,288-293` 对未分类 adapter 异常同样保守 UNKNOWN。`NotifyIdempotencyCoordinator.java:44-58` 把 COMPLETE 和 RETRYABLE_TRANSITION 故障显式区分；`DispatchNotificationService.java:138-160` 仅 SMS、调用 adapter 前的 ACQUIRE 仍按 T35 零发送准备错误进入有界 retry，其余幂等故障/外部 I/O 结果未知进入 UNKNOWN。`NotifyDispatcher` 只有确认转态后才把可重试结果返给 Outbox；转态失败即使 Redis 可能已写入，DB 仍 WAITING，不推断安全重试。REDACT 事件在 `NotifyDispatcher.java:340-370` 使用安全请求/上下文/结果副本；原 providerMessageId 仍留给内部回执关联。
3. **owner、旧值、TTL 与 codec（原静态观察，受上述运行证据修正）。** `RedisNotifyIdempotencyStore.java:38-70,74-120` 给每次新 `IN_PROGRESS` 独立 UUID nonce，同 requestId 的新 owner 期望字节仍不同；转 `RETRYABLE` 和同 digest 再领取均由同一 Lua 对期望完整值 CAS，要求 `PTTL>0`，`SET ... KEEPTTL` 保留剩余窗口。不同 digest 冲突。旧四字段 IN_PROGRESS 不自动认领，旧 COMPLETED（包括 FAILED/PROVIDER_REJECTED）继续作为完成态返回，损坏/无效状态失败关闭；未清键/换前缀。`markRetryable` 与再领取脚本以 bucket 的 codec 对 ARGV 编码，以 `bucket.getName()` 逻辑名由 Redisson 4.6.1 `RedissonScript.eval(String key,...)` 对 route key/KEYS 各作一次 NameMapper 映射。**这一证明不适用于另外通过 `RBucket.compareAndSet/compareAndDelete(Args)` 执行的 complete/release：C1 实测表明它们在生产 NameMapper 下未按预期工作。** `complete` 原意是完成态窗口重设；两个 RETRYABLE 转态原意是不延长 TTL。真实 Redis 当前只局部通过，旧 owner 拒绝可能虚绿，旧四字段断言未走到。
4. **生产可重试事实来源严格且单次。** `Sms4jBlendRegistry.java:42-72,112-121` 先撤旧证明，再以 `maxRetries=0` 的实际厂商 Config 创建对象、包代理、注册**同一引用**并存 map；失败不留认证、同账号 upsert/remove 串行。`SmsAutoConfiguration.java:41-47` 可选注入 Registry 实现的小 SPI，缺失默认失败关闭。`Sms4jNotificationProviderResolver.java:30-85` 在 resolve 时捕获实际选中 blend A，发送亦使用 A；仅 Registry 认证 A、supplier/tencent、非 success、相同 configId、完整 Response、无 Error、非空 RequestId、单匹配 E.164 号码、精确 `LimitExceeded.PhoneNumberThirtySecondLimit`、空 SerialNo、Fee=0 才产生 typed retryable。其它 Code、缺字段、多目标、SDK `errorResp` 自由字符串和异常均无重试权。本地 SMS4J 3.3.5 source 的 `SmsFactory.createSmsBlend` 亦是 provider factory→proxy→register；`TencentSmsImpl.getResponse` 在非 Ok 状态返回非 success 和原始结构化 JSON，默认失败可能进入 `requestRetry`，因此显式 maxRetries=0 是必要闭环。仓内测试涵盖 Registry 重建/撤销/第三方覆盖和腾讯结构反例；真实供应商没有联调，不能据合成响应宣称厂商端到端幂等。

## 固定候选范围及验证出口

三点 diff 为 22 路径（21 backend 产品/测试 + 1 已授权 `notification.md`），含预登记的 `SmsAutoConfiguration` 与 `SmsSingleAttemptBlendVerifier`；未见 T38/T42 或未登记生产路径越界。`git diff --check 38032d2...e2907c41 -- backend .agents/skills/engineering-standards/references/notification.md` 退出 0。notification.md 同步了整批明确未发、owner CAS、旧完成态保留和 SDK 实例来源边界。公开 enum/record/SPI 新语义已在仓内调用方切换；本票对外不承诺兼容桥，既有 FAILED 常量未删除。正式 Java API/序列化、测试计数和 current clean source 等价性仍需 Lead 的适用门禁记录。

旧红绿仅为实施过程：`/tmp/wta-t37/red-typed-dispatch.log` 精确测试 1 run/1 failure/0 skip、exit 1；`red-cache-behavior.log` 1 run/1 error/0 skip、exit 1；`green-typed-dispatch-v1.log` 1 run/0 failure/0 skip、exit 0。这只证明单个 MemoryStore 断言由红转绿，**不证明 C1 整体验收**。本票 required 出口是 clean candidate 上的 39 类受影响测试及真实 Redis（CompositeCodec/前缀与 StringCodec、20 并发、旧四字段、损坏、PTTL、过期 owner、digest 冲突）、真实 MySQL+Redis+Dispatcher+受控 SMS Adapter/Registry+Outbox（两次物理调用、后续重复零调用、COMPLETE/转态 ACK 未知、lease fence）并保留 T35/T36 回归；实际命令/退出码/测试数/skip/资源清理由 Lead 提供。**C1 已知 13 tests / 2 errors，当前不满足出口，需新固定修订及真实重跑；原静态 PASS 已被推翻。**

**限制**：无法从源码证明腾讯线上在所有时刻确实按所引官方语义返回该错误，也不能把 Redis TTL 视为持久 exactly-once；这些超出 T37 既定合同。当前生产 `NotifyDeliveryEvent` 未找到自动重试消费者，事件本身不是 Outbox 重排入口。没有发现仅因任意外部自定义 Adapter/SPI 可以伪造结果而需要新全局平台的证据。
