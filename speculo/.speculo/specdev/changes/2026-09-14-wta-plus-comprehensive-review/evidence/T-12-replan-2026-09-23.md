# T-12 evidence

HEAD `89f94a9b` closes AC-012. The session and navigation behavior was already in the built apps. The missing piece was a harness that serves those apps over HTTPS and opens the real message stream. Default production leaves that stream off, so the admin bundle for this run is built with `VITE_APP_MESSAGE_ENABLED=true` into a temporary directory. Tracked `.env` files were not changed. AC-012 stays in the T-30 combined run.

## Command

```bash
cd backend && ./mvnw -pl wta-admin -am test \
  -Dtest=SessionNavigationLifecycleIntegrationTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Dsession.lifecycle.integration=true \
  -Dnamewta.repo.root=/srv/WTA-plus
```

Exit 0. `SessionNavigationLifecycleIntegrationTest`: tests=1, failures=0, errors=0, skipped=0. Playwright: 20 passed, 0 skipped, 59.0s.

The cases cover admin and home: empty roles recover once, logout under offline, timeout, unauthorized, and HTTP 401 clears the token and dynamic routes, concurrent 401 has one recovery that can be cancelled and then finished, an initial 401 does not open a second dialog, logout cancels a pending identity or menu request, the two Client origins keep separate identity, and the admin logout closes a real HTTPS EventSource after the remote logout fails.

## Skill

engineering-standards `dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9`, verify phase. The harness is outside the original ticket write set and is recorded on the ticket. No production source edit.
