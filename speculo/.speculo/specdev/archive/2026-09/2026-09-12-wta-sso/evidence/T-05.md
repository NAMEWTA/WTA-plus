# Evidence T-05 — FirstPartySsoProvider slot, context, authMode

- Goal: AC-001 (slot), AC-014, AC-015, AC-023. First-party SSO is first social-row **circle + svg W**, `data-testid="sso-first-provider"`; `GET /auth/client/context` exposes `ssoEnabled` / `ssoAuthorizeUrl` / `authMode`; both keeps local login.
- Date: 2026-09-13

## Commands (run twice)

```bash
export JAVA_HOME=/home/box/deps/jdk/jdk-21.0.12.1+1
cd /workspace/vp-dev/WTA-plus/backend
./mvnw -pl wta-admin -am test -Dtest=AuthClientContextSsoUnitTest -Dsurefire.failIfNoSpecifiedTests=false
cd ../frontend
pnpm --filter @namewta/web-domain-admin test
```

| Pass | Exit | Results | Log |
|---|---|---|---|
| 1 BE | 0 | Tests run: 2 | `/tmp/grok-goal-0577f1e6ed19/implementer/sso-r4-t05-unit-1.log` |
| 2 BE | 0 | Tests run: 2 | `/tmp/grok-goal-0577f1e6ed19/implementer/sso-r4-t05-unit-2.log` |
| 1 FE | 0 | web-domain-admin 6 passed | `/tmp/grok-goal-0577f1e6ed19/implementer/sso-r4-t05-fe-1.log` |
| 2 FE | 0 | web-domain-admin 6 passed | `/tmp/grok-goal-0577f1e6ed19/implementer/sso-r4-t05-fe-2.log` |

## Shipped code driven

- `AuthController.clientContext` on enabled admin client → `ssoEnabled=true`, `authMode=both`, `ssoAuthorizeUrl=http://127.0.0.1:4176/authorize`.
- `LoginPage.vue` (web-domain-admin): first social-row control is circle svg (W path `M1.5 4h4.2`), not a primary text “WTA SSO” button.
- `createIdentityLoginState` maps context authMode/ssoAuthorizeUrl; local form hidden only when `authMode === 'sso'`.

## Residual

Icon visibility on live 4174 is T-07 AC-001. No push / no CR.
