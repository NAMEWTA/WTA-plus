# T-04 evidence

Current HEAD `e134d29bbc573653d430077e88bb0b7f9d29908a` already meets AC-004. No new implementation was required. This cancels only the repeated construction for this round. AC-004 stays owned by T-30. Production CIDR was not treated as a reproduced privilege bypass.

## Commands

```bash
cd backend && ./mvnw -pl wta-admin,wta-common/wta-common-core,wta-common/wta-common-web -am test \
  -Dtest=TrustedClientAddressIntegrationTest,ServletUtilsClientAddressTest,ClientAddressFilterTest,ClientAddressConfigTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Dclient.address.nginx.integration=true \
  -Dnamewta.repo.root=/srv/WTA-plus
```

Exit 0.

| Class | tests | failures | errors | skipped |
|---|---:|---:|---:|---:|
| ServletUtilsClientAddressTest | 1 | 0 | 0 | 0 |
| ClientAddressFilterTest | 8 | 0 | 0 | 0 |
| ClientAddressConfigTest | 2 | 0 | 0 | 0 |
| TrustedClientAddressIntegrationTest | 1 | 0 | 0 | 0 |

The integration test ran the shipped `ClientAddressFilter`, `SecurityConfig.validateClientAccessRules`, `RateLimiterAspect`, and `LogAspect` behind real Nginx `1.31.1` (pinned digest `608a100c71651bf5b773c89083b4a1ad7ef4b2bd05d7a7e552271e03123692ad`) and Jetty. It covered direct IPv4 and IPv6, one proxy and two proxies, forged and malformed `X-Forwarded-For`, whitelist allow/deny, the production rate-limit key, and `OperLogEvent`. The malformed trusted chain returned HTTP 400 before an audit event. `nginx -t` passed for the TLS template. Default trusted proxies stay empty in `ClientAddressConfigTest`.
