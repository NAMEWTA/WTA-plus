# T45 two-MinIO-JUnit failure — read-only policy-fact diagnosis

Reviewed fixed product `be35584b0d8dcfbb560dc9f338c303dafc77fbfd`, the retained `/tmp/wta-t45/oss-two-runs/0627f8329450e8f7/result.json` and the source of `OssStorageReadinessMinioIntegrationTest`. The run is **not accepted**: Maven exit 1; AccessUrl consumer passed, Readiness consumer has one failure, zero errors/skips, at `OssStorageReadinessMinioIntegrationTest.java:70–71` (`POLICY_READ` expected ALLOWED, actual UNKNOWN). Source before/after is the same clean SHA and owned process/container/volume/port cleanup has no errors. I did not run a build/service or edit the repository.

The test's configured public policy (`:148–152`) contains one Allow, wildcard AWS principal, `s3:GetObject`, and the owned bucket's `/*` resource. Those shapes should be understood by `AbstractOssClientImpl.policyFacts:1156–1245` **if that exact semantic structure is what the diagnostic client actually receives**. The test writes policy, reads it through the bootstrap client into `policyBefore` (`:55–56`), then runs `publicClient.diagnoseAccess` (`:58–64`). The failing assertion occurs after anonymous HEAD and GET have both asserted ALLOWED (`:65–69`), so object reachability is established for that test; it does not establish a policy declaration. The failed test never reaches the later policy-equality/no-mutation assertion at `:90–91`. The retained XML gives only UNKNOWN, not the `POLICY_READ` fact's basis or the MinIO-returned policy structure. In particular, **an empty `Condition` has not been observed** and must not be reported as the cause.

Minimal next diagnosis on a fresh owned MinIO fixture: immediately after `diagnoseAccess`, capture only the `POLICY_READ` fact's fixed `basis`, `scope`, `source`, and observation before the existing assertion. This distinguishes `COMPLEX_POLICY`/`INVALID_POLICY` from `POLICY_UNREADABLE`/`HTTP_DENIED`/timeout or another read failure without emitting provider text. In the same isolated test process, inspect `policyBefore` with the existing JSON mapper **in memory** and retain a bounded structural summary only: parse-success boolean; Statement count; whether `Condition`/`NotAction`/`NotPrincipal`/`NotResource` keys are present; whether principal is a recognized wildcard form; whether action is recognized `GetObject`; whether resource matches the owned bucket wildcard or exact canary; and whether the read policy is semantically equivalent to the submitted fixture. Record no raw policy JSON, ARNs, bucket/key, error message, credentials, full URL or HTTP signature. A simple `policyBefore.equals(publicPolicy)` boolean may supplement this but cannot by itself identify a semantic difference caused by formatting/reordering.

If basis says a provider read failed, record only its bounded HTTP status/error category from the owned operation and check the same client/credential route; bootstrap `GetBucketPolicy` success is not proof the diagnostic client's read succeeded. If basis says `COMPLEX_POLICY`, use the safe shape flags to find the exact unsupported construct before deciding whether the fixture or finite parser needs correction. Do not erase the POLICY_READ expectation, infer anonymous write denial from the public HEAD/GET, or change the bounded interpreter merely because MinIO serialized a different shape. The existing owned-bucket anonymous PUT 403 checks at test `:88–89` are later test negative controls, not calls made by the product diagnostic; they also did not execute in this failed attempt. Preserve this run's failure and cleanup evidence verbatim.
