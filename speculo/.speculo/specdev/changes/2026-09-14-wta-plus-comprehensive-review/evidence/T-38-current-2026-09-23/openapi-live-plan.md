# T-38 full-backend live OpenAPI capture plan (read-only preparation)

Prepared 2026-09-23 from current files and Git history. Current writer owns T-38 product files; this note created no repo edits, build, container, JVM, HTTP request, or vendor call. While inspected, HEAD had already advanced to `9f8fa2b1e36c851be8cee6763a84ebf6e2072928` and writer's T-38 paths were dirty; the existing `backend/wta-admin/target/wta-admin.jar` predates this implementation and must not be presented as its source.

## Which prior evidence is reusable

- `frontend/tooling/openapi/README.md:5-13` requires a complete real full-backend `/v3/api-docs`, `fetch → generate → check`, with the supplied `backend-commit` independently bound to the built JAR. `src/index.mjs:234-249` validates/persists the raw snapshot and provenance; it **does not verify** that the named commit built the running JAR. The active revision `b36ef1c...` has backendCommit `2ca0371...` and 433 paths/444 schemas (`frontend/packages/api-contracts/README.md:15`), so T-38's `RetryReceipt` shape cannot be patched into that old JSON.
- The actual live precedent is Git commit `9c0e11b`, file `speculo/.speculo/specdev/changes/2026-09-19-remote-issues-phone-ai/evidence/run-t03-owned-runtime.py` at that commit, with `T-03-runtime-source.json`: full JAR, owned MySQL/Redis/MinIO, prod profile, then GET `/v3/api-docs`. That script also did a historical AI preservation migration which T-38 does **not** need. Its Docker `-e` secret argv, single-label cleanup and process handling predate current safety practice: use the resource/cleanup pattern from `/tmp/wta-t38/run-notify-manual-retry-integration.py` instead. Earlier `prepare-t26-openapi.py` and T-31 ModelConverters evidence explicitly compose a baseline or only a changed model; they are useful history, not an acceptable T-38 live source.
- `frontend/packages/api-contracts/openapi/current.json` points to a revision with raw full source. `frontend/tooling/openapi/src/cli.mjs` supports `fetch --source <URL-or-file> --backend-commit <40hex> --backend-repository NAMEWTA/WTA-plus --runtime-endpoint /v3/api-docs`, then `generate` and `check`. The product writer owns these mutations; Lead should only provide a verified raw capture and source/JAR manifest.

## Minimum real startup dependencies from current source

Use the **default full** admin bundle, not `-Pbundle-core`: `backend/wta-admin/pom.xml:147-205` includes Job/AI/Demo/Workflow/Profile in `bundle-full`, and `scripts/ci/verify-admin-bundle.sh full` checks the embedded module jars. `backend/README.md:89-100` requires `clean` before each bundle; independent tests precede a package with skipped tests. After immutable T-38 candidate and clean source, Lead records the exact package command, exit, source HEAD/tree, start/end time, `wta-admin.jar` SHA-256/size, full bundle verification and unchanged SHA after capture. A preexisting target JAR is not source proof.

1. Fresh, dual-labelled owned MySQL 8.4.9 and Redis 8.6.3 on random **127.0.0.1** ports. Import six canonical SQL files into the owned container's `wta-plus` database via `release-artifacts/scripts/init-mysql-container.sh --container <owned-full-id> --env-file <0600-owned-file> --sql-dir <repo-sql-dir>`; its contract is `wta-plus` only, 103 business tables plus Nacos schema, app-scoped account, exactly one PRIVATE default `minio` row (`init-mysql-container.sh:99-205`). Do not try T-38 test runner's differently named `namewta_notify_test_*` DB for the full app. No existing DB/account or `/srv/ops` service is in scope.
2. Fresh owned MinIO on random loopback port. The initializer replaces the two OSS rows with the generated MinIO identity/endpoint. This endpoint must point to that owned port: `SystemApplicationRunner.java:28-31` reads/caches OSS rows and refreshes readiness; `OssUploadDiagnostics.java:33-60` probes default-bucket CORS/lifecycle after startup. MinIO is not needed to describe `RetryReceipt`, but without the owned endpoint startup may contact the seed or environment endpoint. An owned bucket is optional for schema capture; if it is absent, a bounded diagnostic warning is not evidence that the API schema is incomplete. Never point it to production to obtain a green readiness status.
3. Prod profile plus a **0600** additional config file, with all external addresses/credentials explicitly overridden. Set `server.address=127.0.0.1`, `server.port=0`, datasource master URL/user/password to owned MySQL, Redis host/port/password to owned Redis (empty password if locally unauthenticated), `spring.servlet.multipart.location` and `logging.file.path` under private run dir; disable `spring.boot.admin.client.enabled`, `snail-job.enabled`, `nacos.config.enabled`, `spring.ai.mcp.client.enabled`, `easy-es.enable`; set `springdoc.api-docs.enabled=true`, `openapi.enabled=true`, and random valid `openapi.kek`/`kek-version` because `OpenApiStartupValidator.java:27-57` validates them. `application.yml:246` has build-time active-profile placeholder; **force** `prod` at launch and never accidentally load `application-local.yml` or `dev`. `application-prod.yml` has external datasource/Redis placeholders and `/wta/server/temp`, so relying on defaults is unsafe.
4. Before starting the JVM, assert the owned fresh DB has no claimable `notify_outbox`, no SMS ACCEPTED receipt candidates and no enabled production-style channel account; otherwise scheduled `NotifyOutboxWorker` / `SmsReceiptQueryWorker` could perform provider I/O. `NotifyConfigService.java:46-57` reads enabled SMS accounts at startup. The six-file seed should be inspected via SQL **counts/status only**; if any active external account/task appears, stop instead of invoking suppliers. No supplier network is needed to generate docs.

## Candidate capture sequence for Lead and writer

1. Lead freezes clean full implementation SHA and tree; runs independent tests, then `cd backend && ./mvnw -B -ntp clean package -DskipTests`, `bash scripts/ci/verify-admin-bundle.sh full`; records JAR hash and embedded modules. Configure only new owned resources as above. Docker passwords enter `--env-file`/SQL stdin, not `docker -e`, `redis-server --requirepass <literal>`, shell trace, JVM argv, logs, or the report. The overlay path may appear in Java argv; values stay in 0600 file. Use a restrictive child environment, not inherited production env.
2. Start the exact JAR from private cwd in its own session/process group with `--spring.profiles.active=prod --spring.config.additional-location=file:<owned-config>` (arguments contain no secrets). On `server.port=0`, derive the actual loopback port from the application's bounded runtime-info line (`NamewtaApplication.java:72-91`), then require `SystemApplicationRunner` completion and HTTP 200 for GET `/v3/api-docs`. Do not use actuator or an arbitrary 200 before full startup. Save the unmodified response bytes to a 0600 `/tmp/wta-t38/openapi-live/<run-id>/source.json`; record status, content type, byte length and SHA-256, not the full body in public logs.
3. Parse the captured JSON and require OpenAPI 3.0/3.1, nonempty full `paths` and `components.schemas`, the current notification retry/cancel POST paths and `RetryReceipt.properties.queuedCount` as the expected integer. Compare path/schema totals and key non-notify paths with current full snapshot to detect a partial/core/misconfigured capture; any unexplained loss blocks fetch. Record the exact isolated startup flags and any optional-condition exclusions. Do not require exactly 433/444 when current candidate legitimately changes schemas.
4. Stop the JAR after direct HTTP capture, verify its process group is empty and JAR/source hashes unchanged. Product writer runs `pnpm --dir frontend --filter @namewta/tooling-openapi openapi:fetch -- --source <exact-private-captured-source.json> --backend-commit <same-build-SHA> --backend-repository NAMEWTA/WTA-plus --runtime-endpoint /v3/api-docs`, then `openapi:generate` and `openapi:check` per README. Lead verifies new revision `rawSha256` equals the captured bytes and reviews **complete** snapshot diff plus `generated/openapi.ts` field; writer owns generated files/README updates. A file source is valid only with the Lead's direct live HTTP/JAR manifest; a synthetic file is not.
5. Finally Lead removes only dual-labelled full-ID owned containers with `rm -fv`, confirms pre-captured anonymous volumes absent by exact names, loopback ports closed, JVM process group gone, private env/config removed, and source/artifact binding still holds. Save a compact result JSON with failures and cleanup verdict even when startup or fetch fails. This must reuse T-37-v2/T-38 runner hygiene; old T-03 script is a behavior reference only.

## Open questions before first real run

No full-JAR candidate exists yet for T-38; the writer is still changing the product tree. The exact count/shape of enabled seed accounts and current optional full-bundle controllers must be verified against the fresh owned DB and built JAR at execution time. A startup failure caused by one of those facts is a test-environment finding, not permission to use `/srv/ops` or to splice the previous revision. This note is a preparation design, not proof that the live export, fetch, generation or cleanup has run.
