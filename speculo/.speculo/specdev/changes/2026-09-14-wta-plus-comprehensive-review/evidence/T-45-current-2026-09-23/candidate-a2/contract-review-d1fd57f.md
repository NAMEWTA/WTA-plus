# T45 01D URL correction — fixed-source read-only incremental review

**Source:** base `081c75ff92b147bd338ca98a041182973596bca2` → candidate `d1fd57fb6a2a7c1806399e2728f066c842f3cdb4`. The backend product delta is confined to `backend/wta-common/wta-common-oss/src/main/java/org/namewta/common/oss/client/AbstractOssClientImpl.java` and `backend/wta-admin/src/test/java/org/namewta/test/oss/readiness/OssAccessDiagnosticUnitTest.java`. The worktree was clean when reviewed. This is a static incremental review; I did not run Maven, a service, or a test.

**Verdict: no static blocker in this delta.** At `AbstractOssClientImpl.java:1285–1298`, the anonymous diagnostic chooses `getDomainUrl()` for a configured, nonblank bucket-bound domain and otherwise retains `getBucketUrl(bucket)`. This matches `DefaultOssObjectStore.publicUrl:34–44` on base selection and object-key encoding and directly closes A1's `/bucket/bucket/<key>` path. It does not alter `OssClientConfig.getBucketUrl`, signed/S3 operations, upload URL construction, HEAD/GET classification, or redirect policy. `diagnoseAccess:1018` obtains the configured bucket, so the domain is applied to the same default-bucket object whose public URL is being diagnosed.

The new base URI check (`AbstractOssClientImpl.java:1288–1292`) rejects missing host, userinfo, query, and fragment before any anonymous network request. Existing failure handling at `:1316–1326` returns two UNKNOWN facts instead of treating an invalid URL as a safe DENIED observation. The existing `HttpClient.Redirect.NEVER` and per-request timeout remain in place. The loop trims trailing slashes before appending one slash and `SdkHttpUtils.urlEncodeIgnoreSlashes(key)`, so the object key is encoded at the same point as the production public URL.

The test at `OssAccessDiagnosticUnitTest.java:73–87` uses a loopback server that accepts only `/assets/documents/a%20b%2B%23.txt`. It asserts exactly one HEAD and one GET to that raw path, which would reject the former double-bucket path, a repeated slash, and unencoded space/`+`/`#`. The negative test at `:91–105` checks userinfo/query/fragment domains yield UNKNOWN and issue zero requests. Existing no-domain tests (`:53–69`, `:536–542`) still exercise the path-style `/bucket/<key>` route and check anonymous read results. These assertions are substantive regressions for the A1 cause; they do not substitute for the owned MinIO/HTTP retry.

**Evidence limit:** A1 remains a retained failed real run (`/tmp/wta-t45/runs/e9853018a23b0d59/result.json`, accepted=false, cleanup errors empty). Lead's green03 and subsequent A2 real rerun were pending when this report was written. No test result or runtime acceptance is attributed to `d1fd57f` here. The negative-domain test covers the explicit URI components above, not every malformed URL; the production catch still fails those remaining malformed constructions to UNKNOWN.
