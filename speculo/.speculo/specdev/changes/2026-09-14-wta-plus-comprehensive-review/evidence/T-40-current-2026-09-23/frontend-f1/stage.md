# T40 frontend F1/F2 segmented feedback — not ticket acceptance

F1 source eb8cefe0f67895e75832b2a15d8f955b7b01e447 / tree33ef52d0f1250c6221cb4ad3f586563d84332c67, clean before all checks and real18 before/after. First frontend tool invocation exited127 because Lead replaced PATH and lost corepack; no tests started. Inherited PATH plus pinned pnpm10 shim re-run actually executed domain7pass, webdomain25pass, Admin79pass/1fail (lazy route subscription caught exception and emitted inactive). Three selected packages typecheck exit0. Backend real18 all pass/0skip, run c7fac20c13a862b2, cleanup[]; the new fresh MAIL retraction branch has no Provider/Attempt and cancelled delivery, done outbox.

F2 9bd119cf moves the snapshot assertion before subscription. Single Admin host file5pass/1fail: snapshot passes but subscription still falls into catch, narrowing the cause to watcher initialization/callback rather than import resolution. Source change is test-only. This is staged diagnosis, not a complete-candidate attempt. Original failures preserved; browser runner and two Skill references still pending.

F3 explicitly dirty diagnostic (temporary catch error output, not committed product or an acceptance gate) retains source hash and actual single-file5pass/1fail. Cause revealed: `ClientContext.clientId is required` from real App HTTP/push initialization during lazy subscription. F2 snapshot itself passed. Mock/dynamic-import isolation remains under investigation; do not label the product fixed or change expectations to inactive. Temporary console must be removed before next candidate.
