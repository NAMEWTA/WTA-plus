# T44 owned MinIO v4 static review

Fixed candidate: `/tmp/wta-t44/run-minio-real-v4.py`, SHA-256 `9da4734a920245fe2824a8c98f281bdbe77ea84e18a0349decfb8ad9953f87aa`. Read-only review; v4 has not been run by this reviewer. The v1/v2 failed runs remain failures and are not retrospectively accepted.

**No further deterministic SQL/download blocker found in the reviewed slice.** The two previously failing `h('')` uses are gone: `config_a` and `config_b` write SQL literal `''` for `domain_url` (lines 573, 586). All remaining `h(...)` arguments at lines 571-620, 740-754 are nonempty generated values or fixed strings. `base.hexsql` encodes empty text as invalid `CONVERT(0x USING utf8mb4)`, so these two changes address the identified syntax defect without changing the helper.

The config UPDATE/INSERT lists match the current `sys_oss_config` DDL (19 listed columns; lines 330-348 of `10-cde-base-ddl.sql`). The seeded `minio` and `image` rows exist in `50-cde-base-dml.sql` lines 347 and 351; generated `config_key` lengths are 13, under varchar(20); generated IDs are below signed BIGINT max. The public metadata INSERT and negative clone lists match `sys_oss` DDL columns (lines 305-319). `image` has a seeded `prefix='image'`, but the explicit public `domain_url` includes `/<bucket_p>` and `DefaultOssObjectStore.publicUrl` appends the stored `file_name`; the fixture object is placed at that bucket/key. Prefix does not add a second path segment in this download path.

`create_by=1`/`update_by=1` in the synthetic public `sys_oss` row (lines 602-605) do not affect the tested management download. Those columns are nullable and not foreign keys in DDL. `SysOssController.downloadUrl` checks `system:oss:download` and calls `resolveAccessUrl`; `OssLifecycleManager.requireDownloadable` loads by `oss_id` and checks state/service, with no owner predicate. The runner downloads with the permissioned control user and separately verifies the ordinary user is denied. This supports the management permission test; it does **not** prove object-owner scoping, which is outside this test contract.

The v4 `OwnedSqlStep` emits only fixed stage and numeric MySQL errno/SQLSTATE or row count; it does not publish SQL text, raw stderr, credentials or object paths. If v4 fails, its sanitized `failure.sql` should identify the actual SQL stage. Acceptance still depends on Lead's owned live run, zero skips, clean source/JAR and cleanup checks.
