# Evidence: T-01 — 恢复可信的仓库门禁与治理入口

- Change：2026-09-14-wta-plus-comprehensive-review
- Lead / Workspace：single-agent / `/srv/WTA-plus` / main；零子代理、零新worktree，命令逐条串行。
- Parent before：`76dbbe84a34624234e379661a57b232529e34ed3`，实施前工作树干净。
- Implementation commit / result SHA：null / null，尚未提交；不标Done。
- 状态：review，本地实现检查通过；提交授权、direct-parent出口和全局G-plan未完成。

## 1. 实现摘要

干净源码目录不再依赖temp/release或编辑器配置。私密输出仍必须被Git忽略、不能被强制跟踪，且部署工具负责按需创建父目录。删除无submodule仍必失败的遗留入口。分层检查先排除Java注释/字面量，再检查org.namewta依赖和真实事务注解；没有新增allowlist。第三方org.dromara供应商坐标保留。

恢复仓内CI候选并串行连接四个job，分别登记架构/OpenAPI/typecheck/测试/构建/真实服务入口。发布测试限定产品owner，保留Speculo供应商Skill；发布测试runner改为单并发。工程事实更正三个App、50个POM、Third五层登记，以及不再断言本机缺Docker。

## 2. Lead Dispatch And Candidate Return

由Lead本人实施、复核，无dispatch、source/candidate或外部回报。最新用户Goal授权全部实现和本地验证；不可逆出口仍按AGENTS与Goal提交规则处理。

## 3. 修改范围与路径所有权

精确写集和文件SHA-256见`T-01-checkpoint.json`。新增工程事实三份references和发布测试入口已先登记Ticket/Map revision 4。没有修改业务Java/Vue、SQL、依赖锁或供应商Skill；没有修改永久ADR/context。

## 4. 验收与合同映射

| 验收项 | 当前证据 | 结果 |
|---|---|---|
| AC-001 干净clone不要求temp/release | facts checker真实运行 + fresh repository夹具；夹具运行前后temp均不存在 | pass |
| AC-001 注释不误报、非法依赖必失败 | 31个检查器正负用例，覆盖静态import、全限定事务、字面量、真实链缺失、第三方坐标 | pass |
| AC-001 CI事实准确 | 仓内YAML存在且本地解析；远程Actions/required checks没有通过证据，明确not-run/unknown | pass（候选合同）；远程未验证 |
| AC-001 cwd/命令可解析、测试与打包分离 | check-t01-ci.py解析21个shell步骤、11个SpecDev入口、现有package scripts和Wrapper；full校验先于core clean | pass（静态入口）；业务命令未执行 |
| Skill实际应用 | 本Evidence尾部与实际diff/命令JSON | pass |
| 正常/失败/回归和E2E | 31/31检查器、43/43发布合同、构建锁/JAR故障夹具；E2E按本票not-required | pass，真实产品分层失败见下 |
| 写集/owner | T-01-checkpoint.json对照Ticket；单writer | pass |
| 非空implementation commit/direct-parent/result SHA | 无提交授权/提交，结果均null | pending |
| Ticket/Map/Goal/Evidence一致，无未决偏差 | 已同步执行授权与门禁准备；G-plan两票未知和P-validator缺陷保持未关闭 | pending（整体Gate未关闭） |

## 5. Workspace Verification

全部命令、cwd、原始stdout/stderr、退出码见`T-01-verification.json`；修复前9条基线见`T-01-baseline.json`。环境Node v24.21.0；PyYAML 6.0.3；当前JDK提供jar；Docker Compose实际解析通过。

| 命令/范围 | exit | 数量/结果 |
|---|---|---|
| validate-skill-facts.mjs | 0 | 不创建temp/release |
| node --test --test-concurrency=1 两个checker test文件 | 0 | 31 passed，0 failed/skipped |
| validate-skill.mjs / docs/fm/scripts/validate.mjs | 0 / 0 | 原有Skill与模板合同 |
| bash scripts/ci/verify-dev-build-guard.sh | 0 | owner冲突/过期回收/信号清理/缺失或残缺JAR/class哨兵拒绝 |
| bash release-artifacts/scripts/verify-release.sh | 0 | 43 passed，0 failed/skipped；Compose解析通过 |
| python3 evidence/check-t01-ci.py | 0 | 4个串行job、21个shell步骤、11个配置入口 |
| person / enterprise / third layered | 0 / 0 / 0 | 168 / 130 / 71 Java files |
| notify layered | 1 | NotifyOutboxWakePublisher位置错误，T-28负责；原Javadoc事务误报消失 |
| sso layered | 1 | SsoSessionCookie依赖ResponseCookie、SsoTokenExtras依赖LoginHelper；T-06负责 |
| SpecDev --stage tickets | 0 | 117单writer共享写集warnings，0 error |
| SpecDev --stage goal-plan | 1 | 原工具错误要求单change创建两份parent工件，保留2个error，未修改校验器或造假工件 |
| git diff --check | 0 | 无空白问题 |

未运行Maven业务测试/打包、pnpm业务门禁、浏览器和真实MySQL/Redis/OSS；T-01只修改检查/治理入口，不将入口解析等同于这些业务测试通过。远程Actions与分支保护未验证。

## 6. 双轴审查

标准轴：对照基线到T-01-checkpoint的实际实现。确认没有放宽业务规则；删除的submodule脚本本身没有产品数据；所有临时夹具清理自己资源。通过。

规范轴：对照AC-001全部行为、写集与Skill。真实SSO/Notify失败按原计划保留，不把全局绿色作为检查器修复的伪证。提交和G-plan仍未满足，不作Done结论。

## 7. Integration Verification

| 项目 | 当前值 |
|---|---|
| Parent before | 76dbbe84a34624234e379661a57b232529e34ed3 |
| Implementation/source / result SHA | null / null |
| Candidate branch/workspace | not-applicable，使用current |
| Method / conflicts | direct-parent待批准；无冲突 |
| Integration checks | current-workspace验证见上，提交后的re-read待执行 |
| E2E disposition/result | not-required：静态/脚本合同由正负夹具覆盖，无在线业务变更 |

失败历史：首次修复Java词法正则时发生生成转义导致的语法错误，已修复并通过node检查和31项回归；发布合同原2失败已修复。SSO/Notify是其他票产品缺陷，P-validator是既有工具路由错误，均非盲目重试或豁免。

## 8. 偏差与决策

最新用户Goal覆盖旧plan-only范围；先完成T-01可信检查准备，不假称G-plan关闭。为事实同步扩充三份工程references写集；为串行运行扩充verify-release.sh。删除的legacy入口仍保留在Ticket作为删除目标，不作为可运行命令。

## 9. 残余风险与恢复

没有远程CI成功证据。T-06需处理两处SSO越界，T-28处理wake归属；P-validator缺陷、T-03预算、T-23事件身份仍需后续处理。恢复为仅撤回T-01列出的本地diff；没有运行数据动作。

## Skill Execution Records

```json
[
  {
    "id": "engineering-standards",
    "phase": "implement",
    "operation": "apply-scope-contract",
    "sha256": "dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9",
    "status": "passed",
    "evidence": [
      "T-01-checkpoint.json：按本票写集修复检查器/CI/事实引用，未扩大产品分层例外",
      "T-01-verification.json：实际运行定向回归并记录真实失败owner"
    ]
  },
  {
    "id": "engineering-standards",
    "phase": "verify",
    "operation": "verify-affected-contract-and-quality-gates",
    "sha256": "dbc475149e3588cb840c15e0bb287920b876c0eef4c4004dde078de1c4fca6d9",
    "status": "passed",
    "evidence": [
      "T-01-checkpoint.json：按本票写集修复检查器/CI/事实引用，未扩大产品分层例外",
      "T-01-verification.json：实际运行定向回归并记录真实失败owner"
    ]
  }
]
```

## 用户交付与源回读

完整目标仍是31票。T-01当前只是首个可审查实现，未提交、未Done；剩余30票及整体候选验证保持原范围。

## 2026-09-18 补充检查点（取代上文当前阻塞描述）

用户明确全 change 暂不提交、继续可逆工作，commit/result保持null，T-01仍review。ARCH-001按当前普通Git文件模式修正monorepo事实。SpecDev现在按真实工件识别single/parent：single必须有Goal，partial parent仍拒绝，原成员/循环/agent cap检查不变。11项新回归及原31项共42/42通过，0 skipped；真实goal-plan退出0、0 error/117串行共享路径warning；facts、CI入口检查、diff检查退出0。证据见T-01-supplement-verification.json，当前18条路径见T-01-checkpoint-v2.json，原checkpoint保留历史。既有43/43发布证据保留，发布代码自该次验证未再变化。P-validator阻塞已解除，G-plan仅保留T-03/T-23未知。

两次新增fixture运行先后受cwd发现真实workspace、显式repo必须为Git仓库影响；修正测试隔离cwd后11/11通过。未创建Git worktree、修改业务检查或绕过权限。此补充同样应用engineering-standards（原sha不变）的架构事实、写集和定向验证合同；实施/verify记录passed。

## 2026-09-18 Notify 门禁补充

旧门禁依赖不存在的历史commit和backend独立Git根，现改为当前编译产物的长期架构合同与真实同步Dispatcher行为。3项覆盖实际worker/wake的配置控制面/渠道直投依赖、违法注入/调用/Class字面量负例、合法Port和同步线程/事件顺序。保持common Notify同步；未修改Notify生产代码，T-28真实分层缺陷保留。

`T-01-notify-gate.json`记录19模块103项通过（exit 0）；追加Class字面量检查后的最终源码已在`T-02-reactor-2.json`重跑，Notify自身57项全部通过。完整选集651项、625通过、1错误、25跳过，错误为旧sys_notify_log.attachment_oss_ids OSS载体未登记，归T-22 schema收缩，不能报告完整Maven通过。

最新源码checkpoint为`T-01-checkpoint-v3.json`，19条声明路径（包括未修改release合同文件和已删除脚本）；commit/result均null。

补充Skill Execution Record：wta-module-guide，SHA256 `441de2ccc513e09820ed3d7d2faf559eeaa7466202e4fbb0c8dd3eabf09510c9`，apply Notify index并回查归档AC-006、ADR-0006/0007/0044与当前控制面；产出当前长期门禁及原一次性diff断言的退役理由。验证见上述实际Maven记录，状态passed（此门禁局部合同）。

## Revision134 当前结果（历史结果保留）

原SSO/Notify分层、P-validator和T-03/T-23未知均已由责任票闭合并在T-30复验。远程CI/required checks继续明确未运行，仓内CI只作候选；不能把候选配置等同远程启用。 当前验收以T-30-final-quality.json, T-30-v2-release.json, T-30-final-governance.json及T-30-completion-audit-revision134.json为准；本票仍为review，commit/result均null。
